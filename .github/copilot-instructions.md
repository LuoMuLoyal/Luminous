# Luminous Copilot Instructions

Follow `AGENTS.md` at the repository root — it is the single source of all project
rules (stack, commands, architecture, documentation, boundaries). Read `README.md`
and `docs/README.md` next. Copilot has no file-include mechanism, so this file
deliberately duplicates nothing.
