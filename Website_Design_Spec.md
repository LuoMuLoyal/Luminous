# Luminous 官方网站设计规范

> 版本：v2.0
> 日期：2026-07-05
> 状态：review 后优化版

---

## 0. 设计判断

Reading this as: 面向早期用户、竞赛评委与潜在合作方的产品官网，风格应是 trust-first 的轻医疗科技语言，视觉上偏克制、清爽、截图驱动，而不是泛 SaaS 营销页。

建议设计参数：

- `DESIGN_VARIANCE: 5`
- `MOTION_INTENSITY: 3`
- `VISUAL_DENSITY: 4`

这意味着：

- 页面需要有明确设计感，但不能做成 Awwwards 式实验站。
- 动效只服务于理解层级、切换截图和状态反馈，不做炫技。
- 内容密度中等，强调可信、清晰、可读。

---

## 1. 对抗性 Review 结论

原 v1.0 规范可以保留的部分：

- 产品定位、目标用户、不做范围基本正确。
- 五个核心模块与当前 app 信息架构一致。
- Nuxt 4 + Nuxt UI v3 + Nuxt Content v3 的技术方向合理。
- 安全边界和文案约束意识是对的。

原 v1.0 的主要问题：

### 1.1 站点目标过大

原方案把官网同时定义成：

- 产品宣传站
- 对外用户文档站
- 开发者入口

这会带来三个问题：

- 首页叙事被文档结构拖散，无法聚焦转化。
- `/dev` 与现有 `Lumos-docs` 职责重叠，容易形成双份维护。
- 首版官网会变成“能放很多内容”，而不是“最有效地说明 Luminous 是什么”。

### 1.2 首页结构过于平均

原首页是典型的功能卡片平铺：

- Hero
- 五模块
- 三场景
- 五步流程
- 安全边界

这类结构最大的问题不是丑，而是没有记忆点。用户看完会知道“你有很多模块”，但不一定记住“为什么 Luminous 值得信任”。

### 1.3 主标题太泛

`你的日常健康伙伴` 过于宽泛。

问题在于：

- 几乎任何健康 app 都能说这句话。
- 它没有体现 Luminous 最强的切入点：用药安全 + 主动提醒 + 可解释边界。
- 对评委和合作方来说，辨识度不足。

### 1.4 视觉系统过散

原规范在页面 chrome 层面同时引入 teal、blue、green、indigo、pink。

这会削弱两个核心目标：

- 官网的品牌一致性
- 医疗信任感

多色可以出现在产品截图和数据可视化里，但不应该主导页面本身。

### 1.5 CTA 策略不真实

如果产品还没有稳定公开下载链接，首屏主 CTA 直接写 `下载 Luminous` 会显得虚。

更合理的顺序应该是：

- 已上线：`下载 App`
- 内测阶段：`查看产品演示` / `申请内测`
- 比赛展示场景：`查看核心流程`

### 1.6 文档站范围需要收缩

首版官网不应该一开始就承载大而全的 `/dev`。

判断依据：

- 官网首版最重要的是价值表达，不是文档承载量。
- 内部开发文档权威来源已经是 `Lumos-docs`。
- 公开站只需要保留少量“开发者 / 开源入口”，不需要变成第二套文档系统。

---

## 2. 官网目标重定义

## 2.1 官网的唯一主任务

官网首版只做一件事：

> 让第一次接触 Luminous 的人，在 90 秒内理解它是什么、为何可信、现在能做什么，以及下一步该点哪里。

## 2.2 受众优先级

优先级顺序：

1. 竞赛评委 / 路演观众
2. 早期用户
3. 潜在合作方
4. 开发者

由此决定：

- 首页优先讲清价值与可信边界。
- 文档只覆盖用户需要的公开说明。
- 开发者信息以轻量入口形式存在，不抢首页主线。

## 2.3 首版转化目标

首版官网只允许一个主 CTA，按产品阶段切换：

| 产品状态 | 主 CTA | 次 CTA |
|------|------|------|
| 尚未公开上架 | 查看产品演示 | 阅读安全边界 |
| 小范围内测 | 申请内测 | 查看核心流程 |
| 已公开发布 | 下载 App | 阅读使用说明 |

默认假设当前处于“尚未公开上架或内测初期”。

---

## 3. 站点范围与仓库边界

## 3.1 首版站点范围

首版官网建议只包含：

```text
/
├── 首页
├── /download 或 /waitlist
├── /privacy
├── /safety
├── /faq
└── /about-open-source （可选）
```

不建议首版就做：

- 完整 `/docs` 文档中心
- 完整 `/dev` 开发者文档区
- 大量 changelog 页面
- 多层级内容树

## 3.2 与 Lumos-docs 的关系

职责应明确：

- `Luminous-site`：公开官网，强调产品介绍、下载、隐私、安全、FAQ
- `Lumos-docs`：内部开发文档与跨仓库知识聚合

官网可以在 footer 或 `/about-open-source` 放一个开发者入口链接到 `Lumos-docs`，但不应复制那套内容。

## 3.3 技术与仓库决策

本规范按 greenfield 方案设计，不继承任何旧官网的信息架构或视觉包袱。

建议：

- 官网使用独立前端仓库开发
- 与 Flutter app 仓库解耦
- 只复用品牌素材、产品截图和公开内容，不复用旧站页面结构

理由：

- 官网与 app 的构建、部署、素材管理节奏不同
- 重设计应避免被旧页面的结构牵着走
- 可以更干净地验证新的叙事和转化策略

---

## 4. 信息架构

## 4.1 首页叙事结构

首页不要再用“功能清单式平铺”，改成下面这条主线：

```text
Hero
→ 可信定位短证据
→ 一个真实高频场景的展开
→ 五个 tab 如何形成闭环
→ 报告与主动提醒价值
→ 安全边界
→ 最终 CTA
```

这条结构的好处：

- 先讲“为什么存在”
- 再讲“它在具体场景里怎么工作”
- 最后讲“它能做什么，但不会越界做什么”

## 4.2 首页区块顺序

建议顺序：

1. Hero
2. Trust Strip
3. Scenario Narrative
4. Product Loop
5. Tab Showcase
6. Report Preview
7. Safety Boundary
8. FAQ Snapshot
9. Final CTA

不建议把“五大模块展示”放在 Hero 后第一屏。

原因：

- 五个等权模块会让价值感被拆散。
- 用户对 tab 名称没有先验兴趣。
- 先看模块名，不如先看“这个产品如何在真实场景里减少风险和焦虑”。

---

## 5. 首页详细设计

## 5.1 Hero

### 目标

Hero 只回答三件事：

- Luminous 是什么
- 为什么它可信
- 用户下一步能做什么

### 文案方向

主标题不再使用 `你的日常健康伙伴`。

建议主标题候选：

1. `把用药安全和日常健康记录，连成一个主动提醒的闭环`
2. `从用药安全出发，帮你看懂每天的健康变化`
3. `不是再做一个问答框，而是把风险和变化提前告诉你`

推荐首选：

> 把用药安全和日常健康记录，连成一个主动提醒的闭环

副标题建议：

> Luminous 以用药安全为可信入口，连接症状、饮水、饮食、睡眠和用药记录，在合适的时候给出可解释的总结、提醒和低风险下一步建议。

### CTA

默认：

- 主 CTA：`查看产品演示`
- 次 CTA：`阅读安全边界`

如果确认已有正式下载渠道，再替换成：

- 主 CTA：`下载 App`
- 次 CTA：`了解核心流程`

### 视觉

Hero 不用抽象插画，也不要只做三张“浮动卡片假界面”。

改为：

- 以真实 app 截图为核心
- 中间一张 Today 截图为主
- 左右辅以 Medicine 和 Report 截图
- 通过轻微景深、层级和柔和玻璃底板提升完成度

关键要求：

- 所有画面必须来源于真实产品截图或高保真设计稿
- 不使用虚构数据表、假图表、假通知

### 背景

- 使用非常浅的冷白到青灰渐变
- 避免“大面积医疗 teal”
- 允许在背景里放低对比度网格或柔和弧线，但不能喧宾夺主

## 5.2 Trust Strip

Hero 下方增加一条短证据带，不做 logo wall。

内容不是“合作品牌”，而是三条可信陈述：

- `用药安全作为第一入口`
- `AI 输出有边界，不替代诊断`
- `围绕真实记录生成主动提醒与报告`

这是给评委和首次访客的“定位再确认”。

## 5.3 Scenario Narrative

原规范的三张等宽场景卡不够强。

改为：

- 一个主场景展开
- 两个补充场景短卡

推荐主场景：

`新增药物后的主动风险提醒`

因为它最能体现：

- 用药安全切入口
- 主动能力
- 风险解释
- 安全边界

布局建议：

- 左侧：用户情境和痛点演变
- 右侧：Luminous 如何响应的分步画面

补充场景短卡：

- `当天记录后的 AI 总结`
- `疑似漏服后的提醒与确认`

## 5.4 Product Loop

保留“记录 -> 理解 -> 提醒 -> 报告”闭环，但从五步横排流程图改成更具叙事性的带状结构。

建议改为四段式，而不是五段式：

1. 记录
2. 理解
3. 提醒
4. 报告

原因：

- `干预` 这个词在医疗语境里容易过重。
- 对外表达里，用 `建议下一步` 比 `干预` 更稳妥。

每一段都要配一个真实界面局部，而不是只放图标和箭头。

## 5.5 Tab Showcase

五个 tab 仍然值得讲，但位置要后移。

这里的角色不是“功能目录”，而是“证明产品不是单点工具，而是完整工作流”。

建议做法：

- 左侧是 tab 选择器
- 右侧是对应截图大图
- 下方补一句“这个 tab 在闭环里的职责”

必须明确每个 tab 的边界：

| Tab | 对外表达 |
|------|------|
| Today | 今天最该处理的提醒、任务和总结 |
| Record | 记录事实，不替你下结论 |
| Medicine | 处理药物相关安全与提醒 |
| Report | 回顾趋势和可分享摘要 |
| Mine | 管理隐私、档案和通知设置 |

## 5.6 Report Preview

报告区应该单独成章，不要只是第五个模块的附属说明。

原因：

- Web 端最合理、最容易演示的能力之一就是报告预览
- 这也是区别于“普通打卡 app”的一部分

这个区块要强调：

- 日 / 周 / 月总结
- 趋势变化而非医疗判断
- 用户预览后再决定是否分享

推荐 CTA：

- `查看报告预览`

## 5.7 Safety Boundary

安全边界必须保留，而且权重要高。

但不要做成普通的三张卡片墙。

建议布局：

- 左侧大标题 + 一段原则说明
- 右侧三条边界纵向列表

三条边界保留：

1. 不诊断，不替代医生
2. 敏感数据单独授权
3. 高风险场景优先降级到线下专业服务

这一段应该比“漂亮”更像“制度说明”。

## 5.8 FAQ Snapshot

首页底部增加 3 到 5 个折叠 FAQ：

- Luminous 会不会替我判断该吃什么药？
- 它会不会把我的隐私数据自动发给别人？
- 没有持续记录时，AI 还能输出什么？
- Web 端和手机端分别做什么？

这能减少用户离开首页去找答案。

## 5.9 Final CTA

最终 CTA 要和首屏 CTA 保持同一意图。

禁止出现两个不同方向的主要行动，比如：

- 首屏是 `下载`
- 底部变成 `联系我们`

如果当前目标是产品理解与演示，则首尾都围绕演示。

---

## 6. 页面清单

## 6.1 首页 `/`

首版官网主战场，承载完整品牌与产品叙事。

## 6.2 下载或等待列表页

根据发布状态二选一：

- `/download`
- `/waitlist`

若产品未正式发布，不要伪装成可下载。

## 6.3 隐私页 `/privacy`

说明：

- 收集哪些数据
- 哪些数据需要额外授权
- 数据如何用于提醒、总结和报告

## 6.4 安全页 `/safety`

专门讲：

- 不做什么
- 高风险场景如何降级
- AI 输出的边界

## 6.5 FAQ 页 `/faq`

把首页常见问题展开。

## 6.6 开发者入口

不做完整 `/dev` 区。

只保留一种轻量形式：

- footer 中的 `开发者 / 开源` 链接
- 跳转到 `Lumos-docs` 或仓库 README

---

## 7. 视觉系统

## 7.1 视觉方向

关键词：

- 清爽
- 审慎
- 年轻
- 可信
- 截图驱动

避免的方向：

- 过度医疗蓝
- 泛 AI 紫色发光
- 大量玻璃拟态
- 三等宽 SaaS 卡片墙
- 插画代替产品本身

## 7.2 色彩策略

页面 chrome 只保留一套主色：

| Token | 色值 | 用途 |
|------|------|------|
| `--color-accent` | `#13B39D` | 主 CTA、关键链接、焦点态 |
| `--color-accent-hover` | `#0F9E8A` | CTA hover |
| `--color-accent-soft` | `#E8F7F4` | 轻强调背景 |
| `--color-text-primary` | `#0F1720` | 主标题、正文 |
| `--color-text-secondary` | `#425466` | 次级说明 |
| `--color-text-muted` | `#667085` | 注释文字 |
| `--color-bg-base` | `#F6FAF9` | 页面底色 |
| `--color-bg-panel` | `#FFFFFF` | 卡片、容器 |
| `--color-border` | `#D9E4E2` | 默认边框 |
| `--color-divider` | `#E8EFEE` | 分隔线 |

规则：

- teal 是唯一页面强调色
- blue、indigo、pink 只允许出现在产品截图内部，不进入官网主界面组件
- destructive red 只在安全提示或错误状态使用

## 7.3 字体

不建议把 `Inter` 作为默认官网显示字体。

建议：

- 中文：`Noto Sans SC`, `PingFang SC`, `HarmonyOS Sans SC`, `sans-serif`
- 英文与数字：`Geist`, `SF Pro Display`, `system-ui`, `sans-serif`
- 代码：`JetBrains Mono`

理由：

- `Geist` 比 `Inter` 更不容易显得像默认模板
- 中文仍以高可读无衬线为主，符合 trust-first

## 7.4 字号层级

| 层级 | 字号 | 行高 | 字重 | 用途 |
|------|------|------|------|------|
| Hero H1 | 56px | 1.08 | 700 | 首屏标题 |
| H2 | 36px | 1.2 | 700 | 区块标题 |
| H3 | 24px | 1.3 | 600 | 小节标题 |
| Body L | 18px | 1.7 | 400 | 首屏说明、引言 |
| Body | 16px | 1.7 | 400 | 正文 |
| Body S | 14px | 1.6 | 400 | 辅助说明 |
| Caption | 12px | 1.5 | 400 | 脚注 |

移动端：

- Hero H1 降到 34 至 40px
- H2 降到 28px

## 7.5 圆角与阴影

统一规则：

- 页面采用 `12px / 16px / pill` 三档
- 不混入大量 4px、20px、24px 的自由发挥

阴影原则：

- 默认靠边框和留白分层
- 阴影只用于 Hero 截图和悬浮面板
- 阴影颜色带轻微冷灰绿调，不用纯黑阴影

## 7.6 图像策略

优先级：

1. 真实产品截图
2. 高保真产品 mockup
3. 极少量辅助图形

不建议：

- 首版使用 unDraw 风格开源插画作为主视觉
- 用抽象插画替代产品截图
- 依赖图标 + 卡片来假装有产品深度

---

## 8. 组件与交互规范

## 8.1 导航

顶部导航只保留少量锚点或页面链接：

- 产品价值
- 核心场景
- 产品闭环
- 安全边界
- CTA

不要把导航栏塞成文档目录。

## 8.2 按钮

按钮统一使用 pill 圆角，沿用产品气质。

按钮层级：

- Primary：实色 teal
- Secondary：白底 + teal 描边
- Tertiary：纯文本链接

按钮文案规则：

- 用动作，不用空泛词
- 优先 `查看演示`、`下载 App`、`阅读说明`
- 避免 `了解更多`

## 8.3 卡片

卡片只在以下场景使用：

- 场景补充卡
- FAQ 折叠项
- 下载平台卡

首页的大部分区块优先用版式差异，而不是“每块都套卡片”。

## 8.4 动效

允许的动效：

- Hero 截图轻微浮动
- tab 切换淡入
- FAQ 展开收起
- 卡片 hover 的轻微位移

不建议：

- 连续视差滚动
- 重度滚动绑定位移动画
- 大面积 blur + glow

## 8.5 可访问性

必须保证：

- 文本与背景对比符合 WCAG AA
- 交互元素最小触控高度 44px
- 所有截图和关键图像有语义化 alt
- 减少动画模式下不影响阅读

---

## 9. 响应式策略

断点继续使用 Tailwind 默认：

- `sm`: 640px
- `md`: 768px
- `lg`: 1024px
- `xl`: 1280px

关键要求：

- Hero 在移动端改为纵向堆叠，但 CTA 必须在首屏可见
- 复杂展示优先改成纵向滚动，不强保留桌面排版
- 所有截图在移动端优先保证清晰，不为了“摆造型”缩太小
- 不使用 `h-screen`，改用 `min-h-[100dvh]`

---

## 10. 技术方案

## 10.1 技术栈

| 层 | 选型 | 说明 |
|----|------|------|
| 框架 | Nuxt 4 | 适合 SSG/SEO/公开站 |
| UI | Nuxt UI v3 | 与 Tailwind v4 配合 |
| 样式 | Tailwind CSS v4 | 页面与内容样式主力 |
| 内容 | Nuxt Content v3 | 仅用于 FAQ、privacy、safety 等轻量内容页 |
| 部署 | Vercel 或 NuxtHub | 以静态生成为主 |

## 10.2 目录建议

```text
luminous-website/
├── app/
│   ├── app.vue
│   ├── assets/css/
│   └── components/
├── content/
│   ├── faq/
│   ├── privacy/
│   └── safety/
├── public/images/
└── pages/
    ├── download.vue 或 waitlist.vue
    ├── privacy/[...slug].vue
    ├── safety/[...slug].vue
    └── faq/[...slug].vue
```

不建议首版引入：

- 复杂 Pinia 全局状态
- 大型内容树
- `/dev` 二级站

## 10.3 内容组织原则

官网内容分三层：

1. 首页讲价值、场景、边界
2. 独立静态页讲隐私、安全、FAQ
3. 深层开发信息外链到 `Lumos-docs`

---

## 11. 文案策略

## 11.1 官网主语气

语气应该是：

- 清楚
- 克制
- 年轻
- 不装专业腔
- 不做医疗承诺

## 11.2 推荐表达

- `用药安全入口`
- `主动提醒`
- `记录后的总结`
- `低风险下一步建议`
- `在边界内提供解释`
- `先说明不确定，再给建议`

## 11.3 禁止表达

- AI 诊断
- 替代医生
- 智能处方
- 自动判断你该吃什么药
- 全面健康管理平台
- 一站式医疗解决方案

## 11.4 Hero 文案原则

Hero 的副标题必须满足：

- 能在 20 秒内读完
- 不堆四个并列价值点
- 不重复“安全”“AI”“主动”三个词各说两遍

---

## 12. 性能与质量目标

| 指标 | 目标 |
|------|------|
| Lighthouse Performance | >= 95 |
| Lighthouse Accessibility | >= 95 |
| Lighthouse SEO | >= 98 |
| LCP | <= 2.2s |
| CLS | <= 0.05 |
| INP | <= 200ms |

补充要求：

- 首页首屏图像全部使用明确尺寸
- 关键截图做格式压缩和响应式裁切
- 不加载无必要的动画库

---

## 13. 实施顺序

## Phase 1：重构首页主线

- 明确唯一主 CTA
- 重写 Hero 文案
- 调整首页区块顺序
- 替换“五模块先行”为“场景先行”
- 建立单一官网色彩系统

## Phase 2：静态内容页

- `/privacy`
- `/safety`
- `/faq`
- `/download` 或 `/waitlist`

## Phase 3：打磨与验证

- 响应式检查
- SEO metadata
- Lighthouse
- 截图替换为真实产品素材
- 文案一致性复核

## Phase 4：开发者与开源入口

- footer 外链
- 仓库链接
- Lumos-docs 入口

只有在公开文档真的形成稳定外部需求后，才考虑扩展独立 `/docs`。

---

## 14. 当前需要先确认的事项

1. 当前官网主转化到底是 `查看演示`、`申请内测` 还是 `下载 App`。
2. 是否已经有可公开展示的高保真截图，而不是概念图。
3. 官网是否真的需要公开 FAQ / privacy / safety 三个静态页，还是先只保留首页锚点版。
4. `Lumos-docs` 是否允许作为开发者入口的公开链接目标。
5. 新官网是新建独立仓库，还是在现有某个站点仓库里重置实现。

---

## 15. 结论

Luminous 官网首版不应该追求“大而全”，而应该追求三件事：

- 一眼看懂产品切入口
- 明确感受到可信边界
- 愿意继续看演示、申请内测或下载

如果首版还能顺便承载少量隐私、安全与 FAQ 页面，就已经足够。

把官网做成第二套文档系统，不会提高产品说服力，只会稀释它。
