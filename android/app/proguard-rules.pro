# Add project specific ProGuard/R8 rules here.
# By default, the flags in this file are appended to flags specified
# in the Android SDK tools proguard/proguard-android-optimize.txt.

# ML Kit text recognition language model classes are included as
# implementation dependencies in build.gradle.kts, so no -dontwarn
# directives are needed.
