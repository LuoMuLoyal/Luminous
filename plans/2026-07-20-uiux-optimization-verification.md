# UIUX 优化计划核查报告与剩余工作

> 创建日期：2026-07-20
> 性质：对 `2026-07-18-uiux-per-page-optimization.md` 执行结果的逐条核查（53 个提交，截至 964c1826）
> 核查方式：7 路并行逐条比对当前工作区代码（非提交信息），l10n 键双语抽查，外加 `flutter analyze` / `flutter test`
> 结论：**P0 已全部闭环**。**P1 跨页面共性 + 今日模块已完成**。**P2 打磨批已完成**。剩余少量低级别打磨项。

---

## 一、总体验收状态

| 检查项 | 结果 |
|--------|------|
| `flutter analyze` | ✅ 零问题 |
| `flutter test` | ✅ 全部通过 |
| 计划"高"级别条目 | 绝大多数 ✅，P0 修复引入的新 bug 已全部闭环 |
| 计划"中"级别条目 | 大部分 ✅ |
| 计划"低"级别条目 | 大部分 ✅，P2 打磨批已落地 |
| 第五节"不得回退"清单 | ✅ 基本守住 |

---

## 二、计划内未做 / 部分完成清单

> 仅列未闭环项；已 ✅ 的约 132 项不再复述。级别沿用原计划。

### 4.5 报告（report）

- ⚠️（中）clinicShare 全程无进行中态（走独立 share API，不走 data export 请求流，需单独追踪 in-flight 状态）。
- ⚠️（中）移动端"弹窗套弹窗"未改（`range_picker_dialog.dart:15,60`）。
- ⚠️（低）suggestion_history 仍传 `onSuggestionTap: null`（原计划注明可不阻塞）。

### 4.7 设置（settings）

- ⚠️（低）"通知"组仍单 tile；支持 URL 仍为文件内常量（仅作兜底，仅 about.dart 使用）。
- 备注："健康档案"入口已移组但仍 push `/mine` tab 根，返回栈问题实质未解决。

### 4.8 助手 / 通知 / 法律 / 认证

- ❌（低）助手：无时间戳无长按菜单；"正在打开…/生成中"仍纯静态文本。
- ⚠️（中）通知：桌面端无 hover 操作钮，仍只能 Slidable 鼠标拖（`list_item.dart:44-69`）。
- ❌（低）认证：头像仍仅 URL 输入无预览无校验（`account_settings.dart:567-572`）；各页辅助链接仍 sm 小字小触控目标。
- ⚠️（中）认证：改密/注销字段已加 validator 但无 `Form` 包裹，提交仍走 toast 双通道。

---

## 五、剩余工作分批建议

### P1 —— 一个迭代内（中级别实质缺口）

全部已闭环。

### P2 —— 打磨批（低级别合一次提交）

已完成。剩余少量低级别打磨项可在后续迭代处理。

---

## 六、验收方式（沿用原计划）

- 每批完成后 `flutter analyze` + `flutter test` 全绿；改动页面过 zh/en 双语与 1.2 倍文字缩放。
- 完成条目按 plans 规范整节删除；本文件与原计划文件在全部闭环后一并删除。
