# 迁移日志 2026-09-16

本日继续执行 [`plans/2026-09-15-review-remediation-plan.md`](../../../plans/2026-09-15-review-remediation-plan.md)（该计划由 09-08..09-14 的日评报告核实后成文）。
09-15 已完成 Wave 1 的十类修复，当日日志见 `2026-09-15.md`；本文件承接其后的 Wave 2 / Wave 3 各项。

## fix(network): 对象存储直传失败按存储侧语义分类

（修复计划 `plans/2026-09-15-review-remediation-plan.md` §3-1。）

### 变更

`putPresignedObject` 此前只把 `DioException` 原样抛出，交给集中错误 mapper 归类。但对象存储回的是
自家 XML/HTML 错误体，不是 RFC 9457 `application/problem+json`——mapper 的 `_fromProblemResponse`
会直接抛 `FormatException`，结果是**每一个**存储侧失败都被降级成 `unknown`，既看不出是链接过期还是
网络断了，也无法据此提示用户重试。

- `core/network/client/object_upload.dart`：PUT 包 try/catch，失败时薄分类——
  403 → business（签名 URL 过期/签名不匹配，这是链接的正常终点，标 `retryable: true` 并带上状态码，
  可提示「链接已过期请重试」）；其它有响应 → server；无响应 → network。traceId 一路带出，
  与 Lucent 请求的排障口径一致。
- `core/network/contract/error_mapper.dart`：`_errorCodeFromDioType` 提升为公开的
  `errorCodeFromDioType`，让存储侧分类复用同一套连通性错误码词汇，而不是另造一份。
- `core/network/README.md`：补陷阱说明——存储侧错误不经集中 mapper，PUT 自带分类。
- `test/core/network/object_upload_test.dart`：补四条用例——403 为可重试的 business 类并带状态码、
  其它 HTTP 错误为 server 类、无响应的传输错误为 network 类且错误码正确、traceId 透传。

### 验证

- `flutter analyze` 无问题；`flutter test test/core/network test/record test/scan` 全绿
  （另两条直传链路：头像与记录图片上传共用本函数，一并覆盖）。

## fix(health_context): 快照缓存反序列化容忍错类型字段

（修复计划 `plans/2026-09-15-review-remediation-plan.md` §3-2。）

### 变更

快照缓存的反序列化对多个字段用硬 cast（`as String?`、`as double?`、`List.cast<String>()`）。
缓存行由旧版本写入、或只是单纯损坏时，只要有一个字段类型对不上，整份快照就抛
`TypeError`——而它只是缓存，网络上的正确数据就在旁边，却会连带整个 `fetchHealthContext`
失败。`List.cast<String>()` 还更隐蔽：它是惰性转换，异常抛在元素首次访问处，离越界的那条
数据很远。

- `data/utils/health_context_snapshot_codec.dart`：标量读取改走 `_asString` / `_asInt` /
  `_asDouble`，类型不符即丢该字段（`_asDouble` 接受 `num`，因为 JSON 不区分 `170` 与
  `170.0`）；列表读取改走 `_asStringList`，逐元素判定、非字符串丢弃、非字符串但可
  字符串化的保留。文档注释写明这是有意的降级：丢字段后调用方会重新拉取并回写缓存行。
- 集合元素的 `as Map<String, dynamic>` 保持不变——那是快照自身结构，不是缓存陈旧问题。

### 验证

- 新增 `test/health_context/health_context_snapshot_codec_test.dart`：整份快照 round-trip
  保真，加上错类型字符串丢弃、整数接受为 double、错类型数值丢弃、列表元素混合类型、
  字段缺失、summary 字段错类型共六条容错用例。
- 反向验证：临时把 `activityLevel` / `heightCm` 还原成硬 cast，新用例如期以
  `type 'int' is not a subtype of type 'String?'` 等失败；恢复后全绿。
- `flutter analyze` 无问题；`flutter test test/health_context test/core/database test/mine`
  全绿。

## fix(settings): 身高体重滚轮的越界取值回写边界值

（修复计划 `plans/2026-09-15-review-remediation-plan.md` §3-1。）

### 变更

身高/体重 sheet 的滚轮把初始值 clamp 进边界,但只改了**显示的滚轮索引**,没有改那个承载提交值
的对象。于是旧数据越界时(如身高 260cm 超过滚轮 250 上限),用户不动滚轮直接点确认,越界值会被
原样回写——clamp 成了纯显示层的假象。

- `presentation/widgets/shared/quantity_sheet.dart`:两处 `_initialIndexes` 在 clamp 命中时把
  边界值同步写回承载提交值的对象(身高写回 cm,体重写回 kg)。
- 回写条件刻意收紧为「真的越界」而非「与取值不一致」:滚轮按整数取值,若无条件回写,光是打开
  sheet 就会静默改写用户当前值——体重 60kg 会变成 132lb 折回的 59.87kg,比原来的 bug 更隐蔽。
- 给两个 late final 索引字段补注释:`imperial` 在 sheet 打开后视为不可变,要支持中途切换需走
  `didUpdateWidget` 重建,而不是就地改 late final。
- `lib/features/settings/README.md`:把这条双向约束(clamp 命中要回写、非越界不得回写)写进
  陷阱与决策。

### 验证

- 新增 `test/settings/quantity_sheet_test.dart`:公制/英制各覆盖「范围内不被改写」与
  「越界被收敛到边界」,另含英制范围内体重不被改写这条防回归用例。
- 反向验证:临时去掉回写,新用例如期以 `Expected: <250> Actual: <260.0>` 与
  `Expected: <50> Actual: <10.0>` 失败;恢复后全绿。
- `flutter analyze` 无问题；`flutter test test/settings test/health_context` 全绿。

## refactor(record): 热量区间粗化口径收敛到一处

（来源：`plans/Luminous-review-09-15.md` 问题 1。）

### 变更

热量区间「粗化到百位」的实现此前有两份逐字节相同的私有副本：数据层拼列表角标用一份、
视图模型 `MealCalorieRangeViewData.coarseLabel` 拼详情页用一份。同一个记录在两个页面
显示同一个区间，靠的是两边恰好都没改——一旦某一侧调整精度（比如 100 改 50）就会漂移。

- 新增 `domain/constants/meal_calorie_range.dart`：导出 `roundToHundreds` 与
  `formatCoarseCalorieRange`，把取整口径与区间文案（含 en dash 连接符）一起集中。
- `data/utils/record_mappers.dart` 与 `presentation/models/meal_analysis_view_data.dart`
  的两个私有副本删除，改调共享实现；`coarseLabel` 的文档注释写明不得就地重算。
- 放在 record 的 domain 而非 `core/utils`：这对函数只服务于餐食区间的展示口径，
  不跨 feature，提到 core 反而会变成无人认领的通用工具。

### 验证

- 新增 `test/record/domain/meal_calorie_range_test.dart`：取整边界（549/550）、
  负值钳零、区间文案拼装与两端相等等情况。
- 全仓 grep 确认 `_roundToHundreds` 私有副本已无残留。
- `flutter analyze` 无问题；`flutter test test/record` 全绿。

## fix(record): 症状「其它」离开输入态时清空已输入的草稿

（来源：`plans/Luminous-review-09-15.md` 问题 4。）

### 变更

症状快录 sheet 的「其它」是就地展开输入框：点「其它」展开、填名称、保存。但按 Back 回到
症状列表时只把输入态标志置回 false，`_otherController` 里的文字原样留着——再次点开「其它」，
上一次的草稿还在，用户要先手动删干净才能填这次想填的名称（且此时「保存」是可点的，容易
误存上一次的内容）。

- `presentation/widgets/dialogs/symptom_quick_entry_sheet.dart`：「其它」的 Back 与退出多选
  (`_exitMultiSelect`) 两处离开输入态时一并 `_otherController.clear()`。两处都清而不是只清
  Back：多选退回单选同样会离开输入态，只修一处会留下同一个坑的另一半。

### 验证

- `test/record/symptom_quick_entry_sheet_test.dart` 补两条用例：Back 后重开草稿为空且保存置灰；
  进出多选后重开草稿为空。
- 反向验证：临时删掉 Back 分支的 clear，新用例如期失败；恢复后全绿。
- `flutter analyze` 无问题；`flutter test test/record` 全绿。

## perf(record): 餐食确认对话框只在按钮可点状态翻转时重建

（来源：`plans/Luminous-review-09-15.md` 问题 5。）

### 变更

餐食确认对话框的三个输入框都注册了同一个监听，任何一次文本变化都 `setState(() {})`
重建整棵对话框（含图片、按钮、底部栏）。中文输入法选词期间每个字符都会通知一次，
而这条路径又是每个快录餐食的必经之路。

- `presentation/widgets/dialogs/meal_confirmation.dart`：监听里先比较确认按钮的可点状态，
  只在翻转（空↔非空、或保存中↔可用）时重建。可点状态由输入内容与 `_saving` 共同决定，
  因此 `_saving` 的三处写入统一走 `_setSaving`，让重建缓存始终与实际状态一致。

### 验证

- `test/record/presentation/widgets/dialogs/meal_confirmation_test.dart` 补一条持续输入
  （可点状态不变）后提交仍取到最新文本的用例；既有的「清空预填字段后置灰」「失败保留弹窗」
  等用例覆盖了每次翻转。
- 说明：我另外写了一条「保存失败后清空字段」的用例想锁 `_saving` 与缓存的交互，但实测
  在移除缓存同步后它**仍然通过**——说明该脱节路径在当前实现下不可达（`setState` 本身
  已重建该帧）。这条无法证伪的用例已删除，不保留虚假覆盖。
- `flutter analyze` 无问题；`flutter test test/record` 全绿。

## refactor(auth): 条款读屏标签改用与可见文案同源的键拼装

（来源：`plans/Luminous-review-09-15.md` 问题 6。）

### 变更

上一轮把条款提示拆成 `authTermsAgreementPrefix` / `authTermsConjunction` 等独立键之后，
带占位符的 `authTermsAgreement`（`"创建即表示同意{terms}与{privacy}"`）只剩
注册页复选框的 `semanticsLabel` 一处在用——同一句同意文案因此有两个副本：一份可见、
一份只给读屏。

- `presentation/pages/register.dart`：读屏标签改为用可见文案的同一批键拼装，
  两处不再可能分叉。
- `lib/l10n/src/auth_{zh,en}.arb`：删除已零引用的 `authTermsAgreement`（含 `@` 元数据）；
  跑 merge + gen-l10n 重生成。
- 删除前全仓 grep 确认除生成物外无引用。

### 验证

- `test/auth/register_page_test.dart` 补一条用例：开启 semantics 后收集复选框子树的
  读屏标签，断言包含可见文案用到的全部键（前缀/条款/连接词/隐私）。
- 反向验证：临时把标签换成一句固定文本，用例如期以
  `Expected: contains '创建即表示同意' / Actual: 'placeholder diverged label'` 失败；
  恢复后通过。
- `flutter analyze` 无问题；`flutter test test/auth` 全绿。

## feat(record): 支持「分析中断」这一新的失败原因

（对侧：Lucent 同日把餐食过期回收的失败码从 `model_timeout` 拆出独立的作业丢失码。
**跨仓契约变更**，契约与生成客户端已随之重导出。）

### 背景

服务端回收「超时仍在分析中」的记录时一律写「模型超时」，但那批记录实际是**作业丢了**
（进程崩溃、队列丢单），不是模型超时。用户此前看到的文案是「分析超时，请重试」——
语气上像是偶发，掩盖了基础设施故障。

### 变更

- `presentation/widgets/meal/analysis_summary_card.dart`：失败码映射新增该分支，
  文案「分析中断了，请重试」（英文 "Analysis was interrupted. Try again"）。
- `lib/l10n/src/record_{zh,en}.arb`：新增 `recordMealAnalysisFailureJobLost`；
  跑 merge + gen-l10n 重生成。
- `presentation/models/meal_analysis_view_data.dart`：注释里的码表补上新码。
- `generated/lucent_api/**`：`dart run scripts/contract/bootstrap.dart` 重生成。

### 验证

- 契约影响面：该字段是普通 `string` 而非生成枚举，新增取值不产生编译破坏；
  `generated/` 的 diff 仅描述文本变化。
- `test/record/widgets/meal/analysis_summary_card_test.dart` 的「每个失败码各有文案」用例
  是**显式枚举**码表的，因此必须同步新增一项才真正覆盖新码——已加。
  反向验证：临时删掉映射分支，用例如期报
  `Found 0 widgets with text "分析中断了，请重试"`；恢复后通过。
- 客户端另有 `_ =>` 通用兜底，未知码仍显示「分析失败，请稍后再试」，不会白屏。
- `flutter analyze` 无问题；`flutter test test/record` 全绿；
  `dart run scripts/docs/verify.dart --verify` 通过。

## feat(medicine): 详情页打通靶点与外部标识数据

### 背景

Lucent 的药品详情接口此前不返回靶点，外部标识字段虽已下发但在客户端被 mapper 丢弃，
因此详情页看不到「这个药作用于什么」与「去哪里查更权威的条目」。

### 变更

- `domain/entities/medicine_detail.dart`：新增 `MedicineDetailTarget` 与
  `MedicineDetailExternalReference`，明细实体补 `targets` / `externalIdentifiers` /
  `externalLinks` 三组字段。
- `data/mappers/medicine_detail.dart`：补齐上述字段的映射（此前 external 两个字段是直接丢弃的）。
- `generated/lucent_api/**`：`dart run scripts/contract/bootstrap.dart` 重生成。

### 验证

- `flutter analyze` 无问题。
- 本轮只做数据贯通，尚未落到界面；详情页的排序与展示在后续条目中一并处理。

## feat(medicine): 详情页按查看频次重排分区并渲染靶点

### 背景

详情页此前把接口返回的字段按载荷顺序平铺成约 20 个折叠项，首项默认展开——
于是默认展开的是 `Description`，而最高频的「适应症」排在后面；单词字段（药物类型、剂型）
与成段正文同等视觉权重；列表一律用 `', '` 拼接成长段；来源里几千字符的毒性/描述原样铺开。
靶点与外部标识两个字段压根没有界面消费方。

### 变更

- `presentation/pages/detail_sections.dart`（新）：分区顺序与默认展开口径的唯一真源。
  `Tier` 按查看频次排版（primary 适应症 → safety 机制/毒性/相互作用 → clinical 靶点/描述 →
  pharmacokinetics 代谢类 → reference 分类/标识/链接），**仅 `Tier.primary` 默认展开**。
  空字段不再占位成空分区。单词字段（药物类型/剂型）并入头部信息，不再单独成节。
- `presentation/pages/detail_section_view.dart`（新）：按分区类型渲染——长文超过阈值折叠并
  提供展开/收起；分类/ATC/同义词等短枚举改为换行 chip 流；靶点每条一张卡（名称、基因 /
  UniProt / 物种、作用类型徽章、PDB 预览）；外部标识/链接可点按复制。
- `presentation/pages/medicine_detail_content.dart`：改用上述构建器，折叠头带条目数徽章；
  移除页内旧的分区构建代码与 `DetailSection` 数据类。
- `lib/l10n/src/medicine_{zh,en}.arb`：新增分区标题、靶点元信息、关系类型、PDB 展开、
  复制反馈、长文展开/收起等键；merge + gen-l10n 重生成。
- `README.md`：登记 Tier 排序口径与 `FCollapsible` 断言方式。

### 验证

- `flutter analyze` 全仓无问题；`flutter test test/medicine test/search` 全绿。
- 新增 `test/medicine/detail_page_test.dart` 四条用例：仅适应症默认展开、点按可展开折叠分区、
  靶点卡渲染作用类型且 PDB 只显示前 6 个 + `+N more` 展开、长文折叠/展开。
- **反向验证**：把默认展开条件改成「全部展开」，5 条断言的失败原因均为
  `should start collapsed`，确认 Tier 口径真被锁住；恢复后通过。
- 七条自定义规则扫描：本次改动文件 0 新增 finding。

## feat(medicine): 序列数据接入并按需加载

### 背景

Lucent 新开了 `GET /api/v1/medicines/:id/sequences` 与详情里的 `sequenceSummary` 计数。
序列正文实测单药可达 96 KB(Imatinib 28 个靶点 × 蛋白/编码基因两个数据集),
因此**默认不加载**,只在用户展开序列区时才请求。

### 变更

- `domain/entities/medicine_detail.dart`:新增 `MedicineDetailSequenceSummary`、
  `MedicineDrugSequence`、`MedicineTargetSequence`、`MedicineSequences`。
- `data/mappers/medicine_detail.dart`:补 `sequenceSummary` 映射;空计数视为无序列。
- `data/mappers/medicine_sequences.dart`(新):序列资源映射;空序列行丢弃,长度以实际文本为准。
- `data/datasources/medicine_detail_remote.dart`:新增 `fetchSequences()`。
- `presentation/providers/medicine_detail.dart`:新增 `medicineSequences` provider。
- `presentation/pages/detail_sections.dart`:新增 `Tier.reference` 的序列分区。
- `presentation/pages/detail_section_view.dart`(新逻辑):序列区渲染——药物各链 + 按 UniProt
  分组的靶点序列(蛋白优先展示,标注蛋白/编码序列与长度),等宽字体每 60 个残基换行,
  可选中、可复制,加载中用骨架屏、失败给重试。
- `presentation/pages/medicine_detail_content.dart`:页面改为 StatefulWidget,展开集合放进
  `ValueNotifier`,只有序列子树订阅。
- `lib/l10n/src/medicine_{zh,en}.arb`:序列相关文案;merge + gen-l10n。
- `generated/lucent_api/**`:契约重生。
- `README.md`:登记按需加载口径与 accordion 的 `setState` 陷阱。

### 验证

- **反向验证按需加载**:去掉展开门控(即折叠时也加载),两条新用例如期失败并报
  `Expected: <0> Actual: <1>`(fetch 计数从 0 变 1);恢复后通过。
- 新增 2 条 widget 用例:折叠态只显示计数且 provider 一次都没被调用;展开后才拉取并渲染
  链名、靶点名、蛋白/编码标注与 UniProt id。
- `flutter analyze` 全仓无问题;`flutter test` 全绿(含 medicine + search);
  七条自定义规则扫描:新增文件 0 新增 finding。

### 踩坑记录(已写进 feature README)

在 accordion 的 `onChange` 里 `setState` 会**让刚展开的分区立刻弹回**:Forui 的
`InheritedAccordionData` 以整个 expanded 集合为通知依据,重建导致每个 item 重跑
`didChangeDependencies`,命中 `case _` 分支把动画值重置回 `initiallyExpanded`。
改为 `ValueNotifier` + `ValueListenableBuilder`(只订阅序列子树)后消失。

## feat(medicine): 详情页渲染分子性质

### 背景

Lucent 的详情接口新增了 `structure`（计算结构描述符）。这些是标量，随详情一起下发，
前端此前没有消费方。

### 变更

- `domain/entities/medicine_detail.dart`：新增 `MedicineStructure`（32 个可空字段）
  与 `isEmpty`（全空即视为无数据）。
- `data/mappers/medicine_detail.dart`：补 `structure` 映射。OpenAPI 的 `number` 在 Dart
  生成成 `num`，统一用 `_toDouble` 收敛到 `double`；空串收敛为 null。
- `presentation/pages/detail_sections.dart`：新增 `Tier.reference` 的分子性质分区。
- `presentation/pages/detail_section_view.dart`：新增 `MedicineStructureView`——
  标识行（分子式/分子量/精确质量/SMILES/InChIKey/InChI/IUPAC）带复制按钮，
  其余按「尺寸与表面 / 亲脂性与溶解度 / 电荷与 pKa / 类药性规则」分组；
  数值按可读精度取整（分子量 2 位、精确质量 4 位、极化率与折射率 1 位）；
  规则判定把源里的 0/1 旗标翻成「符合/不符合」；盐型用 chip 流。
  **缺失的字段不占位**（不渲染空标签）。
- `lib/l10n/src/medicine_{zh,en}.arb`：分子性质相关文案；merge + gen-l10n。
- `generated/lucent_api/**`：契约重生。

### 验证

- **端到端实跑**:伊马替尼 `DB00619` 详情带上 `structure`,分子式 `C29H31N7O`、
  MW 493.6027、精确质量 493.259、logP 4.378、TPSA 86.28、供体 2 / 受体 7、
  可旋转键 7、环数 5、重原子 68、五规则与 MDDR 达标、盐型 `Imatinib mesylate`;
  响应中**不含**上游坏字段(`traditionalIupac`)。无结构药物 `DB00002` 为 null。
- 新增 3 条 widget 用例:分组渲染与数值取整、规则旗标翻成文案、缺失字段不留空标签、
  无结构时不出现该分区。
- `flutter analyze` 全仓无问题;`flutter test` 全绿;七条自定义规则扫描 252 条 finding
  与基线一致(本次改动文件 0 新增)。

## docs(medicine): 补记分子性质的落地口径

### 变更

- `lib/features/medicine/README.md`:补记详情页分子性质(结构描述符)的三条易踩口径——
  数值字段经生成器落成 Dart `num`、统一在 mapper 的 `_toDouble` 收敛;
  类药性规则在源里是 0/1 旗标、由渲染层翻成文案而实体保留原值;
  缺失字段不占位、全空即分区整体不出现。

### 验证

- `dart run scripts/docs/verify.dart --verify` 通过(链接、front-matter、新鲜度、
  可读性、feature README 覆盖)。
- 本轮仅文档,未改代码行为。
