# Migration Log - 2026-07-15

## Luminous — 7-15 审查遗留问题全部修复

- **目标**：根据 `plans/luminous-review-2026-07-15.md` 审查文档，确认 6 项遗留问题并逐一修复。
- **验证结果**：`flutter analyze` 零问题（9 个修改文件全部通过）。
- **变更内容**：

### #2 router.dart CustomTransitionPage 5 处重复 → 统一 `tabFadePage` 辅助函数

- `lib/core/design/durations.dart`：新增 `tabPageTransitionIn`（150ms）和 `tabPageTransitionOut`（Duration.zero）两个 Duration token。
- `lib/app/router/helpers.dart`：新增 `tabFadePage<T>()` 辅助函数，使用 tab 专用 Duration token，与已有 `fadePage`（auth 400ms）和 `slidePage`（CRUD 220ms）形成完整的三级页面过渡辅助体系。
- `lib/app/router.dart`：5 个 Tab 的 `GoRoute` pageBuilder 从内联 `CustomTransitionPage` + `FadeTransition` + 硬编码 `Duration(milliseconds: 150)` 改为调用 `tabFadePage(key: state.pageKey, child: ...)`；移除不再需要的 `flutter/material.dart` import；新增 `router/helpers.dart` import。

### #3 AppImageCompressor Web 平台静默跳过添加日志

- `lib/core/utils/image_compressor.dart`：`kIsWeb` 分支添加 `appTalker.debug('Image compression skipped on Web (no plugin backend)')` 日志，使 Web 平台跳过行为可追踪。catch 块日志在 7-14 已修复（`appTalker.warning`）。

### #4 box_scan.dart 异常暴露给用户 → 使用本地化错误消息

- `lib/l10n/src/medicine_zh.arb` + `medicine_en.arb` + `app_zh.arb` + `app_en.arb`：新增 `scanRecognitionFailedToast` ARB key（中："识别失败，请重试"，英："Recognition failed. Please try again."）。
- `flutter gen-l10n` 重新生成 `app_localizations.dart`。
- `lib/features/scan/presentation/pages/box_scan.dart`：`AppToast.show(context, '识别失败: $e')` 改为 `AppToast.show(context, AppLocalizations.of(context)!.scanRecognitionFailedToast)`，不再向用户暴露原始异常技术细节。`appTalker.error` 日志保留完整异常信息供排查。

### #5 suggestion.dart 空 catch 缺少 appTalker 日志

- `lib/features/today/presentation/widgets/sections/suggestion.dart`：`_SuggestionFeedbackRowState._submit` 的 `catch (_)` 改为 `catch (e, st)` + `appTalker.error('submitFeedback failed: $e', e, st)`，使异常详情可通过日志追溯。新增 `core/logger/logger.dart` import。

### #6 bootstrap.dart 高对比度颜色硬编码 → 提取为设计系统 token

- `lib/core/design/high_contrast.dart`：新建 `HighContrastColors` 抽象类，定义 6 个高对比度颜色常量（`lightForeground` / `lightMutedForeground` / `lightBorder` / `darkForeground` / `darkMutedForeground` / `darkBorder`），含文档注释说明这些值是有意不随主题族变化的极端无障碍覆盖值。
- `lib/core/design/design.dart`：barrel 导出 `high_contrast.dart`。
- `lib/app/bootstrap.dart`：`_applyHighContrast` 函数中的 6 个硬编码 `Color(0xFF...)` 改为引用 `HighContrastColors.*` 常量。新增 `core/design/design.dart` import。
