# Migration Log — 2026-09-04

## test(review): 建议历史去重用例头注释改单调用点

`test/review/widgets/dedupe_today_suggestions_test.dart` 头注释由两个调用点共享改写为单一生产调用点口径:函数仅被建议历史卡消费,注释描述按 title reason type 键去重并保留最高生命周期状态与首次出现顺序的契约语义,防止未来拆出第二调用点时契约分叉。
验证:改动目录 dart analyze 通过,该测试文件通过。

## refactor(analytics): 平台枚举类型文件级别名收口

`lib/core/analytics/product_event_service.dart` 为超长生成枚举引入文件级 typedef,收口 resolveUserDevicePlatform 分支与私有字段;公开签名保持原名,避免私有类型泄露进公共 API。
验证:改动目录 dart analyze 通过。

## refactor(review): 数据仓库长生成枚举类型别名收口

`lib/features/review/data/repositories/lucent_review.dart` 与 `lucent_dashboard.dart` 为超长生成响应枚举引入文件级 typedef,映射方法改走别名,行为不变。
验证:改动目录 dart analyze 通过,相关仓库与 dashboard 测试通过。

## refactor(auth): 会话映射内联并澄清令牌类型

`lib/features/auth/data/mappers/auth.dart` 的单一调用点 helper 内联进 toSessionFromLogin,注释澄清生成客户端中登录与注册共用同一令牌模型,无独立登录令牌类型;`test/auth/mapper_test.dart` 补该映射路径用例。
验证:auth mapper 测试通过。

## fix(scripts): 命令重试仅限文件锁类失败

`scripts/support.dart` 的 runLoggedCommand 仅在可执行文件为 dart 且错误输出含 Windows 文件锁特征时才重试,其余失败首次即上抛;进程输出改管道转发以读取错误。`test/scripts/support_test.dart` 补重试门控用例。
验证:scripts 测试通过。

