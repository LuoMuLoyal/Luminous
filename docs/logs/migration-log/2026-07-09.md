# 2026-07-09 迁移日志

## debugPrint → Talker 日志系统迁移 (LUM-2026-0709-02)

将全库 68 处 `debugPrint` 统一迁移到已有的 `talker_flutter` 日志基础设施。

### 基础设施改动

- `lib/core/logger/app_logger.dart`：新增全局 `appTalker` 单例访问器，`talkerProvider` 改为返回同一单例实例。这修复了原先每次 `ref.read(talkerProvider)` 创建新实例导致日志级别过滤不全局生效的问题。
- 无 `Ref` 的类（静态工具、纯 Repository）使用 `appTalker.error(...)`
- 有 `Ref`/`WidgetRef` 的类（Notifier、ConsumerWidget）使用 `ref.read(talkerProvider).error(...)`

### 迁移文件清单（28 文件）

**auth 模块**：
- `session_provider.dart` — 1 处
- `account_provider.dart` — 5 处
- `register_form_provider.dart` — 2 处
- `password_reset_provider.dart` — 2 处
- `login_page.dart` — 1 处

**mine 模块**：
- `health_edit_forms.dart` — 7 处

**medicine 模块**：
- `reminder_providers.dart` — 2 处
- `reminder_notification_coordinator.dart` — 1 处
- `page.dart` — 1 处
- `risk_check_repository.dart` — 1 处（appTalker）
- `lucent_workspace_repository.dart` — 2 处（appTalker）

**record 模块**：
- `nlp_controller.dart` — 2 处
- `edit.dart` — 4 处
- `create.dart` — 2 处
- `detail.dart` — 1 处
- `voice_entry_dialog.dart` — 1 处
- `ocr_entry_dialog.dart` — 1 处
- `fast_entry_dialog.dart` — 1 处
- `lucent_repository.dart` — 1 处（appTalker）

**assistant 模块**：
- `controller.dart` — 7 处
- `page.dart` — 5 处

**settings 模块**：
- `profile_sync_provider.dart` — 1 处
- `advanced_settings_page.dart` — 1 处
- `data_export_page.dart` — 1 处
- `language_settings_page.dart` — 1 处

**today 模块**：
- `ai_analysis_provider.dart` — 1 处
- `lucent_repository.dart` — 3 处

**report 模块**：
- `ai_summary_provider.dart` — 1 处
- `page.dart` — 2 处

**search 模块**：
- `provider.dart` — 1 处
- `page.dart` — 1 处
- `lucent_repository.dart` — 1 处（appTalker）

**scan 模块**：
- `box_scan_page.dart` — 1 处（appTalker，独立函数）
- `barcode_scanner_page.dart` — 1 处

**core/app 模块**：
- `app_toast.dart` — 1 处（appTalker，静态类）
- `app.dart` — 1 处

### 验证

- `flutter analyze`：零错误
- `flutter test`：922 个测试全部通过

## 文档治理优化 (LUM-2026-0709-03)

### 索引同步

- `docs/03-logs/MigrationLog.md` 索引更新至 2026-07-09，补充 07-08、07-09 条目及 Quick Navigation by Topic 日期。

### TODO 清理

- `docs/00-current/TODO.md` 删除已完成项"Release 模式错误上报：`debugPrint` 仅 debug 生效"（已由 Talker 迁移解决）。

### Current_State 重构

- `docs/00-current/Current_State.md` 从 280+ 行详细基线重写为简洁索引结构（~40 行）。
- 07-06 ~ 07-09 的详细 completed baselines 归档至 `docs/04-archive/current-state-archive.md`。
- 新增"当前基线摘要"节，保留 5 条核心状态摘要 + 子文件链接。

### Glossary 同步

- `docs/Glossary.md` 新增后端相关术语：NestJS、Prisma、AI Pipeline、Meal Analysis、Data Export、AdminJS、BullMQ、Talker、Slot-aware Dose Log。

## Pre-push 钩子激活 (LUM-2026-0709-04)

- `.githooks/pre-push` 从 no-op 注释激活为实际检查：`flutter analyze` + `dart format --set-exit-if-changed`。
- `tool/git_hook.dart` 新增 `pre-push` 分支，调用 `runPrePushChecks()`。
- `tool/tooling_workflows.dart` 新增 `runPrePushChecks()` 函数。
- `README.md` 更新钩子描述。
- 验证：`dart run tool/git_hook.dart pre-push` 通过（0 issues, 0 changed）。

## CI 简化：移除 Lucent 交叉仓库依赖 (LUM-2026-0709-05)

- `.github/workflows/flutter-ci.yml` 重写，移除以下步骤：
  - Checkout Lucent 仓库
  - Setup pnpm / Node.js
  - Install Lucent dependencies
  - Export Lucent OpenAPI（CI 中因缺少 `generated/prisma/` 而失败）
  - Bootstrap generated sources with `--openapi`（重新生成 API 客户端）
  - Verify Lucent OpenAPI contract sync
- 替换为自举流程：
  - `dart run tool/bootstrap_generated_sources.dart --skip-client`（生成 l10n + 主仓 build_runner）
  - `cd generated/lucent_api && dart pub get && dart run build_runner build`（从已追踪的 API 声明文件生成 .g.dart）
- OpenAPI 合同同步验证改为本地维护步骤（`dart run tool/verify_lucent_openapi_sync.dart`）。
- CI 从 13 步缩减为 7 步，不再需要 pnpm / Node.js 环境。
- `README.md` 更新 CI 描述。

## 审查报告快速修复 (LUM-2026-0709-06)

基于 `plans/luminous-review-2026-07-09.md` 审查报告，完成 7 项低风险修复：

- **Mock 固定日期** (0709-03): `report/mock_repository.dart` 的 `previewDashboard` 和 `_signedOutDashboard` 中硬编码的 `'2026-06-06'`/`'2026-06-12'` 替换为 `clock.now()` 动态计算。
- **`requireBody()` 提取** (0709-07): `core/network/map_utils.dart` 新增 `requireBody()` 函数，替换 `record`/`health_context`/`settings` 三处 `if (body == null)` 重复模式。
- **maxWidth 魔法数字** (0709-05): `AppLayoutTokens` 新增 `dialogMaxWidth`(360) / `wideDialogMaxWidth`(420)，替换 7 处硬编码 `maxWidth: 360/400/420`。
- **动画时长常量** (0709-06): `AppAnimationDurations` 新增 `widgetFadeIn`(220ms) / `widgetExpand`(250ms) / `widgetQuick`(200ms) / `widgetStandard`(300ms)，替换 12 处内联 `Duration(milliseconds:)`。
- **基准年魔法数字** (0709-13): `date_time_formatters.dart` 新增 `formatHourMinute()` 函数，消除 `edit.dart`/`create.dart` 中 `DateTime(2000, 1, 1, ...)` dummy 载体。
- **assistant 日期格式** (0708-06): `assistant/ui_formatters.dart` 的 `conversationTimestampLabel` 从 zh/非zh 硬编码 pattern 改为 `intl.DateFormat.MMMd(locale).add_Hm()`。
- **mine 语义色** (0709-01 部分): `mine/lucent_repository.dart` 的 `_green` → `secondary`, `_red` → `destructive`, `_pink` → `secondary`, `_blue` → `primary`。
- 验证：`flutter analyze` 0 issues。

## 二维语义颜色系统 (LUM-2026-0709-07)

将一维 `AppColors` 枚举（7 个扁平值）升级为二维 `SemanticColor` + `SemanticColorPalette` + `SemanticColors` 三件套，消灭全库 ~30 处 alpha 魔法数字。

### 新增文件

- `lib/core/design/semantic_color_palette.dart` — 不可变值对象，5 个预计算 tone：`solid` / `foreground` / `muted` / `subtle` / `border`。
- `lib/core/design/semantic_colors.dart` — `ThemeExtension<SemanticColors>`，通过 `FColors.extensions` 注入 Forui 主题。包含 6 组 palette：`primary` / `success` / `warning` / `info` / `destructive` / `neutral`。
- `lib/core/design/semantic_color.dart` — 枚举 + resolution 扩展。`primary` / `destructive` 随色族变化；`success` / `warning` / `info` 固定跨色族（医疗健康 app 硬性要求）；`neutral` 派生自主题中性色。

### 主题注入

- `lib/core/theme/theme.dart` — `appThemeData()` 注入 `SemanticColors` extension。暗色模式 alpha 自动补偿（light 0.05/0.10/0.20 → dark 0.08/0.18/0.35）。

### Domain 层迁移（51 文件）

- `AppColors` → `SemanticColor` 全局类型替换。
- `AppColors.secondary` / `.muted` / `.foreground` / `.background` / `.border` → `SemanticColor.neutral`。
- `reportStatusColor` 修正语义映射：`good→success` / `stable→info` / `needsAttention→warning` / `insufficientData→warning` / `unknown→neutral`。
- `report/lucent_repository.dart` — `_metricColor` / `_insightColor` 的 water/sleep 从 `neutral` 改为 `info`。

### Widget 层 alpha 清理（核心文件）

- `findings_section.dart` / `metrics_grid.dart` / `meal_analysis_status_badge.dart` / `safety_panel.dart` / `quick_action_section.dart` / `medicine_add_precheck_dialog.dart` — `resolve(colors).withValues(alpha: X)` 替换为 `palette().muted` / `.border` / `.solid`。
- 消灭 alpha 魔法数字：0.08/0.12 → `palette.muted`，0.18/0.22 → `palette.border`，无 alpha → `palette.solid`。

### Bridge 方法

- `SemanticColor.resolve(FColors)` — 返回 `palette.solid`，兼容旧 `resolve(colors)` 调用。
- `SemanticColor.of(BuildContext)` — 同 `solid(context)`。
- `List<SemanticColor>.resolveAll(FColors)` — 列表批量解析。

### 验证

- `flutter analyze`：0 issues。
- `flutter test`：922/922 passed。

## Today 主动建议引擎后端架构规划 (LUM-2026-0709-08)

### 背景

- 按 `Product_Tab_Component_Blueprint` 核对五个 Tab 后，确认 Today 主/次建议卡类型模型偏窄：前端 `TodayPriorityItemType` 仅支持 `medication` / `water`，无法表达蓝图要求的「明确风险 / 疑似漏服 / 恶化趋势 / 行为建议」四类卡片。
- 根本原因是 Today 的 `priorityItems` 由前端 `LucentTodayRepository` 硬编码组装，后端缺少统一建议裁决引擎。

### 新增文档

- `plans/2026-07-09-today-suggestion-engine-backend.md`
  - 提出「Signal → Candidate（规则引擎）→ Suggestion（仲裁器）」三层后端流水线。
  - 定义 `SuggestionType`、`SuggestionCandidate`、`EvidenceItem`、`SuggestionAction` 等领域模型。
  - 设计新增 API `GET /api/v1/user/today/suggestions`。
  - 给出四类建议的具体规则示例。
  - 分四阶段落地：Phase 1 替换现有两类硬编码；Phase 2 支持蓝图四类；Phase 3 AI 解释层；Phase 4 反馈优化。
  - 明确安全边界：规则优先、AI 仅解释、禁止 LLM 做医疗判定。

### TODO 同步

- `docs/00-current/TODO.md`
  - 删除 "MVP Gated But Not Blocking Right Now" 与 "当前tab缺失" 之间的孤立片段（证据/建议/动作/边界四行）。
  - 在 "MVP Gated But Not Blocking Right Now" 新增：Today 主动建议卡片后端统一裁决引擎。
  - 在 "当前tab缺失" 第一条补充根本原因与后端规划文档引用。

### 影响

- 无代码改动，仅文档与规划。
- 前端当前硬编码行为不变；后续按 Phase 1 开始迁移时，才涉及 `LucentTodayRepository` 和 `TodayPriorityItemType` 改动。

## design/ 目录架构升级 (LUM-2026-0709-09)

将 `lib/core/design/` 下全部 token 文件的命名和结构统一到一致标准，消除 `App` 前缀残留、后缀不统一（`Tokens` vs `Token` vs 无后缀）、`class` + `const _()` 反模式等问题。

### 文件重命名

| 旧文件 | 新文件 |
|---|---|
| `spacing_tokens.dart` | `spacing.dart` |
| `radius_tokens.dart` | `radius.dart` |
| `typography_tokens.dart` | `typography.dart` |
| `animation_durations.dart` | `durations.dart` |
| `layout_tokens.dart` | `layout_scale.dart`（拆分：`LayoutScale` + `LayoutScaleResolver`） |
| `breakpoints.dart` | `breakpoints.dart`（内容更新，类名改） |
| `responsive_sizing.dart` | `responsive_sizing.dart`（内容更新，类名改） |

### 类型重命名

| 旧类型 | 新类型 |
|---|---|
| `AppSpacingTokens` | `Spacing` |
| `AppRadiusTokens` | `RadiusTokens`（避免与 Flutter `Radius` 冲突） |
| `AppTypographyToken` | `TypographyToken` |
| `AppTypographyTokenExtension` | `TypographyTokenExtension` |
| `AppAnimationDurations` | `DurationTokens`（避免与 Flutter `Durations` 冲突） |
| `AppBreakpoints` | `Breakpoints` |
| `AppResponsiveSizing` | `ResponsiveSizing` |
| `AppLayoutScale` | `LayoutScale` |
| `AppLayoutTokens` | `LayoutScaleResolver` |

### 结构改进

- `AppAnimationDurations` 从 `class + const _()` 改为 `abstract final class DurationTokens`。
- `layout_tokens.dart` 中的 `AppLayoutScale`（值对象）和 `AppLayoutTokens`（静态工具）拆分为 `LayoutScale` 和 `LayoutScaleResolver`，职责分离。
- Barrel `design.dart` 按关注点分组并添加注释（color / layout / motion / shape / space / type）。
- 所有调用点统一通过 barrel 导入，移除了对单文件的直接导入。

### 影响范围

- 165 个 `.dart` 文件经脚本批量替换类型名和 import 路径。
- 3 个文件因缺少 barrel 导入需手动补上。
- 7 个文件有重复 import 需去重。

### 验证

- `flutter analyze`：0 issues。
- `flutter test`：922/922 passed。

## Mine 退出登录 tile + Report 趋势折线图 (LUM-2026-0709-10)

### Mine 账号与安全区增加退出登录 tile

- `MineAccountSecuritySection` 从 `StatelessWidget` 改为 `ConsumerWidget`。
- 在 `FTileGroup` 末尾增加退出登录 tile（`FLucideIcons.logOut` + `colors.error` 红色文字）。
- 未登录时显示「去登录」文字，点击导航到登录页；已登录时显示「退出登录」，点击调用 `authSessionProvider.notifier.logout()` 后跳转登录页。
- Settings 页的 `_SignOutTile` 保留作为二级入口。
- 测试更新：`test/mine/page_test.dart` 中 `authGoLogin` 断言从 `findsOneWidget` 改为 `findsNWidgets(2)`（hero CTA + sign-out tile）；第二次 tap 前增加 `ensureVisible`。

### Report 趋势区替换为 fl_chart 折线图

- `_TrendPlaceholder`（手写 bar 条 + 骨架包装）替换为 `_TrendChart`，使用 `fl_chart: ^1.2.0` 的 `LineChart`。
- 多线模式：medication / water / sleep 三条趋势线同图展示，颜色来自 `SemanticColor`。
- 动态日期标签：从 `dashboard.startDate` 解析 `DateTime`，生成 M/D 格式标签；30 天范围自动降密度（每 N 天显示一个）。
- 7 天范围显示圆点 + 直线；30 天范围使用曲线 + 无圆点以避免拥挤。
- `ReportTrendSection` 新增 `startDate` 必填参数，`dashboard_view.dart` 传入 `dashboard.startDate`。
- 移除了对 `reportTrendDateLabels` ARB 硬编码日期标签的依赖（ARB key 保留未删除）。
- 移除了不再使用的 `_TrendValuePill` 和 `AppSkeletonSlot` 包装。

### 验证

- `flutter analyze`：0 issues。
- `flutter test`：922/922 passed。

## Today 主动建议引擎后端架构规划优化 (LUM-2026-0709-10)

### 背景

- 原方案 `plans/2026-07-09-today-suggestion-engine-backend.md` 存在两类问题：命名不符合 `Lucent/AGENTS.md` 文件命名规则（去前缀化），以及架构局限于 MVP 四类卡片范围，未充分利用 `Product_Insights` 的完整设计。

### 优化内容

- 命名合规：模块名统一为 `today-suggestion`，根文件保留前缀，子目录文件不重复前缀，所有 `@Injectable()` 使用 `.service.ts` 后缀，接口放入 `types/`，只使用白名单子目录，`services/` 下按域分组。
- 架构增强：
  - 卡片类型从 4 类扩展到 5 类（新增说明卡 `coverage`）。
  - 新增双触发链（事件触发 + 定时触发）。
  - 新增冷启动与维度基线管理（`BaselineService`）。
  - 新增卡片生命周期管理（生成→激活→消退→失效）。
  - 反馈闭环从 Phase 4 提前到 Phase 3，4 种反馈真实影响后续出卡。
  - 新增信号组合支持（弱信号按预定义规则合成强卡）。
  - 新增通知升级设计（只有高优先级事件触发卡升级为通知）。
  - 新增建议持久化（`UserSuggestion` 表，供 Report 历史回顾）。
  - 新增缓存策略（Redis 多层缓存）。
  - AI 解释层从 Phase 3 后移到 Phase 4，但架构从开始就预留。
  - 复用 `TodayAnalysisContextService` 而非平行采集。
- 目录结构重新设计：严格遵循 AGENTS.md 白名单子目录和 barrel 导出规则。
- 实施阶段从 4 阶段扩展到 6 阶段。

### 影响

- 无代码改动，仅规划文档优化。
- 前端当前硬编码行为不变；后续按 Phase 1 开始迁移时，才涉及 `LucentTodayRepository` 和 `TodayPriorityItemType` 改动。

## Today 主动建议引擎后端 Phase 1+2 实现 (LUM-2026-0709-11)

### 背景

- 后端 `today-suggestion` 模块 Phase 1+2 已实现完成。前端尚未对接，当前硬编码行为不变。

### 后端变更

- 新增 `GET /user/today/suggestions` 端点，返回结构化建议卡数据。
- 5 类卡片规则已就绪：compliance（missed-dose）、behavior_advice（water-shortfall、sleep-shortfall）、trend（deteriorating-symptom）、coverage（profile-incomplete、empty-today）。
- 冷启动基线 + 卡片生命周期管理已就绪。
- 26 个单元测试全部通过。

### 前端影响

- 无代码改动。
- 后续 Phase 3（反馈闭环）和前端对接时，需要修改 `LucentTodayRepository` 和 `TodayPriorityItemType`。

## Pre-commit 文档检查钩子 — 脚本整理与去重

### 背景

上次新建的 `tool/check_docs_updated.dart`（硬编码阻断脚本）与已有的 `tool/check_doc_coverage.dart` + `tool/doc_coverage.dart`（配置驱动的 warning-only 检查）功能重叠。进行合并去重。

### 删除

- `tool/check_docs_updated.dart` — 硬编码逻辑，已被 `check_doc_coverage.dart --enforce` 取代

### 变更

- `tool/check_doc_coverage.dart` — 新增 `--enforce` 模式：
  - `--enforce` + `--staged`：若 staged 中有代码文件但无任何 `docs/` 文件 → `exit(1)` 阻断提交
  - 默认模式不变（warning-only，per-rule 报告缺少的具体文档）
  - 支持 `SKIP_DOC_CHECK=1` 旁路
- `tool/tooling_workflows.dart` → `runPreCommitChecks()`：调用改为 `check_doc_coverage.dart --staged --enforce`
- `tool/tooling_support.dart` — 新增 `verifyOpenApiJson()` 公共函数，提取自 `bootstrap_generated_sources.dart` 和 `verify_lucent_openapi_sync.dart` 中的重复 `_verifyOpenApiJson`
- `tool/bootstrap_generated_sources.dart` — 删除私有 `_verifyOpenApiJson`，改用 `tooling_support.dart` 中的 `verifyOpenApiJson()`
- `tool/verify_lucent_openapi_sync.dart` — 同上
- `docs/00-current/Current_State.md` — 更新文档治理条目

### 整理后 tool/ 目录结构

| 文件 | 职责 |
|---|---|
| `tooling_support.dart` | 共享基础设施：ToolContext、runLoggedCommand、captureCommandLines、verifyOpenApiJson 等 |
| `tooling_workflows.dart` | 工作流编排：runDailyChecks / runPreCommitChecks / runPrePushChecks / validateCommitMessage / runFullstackChecks |
| `git_hook.dart` | Git hook 入口（.githooks/* 委托到此） |
| `install_git_hooks.dart` | 一次性安装脚本（配置 `core.hooksPath`） |
| `run_daily_checks.dart` | 日常检查 CLI（doc coverage warning + bootstrap + analyze + format + test + openapi sync） |
| `run_fullstack_checks.dart` | 全栈 E2E 检查 CLI |
| `bootstrap_generated_sources.dart` | 重新生成 `generated/lucent_api/` + app codegen |
| `verify_lucent_openapi_sync.dart` | 验证 Lucent OpenAPI + 生成客户端布局 |
| `check_doc_coverage.dart` | 文档覆盖检查 CLI（默认阻断；`--warning-only` 用于日常检查） |
| `doc_coverage.dart` | `check_doc_coverage.dart` 的引擎（YAML 配置解析、glob 匹配、报告渲染） |

### 验证

- `dart format tool/` ✅
- `flutter analyze tool/` ✅ (No issues found)
- `dart run tool/check_doc_coverage.dart --staged`（无 staged 文件）→ `exit 0` ✅
- `SKIP_DOC_CHECK=1 dart run tool/check_doc_coverage.dart --staged` → skip ✅
- `dart run tool/check_doc_coverage.dart --warning-only` → per-rule warnings, `exit 0` ✅

## 文档校验改为默认阻断 + Lucent 脚本 .ts 化

### 变更

- Luminous `check_doc_coverage.dart`：
  - 移除 `--enforce` 标志
  - 默认行为改为阻断（有代码变更但无 `docs/` 文件时 `exit(1)`）
  - 新增 `--warning-only` 标志用于非阻断模式（日常检查使用）
  - `SKIP_DOC_CHECK=1` 旁路仅在默认（阻断）模式下生效
- Luminous `tooling_workflows.dart`：
  - `runPreCommitChecks`：`check_doc_coverage.dart --staged`（默认阻断，无需 `--enforce`）
  - `runDailyChecks`：`check_doc_coverage.dart --warning-only`（不阻断）
- Lucent `check-docs-updated`：从 `.mjs` 重写为 `.ts`，与项目其余脚本统一
- Lucent `package.json`：`simple-git-hooks.pre-commit` 更新为 `.ts` 路径

### 验证

- Lucent：`node scripts/hooks/check-docs-updated.ts` → `exit 0` ✅
- Lucent：`SKIP_DOC_CHECK=1` → skip ✅
- Luminous：`dart run tool/check_doc_coverage.dart --staged` → `exit 0` ✅
- Luminous：`--warning-only` → per-rule warnings, `exit 0` ✅
- Luminous：`SKIP_DOC_CHECK=1` → skip ✅
- `flutter analyze tool/` → No issues found ✅

## Today 建议引擎前端接入 Phase 1+2 (LUM-2026-0709-12)

### 背景

- 后端 `today-suggestion` 模块 Phase 1-6 已实现完成，但前端仍使用 `LucentTodayRepository` 中硬编码的 `priorityItems`（medication + water 两个 mock 卡）。
- 本次开始接入真实建议引擎数据源，替换 mock 数据。

### Phase 1: API 客户端生成 + 网络层注册

- `Lucent` 后端 `today-suggestion.controller.ts` 补全 `@ApiResponse` 装饰器 + 4 个 Response DTO 重构为信封模式（`XxxDataDto` + `XxxResponseDto`），详见 Lucent migration log。
- OpenAPI 重新导出，Flutter API 客户端重新生成：新增 `TodaySuggestionApi`（4 个端点），`TodaySuggestionsDataDto`、`SuggestionItemDto`、`EvidenceItemDto`、`SuggestionActionDto` 等完整 DTO 类。
- `dio_client.dart`：新增 `TodaySuggestionApi get todaySuggestionApi => _client.todaySuggestion;`
- `network_providers.dart`：新增 `lucentTodaySuggestionApiProvider`。

### Phase 2: Domain 实体 + Data Source + Repository 清理

**新增文件**：

- `lib/features/today/domain/entities/suggestion.dart` — 完整域实体层（freezed）：
  - 7 个枚举：`TodaySuggestionType`、`TodaySuggestionCardTone`（含 `warning`）、`TodaySuggestionConfidence`、`TodaySuggestionLifecycleState`、`TodaySuggestionTriggerType`、`TodaySuggestionFeedback`、`TodaySuggestionFeedbackEffect`、`TodaySuggestionEvidenceKind`
  - 7 个实体类：`TodaySuggestionCard`、`TodaySuggestionBundle`、`TodaySuggestionEvidence`、`TodaySuggestionAction`、`TodaySuggestionExplanation`、`TodaySuggestionFeedbackResult`、`TodaySuggestionHistory`、`TodaySuggestionHistoryItem`
- `lib/features/today/data/datasources/suggestion_remote_data_source.dart` — DTO→Domain 映射层：
  - `fetchSuggestions()` — GET /today/suggestions → `TodaySuggestionBundle`
  - `submitFeedback()` — POST /today/suggestions/:id/feedback → `TodaySuggestionFeedbackResult`
  - `explainSuggestion()` — POST /today/suggestions/:id/explain → `TodaySuggestionExplanation`
  - `fetchHistory()` — GET /today/suggestions/history → `TodaySuggestionHistory`
  - 所有枚举映射使用字符串值（`.json` 字段），兼容不同生成器产出的同名枚举类型

**修改文件**：

- `lucent_repository.dart`：`priorityItems` 从硬编码 2 个 mock 卡改为空列表 `const <TodayPriorityItem>[]`，真实建议数据将由独立的 `todaySuggestionProvider` 提供。
- `dashboard.dart`：`TodayPriorityItemType`、`TodayPriorityItem`、`priorityItems` 字段标记 `@Deprecated`，指向 `suggestion.dart` 中的新实体。
- `card_style.dart`：`TodayCardTone` 枚举新增 `warning` 变体（amber #F59E0B），对应后端 `warning` cardTone 值。

### 验证

- `flutter analyze`（today + network 范围）：0 issues ✅
- `flutter analyze`（全库）：25 个 pre-existing 错误（`waterTargetCount` 缺失参数 + `AuthApi` 方法名不匹配），本次改动无新增错误。

### Phase 1+2 补充：删除兼容代码 + 修复 pre-existing 错误

**删除废弃类型（不兼容旧版本）**：

- `dashboard.dart`：彻底删除 `TodayPriorityItemType` 枚举、`TodayPriorityItem` freezed 类、`TodayDashboard.priorityItems` 字段及其 `@Deprecated` 注解。
- `view_models.dart`：删除 `TodaySuggestionItem` 类、`buildSuggestionItems()` 函数、`_fallbackPriorityItems()` 函数。
- `mock_repository.dart`：删除两处 mock `priorityItems` 数据。
- `lucent_repository.dart`：删除 `priorityItems: const <TodayPriorityItem>[]` 赋值。
- `test/today/page_test.dart`：删除 `priorityItems` 赋值。

**新增 `todaySuggestionProvider`**：

- `lib/features/today/presentation/providers/suggestion_provider.dart`：
  - `todaySuggestionRemoteDataSourceProvider` — 包装 `TodaySuggestionRemoteDataSource`
  - `todaySuggestionProvider` — `FutureProvider<TodaySuggestionBundle?>`，未登录返回 `null`

**UI 重构 — `suggestion_section.dart`**：

- `TodayPrimarySuggestionSection`：参数从 `TodayDashboard dashboard` 改为 `TodaySuggestionCard? suggestion`；使用新域实体的 `cardTone`、`icon`、`evidence`、`boundary`、`primaryAction` 渲染。
- `TodaySecondarySuggestionsSection`：参数从 `TodayDashboard dashboard` 改为 `List<TodaySuggestionCard> suggestions`。
- `dashboard_view.dart`：从 `StatelessWidget` 改为 `ConsumerWidget`，watch `todaySuggestionProvider`，将 `primary` / `secondary` 传递给建议 section。

**修复 pre-existing `waterTargetCount` 错误（10 处）**：

- `user_settings_controller.dart`（5 处）：所有 `UpdateUserSettingsDto` 构造调用补齐 `waterTargetCount: current.waterTargetCount`。
- `assistant/presentation/providers/controller.dart`（1 处）：补齐 `waterTargetCount: current?.waterTargetCount ?? 8`。
- `test/today/test_helpers.dart`、`test/assistant/page_test.dart`、`test/settings/page_test.dart`（2 处）、`test/settings/user_settings_controller_test.dart`（2 处）：所有 `UserSettingsDataDto` 构造调用补齐 `waterTargetCount: 8`。

**修复 pre-existing `AuthApi` 方法名不匹配错误（13 处）**：

- `auth/data/datasources/remote_data_source.dart`：后端控制器拆分后生成的方法名已变，批量替换：
  - `authControllerLoginV1` → `localControllerLoginV1`
  - `authControllerRegisterV1` → `localControllerRegisterV1`
  - `authControllerSendVerificationCodeV1` → `localControllerSendVerificationCodeV1`
  - `authControllerVerifyEmailV1` → `localControllerVerifyEmailV1`
  - `authControllerForgotPasswordV1` → `localControllerForgotPasswordV1`
  - `authControllerResetPasswordV1` → `localControllerResetPasswordV1`
  - `authControllerLogoutV1` → `sessionControllerLogoutV1`
  - `authControllerCreateWechatWebAuthorizeUrlV1` → `oAuthControllerCreateWechatWebAuthorizeUrlV1`
  - `authControllerLoginWithWechatWebV1` → `oAuthControllerLoginWithWechatWebV1`
  - `authControllerLoginWithWechatMobileV1` → `oAuthControllerLoginWithWechatMobileV1`
  - `authControllerLoginWithAppleV1` → `oAuthControllerLoginWithAppleV1`
  - `authControllerCreateQqAuthorizeUrlV1` → `oAuthControllerCreateQqAuthorizeUrlV1`
  - `authControllerLoginWithQqV1` → `oAuthControllerLoginWithQqV1`

**验证**：

- `flutter analyze`（全库）：No issues found ✅

## Today 建议引擎前端接入 Phase 3：Provider 层 + 反馈 + AI 解释 (LUM-2026-0709-13)

### 背景

- Phase 1+2 完成了 API 客户端生成、Domain 实体层、Remote Data Source、废弃代码删除和静态分析修复。
- Phase 3 将建议数据的生命周期管理从简单 `FutureProvider` 升级为 `AsyncNotifier`，并接入反馈 API 和 AI 解释按需加载。

### Provider 层升级

**`lib/features/today/presentation/providers/suggestion_provider.dart`**：

- `todaySuggestionProvider` 从 `FutureProvider<TodaySuggestionBundle?>` 升级为 `AsyncNotifierProvider<TodaySuggestionNotifier, TodaySuggestionBundle?>`。
- `TodaySuggestionNotifier` 管理完整建议生命周期：
  - `build()` — 检查 `authSessionProvider`，未登录返回 `null`；已登录通过 `_fetch()` 拉取后端建议。
  - `submitFeedback({suggestionId, feedback})` — 调用 `POST /today/suggestions/:id/feedback`，`suppress` 反馈自动将 ID 加入 `_dismissedIds`，反馈后自动刷新建议列表。
  - `dismiss(suggestionId)` — 本地 dismiss（加入 excludeIds + 重新拉取，不走 API）。
  - `refresh()` — 手动刷新。
  - `_dismissedIds` — 本地维护已 dismiss 的建议 ID，作为 `excludeIds` 参数传给后端。
- 新增 `suggestionExplanationProvider` — `FutureProvider.autoDispose.family<TodaySuggestionExplanation?, ({String suggestionId, String language})>`，AI 解释按需请求，未登录返回 `null`。

### UI 重构

**`lib/features/today/presentation/widgets/sections/suggestion_section.dart`**：

- `TodayPrimarySuggestionSection`：从 `StatefulWidget` 改为 `ConsumerWidget`，watch `todaySuggestionProvider`，处理 loading/error/data 三态：
  - `data` + `primary == null` → `_SuggestionEmptyState` 空态提示
  - `data` + `primary != null` → `_PrimarySuggestionCard`（ConsumerStatefulWidget，管理证据折叠动画）
  - `loading` → `_SuggestionSkeleton`（骨架占位）
  - `error` → `_SuggestionErrorState`（inline retry）
- `_PrimarySuggestionCard` 新增：
  - `lifecycleState == fading` 时 `Opacity(opacity: 0.6)` 视觉降级。
  - 证据折叠区内新增 `_SuggestionAiExplainButton`（按需触发 `POST /:id/explain`）。
  - 反馈按钮从空回调改为 `_SuggestionFeedbackRow`（ConsumerStatefulWidget），根据 `feedbackOptions` 动态渲染 accepted/later/notApplicable/suppress 按钮，提交中禁用防重复，成功/失败有 `AppToast` 提示。
- `TodaySecondarySuggestionsSection`：从 `StatelessWidget` 改为 `ConsumerWidget`，直接 watch `todaySuggestionProvider`，不再接收外部参数。
- 导航策略更新：`_openRoute()` 按 route 是否含 `?` 决定 `context.push()`（带参数）或 `context.go()`（tab 切换）。

### 页面集成

- `dashboard_view.dart`：`TodayPrimarySuggestionSection` 和 `TodaySecondarySuggestionsSection` 不再接收 `suggestion`/`suggestions` 参数，各自从 provider 取数据。`_MobileTodayDashboard` 和 `_DesktopTodayDashboard` 移除 `primary`/`secondary` 字段。
- `page.dart`：`_refreshDashboard` 重命名为 `_refreshAll`，同时 invalidate `todayDashboardProvider` 和 `todaySuggestionProvider`，`Future.wait` 并行刷新。

### AI 解释设计

- **按需触发**：用户展开证据折叠区后看到「AI 解释」按钮，点击后调用 `POST /:id/explain`。
- **回退安全**：`aiGenerated == false` 或 `explanation == null` 时不替换原始文案，显示触发按钮。
- **错误重试**：AI 解释失败可重试。
- **不阻塞首屏**：首屏渲染不需要 AI 解释，loading 时显示 `FProgress` + 文案。

### 新增 ARB l10n keys

`app_zh.arb` + `app_en.arb` 新增 12 个 key：

- `todaySuggestionAcceptedAction` / `todaySuggestionSuppressAction` — 反馈按钮文案
- `todaySuggestionFeedbackSuccess` / `todaySuggestionFeedbackError` — 反馈 toast 文案
- `todaySuggestionAiExplainAction` / `todaySuggestionAiExplainLoading` / `todaySuggestionAiExplainRetry` — AI 解释按钮文案
- `todaySuggestionEmptyTitle` / `todaySuggestionEmptySubtitle` — 空态文案
- `todaySuggestionLoadingHint` / `todaySuggestionErrorHint` / `todaySuggestionRetryAction` — 加载/错误/重试文案

### 验证

- `flutter gen-l10n` ✅
- `flutter analyze`（全库）：No issues found ✅

## Today 建议引擎前端接入 Phase 4：主卡证据结构化 + 饮水进度条 (LUM-2026-0709-14)

### 背景

- Phase 3 完成了 Provider 层升级、反馈 API 接入和 AI 解释按需加载。
- Phase 4 补全主卡的两个 UX 细节：证据区从单字符串拼接改为结构化逐条展示，以及 `subtype == 'water'` 建议卡的饮水进度条。

### 证据区结构化

**`suggestion_section.dart`**：

- 新增 `_EvidenceList` widget：渲染"依据"标题 + 逐条 evidence item。
- 新增 `_EvidenceItemRow` widget：左 label（mutedForeground，flex 5）+ 右 value（fontWeight.w600，flex 7，右对齐），形成定义列表风格。
- 旧代码将 `card.evidence.map((e) => '${e.label}: ${e.value}').join('\n')` 拼成单个 `_SuggestionMetaBlock`，现在改为 `for (final e in evidence) _EvidenceItemRow(label: e.label, value: e.value)` 逐条渲染。
- `_SuggestionMetaBlock` 保留，仍用于 boundary 区块。

### 饮水进度条

- `TodayPrimarySuggestionSection` 新增可选 `dashboard` 参数（`TodayDashboard?`）。
- `_PrimarySuggestionCard` 新增可选 `dashboard` 参数，当 `card.subtype == 'water'` 且 `dashboard != null` 时，在 reason 文本下方插入 `_WaterProgressBar`。
- `_WaterProgressBar` 使用 Forui `FDeterminateProgress(value: progress)` + 百分比文本，`ClipRRect` 圆角处理。
- `dashboard_view.dart`：mobile 和 desktop 布局均传入 `TodayPrimarySuggestionSection(dashboard: dashboard)`。

### 验证

- `flutter analyze`（全库）：No issues found ✅

### AI 解释按钮改为真正按需触发

- `_SuggestionAiExplainButton` 从 `ConsumerWidget` 改为 `ConsumerStatefulWidget`。
- 新增 `_isRequested` 本地状态：初始为 `false`，仅显示「AI 解释」按钮；用户点击后设为 `true`，此时才 `ref.watch(suggestionExplanationProvider)` 触发 API 请求。
- 修复了旧实现在 widget build 时即自动触发 Dio 请求导致测试中 pending timer 的问题。

### 测试更新

**`test/today/test_helpers.dart`**：

- 新增 `testSuggestionBundle`（const 值）：包含 1 张主卡（compliance/urgent）、1 张次卡（behavior_advice/soft/water）、1 张观察项（coverage/neutral）。
- 新增 `StaticTodaySuggestionNotifier`：返回 `testSuggestionBundle`。
- 新增 `EmptyTodaySuggestionNotifier`：返回 `null`（空态）。
- 新增 `LoadingTodaySuggestionNotifier`：永不完成（loading 态）。

**`test/today/page_test.dart`**：

- 所有 `ProviderScope.overrides` 新增 `todaySuggestionProvider.overrideWith(StaticTodaySuggestionNotifier.new)`。
- "Today suggestion CTAs stay visible" 测试：断言从 `l10n.todayMedicationTakeAction`（旧硬编码文案）改为 `'去确认'`（后端返回的 `primaryAction.label`）。
- 移除不再需要的 `l10n` 变量和 `AppLocalizations.delegate.load` 调用。

### 验证（最终）

- `flutter analyze`（全库）：No issues found ✅
- `flutter test test/today/page_test.dart`：12/12 passed ✅

## Today 建议引擎前端接入 Phase 5：反馈 + AI 解释完善 (LUM-2026-0709-15)

### 背景

- Phase 4 完成了主卡证据结构化展示和饮水进度条。
- Phase 5 完善 Forui API 使用规范、消除硬编码文案、补全测试覆盖。

### Forui API 修复

- `suggestion_section.dart`：AI 解释 loading 状态从 `FProgress()`（线性进度条）修正为 `FCircularProgress.loader()`（圆形进度指示器），符合 forui-docs 的组件语义。`FProgress` 是线性 indeterminate 进度条，不应放在 `SizedBox(width, height)` 中当圆形用。尺寸统一为 `Spacing.level4`。
- `_AiExplainContent` 中的硬编码 `'AI'` 文本改为 `l10n.todaySuggestionAiLabel`（ARB key），符合"无硬编码用户可见文案"规则。

### 新增 ARB l10n key

`app_zh.arb` + `app_en.arb` 新增：

- `todaySuggestionAiLabel` — AI 解释内容区的标签文案（中英文均为 "AI"）

### 测试覆盖

新增 `test/today/suggestion_section_test.dart`（17 个测试）：

**反馈按钮**（4 个）：
- 按 canonical 顺序渲染（accepted → later → notApplicable → suppress）
- `feedbackOptions == null` 时不渲染
- `feedbackOptions == []` 时不渲染
- 英文 locale 下文案来自 ARB

**AI 解释按钮**（5 个）：
- 初始状态显示触发按钮
- loading 状态显示 `FCircularProgress` + 加载文案
- `aiGenerated == true` 显示 AI 增强内容 + AI 标签
- `aiGenerated == false` 回退显示触发按钮
- error 状态显示重试按钮

**空态 & 错误态**（4 个）：
- `primary == null` 显示空态标题和副标题
- bundle 有数据但 `primary == null` 显示空态
- provider error 显示错误提示 + 重试按钮
- loading 状态显示骨架占位

**主卡内容**（4 个）：
- 标题和原因来自后端
- 主按钮 label 来自 `primaryAction.label`
- 展开后显示结构化证据列表
- 展开后显示 boundary 文本

### 验证

- `flutter gen-l10n` ✅
- `flutter analyze`（全库）：No issues found ✅
- `flutter test test/today/`：41/41 passed ✅

## Today 建议引擎前端接入 Phase 6-9：观察项重构 + 测试 + 文档 (LUM-2026-0709-16)

### 背景

- Phase 1-5 完成了 API 客户端、Domain 层、Provider 层、主卡/次卡重构、反馈 + AI 解释。
- Phase 6-9 完成观察项区数据源切换、测试覆盖和文档同步。

### Phase 6: 观察项区重构

**`lib/features/today/presentation/widgets/sections/observation_section.dart`**：
- 数据源从旧 `todayRecommendationsProvider`（`GET /today-analysis/recommendations`）切换到 `todaySuggestionProvider.observations`（`GET /today/suggestions` 的 `observations[]`）。
- `_mapObservation()` 从 `TodaySuggestionCard` 映射到 `_ObservationItem`：
  - `card.icon`（后端字符串）通过 `_iconFor()` 映射为 `IconData`
  - `card.title` → 标题
  - `card.reason` → 副标题
  - `card.confidence` → tag 文案（`high → 去看看`、`medium/low → 仅供参考`）
  - `card.primaryAction.route` → 导航路由（含 `?` 时 `context.push`，否则 `context.go`）
- `_fallbackObservations()` 保留：睡眠缺失时生成 fallback 观察项，与后端观察项合并展示（fallback 在前）。
- 错误重试改为 `ref.invalidate(todaySuggestionProvider)`。
- 最多展示 3 条后端观察项（`observations.take(3)`）。

**废弃旧 API**：
- `recommendations_provider.dart`：`todayRecommendationsProvider`、`todayRecommendationsRemoteDataSourceProvider`、`TodayRecommendationsNotifier` 全部标记 `@Deprecated`。
- `recommendations_remote_data_source.dart`：`TodayRecommendationsRemoteDataSource` 标记 `@Deprecated`。
- 不删除文件（避免大面积编译错误），后续可在独立 PR 中完全移除。

### Phase 8: 测试更新

**`test/today/page_test.dart`**：
- 移除所有 `todayRecommendationsProvider` override 和 `_LoadingRecommendationsNotifier` / `_EmptyRecommendationsNotifier` 类。
- "Today observation section hides spinner while loading" 测试改用 `LoadingTodaySuggestionNotifier`（suggestion provider loading 态）。

**`test/today/ai_card_test.dart`**：
- 移除 `recommendation.dart` 和 `recommendations_provider.dart` 导入。
- 所有 `todayRecommendationsProvider.overrideWith(() => _EmptyRecommendationsNotifier())` 改为 `todaySuggestionProvider.overrideWith(EmptyTodaySuggestionNotifier.new)`。
- 移除 `_EmptyRecommendationsNotifier` 类。

**`test/app/shell_page_test.dart`**：
- 同上，移除旧 recommendation 导入和 notifier 类，改用 `todaySuggestionProvider` + `EmptyTodaySuggestionNotifier`。

**新增 `test/today/observation_section_test.dart`**（8 个测试）：
- 后端观察项正确渲染（标题、副标题、卡片 key）
- fallback 睡眠缺失提示正确显示
- 无观察项 + 无 fallback 时显示空态文案
- provider loading 时不崩溃、不显示 `FCircularProgress`
- provider error 时显示错误提示 + 重试按钮
- fallback + 后端观察项合并展示
- 置信度 tag 从后端 `confidence` 映射为本地化文案

### Phase 9: 文档更新

- `Active_UI_Today.md`：更新主卡反馈按钮、AI 解释、证据结构化、进度条说明；更新观察项数据源说明。
- `TODO.md`：删除 "Today 主动建议卡片后端统一裁决引擎" 条目；蓝图差距盘点 Today 从"延后"改为"✅ 无差距"。
- `Current_State.md`：基线摘要从 "Phase 1-5" 更新为 "Phase 1-9"。
- 追加本条 migration log。

### 验证

- `flutter analyze`（全库）：No issues found ✅
- `flutter test test/today/ test/app/shell_page_test.dart`：56/56 passed ✅
