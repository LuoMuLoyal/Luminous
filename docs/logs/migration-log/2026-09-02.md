# Migration Log — 2026-09-02

## 代码审查收口(09-01 / 08-30 两份 review 文档)

- 背景:按 `plans/Luminous-review-09-01.md` 与 `plans/Luminous-review-08-30.md` 逐项确认真问题并修复,不适合本轮处理的标 TODO。09-01 文件在 1b6bfd54 后发生过目录迁移(soft_icon → control/、trend → preview/),修复按现状路径落位。

### 09-01 review 处置

- #1(critical)+ #6:`review/presentation/pages/page.dart` `_refresh` 三处 `onError: (_) {}` 改为 `talkerProvider` 日志(`reviewCurrent` / `reviewHistory` / `suggestionHistory` refresh failed),并在 `Future.wait` 前注明失败处理约定——page 层不 toast 不重试,错误由各 provider 的 `AsyncValue.error` 投影到对应 section,日志仅保证可观测。#6 的注释诉求随 #1 一并满足。
- #2:同文件 `_readReasonRecordOptions` 的裸 `DateTime.parse(localDateKey(...))` 改为 `parseDateTimeOrNull`,null 时与外层 catch 同口径降级为空选项列表;解析口径(无时区后缀按 UTC)保持不变,仅收敛 FormatException 风险。
- #3(warning,标 TODO):`range_picker_dialog.dart` 11 处 `Navigator.of(...).pop(value)` 确认为 `showModalBottomSheet` / `showFDialog` 结果返回的既有惯用法(await 拿值,功能正确),属 pre-existing 且 lint 处于观察模式;重构弹层关闭封装不在本轮范围,记入 docs/TODO.md。
- #4(部分落地):活动文档 `docs/reference/design-system.md` 的 `auth_form_mixin.dart` 旧路径已更新为 `shared/form_mixin.dart`;archive 两份归档文档(Design_System_Components.md / Project_Guardrails.md)经维护者决定**不修改**——归档文档进入变更集会触发 links check 全量扫描,暴露其既有的两个退役 wikilink(指向 Design_System 与 Design_System_Migration,09ca01f8 迁移遗留),一并处理超出本轮范围。`scripts/docs/links.dart` 的 `exemptRepoPaths` 豁免条目相应**保留**并注明保留原因,遗留事项见 docs/TODO.md。
- #5:新增 `test/review/widgets/dedupe_today_suggestions_test.dart`,覆盖同 key 高状态覆盖、跨 key 不合并、全同 key 合并为 1 条、输出保持首次出现顺序四类契约。
- #7:`AGENTS.md` Testing 节后新增 "Custom Lint Rules" 小节,列出七条规则的问题描述与豁免路径。

### 08-30 review 处置

- W-1(标 TODO):trend 空态语义确认为真实产品语义问题,但 `ReviewTrendSection` 现仅由 legacy preview/dashboard_view 装配,且把 `observedMetric == null` 纳入空态判定会在旧后端 scalar 兼容路径(observedMetric 可空)误伤有数据曲线;记入 docs/TODO.md,待 backend 口径确认后处理。
- W-3:SoftIcon 调用点全仓审计完成——6 处调用(mine 的 status_alerts / notifications_reminders / archive)的 icon 参数静态类型均为 `IconData`,StateError 分支当前不可达;新增 `test/core/widgets/soft_icon_test.dart` 锁定渲染契约(IconData 正常渲染、duotone + duotone data 正常、duotone + 普通 IconData 降级、非 IconData 抛 StateError 且信息含 runtimeType)。
- S-1:`lib/core/database/database.dart` v5 防御性 sqlite_master 检查加 `TODO(cleanup)` 注释(清理条件为在役版本全部达到 v5,注明勿在 v6 提前移除)。
- S-2:核实为无需行动——`lucide_icon_bridge.dart` 的 `_reverseMap` dartdoc 已在 08-29 修复中写明 lazy init 与 O(1) 语义。
- S-3:`ocr_model_manager.dart` `totalSteps` 注释改为澄清「+1 为 config 拷贝步,新增下载项时随 downloads.length 自动增长」。
- S-4:`test/auth/presentation/providers/session_provider_test.dart` 新增 reconnecting 语义组:冷启动 isReconnecting=false、timeout 后重试挂起期 isReconnecting=true(通过 protected state setter 播种 isTimeout 前置态,附理由注释)、非 timeout 手动重试 isReconnecting=false;fake 的 fetchAccount 增加 Completer gate 以断言中间态。

### 验证

- `flutter analyze` 零问题。
- 新增/修改测试(dedupe 契约、soft_icon 契约、session_provider 全组)与 review 页回归测试(page_test / review_view_test / range_picker_dialog_test)全部通过。
- `dart run scripts/docs/verify.dart --warning-only` 无阻断项;design-system.md front-matter `updated` 已随内容修改刷新。

### 处置

- 代码与文档改动停留工作区未提交,由维护者自行提交。
- docs/TODO.md 新增两条遗留(range_picker_dialog 的 no_direct_navigator 豁免评估、trend 零值视觉区分),front-matter 日期同步。
