# 2026-09-15

## feat(record): 症状目录扩至九项并消费后端目录,「其它」支持就地输入

（症状链路优化 Phase 3 客户端侧；计划文档 `plans/2026-09-14-symptom-chain-optimization.md`，执行完毕后删除。）

### 变更

- `domain/constants/symptom_catalog.dart`：`SymptomCode` 扩到九项（+ nausea / cough / fatigue /
  insomnia / other），新增 `SymptomCatalogEntry`（码 + 展示文案）。
- `domain/constants/fast_entry_choices.dart`：症状选项改由 `fallbackSymptomCatalog(l10n)` 生成（本地兜底清单，
  与 Lucent `SYMPTOM_CATALOG_CODES` 同码同序）；新增 `symptomCodeLabel()` 与
  `resolveSymptomChoices(catalog:)`——**顺序与成员资格以后端目录为准，文案优先本地、未知码用后端 label**。
- 新增 `data/providers/symptom_catalog.dart`：`GET daily-records/symptom-catalog`（生成的
  `getSymptomCatalog()`），失败/未登录返回空表。
- `presentation/services/quick_entry_executor.dart`：症状分支改为同步读已缓存的目录（`loadSymptomCatalog()`）再
  按启用码过滤——**录入不等网络**，首屏立刻用本地目录打开 sheet，后端目录在后台刷新、下次打开生效；「其它」的
  `customLabel` 作记录标题并写进 payload。
- `presentation/widgets/dialogs/symptom_quick_entry_sheet.dart`：选中「其它」不立即落库，在网格下方
  **就地展开输入框**（自动聚焦，空值保存置灰，返回可退出）；多选态下「其它」不可选。
- l10n 新增五个症状文案 + `recordSymptomCustomLabel`；生成客户端随之更新
  （`getSymptomCatalog` → `SymptomCatalogResponse`）。

### 验证

- `flutter analyze` 无问题；`flutter test test/record` 530 例通过。
- 新增/更新用例：目录码表扩到九项、`fromWire` 未知码仍返回 null；sheet 的「其它」就地输入（空值禁用保存 →
  输入后返回 `customLabel`）与多选态禁用；page_test 三条症状用例在新 sheet 上验证 payload
  （`{symptom, severity}` / `{symptom, customLabel, severity}`）。

### 记录一处设计取舍

首版让录入 `await` 目录请求，结果测试里 sheet 永不出现——**快速记录不能等网络**。改为同步读缓存 + 后台刷新：
首屏永远来自本地兜底清单（离线可用、无 loading 态），后端目录的增删与顺序在下一次打开时生效。

## refactor(record): 症状设置面收口(严重度四档 + 症状启用改 pill chip)

（症状链路优化 Phase 4。）

### 变更

- `pages/quick_entry_settings.dart` 与 `widgets/dialogs/quick_type_settings_dialog.dart`：默认严重度选项由写死的
  `['mild','moderate','severe']` 改为 `SymptomSeverity.ordered`（含「无法判断」），与快录 sheet 的四档一致。
- 症状启用列表由 Material `FilterChip` 换成共享 `PillChip`（与快录 sheet、review 范围 chip 视觉同源），
  空集仍表示「全部启用」；设置页把选中判断抽成 `_isSymptomEnabled()`。

### 验证

- `flutter analyze` 无问题（`dart fix --apply` 修正了 import 顺序）。
- `flutter test test/record test/settings` 通过。

## docs(plans): 症状链路优化计划执行完毕,移除计划文件

四个阶段（payload 码契约 / 底部 sheet 交互 / 后端目录与「其它」输入 / 设置面收口）已分别提交，计划文件
`plans/2026-09-14-symptom-chain-optimization.md` 与其在 `plans/README.md` 的登记一并删除（有效结论已沉淀到
两仓的迁移日志、`docs/reference/design-system.md` 与 `lib/features/record/README.md`）。

验证：`dart run scripts/docs/verify.dart --verify` 通过（doc-map 引用、链接完整性、front-matter、新鲜度、
readership、feature 覆盖）。

## feat(record): 列表与详情按需消费餐食结论

（餐食分析链路重构第 3 刀：Luminous 消费侧；计划文档在 `plans/` 下，实施完毕后删除。）

### 变更

- **生成物**：`dart run scripts/contract/bootstrap.dart` 重新生成 `generated/lucent_api`，列表项拿到
  `mealHeadline` / `mealCalorieMin|Max|Bucket`（`mealAnalysisCoverage`、`mealShortDescription`、
  `mealTopFoods` 已从契约删除）。
- **领域与数据层**：`DailyRecordItem` 的餐食投影字段换成 `mealHeadline` + 三个热量字段；离线 codec 与
  Lucent data source 映射同步；`MealAnalysisViewData` 与 payload 解析器重写为 v2
  （`calorieRange` / `dishes[{name,source}]` / `items[{rank,kind,polarity,headline,detail}]`），
  `parseMealDishNames` 取代 `parseMealDishDraftNames`（客户端可编辑的只有菜名）。
- **列表/时间线**：条目标题取 `mealHeadline`、第二行取第一条结论的 `detail`，右侧角标位显示**粗化到百位**的
  热量区间「约 500–800 kcal」（`timelineBadgeLabel` 统一本地化，数据层只给数字）；「已分析」不再有角标
  （原来靠 `识别菜品：…` 硬编码中文与估算/已确认角标，均已删除）。
- **详情页**：`MealAnalysisSummaryCard` 重写为三态——分析中给状态与提示、失败给**本地化的原因码文案** +
  「重新分析」入口、已分析给区间卡 + 全部结论（rank 序，`good/watch` 语义色）+ 菜名列表（用户改过的用主色
  标出）与「改菜名不重算」说明。
- **重试**：新增 `retryMealAnalysis`，把记录已有的那张图作为 attachment 重新 PATCH（服务端据此递增
  `sourceRevision` 并重新入队），成功后刷新详情并广播 `DataChangeTopic.dailyRecords`，失败只提示不改状态。
- **删除人工确认**：编辑页的确认开关、详情页的确认按钮与 `meal_confirm_action.dart` 删除；编辑页提交的
  payload 从 `mealInput.recognizedDishes[{rawName}]` 改为 `mealAnalysis.dishes[{name, source:'user'}]`；
  契约与 l10n 中确认相关键（`recordMealConfirm*`）与 v1 分析展示键（coverage / 识别菜品 / 拆解食材 /
  成分表命中 / 营养估算 / 保守估算免责声明）一并删除。
- **l10n**：新增状态 `已分析`、区间文案、结论与菜名小节标题、五个失败原因码文案与重试提示（zh/en 同步）。

### 验证

- `flutter analyze` 无问题。
- `flutter test test/record`（含重写的解析器/视图模型/状态角标/结果卡测试与详情页重试用例）与
  `flutter test test/core/database` 通过。详情页用例同时覆盖重试成功（提交一张图片 + 状态回到分析中）与
  重试失败（入口保留 + 错误提示）两条路径。

## refactor(record): 餐食确认与快速录入收口

（餐食分析链路重构第 6 刀：Luminous 收口；计划文档在 `plans/` 下，实施完毕后删除。）

### 变更

- **必填校验（UI + 领域两道）**：`MealQuickConfirmationDialog` 监听三个输入框，标题/数值/备注/照片全空时
  「确认」置灰不可提交；`MealQuickEntryFlow.saveDraft` 增加 `MealQuickEntryEmptyException`，无
  title/value/note/image（含仅空白字符）时直接拒绝，不再写出空餐食记录。空输入走新文案
  `recordQuickMealEmptyToast`，其余失败仍复用 `recordCreateFailedToast`。
- **确认逻辑合一**：`handleMealQuickAction` / `handleMealQuickActionManual` 合并为单一入口
  `handleMealQuickAction(source: MealQuickEntrySource.camera | manual)`（鉴权守卫、draft 构造、弹窗调用只留一份）；
  `quick_entry.dart` 的单击/长按分别传 `camera` / `manual`，对外行为不变：单击先开相机、取消不写入，长按直接手动录入。
- **清死配置**：`recordFastEntryChoicesFor` 的 `DailyRecordKind.meal` 分支（四餐硬编码选项）删除（meal 实际走
  `/record/create`），对应测试断言同步改为「meal 返回空列表」。
- **清未用 l10n**：删除 `recordMealCountValue`（含 `@` 占位符元数据）与 `recordMealLogging`（zh/en 分片），
  经 `arb_tools merge` + `flutter gen-l10n` 重生成；`recordFastChoiceMealBreakfast/Lunch/Dinner/Snack` 仍被
  `defaultMealTitle` 使用，保留。
- **长按设置文案错配**：`QuickEntryTypeSettingsDialog._ruleText` 去掉 `_ =>` 回落，改为显式穷尽分支 ——
  meal 用 meal 文案，症状/情绪/用药/睡眠各用自身文案，water/vitals/activity/heartRate/weight/note 用新增的
  通用文案 `recordQuickSettingsGeneralRule`，不再把其它类型显示成餐食规则。
- **测试**：新增 `meal_confirmation_test.dart`（空 draft 不可提交、清空预填标题后重新置灰、填写后可提交并落库、
  仅带图可提交、保存失败保留弹窗 + 失败 toast）、`quick_entry_meal_test.dart`（camera / manual 两条路径都弹确认、
  相机取消不写入不弹窗）、`meal_analysis_poller_test.dart`（分析中按初始间隔刷新、成功后复位、失败指数退避至上限、
  settle 后与 dispose 后停止）；`meal_flow_test.dart` 补空 draft / 仅空白 / 仅备注三种 saveDraft 行为；
  `fast_entry_choices_test.dart` 同步 meal 断言。新增 `test/helpers/tiny_png.dart`（1x1 PNG，供必须渲染
  `Image.memory` 的 widget 测试使用）。

### 验证

- `flutter analyze` 无问题（0 issue）。
- `flutter test test/record` 与全量 `flutter test` 通过。
- `dart run scripts/docs/verify.dart --warning-only` 通过。
- `lib/features/record/README.md` 补一条餐食快录不变量（两条路径共用一个入口 + 空记录双闸）。

## chore(contract,analytics): 同步餐食 digest 工具枚举与饮食规则码

（承接 Lucent 侧 `feat(assistant): 餐食分析 digest 工具` 与 `refactor(today-suggestion): 饮食信号改读餐食 facets` 两刀。）

### 变更

- **契约重生成**：`dart run scripts/contract/bootstrap.dart` 重生成 `generated/lucent_api` —— `GET user/capabilities` 的工具名枚举新增 `get_meal_analysis_digest`。生成物仍带 `unknownDefaultOpenApi` 回落，客户端 `_mapCapabilities` 按线上取值取 id，无需改动。
- **规则码白名单**：`kAllowlistedSuggestionRuleCodes` 把 `caffeine_sleep_correlation` 换为 `diet_imbalance`（服务端 `SUGGESTION_RULE_CODE_ALLOWLIST` 同批更换；不在白名单的码会被服务端 400，客户端先本地丢弃）。
- **建议卡图标映射**：`coffee` → `utensils`（服务端 `diet` 子类型配 `utensils`，映射到 `SemanticIcons.recordMeal`），`suggestion_icon_mapping.dart` 与 `observation.dart` 两处一起改；`SemanticIcons.recordCaffeine` 仍被用药安全提示使用，保留。
- **测试同步**：`product_event_service_test.dart` 的白名单码样例与 `suggestion_icon_mapping_test.dart` 的图标断言改为 `utensils`。

### 验证

- `flutter analyze` 无问题（0 issue）。
- 全量 `flutter test` 与 `flutter test test/today test/core` 通过。

## docs(reference): 登记餐食分析契约与约定

（承接 Lucent 的餐食分析 v2（`docs/reference/adr/0020`）与本次客户端收口。）

### 变更

- `docs/reference/openapi-client.md`：生成基线更新为 124 paths / 347 schemas；并登记三条读侧约定 ——
  餐食 v2 在列表里只有投影字段（`mealAnalysisStatus` / `mealHeadline` / `mealCalorieMin|Max|Bucket` /
  `mealAnalysisFailureReason`），完整 `items` / `dishes` 只在详情 `payload`；建议卡 `ruleId` / `subtype` /
  `icon` 在 DTO 里是自由字符串，规则码白名单由客户端 `kAllowlistedSuggestionRuleCodes` 与服务端
  `SUGGESTION_RULE_CODE_ALLOWLIST` 成对维护；助手工具名是枚举（含 `unknownDefaultOpenApi` 回落），
  新增工具必须重生成客户端。

### 验证

- `dart run scripts/docs/verify.dart --warning-only`：
  `No required docs missing`（仅剩 `docs/howto/add-localization.md` 的可选建议）。
- 全量 `flutter test` 与 `flutter analyze` 通过（见前两节）。

## chore(tests): 清理餐食 v1 残留形状与过期命名

（餐食分析链路重构的收尾：清掉列表与详情消费新契约那一刀漏改的测试侧旧形状。）

### 变更

- `test/core/database` 的本地缓存夹具：行 JSON 里的 `mealAnalysisCoverage` / `mealShortDescription` /
  `mealTopFoods` 换成 v2 的 `mealHeadline` / `mealCalorieMin` / `mealCalorieMax` / `mealCalorieBucket`。
- `test/record/page_test.dart` 的夹具参数 `itemMealShortDescription` 改名 `itemMealHeadline`——它本来就喂给
  `mealHeadline`，旧名字会误导后续改动。
- `test/record/data/repositories/lucent_repository_test.dart` 两条用例名里的 `mealShortDescription` / `topFoods`
  改为实际断言的字段（`mealHeadline` / 热量区间）。

### 验证

- `flutter analyze` 无问题；`flutter test test/core/database test/record` 与全量 `flutter test` 通过。
- 全仓（排除归档与迁移日志）不再出现 `mealShortDescription` / `mealTopFoods` / `mealAnalysisCoverage`
  这三个旧字段记号。

## chore(l10n): 清理零引用文案键

（餐食链路收尾时一并清账：文案键源在 `lib/l10n/src` 分片，`app_*.arb` 与 `app_localizations*.dart` 是生成物。）

### 变更

- 删除 410 个「全仓无 `l10n.<key>` 成员访问」的文案键（zh/en 分片同步删，含各自的 `@key` 元数据块）。
- 保留 7 个环境卡片键（`todayChipEnvironment` 与 `todayEnvironment*`）：`plans/2026-08-16-platform-notification-crosscutting-plan.md`
  明确「死字符串保留、0.1.0 后随真实数据源装配，不删不接」。
- 判定口径是**精确成员访问**（`.key`），不是「同名标识存在」：`MineSectionTitle`、`TodayMedicationSummary`
  这类与键同名的类不会把死键保下来；首轮用大小写不敏感口径时，正是同名标识与大小写折叠留下了 2 个真死键，
  随后由 `flutter analyze` 抓出 `healthSyncTimeRange` / `reviewReferenceNotice` 两个**误删**，已按精确口径重做。

### 验证

- `dart scripts/l10n/arb_tools.dart merge` + `flutter gen-l10n`：分片与生成物一致；12 个分片的 zh/en 键集
  仍逐文件同构（仅 zh / 仅 en 均为 0）。
- `flutter analyze` 0 issue——删错任何一个仍被引用的键都会在这里编译失败；全量 `flutter test`（3750 例）
  与 `scripts/workflows/daily.dart` 通过。
- 每个分片都做过「新内容 == 原内容 − 死键」的逐键比对（键序、值、元数据完全一致），不是重新格式化；
  需要恢复任何键时从 git 历史取回即可。

## docs(plans): 09-08~09-14 审查报告核实收口,修复计划落地

（七份 `plans/Luminous-review-09-{08..14}.md` 已删除；逐条核实结论与修复方案沉淀在
`plans/2026-09-15-review-remediation-plan.md`。）

### 变更

- **核实**：五路并行子代理 + 主线程复核，逐条对照 2026-09-15 工作树（HEAD=98e62f4a）判定
  CONFIRMED / PARTIAL / ALREADY-FIXED / FALSE-POSITIVE / STALE。行号一律按当日现状重标，不采信旧报告行号。
- **计划**：新增 `plans/2026-09-15-review-remediation-plan.md`，分 Wave 1（P0/P1：测试 fixture 清理
  已下线 bloodType/emergencyContact、`_submit` 假成功、onLinkWechat 死分支、terms l10n hack、
  avatar/OCR 上传链路、redirect 测试盲区、legacy profile_edit 退役）、Wave 2（P2 正确性收口）、
  Wave 3（P2 注释/测试/流程约定）三批；含执行顺序、验收命令与「明确不做」清单。
- **显式登记误报与已修复项**（计划 §0），防止执行时顺手"补修"：账号死代码三件套已删（8aba4b8b）、
  OAuthButtonRow 五页重复系误报（git -S 证明只进过 login 一页）、pre-push 摘除 flutter test 是
  7ab36dd9 有意决策且 CI 已覆盖全量测试、AuthScaffold 议题被标准页面骨架终结、
  ResponsiveContentFrame.padding 参数早已存在（报告前提错误）等。
- **删除源报告**：`plans/Luminous-review-09-08.md`（git rm，唯一跟踪文件）与其余六份未跟踪报告删除；
  `plans/README.md` Current Plans 登记2026-09-15计划条目。全库检索确认无其他文档引用七份文件名
  （migration-log 中仅引用 09-01/09-05/09-07 三份早前已删报告的历史记录，按 append-only 不改）。

### 验证

- `dart run scripts/docs/verify.dart --warning-only` 通过；计划文件 front-matter 齐备。
- 核实期间只读不写：全部代码改动留待按计划执行，本轮未动任何 lib/test 文件。

## docs(plans): 审查修复计划补扫同类问题(按问题类别全库重扫)

（承接上一条；增补写入 `plans/2026-09-15-review-remediation-plan.md` §5，计划仍在执行中。）

### 变更

- **方法**：把已确认条目抽象成问题模式（静默吞错 / 假成功 / 空回调 / 硬 cast / 硬编码文案 /
  测试真时钟 / 同文案重复键 / 违约反馈组件 / 未登记 TODO 标记），逐类全库 grep 并读上下文判定。
- **新增可修项**：`profile_edit.dart` 只听 `saved` 无 `errorMessage` 分支（假成功同型）；
  l10n 同文案重复键实际 84 组（原计划只处理 auth 3 组，改为一轮带消费点清单的人工审计）；
  medicine `TODO(archive)` ×13 与 `TODO(cleanup)` ×1 未登记 docs/TODO.md；
  测试真实时钟实际分布 16 个文件（原只列 6 处），按"可改/有意真时钟"分类；
  `session_store.dart` 与 `quick_entry_sleep.dart` 的反序列化硬 cast 同类点。
- **同轮记录"命中但不改"**（避免后续为一致性误改）：10 处 `catch (_)` 与 6 处无日志 `catch (e)`
  逐个定性为合理设计（注释说明的降级、错误写入 UI state、rethrow/handler.next、`_fail` 内含
  talker）；`daily_record_json_codec`/`dose_log_cached` 等自产自销 codec 的硬 cast；
  cooldown 与 450ms debounce 的有意真时钟测试；`sse.dart` 的 Stream cast 非数据 cast。
- **同轮确认干净**：`Text('中文')` 硬编码 0 命中；lib/ 无 `print`（仅 assert 包裹的 debugPrint）；
  空回调仅 security_center 一处、SnackBar 违约仅 avatar_draft 一处（均已在原计划内）。

### 验证

- `dart run scripts/docs/verify.dart --verify` 与 `dart run scripts/docs/links.dart` 通过；
  计划文件复检后章节编号连续（§0-§7）、prettier 无 diff。
- 本轮仍为只读扫描 + 文档写入，未改任何 lib/test 代码。

## doc-map 覆盖映射机制整体退役(观察期满)

- 观察期结论(2026-08-31 起两周窗口,至 2026-09-15 届满):pre-commit 已降级 report-only,
  期间确认 `scripts/docs/verify.dart --verify`(结构)+ `scripts/docs/links.dart`(正文路径)
  覆盖原 doc-touch 保护价值,按 docs/TODO.md 口径整体退役,该条目已删除。
- `scripts/docs/verify.dart`:删除映射报告/阻断路径(loadDocCoverageConfig、
  buildDocCoverageReport、renderDocCoverageReport、collectChangedFiles、SKIP_DOC_CHECK 旁路)
  与 `--staged`/`--config` 参数;默认模式仅保留 front-matter 新鲜度通报(不阻断),
  `--warning-only` 收窄为默认模式别名;`--verify` 保留链接完整性/front-matter/新鲜度,
  readership 改为纯"被其他文档链接"判定,feature 覆盖改为 `lib/features/*/README.md` 存在性。
- `scripts/docs/coverage.dart`:删除 DocCoverageConfig/Rule/Report/Match、YAML 配置解析、
  报告渲染、`defaultDocCoverageConfigPath`、doc-map 孤儿检查与 `findUncoveredFeatureDirs`
  code-glob 版本(改为 README 存在性谓词注入)。
- `docs/doc-map.yaml` 删除(git rm);pre-commit 移除 `--staged --warning-only` 报告步骤
  (迁移日志 append-only 阻断与链接完整性阻断保持);daily 去掉冗余的 `--warning-only` 步骤。
- AGENTS.md(文档规则与命令口径)、docs/README.md、docs/TODO.md、docs/explanation/project-governance.md、
  docs/howto/add-new-feature.md(删除 doc-map 登记步骤)、plans/README.md 同步;
  `test/scripts/docs_verify_test.dart` 重写为保留功能用例(含 README 覆盖判定与链接式 readership)。

### 验证

- `dart run scripts/docs/verify.dart --verify` 通过(链接完整性/front-matter/新鲜度/readership/
  feature README 覆盖);`dart run scripts/docs/links.dart` 通过(51 文件无坏链);
  `flutter analyze` 无问题;`flutter test test/scripts/` 全绿;改动文件 `dart format` 无 diff。

## fix(settings): 健康档案单字段保存失败不再提示「已保存」

（修复计划 `plans/2026-09-15-review-remediation-plan.md` §2-2，执行中。）

### 变更

- `mine/presentation/providers/health_edit_forms.dart`：`HealthProfileFormNotifier.save` 由
  `Future<void>` 改为 `Future<bool>`——成功返回 true，失败仍写 `state.errorMessage` 并返回 false，
  让调用方能区分「已落库」与「写失败」。
- `settings/presentation/pages/profile.dart`：`_HealthProfileCard._submit` 按返回值分支——成功提示
  `mineEditSavedToast` 并触发 `onChanged()`；失败提示新增的 `mineEditSaveFailedToast`，不再提示成功。
  原先无条件调用 `onChanged()`（内部固定弹「已保存」），断网或 4xx 时用户会以为改动已保存。
- l10n 分片新增 `mineEditSaveFailedToast`（zh「保存失败，请稍后重试」/ en），
  经 merge + `flutter gen-l10n` 生成。
- `test/mine/.../health_edit_forms_test.dart`：补断言 save 成功/失败各自的返回值。
- `test/settings/profile_avatar_test.dart`：补端到端用例——注入写失败的 repository，从身高行打开
  sheet 并确认提交，断言出现失败提示且**不出现**「已保存」。

### 验证

- `flutter analyze` 无问题；`flutter test test/settings test/mine` 通过（1 例既有 skip）。
- 回归有效性反向验证：临时把 `_submit` 还原为无条件提示成功，新用例如期失败
  （找到 1 个「已保存」文本）；恢复修复后通过。
- 复核订正：计划 §5-1 原判定 `mine/.../profile_edit.dart` 缺 `errorMessage` 分支，实为该文件
  88-100 行早已处理两者，属补扫期窗口读窄导致的误报，已在计划中改记为误报（该文件确实无需改动）。

## refactor(auth): 微信绑定回调改可空,消灭不可达的空分支

（修复计划 `plans/2026-09-15-review-remediation-plan.md` §2-3，执行中。）

### 变更

- `auth/presentation/pages/account_identity.dart`：`LinkedIdentitiesSection.onLinkWechat` 由必填改为
  可空，渲染条件收紧为 `showWechatLink && onLinkWechat != null`——隐藏入口的宿主页不再需要传一个
  永远不可达的空回调，死分支在组件内被消掉。
- `auth/presentation/pages/account_manage_sections.dart`：删掉 `onLinkWechat: () async { /* TODO */ }`
  空实现，改传 `null`（该页 `showWechatLink: false`，本就不可达）。
- 未改动 `security_center.dart` 的真实现：按 `docs/TODO.md`「微信入口 UI 隐藏、底层流程保留」的既有决策，
  恢复入口只需改回显示参数，因此真回调继续保留、按钮仍隐藏。
- `test/auth/ui_widgets_test.dart`：新增 `LinkedIdentitiesSection` 两条用例——传回调时渲染按钮且可触发、
  传 null 时不渲染按钮（把"空回调"这一形态钉死在组件契约上）。

### 验证

- `flutter analyze` 无问题；`flutter test test/auth test/mine` 全绿。
- 既有隐藏入口用例（`account_settings_page_test.dart` 的「hides WeChat identity link entry」）继续通过，
  确认单侧改动未影响 `showWechatLink: false` 的可见性行为。

## test: 清理已下线契约字段的测试 fixture

（修复计划 `plans/2026-09-15-review-remediation-plan.md` §1-1，执行中。）

### 变更

契约收敛时删除的 profile 字段曾在 8 处测试 fixture 里留存。wire DTO 对未知键宽松解析、不报错，
测试因此一直是绿的，但 fixture 已与真实响应脱节——是"改了契约但测试没跟着收敛"的静默漂移。

- JSON fixture（health_context / settings 数据源测试，含一处 JSON 字符串）删除已退场字段，
  改补当前契约的 `activityLevel` / `dietaryPreferences`，让新字段的解码路径真正被覆盖。
- 3 处 `missingCoreProfileFields` 的占位值改为后端真实会返回的缺口字段；取值口径以 Lucent 侧
  `CORE_PROFILE_FIELDS`（birthDate / sexAtBirth / heightCm / unitSystem）为准——原先凭 DTO 生成物
  拟定的占位值并不在该集合内，核对后端 types 后纠正。
- 删除对已退场字段的恒真断言（该字段已不存在于 payload，断言恒成立、无覆盖价值）。
- clinic summary 的 profile 段按其后端 schema 收敛，去掉该段不具备的字段。

### 验证

- `flutter analyze` 无问题；涉及目录 `flutter test` 全绿（health_context / settings / mine / record / review）。
- 全仓检索确认这两个已下线字段在 lib/test 中零残留。

## fix(auth): 条款提示改为独立文案键,去掉模板裁剪与语言硬编码

（修复计划 `plans/2026-09-15-review-remediation-plan.md` §2-3，执行中。）

### 变更

登录/注册页底部的「创建即表示同意《服务条款》与《隐私政策》」此前是把整句模板传两个空占位符渲染，
再正则裁掉结尾的「与/and」，连接词又按 `l10n.localeName.startsWith('zh')` 硬编码——模板改一个字就渲染错，
注册页更因完全没裁剪而渲染出悬空的「与」。

- `lib/l10n/src/auth_{zh,en}.arb`：拆出 `authTermsAgreementPrefix`、`authLoginTermsAgreementPrefix`、
  `authTermsConjunction` 三个独立键，各语言各自成句；删除因此失去引用的 `authLoginTermsAgreement` 整句键。
- `login.dart` 删除正则裁剪与 `localeName` 判断，前缀和连接词直连渲染。
- `register.dart` 同样改直连（顺带修掉悬空「与」），勾选框的无障碍标签继续用 `authTermsAgreement` 整句键
  ——那是需要完整可读文本的正当用法。
- `widgets/shared/branding.dart` 是同一 hack 的第三份拷贝（计划中未记录），一并收敛。
- `docs/reference/localization.md` 补规则：需要「前缀 + 富文本链接 + 连接词」拼接时拆独立键，
  不要传空占位符再裁尾或按 locale 硬编码连接词。
- `test/auth/ui_widgets_test.dart` 补两条用例，钉住前缀/连接词/两个链接各自渲染，且 en locale 下用英文连接词。

### 验证

- `flutter analyze` 无问题；`flutter test test/auth` 全绿。
- 流程按分片 → merge → `flutter gen-l10n` 走完，两个 locale 的生成结果均确认无误。
