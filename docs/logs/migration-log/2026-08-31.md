# Migration Log — 2026-08-31

## 目录结构整理(批次 1c 起)日志位置说明

- `plans/2026-08-30-directory-structure-cleanup.md` 的批次 1a/1b 实际执行于 8 月 30 日晚,其条目记录于 `2026-08-30.md`,位置正确。
- 批次 1c 起实际执行于本日;因该计划条目此前沿用 `2026-08-30.md` 追加(迁移日志只追加不回改),批次 1c/1d/2/3 的条目仍在该文件内,时间线以各条目内 commit hash 为准。自本日起,本计划的后续批次条目记录于本文件。

## 目录结构整理批次 4:core/network 分组

- 范围:`lib/core/network/` 直下裸文件按职责分组,目标 network/ 直下裸文件 ≤5(达成,余 2)。
- 分组清单(git mv 保历史):
  - `client/`(HTTP 执行栈):`dio_client.dart`、`client_providers.dart`、`session_store.dart`、`sse.dart`、`base_url.dart`、`retry_policy.dart`、`trace_context.dart`,及 `interceptors/` 子目录整体平移(auth/error/retry/trace 4 文件)。`client_providers.g.dart` 为 gitignored 生成物,随源文件平移(非 git mv),`part` 声明同目录继续有效。
  - `contract/`(响应与错误合同):`response_body.dart`、`problem_details.dart`、`error_code.dart`、`result_code.dart`、`error_mapper.dart`、`api_paths.dart`。
  - 根保留:`api.dart`(barrel 留根,仅更新 export 路径,barrel 消费者零改动)、`map_utils.dart`(纯 Map/响应体强制转换工具,非合同类型也非执行栈,被 client 与 datasource 两侧引用,留根最贴切)。
- 引用更新:
  - barrel `api.dart` export 全部改为 client//contract/ 相对路径并保持字母序。
  - 全库 grep `package:luminous/core/network/` 深引用(排除 `api.dart`),逐一替换为新路径,共 148 个 dart 文件(lib/test/integration_test);同批修正因 client/ 前于 contract/ 字母序变化产生的 17 处 `directives_ordering` 失序。
  - test/core/network/ 测试 import 全部更新,测试文件名未动(含 `target_error_contract_test.dart`)。
  - docs 同步:`docs/02-reference/data-layer.md`(路径 + client/contract 分组说明)、`docs/02-reference/OpenApi_Client.md`、`docs/00-current/Lucent_Contract_Snapshot.md` 的路径文字与 front-matter `updated`;历史日志与 `plans/` 未动。
- 验证:`flutter analyze` No issues found;`flutter test` 全量通过(All tests passed,~5 skip 为既有标记,与本批无关)。

## 目录结构整理批次 5+6:错层归位、规则固化与收尾

- **批次 5 错层归位**(git mv 保历史,类名与测试文件名不动):
  - `features/settings/utils/version_check.dart` → `features/settings/domain/services/version_check.dart`(纯函数 semver 比较,归 domain/services);原 `settings/utils/` 目录清空删除。引用更新:`settings/presentation/pages/about.dart` 与 `test/settings/version_check_test.dart`(仅 import)。
  - `features/record/data/quick_entry_preferences.dart` → `features/record/data/datasources/quick_entry_preferences.dart`(SharedPreferences 数据源,归 datasources)。引用更新 lib/test 共 19 个文件(仅 import)。
  - 因新路径字母序变化,7 个引用方同步修正 `directives_ordering` 失序。
- **批次 6a 规则固化**:AGENTS.md「File Naming Rules」追加第 8 条——直下源文件(不含生成文件)超过 10 的目录应分子目录,豁免须登记于迁移日志。
- **残留检查结论**(git grep 限定 lib/test/integration_test):`_dao.dart`/`_utils.dart` 旧 import、旧 core 路径(logger/theme/shortcuts/icon_tokens/router helpers/command_palette)、design 旧平铺路径(semantic_colors/semantic_color_palette)、common widgets 与 network 旧直引、record `constants.dart`、search `view.dart`、本批两文件旧路径——全部零命中;feature 去前缀项仅剩 `domain/entities`、`data/mappers`、`presentation/providers` 下合法同名 `medicine_detail.dart` 引用。`core/network/map_utils.dart` 为批次 4 已登记的留根豁免(见上文批次 4 条目)。顺带同步 `lib/features/health_context/README.md` 中一处旧文件名文字(`medicine_detail.dart` → `pages/detail.dart`)。
- **豁免与观察项登记**(对应 AGENTS 第 8 条):`settings/presentation/pages`(审计时 14 文件)暂不分组——设置子页面为叶子页,平铺可辩护;8–10 文件观察目录(阈值内不触发):`record/presentation/widgets/sections`、`medicine/presentation/widgets/risk`、`auth/presentation/pages`、`mine/presentation/widgets/sections`、`medicine/presentation/widgets/reminder`、`auth/data/datasources/wechat`,仅观察,下次触碰时再评估。
- **验证**:`flutter analyze` No issues found;`flutter test` 全量通过(All tests passed,~5 skip 为既有标记,与本批无关);`dart run scripts/check_doc_coverage.dart --warning-only` 通过(迁移日志补齐后迁移日志类提示消除);计划文档 `plans/2026-08-30-directory-structure-cleanup.md` 执行批次 0/1a–1d/2/3/4/5/6 已全部勾选。

## 文档治理改进计划 Phase 0:存在性审计与归档(plans/2026-08-31-doc-governance-overhaul-plan.md)

- 全量四问审计(基线 44 篇,排除 adr/migration-log/04-archive),裁决表填入计划 §11:归档 17、并入后归档 6(Phase 2 执行)、存活 22(较基线 -50%)。
- 归档(git mv 保历史,目标 `docs/04-archive/2026-08-31-doc-governance/`):`_MOC`、`Active_Mobile_UI`、`Active_UI_*` 五篇、`Current_State`、`Desktop_UI`、`Runtime_Snapshot`、`Lucent_Contract_Snapshot`、`Mock_Or_Deferred`、`Next_Plan`、`architecture-upgrade-analysis`、`Auth_Forui_Migration_Pattern`、`Design_System_Migration`、`how-to/run-tests.md`。
- 断言承接:延后清单与用药安全后续并入 `docs/00-current/TODO.md`(新增 2026-08-31 分节);Deferred 注释约定与平台 fake 注入约定并入 AGENTS Testing;run-tests 命令面并入 AGENTS Commands;Active_* 数值细节将随 Phase 2 feature README 下沉(承接表见计划 §11)。
- 检查器同步:`doc_coverage.dart` front-matter 必填去 quadrant、readership 改为路径选择、新增目标目录(explanation/product/reference/howto/logs)模式与 explanation|product 陈旧告警豁免;`check_doc_coverage.dart` 新鲜度告警跳过 archive;`tooling_workflows.dart` 迁移日志删行阻断兼容未来 `docs/logs/` 布局。
- `docs/doc-map.yaml` 清零已归档文档引用(17 条规则的 docs_any_of/docs_info 清理,规则 code glob 不变)。
- AGENTS.md 文档规则段缩减:删除 Current state 子文件规则、冻结新增手写"现状叙事"文档、front-matter 描述去 quadrant、Commands 吸收 run-tests 命令面、Testing 吸收 mock/Deferred 约定。
- 存量链接修复:adr/0005 指向 Lucent ADR-0012 的跨仓路径(Lucent 已重建为 `docs/reference/adr/`);docs/README、TODO 与归档文档间因移动产生的路径式 wikilink 改 basename 式(README 等非唯一名除外)。
- 验证:`dart run scripts/check_doc_links.dart` 全库 150 文件通过;`check_doc_coverage.dart --verify` 通过;`flutter analyze` No issues found。

## 文档治理改进计划 Phase 1:指令单一来源

- CLAUDE.md 独有约束上收 AGENTS.md:Commands 补 `flutter pub get`/`arb_tools merge`/`gen-l10n`;Architecture 补新代码位置边界(禁入 legacy `lib/pages|stores|viewmodels|components`)与五 Tab 命名;State Management 补 `select` 切片建议;Routing 补 typed routes;Design System 补 barrel `design.dart` 与 UI 反模式三条(状态视图/骨架/Toast);新增 Non-Negotiable Boundaries 段(技术栈、AI 边界、直接修复不扩散、sibling 仓、生成物禁手编)。
- `CLAUDE.md`/`GEMINI.md` 改为 `@AGENTS.md` 一行 include + ≤2 行工具专属说明;`.github/copilot-instructions.md`(无 include 机制)改为指向 AGENTS.md 的单一来源声明,删除陈旧版本快照(Flutter 3.44.0/Dart 3.12.0)与 `tool/` 验证路径。
- 验收:grep 三个指针文件规则正文残留 0 命中(`tool/run_daily_checks`、版本快照、docs/00-current 规则);AGENTS.md 成为仓库唯一规则正文。

## 文档治理改进计划 Phase 2.1:docs 去编号重建

- `git mv` 平移(保留历史):`Glossary`→`reference/`;`TODO`→docs 根;`Project_Governance`+`AI_Development_Workflow`→`explanation/`;product 四篇→`product/`;reference 八篇→`reference/`;adr 九篇→`reference/adr/`;how-to 三篇→`howto/`;`MigrationLog`+`migration-log/`→`logs/`;`04-archive/`→`archive/`。空编号壳(00-current/01-product/02-reference/03-logs)移除。
- 并入后归档 6:`Work_Phase_Guide`→Project_Governance;`Product_AI_Design`→Product_MVP_Scope(AI 能力边界节);`Product_Tab_Component_Blueprint`→IA(语义边界,组件细节待 Phase 2.4 feature README 承接);`Design_System_Components`→Design_System;`Project_Guardrails`→AGENTS(Phase 0)+OpenApi_Client(Contracts 段);how-to/`README`→docs/README 索引。
- 索引重建:`docs/README.md` 重写为唯一索引(目录规则+存活文档清单+放置规则);`MigrationLog` 瘦身为"目录即索引"(79→24 行)。
- 存量矛盾处理:"ADR-0009"实指 adr/0006(data-layer、routing);data-layer/routing/state-management 误标 ADR-0008 实为 adr/0005(fpdart/Result 边界);Security Elevation 段删除(state-management);Glossary Security PIN 词条删除;architecture Melos 段删除;ADR-0006 内部死引用改指实施位置;how-to 两篇直编 app_*.arb 矛盾重写为分片工作流(lib/l10n/src 12 分片×2 locale);Localization 611→51 行(键清单是 gen-l10n 投影,ARB 分片为真相);ROADMAP ADR-0011/0012 漂移编号修正为 0007/0008。
- 检查器收束:doc-map.yaml 17 规则路径全量更新;`doc_coverage.dart`/`check_doc_coverage.dart`/`tooling_workflows.dart` 删除旧布局双写模式,收束为单布局;`test/scripts` 两个测试文件夹具同步新布局(37/37 通过)。
- 联动同步:AGENTS/README/CHANGELOG/CONTRIBUTING/ROADMAP/lib l10n AGENTS/today README/core design README/docs .obsidian app.json 路径更新;plans/ 7 个活跃计划文件路径映射;Lucent contracts 仅 assistant-safety 存活于 `Lucent/docs/reference/`,其余 15 篇归档,README 合同段改为指向存活件+归档说明;README 五 Tab 名 `report`→`review`(运行时已迁移)、OpenAPI 源路径改 `Lucent/docs/reference/generated/openapi.json`。
- 验证:`check_doc_links.dart` 全库 150 文件 0 断链;`check_doc_coverage.dart --verify` 全绿;`dart analyze scripts` 0 issues;grep 活跃面旧编号路径残留 0(豁免:迁移日志历史条目、`docs/archive/`、`research/` 调研快照、本计划文件)。
