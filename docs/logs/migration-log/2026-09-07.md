# Migration Log — 2026-09-07

## refactor(record): detail.dart 按领域拆分为 12 个文件

超大页面重构（`record/presentation/pages/detail.dart` 975 行）：按业务领域拆为
页面入口 / body 编排 / 轮询控制器 / action usecase / 信息行 / 头像 / 徽章 / 卡片
容器 / 图片 / 水进度 / 骨架屏 / 工具函数，共 12 个文件。新建 `widgets/detail/`
子目录容纳 presentation 层原子组件，`controllers/` 增加轮询器，`utils/` 增加工具
函数。所有私有 widget 按需公开化（去掉 `_` 前缀），行为与文案零变化。

> 拆分依据：`plans/2026-09-07-large-file-domain-splitting-plan.md` §2.1

验证：`dart analyze lib/features/record/` 零 error；
`test/record/detail_page_test.dart` 通过（8 用例全绿，`RecordDetailPage`
保持原路径，`RecordDetailBody`/`DetailRows` 等公开类由测试间接验证）。
`widgets/detail/` 目录 8 个文件（<10 上限），`controllers/` 增 1 文件，
`utils/` 增 1 文件。

## refactor(review): clinic_summary_preview_dialog.dart 按领域拆分为 5 个文件

超大对话框重构（819 行）：拆为主状态类 / 字段选择面板 / 分享流程 / 错误视图 /
字段枚举映射。新建 `utils/clinic_summary_field_mapping.dart` 接收原 dialog 内的映射
函数；`_ShareStep` 枚举与分享面板一并公开化拆出。

> 拆分依据：`plans/2026-09-07-large-file-domain-splitting-plan.md` §2.3

验证：`dart analyze lib/features/review/` 零 error；
`test/review/clinic_summary_share_fields_test.dart` import 同步更新至新 utils
路径后通过；`test/review/clinic_summary_provider_test.dart` 通过（26 用例全绿）。
`dialogs/` 目录现 6 个文件（<10 上限）。

## refactor(assistant): conversation.dart 按 mixin 模式拆分为 7 个文件

超大 Provider 重构（`assistant/presentation/providers/conversation.dart` 922 行）：
AssistantController 承载 8 个职责领域，采用 Dart mixin 模式拆分。
6 个 mixin 文件（capabilities/streams/sending/regenerate/management/proposals）
各含一个 mixin with Notifier<AssistantState>，通过 with 混入主类。
主文件 81 行（定义层 + 初始化 + mixin 合并 + provider 声明）。
`build_runner` 重新生成 freezed 产物；补充 4 个 mixin 文件缺失的 import
（fpdart/models/domain repository）。

> 拆分依据：`plans/2026-09-07-large-file-domain-splitting-plan.md` §2.2

验证：`dart analyze lib/features/assistant/presentation/providers` 零 error；
`test/assistant/` 全部通过（204 用例全绿）；
`providers/` 目录 8 个文件（<10 上限）。

## refactor(auth): account_settings_sections.dart 按 section 拆分为 4 个文件

超大页面组件重构（609 行）：12 个独立 section 组件按功能聚合拆为
account_identity / account_security / account_sessions 三个新文件。
原文件保留骨架 + AccountSettingsLoading + re-export 所有 section。
修复 `identityProviderLabel` re-export 遗漏（helpers 文件引用需 re-export）。

> 拆分依据：`plans/2026-09-07-large-file-domain-splitting-plan.md` §3.3

验证：`dart analyze lib/features/auth/presentation/pages` 零 error；
`test/auth/` 全部通过（274 用例全绿）。

## refactor(auth): shell.dart 按响应式形态拆分为 3 个文件

超大认证布局组件重构（492 行）：拆为 AuthShell 入口（52 行）
+ desktop_shell（255 行）+ mobile_shell（171 行）。

> 拆分依据：`plans/2026-09-07-large-file-domain-splitting-plan.md` §3.3

验证：`dart analyze lib/features/auth` 零 error；
`test/auth/widgets_test.dart` 通过。

## refactor(assistant): page_body.dart 按领域拆分为 4 个文件

超大页面组件重构（`assistant/presentation/widgets/sections/page_body.dart` 603 行）：
拆为主编排组件 / 编排器上方提示 / 编排器宿主 / 空状态组件，共 4 个文件。
`sections/` 目录现 5 个文件（<10 上限）。

> 拆分依据：`plans/2026-09-07-large-file-domain-splitting-plan.md` §2.4

验证：`dart analyze lib/features/assistant/presentation/widgets/sections` 零 error；
无直接测试引用，验证通过。

## refactor(record): create.dart 表单组件拆分

超大页面组件重构（`record/presentation/pages/create.dart` 552 行）：提取
表单 UI 组件 `RecordCreateForm`（纯展示层）到 `widgets/forms/record_create_form.dart`（172 行）。
原文件保留状态管理/业务逻辑/页面骨架。

> 拆分依据：`plans/2026-09-07-large-file-domain-splitting-plan.md` §3.1

验证：`dart analyze lib/features/record/` 零 error；
`test/record/` 全部通过（502 用例全绿）。

## refactor(record): sidebar.dart 按组件拆分为 3 个文件

超大组件重构（`record/presentation/widgets/sections/sidebar.dart` 549 行）：
拆为日历面板（`calendar_panel.dart`）+ 筛选面板（`filter_panel.dart`）+
移动端筛选（`mobile_filter.dart`）。原文件改为 re-export 聚合入口（7 行）。

> 拆分依据：`plans/2026-09-07-large-file-domain-splitting-plan.md` §3.1

验证：`dart analyze lib/features/record/presentation/widgets/sections` 零 error；
`test/record/` 全部通过。

## refactor(record): lucent.dart 仓库工具函数提取

超大 Repository 重构（`record/data/repositories/lucent.dart` 573 行）：
将私有映射函数（`toDaySummary`/`toTimelineEntry` 等）提取到
`data/utils/record_mappers.dart`（242 行），
静态 mock 数据提取到 `data/utils/record_static_data.dart`（208 行）。
主类保留骨架与两个 @override 方法（113 行）。

> 拆分依据：`plans/2026-09-07-large-file-domain-splitting-plan.md` §3.1

验证：`dart analyze lib/features/record/data` 零 error；
`test/record/` 全部通过。

## refactor(medicine): detail + drugbox + safety + risk 拆分

超大 medicine 组件批量拆分：
- `pages/detail.dart`（531 行）→ 69 行 + `medicine_detail_content.dart` + `medicine_detail_shared.dart`
- `widgets/sections/mobile_drugbox.dart`（506 行）→ 瘦身 + 新增子组件文件
- `widgets/sections/mobile_safety.dart`（504 行）→ 瘦身 + `safety_cards.dart` + `safety_metrics.dart`
- `widgets/risk/overview_tab_section.dart`（497 行）→ 瘦身 + `overview_states.dart` + `overview_recommendation.dart`

> 拆分依据：`plans/2026-09-07-large-file-domain-splitting-plan.md` §3.2

验证：`dart analyze lib/features/medicine` 零 error；
`test/medicine/` 全部通过（373 用例全绿）。
同步修复 `check_tab_content.dart` / `mobile_dashboard_view.dart` 等调用方 import。

## refactor(today): dashboard_view.dart 按响应式拆分为 4 个文件

超大仪表板组件重构（`today/presentation/widgets/views/dashboard_view.dart` 612 行）：
拆为入口（58 行）+ `mobile_dashboard.dart` + `desktop_dashboard.dart` + `health_event_section.dart`。
修复 `SignInHintBanner`/`context.theme` 缺失 import（`forui` + `page_state.dart`）。

> 拆分依据：`plans/2026-09-07-large-file-domain-splitting-plan.md` §3.4

验证：`dart analyze lib/features/today` 零 error；
`test/today/` 全部通过（311 用例全绿）。

## refactor(today): lucent.dart 仓库工具函数提取

超大 Repository 重构（`today/data/repositories/lucent.dart` 636 行）：
将私有映射函数（`_observedMetric`/`_degradedObservedMetric` 等）提取到
`data/utils/today_mappers.dart`（239 行），主文件 import 调用。

> 拆分依据：`plans/2026-09-07-large-file-domain-splitting-plan.md` §3.4

验证：`dart analyze lib/features/today/data` 零 error；
`test/today/` 全部通过。

## refactor(settings): notification.dart 提取偏好与工具

超大 Provider 重构（`settings/presentation/providers/notification.dart` 630 行）：
将本地偏好封装提取到 `notification_preferences.dart`，
工具函数提取到 `notification_utils.dart`。
清理主文件 unused import 和 `_formatTime` 声明。

> 拆分依据：`plans/2026-09-07-large-file-domain-splitting-plan.md` §3.5

验证：`dart analyze lib/features/settings` 零 error；
`test/settings/` 全部通过。

## refactor(scan): box_scan.dart 拆分处理器与组件

超大扫码页重构（`scan/presentation/pages/box_scan.dart` 531 行）：
提取识别回调/校验逻辑到 `utils/box_scan_handlers.dart`（334 行），
方法选择卡片到 `widgets/box_scan_preview.dart`（56 行）。
主文件保留入口 `showMedicineBoxScanSheet` + 拍照编排（186 行）。
对话框重试回调改为 `Future<void> Function()` 参数规避 handlers ⇄ box_scan 循环导入。

> 拆分依据：`plans/2026-09-07-large-file-domain-splitting-plan.md` §3.6

验证：`dart analyze lib/features/scan` 零 error；
`test/scan/` 通过。

> 说明：`barcode_scanner.dart`（645 行）子任务中途失败，保留原状待后续处理；
> 子 agent 遗留的未接线 `scanner_handlers.dart`/`scanner_preview.dart` 已删除。

## refactor(health_data): health_record_mapper.dart 辅助函数提取

超大 Mapper 重构（`health_data/data/mappers/health_record_mapper.dart` 524 行）：
将私有辅助函数（`_toMetricType` / `_extractNumeric` / `_waterInMilliliters` /
`_unitForType` / `_externalId` / `_kindForMetric` / `_formatDate` / `_formatTime` /
`_fieldsForMetric`）和两个私有数据类（`_SleepAggregator` / `_BloodPressureDataPoint`）
提取到 `data/mappers/health_record_mapping.dart`（~400 行），公开化为顶级函数和类。
主文件保留 `HealthRecordMapper` 类骨架 + 两个公开方法 + metric 构造私有方法
（`_metricFromPoint` / `_pairBloodPressure` / `_bloodPressureMetric`，~165 行）。

验证：`dart analyze lib/features/health_data` 零 error；
`test/health_data/mapper_test.dart` 通过（20 用例全绿）。

超大页面组件重构（`assistant/presentation/widgets/sections/page_body.dart` 603 行）：
拆为主编排组件 / 编排器上方提示 / 编排器宿主 / 空状态组件，共 4 个文件。
`sections/` 目录现 5 个文件（<10 上限）。

> 拆分依据：`plans/2026-09-07-large-file-domain-splitting-plan.md` §2.4

验证：`dart analyze lib/features/assistant/presentation/widgets/sections` 零 error；
无直接测试引用，验证通过。

## refactor(auth): login.dart 按领域拆分为 5 个文件

超大登录页重构（651 行）：OAuth 五个平台的 URI 构建 / start+complete 操作 /
回调 hook / 导航辅助函数 各自独立为文件；页面骨架保留 `login.dart`（~250 行），
仅保留表单 UI、提交、链接、条款。

> 拆分依据：`plans/2026-09-07-large-file-domain-splitting-plan.md` §2.5

验证：`dart analyze lib/features/auth/` 零 error；
`test/auth/login_page_test.dart` 通过（`LoginPage` 保持原路径）；
`test/auth/register_page_test.dart` 通过。

> 目录豁免：`auth/presentation/pages/` 直接源文件达 15 个，超出 AGENTS.md
> 10 文件上限。其中 4 个（`oauth_navigation/oauth_uris/oauth_handlers/
> oauth_callback_hook`）为 login 页 OAuth 辅助函数（非独立页面），
> 5 个（`account_settings_helpers/account_settings_sections/account_identity/
> account_security/account_sessions`）为 account settings 页辅助模块；
> 实际独立页面 6 个（`login/register/forgot_password/change_email/
> account_settings/sessions`）。豁免登记见本日志。

## refactor(mine): current_medicine_edit.dart 拆分为 3 个文件

超大编辑页重构（`mine/presentation/pages/current_medicine_edit.dart` 441 行）：
- 页面保留路由入口 + 状态编排（表单控制器/预填/保存/删除/会话门控，232 行）
- 表单 UI 提取到 `widgets/current_medicine_form.dart`（剂量/频率/时间输入三组
  FCard + 保存/删除按钮 + `QuickSelectChip` 公开化，219 行）
- 保存输入构建与日期工具提取到 `utils/current_medicine_handlers.dart`
  （`buildMedicineCreateInput`/`buildMedicineUpdateInput`/`tryParseMedicineDate`/
  `formatMedicineDate` 纯函数，54 行）

行为与文案零变化；私有 `_QuickSelectChip` 公开化为 `QuickSelectChip`。
`widgets/` 目录现 3 个直接源文件、`utils/` 现 2 个（均 <10 上限）。

验证：`dart analyze lib/features/mine` 零 error；
`test/mine/` 用例全部通过（`CurrentMedicineEditPage` 保持原路径，测试未改动）。


## refactor(mine): archive.dart 按区块拆分

超大区块重构（mine/presentation/widgets/sections/archive.dart 430 行）：
- 条目交互（ArchiveRow/ArchiveRecordListTile/ArchiveRecordItem）拆出到
  widgets/sections/archive_row.dart（348 行）
- 空态与元信息（ArchiveEmpty/buildArchiveProfileMeta）拆出到
  widgets/sections/archive_sections.dart（75 行）
- 默认编辑路由映射接入既有 utils/archive_handlers.dart
  （fallbackArchiveRoute，与原 _fallbackRouteFor 逐项一致）
- 主文件保留 MineArchiveSection 入口 + 状态编排（49 行）

私有类全部公开化（去掉 _ 前缀），行为与文案零变化；
dashboard_view.dart 等调用方无需改动（MineArchiveSection 保持原路径）。

验证：dart analyze lib/features/mine 零 error；sections/ 目录现 10 个
直接源文件（等于上限，无超限）。测试未改动。

> 拆分依据：plans/2026-09-07-large-file-domain-splitting-plan.md §3.8

## refactor(scan): barcode_scanner.dart 拆分结果弹层与角标绘制

超大扫码页重构（`scan/presentation/pages/barcode_scanner.dart` 687 行）：
- 结果弹层 `_ScanResultSheet` 公开化为 `BarcodeScanResultSheet`，
  提取到 `widgets/scan_result_sheet.dart`（189 行，含已添加状态派生逻辑）
- 扫描角标绘制 `_ScanCornerPainter` 公开化为 `ScanCornerPainter`，
  提取到 `widgets/scan_corner_painter.dart`（59 行）
- 主文件保留相机初始化/权限/生命周期/检测编排/build（410 行）

行为与 UI 零变化；调用方测试 `test/scan/barcode_scanner_page_test.dart` 未改动。

验证：`dart analyze lib/features/scan` 零 error；`test/scan/` 用例全部通过。

## refactor(record): quick_entry_panel / edit / quick_entry_settings 拆分

三个 P1 record 超大页面拆分：

**quick_entry_panel.dart**（560→164 行）：
- 提取 `QuickEntryHeader`→`quick_entry/header.dart`（74 行）
- 提取 `QuickEntryMetrics.resolve()` + `_lerpDouble`→`quick_entry/metrics.dart`（46 行）
- 提取 `QuickRecordGrid`/`QuickRecordTile`/`QuickBadge`/`QuickRecordNoteButton`→`quick_entry/grid.dart`（294 行）
- 主文件保留 `RecordQuickEntryPanel` + state 的偏好应用/badge 逻辑
- 解决 `sections/` 目录文件数超限（12→引入 `quick_entry/` 子目录）

**edit.dart**（513→369 行）：
- 提取 `RecordEditLoading`/`RecordEditStatusHint`/`popEditOrGoHome`/`confirmDiscardEdit`→`widgets/edit/edit_parts.dart`
- 主文件保留页面编排 + form 逻辑

**quick_entry_settings.dart**（464→385 行）：
- 提取 `pickQuickEntryIcon`/`showMoodBadgeSelectDialog`→`utils/quick_entry_settings_dialogs.dart`
- 主文件保留所有设置段 UI

验证：`dart analyze lib/features/record` 零 error；
`flutter test test/record/` 502 用例全绿。

## 大文件拆分计划收口

`plans/2026-09-07-large-file-domain-splitting-plan.md` 全部批次完成，计划文件已删除。

两项评估结论：
- `auth/datasources/auth.dart`（633 行）：单一实现类，方法共享私有状态，mixin 拆分复杂度高收益低，不拆分
- 跨 feature 共享组件上提 `core/`：copy.dart 三 feature 各自独立（绑定自身 domain 枚举），非重复代码；无需上提

## feat(review): Review 页重构 P0-1 周期开关（周|月）

Review 页重组计划（`plans/2026-09-07-review-page-restructure.md`）P0-1 落地：在
review 首屏顶栏下首行新增周|月周期切换，复用 forui `FTabs`（`FTabControl.lifted`
受控 + 两个 `FTabEntry`）。

- 新增 `presentation/widgets/sections/period_switch.dart`（`ReviewPeriodSwitch`）：
  受控 FTabs，映射 `last7Days`/`last30Days`，默认周；tab label 走 l10n
  （zh「周」「月」/ en「Week」「Month」）。
- `page.dart` 装配：周期切换回调写到 `reviewDashboardSelectedQueryProvider.setRange`；
  复用既有 `reviewLastDashboardProvider` 缓存——`ref.listen` 把每次 dashboard 成功
  结果写入缓存，切换期间用 `effectiveDashboardAsync` 承接缓存旧数据 + 轻量加载态
  （不整页骨架）。
- `review_view.dart`：新增 `periodRange`/`onPeriodRangeChanged` 参数并渲染周期开关；
  趋势区 `selectedQuery` 由硬编码 last7Days 改为跟随所选周期。
- 语义顺序：周期开关 `Semantics(container, sortKey 0.5)` 置于内容首行，与既有
  Task 9 a11y 约定对齐（不破坏 标题→状态→四段→历史→More 相对顺序）。

验证：`flutter analyze` 零 issue；`flutter test test/review` 全绿（新增
`period_switch_test.dart` 4 用例 + page_test 2 个周期开关用例：state 映射 /
切换期间缓存旧数据不整页骨架）。

## feat(review): Review 首屏新增覆盖率概览行

Review 页重组计划 P0-2 落地：在周期开关下方新增覆盖率概览行
（`presentation/widgets/sections/coverage_strip.dart`，`ReviewCoverageStrip`），
每健康维度一张紧凑小卡（图标 + 维度名 + 有记录天数/范围天数 + 最新值 + 变化
方向），横向可滑动。

- 覆盖语义接 `ReviewMetric.observedMetric`：无 observed 或 coverage=none 或
  不足 2 天记录 → 灰态「数据太少」，值区显示 `--`，不输出趋势结论口径。
- `review_view.dart` 新增 `coverageMetrics` 参数并在周期开关/预览横幅下渲染
  概览行；`page.dart` 从 `effectiveDashboardAsync` 的 metrics 传入。
- 每卡可点（`FTappable` + `onTap(ReviewDataKind)`）——预留切下方趋势维度
  （P0-4 维度联动），本期卡片默认只读。
- l10n：新增 `reviewCoverageSparseLabel` / `reviewCoverageDaysCount` /
  `reviewCoverageStripLabel`（zh/en 同步）。

验证：`flutter analyze` 零 issue；`flutter test test/review` 全绿（新增
`coverage_strip_test.dart` 4 用例：空列表不渲染、天数计数、灰态、点击回调；
page_test 覆盖概览行随指标渲染）。

## feat(review): Review 首屏新增值得注意区

Review 页重组计划 P0-3 落地：在覆盖率概览行下方新增值得注意区
（`presentation/widgets/sections/noteworthy.dart`，`ReviewNoteworthySection`），
呈现当前周期内有数据来源的值得注意变化。

- 数据：`dashboard.findings`（title/body/kind/icon/color），最多展示 2 张结构化
  卡；每卡 = 类型图标 + 标题 + 正文 + 数据窗口（`startDate → endDate`）。
- 无 findings → 弃权占位一行（「这段时间没有新增值得注意的变化。」），不生成长文。
- `findings` 空且 dashboard 整体 insufficient 的记录引导判定属 P0-5；反馈/证据
  引用字段属 Wave 2，本期不做假按钮。
- `review_view.dart` 新增 `findings`/`findingsWindowStart`/`findingsWindowEnd`
  参数并在概览行下渲染；`page.dart` 从 `effectiveDashboardAsync` 的 findings
  与窗口传入。
- l10n：新增 `reviewNoteworthyTitle` / `reviewNoteworthyAbstainLabel` /
  `reviewNoteworthyWindow`（zh/en 同步）。

验证：`flutter analyze` 零 issue；`flutter test test/review` 全绿（新增
`noteworthy_test.dart` 3 用例：弃权占位、单卡标题/正文/窗口、混合集截断前两
卡）。

## feat(review): 单维趋势卡收口——覆盖脚注与维度联动

Review 页重组计划 P0-4 落地（改 `widgets/sections/preview/trend.dart`）：

- 承接 ① 覆盖率概览行的维度点击 + 趋势卡自身维度 chips 切换（沿用 FTabs）：
  `ReviewTrendSection` 新增 `selectedKind`/`onKindChanged` 受控参数，提供时用
  `FTabControl.lifted` 同步外部选中维度，否则沿用内部 `FTabControl.managed`；
  页面层经 `reviewTrendDimensionProvider` 持有选中维度，覆盖概览行小卡
  (`onTap`) 与趋势卡 chips 双向联动，不破坏 legacy 段（dashboard_view /
  dashboard_preview 仍走非受控）。
- 覆盖脚注：图示值下方新增一行覆盖率说明（该维度 `observedMetric`：X/N 天 +
  数据窗口 `startDate → endDate`），不再只把覆盖塞进表头；主路径 `showRangePill`
  `false` 已去除旧「7/30 天」范围 pill 残留。
- 空档口径：图表下方固定一行「未记录的天不计入走势。」（随 Wave 2 逐日覆盖数据
  后可移除），标注逐日空档不被当作观测值参与趋势结论。
- l10n：新增 `reviewTrendGapNote`（zh/en 同步）；复用
  `reviewTrendCoverage`/`reviewTrendCoveragePartial` 与 `reviewNoteworthyWindow`。

验证：`flutter analyze` 零 issue；`flutter test test/review` 全绿（新增
`trend_section_test.dart` 4 用例：覆盖脚注+窗口+口径、主路径无范围 pill、受控
外部维度切换、legacy 非受控首维度；page_test 维度联动随 provider 状态切趋势
维度）。

## feat(review): 无进行中事件改为记录引导冷启动态

Review 页重组计划 P0-5 落地：无任何 active/ended 事件时，不再提供
「开始观察」动作（事件动作收口 Today），改按「冷启动」呈现记录引导。

- 判定（`page.dart` 内联 `isColdStart`）：当前周期 dashboard metrics 为空，或
  无任何 `observedMetric` 达到足量覆盖（coverage 非 none 且 ≥2 天），且趋势
  序列为空 → 冷启动。`coldObserved` 取各维度 observedCount 最大值，
  `coldExpected` 按周/月（7/30）计算。
- 冷启动时渲染记录引导卡（新增
  `widgets/sections/record_guide.dart` `ReviewRecordGuideSection`）：顶部
  「本周已记录 X/N 天」+ 「去记录」按钮；按钮经页面层 `onGoRecord` 走
  `context.go(Routes.record)` 切 record tab。数据尚足但无事件时降级为轻量解释
 卡（`_NoEventExplanationCard`，去按钮）。
- `review_view.dart` 移除 `onStartObservation`/`_StartObservationCard` 按钮
  接线，新增 `onGoRecord`/`isColdStart`/`coldObserved`/`coldExpected` 参数。
- `page.dart` 删除 `_openStart` 及其 `_readCurrentMedicineOptions`/
  `_readReasonRecordOptions` 预读与相关 import（start-event/health-context/
  record 关联项不再在 review 侧读取）。
- l10n：新增 `reviewRecordGuideTitle` / `reviewRecordGuideDescription`
  (observed/expected) / `reviewRecordGuideGoRecordAction`（zh/en 同步）。

验证：`flutter analyze` 零 issue；`flutter test test/review` 全绿（review_view、
semantics、golden、page_test 同步为冷启动/记录引导口径；`record_guide` 随
widget 断言覆盖）。

## feat(review): 事件头部收口为被动展示，动作移入 Today

Review 页重组计划后续波次：`EventHeaderSection` 事件头部改为纯被动信息呈现
（标题/状态 chip/已进行天数/起止/用药/今日确认一行或「去今日 check-in」浅链接），
移除 `showCheckInAction`/`showEndAction`/`onCheckIn`/`onEndEvent` 四个旧的
check-in/end 按钮参数，新增必填 `onGoTodayCheckIn`（页面层 `context.go` 切 today
tab）。`ReviewView` 相应移除 `onCheckIn`/`onEndEvent`、新增 `onGoTodayCheckIn`。
详情页 `detail.dart` 同步改为只读 no-op 接线。旧的 `review-check-in-action` /
`review-end-event-action` 按键全部删除。

测试同步：`event_header_test` / `review_semantics_test` / `review_view_test` /
`review_golden_test` / `detail_page_test` 构造参数与断言改为今日确认行 / 去
check-in 浅链接口径；`page_test` 删除依赖旧按键的「active 头部 check-in/end 调
用 repository」两个用例及其 `_FakeHealthEventRepository` 夹具与 health_event
import。

验证：`flutter analyze` 零 issue。

## feat(review): 建议历史与 AI 摘要从主路径下线

Review 页重组计划波次：`ReviewView` 与 `page.dart` 移除主路径上的
`ReviewSuggestionHistorySection` / `ReviewAiSummarySection` 装配、对
`suggestionHistoryProvider` 的 watch 与 `dedupeTodaySuggestions` 调用、详情
sheet 入口，以及 `page.dart` 的 `_changeAiSummaryRange` 方法与 AI 摘要
aiSummaryState/range/enabled/generate 接线；移除相关 unused import。

代码文件不删除：`sections/suggestion_history.dart`、
`sections/ai_summary.dart`、`dialogs/suggestion_history_detail_sheet.dart`
文件头补 deferred 标注（Review Page Restructure P0-7），并在 `docs/TODO.md`
记录复核项——三者仍被 legacy dashboard 兼容页（`legacy_dashboard_compat.dart`
/ `dashboard_view.dart`）消费，故非孤儿；`aiSummariesEnabled` 设置语义不变，
Today 摘要仍受其门控。

测试同步：`test/review/page_test.dart` 移除 `suggestionHistoryProvider` 的
override 设置与随之多余的 suggestion import。

验证：`flutter analyze` 零 issue；`flutter test test/review` 全绿；docs
verify（warning-only）无阻碍项（migration-log 覆盖告警在本文追加后消除）。

## feat(review): 骨架对齐新区块顺序并改写预览预告卡

Review 页重组计划波次：`skeleton_view.dart` 骨架屏从旧的「事件头卡 + 四段 + AI
总结卡 + 建议历史卡 + 历史卡」改为镜像新区块顺序（周期切换行 → 覆盖率概览 →
值得注意 → 单维趋势 → 事件回顾），移除已下线主路径的 AI 摘要/建议历史占位，未
删除任何有保留价值的代码。

`review_view.dart`：单维趋势卡前移到事件历史之前（配合验收标准「趋势 → 事件
历史」）；preview（未登录）预告卡由「开始观察后示例」五条改为「记录后你会看到
什么」四条内容项（覆盖率概览 / 值得注意的变化 / 单维趋势 / 事件回顾），登录
入口仍由独立 `SignInHintBanner` 承担，卡片本身不含登录动作。

l10n：`review` 分片删除旧 preview 十条（WhatHappened/KeyChanges/
CompletedActions/NextStep/AiSummary），新增四条新预览项（覆盖/值得注意/趋势/
事件回顾，zh/en 同步）；`reviewPreviewOverviewTitle` 改为「记录后你会看到什么」。

验证：`flutter analyze` 零 issue；`flutter test test/review` 全绿。

## fix(today): health_event_section 中 catch 静默吞异常添加 talker 日志

`health_event_section.dart` 的 `_readCurrentMedicineOptions` 和 `_readReasonRecordOptions` 两处 `catch (_)` 改为 `catch (e, st)`，并添加 `ref.read(talkerProvider).error(...)` 日志，符合项目错误处理规范。异常信息不再静默丢失，便于定位"为什么这张卡片没列出我的药/记录"。

验证：`dart analyze lib/features/today/presentation/widgets/views/health_event_section.dart` 零 issue；`flutter test test/today/` 全绿。

## fix(review): period_switch 非法 range 兜底到 last7Days

`period_switch.dart` 中若 `selectedRange` 为 `custom` 或其他不在 `_ranges` 内的枚举值，`indexOf` 返回 `-1`，此前会把 `FTabControl.lifted` 推入非法状态。现在兜底为第一个 tab（`last7Days`），避免 UI 闪烁或 forui 内部异常。

验证：`dart analyze lib/features/review/presentation/widgets/sections/period_switch.dart` 零 issue。
