# Migration Log — 2026-09-09

## 09-07 review 清单收口

对应 `plans/Luminous-review-09-07.md` 剩余项收口（W-1~W-4 与 S-2~S-5 已于 09-08 修复提交落地，本节补齐 S-1/S-6/S-7 评估与 W-1 日志消息优化）。

- **W-1（today 健康事件关联候选读取）**：`fix(today)` 已于 09-08 落地（两处 `catch (_)` → `catch (e, st)` + `talker.error`）；本轮优化日志消息由通用 `_readXxxOptions` 占位符改为具体方法名 `_readCurrentMedicineOptions` / `_readReasonRecordOptions`，便于按来源过滤与排查。
- **S-1（noteworthy 标题 typography）**：评估无需修复。`core/design` 设计系统已于 2026-08-30 退役 `TypographyToken`/`TypographyRoles`（见 `lib/core/design/README.md`），当前 `body.md.copyWith(w700)` 为全仓 review 区块统一写法（41 处）；若后续 design 引入 role token 再统一收口，本项不追加新 token。
- **S-6（lucent_dashboard typedef）**：无需额外登记。`docs/logs/migration-log/2026-09-04.md` §「数据仓库长生成枚举类型别名收口」已对 `lucent_review.dart`/`lucent_dashboard.dart` 的文件级 typedef 做法记录；`lucent_dashboard.dart` 文件头注释 `2026-09-03 审查 #4 纯可读性收口`与该节一一对应。
- **S-7（desktop_shell/mobile_shell if-else）**：在 `docs/TODO.md` 2026-08-23 认证迁移段追加条目，将两 shell 文件布局 if-else 与 `AuthPageHeader`/`AuthFormPanel` 装配顺序的结构重复登记为「风格级暂不抽取，下个 auth/presentation 重构迭代统一收口」（与既有 `_resolve` 三处重复同型）。

验证：`flutter analyze` 零 issue；`test/today` 全绿。

