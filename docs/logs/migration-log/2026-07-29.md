# 2026-07-29 迁移日志

## 用药模块 — 审查问题修复

### 修复 1：提醒表单 firstWhere 崩溃防护

- **文件**: `lib/features/medicine/presentation/widgets/reminder/form_body.dart`
- **变更**: `FSelect.rich` 的 `format` 回调中 `firstWhere` 改为 `firstWhereOrNull` + `?.displayName ?? ''`
- **原因**: 药品被删除或标记为非当前后，`selectedMedicineId` 仍保留旧值，`firstWhere` 会抛出 `StateError` 导致 widget 构建崩溃

### 修复 2：fetchWorkspace getRecords() 错误降级

- **文件**: `lib/features/medicine/data/repositories/lucent_workspace.dart`
- **变更**: `riskCheckRepository.getRecords()` 调用包裹 try-catch，失败时记录日志并降级为 `null`（`riskCheckRecords` 字段已为 nullable）
- **原因**: 同方法中 dose logs 和 reminders 都有独立的 try-catch，`getRecords()` 是新增的网络依赖但缺少错误保护，网络失败会导致整个用药仪表盘白屏

### 修复 3：风险检查时间格式化跨天显示

- **文件**: `lib/features/medicine/presentation/widgets/shared/copy.dart`
- **变更**: `medicineRiskCheckFormatTime` 增加可选 `now` 参数，当检查时间不是今天时显示 `MM-dd HH:mm` 而非仅 `HH:mm`
- **原因**: 跨天检查只显示 "14:30" 会让用户误以为是今天的检查。`mobile_safety.dart` 的 `isStale` 守卫不完全覆盖此问题（`stale` 由后端在上下文变更时设置，"not stale" ≠ "today"）
- **影响调用点**: `check_tab_content.dart:197`、`mobile_safety.dart:133` — 均无须修改（`now` 可选参数默认 `DateTime.now()`）
- **验证**: `flutter analyze` 通过（3 个修改文件）

## 工具链优化

### tool/ → scripts/ 目录迁移

- 将 `tool/` 下所有 11 个脚本文件迁移到 `scripts/`（与 `arb_tools.dart` 统一）
- 更新所有引用：`.githooks/*`、CI workflows、`AGENTS.md`、`CLAUDE.md`、`CONTRIBUTING.md`、`README.md`、`ROADMAP.md`、`pubspec.yaml` melos scripts
- 测试文件 `test/tool/` → `test/scripts/`，更新 import 路径

### 删除 golden 测试和 alchemist 依赖

- 删除 `test/golden/` 目录（`auth_widgets_golden_test.dart`）
- 从 `pubspec.yaml` 移除 `alchemist: ^0.14.0`
- CI 移除 `--exclude-tags golden` 参数
- `.gitignore` 移除 `test/golden/goldens` 和 `test/golden/failures` 条目
- `CONTRIBUTING.md` 移除 Golden Tests 章节

### P0: 文档修正 + 项目卫生

- `CONTRIBUTING.md` — Patrol 描述替换为标准 `integration_test` 用法
- `CONTRIBUTING.md` — pre-push 描述从 "no-op" 修正为 "runs flutter analyze and dart format --set-exit-if-changed"
- `CLAUDE.md` — 代码生成路径统一为 `dart run scripts/bootstrap_generated_sources.dart`，移除 `cd generated/lucent_api && dart run build_runner build` 单独命令
- 删除 6 个残留日志文件（`flutter_01.log` 等）
- 删除 `parse_coverage.py`（与 `coverage/calc_coverage.py` 重复）
- `.vscode/launch.json` — 删除重复的第二个 launch configuration

### P1: CI 加速

- `flutter-ci.yml` — 增加 `actions/cache@v4` pub packages 缓存
- `flutter-ci.yml` — `cancel-in-progress` 改为 `${{ github.event_name == 'pull_request' }}`
- `flutter-ci.yml` — APK 构建改为仅 `push: main` 时执行
- `luminous-cd.yml` — 增加 pub packages 缓存

### P2: Lint / Analysis 增强

- `analysis_options.yaml` — 启用 `cancel_subscriptions` 和 `close_sinks`（零报错）
- `analysis_options.yaml` — 启用 `discarded_futures`，修复全部 107 个 fire-and-forget Future 调用（用 `unawaited()` 包裹）
- `analysis_options.yaml` — 启用 `avoid_void_async`（Dio interceptor 的 `void async` 方法改为 `Future<void>`）
- `analysis_options.yaml` — 启用 `parameter_assignments`（`auth_interceptor.dart` 的 `err` 参数赋值改为局部变量 `currentErr`）
- `analysis_options.yaml` — 启用 `use_decorated_box`（`suggestion_state_views.dart` 的 `Container` + `decoration` 改为 `DecoratedBox`）
- 创建 `dart_test.yaml`（concurrency: 4, timeout: 5m）
- 创建 `build.yaml`（排除 `generated/lucent_api/**`）
- `l10n.yaml` — 增加 `synthetic-package: false`
- `pubspec.yaml` — `fluwx: 5.7.5` 增加注释说明精确版本锁定原因

### P3: 移除 Melos + VS Code + bootstrap 并行

- 从 `pubspec.yaml` 移除 `melos` dev_dependency 和 `melos:` 配置块
- `README.md` — 移除 melos 命令引用
- `.vscode/extensions.json` — 增加 `redhat.vscode-yaml` 和 `github.vscode-github-actions`
- `scripts/bootstrap_generated_sources.dart` — 使用 `Future.wait` 将 client build 和 app codegen 并行化

## 架构精炼 Phase 1：Core 提升

### session.dart 迁移到 core/auth/

- **新增**: `lib/core/auth/session_state.dart` — `AuthSessionState`（freezed）、`AuthSessionStateStatus` extension、`AuthRequiredException`、`pendingAuthSessionResolution`
- **新增**: `lib/core/auth/session_provider.dart` — `AuthSessionNotifier`、`authSessionProvider`（re-export `session_state.dart`）
- **变更**: `lib/features/auth/presentation/providers/session.dart` → 纯 re-export 文件（export core/auth/ 两个文件）
- **删除**: `lib/features/auth/presentation/providers/session.freezed.dart`（由 build_runner 在新位置重新生成）
- **全局**: ~95 个文件的 import 路径从 `features/auth/presentation/providers/session.dart` 更新为 `core/auth/session_provider.dart`

### required_dialog.dart 拆分迁移

- **新增**: `lib/core/widgets/auth/required_dialog.dart` — `AuthRequiredDialogGate` widget、`showAuthRequiredDialog` 函数（纯 UI，无 auth 路由依赖）
- **变更**: `lib/features/auth/presentation/widgets/shared/required_dialog.dart` → re-export core 文件 + 保留 `pushAuthRequiredRoute`、`loginRouteForReturnTo`、`loginRouteForCurrentLocation`（依赖 auth routes，不可迁移到 core）
- **全局**: 使用 `showAuthRequiredDialog` 或 `AuthRequiredDialogGate` 且不使用路由辅助函数的文件，import 路径更新为 `core/widgets/auth/required_dialog.dart`；同时使用路由辅助函数的文件保持从 auth feature 导入

### 验证

- `flutter analyze`: 0 error, 0 warning（84 个 info 级 directives_ordering 为 import 排序提示）
- `flutter test test/auth/ test/core/`: 867 passed, 1 failed（`layout_tokens_test.dart` 预存失败，与本次修改无关）
- `flutter test test/core/providers/auth_guarded_test.dart test/auth/required_dialog_test.dart test/auth/session_gate_test.dart`: 29 passed, 0 failed

## 架构精炼 Phase 2：Record Feature 示范重构（步骤 2.1-2.4）

### Step 2.1：Application 层创建

- **新增** `lib/features/record/application/usecases/change_record_date.dart` — 日期变更 use case
- **新增** `lib/features/record/application/usecases/quick_entry.dart` — 快速入口调度器（dispatcher）
- **新增** `lib/features/record/application/usecases/quick_entry_meal.dart` — 饮食快速入口流程
- **新增** `lib/features/record/application/usecases/quick_entry_sleep.dart` — 睡眠快速入口流程（含合并对话框、开始选择对话框）
- **新增** `lib/features/record/application/usecases/quick_entry_medication.dart` — 用药快速入口流程（含选择对话框、无药品提示）
- **新增** `lib/features/record/application/orchestrators/nlp_flow.dart` — NLP 输入面板编排
- **新增** `lib/features/record/presentation/widgets/dialogs/meal_confirmation.dart` — 饮食确认对话框（从 page.dart 抽出）
- **新增** `lib/features/record/presentation/widgets/dialogs/sleep_merge.dart` — 睡眠合并摘要行组件（从 page.dart 抽出）

### Step 2.2+2.3：page.dart 瘦身 + header_actions

- **重写** `lib/features/record/presentation/pages/page.dart` — 从 1290 行瘦身到 232 行（82% 缩减）
- **新增** `lib/features/record/presentation/widgets/header_actions.dart` — 从 page.dart 抽出 header actions 构建

### Step 2.4：timeline.dart 拆分

- **新增** `lib/features/record/presentation/widgets/sections/timeline_item.dart` — TimelineDot, TimelineCard, TimelineImageThumbnail, TimelineImageFallback
- **新增** `lib/features/record/presentation/widgets/sections/timeline_drag_handler.dart` — TimelineDragFeedback
- **瘦身** `lib/features/record/presentation/widgets/sections/timeline.dart` — 从 734 行瘦身到 ~190 行

### 验证

- `flutter analyze`: 0 error, 0 warning
- `flutter test test/record/`: 472 passed, 0 failed

## 架构精炼 Phase 2：Record Feature 示范重构（步骤 2.5-2.6）

### Step 2.5：edit.dart form sections 抽出

- **新增** `lib/features/record/presentation/widgets/forms/meal_confirm_action.dart` — 餐食分析确认按钮 widget（从 edit.dart 内联抽出）
- **新增** `lib/features/record/presentation/widgets/forms/edit_actions.dart` — 保存/删除操作按钮 widget（从 edit.dart 内联抽出）
- **新增** `lib/features/record/presentation/widgets/forms/pending_image.dart` — `PendingDailyRecordImage` 类 + `resolveImageContentType` + `allowedImageContentTypes`（从 edit.dart 私有实现抽出，可与 create.dart 后续共享）
- **瘦身** `lib/features/record/presentation/pages/edit.dart` — 从 709 行瘦身到 ~590 行，移除内联 FButton 确认/保存/删除按钮和私有类型

### Step 2.6：detail.dart edit/delete 编排抽出

- **新增** `lib/features/record/application/usecases/record_detail_actions.dart` — `deleteRecord` use case（确认对话框 + repo.delete + provider invalidation + toast + pop）+ `editRecord` use case（auth-required route push）
- **瘦身** `lib/features/record/presentation/pages/detail.dart` — 从 634 行瘦身到 ~560 行，移除内联 `_deleteRecord` 方法和 `pushAuthRequiredRoute` 直接调用
- **瘦身** `lib/features/record/presentation/pages/edit.dart` — `onDelete` 方法简化为直接调用 `deleteRecord` use case（popCount: 2），移除内联确认对话框和删除编排逻辑
- 编辑页删除按钮不再管理 `deleting` loading 状态（`FCircularProgress`），与详情页删除按钮行为一致

### 验证

- `flutter analyze`: 0 error, 0 warning
- `flutter test test/record/`: 472 passed, 0 failed

## 架构精炼 Phase 3：跨 Feature 耦合拆解

### Step 3.1：DoseLogRepository domain 接口

- **新增** `lib/features/medicine/domain/entities/dose_log.dart` — `DoseLogItem`、`DoseLogStatus` 从 data 层提取到 domain 层
- **新增** `lib/features/medicine/domain/repositories/dose_log.dart` — `DoseLogRepository` 抽象接口
- **变更** `lib/features/medicine/data/datasources/dose_log_remote.dart` — 从定义实体改为 re-export domain 实体
- **变更** `lib/features/medicine/data/datasources/dose_log_cached.dart` — `CachedDoseLogDataSource` 实现 `DoseLogRepository`，新增 `doseLogRepositoryProvider`

### Step 3.2：ReminderRepository domain 接口

- **新增** `lib/features/medicine/domain/entities/reminder.dart` — `MedicineReminderItem`、`ReminderDeliveryItem`、`MedicineReminderWriteInput` 从 data 层提取到 domain 层
- **新增** `lib/features/medicine/domain/repositories/reminder.dart` — `ReminderRepository` 抽象接口
- **变更** `lib/features/medicine/data/datasources/reminder_remote.dart` — `MedicineReminderRemoteDataSource` 实现 `ReminderRepository`，re-export domain 实体
- **变更** `lib/features/medicine/data/providers/workspace.dart` — 新增 `reminderRepositoryProvider`

### Step 3.3：LucentTodayRepository 构造函数重构

- **变更** `lib/features/today/data/repositories/lucent.dart` — 构造函数参数从 `CachedDoseLogDataSource` 改为 `DoseLogRepository`，从 `MedicineReminderRemoteDataSource` 改为 `ReminderRepository`，import 从 data 层改为 domain 层

### Step 3.4：today_suggestion provider 组装层重构

- **变更** `lib/features/today/data/providers/today_suggestion.dart` — 通过 `doseLogRepositoryProvider` 和 `reminderRepositoryProvider` 注入 domain 接口

### Step 3.5：record 跨 feature 耦合消除

- **变更** `lib/features/record/application/usecases/quick_entry_medication.dart` — `cachedDoseLogDataSourceProvider` 改为 `doseLogRepositoryProvider`，`medicineReminderListProvider` 改为 `reminderRepositoryProvider`
- **变更** `lib/features/record/presentation/quick_entry/medication_flow.dart` — import 从 `medicine/data/datasources/` 改为 `medicine/domain/entities/`

### Step 3.6：report 跨 feature 耦合消除

- **新增** `lib/features/today/domain/repositories/suggestion.dart` — `SuggestionRepository` 抽象接口
- **新增** `lib/features/today/data/providers/suggestion.dart` — `todaySuggestionRemoteDataSourceProvider`、`suggestionRepositoryProvider`、`suggestionHistoryProvider` 从 presentation 层迁移到 data 层
- **变更** `lib/features/today/data/datasources/suggestion_remote.dart` — `TodaySuggestionRemoteDataSource` 实现 `SuggestionRepository`
- **变更** `lib/features/today/presentation/providers/suggestion.dart` — 移除已迁移的 provider，从 data 层 import
- **变更** `lib/features/report/presentation/pages/page.dart` — import 从 `today/presentation/providers/` 改为 `today/data/providers/`

### 验证

- `flutter analyze`: 0 error, 0 warning, 0 info
- `flutter test test/record/page_test.dart`: 54 passed, 0 failed
- 其余 6 个测试失败均为预存问题（layout_tokens、tooling_support、help_settings、shell_page、view_models_extended、report hospital export FToaster），与本次修改无关

## 架构精炼 Phase 4：页面/Widget 文件拆分

### P1-1：Settings page.dart（852→165 行）

- 拆分为 8 个 widget 文件：
  - `widgets/navigation_tile.dart` — `SettingsNavigationTile`（共享 tile）
  - `widgets/account_header.dart` — `AccountHeader`
  - `widgets/general_section.dart` — `GeneralSection`
  - `widgets/quick_entry_section.dart` — `QuickEntrySection`
  - `widgets/privacy_section.dart` — `PrivacySection`
  - `widgets/about_section.dart` — `AboutSection`
  - `widgets/sign_out_tile.dart` — `SignOutTile`
  - `widgets/master_detail.dart` — `SettingsMasterDetail` + `MasterNavItem` + `SettingsGroup`

### P1-2：Medicine check_tab_content.dart（826→145 行）

- 按 tab 拆为 3 个 section 文件：
  - `risk/overview_tab_section.dart` — `TabHeader`、`RiskScoreHero`、`MetricGrid`、`SafeStateCard`、`OverallRecommendationCard`、`StaleBanner`、`LlmEmptyState`、`NeverCheckedState`、`LlmUnavailableState`
  - `risk/findings_tab_section.dart` — `FindingsTabSection`
  - `risk/coverage_tab_section.dart` — `CoverageTabSection`

### P1-3：Assistant conversation.dart（591 行）

- `_executeProposal` 跨功能写逻辑提取到 `application/orchestrators/proposal_flow.dart` — `ProposalExecutionOrchestrator`
- Notifier 现委托 orchestrator 执行 proposal，自身专注状态管理

### P2-1：Auth account_settings.dart（614→325 行）

- `_ProfileSection` 和 5 个 helper 函数提取到 `pages/account_settings_helpers.dart`
- 提取的函数：`ProfileSection`、`startWechatIdentityLink`、`webWechatLinkCallbackUri`、`confirmUnlinkIdentity`、`verifyEmailFlow`、`showVerifyEmailDialog`

### P2-2：Medicine reminder_edit.dart（567 行）

- `_MedicineSelectorPrompt` → `widgets/reminder/medicine_selector.dart`
- `_ReminderTimePickerDialog` → `widgets/reminder/time_selector.dart`

### P2-3：Medicine reminder_detail.dart（515 行）

- 内联 FTileGroup（9 个 detail tiles）提取到 `widgets/reminder/detail_info_tiles.dart` — `ReminderDetailInfoTiles`

### P3-1：Record create.dart（601 行）

- 移除私有 `_PendingDailyRecordImage`、`_RecordFormLoading`、`resolveImageContentType`、`_allowedImageContentTypes` 重复定义
- 改用 `widgets/forms/pending_image.dart` 中的共享 `PendingDailyRecordImage`、`resolveImageContentType`、`allowedImageContentTypes`

### P3-2：Assistant page.dart（556 行）

- `_AssistantControlsSheet` 提取到 `widgets/controls_sheet.dart` — `AssistantControlsSheet`

### P3-3：Report dashboard_view.dart（552→~360 行）

- Preview 布局和 mock 数据提取到 `widgets/views/dashboard_preview.dart` — `ReportDashboardPreview`
- 移除内联 `_buildMobilePreviewLayout`、`_buildDesktopPreviewLayout`、`_previewTrends`、`_previewExportActions`

### 验证

- `flutter analyze --no-fatal-infos`: 0 error, 0 warning（11 个 info 级 directives_ordering / eol_at_end_of_file）

## 架构精炼 Phase 5：文件命名规范化

### R1：类型后缀移除（4 处，core/）

- `core/accessibility/settings_controller.dart` → `settings.dart`（+ `.freezed.dart`）
- `core/config/developer_settings_controller.dart` → `developer_settings.dart`（+ `.freezed.dart`）
- `core/config/feature_flags_controller.dart` → `feature_flags.dart`（+ `.freezed.dart`）
- `core/i18n/locale_controller.dart` → 合并入已有 `locale.dart`（`AppLocale` enum + `LocaleController` + `localeControllerProvider` 统一在同一文件）

### R3：目录名前缀移除（19 处源文件）

**Feature 层（11 处）**
- `medicine/presentation/widgets/risk/` — 6 个文件去掉 `risk_` 前缀：`check_loading.dart`、`coverage_issue_tile.dart`、`finding_tile.dart`、`metric_chip.dart`、`red_flag.dart`、`score_ring.dart`
- `medicine/presentation/pages/reminder/` — 2 个文件去掉 `reminder_` 前缀：`detail.dart`、`edit.dart`
- `record/presentation/widgets/nlp/` — 3 个文件去掉 `nlp_` 前缀：`candidate_editor.dart`、`candidate_review.dart`、`retry_panel.dart`

**Core 层（8 处源文件 + 4 个 `.g.dart` 自动跟随）**
- `core/ai/` — `ai_runtime_config.dart` → `runtime_config.dart`；`ai_runtime_providers.dart` → `runtime_providers.dart`（+ `.g.dart`）
- `core/database/` — `database_connection.dart` → `connection.dart`；`database_connection_io.dart` → `connection_io.dart`；`database_connection_web.dart` → `connection_web.dart`；`database_providers.dart` → `connection_providers.dart`（+ `.g.dart`）
- `core/database/sync/` — `sync_worker.dart` → `worker.dart`（+ `.g.dart`）
- `core/network/` — `network_providers.dart` → `client_providers.dart`（+ `.g.dart`）

### R4：`app_` 前缀移除（1 处）

- `core/shortcuts/app_shortcuts.dart` → `shortcuts.dart`

### 全局 import 路径替换

- ~60 个 lib/ 文件的 import 路径更新
- ~20 个 test/ 文件的 import 路径更新
- `integration_test/support/e2e_test_helpers.dart` import 路径更新
- `core/network/api.dart` 的 `export` 路径更新
- `core/database/database.dart` 的条件 import 路径更新
- 12 个文件的 `locale_controller.dart` + `locale.dart` 重复 import 合并
- 10 个测试文件同步重命名
- `build_runner clean` + `build_runner build` 重新生成所有 `.g.dart` 和 `.freezed.dart`
- `dart fix --apply` 修复 19 个 directives_ordering

### 验证

- `flutter analyze`: 0 error, 0 warning, 0 info
- `flutter test test/core/ test/medicine/ test/record/`: 1353 passed, 1 failed（`layout_tokens_test.dart` 预存失败，与本次修改无关）

## 架构精炼 Phase 6：规则固化

### 文档更新

- `AGENTS.md` — 新增 `application/` 层到 Architecture 部分；新增 Cross-Feature Import Rules 三条边界规则
- `docs/02-reference/architecture.md` — 更新 `core/` 目录结构图（反映重命名后的文件）；更新 Feature Module Inner Structure（新增 `application/` 层说明）；新增 Application Layer 章节；新增 Cross-Feature Import Boundaries 章节
- `docs/02-reference/data-layer.md` — 新增 Domain Interface Injection (Cross-Feature) 章节，记录 `DoseLogRepository`、`ReminderRepository` 接口注入模式
- `docs/02-reference/state-management.md` — 新增 Application Layer Pattern 章节，记录函数式 use case 和类式 orchestrator 两种形式

### 计划文件清理

- 删除 `plans/2026-07-29-architecture-refinement.md`（计划已全部完成，稳定决策已更新到 reference 文档）

## OCR 引擎替换：ML Kit → PaddleOCR PP-OCRv6

### 依赖替换与平台配置

- **pubspec.yaml**: `google_mlkit_text_recognition: ^0.15.1` → `paddle_ocr_native: ^0.1.1`
- **android/app/build.gradle.kts**: 移除 ML Kit 四个语言模型依赖（chinese/devanagari/japanese/korean），新增 `ndk.abiFilters = arm64-v8a` 和 `packaging.jniLibs.excludes` 剔除冗余 ABI
- **android/app/src/main/AndroidManifest.xml**: 移除 `RECORD_AUDIO` 权限（`speech_to_text` 残留）
- **ios/Runner/Info.plist**: 移除 `NSMicrophoneUsageDescription`（同上）
- **ios/Podfile**: `platform :ios` 从 `13.0` 升至 `16.0`；新增 `use_frameworks! :linkage => :static`；`IPHONEOS_DEPLOYMENT_TARGET` 从 `13.0` 升至 `16.0`

### OCR 引擎 Provider（新建）

- **新增** `lib/features/scan/domain/services/paddle_ocr_provider.dart` — `PaddleOcrEngine` 封装 `PaddleOcr` 单例，延迟初始化 ONNX Runtime sessions，将 `OcrResult`/`OcrPoint` 转换为域类型 `OcrTextBlock`；Riverpod `paddleOcrProvider` 管理生命周期
- **删除** `lib/features/scan/domain/services/ocr.dart` — 旧 `OcrService`（ML Kit 封装）

### 候选提取器重写

- **新增** `lib/features/scan/domain/services/medicine_ocr_extractor.dart` — `MedicineOcrExtractor`，基于空间布局评分（面积 50% + 位置 30% + 置信度 20%）的多策略提取器：
  - 策略 1：批准文号模糊匹配（OCR 字符混淆表 + 正则）
  - 策略 2：药品名空间评分（停用词子串惩罚 + 短文本惩罚）
- **删除** `lib/features/scan/domain/services/text_matcher.dart` — 旧 `MedicineTextMatcher`（纯文本正则匹配）

### 域实体重构

- **变更** `lib/features/scan/domain/entities/scan_result.dart` — 新增 `OcrTextBlock`（含 `boundingBox`、`points`、`confidence`、`text`）；将 `MedicineMatchResult`、`MedicineMatchType`、`MedicineMatchCandidate` 从 `text_matcher.dart` 迁移至此
- `MedicineMatchType` 枚举移除 `barcode` 值（条码扫描走独立路径，不经 OCR）

### 调用层适配

- **变更** `lib/features/scan/presentation/pages/box_scan.dart` — `_processPhoto` OCR 分支从 `OcrService().recognizeText(photo)` + `MedicineTextMatcher().extractCandidates(ocrText)` 改为 `paddleOcrProvider.recognize(photo.path)` + `MedicineOcrExtractor().extractCandidates(ocrBlocks)`
- **变更** `lib/features/scan/presentation/widgets/dialogs/recognize_dialog.dart` — import 从 `text_matcher.dart` 改为 `scan_result.dart`；`_matchTypeLabel` switch 移除 `barcode` 分支

### 测试

- **新增** `test/scan/medicine_ocr_extractor_test.dart` — 12 个单元测试覆盖：批准文号模糊匹配（4）、空间评分（4）、边界情况（4）
- **删除** `test/scan/text_matcher_test.dart` 和 `test/scan/domain/services/text_matcher_test.dart` — 旧 `MedicineTextMatcher` 测试
- **删除** `test/record/ocr_test.dart` — 旧 ML Kit `OcrService` 测试
- **变更** `test/scan/widgets/recognize_dialog_test.dart` — import 从 `text_matcher.dart` 改为 `scan_result.dart`

### 验证

- `flutter analyze`: 0 error, 0 warning, 0 info
- `flutter test test/scan/medicine_ocr_extractor_test.dart`: 12 passed, 0 failed
