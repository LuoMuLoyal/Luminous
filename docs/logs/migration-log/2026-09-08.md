# 2026-09-08

## feat(review): 事件动作收口后同步 review 测试断言

Review 页重组计划收口：事件动作已收口 Today，review 侧不再渲染任何事件动作
入口。P1-3 单元/Widget 测试更新——`test/review/page_test.dart`、
`test/review/review_golden_test.dart`、`test/review/widgets/review_view_test.dart`
中的「无事件无开始观察动作」断言由旧 `review-start-observation-action` key 改为
`health-event-start-action` findsNothing（并补注释说明动作收口事实），确保断言
跟随主路径现状，避免对已下线动作的误判。

新增测试（coverage_strip 含灰态、noteworthy 有卡/弃权、周期切换、冷启动引导、
compact 事件卡、按下状态映射与旧值缓存）已随对应区块落地提交，本节为断言同步
收尾。

验证：`flutter analyze` 零 issue；`flutter test test/review` 全绿。

## feat(review): 集成测试事件动作迁移至今日链路

Review 页重组计划 P1-4：事件动作已收口 Today，review 侧不再提供开始/打卡/结束
入口。`integration_test/review/review_closed_loop_e2e_test.dart` 的开始事件与
结束事件步骤由 Review UI 入口改为经 `activeHealthEventProvider.notifier` 的
`create` / `end` 驱动（与 Today 卡片共享同一 controller 与 DataChangeBus
广播链路）；`review-check-in-action` 断言改为 `review-go-today-check-in`
findsOneWidget；doc 注释同步说明桌面布局无 Today 卡片的驱动方式。

`integration_test/review/report_e2e_test.dart` 的
`review-start-observation-action` / `review-end-event-action` 断言改为
`health-event-start-action` / `health-event-end-action` findsNothing，确认
review 侧不渲染任何事件动作。

核对 `shell_navigation_e2e_test.dart`（仅 `review-no-event-card`，仍有效）与
`app_smoke_test.dart`（无 review 内容 key 依赖）：全仓库已无
`review-start-observation-action` / `review-check-in-action` /
`review-end-event-action` 残留引用。

验证：`flutter analyze` 零 issue（含 integration_test 编译）。

## docs(review): review README 与模块注释对齐洞察优先主路径

Review 页重组计划 P1-5：重写 `lib/features/review/README.md`——一句话定位由
「以健康事件为主单位」改为「以纵向洞察为主单位的洞察优先主路径」；职责与边界、
对外契约、不变量、依赖禁区、陷阱与决策全部按重组后的运行时事实重述：周期开关
→ 覆盖率概览 → 值得注意 → 单维趋势 → 事件回顾的块序、事件动作收口 Today（`health_event`
presentation 依赖豁免解除）、AI 摘要/建议历史主路径下线（deferred 标注 + TODO 登记）、
覆盖率口径与冷启动分支、`reviewDashboardProvider` 数据驱动。

同步修正主路径模块注释：`page.dart` 类注释与 `review_view.dart` 类注释由「事件优先/
复用 health_event notifier」改为「洞察优先 / 事件动作已收口 Today、无事件动作回调，
仅「去今日 check-in」浅链接」。

核对 ADR/IA/vision 文档：ADR-0007 本身已是 superseded 并标注「事件为平级专题、纵向
洞察升为核心」，`product-vision.md` / `product-mvp-scope.md` / `adr/README.md` /
`state-management.md` 均无「事件优先主路径」的过时运行时断言，无需指正（仅 review
README 与模块注释为过时表述，本轮已更新）。

验证：`flutter analyze` 零 issue。

## docs(review): Review 页重组收尾验证与后置依赖登记

Review 页重组计划 P2 收尾：全量验证通过——`flutter analyze` 零 issue、全量
`flutter test` 通过、`dart run scripts/docs/verify.dart --warning-only` 无本计划
相关阻碍项（仅无关文件 `scripts/hooks/git.dart` 的既有警告）。金样测试未受影响，
无需重生成。

后置依赖登记至 `docs/TODO.md`（2026-09-08 节）：Review 洞察的预置上下文入口
（agentic 计划承接，顶栏 [问助手] 升级）与 Lucent observed-metric 域迁移
（target 2026 Q4）后的 Wave 2 客户端适配（维度扩展、逐日 presence、findings
证据引用与反馈）；两项目前均不在本期客户端范围，仅登记跟踪。
