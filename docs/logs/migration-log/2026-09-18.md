# 迁移日志 2026-09-18

本日执行 [`plans/Luminous-review-09-17.md`](../../../plans/Luminous-review-09-17.md) 的
warning / suggestion 级条目（W1 / W2 / S1 ~ S4，共六项；S2 为文档性说明）。

## 修复计划来源

计划 `plans/Luminous-review-09-17.md` 的落地节奏表列出的 items：

| 项 | 内容 | 本次动作 |
|---|---|---|
| W1 | `_showCropFailure` 通过 State 参数接收 BuildContext 有寿命风险 | 改为无参 + `State.context` |
| W2 | `pickAvatarDraft` 缺 mounted 约定 doc | 补 doc |
| S1 | `_wireListElement` 的 assert 仅 debug 期生效 | 注释补 release 期说明 |
| S2 | `archive_sections` 的 `weightKg!` 强解包语义未说明 | 注释补安全前提 |
| S3 | `MarkdownStyle` 缺 token name ↔ 设计 token 对照表 | 文件 doc 顶部补对照表 |
| S4 | `account_hero` 的 `_ReadinessGap` 只剩 type 字段 | 简化为 `Set<_ReadinessGapType>` |

## fix(core): 头像选图裁剪失败提示改用 State.context，消除 context 寿命风险

（计划 W1 / W2。）

### 变更

- `lib/core/widgets/common/avatar/avatar_draft.dart`：
  - `_showCropFailure(BuildContext)` 收参数改为无参，直接使用 `State.context`，
    入口加 `if (!mounted) return;`。`onCropped` 回调开头本已有 `if (!mounted) return;`，
    但现在规则固化为「失败提示只在 State 仍挂载时展示」，杜绝未来从 dispose 路径
    误把失效 context 传给 `Toast.show` 的可能。
  - `pickAvatarDraft` doc 补一段「调用方需保证传入的 context 在 await 边界后仍
    mounted（例如 dialog / showModalBottomSheet 内部），否则空文件/裁剪失败等
    反馈 Toast 会静默不显示」——把 `context.mounted` 约定从代码显式化到签名文档，
    与 W1 同一组改动一起收敛。

### 验证

- `flutter analyze` 全仓无问题；`flutter test test/core/widgets/avatar_draft_test.dart`
  通过（原有 2 条用例，本次改动不涉其断言路径）。

## fix(health-context): 快照 wire 负载列表元素 assert 补 release 期行为说明

（计划 S1。）

### 变更

- `lib/features/health_context/data/datasources/snapshot.dart` `_wireListElement`：
  注释追加一句——`assert` 仅 debug 期生效，release 期不会拦截，届时坏元素会落到
  JSON 序列化才报 `JsonUnsupportedObjectError`，错误位置偏离 wire 这一层。
  行为不变，仅文档化已有 trade-off。

### 验证

- `flutter analyze` 无问题；无行为改动，不涉测试变更。

## fix(mine): 档案摘要区 weightKg! 强解包补安全前提注释

（计划 S2。）

### 变更

- `lib/features/mine/presentation/widgets/sections/archive_sections.dart`：
  `profile.weightKg!` 处补注释——安全是因为外层 `if (profile.weightKg != null)`
  已保证非 null，无需再走 `kgToLb` 的 null 兜底。逻辑等价，仅消除新人 review 时的歧义。

### 验证

- `flutter analyze` 无问题；无行为改动。

## docs(design): MarkdownStyle 顶部补 token name ↔ 设计 token 对照表

（计划 S3。）

### 变更

- `lib/core/design/tokens/markdown_style.dart` 类 doc 追加对照表：
  - `listIndent` ↔ `Spacing.xl`（列表缩进，20/级）
  - `listBulletPadding` ↔ `Spacing.sm`（bullet 与文字间距，右 6）
  避免再次出现注释与实现脱钩（此前「level5=20/级」旧注释失实已在 8706c8b4 修正）。

### 验证

- `flutter analyze` 无问题；无行为改动。

## refactor(mine): account_hero 的 _ReadinessGap 简化为一组 flag

（计划 S4。）

### 变更

- `lib/features/mine/presentation/widgets/sections/account_hero.dart`：
  删除只剩 `type` 字段的 `_ReadinessGap` 类，`_deriveGaps` 改返回
  `Set<_ReadinessGapType>`（集合推导，5 个 gap 条件逐条 add）；label 文案选择
  改为独立顶层 `_gapLabel(l10n, type)`。功能等价，去掉「5 个 const 实例只是
  5 个 flag」的多余包装层。

### 验证

- `flutter analyze` 无问题；`flutter test test/mine/page_test.dart test/mine/edit_pages_test.dart`
  全绿（含 account hero 的 gap 展示断言路径）。

## chore(contract): 补齐 profile_edit 下线遗留的生成物

### 变更

- `docs/reference/generated/routes.md`：删除已下线路由 `mineProfileEdit`（`/mine/profile/edit`）
  行。该行在 `0924fc6b refactor(mine)` 下线 profile_edit 后遗留未同步，本次由
  `dart run scripts/docs/generate.dart` 重新生成收口。

### 验证

- `dart run scripts/docs/generate.dart` 再跑无额外 diff；`dart run scripts/docs/verify.dart`
  无新增告警。

## fix(contract): 修复生成客户端工具枚举漂移(`flutter test` 全仓编译失败)

### 变更

- `generated/lucent_api/lib/src/model/assistant_capabilities_response_tools.dart`:
  `AssistantCapabilitiesResponseToolsNameEnum` 补回 `searchCnMedicineKnowledge`。
  该成员在序列化伴生文件 `.g.dart` 已被引用,但枚举本体缺失(漂移随 `8b563d41`
  入库),导致**任何** `flutter test` 目标在编译期失败——`flutter analyze` 不报,
  仅在 test 编译时暴露,故此前未被发现。
- 经 `dart run scripts/contract/bootstrap.dart` 重新生成收敛(净变更 1 文件 +2 行)。
  Lucent `tool-types.ts` 与 `openapi.json` 本已含该工具,属生成物内部不一致,非契约缺口。

### 验证

- `flutter analyze` 无问题;`flutter test test/record/page_test.dart` 55 项全绿
  (修复前为编译失败,无法运行任何测试)。

## docs(plans): 新增 09-15 UI 巡检修复计划

### 变更

- 新增 `plans/2026-09-18-ui-review-remediation-plan.md` 并在 `plans/README.md` 登记:
  对 `plans/ui-review-2026-09-15.md` 的 31 条问题逐条核实,修正原报告错判的根因
  (`complete_profile` 实为 Lucent 规则下发裸 key 而键本身已注册;`scheduledFor` 格式合法、
  `VALIDATION_FAILED` 只是所有 400 的通用兜底;Medication 快捷入口跳相机、Account 子页无底栏、
  收件箱按钮可点击均为误报;Review `+0.0` 为中性色非绿色),并记录已完成项与待办顺序。
  源巡检文件待计划执行完毕后随计划一并删除。

### 验证

- 纯文档变更,无代码影响;引用路径经核实存在。

## fix(mine): 同步失败项支持丢弃

巡检 P0「3 条 dose_log 永久卡在队列、横幅只有 View details」的真实缺口:队列页已有
`retryAll`,但对语义性拒绝(服务端持续拒绝 payload 身份)重试逐字节重放永远不会成功,
且 `fetchPermanentlyFailed` 会把达上限项一直列出,用户没有任何终止手段。

### 变更

- `sync_failed_entry.dart`:新增 `onDiscard` 回调与「丢弃」按钮;经
  `showDangerConfirmationDialog` 二次确认(丢弃会销毁未同步本地变更的唯一副本),
  进行中显示 loading 并禁用按钮。
- `sync_failures.dart`:新增 `_discard(entry)`,复用既有 `PendingSyncDao.remove`,
  成功后失效 `syncFailedCountProvider` 与 `mineSyncFailedEntriesProvider`;
  失败走与重试失败同一个 footer 错误位。
- `l10n/src/mine_{en,zh}.arb`:新增 `mineSyncFailedDetailsDiscard` /
  `...DiscardFailed` / `...DiscardTitle` / `...DiscardDescription` / `...DiscardConfirm`
  五个键(经 `arb_tools.dart merge` + `flutter gen-l10n` 生成)。
- 未新增「重试」按钮:页面已存在的「全部重试」即该功能(计划已核实此点为部分误报)。

### 验证

- `flutter analyze` 无问题;`flutter test test/mine/ test/core/database/` 202 项通过,
  其中新增 3 项覆盖「确认后才删除」「取消保留」「持久化失败提示」。

## fix(sync): 队列按服务端 retryable 分流永久失败

`worker.dart` 对每个 `DioException` 一视同仁,永久性纯由 `retryCount >= maxRetry`
算术决定,且从不读 `LucentFailure.retryable` / `statusCode`。后果:服务端已明确判定
语义性拒绝(如 `DOSE_LOG_TARGET_UNRESOLVED` 这类重放必然同样失败的请求)时,客户端仍会
把剩余重试次数全部耗尽,用户看到的是"还在重试"而非"已被拒绝"。

### 变更

- `pending_sync_error_details.dart`:新增 `retryable` 字段并从 `LucentFailure` 传递;
  `fromJson` 对旧行保持容错(缺省即 `null`,回落既有算术行为)。
- `daos/pending_sync.dart`:新增 `markPermanentlyFailed`,一次性把 `retryCount` 置为
  `maxRetry`(而非 +1),使 `fetchReady()` 立即排除该项且不消耗剩余额度。
- `sync/worker.dart`:`details.retryable == false` 时走 `markPermanentlyFailed` 并 return,
  不再重复 `markFailed`;未声明 `retryable` 时保持原算术判定。
- `test/core/database/sync/worker_test.dart`:新增 2 项 —— 非可重试错误进入永久失败且
  不调用 `markFailed`;声明 `retryable: true` 时仍走常规重试路径。

### 验证

- `flutter analyze` 无问题;`flutter test test/core/ test/mine/` 848 项通过
  (含 `build_runner` 重新生成的 freezed/json 产物)。

## fix(assistant): 区分模型故障与通用服务端错误,不再对确定性拒绝提供重试

配套 Lucent「上游模型故障映射为 dependency 失败」的客户端侧。此前
`conversation_sending.dart` 把一切非网络失败折叠为 `AssistantSendErrorType.server`,
恒定渲染通用错误卡,并始终展示「Continue generating」——对模型直接拒绝的请求重试是
确定性失败,用户点多少次都是同一结果。

### 变更

- `conversation.dart`:`AssistantSendErrorType` 增加 `dependency`(可重试的模型故障)与
  `modelRejected`(确定性拒绝)。
- `conversation_sending.dart`:`classifySendError` 按服务端下发的 Problem Details code
  (`DEPENDENCY_*`) 判定为模型故障;其中 `retryable == false` 归入 `modelRejected`。
  非 dependency 码仍回落 `server`,取消/断网仍为 `streamInterrupted`。
- `above_composer.dart`:`modelRejected` 时**不渲染**「继续生成」动作(按钮与文案同时省略)。
- `ui_formatters.dart` 补两条文案与图标分支。
- `l10n/src/assistant_{en,zh}.arb`:新增 `assistantErrorDependency` /
  `assistantErrorModelRejected`。

### 验证

- `flutter analyze` 无问题;`flutter test test/assistant/` 213 项通过,
  其中新增 4 项覆盖 dependency / modelRejected / server / streamInterrupted 四类分流。

## fix(review): 覆盖卡稀疏期不再同时显示变化量

配套 Lucent「覆盖卡稀疏期不再输出变化量」。`coverage_strip.dart` 的 delta 渲染条件
只判 `metric.delta` 非空,未纳入本卡已算出的 `sparse`;后端取值口径更严,于是
`coverage: partial` 且观测 ≥2 时会出现「`--` + 变化量」并存的矛盾窗口。

### 变更

- `coverage_strip.dart`:delta 渲染条件补 `!sparse`。
- `test/review/widgets/coverage_strip_test.dart`:新增 2 项 —— sparse 卡片隐藏 delta
  (回归原「数据太少 + 变化量」矛盾)、非 sparse 卡片仍正常渲染 delta。

### 验证

- `flutter analyze` 无问题;`flutter test test/review/widgets/coverage_strip_test.dart`
  6 项通过。

## fix(record): 空态时间线卡高度与滚动偏移保持

巡检 P1「新增记录后页面顶部出现大块空白(内容起点 y=337 → y=573)」。核实结论:
布局树**无分支**(固定同一 `Column`),原报告「空态/有数据分支切换」的根因判断不成立;
真实机制是空态卡过高 + 滚动偏移丢失。

- `_MobileTimelineEmptyState` 单卡约 280px(垂直 `Spacing.xl4`×2 + 64px 图标),
  远高于一条时间线记录行;记完后内容**变短**并低于视口,`SingleChildScrollView`
  不再可滚,观感即「ScrollView 退化为普通 View」。
- 约 236px 位移是滚动偏移伪影:移动端用普通 `Key`(`page.dart:236`)而非
  `PageStorageKey`,偏移不恢复;`onQuickAction` 触发 `DataChangeTopic.dailyRecords`
  重取后时间线收缩,原偏移不可达。

### 变更

- `mobile_timeline.dart`:空态卡垂直内边距 `Spacing.xl4` → `Spacing.xl2`,
  图标 `Spacing.xl4`(64) → `Spacing.xl3`,使其贴近有数据时的高度。
- `pages/page.dart`:移动端滚动视图 `Key` → `PageStorageKey<String>`
  (桌面端 `DesktopTabShell.scrollStorageKey` 已有等价语义)。

### 验证

- `flutter analyze` 无问题;`flutter test test/record/` 504 项通过。

### 未处理(需真机复验)

§2-6 的溢出像素与本次布局改动都需在模拟器上实际复验,见收尾端到端校验。

## fix(assistant): 每条消息的免责声明可展开

巡检 P1(合规):`widgets/disclaimer_bar.dart` 硬 `maxLines: 1` + ellipsis,**且无任何
展开入口**。这是每条助手消息都渲染的医疗免责声明,截断后内容不完整且用户无从查看全文。
对照欢迎态 `sections/empty_support.dart` 的 `AssistantDisclaimer` 已有展开/收起交互。

### 变更

- `disclaimer_bar.dart`:由 `StatelessWidget` 改为 `StatefulWidget`,复用欢迎态已有的
  交互与**现成 ARB 键**(`assistantDisclaimerShowAction` /
  `assistantDisclaimerCollapseAction`)——未新增任何字符串;收起态保持单行,点按展开全文,
  并加展开/收起图标与 `Semantics` 标签。

### 验证

- `flutter analyze` 无问题;`flutter test test/assistant/` 218 项通过。

## fix(layout): 底部 viewInset 不再与 Forui 重复计入

巡检 P1「AI chat 打开表情面板时 `BOTTOM OVERFLOWED BY 55 PIXELS`」。核实结论:机制
确为底部 inset 双重计入,但原报告未能由代码推出具体 55px 数值(属特定设备余数)。

- Forui `_RenderScaffold.performLayout`(forui-0.26.0 `scaffold.dart:245-258`)为 footer
  预留 `max(insets.bottom, footer.size.height)`,即底部 inset **已在 scaffold 层计入一次**;
  但 Forui 不像 Material `Scaffold` 那样对主体 `removeViewInsets`。
- 主体内再消费一次底部 inset 的组件(AI chat composer 给自身加底部内边距)于是把同一份
  inset 算两遍;视口被压到不足 `inset + composer` 高度时即溢出。

### 变更

- `page_scaffold.dart`:`resizeToAvoidBottomInset` 为 true(scaffold 已消费 inset)时,
  主体包一层 `MediaQuery.removeViewInsets(removeBottom: true, ...)` —— 这正是 Forui 缺失、
  Material 具备的语义;布局收缩本身仍由 `FScaffold` 负责。
  为 false 时 scaffold 未消费 inset,保留给主体自行处理。

### 验证

- `flutter analyze` 无问题;`flutter test` 全量 3804 项通过(scaffold 改动影响所有页面,
  故跑全量);`page_scaffold_test.dart` 新增 2 项断言两种分支下 `viewInsets.bottom` 的取值。
- 未复现原报告的 55px 具体数值;该数字取决于设备余数。
