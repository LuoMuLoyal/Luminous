## Proactive Suggestion Runtime — Task 7 final contract hardening

- Today Analysis generated client 已同步 Lucent 的 envelope DTO；REST data source 解包 generate response 的 `analysis`，无嵌套分析时保留 envelope data 映射，SSE 仍沿用现有手动解析边界。
- `bootstrap_generated_sources.dart` 使用过滤后的 OpenAPI Generator 输出，`verify_lucent_openapi_sync.dart` 校验 Today Analysis GET/refresh 方法；相关参考文档已同步。
- 验证：`flutter analyze --fatal-warnings`、`flutter test`（2933 tests）、`dart format --set-exit-if-changed .`、`dart run scripts/check_doc_coverage.dart --warning-only`、`dart run scripts/check_doc_links.dart` 和 OpenAPI sync 通过。

## Proactive Suggestion Runtime — Task 8 Today 状态机

- Today suggestion domain、远程 mapper 和本地 codec 已接入 `ready / stale / pending / failed / empty` 物化状态及 `sourceVersion / computedAt / retryAfterSeconds` 元数据；旧缓存默认按 `ready` 读取。
- Provider 只通过 GET 读取服务端物化结果，监听相关 `DataChangeBus` 事件做 300ms 去抖刷新，并在应用 resume 时检查 `sourceVersion`；pending、stale、failed 保留旧内容，failed 提供重试。
- UI 已覆盖五种状态，stale 缺少更新时间时显示明确的更新中提示；新增定向测试覆盖状态渲染、缓存兼容、GET 去抖和 resume 版本检查。

## Proactive Suggestion Runtime — Task 8 cold-start cache follow-up

- 修复应用重启后首次 GET 返回 `pending / stale / failed` 时的旧内容保留：Provider 先恢复本地 suggestion cache，再合并服务端物化状态，避免空状态覆盖可用旧卡。
- 增加 cold-start 回归测试；缓存损坏仍按原语义清理并将网络错误交回 Provider。
- 所有建议 GET 通过 FIFO 队列串行执行，避免数据变更、resume 和手动刷新之间的旧响应覆盖新响应。

## Proactive Suggestion Runtime — Task 9 完成

- Luminous Task 9 已完成：Today 文档明确服务端主动重算、只读 GET、版本收敛和 `pending / stale / failed` 旧卡回退边界；`Next_Plan.md` 已切换到 Sparse Record Semantics。
- Flutter 验证通过：`flutter analyze --fatal-warnings` 无问题；`flutter test` 2946 通过、1 跳过、0 失败；`dart run scripts/check_doc_coverage.dart --warning-only`、`dart run scripts/check_doc_links.dart`（135 个文件）和 `dart format --set-exit-if-changed .` 均通过。
- Lucent PostgreSQL + Redis live acceptance 已完成：相关记录写入后 worker 生成 ready materialization，首个 Today GET 返回 ready/stale 既有结果；连续 10 次写入后最终 `sourceVersion === computedVersion`。
- `plans/2026-08-07-proactive-suggestion-runtime.md` 的 Task 9 已执行完毕并删除；下一阶段改由 `plans/2026-08-07-sparse-record-semantics.md` 承接。

## Sparse Record Semantics — Water canonicalization

- Lucent 已将 Today suggestion 与 Report dashboard 的水量聚合统一到共享 ml mapper；Luminous 文档同步记录该兼容期边界，Flutter 现有 scalar Report client 暂不改动。
- Water 阶段已完成，`plans/2026-08-07-sparse-record-semantics.md` 的 Task 3 已执行完毕并删除；后续继续 Task 4 药品 reminder slot。

## Sparse Record Semantics — Water downstream boundary

- Report 的兼容升数序列只保留 sufficient observed 水量，明确 `0 ml` 不再被排除，unknown/partial 不投影成 0；Flutter 现有 Report client 无需修改，coverage-aware observed DTO 留到后续合同同步阶段。
- water-shortfall 的 LLM copy params 已改为 canonical ml 字段，旧 record-count 字段不再进入生成事实；water trend 的旧计数字段带有 legacy semantics/source 标记。

## Sparse Record Semantics — Medication reminder slots

- Lucent 后端已统一 Today/Report 的 reminder slot 身份和服药 coverage：同药多提醒独立计算，`planned` 消费为 `unconfirmed`，taken/skipped/overdue-unconfirmed 分开保留。
- 无 reminder 的临时 dose log 每条独立且不进入 adherence 分母；无计划窗口保持 unknown。Flutter generated client/domain 尚未消费新的 observed medication 对象，留到后续合同阶段。
- Lucent 定向验证：4 files / 80 tests、typecheck、lint（max warnings 0）、build、format 通过；本次未产生 OpenAPI 变更。

## Sparse Record Semantics — Sleep episodes

- Lucent 后端已支持 `nightSleep`/`nap` episode、起止时间、时长与质量校验，旧 `startAt/endAt` 保持读取兼容；Today collector 返回夜睡、午睡、总睡眠及重叠数据质量 warning。
- Flutter 本阶段未改生成客户端或 domain mapper；现有快速录入继续走兼容 payload，observed sleep DTO/domain 接入留到下一阶段。
- Lucent 定向验证：daily-record 与 Today record collector 2 files / 59 tests、typecheck、lint、build、format 通过。

## Sparse Record Semantics — Observed metric OpenAPI contract

- Lucent OpenAPI 现已暴露 Report、Today Suggestion、Today Analysis 的 `observedMetric`：`value`/`state`/`coverage`/`sources`/`observedCount`/`expectedCount`/`windowStart`/`windowEnd`；nullable 字段保持 required，unknown 不被映射为 0。
- Flutter 已运行 `dart run scripts/bootstrap_generated_sources.dart` 同步 generated client，且 bootstrap 白名单已覆盖 `ReportMetricDto`/`SuggestionItemDto` 对新 observed metric 的引用；兼容期内 Report scalar 字段仍可读取，Report domain mapper 保留 legacy scalar 主字段并附加 observed metric，尚未切换主展示路径。
- 生成器对 enum array 错误附加 `unknownEnumValue` 的问题已在 bootstrap 后处理中收敛；不修改 OpenAPI enum，也不影响标量 enum 的 unknown fallback。
- Report mapper 暂保留 deprecated scalar 主字段，并在单文件范围抑制对应静态提示；observed metric 仅作为附加 domain 字段保留，Task 7 已切换 Today domain observed metric。

## Sparse Record Semantics — Mobile quick-entry and domain mapping

- Today/Report domain 新增 `ObservedMetric` 同构字段与 unknown/coverage/source 枚举；本阶段 Report mapper 仍以 generated legacy scalar 为主，并附加映射 `observedMetric`。
- 水 quick-entry 仅在服务端保存成功后 emit DataChangeBus 与登记 undo；睡眠合并写入 `sleepType`、`startedAt`、`endedAt`、`durationMinutes`，同一流程保留 `nap`；无匹配提醒的临时服药不再猜测 `scheduledTime`。
- 验证（quick-entry/domain mapping 阶段）：quick-entry 20 tests、Today/Report domain/repository 51 tests；`flutter analyze --fatal-warnings`、`dart format --set-exit-if-changed .`、文档 coverage/link 检查通过。
- 验证（跨模块集成收口阶段）：`flutter analyze --fatal-warnings`、Today/Report 定向测试、OpenAPI sync 校验、文档 coverage/link 检查与 `dart format --set-exit-if-changed .` 通过；本条不与上一条测试数相加。

## Sparse Record Semantics — Today water domain boundary correction

- Today repository 改为读取当天 water records，只把 `unit == ml` 且数值可解析的记录汇总为 canonical observed value；`0 ml` 保留为 observed zero，无记录或仅有不可换算单位时返回 `unknown/none`，混合单位或结果被分页截断时返回 `partial`。
- 增加 Today repository 回归断言，覆盖 `0 ml`、`500 ml`、不可换算杯量和 summary count 不得冒充毫升值。
- 验证：Today/Report/quick-entry 定向测试 78 tests、`flutter analyze --fatal-warnings`、`dart format --set-exit-if-changed .`、文档 coverage/link 检查和 `git diff --check` 通过。

## Sparse Record Semantics — Quick-entry semantic completion

- Today 概览在 observed metric 存在时显示 canonical ml，unknown 显示 `-- / target ml`，不再使用记录条数冒充饮水健康值。
- 睡眠开始入口增加夜间睡眠/午睡选择，并支持可选的大致分钟数与质量；这些信息写入临时 start fact，合并后的 episode 持续写入 `sleepType`、`startedAt`、`endedAt`、`durationMinutes` 与质量。
- 无匹配 reminder 的单药快速服药返回显式 temporary outcome；选择弹窗和单药入口均提示未关联提醒，且仍保留撤销操作。
- 本次新增/调整文案均通过 `lib/l10n/src/` fragment，经 merge 与 `flutter gen-l10n` 生成。

## Sparse Record Semantics — Health platform import hardening

- HealthMetric/mapper 保留平台 external ID、source、episode 起止时间与睡眠类型；饮水统一映射为 canonical `ml`，睡眠不再按日期合并，保留同日多个 episode。
- Health sync repository 使用 external ID 优先、`kind|source|start|end|value|unit` 稳定指纹回退；同日多条饮水或睡眠记录不会互相覆盖，重试同一 external ID 只保存一次。
- 自动同步 provider 区分 `unsupported` / `notConfigured` / `available`，当前没有后台 executor 时强制禁用本地开关；导入页与 Settings 显示具体边界，平台不可用不再被描述为通用能力。
- 新增文案均通过 `lib/l10n/src/health_sync_{zh,en}.arb`，经 merge 与 `flutter gen-l10n` 生成。

## Sparse Record Semantics — Task 9 文档收口

- Luminous Record 文档现已统一记录 canonical water ml、unknown/partial coverage、reminder slot、sleep episode 以及健康平台导入的 external ID/稳定指纹边界；Record 的旧快捷角标与 Report scalar 仅作为兼容行为保留。
- Report 文档补充 `observedMetric` 字段、来源枚举和 legacy scalar fallback 说明；Localization 文档确认本次没有新增用户可见字符串，因此不修改 ARB fragments，也不运行 l10n 生成链路。
- 删除 Luminous TODO 中已完成的 sparse record 缺口和计划文件中的 Task 9 段落；本次仅更新 Luminous 文档，Task 9 提交未包含 `docs/.obsidian/*`。既有工作区状态保持不变。

## Product Loop — 清理已完成计划

- 删除已完成的 Health Event Contract、Proactive Suggestion Runtime 和 Sparse Record Semantics 子计划文件；总计划仅保留 Review Experience 与 Visit Summary and Product Measurement。
- `Next_Plan.md` 与 `Work_Phase_Guide.md` 已切换到 Review Experience；已完成阶段的检查项从总计划中删除，不保留完成标记。
