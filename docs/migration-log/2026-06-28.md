# 2026-06-28 Luminous 设置页重构

## 变更摘要

将设置页从「平铺分组 + 多处 dialog/行内操作」重构为标准 App 式的一级/二级/三级页面结构（参考百度网盘式设计）。

## 主要改动

### 页面结构

- 设置首页按「账号与安全、通知、隐私、通用、关于」五组重新组织。
- 新增顶部账号入口卡片，保留邮箱/昵称展示。
- 新增子页面：
  - `/settings/notifications` — 通知设置
  - `/settings/notifications/sleep` — 睡眠提醒详情
  - `/settings/ai` — AI 设置
  - `/settings/export` — 数据导出
  - `/settings/help` — 帮助与反馈
  - `/settings/about` — 关于

### 交互规范

- 二态设置项（如数据共享、AI 总结）使用整行可点击的 `Switch`。
- 多状态设置项进入子页（如主题、语言、AI 设置）。
- 睡眠提醒子页采用「总开关 + 子项灰显」模式：总开关关闭时，就寝时间/起床时间置灰且不可点击。
- 未登录用户仍能看到所有设置项，点击需要登录的项时弹出登录提示。

### 组件抽象

新增可复用组件：

- `lib/core/widgets/app_settings_switch_row.dart`
- `lib/core/widgets/app_settings_navigation_row.dart`
- `lib/core/widgets/app_settings_section.dart`
- `lib/features/settings/presentation/widgets/app_settings_master_toggle_page.dart`
- `lib/features/settings/presentation/widgets/settings_selection_icon.dart`

### 清理

- 删除 `lib/features/settings/presentation/widgets/notification_setting_dialogs.dart`。
- 移除 `SettingsPage` 中的 help/about dialog 和行内导出逻辑。
- `ThemeSettingsPage` / `LanguageSettingsPage` 复用公共的 `SettingsSelectionIcon`。

### 路由

在 `lib/app/router.dart` 的 `/settings` 下注册了新子路由。

### 文案

新增/更新 ARB 键：

- `settingsAccountSecuritySectionTitle`
- `settingsNotificationsSectionTitle`
- `settingsNotificationsSummary`
- `settingsPrivacySectionTitle`
- `settingsAiTitle` / `settingsAiSubtitle`
- `settingsAiSummariesTitle` / `settingsAiSummariesSubtitle`
- `settingsAiAssistantTitle` / `settingsAiAssistantSubtitle`
- `settingsAiMemoryTitle` / `settingsAiMemorySubtitle`
- `settingsGeneralSectionTitle`
- `settingsAboutSectionTitle`
- `settingsNotificationsGeneralGroup`
- `settingsNotificationsReminderGroup`
- `settingsNotificationsSleepReminderSubtitle`
- `settingsExportDescription`
- `settingsExportRequestButton`
- `settingsHelpEmpty` / `settingsHelpError`
- `settingsAboutPrivacyPolicy` / `settingsAboutTermsOfService`
- `settingsAboutLicenses` / `settingsAboutSupport`

## 测试

- 更新 `test/settings/settings_page_test.dart` 以匹配新结构。
- 更新 `test/settings/settings_flow_test.dart` 以验证子页导航和开关持久化。
- `flutter analyze` 无错误，`flutter test` 全绿。

---

## UX 审计 Critical 项整改

### 背景

根据 `docs/review/luminous-ux-audit-report.md` 的 Critical 项，将 5 个主 Tab 的加载态从 Mock 数据替换为专用 Skeleton 占位组件，避免在真实网络延迟下向用户展示虚假内容；同时清理审计报告中已失效的 CRIT-3。

### 新增 Skeleton 视图

- `lib/features/today/presentation/widgets/today_skeleton_view.dart`
- `lib/features/record/presentation/widgets/record_skeleton_view.dart`
- `lib/features/medicine/presentation/widgets/medicine_skeleton_view.dart`
- `lib/features/report/presentation/widgets/report_skeleton_view.dart`
- `lib/features/mine/presentation/widgets/mine_skeleton_view.dart`

以上组件均基于现有的 `AppSkeletonShimmer` / `AppInlineSkeletonBlock` / `AppInlineSkeletonSection` / `AppInlineSkeletonCircle` 实现，不引用任何 mock domain 对象。

### 替换加载态

以下页面的 `AsyncValue.when(loading: ...)` 分支已改为对应 Skeleton：

- `lib/features/today/presentation/pages/today_page.dart`
- `lib/features/record/presentation/pages/record_page.dart`
- `lib/features/medicine/presentation/pages/medicine_page.dart`（desktop / mobile shell 均替换）
- `lib/features/report/presentation/pages/report_page.dart`（desktop / mobile shell 均替换）
- `lib/features/mine/presentation/pages/mine_page.dart`

### Report 日期标签修复

`report_page.dart` 顶部的日期范围标签在 loading 时显示 shimmer block，error 时显示 `'--'`，不再泄漏 `MockReportRepository.previewDashboard.startDate/endDate` 等 mock 日期。

### Mock Repository 清理

删除/简化以下 mock repository 中生产代码无引用的工厂方法：

- `MockReportRepository.previewDashboard`
- `MockReportRepository.previewWorkspace`
- `MockReportRepository.loadingDashboard`
- `MockReportRepository.loadingWorkspace`
- `MockRecordRepository.previewDashboard`
- `MockMedicineWorkspaceRepository.previewWorkspace`
- `MockMineRepository` 中相关 dashboard 工厂

### 审计报告更新

- `docs/review/luminous-ux-audit-report.md` 移除 CRIT-3（`/notifications` 路由已存在）。
- 头部 Critical 统计由 5 改为 4；Executive Summary 删除 notifications 路由相关描述；Quick Reference 中 `lib/app/router.dart` 的 issue 列表移除 CRIT-3。
- 当前报告统计：**4 Critical / 12 High / 11 Medium / 8 Low**。

### 测试更新

更新以下测试的 loading 断言，由「等待静态 section title 出现」改为「验证 skeleton block 存在」：

- `test/today/today_page_test.dart`
- `test/record/record_page_test.dart`
- `test/medicine/medicine_page_test.dart`
- `test/report/report_page_test.dart`
- `test/mine/mine_page_test.dart`

同时修复了 `report_page_test.dart` 的 pending timer 问题。

### 验证

- `flutter analyze`: No issues found
- `flutter test`: 484 / 484 全部通过
- `dart run tool/run_daily_checks.dart`: 通过

---

## UX 审计 HIGH 项 Phase 1：交互修复与返回按钮统一

### 完成内容

- **HIGH-10**：`SearchPage` 用 `PageScaffoldShell` 包装，新增 `AppBar` + `AppBackButton`，标题使用 `medicineSearchPageTitle`。
- **HIGH-3**：`TodayEmptyView` 的 `onAction` 由空回调改为 `() => context.push('/record/create')`（该组件当前未在生产代码使用，保留）。
- **HIGH-4 / HIGH-5**：
  - 推荐区"查看更多"改为刷新行为，调用 `_refresh(ref)`。
  - 推荐行按 `category` 映射真实路由：
    - `medicine` → `/medicine`
    - `sleep` → `/record/create?kind=sleep`
    - `record` → `/record/create?kind=water`
    - `report` → `/report`
    - `habit` / 未知 → `/record`
- **HIGH-6**：
  - 药品 dashboard 提醒入口 fallback 改为 `context.push('/medicine/reminders/new')`。
  - `MedicineReminderEditPage` 无药品时显示 inline 提示"请先选择药品" + 跳转 `/medicine/search` 的按钮。
- **HIGH-9 / HIGH-11**：
  - 新建 `lib/core/widgets/app_back_button.dart`：优先 `pop()`，无法 pop 时 `go('/today')` 或自定义 `fallbackRoute`。
  - 在 settings 子页、record 子页、medicine 子页、mine 编辑页、assistant 页、risk-check 页替换 `SettingsBackButton`。
  - 删除 `lib/features/settings/presentation/widgets/settings_components.dart`。

### 新增/更新文案（ARB）

- `medicineReminderSelectMedicineHint`
- `medicineReminderSelectMedicineAction`

### 测试更新

- `test/search/search_page_test.dart`：新增搜索页返回按钮测试。
- `test/medicine/medicine_reminder_pages_test.dart`：新增无药品时显示选药提示测试。

### 验证

- `flutter analyze`: No issues found
- `flutter test`: 489 / 489 全部通过

---

## UX 审计 HIGH 项 Phase 2：Shell 路由架构统一

### 完成内容

- **HIGH-7 / HIGH-8**：
  - `lib/app/router.dart` 中 `/settings`、`/settings/*`、`/assistant`、`/notifications`、`/notifications/:id` 不再作为 `StatefulShellBranch`，改为顶层 `GoRoute`。
  - `/record/*`（`/record/create`、`/record/:id`、`/record/:id/edit`）、`/medicine/*`（search、risk-check、reminders 相关）、`/mine/*`（profile/allergy/condition/medicine 编辑页）全部移出 Shell，改为顶层全屏路由。
  - 仅保留 5 个主 Tab 根路由在 `StatefulShellRoute` 内：`/`、`/record`、`/medicine`、`/report`、`/mine`。
- **Shell 分支清理**：
  - `ShellBranch` enum 移除 `settings`、`assistant`、`notifications`，仅保留 5 个可见 Tab 分支。
  - `ShellPage` 移除 `isHiddenBranch` 逻辑；桌面端侧边栏 settings/help 入口从 `goBranch` 改为 `context.push('/settings')` / `context.push('/assistant')`。
- **MED-5**：`ReportPage` 指标点击由 `selectTab(1)` 改为 `context.push('/record')`，并保留过滤 provider 设置。
- **MED-6**：`AssistantPage` 已在 Phase 1 使用 `AppBackButton`。
- **MED-7**：`RecordDetailPage` 编辑通过 `pushAuthRequiredRoute` 使用 `context.push('/record/$id/edit')`。

### 测试更新

- `test/app/router_test.dart`：重构为验证「5 个主 Tab 根路由在 Shell 内」与「所有 create/detail/edit / settings / assistant / notifications / auth / account 路由为顶层全屏路由」；新增 `/medicine/search` 在桌面端隐藏侧边栏、移动端隐藏底部导航的测试。
- `test/app/shell_page_test.dart`：通过，验证 5 个 Tab 与桌面 sidebar settings/help 动作仍可渲染。

---

## UX 审计 HIGH 收尾：返回按钮统一 + Mock 数据标记

### 完成内容

- **返回按钮收尾**：
  - `AuthBackButton` 删除；`/login`、`/register`、`/forgot-password` 统一使用 `AppBackButton(fallbackRoute: '/')`。
  - `lib/features/auth/presentation/widgets/auth_back_button.dart` 删除，`auth_shell.dart` 中对应 export 移除。
- **HIGH-1**：所有 mock repository 中用户可见的逼真数据改为占位/demo 标记：
  - `MockMineRepository`：`Lumi User` → `[DEMO] User`，`lumi@example.com` → `demo@example.com`，登录时间改为 `2099-01-01`。
  - `MockTodayRepository`：`previewDashboard` 中心率/血压/睡眠/待办时间等具体数值改为 `--`/`--:--`。
  - `MockMedicineWorkspaceRepository`：`previewWorkspace` hero 指标改为 `--`。
  - `MockRecordRepository`：血压、体重、趋势点等改为 `--` 或空数组。
- **HIGH-2**：`MockMedicineSearchRepository` 的 ID 改为 `__mock_cn_ibuprofen__` / `__mock_cn_acetaminophen__`，名称/副标题/摘要加 `[DEMO]` 前缀。

### 测试更新

- `test/search/search_page_test.dart`：同步更新为新的 demo ID 和名称。

### 验证

- `flutter analyze`: No issues found
- `flutter test`: 495 / 495 全部通过

---

## UX 审计 HIGH 项 Phase 4：错误/空态补强

### 完成内容

- **HIGH-12**：`TodayRecommendationSection` error 状态由 `SizedBox.shrink()` 改为 `AppStateErrorView` compact 版本，显示标题、描述与重试按钮。
- **HIGH-13**：`MedicineReminderDetailPage` error 状态区分 404（药品不存在）与通用加载失败：
  - 404：显示 `medicineReminderNotFoundTitle` + `medicineReminderNotFoundDescription`
  - 通用错误：显示 `medicineReminderGenericErrorTitle` + `medicineReminderGenericErrorDescription`
- **组件增强**：`AppStateErrorView` 新增 `compact` 参数；非 compact 模式使用 `LayoutBuilder` 兼容有限高度父节点与滚动容器，避免在 `SingleChildScrollView` 内出现无限高度异常。

### 新增/更新文案（ARB）

- `todayRecommendationErrorTitle`
- `todayRecommendationErrorDescription`
- `medicineReminderNotFoundDescription`
- `medicineReminderGenericErrorTitle`
- `medicineReminderGenericErrorDescription`

### 测试更新

- `test/today/today_page_test.dart`：新增推荐区 error 重试测试。
- `test/medicine/medicine_reminder_pages_test.dart`：新增 404 与通用错误两种错误态测试。

### 验证

- `flutter analyze`: No issues found
- `flutter test`: 全量通过
