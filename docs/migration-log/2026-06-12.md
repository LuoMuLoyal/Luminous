# 2026-06-12

## Report frontend contracts

- wired `Report` to the new Lucent `/api/v1/user/reports/dashboard` contract instead of the old static mock-only provider path
- added `ReportRemoteDataSource` and `LucentReportRepository`, and mapped generated OpenAPI report DTOs into a dynamic frontend report domain model
- removed `ReportCopyKey`-driven fake data modeling so score summary, findings, pattern text, metric values, and trend values now come from the backend contract
- changed Report page behavior to fetch real data on entry for signed-in users, keep skeleton loading during fetch, and show an explicit signed-out gate instead of rendering a fake report as the main path
- kept report export cards as frontend placeholders because export contracts/files are still out of scope for this phase
- updated report widget tests to cover signed-in rendering, loading skeleton behavior, and signed-out gating against the new provider flow

## Report chrome and refresh cleanup

- removed remaining report-layer hardcoded English chrome from repository/entity mapping and moved metric titles, summary mode copy, and export card copy back under frontend l10n ownership
- changed report summary/export domain modeling to enum-driven UI chrome so the data layer only carries dynamic backend content and stable action kinds
- added mobile `RefreshIndicator` as the primary manual refresh path for Report while keeping the explicit sync button as a fallback action
- updated report widget tests to cover pull-to-refresh without relying on duplicated loading copy

## Report signed-out state alignment

- changed Report signed-out behavior from a full-page auth error gate to an in-tab placeholder dashboard with an inline login notice
- aligned Report with the existing `Today / Record / Mine` protected-tab pattern: signed-out root tabs keep their chrome and placeholder sections instead of rendering an error page
- kept protected-data requests blocked while signed out; the signed-out dashboard is local placeholder data only

## OpenAPI generated-noise cleanup

- updated `tool/regenerate_lucent_openapi.dart` to pass generator global properties that disable generated Markdown docs and generated package stub tests
- added a post-generation cleanup step that removes legacy `packages/lucent_openapi/doc/` and `packages/lucent_openapi/test/` directories and strips those entries from `.openapi-generator/FILES`
- kept the generated Dart client itself committed and analyzable; only low-value doc/test noise is being removed

## Report phase-2 closeout and review triage

- folded the completed report-contract plan back into `Current_State.md`, `Next_Plan.md`, and guardrails, then removed the now-finished `plans/2026-06-12-report-contracts.md`
- reviewed the external `problum.md` audit instead of importing it blindly; kept only the findings that still reflect real unresolved work
- confirmed that guest preview repositories for signed-out root tabs are intentional current architecture, not accidental production data-source drift
- confirmed that Report already has repository-level real-contract mapping coverage, so the remaining gap is the remote-data-source/request path rather than "report has only mock tests"
- carried forward the still-real backlog only:
  - sleep scope drift across Today / Record / Settings placeholder surfaces
  - user-visible `mock data` and fake medicine-name placeholder copy
  - missing frontend controller/HTTP-layer tests for settings, export, report remote fetch, and health-context writes
  - Lucent config hardening around fallback secrets plus `testing-support` argon2 option alignment

## Today AI analysis manual path

- wired Today's AI summary card to the real Lucent `POST /api/v1/user/today-analysis/generate` contract instead of keeping it as a static placeholder-only panel
- added frontend Today AI entity/data-source/repository/provider layers and kept the card non-blocking relative to the rest of the Today page
- aligned the card with existing auth/settings boundaries:
  - signed-out users keep the card shell and see a login-required hint
  - users with `aiSummariesEnabled=false` see a settings-directed disabled state
  - loading, success, and retryable error states stay local to the card
- split Today tests by responsibility instead of continuing to grow one mixed file:
  - `today_page_test.dart` for page/layout smoke coverage
  - `today_ai_card_test.dart` for widget-state coverage
  - `today_ai_analysis_provider_test.dart` for provider-state coverage
- fixed a real Riverpod initialization hazard in `TodayAiAnalysisController`: the old `ref.listen(... fireImmediately: true)` path could read/write self state during initialization

## Today AI backend locale support

- moved Today AI fallback copy and prompt-user-copy off service-local hardcoded Chinese strings and into Lucent i18n resources
- Today AI now follows request language for `zh-CN` and `en` in:
  - fallback summary/bullets/action/confidence note
  - prompt user instructions for the model call
- expanded Today AI policy blocking to catch English dosage/stop-medication wording too, not only Chinese safety-violation text
