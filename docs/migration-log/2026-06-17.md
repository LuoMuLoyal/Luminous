# 2026-06-17

## MVP 收尾与下一阶段切换

- 明确把当前移动端主路径收口为已完成的 MVP：`record -> summarize -> bounded medicine safety check -> export`。
- 更新 `docs/Current_State.md`、`docs/TODO.md`、`docs/Next_Plan.md`，消除“导出已经是真能力”与“真实导出还没做完”之间的文档冲突。
- 删除已经完成使命的 `plans/2026-06-15-mvp-closeout-plan.md`，新建 `plans/2026-06-17-report-export-productization.md` 作为当前执行计划。

## 导出产品化第一轮

- 在 `data_export_controller.dart` 中抽出统一的 `DataExportUiStatus`，把导出状态收口为 `idle / requested / processing / completed / completedLinkMissing / failed / unavailable`。
- Report 导出点击后的反馈改为真实生命周期文案：
  - 已提交
  - 处理中
  - 已完成并尝试打开
  - 已完成但下载链接缺失
  - 失败
  - 当前环境暂不可用
- Settings 导出行改为展示与 Report 同源的最新导出状态，不再只用粗粒度旧标签。
- Report 导出卡片现在会在匹配当前导出类型时直接显示最近一次状态，例如“处理中”“已完成”“暂不可用”，而不是永远只显示静态副标题。
- “导出入口还没接通” 的旧文案已移除，改成更符合真实现状的“当前环境暂时无法生成这个导出文件”。

## 验证

- `flutter gen-l10n`
- `flutter test test/settings/data_export_controller_test.dart`
- `flutter test test/report/report_page_test.dart`
- `flutter test test/settings/settings_page_test.dart`
- `flutter analyze`

## 部署后基线与演示基线

- 新增 `docs/MVP_Demo_Baseline.md`，把当前已部署 MVP 的演示前置条件收口为固定基线：后端 smoke、前端检查、full-stack lane、demo 账号/数据要求。
- 更新 `docs/MVP_Demo_Script.md`，明确它只负责现场 walkthrough，部署后核查和 demo baseline 改由 `MVP_Demo_Baseline.md` 承担。
- 更新 `README.md` 和 `docs/README.md`，把 demo baseline 文档加入正式入口。

## 报表导出交接与下一阶段重排

- 新增 `plans/2026-06-17-report-export-finish-pass.md`，把 report/export 剩余简单活整理为单独交接清单：状态文案收口、过期链接处理、真实环境验收、文档闭环。
- 更新 `docs/Next_Plan.md`，把 export 从“下一主线”降级为 finish pass，把更高价值的下一阶段明确切换为 medicine safety depth。
- 更新 `docs/TODO.md`，把导出剩余项收口为客户端收尾，不再把它当成主要产品推进方向。

## AI Chat 阶段切换与路线定稿

- 新增 `plans/2026-06-17-ai-chat-phase1.md`，把下一阶段 AI Chat Phase 1 的目标、边界、交付顺序和依赖选择正式落进计划。
- 更新 `docs/Next_Plan.md`，把“下一主线”从导出/纯用药深挖改成“后端优先的受限工具 AI chat foundation -> 简单移动端聊天面”。
- 更新 `docs/Current_State.md` 与 `docs/TODO.md`，明确当前还没有真实 AI chat/agent 会话面，只有 Today/Report 总结和记录候选解析。
- 明确前端路线：
  - 先不上 `flutter_ai_toolkit` 作为基础
  - 先保留自控聊天页 + 流式 Markdown 渲染器的方向
  - `flutter_ai_chat_ui` 只作为后续可选 UI 加速器重新评估
