# Migration Log - 2026-06-13

## Report AI Summary Range Switch

- Regenerated `packages/lucent_openapi` from Lucent after the report AI summary contract moved from weekly-only naming to range-based summary naming.
- Switched the Flutter report AI summary data source/repository from `/api/v1/user/reports/weekly-summary/generate` to `/api/v1/user/reports/summary/generate`.
- Added local `ReportAiSummaryRange` mapping for `last_7_days` and `last_30_days`.
- Added an in-card `近 7 天 / 近 30 天` toggle for report AI summary and kept each range's loading/success/error state independent through family notifiers.
- Updated report AI summary tests to cover the new endpoint, DTO names, and range-aware generation flow.

## Verification

- `cd Lucent && pnpm export:openapi`
- `cd Luminous && dart run tool/regenerate_lucent_openapi.dart`
- `cd Luminous && flutter analyze`
- `cd Luminous && flutter test test/report`

## Sleep Contract + Persistence MVP

### Lucent Backend

- Added `sleep` to `DailyRecordKind` Prisma enum with migration `20260613140000_add_sleep_to_daily_record_kind`.
- Added optional `payload` field to `CreateDailyRecordDto`, `UpdateDailyRecordDto`, and `DailyRecordItemDto`.
- `DailyRecordsMapperService` passes through `payload` in `toItem` and `toRecordUpdateData`.
- `DailyRecordsService.create` writes `payload` to database.
- `TodayAnalysisContextService` reads real sleep data from payload (durationMinutes, quality, startAt, endAt, deepMinutes, lightMinutes, remMinutes).
- `ReportsContextService.buildSleepSeries` computes real sleep hours from persisted `durationMinutes`.
- `ReportsComputationService.buildSleepMetric` computes real sleep status (>=7h good, >=5h stable, <5h needs_attention).

### Luminous Frontend

- Added `sleep` to `DailyRecordKind` enum in `daily_record.dart`.
- Added `payload` field to `DailyRecordItem`, `DailyRecordCreateInput`, `DailyRecordUpdateInput`.
- `DailyRecordRemoteDataSource` serializes/deserializes `payload` in create/update/get.
- `record_type_mapping.dart`: `RecordEntryType.sleep` ↔ `DailyRecordKind.sleep` bidirectional mapping.
- Unlocked sleep quick action and filter in `LucentRecordRepository` and `MockRecordRepository` (removed `locked: true`).
- Added sleep to timeline entry switch (icon, accent, titleKey).
- `daily_record_form_fields.dart`: sleep form rules (showValue + showUnit), kind label, value label, added to `activeDailyRecordKinds`.
- `record_detail.dart`: sleep kind icon and label.
- `LucentTodayRepository`: reads sleep payload from daily record summary, formats duration as `Xh` label.
- Added `recordCreateValueSleep` l10n key (EN: "Duration (e.g. 8h)", ZH: "时长（如 8h）").
- Regenerated `packages/lucent_openapi` client.
- Updated tests to reflect sleep being unlocked and new sleep context interface.

### Verification

- `cd Lucent && pnpm lint && pnpm typecheck && pnpm build && pnpm test:ci && pnpm export:openapi`
- `cd Luminous && dart run tool/regenerate_lucent_openapi.dart && flutter analyze && flutter test`
