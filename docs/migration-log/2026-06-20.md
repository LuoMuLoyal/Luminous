# 2026-06-20

## Record

- Added the first lightweight Record fast-entry flow for `water`.
- Changed Record quick-action behavior so signed-in `water` taps open a bottom
  sheet fast-entry surface first instead of jumping straight to the full create
  page.
- Kept the existing full create flow available through a `more` action from the
  fast-entry sheet.
- Hid the kind selector inside the fast-entry sheet while reusing the existing
  Record field rules and save path.
- Added focused Record tests for:
  - `water` fast-entry save
  - `water` fast-entry `more` navigation to the full create page
  - signed-out quick-action auth gating remaining intact
- Extended the same fast-entry path to `meal`, keeping the full create page
  behind the `more` action.
- Added focused Record tests for:
  - `meal` fast-entry save
  - `meal` fast-entry `more` navigation to the full create page

## Planning

- Added `plans/2026-06-20-record-fast-entry.md` to drive the per-type
  implementation / test / commit sequence for the full fast-entry slice.
