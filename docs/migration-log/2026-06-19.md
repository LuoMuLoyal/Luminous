# Migration Log - 2026-06-19

## Assistant Read/Proposal Contract Tightening

- Updated frontend assistant proposal models and parsing to accept backend `target`, `constraints`, and `expiresAt` fields on streamed proposal cards.
- Regenerated `packages/lucent_openapi` after the Lucent assistant proposal DTO contract change.
- Refreshed assistant widget-test fixtures so proposal cards use the new explicit target/constraint metadata.
- Documented that assistant write proposals are now richer snapshots, while high-certainty read envelopes remain backend-side prompt context rather than a public Flutter DTO.
- Upgraded assistant proposal cards to actually render target labels, match hints, setting keys, expiry time, and confirmation constraints instead of dropping that metadata on the floor.
- Added expired-proposal UX guardrails: expired proposals now show an inline warning and the confirm action is disabled before any write flow is reached.

## Documentation Boundary Cleanup

- Trimmed `Next_Plan.md` back to actual next-step sequencing and removed status-style content that belonged in state/history docs.
- Tightened `docs/README.md` and `Project_Guardrails.md` so plan/state/history boundaries are stated explicitly instead of relying on convention.
