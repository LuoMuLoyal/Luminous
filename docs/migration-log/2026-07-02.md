# Migration Log - 2026-07-02

## Luminous

- Extracted `_nonEmptyOrNull` from the medicine reminder planner into reusable `normalizeNullableText` in `lib/core/utils/string_utils.dart`.
- Refactored `LucentDioClient` to remove duplicated `BaseOptions`: added a private `_createBaseOptions` factory and exposed `connectTimeout`/`receiveTimeout`/`sendTimeout` as constructor parameters so callers can override the hard-coded 10s defaults.
- Extracted manual JSON type-casting helpers (`asString`, `asNum`, `asMap`, `asList`) from `meal_analysis_payload_parser.dart` into `lib/core/utils/type_conversion_utils.dart` for reuse.
- Moved hard-coded fast-record choices out of `record_fast_entry_dialog.dart` into `lib/features/record/domain/constants/record_fast_entry_choices.dart`, leaving a TODO to load them from remote/local config later.
- Documented the FNV-1a 32-bit hash constants and the `0x7fffffff` sign-bit mask in `MedicineReminderNotificationPlanner`.
- Introduced a `Clock` abstraction (`lib/core/utils/clock.dart`) and injected it into `MedicineReminderNotificationPlanner` so time-based planning can be mocked in tests.
- Verified the client with `flutter analyze` and the full `flutter test` suite.
