# 2026-06-15

- Wired the Report `给校医院 / For clinic` export card to the real Lucent data-export request flow for `hospital + pdf + last_7_days`, including latest-download-url refresh before opening the file.
- Kept `月度报告 / Monthly` and `打印预览 / Print` visible in Report, but changed them to inactive entries until Lucent exposes distinct real export contracts for those actions.
- Updated Settings export to use the same request body and status handling as the new Report export flow instead of the old empty-body POST assumption.
- Extended `tool/regenerate_lucent_openapi.dart` so client regeneration now patches the OpenAPI generator's broken enum defaults for `CreateDataExportRequestDto` in both source `*.dart` and generated `*.g.dart`.
- Added focused Flutter coverage for export request parameters and Report export link opening.
- Unified Medicine risk-check coverage messaging so the main risk-check page and add-before-save precheck both surface an explicit summary when manual entries or missing source detail cannot be checked.
- Expanded the bounded Medicine rule set for current MVP safety checks: allergy matching now covers a small reviewed Chinese/English alias map, pediatric/geriatric warnings are age-gated, pregnancy/lactation can emit explicit low-confidence warnings when source text is missing, and duplicate-ingredient detection now also covers reviewed DrugBank synonym overlap.
- Added broader Medicine risk-check tests for duplicate ingredients, allergy matching, age-gated special-group warnings, missing-source warnings, and DrugBank synonym overlap.
