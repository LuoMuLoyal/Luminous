# Migration Log - 2026-06-11

## Mine And Settings Contracts — Luminous Frontend (Phase 3 & 4)

### Mine Campus Services Remote API

- Replaced static `_campusServices` in `LucentMineRepository` with a live call through `supportResourcesProvider('campus')`, backed by `GET /api/v1/public/support-resources?scope=campus`.
- Added `_mapSupportResource` to convert `SupportResourceDto` → `MineActionEntry`, including string-to-`IconData` mapping (`local_hospital`, `favorite`, `medical_services`, `emergency`, `school`, `support`, etc.) and color routing.
- API failures gracefully fall back to `_fallbackCampusServices` so the Mine page never blocks on a missing network.
- Removed the redundant `.timeout(const Duration(seconds: 5))` from `mine_dashboard_provider.dart`; Dio already enforces its own connect/send/receive timeouts.

### Settings Privacy / AI Toggles

- Extracted the privacy group into `_PrivacySettingsSection`, reading from `userSettingsControllerProvider`.
- Privacy report row now shows `dataSharingConsent` state dynamically (`minePrivacyShareAfterGrant` vs `minePrivacyOnlyMe`) and tapping it toggles consent via `PATCH /api/v1/user/settings`.
- AI summary row now binds its `Switch` to `aiSummariesEnabled` and tapping toggles the value remotely. Unsigned-in users see the auth-required dialog instead of a mute toggle.

### Settings Reminder Summary Reads Local State

- Extended `NotificationSettingsController` with `waterReminders` and `sleepReminders`, persisted via `SharedPreferences` alongside the existing medication/health/weekly keys.
- Extracted the reminders group into `_ReminderSettingsSection`; medicine/water/sleep rows now read `notificationSettingsControllerProvider` and display `mineReminderEnabled` / `mineReminderDisabled` instead of a hardcoded "已开启".

### Data Export Status

- Extracted the advanced group into `_AdvancedSettingsSection`; the export row reads `dataExportControllerProvider` (latest request via `GET /api/v1/user/data-export-requests/latest`).
- Trailing label switches on `DataExportStatus`: `requested`/`processing` → `mineExportStatusPending`, `completed` → `mineExportStatusCompleted`, `failed` → `mineExportStatusFailed`.
- Tapping triggers `POST /api/v1/user/data-export-requests` and shows a toast confirming the request. No file download is performed.

### Help / About Real Pages

- Help row fetches `supportResourcesProvider('help')` and opens a `showModalBottomSheet` listing each resource title/subtitle.
- About row fetches `appInfoProvider` (`GET /api/v1/public/app-info`) and opens a bottom sheet showing app name, version, and description.

### Localization

- Added five new ARB keys:
  - `mineReminderDisabled`
  - `mineExportStatusPending`
  - `mineExportStatusCompleted`
  - `mineExportStatusFailed`
  - `mineExportRequested`
- Regenerated Flutter localizations (`flutter gen-l10n`).

### Post-Review Fixes

- **P1 — Signed-out Settings must not call protected APIs**: `_PrivacySettingsSection` and `_AdvancedSettingsSection` now guard `ref.watch(userSettingsControllerProvider)` and `ref.watch(dataExportControllerProvider)` behind `signedIn`. When signed out the widgets render with default local values (`false` / `null`) and never trigger `GET /api/v1/user/settings` or `GET /api/v1/user/data-export-requests/latest`.
- **P2 — Mine campus resources display backend title/subtitle**: `_ServiceRow` now uses `entry.rawTitle ?? mineCopy(...)` and `entry.rawSubtitle ?? mineCopy(...)` so backend-provided text takes precedence over localization keys. `_mapSupportResource` now maps resource icons to distinct `MineCopyKey` pairs (`campusSupportTitle`, `campusPharmacyTitle`, `campusEmergencyTitle`, `campusHospitalTitle`) instead of hardcoding the hospital key for every entry.

### API Namespace Rename (/me → /user)

- Renamed Lucent controller paths from `/api/v1/me/*` to `/api/v1/user/*` for all user-scoped business data:
  - `user-health-context`, `daily-records`, `medicine-dose-logs`, `medicine-reminders`, `reminder-deliveries`, `user-settings`, `data-export`.
- Renamed controller methods (`getMeHealthContext` → `getUserHealthContext`, `updateMeHealthContextProfile` → `updateUserHealthContextProfile`) so generated OpenAPI operationIds no longer contain `Me`.
- Updated Lucent e2e tests, README, and public contract docs (`mine-settings-contract.md`, `reminder-contract.md`) to reflect new paths.
- Regenerated Luminous OpenAPI client; all generated Dart paths and method names now use `/user/`.
- Updated Luminous hand-written data sources and their tests:
  - `health_context_remote_data_source.dart`
  - `medicine_reminder_remote_data_source.dart`
  - `dose_log_remote_data_source.dart`
  - `daily_record_remote_data_source.dart`
  - `settings_profile_remote_data_source.dart`
- Updated provider comments (`user_settings_controller.dart`, `data_export_controller.dart`) and entity comments (`health_context_snapshot.dart`).
- Final `rg` sweep confirms no runtime code references to `/api/v1/me/` or `Controller*Me` operationIds remain.

### Verification

- Lucent: `pnpm lint:check`, `pnpm build`, `pnpm test:ci` (193 tests), `pnpm export:openapi` — all clean.
- Luminous: `flutter analyze` — no issues; `flutter test` — 165 tests pass.
- OpenAPI baseline: 44 paths / 123 schemas.

### Emulator E2E Stability

- Replaced integration-test `pumpAndSettle()` usage with bounded `settleE2e`, `pumpUntilFound`, `tapVisible`, and `tapSettingsFooterAction` helpers to avoid silently waiting on ongoing frames for minutes.
- Changed the default offline E2E auth override to a confirmed signed-out state instead of leaving session restore in `isLoading`.
- Updated account, auth, mine, settings, medicine, record, and app-smoke integration tests to use stable widget keys/current Mine section keys where needed.
- Added stable Mine signed-out login and Medicine dose action keys so E2E flows do not depend on Chinese-only text when the emulator resolves English strings.
- Fixed the Medicine dose action fake to exercise `markForDate`, matching the production path, and kept signed-out Medicine from exposing protected dose actions.
- Added mobile Record previous/next date controls with stable keys; the emulator E2E now verifies mobile date reloads instead of desktop-only header actions.
- Fixed narrow mobile overflow in Medicine dose action buttons by allowing action labels to ellipsize inside constrained rows.
- Updated `tool/regenerate_lucent_openapi.dart` to fix generated API Dart lines that would fail `git diff --check`, without normalizing unrelated generated docs.
- Device E2E should be run sequentially against a single emulator. Concurrent runs can race app install / VM service attachment; a sequential rerun of the affected scenario is required before treating it as a product failure.
- Emulator verification passed sequentially for `auth_entry_e2e_test.dart`, `medicine_e2e_test.dart`, `record_navigation_e2e_test.dart`, `mine_e2e_test.dart`, `settings_preferences_e2e_test.dart`, `settings_advanced_e2e_test.dart`, and `app_smoke_test.dart` on `emulator-5554`.
- Completed the Mine/Settings execution plan and removed `plans/2026-06-10-mine-settings-contracts.md`; durable facts now live in current docs and this migration log.
