# Migration Log - 2026-07-01

## Luminous

- Verified the two temporary review docs under `plans/` against the current repo state and folded the stable outcomes back into repo docs instead of keeping stale audit notes as active plan files.
- Fixed Record voice input to stop hardcoding `zh_CN`; speech locale selection now follows the current app locale and falls back to any available locale with the same language when an exact id is unavailable.
- Fixed Record OCR to stop hardcoding the Chinese recognizer script; OCR now uses the Chinese script only for Chinese UI locales and uses the Latin recognizer for other locales.
- Added a dedicated `recordOcrRecognitionFailed` l10n key and switched OCR failure feedback away from the unrelated NLP input-required toast.
- Added small focused tests for speech locale resolution, OCR script selection, and voice sound-level normalization.
- Corrected repo docs that had drifted from reality: `Current_State.md` now describes Record voice/photo-OCR intake accurately, and `Next_Plan.md` / `TODO.md` now distinguish already-shipped Record OCR from still-deferred medicine-side recognition flows.
- Dropped the manual `minSdk = 31` pin in Android Gradle config and restored Flutter's default `flutter.minSdkVersion`; Android 12 splash differences are now treated as a resource-layer concern, not a reason to hard-cut Android 11 and below.
- Corrected repo engineering docs to match the current client/tooling reality: generated Lucent client lives under `packages/lucent_openapi/`, `integration_test/` is split between offline/mock flows and local full-stack emulator lanes, and contract regeneration remains a manual pre-merge step outside the current CI pipeline.
- Added a lightweight local contract-sync gate: `tool/verify_lucent_openapi_sync.dart` now checks that Lucent `docs/openapi.json` and Luminous `packages/lucent_openapi/` are both committed, and `tool/run_daily_checks.dart` now includes that gate.
- Integrated Lucent meal-analysis hot fields into the Record frontend boundary: `DailyRecordItem` now preserves meal analysis status/coverage/summary fields, and the record timeline surfaces meal estimate status plus recognized dish summary when available.
- Added Record meal-analysis detail and edit flows: meal detail now renders a conservative layered summary card from `payload.mealAnalysis`, while meal edit now supports dish-level editing through `payload.mealInput.recognizedDishes` and an explicit confirm-result action that saves `mealAnalysis.analysisStatus = confirmed`.
- Added record-local meal-analysis parsing/models/widgets plus focused test coverage for remote-data mapping, meal timeline mapping, meal detail rendering, and meal edit payload writes.
- Fixed the meal-analysis read path to use the regenerated `packages/lucent_openapi` client again after Lucent `docs/openapi.json` was refreshed with the missing meal hot fields; removed the temporary handwritten GET-envelope dependency from the Record frontend data source.
