# Migration Log - 2026-07-01

## Luminous

- Verified the two temporary review docs under `plans/` against the current repo state and folded the stable outcomes back into repo docs instead of keeping stale audit notes as active plan files.
- Fixed Record voice input to stop hardcoding `zh_CN`; speech locale selection now follows the current app locale and falls back to any available locale with the same language when an exact id is unavailable.
- Fixed Record OCR to stop hardcoding the Chinese recognizer script; OCR now uses the Chinese script only for Chinese UI locales and uses the Latin recognizer for other locales.
- Added a dedicated `recordOcrRecognitionFailed` l10n key and switched OCR failure feedback away from the unrelated NLP input-required toast.
- Added small focused tests for speech locale resolution, OCR script selection, and voice sound-level normalization.
- Corrected repo docs that had drifted from reality: `Current_State.md` now describes Record voice/photo-OCR intake accurately, and `Next_Plan.md` / `TODO.md` now distinguish already-shipped Record OCR from still-deferred medicine-side recognition flows.
- Dropped the manual `minSdk = 31` pin in Android Gradle config and restored Flutter's default `flutter.minSdkVersion`; Android 12 splash differences are now treated as a resource-layer concern, not a reason to hard-cut Android 11 and below.
