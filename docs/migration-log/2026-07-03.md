# Migration Log - 2026-07-03

## Luminous

- Phase 2 of the Material-widget migration in `lib/features/medicine/`, `lib/features/record/`, `lib/features/report/`, and `lib/features/scan/`:
  - Replaced the remaining `OutlinedButton` / `ElevatedButton` usages with `FButton` (`FButtonVariant.outline`, `FButtonVariant.destructive`, or `FButtonVariant.ghost` as appropriate). This covered `record_occurred_at_fields.dart`, `meal_dish_editor_section.dart`, `record_nlp_retry_panel.dart`, `record_nlp_dialog.dart`, `daily_record_image_attachment_field.dart`, `report_page.dart`, `report_ai_summary_section.dart`, and `medicine_recognize_dialog.dart`.
  - Removed transparent `Material(color: Colors.transparent)` wrappers from page-level scroll shells and simple container wrappers across medicine/record/report pages, including `medicine_risk_check_page.dart`, `medicine_reminder_edit_page.dart`, `medicine_reminder_detail_page.dart`, `record_page.dart`, `record_create.dart`, `record_edit.dart`, `record_detail.dart`, and the `RecordGuideRow` inside `record_quick_entry_panel.dart`.
  - Replaced `Material + InkWell` tappable wrappers with `FTappable` in `record_mobile_timeline.dart`, `record_timeline.dart`, `record_mobile_filter.dart`, `record_components.dart`, `record_sidebar.dart`, `record_summary_grid.dart`, `record_week_strip.dart`, `sleep_structured_fields.dart`, `record_ocr_entry_dialog.dart`, `report_export_section.dart`, `report_top_bar.dart`, and `report_metrics_grid.dart`.
  - In `record_new_entry_panel.dart` and the `_QuickRecordTile` / `RecordAiInputBar` parts of `record_quick_entry_panel.dart`, replaced the inner `InkWell` widgets with `FTappable` (and `onTap` → `onPress`) but intentionally kept the surrounding transparent `Material` wrappers after bracket-matching issues appeared; per runtime priority, these two files will be revisited in a final cleanup sweep rather than blocking the phase.
  - `flutter analyze` reports 154 issues (info/warnings only, no errors), matching the pre-migration baseline.
