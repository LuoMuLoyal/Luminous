# Migration Log - 2026-07-03

## Luminous

- Phase 2 of the Material-widget migration in `lib/features/medicine/`, `lib/features/record/`, `lib/features/report/`, and `lib/features/scan/`:
  - Replaced the remaining `OutlinedButton` / `ElevatedButton` usages with `FButton` (`FButtonVariant.outline`, `FButtonVariant.destructive`, or `FButtonVariant.ghost` as appropriate). This covered `record_occurred_at_fields.dart`, `meal_dish_editor_section.dart`, `record_nlp_retry_panel.dart`, `record_nlp_dialog.dart`, `daily_record_image_attachment_field.dart`, `report_page.dart`, `report_ai_summary_section.dart`, and `medicine_recognize_dialog.dart`.
  - Removed transparent `Material(color: Colors.transparent)` wrappers from page-level scroll shells and simple container wrappers across medicine/record/report pages, including `medicine_risk_check_page.dart`, `medicine_reminder_edit_page.dart`, `medicine_reminder_detail_page.dart`, `record_page.dart`, `record_create.dart`, `record_edit.dart`, `record_detail.dart`, and the `RecordGuideRow` inside `record_quick_entry_panel.dart`.
  - Replaced `Material + InkWell` tappable wrappers with `FTappable` in `record_mobile_timeline.dart`, `record_timeline.dart`, `record_mobile_filter.dart`, `record_components.dart`, `record_sidebar.dart`, `record_summary_grid.dart`, `record_week_strip.dart`, `sleep_structured_fields.dart`, `record_ocr_entry_dialog.dart`, `report_export_section.dart`, `report_top_bar.dart`, and `report_metrics_grid.dart`.
  - In `record_new_entry_panel.dart` and the `_QuickRecordTile` / `RecordAiInputBar` parts of `record_quick_entry_panel.dart`, replaced the inner `InkWell` widgets with `FTappable` (and `onTap` → `onPress`) but intentionally kept the surrounding transparent `Material` wrappers after bracket-matching issues appeared; per runtime priority, these two files will be revisited in a final cleanup sweep rather than blocking the phase.
  - `flutter analyze` reports 154 issues (info/warnings only, no errors), matching the pre-migration baseline.

## Luminous - Phase 1 continuation

- Completed aggressive Phase 1 Material → Forui migration across `lib/`:
  - Converted all remaining `AlertDialog` / `showDialog` usages to `FDialog` / `showFDialog` in `auth_required_dialog.dart`, `medicine_reminder_delete_dialog.dart`, `record_fast_entry_dialog.dart`, `record_nlp_dialog.dart`, `report_range_picker_dialog.dart`, `record_detail.dart`, `record_edit.dart`, `medicine_box_scan_page.dart`, and `medicine_recognize_dialog.dart`.
  - Replaced `FilledButton`, `TextButton`, `IconButton`, `ElevatedButton`, and `OutlinedButton` with `FButton` / `FButton.icon` and appropriate variants (`ghost`, `outline`, `destructive`) across auth, medicine, record, scan, search, today, mine, assistant, settings, and notification features.
  - Replaced `CircularProgressIndicator` and `LinearProgressIndicator` with `FCircularProgress`, `FProgress`, and `FDeterminateProgress` where applicable.
  - Replaced remaining `InkWell` / `Material + InkWell` tappable surfaces with `FTappable`, including `app_toast.dart`, `today_top_bar.dart`, `today_recommendation_section.dart`, `mine_components.dart`, `assistant_conversation_drawer.dart`, `barcode_scanner_page.dart`, `search` widgets, `record` widgets, and auth pages.
  - Removed the unused `MineProgressRing` widget and the now-unused `AuthTermsNotice` `TextButton.styleFrom` helper.
  - Renamed internal helper classes `_IconButton` / `_CalendarIconButton` to `_IconActionButton` / `_CalendarIconActionButton` to avoid misleading Material naming.
- Verification:
  - `flutter analyze` reports 149 issues (info/warnings only, 0 errors), down from the 154-issue baseline.
  - `flutter test test/mine/mine_edit_pages_test.dart` passes (12/12).
  - `dart format -l 80 lib` applied.

- Post-migration test fixes:
  - `app_toast_test.dart`: added a 200 ms pump after tapping the Forui close button to let `FTappable` animation timers settle.
  - `meal_dish_editor_section.dart`: wrapped the section Column in a `Material(type: MaterialType.transparency)` so the remaining Material `TextFormField` has the required ancestor without restoring a page-level wrapper.
  - `sleep_structured_fields.dart`: replaced the Material `DropdownButtonFormField` with `FSelect<String>.rich` using `FSelectControl.lifted`.
  - `record_nlp_candidate_editor.dart`: deferred controller text updates inside `useEffect` with `Future.microtask` to avoid modifying the Riverpod provider during the widget build phase.
  - `record_page_test.dart`: updated the "clear nullable fields" test to expect 5 `TextField` widgets (kind selector is now an `FSelect`/`FTextField`) and clear indexes 1-4.
  - Final verification: `flutter test --no-pub` passes (877/877), `flutter analyze --no-pub` reports 149 info/warnings (0 errors).

## Luminous - Color migration (decision updated)

- Time: ~09:30 local time.
- Decision updated: do not create a color mapping. Instead, delete the project's existing color system entirely and replace every color reference at its call site with Forui semantic colors.
- Key points agreed:
  - No custom color extension; no custom theme for now; default family remains `neutral`.
  - All project-specific color constants and helper classes (e.g. `AppColors`, theme extensions, color palettes) will be removed.
  - All `Color(0xFF...)` literals in `lib/` will be replaced at the usage site with `context.theme.colors.*` when a `BuildContext` is available.
  - All `Theme.of(context).colorScheme.*` reads will be replaced with `context.theme.colors.*`.
  - Files without `BuildContext` (data/domain/repository layer) will use static `FThemes.neutral.light.touch.colors.*` references, not project constants.
  - `const` contexts that become invalid after switching to non-const Forui colors will be fixed by removing `const` from the relevant widget/literal.
- Action taken: exploratory automated mapping script remains reverted; migration will be performed by directly editing call sites.
- Next step: enumerate all color references in `lib/`, remove color definition files, replace each reference, then verify with `flutter analyze --no-pub` and `flutter test --no-pub`.

## Luminous - Color migration (executed)

- Time: ~09:35 local time.
- Executed the color migration according to the updated decision:
  - Deleted `lib/features/record/domain/entities/record_type_colors.dart` and its dedicated test `test/record/record_type_colors_test.dart`.
  - Removed `MedicineWorkspacePalette` from `lib/features/mine/presentation/widgets/shared/mine_shared.dart` and inlined its usages.
  - Replaced all remaining `Color(0xFF...)` literals in `lib/` with Forui semantic colors (`context.theme.colors.*` in widget code, `FThemes.neutral.light.touch.colors.*` in data/domain/repository code).
  - Replaced all `Theme.of(context).colorScheme.*` reads with `context.theme.colors.*`.
  - Replaced non-transparent `Colors.*` usages (`Colors.white`, `Colors.black`, `Colors.red`, etc.) with Forui semantic colors; kept `Colors.transparent` unchanged.
  - Fixed `const` contexts invalidated by non-const Forui color references by removing `const` or switching `static const` to `static final`.
  - Removed `forui` imports from Dart `part` files and relied on the host library import instead.
  - Updated tests that asserted specific hex colors to assert Forui semantic colors.
- Verification:
  - `flutter analyze --no-pub`: 33 info-level issues (all `prefer_const_constructors` in mock data), 0 errors, 0 warnings.
  - `flutter test --no-pub`: 866/866 passed.
  - `dart format -l 80 lib test` applied.

- Also replaced the few remaining ARGB literals (`Color(0x33...)`) in `record_dashboard.dart` with `FThemes.neutral.light.touch.colors.secondary`.
- Final verification after cleanup:
  - `flutter analyze --no-pub`: 28 info-level issues (`prefer_const_constructors` in mock data), 0 errors, 0 warnings.
  - `flutter test --no-pub`: 866/866 passed.

## Luminous - AppColors semantic color tokens

- Time: ~09:50 local time.
- Revisited the data/domain vs. widget color boundary after the earlier "delete all project colors" migration created dark-mode leakage and hardcoded `FThemes.neutral.light.touch.colors.*` in repositories.
- Introduced a minimal semantic color token enum so the data/domain layers can stay theme-agnostic while widgets still resolve against the current Forui theme:
  - Created `lib/core/design/app_colors.dart` with `enum AppColors { primary, secondary, destructive, muted, background, border, foreground }`.
  - Added `AppColorsExtension.resolve(FColors colors)` and `AppColorsContext.of(BuildContext context)` plus `AppColorsListExtension.resolveAll(FColors colors)` for list mapping.
  - Renamed the file from the earlier `app_color.dart` to `app_colors.dart` and the enum from `AppColor` to `AppColors` to match Dart plural conventions.
- Migrated domain entities from `Color` / hardcoded Forui light colors to `AppColors`:
  - `record_dashboard`: `RecordQuickAction`, `RecordFilter`, `RecordSummaryItem`, `RecordTimelineEntry`, `RecordTrend` accent/soft colors now use `AppColors`.
  - `medicine_workspace`: workspace safety/convenience accent colors now use `AppColors`.
  - `report_dashboard`: report metric/status accent colors now use `AppColors`.
  - `mine_dashboard`: mine status/archive accent colors now use `AppColors`.
  - `search_entities`, `report_ai_summary`: related accent/foreground tokens now use `AppColors`.
- Migrated data repositories to return `AppColors` tokens instead of `Color` or `FThemes.neutral.light.touch.colors.*`.
- Migrated shared public widgets to accept `AppColors` and resolve internally via the current Forui theme:
  - `AppIconBadge` (`color` / `backgroundColor` are `AppColors`).
  - `AppStatusPill` (`color` is `AppColors`).
  - `AppTextAction` (`color` is `AppColors?`).
- Migrated all presentation call sites from `.resolve(context.theme.colors)` or direct `Color` arguments to the new semantic tokens; calendar markers remain `List<Color>` (resolved at render time).
- Updated `integration_test/support/e2e_test_helpers.dart` to use `AppColors` tokens.
- Regenerated freezed files with `build_runner build`.
- Verification:
  - `flutter analyze --no-pub`: 0 issues.
  - Tests were intentionally not run/fixed per the "prioritize speed over tests" directive for this pass.
  - `dart format -l 80 lib integration_test` applied.

## Luminous - Divider → AppDivider migration

- Time: ~10:00 local time.
- Replaced all 39 Material `Divider` / `VerticalDivider` usages in `lib/` with a new shared `AppDivider` wrapper based on `FDivider`.
- Created `lib/core/widgets/common/app_divider.dart`:
  - Wraps `FDivider`.
  - Defaults to `context.theme.colors.border`.
  - Sets padding to zero to match the project's existing 1px separator visual (the default `FDivider` carries vertical/horizontal padding).
  - Supports `Axis.horizontal` / `Axis.vertical`.
- Updated internal helper widgets to delegate to `AppDivider`:
  - `RecordIndentedDivider` (`lib/features/record/presentation/widgets/shared/record_shared_widgets.dart`)
  - `RecordShortVerticalDivider`
  - `_ShortVerticalDivider` in `record_quick_entry_panel.dart`
  - `_MetricDivider` in `medicine_mobile_drugbox_section.dart`
  - `_VerticalMetricDivider` in `today_overview_section.dart`
- Added the import in host library files for `part of` files (not in the part files themselves).
- Removed temporary migration scripts from `tool/`.
- Verification:
  - `flutter analyze --no-pub`: 0 issues.
  - `dart format -l 80 lib` applied.
  - Tests not run per the speed-first directive.

## Luminous - Dialog and Material input migration

- Time: ~10:15 local time.
- Replaced the last 3 `showDialog` usages in `lib/features/record/presentation/pages/record_page.dart` with `showAppDialog` (wrapping `RecordNlpDialog`).
- Replaced the remaining Material input widgets with Forui equivalents:
  - `record_nlp_candidate_review.dart`: `Checkbox` → `FCheckbox`.
  - `meal_dish_editor_section.dart`: `TextFormField` → `FTextField` with `FTextFieldControl.managed`, and removed the leftover transparent `Material` wrapper.
  - `reminder_rows.dart`: `Switch` → `FSwitch`; `DropdownButton<MedicineReminderSoundPreference>` → `FSelect<MedicineReminderSoundPreference>` with `FSelectControl.lifted`.
- Verification:
  - `flutter analyze --no-pub`: 0 issues.
  - `dart format -l 80 lib` applied.
  - Tests not run per the speed-first directive.

## Luminous - Final transparent Material wrapper cleanup

- Time: ~10:30 local time.
- Reset the 24 target files to HEAD, then removed all remaining `Material(color: Colors.transparent, child: ...)` wrappers in `lib/`:
  - `lib/core/widgets/common/app_dialog_shell.dart`
  - `lib/features/assistant/presentation/pages/assistant_page.dart`
  - `lib/features/medicine/presentation/pages/reminder/medicine_reminder_detail_page.dart`
  - `lib/features/medicine/presentation/pages/reminder/medicine_reminder_edit_page.dart`
  - `lib/features/mine/presentation/widgets/sections/mine_account_hero.dart`
  - `lib/features/mine/presentation/widgets/sections/mine_archive_section.dart`
  - `lib/features/mine/presentation/widgets/sections/mine_service_privacy.dart`
  - `lib/features/mine/presentation/widgets/sections/mine_status_overview.dart`
  - `lib/features/notification/presentation/pages/notification_detail_page.dart`
  - `lib/features/record/presentation/pages/record_create.dart`
  - `lib/features/record/presentation/pages/record_detail.dart`
  - `lib/features/record/presentation/widgets/sections/record_new_entry_panel.dart`
  - `lib/features/record/presentation/widgets/sections/record_quick_entry_panel.dart`
  - `lib/features/search/presentation/widgets/sections/search_categories.dart`
  - `lib/features/search/presentation/widgets/sections/search_quick_actions.dart`
  - `lib/features/search/presentation/widgets/sections/search_source_switch.dart`
  - `lib/features/search/presentation/widgets/shared/search_result_widgets.dart`
  - `lib/features/settings/presentation/pages/about_settings_page.dart`
  - `lib/features/settings/presentation/pages/data_export_page.dart`
  - `lib/features/settings/presentation/pages/help_settings_page.dart`
  - `lib/features/settings/presentation/pages/language_settings_page.dart`
  - `lib/features/settings/presentation/pages/notification_settings_page.dart`
  - `lib/features/settings/presentation/widgets/app_settings_master_toggle_page.dart`
  - `lib/features/today/presentation/widgets/shared/today_top_bar.dart`
- Preserved the two `key:` arguments that were on `Material` by moving them onto the child widget (`DecoratedBox` and `FTappable` in `record_quick_entry_panel.dart`).
- Cleaned up temporary helper scripts in `tool/`.
- Verification:
  - `dart format -l 80 lib`
  - `flutter analyze --no-pub`: 0 issues
  - Tests not run per the speed-first directive.

## Luminous - Scaffold/AppBar/Drawer → Forui migration

- Time: ~10:45 local time.
- Replaced the last Material `Scaffold`/`AppBar`/`Drawer` usages in runtime `lib/` with Forui equivalents:
  - `search_page.dart`: `Scaffold` + `AppBar` → `FScaffold` + `FHeader.nested` (desktop keeps no header).
  - `barcode_scanner_page.dart`: `Scaffold` + `AppBar` → `FScaffold` + `FHeader.nested` with title, back button, and torch action.
  - `report_page.dart`: error-state `Scaffold` + `AppBar` → `FScaffold` + `FHeader.nested`.
  - `assistant_page.dart`: removed the outer `Scaffold` + `endDrawer: Drawer(...)` wrapper and migrated the recent-conversations drawer to `showFSheet<void>(side: FLayout.rtl, ...)`.
  - `assistant_conversation_drawer.dart`: replaced `Drawer` with `SizedBox(width: ...)` so it works inside the Forui sheet.
- Removed the now-unused `drawerScaffoldKey` and `GlobalKey<ScaffoldState>` from `assistant_page.dart`.
- Verification:
  - `flutter analyze --no-pub`: 0 issues.
  - `dart format -l 80 lib` applied.
  - Tests not run per the speed-first directive.

## Luminous - Introduce `forui_hooks` and debt paydown plan

- Time: ~12:55 local time.
- Added `forui_hooks: ^0.23.0` to `pubspec.yaml` alongside existing `forui: ^0.23.0`.
- `flutter pub get` succeeded; `flutter analyze --no-pub` reports 0 issues.
- Created active execution plan `Luminous/plans/2026-07-03-forui-debt-paydown-plan.md` covering:
  - Remaining custom token files in `lib/core/design/`
  - Thin `App*` wrappers in `lib/core/widgets/`
  - Hand-built button/card/badge/avatar surfaces across medicine/record/report/today/mine
  - Material icon leakage from data/domain layers and a few UI spots
  - `forui_hooks` adoption rule for all future Forui controller usage
  - Phased cleanup: icons → custom surfaces → wrapper inlining → token finalization → test/CI recovery
- Next immediate actions are documented in the plan: remove `test/**` analyzer exclusion and start replacing presentation-layer `Icons.*` usages.

## Luminous - Local hand-built surface → Forui widgets (section 1.3)

- Time: ~13:45 local time.
- Continued the Forui-first debt paydown by replacing local hand-built surfaces (combinations of `FTappable` + `DecoratedBox`/`Container`) with native Forui widgets (`FButton`, `FCard`, `FAvatar`) across runtime `lib/`.
- Files converted in this pass:
  - **Search**: `search_source_switch.dart` (outline source chips via `FButton.raw`), `search_categories.dart` (ghost category chips + circular icon via `FButton.raw` + `FAvatar.raw`).
  - **Record**: `record_mobile_filter.dart` (outline filter chips), `record_date_bar.dart` (outline date pill and circular step buttons), `record_new_entry_panel.dart` (ghost action chips and voice action bar), `record_timeline.dart` (timeline cards), `record_summary_grid.dart` (summary tiles), `record_ocr_entry_dialog.dart` (outline option cards), `record_components.dart` (header action chip).
  - **Medicine**: `medicine_mobile_shared.dart` (`_DoseActionButton`, `_MedicationAvatar`, `_FilterText`), `medicine_workspace_parts.dart` (header action chip), `medicine_box_scan_page.dart` (method tiles inside `FCard.raw`), `medicine_quick_action_section.dart` (ghost quick-action tiles), `medicine_today_plan_section.dart` (medication plan tiles), `medicine_safety_panel.dart` (alert and promise panels).
  - **Mine**: `mine_components.dart` (header action chip), `mine_service_privacy.dart` (privacy notice card).
  - **Report**: `report_top_bar.dart` (period pill and snapshot status).
  - **Today**: `today_priority_section.dart` (primary action pill).
  - **Notification**: `notification_list_item.dart` (list item card + unread dot).
  - **Assistant**: `assistant_conversation_drawer.dart` (conversation list items).
- Pattern notes:
  - Header action chips and pill/ghost/outline chips now use `FButton.raw` with `FButtonVariant.outline` / `ghost` / `primary` plus `FButtonStyleDelta.delta(...)` to preserve existing visuals without adding new wrapper widgets.
  - Card-like list items, timeline rows, summary tiles, and scan-method tiles now use `FCard.raw` with `style: .delta(decoration: .shapeDelta(...))`.
  - Circular icon surfaces and unread dot indicators now use `FAvatar.raw`; unread dots are rendered as a tiny `FAvatar.raw(size: 8, child: SizedBox.shrink())`.
  - `_DoseActionButton` now picks `colors.primaryForeground` for filled buttons and `color.resolve(colors)` for outline buttons so callers can keep passing semantic `AppColors`.
- Buglets fixed along the way:
  - Wrapped the `decoration` delta for `_QuickActionTile` in `.delta([.all(.shapeDelta(...))])` to match `FButtonStyleDelta.delta`'s `FVariantsDelta` type.
  - Replaced incorrect `context.theme.style.borderRadius.full` with `.pill` (`FBorderRadius` does not expose a `full` getter).
  - Re-added the missing `app_colors.dart` import in `report_export_section.dart` and `report_patterns_section.dart`.
- Verification:
  - `flutter analyze --no-pub`: 0 issues.
  - `flutter test --no-pub` was intentionally not run/fixed per the speed-first directive for this pass.
  - `dart format -l 80 lib` applied.

## Luminous - Section 1.3 surface debt closed

- Time: ~14:00 local time.
- Verified the remaining plan-section-1.3 surfaces and confirmed they are already implemented with native Forui widgets:
  - `today_top_bar.dart`: `_AssistantEntryButton` uses `FButton.raw` with `FButtonVariant.ghost` and `_NotificationButton` uses `FButton` + `FBadge.raw`.
  - `medicine_page.dart`: `_MedicineNotificationButton` uses `FButton.icon` + `FBadge.raw`.
  - `mine_top_bar.dart`: `_IconActionButton` uses `FButton` + `FBadge.raw`.
  - `report_metrics_grid.dart`: `_MetricCard` uses `FButton.raw` + `FCard.raw` + `FAvatar.raw`; `_MetricBadge` uses `FBadge.raw`.
  - `report_export_section.dart`: `_ExportCard` uses `FButton.raw` + `FCard.raw` + `FAvatar.raw`.
  - `report_score_hero.dart`: `_StatusBadge` uses `FBadge.raw`.
  - `report_findings_section.dart`: `_FindingCard` uses `FCard.raw` + `FAvatar.raw`.
  - `report_patterns_section.dart`: `_PatternCard` uses `FCard.raw` + `FAvatar.raw`.
- Updated the active plan: deleted the completed `1.3` and `Phase 2` sections; the next immediate action is now Phase 1 icon cleanup.
- Verification: `flutter analyze --no-pub` reports 0 issues.

## Luminous - Phase 1 Material icons → FLucideIcons

- Time: ~14:00 local time.
- Replaced all remaining `Icons.*` (Material3) usages in runtime `lib/` with `FLucideIcons.*` equivalents.
- Coverage:
  - **Core**: `app_image_placeholder.dart` default icon.
  - **Medicine**: `lucent_medicine_workspace.dart`, `mock_medicine_workspace_repository.dart`, `medicine_workspace.dart` entity, `medicine_copy.dart`, `medicine_mobile_drugbox_section.dart`.
  - **Mine**: `lucent_mine_repository.dart`, `mock_mine_repository.dart`, `mine_dashboard.dart` entity, edit pages (`allergy_edit`, `condition_edit`, `current_medicine_edit`, `profile_edit`).
  - **Record**: `lucent_record_repository.dart`, `mock_record_repository.dart`, `record_dashboard.dart` entity, `record_timeline.dart` trailing-icon mapping.
  - **Report**: `lucent_report_repository.dart`, `lucent_report_ai_summary_repository.dart`, `mock_report_repository.dart`.
  - **Today**: `today_dashboard_view.dart`, `today_view_models.dart`, `today_components.dart`.
  - **Scan**: `barcode_scanner_page.dart` flash toggle.
- Mapping examples:
  - `Icons.search_rounded` → `FLucideIcons.search`
  - `Icons.water_drop_rounded` / `water_drop_outlined` → `FLucideIcons.droplets`
  - `Icons.medication_rounded` / `medication_liquid_outlined` → `FLucideIcons.pill`
  - `Icons.restaurant_rounded` / `restaurant_menu_rounded` → `FLucideIcons.utensils`
  - `Icons.favorite_rounded` → `FLucideIcons.heart`
  - `Icons.dark_mode_rounded` / `nightlight_round` → `FLucideIcons.moon`
  - `Icons.check_circle_outline_rounded` → `FLucideIcons.checkCircle2`
  - `Icons.qr_code_scanner_rounded` → `FLucideIcons.scanLine`
  - `Icons.flash_on` / `flash_off` → `FLucideIcons.zap` / `zapOff`
  - `Icons.more_horiz_rounded` → `FLucideIcons.ellipsis`
- Clean-up after replacement:
  - Removed now-unused `package:flutter/material.dart` imports from the data repositories that only used it for `Icons`.
  - Fixed a `part-of` directive issue in `medicine_mobile_drugbox_section.dart` by removing the auto-added `forui` import from the part file (the host library already imports it).
- Verification:
  - `rg -n "(?<!FLucide)Icons\\." lib` returns no matches (excluding generated `*.g.dart` / `*.freezed.dart`).
  - `flutter analyze --no-pub`: 0 issues.
  - `dart format -l 80 lib` applied.
- Updated the active plan: deleted the completed `Phase 1` section; next action is Phase 3 wrapper inlining.

## Luminous - Phase 3 wrapper inlining

- Time: ~14:30 local time.
- Inlined the thin visual-alias wrappers into native Forui widgets at their call sites and deleted the wrapper files.
- Deleted wrappers:
  - `lib/core/widgets/common/app_header_action_chip.dart` → `FButton` (outline/primary) with icon + label.
  - `lib/core/widgets/common/app_icon_badge.dart` → `FAvatar.raw` for circles; `DecoratedBox` + `Icon` for rectangles (kept existing visuals).
  - `lib/core/widgets/common/app_image_placeholder.dart` → `FCard.raw` + `Icon` + `Text`.
  - `lib/core/widgets/common/app_section_header.dart` → `Row` + `Text` + optional leading/trailing.
  - `lib/core/widgets/common/app_status_pill.dart` → `FBadge.raw` + `ShapeDecoration` + `RoundedSuperellipseBorder` + `Padding` + `Row`.
  - `lib/core/widgets/common/app_text_action.dart` → `FButton` ghost variant.
  - `lib/core/widgets/settings/app_setting_row.dart` → `FTile` + optional `AppDivider`.
  - `lib/core/widgets/settings/app_settings_navigation_row.dart` → `FTile` with chevron suffix + optional `AppDivider`.
  - `lib/core/widgets/settings/app_settings_section.dart` → `Padding` + `Text`.
  - `lib/core/widgets/settings/app_settings_switch_row.dart` → `FTile` + `FSwitch` + optional `AppDivider`.
- Kept wrappers (shared behavior/layout, not just visual aliases):
  - `lib/core/widgets/common/app_back_button.dart` — routing fallback logic.
  - `lib/core/widgets/common/app_dialog_shell.dart` / `showAppDialog` — dialog constraints, padding, scroll, keyboard inset.
  - `lib/core/widgets/common/app_divider.dart` — unified `FDivider` style.
  - `lib/core/widgets/common/app_state_views.dart` — skeleton shimmer + full-page error layout.
  - `lib/core/widgets/feedback/app_toast.dart` — custom overlay behavior.
- Affected areas: medicine reminder/detail/log/risk/workspace, record timeline/summary/sidebar, report AI summary, settings master toggle, today sections.
- Fixed a residual `unnecessary_null_comparison` warning in `meal_analysis_status_badge.dart` after inlining (the status icon is always non-null).
- Verification:
  - `flutter analyze --no-pub`: 0 issues.
  - `dart format -l 80 lib` applied.
- Updated the active plan: deleted the completed `Phase 3` section; next action is Phase 4 token finalization.
