# Migration Log - 2026-06-08

## Next Plan Sync

- Confirmed `docs/Current_State.md` already records the current Record mobile logic v1 state, including quick-action routing, selected-date behavior, filter mapping, and unsupported-domain boundaries.
- Updated `docs/Next_Plan.md` so Target 1 is marked completed for the first Record mobile logic pass, and the immediate next work is Record form-by-kind refinement for water, vital, symptom, and note.

## Record Common Form Refinement

- Extracted shared Record create/edit form fields into `DailyRecordFormFields` so field visibility is feature-local and testable.
- Tightened first-pass common type rules: water uses value/unit/note with default `ml`, vital uses title/value/unit/note, symptom uses title/value/note without unit, and note uses title/note without value/unit.
- Kept single-image attachment behavior and Record/Today provider invalidation intact, and also invalidated the edited record detail cache after edit/delete.
- Added Record form widget coverage plus create/edit mapping regression tests for water, symptom, and note.

## Medicine Mobile UI Refinement

- Refined the Medicine tab mobile surface toward the north-star reference with paler teal/orange accents, lower-alpha section backgrounds, and less visual weight.
- Merged the standalone next-dose reminder into the drugbox section while preserving the `medicine-next-reminder` test anchor.
- Removed unnecessary inner card nesting from drugbox rows, safety-engine alerts, and the medicine-record list; those areas now rely on spacing plus simple dividers.
- Compressed the safety engine from large alert cards into a compact single-surface alert list, keeping the existing bounded mock/placeholder safety data.

## Today Mobile UI Refinement

- Refined Today mobile priorities toward the approved reference with smaller tiles, tighter spacing, thinner progress, and compact action pills.
- Removed the Today trend-card section from the active Today layout and replaced it with a flat `today-todo-card` list.
- Added UI-only Today todo rows for system-created medication/hydration tasks and user-created mood/custom tasks; backend todo contracts are not introduced yet.
- Kept skeleton loading scoped to dashboard-backed todo status/subtitle fields while static todo labels render immediately.
- Added Today todo l10n keys and widget coverage for mobile render/loading states.

## Mine / Settings Responsibility Split

- Removed privacy permissions, reminder preferences, and secondary settings sections from the Mine tab UI and Mine dashboard domain model.
- Kept Mine focused on account header, health archive, campus services, and the privacy notice row; the Mine header settings icon still routes to `/settings`.
- Moved privacy entries, reminder entries, data export, help, and about actions into the Settings page, using Settings-owned row/surface components instead of Mine UI components.
- Updated Mine and Settings widget tests to lock the new boundary: moved sections no longer render in Mine, while Settings owns the migrated rows and notification reminder routing.

## Protected Entry Signed-Out Dialog

- Added a shared auth-required dialog flow for protected entry taps. Confirmed signed-out users now stay on the current page and see `尚未登录 / 是否去登录`, with no punctuation in the Chinese prompt.
- Cancel dismisses the dialog and leaves the current page unchanged; choosing login opens `/login?returnTo=<current page>`, and successful login returns to that original page.
- Applied current-page interception to Mine archive/account entries, Record quick actions and timeline/detail entry taps, Settings account entry, Medicine dose marking, and Search add-to-current-medicine.
- Kept `AuthRequiredDialogGate` on protected edit/detail pages only as a direct/deep-link fallback, while preserving auth-restoring skeleton states and preventing protected repository calls while signed out.
- Added `authLoginRequiredPrompt`, regenerated `app_localizations*.dart`, and updated widget tests for Mine, Record, Search, Settings, and Login return-to behavior.

## Medicine Closed Loop v1

- Wired Medicine dose marking through `markForDate`: the client now lists today's dose logs first, updates an existing same-day current-medicine dose log when present, and only creates a new one when needed.
- Derived Medicine drugbox rows from active Lucent current medicines only, mapped today's taken/skipped/pending state from dose logs, and hid dose action buttons once a medicine is no longer pending for today.
- Kept formal schedule occurrences and inventory/refill fields as explicit TODOs because Lucent does not expose those contracts yet; the mobile UI now uses localized `提醒未设置`, `库存未跟踪`, and no-pending-dose labels instead of pretending mock stock or reminder data is real.
- Updated Today medication counts to use active current medicines plus today's dose logs, then refresh Today after Medicine dose marking.
- Added dose-log data-source tests for same-day update/create behavior and updated Medicine workspace tests for active-current filtering and completed-state mapping.
- Added Medicine mobile widget coverage for the completed-dose state so taken/skipped medicines keep status text visible but hide today's dose action buttons.
