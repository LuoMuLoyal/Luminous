## Proactive Suggestion Runtime — Task 7 final contract hardening

- Today Analysis generated client 已同步 Lucent 的 envelope DTO；REST data source 解包 generate response 的 `analysis`，无嵌套分析时保留 envelope data 映射，SSE 仍沿用现有手动解析边界。
- `bootstrap_generated_sources.dart` 使用过滤后的 OpenAPI Generator 输出，`verify_lucent_openapi_sync.dart` 校验 Today Analysis GET/refresh 方法；相关参考文档已同步。
- 验证：`flutter analyze --fatal-warnings`、`flutter test`（2933 tests）、`dart format --set-exit-if-changed .`、`dart run scripts/check_doc_coverage.dart --warning-only`、`dart run scripts/check_doc_links.dart` 和 OpenAPI sync 通过。

## Proactive Suggestion Runtime — Task 8 Today 状态机

- Today suggestion domain、远程 mapper 和本地 codec 已接入 `ready / stale / pending / failed / empty` 物化状态及 `sourceVersion / computedAt / retryAfterSeconds` 元数据；旧缓存默认按 `ready` 读取。
- Provider 只通过 GET 读取服务端物化结果，监听相关 `DataChangeBus` 事件做 300ms 去抖刷新，并在应用 resume 时检查 `sourceVersion`；pending、stale、failed 保留旧内容，failed 提供重试。
- UI 已覆盖五种状态，stale 缺少更新时间时显示明确的更新中提示；新增定向测试覆盖状态渲染、缓存兼容、GET 去抖和 resume 版本检查。
