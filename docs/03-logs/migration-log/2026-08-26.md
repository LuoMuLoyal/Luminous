# 2026-08-26 — Review 08-24/08-25 问题修复

## 变更摘要

根据 `plans/Luminous-review-08-24.md` 和 `plans/Luminous-review-08-25.md` 中记录的遗留问题，逐项修复 W-1~W-4（warning）和 S-1~S-4（suggestion）。

## 修改记录

### fix(auth): 清理死分支，统一使用 kPasswordNotSetCode 常量替代 magic string

- `lib/core/errors/lucent_failure.dart` — 新增 `kPasswordNotSetCode` 静态常量，`isPasswordNotSet` getter 改用常量
- `lib/features/auth/presentation/pages/account_settings_helpers.dart` — W-1/W-4：清理 `AUTH_ELEVATION_TOKEN_INVALID` 死分支残留，改用 `LucentFailure.kPasswordNotSetCode` 替代 magic string
- `lib/l10n/src/auth_zh.arb` / `auth_en.arb` — 新增 `authResetPasswordMissingToken*`、`authPasswordNotSetAction` l10n key

### fix(auth): ResetPasswordPage 添加空 token 守卫

- `lib/features/auth/presentation/pages/reset_password.dart` — S-1：添加空 token 守卫，当 `token.isEmpty` 时显示错误状态 + "前往忘记密码"按钮

### fix(settings): 修正导出 dialog 标题并消费 passwordReauthenticationRequired flag

- `lib/features/settings/presentation/pages/data_export.dart` — W-2：移除错误的 `title: l10n.mineSettingExportValue`；W-3：消费 `passwordReauthenticationRequired` flag；S-2：catch 块检测 `AUTH_PASSWORD_NOT_SET`，显示 actionable toast
- `lib/features/report/presentation/utils/export_actions.dart` — W-3/S-2：同上消费 flag + actionable toast

### docs(data_export): 为 requestExport 添加 throws-on-failure 契约注释

- `lib/features/settings/presentation/providers/data_export.dart` — S-3：文档化 throws 契约

### test(e2e): E2E fake 添加密码校验能力

- `integration_test/support/e2e_test_helpers.dart` — S-4：新增 `expectDataExportPassword` 字段，密码不匹配时返回 `AUTH_PASSWORD_INVALID`

## 问题对照

| ID | 级别 | 描述 | 状态 |
| --- | --- | --- | --- |
| W-1 | Warning | `AUTH_ELEVATION_TOKEN_INVALID` 死分支未清理 | 已修复 |
| W-2 | Warning | 数据导出 dialog 使用了错误的 l10n 标题 | 已修复 |
| W-3 | Warning | `passwordReauthenticationRequired` flag 未被消费 | 已修复 |
| W-4 | Warning | `LucentFailure.isPasswordNotSet` getter 未被消费 | 已修复 |
| S-1 | Suggestion | `ResetPasswordPage` 缺少空 token 守卫 | 已修复 |
| S-2 | Suggestion | OAuth-only 用户面对 `AUTH_PASSWORD_NOT_SET` 缺乏可行动引导 | 已修复 |
| S-3 | Suggestion | `DataExportController.requestExport` 缺 catch / 文档 | 已修复 |
| S-4 | Suggestion | E2E fake 缺密码校验能力 | 已修复 |

## 验证

- `flutter gen-l10n` 通过
- `flutter analyze` — 0 issues
