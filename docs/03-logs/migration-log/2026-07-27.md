# 2026-07-27 迁移日志

## 代码审查修复（7-26 审查报告）

来源：`plans/Luminous-review-2026-07-27.md`

### 🔴 严重：`_parseOptionalDateTime` 未捕获 `FormatException`（`lib/features/auth/data/datasources/auth.dart`）

- 原代码 `DateTime.parse(raw)` 在后端返回非空但格式异常的日期字符串时会抛出 `FormatException`，导致 `_authUserFromAccount` 崩溃，用户无法登录或获取账户信息。
- 改为 `try { DateTime.parse(raw) } on FormatException { return null; }`，格式异常时安全降级为 `null`。
- 该修复同时影响 `emailVerifiedAt`、`lastLoginAt`、linked identity 中的 `emailVerifiedAt` 等所有走 `_parseOptionalDateTime` 的字段。

### 🔴 严重：`changeEmail` 未防御性解析 nullable 日期字段（`lib/features/auth/data/datasources/auth.dart`）

- 原代码 `DateTime.parse(response.data!.data.emailVerifiedAt)` 直接解析，若后端返回 null 或格式异常会导致 `FormatException` 崩溃。
- 改为复用同文件已有的 `_parseOptionalDateTime(response.data!.data.emailVerifiedAt)`，与 `_authUserFromAccount` 保持一致的防御性处理。

### 🟡 警告：网络错误被当作非认证错误处理，导致弱网环境下用户被强制登出（`lib/features/auth/presentation/providers/session.dart`）

- 原代码在 `restore()` 的 catch 块中，非认证错误一律 `store.clear()` + 设置未登录状态。网络超时/连接失败等无 HTTP status code 的错误也落入此分支，导致弱网环境下用户被强制登出。
- 新增 `LucentApiException.isNetworkConnectivityError` getter（`lib/core/network/api_exception.dart`），判断 `networkErrorCode` 是否为 `connectionTimeout`、`sendTimeout`、`receiveTimeout`、`connectionError`、`badCertificate`。
- `restore()` 内层 catch（`fetchAccount` 失败）和外层 catch（`refreshSession` 失败）均改为：网络连接错误时保留 session store 仅设置错误提示，网络恢复后可自动恢复会话。

### 测试

- 新增测试覆盖：
  - `changeEmail` 返回 null `emailVerifiedAt` 当日期格式异常
  - `changeEmail` 返回 null `emailVerifiedAt` 当日期为空字符串
  - `fetchAccount` 返回 null `emailVerifiedAt` 当日期格式异常
  - `fetchAccount` 返回 null `lastLoginAt` 当日期格式异常
  - `restore()` 网络错误时保留 session store
- 全部 106 个测试通过（含 13 个新增）。

### 验证

- `flutter analyze` 修改文件：0 issues
- `flutter test test/core/network/ test/auth/`：全部通过

## 新增 EnvelopeInterceptor：拦截器层统一校验业务 envelope

### 背景

`response.data!.data` 强制解引用存在两层风险：
1. **业务错误 `data: null`**：后端返回 HTTP 200 + `{ code: 401001, data: null }` 时，生成的 `fromJson` 因 `@JsonKey(required: true)` 抛出 `CheckedFromJsonException`，此异常未被 `ErrorInterceptor` 捕获，以原始 `DioException(type: unknown)` 传播到调用方。
2. **空响应体**：后端返回 HTTP 200 + 空 body 时 `response.data` 为 null，`response.data!` 直接崩溃。

根因：拦截器链 `[AuthInterceptor, RetryInterceptor, ErrorInterceptor]` 全部只实现 `onError`，缺少 `onResponse` 校验。

### 改动

- 新增 `EnvelopeInterceptor`（`lib/core/network/interceptors/envelope_interceptor.dart`）：
  - `onResponse` 中检查 HTTP 200 响应的 envelope `code` 字段
  - `code != 0` → reject 为 `DioException(type: badResponse)`，携带 `LucentApiException`（`networkErrorCode: businessFailure`），阻止业务错误响应到达生成代码的反序列化阶段
  - 响应体为 null + 2xx 状态码 → reject 为 `LucentApiException(networkErrorCode: emptyResponse)`
  - 支持 `skipEnvelopeCheck` extra 选项跳过（预留文件下载等场景）
  - 非 envelope 响应（无 `code` 字段的 Map）直接放行

- 注册到拦截器链（`lib/core/network/dio_client.dart`）：
  ```
  [AuthInterceptor, RetryInterceptor, ErrorInterceptor, EnvelopeInterceptor]
  ```
  Dio 响应处理为逆序，`EnvelopeInterceptor` 最先执行 `onResponse`；reject 后错误正向流经 `ErrorInterceptor.onError` → `RetryInterceptor.onError` → `AuthInterceptor.onError`，复用现有错误处理管线。

- 从 `lib/core/network/api.dart` 导出。

### 效果

到达调用方的成功响应保证 `code == 0` 且 body 非 null，全项目 20+ 处 `response.data!.data` 不再有崩溃风险。

### 测试

新增 8 个测试覆盖：
- `code == 0` 正常放行
- 非 envelope Map 响应放行
- `code != 0` reject 为 `LucentApiException`（验证 code/message/statusCode/networkErrorCode）
- envelope message 为空时使用 fallback
- `data: null` + `code != 0` 在反序列化前被拦截（关键场景）
- 空响应体 reject 为 `emptyResponse`
- `skipEnvelopeCheck` 选项正常工作

## Mine Hero 动态化 + 个人信息扩展 + 邮箱验证状态

### Hero 区域改动（`account_hero.dart`）

- **动态描述文案**：`_buildDescription` 根据 `_deriveGaps` 返回的缺失字段类型生成针对性描述。单缺口显示对应说明（如 `mineReadinessGapDescWeight`），多缺口显示 `mineReadinessGapDescMultiple` 汇总。
- **缺口检测扩展**：`_deriveGaps` 从 3 项扩展至 6 项：`basicInfo` / `sexAtBirth` / `weight` / `allergy` / `medicine` / `emergencyContact`。
- **移除角色文字"用户"**：Hero 不再显示 `mineAccountStudentRole` 胶囊。
- **邮箱验证状态替换登录胶囊**：`_StateBadge` 根据 `account.emailVerified` 切换显示：已验证 → `primary` 蓝色白字"邮箱已验证"；未验证 → `secondary` + warning 色"邮箱未验证"；preview → `secondary`灰色"预览模式"。
- **缺口标签新增**：`mineCompletenessGapSexAtBirth`、`mineCompletenessGapWeight`、`mineCompletenessGapEmergencyContact` 及对应描述文案。

### 个人信息扩展（`profile_edit.dart`）

- 新增 `HealthSexAtBirth` 下拉框（female/male/intersex/unknown）。
- 新增体重输入框（`profile-weight-field`）。
- 新增紧急联系人姓名（`profile-emergency-contact-name`）和电话（`profile-emergency-contact-phone`）输入框。
- 保存按钮添加 `Key('profile-save-button')` 供测试精确定位。

### 完整度计算更新（`lucent.dart`）

- `_buildCompletion` 总项数从 5 扩展至 7：`onboardingCompleted` / `allergyCount` / `currentMedicineCount` / `birthDate` / `heightCm` / `sexAtBirth` / `weightKg`。
- `_buildProfile` 新增 `weightKg`、`sexAtBirth`、`hasEmergencyContact` 字段映射。

### Domain Entity 扩展（`dashboard.dart`）

- `MineProfileSnapshot` 新增 `weightKg`、`sexAtBirth`、`hasEmergencyContact` 字段。
- `MineAccount` 新增 `emailVerified` 字段（`@Default(false)`）。

### 测试修复

- `page_test.dart`：移除对 `mineAccountStudentRole` 的断言，改为验证 `mineAccountEmailVerified`；完整度百分比从 `80%` 更正为 `57%`（4/7）；`_completeSnapshot` 填充所有 7 项字段（含 sexAtBirth、weightKg、emergencyContact）。
- `edit_pages_test.dart`：保存按钮改用 `Key('profile-save-button')` + `ensureVisible` 定位；修复 `weightKg` 和 `emergencyContact` 字段格式。
- `remote_data_source_test.dart`：mock JSON 响应中 `profile` 添加 `weightKg` 和 `emergencyContact` 字段。
- `profile_remote_test.dart`：同上 mock JSON 修复。
- `record/page_test.dart`：修复 `weightKg` 和 `emergencyContact` 字段格式。

## About 页面重构 — 客户端元数据 + API 精简

### 背景

About 页面原先从 `GET /api/v1/public/app-info` 获取 App 名称、版本、描述和构建日期，但这些值来自后端 `package.json`（如 "lucent"、"1.0.0-dev"、"NestJS backend for Luminous..."），对用户毫无意义。

### 后端改动（Lucent）

- `AppInfoDataDto` 移除 `name`/`version`/`description`/`buildDate`，只保留 `supportEmail` 和 `minClientVersion`。
- `SupportResourcesService` 改为从环境变量 `SUPPORT_EMAIL` 和 `MIN_CLIENT_VERSION` 读取，移除 `readPackageJson()`。
- 新增 `SUPPORT_EMAIL` 和 `MIN_CLIENT_VERSION` 环境变量（可选）。
- OpenAPI 契约已重新生成。

### 前端改动（Luminous）

- **新增依赖**：`package_info_plus: ^10.2.0`，用于获取客户端真实的 App 名称、版本号和构建号。
- **新增 provider**：`lib/features/settings/presentation/providers/package_info.dart`，提供 `PackageInfo.fromPlatform()`。
- **`AppInfo` entity 精简**：移除 `name`/`version`/`description`/`buildDate`，只保留 `supportEmail` 和 `minClientVersion`。
- **`lucent.dart` repository**：适配精简后的 DTO，只映射 `supportEmail` 和 `minClientVersion`。
- **`about.dart` 重写**：
  - 顶部卡片改为横向布局：左侧 App 图标 (`assets/icon/app_icon.png`，64x64) + 右侧 App 名称、版本号、tagline。
  - 版本号格式为 `版本 0.1.0 · Build 1`（来自 `package_info_plus`）。
  - Tagline 走 ARB 国际化（`settingsAboutTagline`）。
  - 移除服务器构建时间展示。
  - 法律文档入口和许可证入口保持不变。
  - 帮助与反馈 tile 继续从 API 获取 `supportEmail`。
- **l10n 改动**：
  - 新增 `settingsAboutVersionLabel`（带 `{version}` 占位符）。
  - 新增 `settingsAboutBuildLabel`（带 `{buildNumber}` 占位符）。
  - 新增 `settingsAboutTagline`。
  - 移除 `settingsAboutBuildNumberLabel`（不再使用）。
  - 移除 `mineSettingAboutValue`（不再使用）。
- **生成客户端更新**：手动更新 `app_info_data_dto.dart` 并重新生成 `.g.dart`。
- **测试更新**：
  - `about_settings_page_test.dart`：重写测试，override `packageInfoProvider`，验证图标/名称/版本/tagline/法律行。
  - `resources_providers_test.dart`：更新 `AppInfoDataDto` 构造和断言，移除已删字段。

## 用药安全风险检查 — 前端重构（数据层 + 安全卡片）

来源：`plans/2026-07-27-medicine-safety-risk-check-redesign.md`

### 背景

原先用药风险检查逻辑全部在 Flutter 客户端执行（6 个 domain/services 文件），每次打开用药 Tab 都要拉取所有药品详情再本地交叉分析。本次将全部检查逻辑迁移到后端 API，前端改为直接消费 API 返回的检查记录。

### API 客户端重新生成

- 使用 `openapi-generator-cli generate -i ../Lucent/docs/openapi.json -g dart-dio -o generated/lucent_api -c openapi_gen_config.json` 重新生成
- 新增 7 个 DTO 文件：`medicine_risk_check_response_dto.dart`、`medicine_risk_check_record_dto.dart`、`medicine_risk_check_records_dto.dart`、`medicine_risk_finding_dto.dart`、`medicine_risk_coverage_issue_dto.dart`、`medicine_red_flag_dto.dart`、`run_risk_check_dto.dart`
- `MedicinesApi` 新增 `medicinesControllerGetRiskCheckV1()` 和 `medicinesControllerRunRiskCheckV1()` 方法
- `openapi_gen_config.json` 配置文件新增到仓库根目录

### 删除的客户端检查逻辑

- `domain/services/risk_checker.dart` — 客户端风险检查器
- `domain/services/risk_checker_utils.dart` — 工具函数
- `domain/services/ingredient_canonicalizer.dart` — 成分标准化
- `domain/services/allergy_severity_helper.dart` — 过敏严重度推断
- `domain/services/red_flag_evaluator.dart` — 红旗评估
- `domain/entities/risk_medicine_detail.dart` — 药品详情包装
- 对应测试文件全部删除

### 新建数据层

- **Domain Entity**（`domain/entities/risk_check.dart`）：新增 `MedicineRiskLevel`、`MedicineRiskCheckType`、`MedicineRiskCheckRecord`、`MedicineRiskCheckRecords` 等；`MedicineRiskCheckResult` 增加 `overallRiskLevel`、`overallRiskScore`、`redFlags`、`overallRecommendation` 字段；`MedicineRiskFinding` 增加 `recommendation` 字段；`MedicineRiskFindingType` 增加 `longTermUse`、`schedulingConflict`
- **Domain Repository**（`domain/repositories/risk_check.dart`）：接口改为 `getRecords()` + `runCheck(type)`
- **Data Mapper**（`data/mappers/risk_check.dart`）：`MedicineRiskCheckMapper` 将生成 DTO 映射到 domain entity，处理所有枚举转换
- **Data Repository**（`data/repositories/risk_check.dart`）：`LucentMedicineRiskCheckRepository` 使用 `LucentClient.medicines` API 调用 GET/POST risk-check 端点
- **Providers**（`presentation/providers/risk_check.dart`）：`medicineRiskCheckRecordsProvider`（keep-alive）+ `runMedicineRiskCheckProvider`（触发检查并 invalidate 缓存）

### 用药安全卡片重构（`mobile_safety.dart`）

- 消除 `FCard` 嵌套 `DecoratedBox` 的盒子套盒子问题
- 改为单一 `FTappable` → `DecoratedBox` 容器，整体可点击跳转
- 新增 `_SafetyHeader`：标题 + 最后检查时间（"上次检查: HH:mm" / "可能已过期"）
- 新增 `_MetricRow`：三个指标（用药数 / 发现数 / 覆盖缺口）横向排列，竖线分隔，语义着色
- 新增 `_AlertChip`：紧凑型 alert（图标 + 单行标题 + severity badge），最多 2 条 + "+N"
- 空状态改为盾牌图标 + "暂无风险数据" 文案，不再用 `FCard` 包裹

### 其他适配

- `MedicineWorkspace` 实体：`riskCheckResult: MedicineRiskCheckResult?` → `riskCheckRecords: MedicineRiskCheckRecords?`
- `lucent_workspace.dart`：`fetchWorkspace()` 改为调用 `getRecords()` 获取完整记录
- `mobile_dashboard_view.dart`：所有 `_SafetyEngineSection` 调用改为传递 `records` 而非 `result`
- `search/presentation/pages/page.dart`：搜索预检改为从 API 获取当前风险记录，不再客户端计算
- `copy.dart`：switch 表达式覆盖新增的 `longTermUse` 和 `schedulingConflict` 枚举值
- 删除 `_snapshotWithCandidate` 方法（不再需要客户端构造快照）
- E2E mock repository 更新为实现新接口
- 旧测试文件删除（6 个），`page_test.dart` 和 `workspace_test.dart` 适配新实体

### 验证

- `flutter analyze`：0 error（10 info/warning）
- `dart run build_runner build`：通过
