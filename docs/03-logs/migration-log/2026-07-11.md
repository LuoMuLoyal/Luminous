# 2026-07-11 Migration Log

## 审查报告修复（luminous-review-2026-07-11）

基于 `plans/luminous-review-2026-07-11.md` 回查报告中未修复的 🟡/🔴 问题，进行以下修复：

### 🟡: mine 页面统一使用 SignInHintBanner

- **文件:** `lib/features/mine/presentation/pages/page.dart`
- **问题:** `MinePage` 在 `isPreview`（未登录预览）场景下未显示 `SignInHintBanner`，与其他四个 Tab 页面（Today/Record/Medicine/Report）UX 不一致。7-8、7-9、7-11 连续三次审查未修复。
- **修复:** `readyBuilder` 中检测 `isPreview`，在 `MineDashboardView` 上方插入 `SignInHintBanner`，`onSignIn` 回调导航到 `loginRouteForCurrentLocation(context)`。新增 `required_dialog.dart` 导入。

### 🟡: `_stateColor` 为不同状态添加颜色区分

- **文件:** `lib/features/medicine/data/repositories/lucent_workspace_repository.dart`
- **问题:** `_stateColor` 对 `taken`/`skipped`/`pending` 三种状态全部返回 `SemanticColor.primary`，用户无法一眼识别待服用药物。
- **修复:** `taken` → `SemanticColor.success`（绿色，已完成）；`skipped` → `SemanticColor.neutral`（中性色，已跳过）；`pending` → `SemanticColor.warning`（警告色，待服用）。

### 🔴: 移除 `updatedAt` 前端不必要的 fallback

- **文件:** `lib/features/medicine/data/datasources/dose_log_remote_data_source.dart`
- **问题:** 后端 `DoseLogItemDto` 已新增 `updatedAt` 字段并正确返回，但前端 `_fromJson` 仍保留 `json['updatedAt'] as String? ?? json['createdAt'] as String` fallback，属于不必要的兼容代码。
- **修复:** 移除 fallback，直接使用 `json['updatedAt'] as String`。

### 🟡: SearchBar 固定高度改为 ConstrainedBox + minHeight

- **文件:** `lib/features/medicine/presentation/pages/page.dart`
- **问题:** `_MedicineMobileSearchBar` 使用 `SizedBox(height: 56)` 固定高度，在系统字体放大场景下可能截断文字。
- **修复:** 改为 `ConstrainedBox(constraints: BoxConstraints(minHeight: 56))`，允许内容超出时自动扩展高度。

### 🔴: 移除 `_slotStatusForReminder` 单提醒 fallback

- **文件:** `lib/features/medicine/data/repositories/lucent_workspace_repository.dart`
- **问题:** 当药品只有 1 个提醒且 dose log 精确匹配（`reminderId|timeLabel`）失败时，回退到药品级别的 `fallbackStatus`（`medicineId → status`）。用户新增提醒后 `reminderCount` 从 1 变为 2，fallback 不再触发，已服药的旧 slot 会错误回退为 `pending`，造成状态误判。
- **修复:** 移除 `reminderCount == 1 && fallbackStatus != null` 分支，slot 状态完全依赖精确的 `doseStatusBySlot` 匹配，匹配失败则返回 `pending`。同时清理 `_slotStatusForReminder` 中不再需要的 `reminderCount` 和 `fallbackStatus` 参数。`fallbackStatus` 仍保留用于 `slots.isEmpty`（无提醒时）的药品级状态展示。

## 测试缺口补测（第一批）

基于 `plans/2026-07-10-test-coverage-gap-analysis.md` 优先级清单，补测 5 个高优先级模块，新增 84 个测试用例。

### text_matcher_test.dart — OCR 文本匹配引擎

- **文件:** `test/scan/domain/services/text_matcher_test.dart`
- **源文件:** `lib/features/scan/domain/services/text_matcher.dart`
- **测试数:** 28
- **覆盖:** 国药准号正则匹配（H/Z/S/B/J 五种前缀，优先级高于条码）、13 位条码匹配（69 开头，优先级高于名称模糊）、CJK 名称分词提取（2-8 字，停用词过滤，去重，最多 5 条）、置信度计算（长度 ≤6 线性增长，封顶 0.85）、边界情况（空字符串/纯空白/非 CJK 文本/中英混合）

### auth_interceptor_test.dart — Token 刷新/401 重试/会话清除

- **文件:** `test/core/network/interceptors/auth_interceptor_test.dart`
- **源文件:** `lib/core/network/interceptors/auth_interceptor.dart`
- **测试数:** 15
- **覆盖:** onRequest — Bearer token 注入、Accept/Accept-Language 头设置、skipAuthorization 跳过、已有 Authorization 不覆盖、null token 不注入；onError — 401+tokenExpired 刷新重试、skipAuthRefresh 跳过、非 401 不刷新、401 非 tokenExpired 清会话、无 refreshToken 清会话、hasRetriedAfterRefresh 防死循环、刷新失败清会话+回调、setter 后置回调、并发刷新去重

### sync_worker_test.dart — 离线同步引擎

- **文件:** `test/core/database/sync/sync_worker_test.dart`
- **源文件:** `lib/core/database/sync/sync_worker.dart`
- **测试数:** 14
- **覆盖:** registerHandler 注册机制、flush — 空队列不操作、成功回放+删除、DioException 标记失败不删除、通用异常标记失败、无 handler 跳过、多条目顺序回放、并发 flush 互斥锁、异常恢复；start/stop 生命周期（含 connectivity_plus 平台通道 mock）；handler 接收完整 entry、不同 entityType 路由到不同 handler

### suggestion_json_codec_test.dart — 建议卡片 JSON 序列化

- **文件:** `test/today/data/utils/suggestion_json_codec_test.dart`
- **源文件:** `lib/features/today/data/utils/suggestion_json_codec.dart`
- **测试数:** 11
- **覆盖:** Bundle 往返（仅 primary / 全填充 / null+空列表）、Card 全字段往返（所有枚举类型、可选字段、secondaryActions、feedbackOptions）、Evidence 往返（含 recordId/medicineId 可选字段）、枚举 fallback（unknown confidence→medium、unknown trigger→timer、unknown feedback→later）、JSON 结构验证（顶层 key + card key）
- **Bug 修复:** 发现并修复 `TodaySuggestionType`、`TodaySuggestionEvidenceKind`、`TodaySuggestionFeedback` 三个枚举的序列化/反序列化不匹配 bug —— codec 使用 `.name`（Dart 枚举名如 `confirmedRisk`）序列化，但 `fromJson` 期望 snake_case（如 `confirmed_risk`），导致往返后枚举值丢失。修复方式：给 `TodaySuggestionType` 和 `TodaySuggestionEvidenceKind` 新增 `toJson()` 方法（`TodaySuggestionFeedback` 已有），codec 中 `.name` 调用改为 `.toJson()`。

### daily_record_json_codec_test.dart — 记录 JSON 编解码

- **文件:** `test/record/data/utils/daily_record_json_codec_test.dart`
- **源文件:** `lib/features/record/data/utils/daily_record_json_codec.dart`
- **测试数:** 16
- **覆盖:** 最小 item 往返（仅必填字段）、完整 water 记录往返、meal 记录往返（含 mealAnalysis 全字段）、meal 分析失败往返、全部 DailyRecordKind 枚举往返、unknown kind fallback→note、null payload 往返、空 mealTopFoods 往返、null mealTopFoods→空列表；Attachment — 完整字段往返、最小字段往返、多附件往返、null attachments→空列表、unknown kind→image；JSON 结构验证（item 顶层 key + attachment key）

## 测试缺口补测（第二批）

基于 `plans/2026-07-10-test-coverage-gap-analysis.md` 优先级清单第 6-8 项及 `core/errors/` 三件套和两个零覆盖拦截器，新增 8 个测试文件共 116 个测试用例。

### lucent_repository_test.dart — AI 助手仓库

- **文件:** `test/assistant/data/repositories/lucent_repository_test.dart`
- **源文件:** `lib/features/assistant/data/repositories/lucent_repository.dart`
- **测试数:** 28
- **覆盖:** getCapabilities 全字段映射（phase/enabled/memory/context/tools/updatedAt、canSendMessages 组合判断、enabledToolCount、$unknown name fallback→空字符串、invalid updatedAt→null）；getLatestConversation（null→null、消息映射、role 映射含 $unknown→assistant、clock.now fallback）；listRecentConversations（列表映射、空列表）；openConversation（conversationId 传递、返回映射）；clearLatestConversation（委托+返回值）；streamMessages（chunk 事件透传、result 事件映射、四种 proposedAction 类型完整映射：create/update/delete_daily_record + update_user_settings、unknown type 过滤、null payload 过滤、confirmationRequired 默认 true/false、payloadVersion 默认 1、chunk+result 顺序事件）

### app_error_test.dart — 应用错误类型

- **文件:** `test/core/errors/app_error_test.dart`
- **源文件:** `lib/core/errors/app_error.dart`
- **测试数:** 10
- **覆盖:** 默认 kind=unknown、全字段保留、toString 各字段条件输出（message+kind 必有、code/statusCode/requestId 条件出现、空 requestId 省略）、AppErrorKind 枚举完整性

### result_test.dart — Result 密封类

- **文件:** `test/core/errors/result_test.dart`
- **源文件:** `lib/core/errors/result.dart`
- **测试数:** 10
- **覆盖:** Success（valueOrNull/errorOrNull/isSuccess/isFailure、fold onSuccess、nullable value、list value）、Failure（errorOrNull/valueOrNull/isSuccess/isFailure、fold onFailure、cause 保留）、switch 模式匹配（Success/Failure）、自定义类型

### run_guarded_test.dart — 统一错误守卫

- **文件:** `test/core/errors/run_guarded_test.dart`
- **源文件:** `lib/core/errors/run_guarded.dart`
- **测试数:** 10
- **覆盖:** 成功路径（String/int/nullable 返回值、Ref from ProviderContainer）、错误路径（generic Exception→unknown、LucentApiException→auth kind+code/statusCode 保留、cause 保留）、ref 处理（null fallback→appTalker、非 Ref/WidgetRef fallback、Ref from provider 正常工作）

### retry_interceptor_test.dart — 重试拦截器

- **文件:** `test/core/network/interceptors/retry_interceptor_test.dart`
- **源文件:** `lib/core/network/interceptors/retry_interceptor.dart`
- **测试数:** 21
- **覆盖:** 可重试错误（500/503/429/408 状态码、connectionTimeout/sendTimeout/receiveTimeout/connectionError 四种 Dio 异常类型）；不可重试错误（400/401/404 状态码、badCertificate/cancel 异常类型）；最大重试次数（2 次后停止、retryCount extra 递增）；方法处理（POST/PUT/DELETE 默认不重试、retryEnabled=true opt-in POST 重试、retryEnabled=false opt-out GET 不重试）

### error_interceptor_test.dart — 错误映射拦截器

- **文件:** `test/core/network/interceptors/error_interceptor_test.dart`
- **源文件:** `lib/core/network/interceptors/error_interceptor.dart`
- **测试数:** 13
- **覆盖:** 信封消息映射（envelope message 优先、code 提取、X-Request-Id 头提取/缺失为 null）、fallback 消息（connectionTimeout/sendTimeout/connectionError/cancel 四种异常类型各对应中文消息、badResponse 无 envelope fallback、空 envelope message fallback）；数据保留（response data 保留为 Map、null body→null data）；statusCode 保留

### reminder_form_fields_test.dart — 用药提醒表单字段

- **文件:** `test/medicine/presentation/widgets/reminder/reminder_form_fields_test.dart`
- **源文件:** `lib/features/medicine/presentation/widgets/reminder/reminder_form_fields.dart`、`reminder_formatters.dart`、`reminder_providers.dart`
- **测试数:** 20
- **覆盖:** FrequencySegments（三个频次标签渲染）；WeekdayPicker（7 个 FilterChip 渲染、选中状态、onToggled 回调）；TimePickerRow（时间 chip+添加按钮渲染、onAddTime 回调、多时间 onDeleted 启用、单时间 onDeleted 禁用、空列表仅添加按钮）；MedicineReminderTimeInput（label 格式化、fromTimeOfDay 转换）；ReminderFrequency 枚举；MedicineReminderSoundPreference（fromStorage null/匹配/未知 fallback、storageValue）；remindersFor（按药品 ID 过滤+按时间排序、无匹配返回空）

### reminder_delete_dialog_test.dart — 用药提醒删除对话框

- **文件:** `test/medicine/presentation/widgets/reminder/reminder_delete_dialog_test.dart`
- **源文件:** `lib/features/medicine/presentation/widgets/reminder/reminder_delete_dialog.dart`
- **测试数:** 4
- **覆盖:** 标题和正文渲染、取消/确认删除按钮渲染、取消返回 false、确认删除返回 true

## 测试缺口补测（第三批 — 高优先级）

基于 `plans/2026-07-10-test-coverage-gap-analysis.md` 高优先级清单，补测 7 个核心模块，新增 173 个测试用例。

### dao_extended_test.dart — DAO 层扩展覆盖

- **文件:** `test/core/database/dao_extended_test.dart`
- **源文件:** `lib/core/database/daos/medicine_dose_log_dao.dart`、`daily_record_dao.dart`、`current_medicine_dao.dart`、`health_context_dao.dart`、`today_suggestion_dao.dart`、`pending_sync_dao.dart`
- **测试数:** 31
- **覆盖:** MedicineDoseLogDao（fetchByDate 空表、replaceByDate 插入/清空/替换/跨日期隔离、insertOptimistic pending 状态、confirmSync 替换乐观副本、cleanup 旧数据清理保留 pending、watchByDate 响应式更新）；DailyRecordDao（markPendingSync、updateData 更新/不存在行不报错、fetchByDate kind 过滤）；CurrentMedicineDao/HealthContextDao/TodaySuggestionDao（clear + replace 覆盖）；PendingSyncDao（pendingCount 空表/计数/排除 syncing、markFailed 不存在行 no-op、isPermanentlyFailed 阈值、fetchReady 立即返回/排除 syncing、enqueue 唯一 ID、remove no-op）

### remote_data_source_extended_test.dart — 认证远程数据源扩展

- **文件:** `test/auth/remote_data_source_extended_test.dart`
- **源文件:** `lib/features/auth/data/datasources/remote_data_source.dart`
- **测试数:** 53
- **覆盖:** OAuth 登录（WeChat Web/Mobile code+state trim、Apple identityToken+可选字段、QQ code+state trim、token 持久化验证）；OAuth 授权 URL（WeChat Web/QQ authorize URL 生成、callbackUri trim/空值处理）；身份链接（WeChat Web/Mobile link、返回 AuthUser 映射）；登出（有 refreshToken 发请求+清 store、无 refreshToken 仅清 store、空字符串 refreshToken 仅清 store）；注册（email/password/code trim、nickname 可选/空字符串省略）；账户获取（linkedIdentities 解析、emailVerifiedAt/lastLoginAt 日期解析、null avatar 处理、空 linkedIdentities）；验证码发送（email trim、scene 区分 resetPassword/changeEmail）；密码重置（全字段 trim、成功完成）；忘记密码（cooldown 消息、email trim）；邮箱验证（成功完成、email+code trim）；账户资料更新（nickname/avatar trim、null 值传递）；密码修改（清 store、trim）；邮箱修改（返回更新后 user、trim）；账户删除（清 store、password trim）；身份解绑（返回 AuthUser）；_authUserFromAccount 边界情况（null avatar/emailVerifiedAt、hasPassword false、空 linkedIdentities、emailVerifiedAt 日期解析）；login code flow（空 password→null、空 code→null）

### lucent_repository_test.dart — 药品搜索仓库

- **文件:** `test/search/lucent_repository_test.dart`
- **源文件:** `lib/features/search/data/repositories/lucent_repository.dart`
- **测试数:** 17
- **覆盖:** search（成功映射结果、空列表、非零业务码抛异常、空 message 默认值、source/query/page/pageSize 参数传递、source=cn 名称映射、默认分页、dataSource 错误传播、null subtitle→空字符串）；fetchDetail（成功返回安全预览、非零码返回 null、dataSource 异常返回 null、null subtitle→空 conditions、空 subtitle→单个空 condition、id+source 传递、单行/多行/含空行 subtitle 处理）

### lucent_ai_repository_test.dart — 今日 AI 仓库

- **文件:** `test/today/lucent_ai_repository_test.dart`
- **源文件:** `lib/features/today/data/repositories/lucent_ai_repository.dart`
- **测试数:** 16
- **覆盖:** generateStream（summary 事件透传、result 事件映射 TodayAiAnalysis、unknown bullet kind→general、多 summary 事件后 result、仅 result 事件、空流无事件、流错误传播、date 参数传递、空 bullets 列表映射）；generate（从 result 事件返回分析、无 result 事件抛 StateError、空流抛 StateError、result 首事件立即返回、date 参数传递、流错误传播）

### risk_check_repository_test.dart — 药物风险检查仓库

- **文件:** `test/medicine/risk_check_repository_test.dart`
- **源文件:** `lib/features/medicine/data/repositories/risk_check_repository.dart`
- **测试数:** 15
- **覆盖:** fetchForSnapshot（空快照返回空结果、无 source 返回 coverage issue、cn 无 sourceRefId 返回 coverage issue、cn 空 sourceRefId 返回 coverage issue、cn 有效 sourceRefId 获取详情、drugbank 有效 sourceRefId 获取详情、未知 source 跳过、非当前药品跳过、API 非零码返回 coverage issue、网络异常优雅降级、混合场景处理、自定义 MedicineRiskChecker、多药品顺序获取详情、sourceRefId trim）

### cached_dose_log_data_source_test.dart — 缓存剂量日志数据源

- **文件:** `test/medicine/cached_dose_log_data_source_test.dart`
- **源文件:** `lib/features/medicine/data/datasources/cached_dose_log_data_source.dart`
- **测试数:** 21
- **覆盖:** fetchForDate（缓存空走网络、缓存有值直接返回、全字段往返序列化、多项目缓存、null currentMedicineId、null 可选字段、planned 状态、空列表返回、网络错误传播）；create（返回远程结果、刷新缓存）；update（返回远程结果、不刷新缓存无日期信息）；mark（返回远程结果、reminderId+scheduledTime 传递、null 可选字段、刷新缓存）；JSON 序列化往返（所有 DoseLogStatus 值通过缓存往返）

### auth_guarded_test.dart — 认证保护提供者

- **文件:** `test/core/providers/auth_guarded_test.dart`
- **源文件:** `lib/core/providers/auth_guarded.dart`
- **测试数:** 20
- **覆盖:** authGuarded（已认证调用 fetch、未认证无 fallback 抛 AuthRequiredException、未认证有 fallback 调用 fallback、恢复中保持 pending、fetch 错误传播、fallback 错误传播、nullable 返回类型、int 返回类型、List 返回类型）；AuthRequiredException（toString、is Exception）；AuthSessionStateStatus 扩展（isRestoring 三种状态、isConfirmedSignedOut 三种状态、canAccessProtectedData 三种状态）

## 测试缺口补测（第四批 — 中优先级）

基于 `plans/2026-07-10-test-coverage-gap-analysis.md` 中优先级清单，补测 OAuth 认证链、AI 助手数据源、健康上下文仓库、今日建议、记录组件及设置子页面，新增 15 个测试文件共 ~370 个测试用例。

### oauth_callback_parser_test.dart — OAuth 回调解析器

- **文件:** `test/auth/oauth_callback_parser_test.dart`
- **源文件:** `lib/features/auth/presentation/widgets/shared/oauth_callback_parser.dart`
- **测试数:** 15
- **覆盖:** 从 URL 解析 code 和 state、从字符串解析、带 query 参数的 URL、code/state 缺失返回 null、空 code 返回 null、前后空格 trim、特殊字符、重复参数取第一个、fragment 格式（#code=...&state=...）

### auth_action_runner_test.dart — 认证动作执行器

- **文件:** `test/auth/auth_action_runner_test.dart`
- **源文件:** `lib/features/auth/presentation/providers/shared/auth_action_runner.dart`
- **测试数:** 10
- **覆盖:** 成功路径（返回 AuthActionResult.success）、DioException 错误映射（网络错误→通用消息、业务码错误→信封消息）、通用异常映射（→"发生了未预期的错误。"）、loading 状态正确设置和清除

### oauth_panels_test.dart — OAuth 面板 Widget

- **文件:** `test/auth/oauth_panels_test.dart`
- **源文件:** `lib/features/auth/presentation/widgets/shared/oauth_panels.dart`
- **测试数:** 13
- **覆盖:** WechatOAuthPanel（开始按钮渲染、isStarting/isCompleting 禁用、authorizeUrl 有值/无值/空值显示回调输入框、完成按钮禁用、点击开始触发 onStart）；QqOAuthPanel（开始按钮渲染、isStarting 禁用、authorizeUrl 有值/无值显示回调输入框、点击开始触发 onStart）

### assistant/remote_data_source_test.dart — AI 助手远程数据源

- **文件:** `test/assistant/remote_data_source_test.dart`
- **源文件:** `lib/features/assistant/data/datasources/remote_data_source.dart`
- **测试数:** 5
- **覆盖:** getCapabilities（全字段映射）、getLatestConversation（返回数据）、listRecentConversations（列表映射）、openConversation（conversationId 传递）、clearLatestConversation（cleared 标志）
- **修复:** 将自定义 JSON 编码器替换为 `dart:convert` 的 `jsonEncode`，将 `responseBody` 类型从 `Map<String, dynamic>?` 改为 `Object?` 以支持列表响应

### health_context/lucent_repository_test.dart — 健康上下文仓库

- **文件:** `test/health_context/lucent_repository_test.dart`
- **源文件:** `lib/features/health_context/data/repositories/lucent_repository.dart`
- **测试数:** 19
- **覆盖:** fetchHealthContext（缓存空→网络获取并缓存、缓存命中→不请求网络、缓存命中→后台刷新触发、后台刷新 30s 节流、网络错误传播）；CRUD 操作（updateProfile/createAllergy/updateAllergy/deleteAllergy/createCondition/updateCondition/deleteCondition/createCurrentMedicine/updateCurrentMedicine/deleteCurrentMedicine 均验证 dataSource 委托+缓存写入）；缓存 JSON 往返（过敏项、状况项、当前药品、profile extras 全字段序列化/反序列化）
- **修复:** 使用 mocktail `Mock` 替代直接 `implements HealthContextDao`（因 DAO 继承 `DatabaseAccessor<AppDatabase>`），修正生成 DTO 枚举名称（`SexAtBirth` 替代 `UserHealthProfileDtoSexAtBirth` 等），补充 `extras`/`recordedAt`/`sourcePayload` 必填参数

### today/ai_remote_data_source_test.dart — 今日 AI 远程数据源

- **文件:** `test/today/ai_remote_data_source_test.dart`
- **源文件:** `lib/features/today/data/datasources/ai_remote_data_source.dart`
- **测试数:** 13
- **覆盖:** generate（成功返回 DTO、date 参数传递）；generateStream（summary 事件→SummaryEvent、result 事件→ResultEvent、空 summary 跳过、非 Map summary 跳过、error 事件抛 LucentApiException、空 error 数据使用默认消息、done 事件终止流、unknown 事件忽略、summary+result+done 序列、date 参数传递、null date 省略）
- **修复:** 添加 `dart:convert` 导入、将 `ResponseBody.fromStream` 替换为 `ResponseBody` 构造函数、使用 `jsonEncode` 替代自定义 JSON 编码器

### today/suggestion_remote_data_source_test.dart — 今日建议远程数据源

- **文件:** `test/today/suggestion_remote_data_source_test.dart`
- **源文件:** `lib/features/today/data/datasources/suggestion_remote_data_source.dart`
- **测试数:** 10
- **覆盖:** fetchSuggestions（完整 bundle 映射、null primary、secondary+observations 卡片映射、evidence 项映射）；submitFeedback（结果映射、全 feedback 类型往返）；explainSuggestion（解释映射）；fetchHistory（历史映射、无 feedback 项）；枚举 fallback（unknown confidence→medium、unknown triggerType→timer）
- **修复:** 使用 `jsonEncode` 替代自定义编码器、修正 `TodaySuggestionFeedbackEffect.dismissed`→`noted`、可空列表字段加 `!` 空断言、DTO 必填字段（boundary/primaryAction/ruleId/ruleVersion）提供有效值

### record/dashboard_entity_test.dart — 记录仪表盘实体

- **文件:** `test/record/dashboard_entity_test.dart`
- **源文件:** `lib/features/record/domain/entities/dashboard.dart`
- **测试数:** 8
- **覆盖:** RecordWeekDay 构造与属性、 weekday 属性、RecordDashboard 空构造、带数据构造、copyWith、equality

### record/widgets/quick_actions_test.dart — 快捷操作区

- **文件:** `test/record/widgets/quick_actions_test.dart`
- **源文件:** `lib/features/record/presentation/widgets/sections/quick_actions.dart`
- **测试数:** 6
- **覆盖:** 标题渲染、快捷操作 tile 渲染、点击触发 onQuickAction
- **修复:** `pump()` 改为 `pumpAndSettle()` 避免 `FButton.raw` 点击动画定时器未完成导致的 `!timersPending` 断言错误

### record/widgets/week_strip_test.dart — 周日历条

- **文件:** `test/record/widgets/week_strip_test.dart`
- **源文件:** `lib/features/record/presentation/widgets/sections/week_strip.dart`
- **测试数:** 8
- **覆盖:** 7 天日期渲染、选中日期高亮、点击触发 onDateSelected
- **修复:** `pump()` 改为 `pumpAndSettle()` 避免 `FTappable` 点击动画定时器未完成导致的 `!timersPending` 断言错误

### record/widgets/ocr_entry_dialog_test.dart — OCR 录入对话框

- **文件:** `test/record/widgets/ocr_entry_dialog_test.dart`
- **源文件:** `lib/features/record/presentation/widgets/dialogs/ocr_entry_dialog.dart`
- **测试数:** 10
- **覆盖:** 对话框标题渲染、识别文本显示、使用文本按钮启用/禁用（空文本/空白文本）、拍照按钮、相册按钮、取消按钮
- **修复:** 使用 `find.ancestor` 替代 `find.text` 定位 `FButton`（`find.text` 返回 `Text` widget 而非 `FButton`，导致类型转换失败）

### mine/widgets/status_overview_test.dart — 状态概览

- **文件:** `test/mine/widgets/status_overview_test.dart`
- **源文件:** `lib/features/mine/presentation/widgets/sections/status_overview.dart`
- **测试数:** 7
- **覆盖:** 卡片 key 渲染、告警标题渲染、告警副标题渲染、告警徽章渲染、双告警+分隔线、单告警、空告警列表

### report/widgets/range_picker_dialog_test.dart — 日期范围选择对话框

- **文件:** `test/report/widgets/range_picker_dialog_test.dart`
- **源文件:** `lib/features/report/presentation/widgets/dialogs/range_picker_dialog.dart`
- **测试数:** 10
- **覆盖:** 对话框标题渲染、三个范围选项渲染、选中范围 check 图标、last7Days/last30Days 返回正确 query、点击 custom 打开日历、已有日期预填日历、ReportDashboardQuery.isCustom 判定
- **修复:** 将 `clock.withClock()` 改为顶层函数 `withClock()`、将无 `tester` 参数的 `testWidgets` 改为 `test`

### settings/subpages_test.dart — 设置子页面

- **文件:** `test/settings/subpages_test.dart`
- **源文件:** `lib/features/settings/presentation/pages/dnd_settings_page.dart`、`sleep_reminder_settings_page.dart`、`accessibility_settings_page.dart`、`data_storage_settings_page.dart`
- **测试数:** 26
- **覆盖:** DnD（页面标题、启用开关、开始/结束时间字段、FSwitch 渲染）；SleepReminder（标题、开关+副标题、FSwitch）；Accessibility（标题、字号分区标签、字号选项、行 key、减少动画开关、高对比度开关）；DataStorage（标题、保留期分区标签、保留期选项、图片质量分区标签、图片质量选项、同步分区标签、同步选项、行 key 验证）
- **修复:** 添加 `forui` 导入、修正 storageValue key 名称（`30` 替代 `30_days`、`dataSaver` 替代 `data_saver`、`wifiOnly` 替代 `wifi_only`）、SleepReminder 标题 `findsNWidgets(2)` 替代 `findsOneWidget`

### medicine/presentation/widgets/reminder/reminder_form_body_test.dart — 提醒表单主体

- **文件:** `test/medicine/presentation/widgets/reminder/reminder_form_body_test.dart`
- **源文件:** `lib/features/medicine/presentation/widgets/reminder/reminder_form_body.dart`
- **测试数:** 15
- **覆盖:** 无当前药品时显示错误视图、有药品时渲染表单、药品选择器、频次选择器、周选择器（weekly 时显示）、时间选择器行、保存/删除按钮、isSaving 禁用、onDelete null 时隐藏删除按钮、daily 时隐藏周选择器、保存按钮触发 onSave
- **修复:** 添加 `reminder_formatters.dart` 导入（`ReminderFrequency` 枚举）、修正 `MedicineReminderSoundPreference.systemDefault`→`defaultTone`、修正 `MedicineReminderTimeInput` 构造参数（`hour`/`minute` 替代 `label`/`fromTimeOfDay`）、使用 `tester.takeException()` 捕获 forui `FSelect` 弹出层在测试环境中的 `FollowerLayer` 无界宽度异常（不影响主 widget 树布局）、`isEdit: true` 默认跳过药品选择器的 `FSelect` overlay、直接调用 `onPress` 替代 `tester.tap` 避免触发 FSelect 重新布局

---

## 第五批：低优先级测试补全（89 个测试）

### scan/data/scan_repository_test.dart — 扫描仓库

- **文件:** `test/scan/data/scan_repository_test.dart`
- **源文件:** `lib/features/scan/data/scan_repository.dart`
- **测试数:** 11
- **覆盖:** search 方法（正常返回、空列表、API 错误传播）、uploadImage 方法（返回 publicUrl、fallback 到 uploadUrl、sizeBytes 参数传递、fileName 参数传递、空响应异常）、recognizeMedicine 方法（返回数据 map、空响应异常、imageUrl 参数传递）
- **修复:** `MedicineSearchResponseDto` 缺少 `code`/`message` 必填参数、`dio.post<Object>` mock 返回类型需为 `Response<Object>` 而非 `Response<dynamic>`

### shell/branch_test.dart — Shell 分支枚举

- **文件:** `test/shell/branch_test.dart`
- **源文件:** `lib/features/shell/presentation/branch.dart`
- **测试数:** 9
- **覆盖:** ShellBranch 5 个枚举值顺序、isVisible 全部为 true、index 匹配位置、indexForTab 返回正确索引、ShellTabBranchX.branchIndex 等于 tab.index

### shell/deferred_content_test.dart — 延迟内容加载

- **文件:** `test/shell/deferred_content_test.dart`
- **源文件:** `lib/features/shell/presentation/deferred_content.dart`
- **测试数:** 3
- **覆盖:** 首帧显示 placeholder、次帧切换到 child、默认 placeholder 渲染骨架屏、rebuild 后 child 保持可见

### shell/tab_test.dart — Shell Tab 枚举

- **文件:** `test/shell/tab_test.dart`
- **源文件:** `lib/features/shell/presentation/tab.dart`
- **测试数:** 8
- **覆盖:** 5 个 tab 顺序、icon/activeIcon 非空、testKey 返回 `shell-tab-*` 前缀的 ValueKey、testKey 唯一性、label 在 l10n 为 null 时返回 fallback 字符串

### record/quick_entry_preferences_test.dart — 快捷录入偏好

- **文件:** `test/record/quick_entry_preferences_test.dart`
- **源文件:** `lib/features/record/data/quick_entry_preferences.dart`
- **测试数:** 13
- **覆盖:** QuickEntryPreferences 默认值、copyWith 全量/部分更新、Controller 加载默认值/已存储值、setDynamicSortEnabled/setCustomOrder/setCollapsed 持久化、recordTap 递增频率+持久化、频率超过 50 时按比例裁剪、裁剪后移除 0 值条目、reset 清空状态和 SharedPreferences
- **修复:** `AsyncNotifier` 不能直接实例化，需通过 `ProviderContainer` 使用；`asData` 改为 `requireValue`

### record/voice_recording_service_test.dart — 语音录制服务

- **文件:** `test/record/voice_recording_service_test.dart`
- **源文件:** `lib/features/record/domain/services/voice_recording_service.dart`
- **测试数:** 22
- **覆盖:** normalizeSpeechSoundLevel（边界值 -2.0/10.0、中间值 4.0→0.5、超范围 clamp、0.0→0.167）、isAvailable/isListening/lastRecognizedWords/hasPermission/locales 委托、initialize（已可用跳过/成功/失败）、startListening（未初始化抛 StateError/调用 speech.listen）、stopListening（未可用返回空/返回 lastRecognizedWords）、cancelListening（未可用跳过/调用 cancel）、soundLevelStream 发射归一化值、dispose 关闭所有流控制器

### settings/notification_permission_service_test.dart — 通知权限服务

- **文件:** `test/settings/notification_permission_service_test.dart`
- **源文件:** `lib/features/settings/data/services/notification_permission_service.dart`
- **测试数:** 15
- **覆盖:** ensureInitialized（首次调用/跳过重复调用/MissingPluginException/PlatformException）、getPermissionState（Android granted/denied、iOS granted/denied、异常处理）、requestPermission（Android granted/denied、iOS granted/denied、异常处理、macOS 透传）
- **修复:** `InitializationSettings` 需注册 `registerFallbackValue`、`MissingPluginException` 非 const 构造、`NotificationsEnabledOptions` 替代 `NotificationPermissions`、异常后 fall-through 到 `permission_handler` 在测试环境会抛异常

### medicine/presentation/widgets/reminder/reminder_rows_test.dart — 提醒行组件

- **文件:** `test/medicine/presentation/widgets/reminder/reminder_rows_test.dart`
- **源文件:** `lib/features/medicine/presentation/widgets/reminder/reminder_rows.dart`
- **测试数:** 11
- **覆盖:** ReminderInfoRow（图标/标签/值渲染、showDivider 分隔线）、ValueActionRow（标题/值渲染、onTap 回调、onClear 显示清除按钮、无 onClear 显示箭头）、SwitchRow（标题/副标题/FSwitch 渲染、onChanged 值切换）、UnavailableMethodRow（标题/副标题/状态徽章）、SelectedMedicineRow（药品名+剂量文本、无剂量时显示 not-set）
- **修复:** `_getL10n` 使用 `find.byType(Scaffold)` 而非 `TestForuiApp`（后者在 MaterialApp 之上无法获取 l10n）、SoundPreferenceRow 因 FSelect `FollowerLayer` 无界宽度断言跳过测试

## 计划文件校验与清理

校验两个计划文件的完成状态，已完成则删除，未完成项移入 TODO.md。

### `plans/2026-07-07-mobile-tab-ui-refresh.md` — 已删除（全部完成）

- 五 Tab UI 刷新计划全部完成：跨页面统一规则（状态机、卡片层级、CTA、文案）、Record/Medicine/Report/Mine 各页改造项均已落地
- 验收标准全部满足：PageViewState 统一状态机、SignInHintBanner 轻量提示、每页单一主任务、1002+ 测试通过
- 计划文件已删除

### `plans/2026-07-09-today-suggestion-engine-backend.md` — 已删除（后端全部完成，1 项前端剩余移入 TODO）

- 后端 Phase 1-6 + 审查修复全部完成
- 前端 Phase 1-4 全部完成（API 客户端、Domain 实体、Riverpod Provider、反馈按钮、AI 解释）
- 前端 Phase 5 深层链接导航已完成（`_openRoute` → `primaryAction.route`）
- 前端 Phase 5 剩余项移入 `TODO.md`：Report 页历史建议回顾接入 `GET /today/suggestions/history`（data source `fetchHistory()` 已就绪，但无 UI 消费；Report 页当前使用通知接口替代）
- 计划文件已删除
