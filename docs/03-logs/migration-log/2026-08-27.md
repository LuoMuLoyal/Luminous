# Migration Log — 2026-08-27

## fix(l10n): 移除 flutter_localizations 重复 delegate 注册

`lib/app/bootstrap.dart`、`test/helpers/test_forui_app.dart`、`test/core/feedback/app_toast_test.dart`：移除 `flutter_localizations` 的 `fl.GlobalMaterialLocalizations.delegate` / `fl.GlobalWidgetsLocalizations.delegate` / `fl.GlobalCupertinoLocalizations.delegate` 三行重复 delegate，仅保留 `material_ui` 的 `...GlobalMaterialLocalizations.delegates`（已覆盖 Material / Widgets / Cupertino）。移除对应的 `import 'package:flutter_localizations/flutter_localizations.dart' as fl` 导入。

## refactor(auth): 抽公共 sensitive action password resolver

- 新增 `lib/core/auth/sensitive_action_password_resolver.dart`，导出两个 helper：
  - `resolveSensitiveActionPassword(WidgetRef, BuildContext)` — 等待 `userSettingsControllerProvider.future` 就绪后再决定是否弹密码框，避免 settings 在 `AsyncLoading` 时 `?? true` 强制弹窗。
  - `handleSensitiveActionFailure({context, l10n, error, failurePrefix})` — 统一处理 `AUTH_PASSWORD_NOT_SET` 的 actionable toast 和通用失败 toast。
- `lib/features/settings/presentation/pages/data_export.dart`、`lib/features/report/presentation/utils/export_actions.dart`：两处 call-site 改为调用 helper，消除逐行复制的重复逻辑。
- `lib/core/providers/sensitive_action_password.dart`：provider 定义处增加 dartdoc 说明 default title / message / label 来源。

## refactor(auth): reset_password 抽出 _MissingTokenView 修复 Hook 调用顺序

`lib/features/auth/presentation/pages/reset_password.dart`：将空 token 守卫的早返回 UI 抽为 `_MissingTokenView` 私有 widget，使 `build` 方法仅剩 if/else 分支，hooks 始终在主路径无条件调用，避免未来误加 hook 顺序违规。

## fix(test): e2e_test_helpers 空 password assert 校验

`integration_test/support/e2e_test_helpers.dart`：`requestDataExport` 方法在 `expectDataExportPassword` 模式下增加 `assert(password.isNotEmpty)`，空密码视为无效而非静默接受。

## docs(test): flow_theme_bridge_test as cast TODO 注释

`test/assistant/flow_theme_bridge_test.dart`：将 `as Iterable<ThemeExtension<dynamic>>` cast 的注释改为 `TODO(flow-ui-upgrade)` 格式，说明运行时兼容的前提条件和移除时机。

## test(auth): 新增 reset_password 空 token 守卫 widget test

`test/auth/presentation/pages/reset_password_test.dart`：新增空 token 守卫 widget test（2 条 test），验证空 token 时显示错误状态且点击按钮可导航至忘记密码页。

## test(core): 新增 lucent_failure kPasswordNotSetCode 单元测试

`test/core/errors/lucent_failure_test.dart`：新增 `kPasswordNotSetCode` 常量断言组（4 条 test），覆盖常量值、isPasswordNotSet 正匹配、负匹配及 null code 场景。

## test(core/auth): 新增 sensitive_action_password_resolver_test

`test/core/auth/sensitive_action_password_resolver_test.dart`：新增 helper 逻辑分支 test（4 条 test），覆盖 `resolveSensitiveActionPassword` 在 `passwordReauthenticationRequired` 为 true/false 时的行为，以及 `handleSensitiveActionFailure` 对 `AUTH_PASSWORD_NOT_SET` 和非密码错误的返回值。

## fix(l10n): 恢复 flutter_localizations delegate 注册

前一次提交错误地移除了 `flutter_localizations` 的 `fl.GlobalMaterialLocalizations.delegate` / `fl.GlobalWidgetsLocalizations.delegate` / `fl.GlobalCupertinoLocalizations.delegate`，认为 `material_ui` 的 `GlobalMaterialLocalizations.delegates` 已覆盖。实际上 `material_ui` 定义了自己独立的 `MaterialLocalizations` 抽象类，与 `flutter` 包的 `MaterialLocalizations` 是不同的类型。`RefreshIndicator` 等 Flutter 框架 widget 查找的是 `flutter` 包的 `MaterialLocalizations`，需要 `fl.*` delegate 来注册。两组 delegate 各自注册不同的接口类型，不是重复注册。

恢复位置：`lib/app/bootstrap.dart`、`test/helpers/test_forui_app.dart`、`test/core/feedback/app_toast_test.dart`、`test/core/auth/sensitive_action_password_resolver_test.dart`。

## fix(today): water records pageSize 200→100 适配后端 Max(100) 限制

`lib/features/today/data/repositories/lucent.dart`：`fetchRecords` 请求 water records 时 `pageSize: 200` 超出后端 `QueryDailyRecordDto` 的 `@Max(100)` 验证限制，导致 400 `VALIDATION_FAILED`。修正为 `pageSize: 100`。

### 验证

- `flutter analyze` 修改文件全部通过（0 issues）。

## feat(report): R-3/R-4 #19/#22 装配纵向洞察与建议历史到 Review 主路径

执行 `plans/2026-08-16-report-remediation-plan.md` 中 R-3（AI 纵向洞察装配到 Review 视图）与 R-4 #19/#22（legacy 视图重装配、建议历史移入 Review）的客户端装配部分。

### 改动

- **空态丰富化**：`review_view.dart` 空态（无事件时）从单张 `_StartObservationCard` 扩展为 5 张 `ReviewPreviewLockedSection` 预览锁定卡片（发生了什么 / 有什么变化 / 完成了什么 / 接下来怎么办 / AI 纵向洞察），与旧 report 页 `dashboard_preview.dart` 的预览风格对齐。
- **AI 总结装配（R-3）**：新建 `review_ai_summary.dart`（`ReviewAiSummarySection`），不再硬依赖 `ReportDashboard` 实体，只接收 `aiSummaryEnabled` 布尔值；`page.dart` 引入 `reportAiSummaryControllerProvider` / `reportAiSummarySelectedRangeProvider` 并装配到 `ReviewView`；`section_models.dart` 新增 `buildReviewAiSummaryContent` 函数。
- **建议历史移入 Review（R-4 #22）**：新建 `review_suggestion_history.dart`（`ReviewSuggestionHistorySection`），从旧 `ReportSuggestionHistorySection` 改名而来；`page.dart` 引入 `suggestionHistoryProvider` 并按 title|reason|type 去重取最高生命周期状态，装配到 `ReviewView`。
- **文件重命名**：`preview_empty.dart` → `review_preview_locked.dart`（`ReportPreviewLockedSection` → `ReviewPreviewLockedSection`），`dashboard_preview.dart` 同步更新引用。
- **l10n**：新增 `reviewPreviewWhatHappenedTitle/Body`、`reviewPreviewKeyChangesTitle/Body`、`reviewPreviewCompletedActionsTitle/Body`、`reviewPreviewNextStepTitle/Body`、`reviewPreviewAiSummaryTitle/Body` 共 10 条（zh + en）。

### 未完成（计划后续项）

- R-4 #19 日/周/月范围切换（`top_bar.dart` 范围切换改为日/周/月）。
- R-4 #21 图表改 `observedMetric` 口径（unknown 不绘点、标注覆盖率）。
- R-5 服务端 409 双保险。
- R-6 文档漂移更新。
- 后端零消费端点裁剪。

### 验证

- `flutter analyze lib/features/report/` 全部通过（0 issues）。

## feat(medicine): 药箱条目点击改为进入药品详情页，药品详情页新增提醒详情入口

用药页面"我的药箱"药品条目点击行为变更：由原来的直接跳转提醒详情页，改为默认进入药品详情页（说明书），并在药品详情页底部新增"查看提醒详情"按钮跳转到提醒详情页。

- `lib/features/medicine/domain/entities/workspace.dart`：`MedicinePlanItem` 新增 `source` 和 `sourceRefId` 字段，用于构造药品详情页路由参数。
- `lib/features/medicine/data/repositories/lucent_workspace.dart`：构建 plan item 时从 `CurrentMedicineItem` 透传 `source` 和 `sourceRefId`。
- `lib/features/medicine/presentation/widgets/sections/mobile_drugbox.dart`：`_DrugBoxMedicationRow` 的 `onPress` 逻辑改为：当 `source` + `sourceRefId` 可用且为有效数据源（`cn`/`drugbank`）时跳转 `MedicineDetailRoute`，否则回退到原有提醒详情页逻辑。
- `lib/features/medicine/presentation/pages/medicine_detail.dart`：药品详情页底部按钮区，当药品已加入药箱时新增"查看提醒详情"按钮，跳转 `MedicineReminderDetailRoute`。
- `lib/l10n/src/medicine_zh.arb` / `medicine_en.arb`：新增 `medicineDetailOpenReminderAction` 字符串（"查看提醒详情" / "View reminder details"）。

### 验证

- `flutter analyze` 修改文件全部通过（0 issues）。

## fix(medicine): 药品详情页 CheckedFromJsonException 修复

药品详情页（`GET /api/v1/medicines/:id?source=cn`）一直显示骨架屏。前端日志显示 `CheckedFromJsonException: Could not create MedicineDetailResponseDtoDetail. Required keys are missing: groups, categories, atcCodes, synonyms, foodInteractions`。

后端 `DrugbankMedicineDetailDto` 中这 5 个 DrugBank 列表字段使用 `@ApiProperty`（非 optional），OpenAPI spec 标记为 required。生成的 Dart DTO 用 `required: true` + `checked: true`，CN 药品详情 JSON 不含这些字段时 `fromJson` 抛异常。

### 改动

- `generated/lucent_api/`：重新生成 Flutter 客户端（后端 DTO 改为 `@ApiPropertyOptional(nullable: true)` 后，生成代码中这 5 个字段变为 `required: false` + `List<String>?`）。
- `lib/features/medicine/data/mappers/medicine_detail.dart`：`detail.groups` 等改为 `detail.groups ?? const []` 处理 null。

## fix(today): 今日页 DO this first 内容区统一边框样式

`lib/features/today/presentation/widgets/sections/suggestion_state_views.dart`：将"DO this first"区域的空态（`SuggestionEmptyState`）、错误态（`SuggestionErrorState`）和骨架屏（`SuggestionSkeleton`）从裸 `Padding` / `DecoratedBox` 改为 `FCard` + `todayCardStyle(context)`，使边框和圆角与 Observation 卡片、次要建议卡片等其他组件统一。

## refactor(theme): Scaffold 背景统一为淡灰 + 卡片去边框

将应用背景色从纯白（`colors.background` = `#FFFFFF`）改为淡灰（`colors.secondary` = `#F5F5F5`），卡片背景保持纯白（`colors.card`），通过色差而非边框实现层次分离。这是成熟应用（iOS Health、Linear、Notion）的通用做法。

### 改动

- **`lib/core/theme/theme.dart`**：`appThemeData()` 新增 `scaffoldStyle` override，将 `FScaffoldStyle.backgroundColor` / `sidebarBackgroundColor` 设为 `colors.secondary`（light）/ `Color.lerp(background, secondary, 0.5)`（dark）；新增 `cardStyle` override 去掉 `FCard` 默认 `BorderSide(color: colors.border)`；`foruiMaterialTheme()` 同步 `scaffoldBackgroundColor` / `canvasColor` 为 `scaffoldStyle.backgroundColor`，`cardTheme` 去边框。
- **`lib/features/today/presentation/widgets/shared/card_style.dart`**：`todayCardStyle` 中 `emphasis`/`soft`/`neutral` 三种 tone 去掉边框（`BorderSide.none`）；`urgent`/`warning` 保留语义色边框（警示而非分隔）。
- **`lib/features/today/presentation/widgets/views/dashboard_view.dart`**：`_HealthEventCard` 去掉 `Border.all(color: colors.border)`。
- **`lib/features/shell/presentation/desktop_tab_shell.dart`**：内容区背景从 `SemanticColor.neutral.shimmerBase` 改为 `context.theme.scaffoldStyle.backgroundColor`，与移动端统一。
- **4 个移动端 tab 页面**：去掉 `DecoratedBox(color: colors.background)` 手动背景色包裹，背景统一由顶层 `ShellPage` 的 `FScaffold` 提供：
  - `lib/features/today/presentation/pages/page.dart`
  - `lib/features/mine/presentation/pages/page.dart`
  - `lib/features/report/presentation/pages/page.dart`（`_ReportMobileShell`）
  - `lib/features/medicine/presentation/pages/page.dart`（`_MedicineMobileShell`）

### 不改动

- 桌面端 sidebar 背景保留 `colors.background`（纯白），白色 sidebar vs 灰色内容区是好对比。
- 认证页面 `ColoredBox(color: colors.background)` 保留不变（特殊全屏体验）。
- `colors.background` token 本身不动，不影响用它做边框/前景色的业务代码。

### 验证

- `flutter analyze` 全部通过（0 issues）。

## fix(theme): Scaffold 背景色从 #F5F5F5 调淡为 #FAFAFA

`#F5F5F5`（Forui `colors.secondary` / `colors.muted`）作为大面积移动端背景偏灰，视觉上有"脏"感。改为 `#FAFAFA`（Tailwind neutral-50），这是业界近白背景的常用色值（Linear、Apple Notes、GitHub 内容区），和白色卡片仍有微妙色差但大面积铺开几乎像白色。同时 Forui `FTabs` 背景仍为 `colors.muted`（`#F5F5F5`），与 `#FAFAFA` scaffold 背景形成 2% 亮度的微妙层级差。

### 改动

- `lib/core/theme/theme.dart`：`scaffoldBackgroundColor` light 模式从 `colors.secondary` 改为 `const Color(0xFFFAFAFA)`；dark 模式不变。

### 验证

- `flutter analyze lib/core/theme/theme.dart` 通过（0 issues）。

## refactor(theme): FCard 与 FTileGroup 统一极淡边框

将 FCard 和 FTileGroup 的边框统一为极淡色 `#F5F5F5`（light）/ `colors.border.withAlpha(0.5)`（dark），取代上一轮的"FCard 无边框 + FTileGroup 默认 `#E5E5E5` 边框"不一致方案。`#F5F5F5` 在 `#FAFAFA` scaffold 背景上亮度差仅 0.8%，几乎不可见，但提供一致的视觉收口。同时为 FTileGroup 增加纯白背景（`colors.card`）使与 FCard 视觉一致，divider 颜色同步调淡。

### 改动

- **`lib/core/theme/theme.dart`**：
  - `cardStyle`：从无边框改为 `FCardStyle.inherit().copyWith(decoration: DecorationDelta.value(...))`，极淡边框 `#F5F5F5` + 纯白背景。
  - `tileGroupStyle`（新增）：`FTileGroupStyle.inherit().copyWith(...)`，纯白背景 + 极淡边框 `#F5F5F5` + divider 调淡为 `#F5F5F5`。
  - `foruiMaterialTheme`：`cardTheme` 同步极淡边框。
- **`lib/features/today/presentation/widgets/shared/card_style.dart`**：`todayCardStyle` 的 `emphasis`/`soft`/`neutral` tone 不再覆盖边框为 `BorderSide.none`，改为仅覆盖背景色（继承全局极淡边框）；`urgent`/`warning` 保留语义色边框。
- **`lib/features/today/presentation/widgets/views/dashboard_view.dart`**：`_HealthEventCard` 的 `DecoratedBox` 加回 `Border.all(color: #F5F5F5)`。

### 验证

- `flutter analyze` 修改文件全部通过（0 issues）。

## refactor(theme): 提取 SurfaceTokens 消除硬编码色值

将 `appThemeData` 和 `foruiMaterialTheme` 中散落的硬编码色值 `#FAFAFA`（scaffold 背景）和 `#F5F5F5`（容器边框）提取为 `SurfaceTokens` 设计 token 类（`lib/core/design/surface.dart`），与 `ElevationTokens` 同一模式：`abstract final class` + 接收 `FColors` 的静态方法，dark 模式自动从 Forui 色彩系统派生。`dashboard_view.dart` 中 `_HealthEventCard` 的硬编码 `Color(0xFFF5F5F5)` 同步替换为 `SurfaceTokens.containerBorder(colors)`。

### 改动

- **新增 `lib/core/design/surface.dart`**：`SurfaceTokens.scaffoldBackground(colors)` 和 `SurfaceTokens.containerBorder(colors)`。
- **`lib/core/design/design.dart`**：barrel export 新增 `surface.dart`。
- **`lib/core/theme/theme.dart`**：3 处硬编码替换为 `SurfaceTokens` 调用；移除不再使用的 `isDark` 变量。
- **`lib/features/today/presentation/widgets/views/dashboard_view.dart`**：1 处硬编码替换。

### 验证

- `flutter analyze` 全部通过（0 issues）。

## feat(auth): AuthSessionNotifier 增加底线超时

离线状态下打开应用时，`AuthSessionNotifier.restore()` 的网络请求受 Dio 超时限制（最长 ~20s），期间 `isLoading: true` 导致所有 tab 骨架屏长时间不消失。

### 改动

- `lib/core/database/cache_constants.dart`：新增 `loadingFloorTimeout`（6s）和 `sessionRestoreTimeout`（8s）常量。
- `lib/core/auth/session_state.dart`：`AuthSessionState` 新增 `isTimeout` 布尔字段；新增 `kSessionRestoreTimeout` 常量导出。
- `lib/core/auth/session_provider.dart`：`restore()` 改为 `Future.any` 竞赛模式——`_doRestore` 正常路径 vs `sessionRestoreTimeout` 延时路径，超时后设 `isTimeout: true` 状态。`_doRestore` 抽取为独立方法，返回 `AuthSessionState` 而非直接写 state。
- `lib/core/providers/auth_guarded.dart`：`authGuarded` 在 `!canAccessProtectedData`（含超时）时走 `signedOutFallback` 或抛 `AuthRequiredException`，不再卡在骨架屏。
- `lib/l10n/src/auth_zh.arb` / `auth_en.arb`：新增 `authSessionRestoreTimeout` 字符串。

### 验证

- `flutter analyze` 修改文件全部通过（0 issues）。
- `flutter test test/core/auth/` 全部通过。

## feat(core): PageStateSwitch loading 增加底线超时提示

骨架屏超过 6s 不消失时，在骨架屏上方显示"加载较慢"提示条，避免用户在离线/网络差时无限等待。

### 改动

- `lib/core/widgets/common/page_state.dart`：`PageStateSwitch` 的 loading 分支包裹 `_LoadingTimeoutWrapper`，该 widget 在 `loadingFloorTimeout`（6s）后在骨架上方显示 warning 色提示条（使用已有 `todayLoadingSlowHint` l10n 字符串）。所有 tab 无论是否自定义 `loadingBuilder` 都自动获得超时提示。

### 验证

- `flutter analyze` 修改文件通过（0 issues）。
- 预先存在的 13 个 medicine 测试失败与本次改动无关（已 stash 验证）。

## feat(report): LucentReviewRepository cache-first 离线兜底

Review 端点无缓存层，离线时直接报错。新增 Drift 缓存表 + DAO + cache-first repository。

### 改动

- **新增 `lib/core/database/tables/reviews.dart`**：`ReviewCacheEntries` 表，JSON 缓存 review 数据。
- **新增 `lib/core/database/daos/review_dao.dart`**：`ReviewDao`，提供 `fetchCurrent`/`replaceCurrent`/`fetchHistory`/`replaceHistory`/`historyKey` 方法。
- **`lib/core/database/database.dart`**：schema version 2→3，新增 `ReviewCacheEntries` 表 + `ReviewDao`，migration step `m.createTable(reviewCacheEntries)`。
- **`lib/core/database/connection_providers.dart`**：新增 `reviewDaoProvider`。
- **`lib/features/report/data/repositories/lucent_review.dart`**：`LucentReviewRepository` 改为 cache-first——`fetchCurrentReview()` 和 `fetchHistory()` 先查缓存，有缓存则返回 + 后台节流刷新（30s），缓存空时走网络并写入缓存。
- **`lib/features/report/data/providers/review.dart`**：provider 注入 `ReviewDao`。

### 验证

- `flutter analyze lib/core/database/ lib/features/report/data/` 全部通过（0 issues）。

## feat(report): LucentReportRepository cache-first 离线兜底

Report Dashboard 端点无缓存层，离线时直接报错。新增 Drift 缓存表 + DAO + cache-first repository。

### 改动

- **新增 `lib/core/database/tables/report_dashboards.dart`**：`ReportDashboardCacheEntries` 表，JSON 缓存 dashboard 数据。
- **新增 `lib/core/database/daos/report_dashboard_dao.dart`**：`ReportDashboardDao`，提供 `fetch`/`replace`/`cacheKey`/`clear` 方法。
- **`lib/core/database/database.dart`**：schema version 3→4，新增 `ReportDashboardCacheEntries` 表 + `ReportDashboardDao`，migration step `m.createTable(reportDashboardCacheEntries)`。
- **`lib/core/database/connection_providers.dart`**：新增 `reportDashboardDaoProvider`。
- **`lib/features/report/data/repositories/lucent.dart`**：`LucentReportRepository` 改为 cache-first——`fetchDashboard()` 先查缓存，有缓存则返回 + 后台节流刷新（30s），缓存空时走网络并写入缓存。
- **`lib/features/report/data/providers/report.dart`**：provider 注入 `ReportDashboardDao`。

### 验证

- `flutter analyze lib/core/database/ lib/features/report/data/` 全部通过（0 issues）。
- `flutter test test/report/repository_test.dart` 全部通过（9 tests）。
