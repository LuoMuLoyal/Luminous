# 2026-08-23

## Today AI 数据源空响应使用 LucentFailure

修复 `TodayAiRemoteDataSource` 在直接资源迁移中引入的裸 `StateError`。`read()` 与 `refresh()` 在响应体为空或格式非法时，现在统一抛出 `LucentFailure.network(..., networkErrorCode: NetworkErrorCode.emptyResponse)`，由 `LucentErrorMapper` 映射为 `AppErrorKind.network`，不再穿透未分类异常到 UI。同步补充 `read` 与空/畸形 `refresh` 响应的单元测试。

验证：`test/today/ai_remote_data_source_test.dart` 全部通过；`flutter analyze` 无问题。

## 收口网络失败边界

错误迁移 Task 1：固定网络层错误分类、重试与 token refresh 的唯一语义（LucentErrorMapper → RetryPolicy → AuthInterceptor），全部收敛到 `LucentFailure`。

- **error_mapper 契约**：确认并锁定 HTTP 错误必须先校验 `application/problem+json` 再解析；缺少 body、错误媒体类型、字段类型不符（如 `code` 非字符串、`retryAfter` 为负/小数）保持 `FormatException`（协议不变量，不转业务 LucentFailure）；连接/超时/证书类无 response 错误映射为 `LucentFailure.network`。`target_error_contract_test.dart` 补充对应契约测试。
- **refresh 触发条件**：`AuthInterceptor._shouldRefresh` 从「401 且不在黑名单」改为白名单语义——仅当 Problem Details `code == 'AUTH_TOKEN_EXPIRED'`（`LucentFailure.isTokenExpired`）才触发 refresh；`AUTH_REQUIRED`、`AUTH_REFRESH_TOKEN_INVALID`、`AUTH_WRONG_PASSWORD`、普通 401/403 一律不刷新（401 清 session 并通知，403 保留 session）。保留 `skipAuthRefresh`/`hasRetriedAfterRefresh` 短路、`_refreshFuture` 并发去重，以及 refresh 明确认证失败（401/403）清 session 并把原始请求错误传回调用方、临时失败（网络/5xx/空 body）保留 session 的既有行为。
- **重试边界**：确认 `RetryPolicy` 写请求必须显式 `retryEnabled == true` 且带非空 `Idempotency-Key`；`retryable=false` 禁止重试；`retryable=true` 不扩大客户端允许的状态集合。`Retry-After` 只接受非负整数秒，负数/小数/HTTP-date 回退指数退避（`retry_policy_test.dart` 补充用例）。运行时链上 `RetryInterceptor` 注册于 `ErrorInterceptor` 之前，`onError` 决策时先经 `LucentErrorMapper` 映射失败（畸形 body 捕获 `FormatException` 视为无 hint）再交给 `RetryPolicy` 判断，使 `retryable=false` 与 `retryAfter` 在生产链上真正生效；最终错误仍由 `ErrorInterceptor` 映射，职责划分不变（`retry_interceptor_test.dart` 补充拦截器链测试验证）。
- **遗留类型**：`LucentApiException` 仅加遗留类型文档注释（不新增构造点、不加 `toAppError` 调用）。

验证：`flutter test test/core/network` 全部通过；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 无阻塞警告（core-network 规则 docs_any_of 建议文档未更新，属提示项）。

## 迁移认证 repository 到 TaskEither

错误迁移 Task 2：Auth/session repository 与 provider 迁移到 fpdart `TaskEither<LucentFailure, T>`。

- **repository 边界**：`AuthRepository` 全部方法与 `AuthSessionsRepository.listSessions/revokeSession` 签名改为 `TaskEither<LucentFailure, T>`；`LucentAuthRepository`、`LucentAuthSessionsRepository` 实现用 `TaskEither.tryCatch(() => datasource 调用 + DTO 映射 + session 持久化, (error, stackTrace) => LucentErrorMapper.fromObject(error))` 包裹，datasource 仍只负责 Future 传输并抛出 DioException。空响应体 `_requireBody` 按 today `ai_remote.dart` 先例改为抛 `LucentFailure.network(networkErrorCode: NetworkErrorCode.emptyResponse)`（保留 operation 上下文）；sessions 列表结构不符的 `StateError` 属协议不变量，保持抛出并经 mapper 转 `Left(unknown)`，非 problem+json 畸形 HTTP body 仍由 mapper 抛 `FormatException` 从 `.run()` 直接传播。
- **注销语义**：移除 `logout()` 的 `finally` 无条件 `_sessionStore.clear()`——远程注销成功（或无 refresh token 可注销）才在 Right 路径清本地 session；远程注销失败返回 Left 且不清本地 session、不写 token holder。`changePassword`/`deleteAccount` 成功清 session 的既有行为保留在 Right 路径内。登录成功后的 `_persistSession` 位于 Right 路径（持久化失败则整操作 Left）。
- **refresh 语义**：`refreshSession` 的 Left 保留服务端 Problem Details 的 code/status，`AUTH_REFRESH_TOKEN_INVALID` 时 `LucentFailure.isRefreshTokenInvalid == true`，不会被转成普通网络错误；网络类失败仍映射为 `LucentFailure.network`。
- **provider 消费**：`session_provider.restore()` 用 `run()` + Left 分类重写（快速恢复 fetchAccount → 认证类失败回落 refresh、网络连接错误保留 session store、其余清 store），`logout()` 改为 Right 清 UI 状态、Left 投影 errorMessage 且不误登出；`forms/{login,register,password_reset}.dart` 用 `run()` + switch 投影到既有 form state，不再引用 `Result`/`runGuarded`/`AppError`；`oauth_login.dart`、`account.dart`、`presentation/providers/sessions.dart`、`wechat_oauth.dart` 消费 `TaskEither.run()`（`_resolve` 适配器把 Left 重抛进既有 action-state 处理路径，等价投影）。widget 层（pages/widgets）不导入 fpdart、不读 DioException、不解析 code/status。
- **elevation 语义**：检查 `security_elevation.dart`、`security_elevation_token_holder.dart`、`security_elevation_interceptor.dart` 后确认无需改动——`verify()` 失败不写 token holder、401/403/网络失败与意外异常均返回 false 保持 Unverified；账户操作里的 403 elevation 失败由 `_isSecurityElevationFailure` 标记 `requiresSecurityElevation` 并清空本地 token，不吞成"普通未登录"；既有 `security_elevation_test.dart` 与 `account_provider_test.dart` 已锁定该语义。elevation token holder 与 session_store 未改动。
- **测试**：`test/auth/**` 仓库层测试改为消费 `run()` 断言 Left/Right；新增 `remote_data_source_failure_test.dart` 覆盖 AUTH_TOKEN_EXPIRED、AUTH_REFRESH_TOKEN_INVALID（code/status 保留）、AUTH_WRONG_PASSWORD、验证码错误、网络 timeout、Problem Details 4xx、非 problem body → FormatException 传播、空 body → emptyResponse、logout/changePassword/deleteAccount 失败保留本地 session；form/account/session/sessions 的 mock 与 provider 测试同步到 TaskEither 签名；`integration_test` 的 `E2eLucentAuthRepository` 同步接口签名。

验证：`flutter test test/auth test/core/network/interceptors/auth_interceptor_test.dart test/core/providers/security_elevation_test.dart` 通过，除 HEAD 基线已存在的页面 widget 测试失败（semantics 断言，与本次迁移无关，变更前后失败集一致）外无新增失败；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 无阻塞警告。未运行 build_runner（`@riverpod` 注解签名无变化，`.g.dart` 无需重新生成）；未运行 bootstrap_generated_sources.dart。

## 迁移 health_context 与 today repository 到 TaskEither

错误迁移 Task 3：health_context 与 today 领域迁移到 fpdart `TaskEither<LucentFailure, T>`，沿用 Task 2 的 repository 边界模式（datasource 保持 Future/Stream 传输职责，`LucentErrorMapper.fromObject` 为唯一映射入口，非 problem+json/畸形 body 的 `FormatException` 从 `.run()` 直接传播）。

- **health_context**：`HealthContextRepository` 全部方法签名改为 `TaskEither<LucentFailure, HealthContextSnapshot>`；`LucentHealthContextRepository` 用 `TaskEither.tryCatch` 包裹。fetch 缓存优先 + 后台刷新（30s 节流）、写失败入 pending sync 队列后 Left 的既有合同保留（网络失败仍先 `_enqueueWriteFailure` 再映射）。服务端失败（如资源不存在 404）保留 Problem Details 的 code/status，不本地猜测。
- **缓存写失败两路径（Step 3）**：路径 A——缓存为空时网络拉取后写缓存是请求成功必要条件，`dao.replace` 失败返回 Left；路径 B——后台刷新写缓存属 best-effort，talker 记录并继续返回缓存快照。写方法在远端成功后缓存写失败同样映射 Left（不 catch 后无条件成功）。两种路径均有 repository 测试。
- **today**：`TodayRepository.fetchDashboard`、`TodayAiRepository.read/refresh/generate`、`SuggestionRepository` 全部方法迁移到 TaskEither；`generateStream` 保持 Stream 事件语义（不包 TaskEither），`signedOutDashboard` 为纯本地值保持 Future。`LucentTodayRepository` 的 degraded 降级是产品行为，各 catch 点继续 talker 记录观测；仅意外异常成为 Left。`TodaySuggestionRemoteDataSource` 作为 `SuggestionRepository` 的唯一实现直接返回 TaskEither（`requireData` 空体经 mapper 转 unknown Left，未改传输层语义）。AI 分析空状态（materializationStatus empty/pending/stale/failed）与空建议 bundle 保持 Right/流语义，不转 server failure；`generate()` 流结束无结果属本地不变量，映射 unknown Left。
- **provider 消费**：`healthContextSnapshot`、`todayDashboard`、`todaySuggestion`（fetch/submitFeedback/explain）、`suggestionHistory`、`todayAiAnalysisController`（_fetch/refresh）改为 `.run()` + fold，Left 投影到 AsyncValue.error 或既有 action state；AI 分析 403 仍映射 disabled 态。widget 不导入 fpdart、不解析 code/status。
- **被迫适配调用方**（仅接口签名变化的机械适配）：mine `health_edit_forms.dart`（run()+fold 到既有 form state）；medicine `medicine_detail.dart` 与 search `add_to_box.dart`（`createCurrentMedicine`）；medicine `lucent_workspace.dart`（`fetchHealthContext`）。
- **测试**：`test/health_context/**`、`test/today/**` 全部适配 run()/Left/Right 断言；新增失败分支测试（health_context：缓存写失败路径 A/B、404 Problem Details code 保留、网络失败 Left；today AI：read/refresh 网络失败与 403 code 保留、suggestion：404 code 保留 / 网络超时 / 非 problem body → `FormatException` 从 `.run()` 传播）；其余 feature 测试因调用方适配同步更新 fake 签名（mine/medicine/search/scan/auth/session_gate），`integration_test/support/e2e_test_helpers.dart` 的两个 E2e 仓库 fake 同步接口。

验证：`flutter test test/health_context`、`flutter test test/today` 全部通过；受适配影响的 mine/medicine/search/scan/auth 测试套件与 HEAD 基线对比，失败集一致（均为基线已存在的 semantics/FToaster 环境问题，变更前后均为相同失败，无新增失败）；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 退出码 0（today/medicine/mine 规则提示更新 Active_UI_*.md、health-context/search 为可选建议，均属提示项——本次为纯内部错误映射重构、无 UI 行为变化，未改 UI 文档）。未运行 build_runner（`@riverpod` 注解签名无变化，`.g.dart` 无需重新生成）；未运行 bootstrap_generated_sources.dart；未提交。

## 迁移 today repository 到 TaskEither（提交补充）

错误迁移 Task 3 的 today 领域独立提交（health_context 领域在上一提交落地），并在本提交补充两轮审查的修复结论。

- **today repository**：`TodayRepository.fetchDashboard`、`TodayAiRepository.read/refresh/generate`、`SuggestionRepository` 全部方法迁移到 `TaskEither<LucentFailure, T>`；`generateStream` 保持 Stream 事件语义（不包 TaskEither）；`signedOutDashboard` 为纯本地值保持 Future。`LucentTodayRepository` 的 degraded 降级（各子数据源失败）是产品行为，各 catch 点保留 talker 日志观测；`generate()` 当前无生产消费者，仅保留 API 兼容，流结束无结果按本地不变量映射 Left(unknown)（接口与实现均已注释说明）。
- **provider 消费**：`todayDashboard`、`todaySuggestion`（fetch/submitFeedback/explain）、`suggestionHistory`、`todayAiAnalysisController`（_fetch/refresh）改为 `.run()` + fold；AI 分析 403 仍映射 disabled 态，refresh 的 `FormatException` 显式投影为 AsyncError；widget 不导入 fpdart、不解析 code/status。
- **复审修复补充**（覆盖两个领域）：health_context `_enqueueWriteFailure` 增加内部 try/catch + talker 日志（入队/flush 自身失败不得掩盖原始网络失败 kind，仍按网络失败 Left）；补充三类测试——写路径缓存写失败（远端成功 + dao.replace 失败 → Left 且不入队）、suggestion 空成功体 → Left(unknown)、ai_analysis refresh FormatException → AsyncError。
- **测试**：`test/today/**` 全部适配 run()/Left/Right 断言，新增失败分支（AI read/refresh 网络失败与 403 code 保留、suggestion 404 code 保留 / 网络超时 / 非 problem body FormatException 传播、空成功体 unknown）；`test/auth/session_gate_test.dart` 的 today fake 随接口同步。

验证：`flutter test test/today` 全部通过；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 退出码 0（today 规则提示项未更新 Active_UI_Today.md，本次为纯内部错误映射重构、无 UI 行为变化）。未运行 build_runner 与 bootstrap_generated_sources.dart。

## 迁移 search repository 到 TaskEither

错误迁移 Task 4a：search 领域迁移到 fpdart `TaskEither<LucentFailure, T>`，沿用 Task 2/3 的 repository 边界模式（datasource 保持 Future 传输职责并抛 DioException，`LucentErrorMapper.fromObject` 为唯一映射入口，非 problem+json/畸形 body 的 `FormatException` 从 `.run()` 直接传播）。

- **repository 边界**：`MedicineSearchRepository.search/fetchDetail` 签名改为 `TaskEither<LucentFailure, T>`；`LucentMedicineSearchRepository` 用 `TaskEither.tryCatch` 包裹。识别/搜索候选为空是合法 Right（空列表不是错误）；服务端业务失败（如资源不存在 404）保留 Problem Details 的 code/status，不本地猜 status。
- **fetchDetail 吞错修复**：原 catch→null 把失败与无数据混为一谈；现 4xx/5xx（含资源不存在）为 Left 保留 Problem Details code，网络类为 Left(network)，成功响应为 Right（Right 类型保留可空语义，null 表示无预览数据、非错误）。
- **datasource**：`MedicineSearchRemoteDataSource` 保持 Future 传输并抛 DioException；空成功响应体按 auth `_requireBody` 先例抛 `LucentFailure.network(networkErrorCode: NetworkErrorCode.emptyResponse)`。`recent_searches.dart`（`RecentSearchesLocalPreferences`）为纯本地 SharedPreferences 偏好存储、无网络边界，保持 Future 不迁移（同 today `signedOutDashboard` 纯本地值先例）。
- **provider 消费**：`medicineSearchNotifier` 的 search/selectResult 改为 `.run()` + fold；search Left 投影到既有 errorMessage（`userMessageFromError`），detail Left 仅记录 talker 并清空预览、不使搜索失败；5s 超时保留在 `.run()` 返回的 Future 上（fpdart TaskEither 无 timeout，超时语义不变）。`recentSearchesProvider` 为 AsyncNotifier 本地偏好消费，不改。widget 层（`add_to_box.dart` 等）不导入 fpdart、不读 DioException、不解析 code/status。
- **被迫适配调用方**（仅接口签名变化的机械适配）：`test/search/**` 全部 fake 与 `integration_test/support/e2e_test_helpers.dart` 的 `MockMedicineSearchRepository` 同步 TaskEither 签名；`test/helpers/mocks.dart` 的 mocktail mock 声明无需改动。
- **测试**：`test/search/**` 仓库层断言改为 run()/Left/Right；新增失败分支——search/fetchDetail 404 Problem Details code/status 保留、网络超时 Left(network)、非 problem body → `FormatException` 从 `.run()` 传播、普通异常 → Left(unknown)；新增 datasource 空成功响应体 → emptyResponse 与 404/超时 DioException 传播测试；notifier 补充 search Left 投影 errorMessage、detail Left 仅降级预览不清空结果。

验证：`flutter test test/search` 通过——除 HEAD 基线已存在的页面 widget 测试失败（page_test 的加入药箱/预检/来源切换与 recent_searches_test 的时序类断言为 semantics/FToaster 环境问题，medicine_search_notifier_test 的 isSearching 时序测试在整包运行时偶发失败、单独运行通过），变更前后失败集与 HEAD 基线逐一相同、无新增失败；本次新增的 repository/datasource/notifier 失败分支测试全部通过；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 退出码 0（search 规则提示项未更新 Active_UI_Medicine.md，本次为纯内部错误映射重构、无 UI 行为变化）；改动文件已 `dart format`。未运行 build_runner（`@riverpod` 注解签名无变化，`.g.dart` 无需重新生成）；未运行 bootstrap_generated_sources.dart；未提交。

审查修复（提交前）：detail 预览路径对协议异常（非 problem+json 错误体导致的 FormatException 从 run() 逃逸）增加防御——`_fetchDetailPreview` 捕获后记录 talker 并返回 null，不使搜索失败、不产生未处理异步异常（新增 notifier 测试锁定）；同时修正 repository 类注释中「畸形上游也成 Left」的不准确表述（实际为 FormatException 传播）。

## 迁移 scan repository 到 TaskEither

错误迁移 Task 4b：scan 领域迁移到 fpdart `TaskEither<LucentFailure, T>`，沿用 Task 2/3/4a 的 repository 边界模式（`LucentErrorMapper.fromObject` 为唯一映射入口，非 problem+json/畸形 body 的 `FormatException` 从 `.run()` 直接传播）。

- **repository 边界**：`ScanRepository.search/uploadImage/recognizeMedicine` 签名改为 `TaskEither<LucentFailure, T>`；`LucentScanRepository` 用 `TaskEither.tryCatch` 包裹。识别/搜索候选为空是合法 Right（空列表/无识别结果不是错误，OCR/AI 两条路径的空结果分支保留为 Right 空列表投影到「无结果」态）；服务端业务失败（4xx/5xx Problem Details）为 Left 保留 code/status，不本地猜 status。
- **空成功响应体**：search 的 `requireData` 与 uploadImage/recognizeMedicine 的两处既有 `StateError`（上传预签名空响应、识别空响应）按 auth `_requireBody` 先例改为抛 `LucentFailure.network(networkErrorCode: NetworkErrorCode.emptyResponse)`，经 mapper 原样透传为 Left(network)。
- **协议异常保持**：生成客户端结构不符（预签名缺 `uploadUrl`、`headers` 非 map、识别响应的 `name`/`approvalNumber` 非字符串）保持抛 `StateError`/`FormatException`，抛前 `appTalker.error` 记录可诊断，经 mapper 转 `Left(unknown)`（cause 保留原异常），不转成业务失败、不本地猜 code。
- **provider/页面消费**：`scanRepositoryProvider` 签名不变（`.g.dart` 无需重新生成）；`box_scan.dart` 的 `_processPhoto`（OCR 候选逐条 search、AI 路径 uploadImage/recognizeMedicine/search）与 `barcode_scanner.dart` 的 `_handleDetect` 改为 `.run()` + `fold`（Left → 抛入既有 catch → 失败弹窗/失败 toast），页面不导入 fpdart、不读 DioException、不解析 code/status；空结果 Right 仍走「无结果/未找到」分支。
- **OCR 本地边界**：`paddle_ocr_provider.dart`（`PaddleOcrEngine`）是纯本地 ONNX 引擎封装，无网络边界，保持 Future/抛异常语义不迁移（同 today `signedOutDashboard` 纯本地先例）；`candidate_merger`/`medicine_ocr_extractor` 纯领域服务不动。
- **测试**：`test/scan/data/repositories/scan_test.dart` 断言改为 run()/Left/Right，新增失败分支——search/uploadImage/recognizeMedicine 的 404 Problem Details code/status 保留、网络超时 Left(network)、search 非 problem body → `FormatException` 从 `.run()` 传播、普通异常 → Left(unknown)、空成功响应体 → emptyResponse、结构不符 → Left(unknown) 且 cause 为 StateError/FormatException；`box_scan_test.dart`/`barcode_scanner_page_test.dart` 的 mock 改为返回 `TaskEither.right/left`，新增「search Left 显示失败弹窗」「search Left 显示失败 toast 并恢复扫描」两个 widget 失败分支测试；`test/helpers/mocks/scan.dart` 的 mocktail mock 声明无需改动。

验证：`flutter test test/scan` 全部通过；与 HEAD 基线（git stash 对比）一致——基线本套件全绿、无既有失败，迁移后新增的 repository 失败分支与两个页面 Left 投影测试亦全绿，无新增失败；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 退出码 0（scan 规则提示项未更新 Active_UI_Medicine.md/Mock_Or_Deferred.md，本次为纯内部错误映射重构、无 UI 行为变化）；改动文件已 `dart format`。未运行 build_runner（`@riverpod` 注解签名无变化，`.g.dart` 无需重新生成）；未运行 bootstrap_generated_sources.dart；未提交。

## 迁移 medicine repository 到 TaskEither

错误迁移 Task 4c：medicine 领域迁移到 fpdart `TaskEither<LucentFailure, T>`，沿用 Task 2/3/4a/4b 的 repository 边界模式（datasource 保持 Future 传输职责并抛 DioException / LucentFailure，`LucentErrorMapper.fromObject` 为唯一映射入口，非 problem+json/畸形 body 的 `FormatException` 从 `.run()` 直接传播）。

- **repository 边界**：`DoseLogRepository`、`ReminderRepository`、`MedicineRiskCheckRepository` 全部方法签名改为 `TaskEither<LucentFailure, T>`；`MedicineWorkspaceRepository.fetchWorkspace` 改为 TaskEither，`signedOutWorkspace` 为纯本地预览值保持 Future（today `signedOutDashboard` 先例）。药品查询/剂量日志/提醒请求按服务端 code 进入 LucentFailure；识别/查询候选为空是合法 Right。
- **实现类**：`CachedDoseLogDataSource`（DoseLogRepository 唯一实现）、`MedicineReminderRemoteDataSource`（ReminderRepository 唯一实现）直接返回 TaskEither（today suggestion datasource 先例，tryCatch + fromObject）；`LucentMedicineRiskCheckRepository` 用 tryCatch 包裹薄转发；`LucentMedicineWorkspaceRepository.fetchWorkspace` 用 tryCatch 包裹，health-context 快照失败为 Left。
- **lucent_workspace 空集降级评估**：risk records / dose logs / reminders 是工作台次级输入，失败降级为空/null 并 talker 记录是既有产品行为（同 today dashboard degraded 先例），保留「记录+继续」，不 catch 后无条件成功；仅主依赖 health-context 失败成为 Left。
- **离线缓存两路径（dose_log_cached）**：路径 A——缓存为空时网络拉取后写缓存是请求成功必要条件，`dao.replaceByDate` 失败返回 Left（含缓存读/解码失败）；路径 B——缓存命中后的后台刷新与写成功后缓存刷新属 best-effort，talker 记录并继续返回结果，写成功不被缓存刷新失败掩盖、也不入队 replay。写失败仍按既有离线合同先入 pending sync 队列再映射 Left(network)（入队自身失败仅记录、不掩盖原始网络失败）。
- **datasource 抛错改造**：`dose_log_remote`、`medicine_detail_remote`、`safety_tips_remote`、`risk_check_remote`、`reminder_remote` 的 `LucentApiException` 构造点全部移除——空成功响应体统一按 emptyResponse 先例（`reminder_remote._responseData` / today `ai_remote` 直查 `coerceToStringMap` 判空）抛 `LucentFailure.network(networkErrorCode: NetworkErrorCode.emptyResponse)`；结构不符（items 非 List、item 非 Map）保持抛 `StateError` 协议异常并 `appTalker.error` 记录可诊断，经 mapper 转 Left(unknown)（cause 保留原异常）。medicine 领域不再新增 `LucentApiException` 构造点。
- **本地偏好**：`reminder_local_preferences.dart` 为纯本地 SharedPreferences 存储、无网络边界，保持 Future 不迁移（同 search `recent_searches` 纯本地先例）。
- **provider 消费**：`medicineReminderList`、`medicineTodayDoseLogs`、`medicineReminderDeliveryLog`、`medicineRiskCheckRecords`、`runMedicineRiskCheck`、`medicineWorkspace` 改为 `.run()` + fold，Left 投影到 AsyncValue.error；`MedicineReminderFormNotifier.saveGroup/deleteGroup`、`reminder_delivery_reporter`、`reminder_notification_coordinator._reportLocalCapability` 用 run()+fold 投影到既有 action state / talker 记录；`medicineDetail`/`safety_tips` 直接消费 Future datasource 的 provider 不改（仅传输层抛错类型变化）。widget 层不导入 fpdart、不读 DioException、不解析 code/status。
- **被迫适配调用方**（仅接口签名变化的机械适配）：record `quick_entry_medication.dart`（delete/update/mark/fetchAll/fetchForDate 加 run()+fold，Left 抛入既有 catch）；today `skip_dose.dart`（mark run()+fold）；today `lucent.dart`（doseLog fetchForDate、reminder fetchActive 三处改 run()+fold，Left 走既有 degraded 分支）；search `add_to_box.dart`（runPrecheck run()+fold，Left 记录+提示继续添加）。
- **测试**：`test/medicine/**` 仓库/数据源断言改为 run()/Left/Right 与 `LucentFailure`（emptyResponse → kind network）；新增/保留缓存两路径、离线入队网络 Left、`fetchForDate` 失败 Left(unknown) 断言；today/record/search/scan/auth session_gate 的 fake 与 mock 同步 TaskEither 签名（`MockDoseLogRepository` stub 改 `TaskEither.right/left`）。`medicine_detail_page_test`、`reminder_edit_page_test`、`reminder_providers_test`、`safety_tips_provider_test`、`dose_log_remote_data_source_test` 无接口消费点，未改动。

验证：`flutter test test/medicine` 全绿或与 HEAD 基线失败集一致（git stash 对比确认 semantics/FToaster 环境问题）；`flutter test test/today test/record test/search` 无新增失败（基线既有失败对比说明）；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 退出码 0；改动文件已 `dart format`；`runGuarded|Result<|AppError|toAppError` 在 lib/features/medicine/** 0 命中（LucentApiException 除外，保留为遗留兼容）。未运行 build_runner（`@riverpod` 注解签名无变化，`.g.dart` 无需重新生成）；未运行 bootstrap_generated_sources.dart；未提交。

## medicine 错误迁移复审修复（Task 4c 补丁）

审查修复（提交前）：`dose_log_remote._requireData` 删除不可达的 `requireBody` 依赖分支，改为直接 `coerceToStringMap(response.data)` 判空后抛 `LucentFailure.network(emptyResponse)`（对齐 `reminder_remote._responseData` / today `ai_remote` 先例）——空成功响应体行为从「经 `requireBody` 抛 DioException、在边界转 `FormatException` 从 `.run()` 传播」变为「Left(network/emptyResponse)」。同步修正本条目上文「按 auth `_requireBody` 先例」的表述（Dio 系 datasource 现均按 emptyResponse 先例）与 `dose_log_remote.dart` 文件 doc 注释。

补充测试（端点级失败分支）：写失败且 `pendingSyncDao.enqueue` 自身抛异常 → 结果仍为 Left(network) 且保持原始连接错误 kind（入队异常仅记录、不掩盖）；dose_log/reminder/risk_check 的 404 problem+json → Left 保留服务端 code/status/kind business；结构不符（items 非 List、item 非 Map）→ Left(unknown) 且 cause 保留 StateError；非 problem+json 畸形错误体 → `FormatException` 从 `.run()` 传播。

验证：`flutter test test/medicine` 全绿；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 退出码 0；改动文件已 `dart format`；`git diff --check` 无空白错误。未运行 build_runner 与 bootstrap_generated_sources.dart；未提交。

补充（第二轮复审）：dose_log_remote fetchForDate 空成功体 → Left(network/emptyResponse) 契约补测试钉住。

## 迁移 record repository 到 TaskEither

错误迁移 Task 4d：record 领域迁移到 fpdart `TaskEither<LucentFailure, T>`，沿用 Task 2/3/4a/4b/4c 的 repository 边界模式（datasource 保持 Future 传输职责并抛 DioException / LucentFailure，`LucentErrorMapper.fromObject` 为唯一映射入口，非 problem+json/畸形 body 的 `FormatException` 从 `.run()` 直接传播）。

- **repository 边界**：`DailyRecordRepository` 全部方法（fetchRecords/fetchSummary/get/uploadImage/generateCandidates/create/update/delete）签名改为 `TaskEither<LucentFailure, T>`；`RecordRepository.fetchDashboard` 改为 TaskEither，`signedOutDashboard` 为纯本地预览值保持 Future（today `signedOutDashboard` 先例）。分页空页/空 summary 是合法 Right；服务端资源不存在/冲突/校验失败（4xx/5xx Problem Details）为 Left 保留 code/status，不本地猜 status。
- **lucent_daily 实现**：全部方法用 `TaskEither.tryCatch` + mapper 包裹。缓存两路径——路径 A：缓存为空时网络拉取后写缓存是请求成功必要条件，`dao.replaceByDate` 失败返回 Left；路径 B：缓存命中后的后台刷新属 best-effort，talker 记录并继续返回缓存快照。**删除全库唯一且无调用方的 `fetchRecordsResult`（`Future<Result<...>>` 死代码，Task 0 盘点确认；删除后全库 0 引用）**，同时移除 `Result`/`AppError`/`toAppError` 依赖。
- **离线队列语义决策（create 行为变更）**：写失败统一「先入 pending sync 队列再 Left(network)」（对齐 health_context/medicine `_enqueueWriteFailure` 模式：入队自身失败只记录、不掩盖原始网络失败 kind）。create 网络失败从旧行为「入队后返回乐观副本当作成功」改为「入队后 Left」——乐观副本仍留在本地缓存（离线可显示）并在 pending sync 队列待重放，UI 走既有失败 toast 分支；update/delete 保持既有入队后失败的合同，仅从抛 AppError 改为 Left。测试同步改为断言 Left(network) + 入队 + flush，并新增「入队自身抛异常仍保持 Left(network)」分支。
- **lucent.dart（RecordRepository）吞错降级评估**：records/summary 失败降级为空列表/空 summary 是既有产品行为（同 today dashboard degraded / medicine workspace 次级输入先例，代码注释与既有测试均已锁定），保留「记录 + 继续」——改为 `.run()` + fold，Left 经 appTalker 记录后降级；仅意外异常（含协议 `FormatException` 逃逸 `.run()`）成为该边界 Left，不 catch 后无条件成功。
- **datasource**：`DailyRecordRemoteDataSource` 保持 Future；空成功响应体（`_requireData` 五处 + `_write` 的 `requireBody`）统一改为抛 `LucentFailure.network(networkErrorCode: NetworkErrorCode.emptyResponse)`（today `ai_remote` / medicine `dose_log_remote` 先例）；delete 非 204 的 `StateError` 属协议不变量，保持抛出并补 `appTalker.error` 记录，经 mapper 转 Left(unknown)（cause 保留原异常）。
- **water_target 降级决策**：settings 读取失败回退默认目标值是既有文档化产品合同（水进度卡不因 settings 暂不可用而消失/除零），保留 best-effort 降级并在 catch 补 `appTalker.warning` 记录（原静默 catch），不转 Left。
- **provider/controller/usecase 消费**：`recordDashboard`、`dailyRecordDetail`、`dailyRecordListForDateProvider` 改 `.run()` + fold，Left 投影 AsyncValue.error（5s 超时保留在 `.run()` 返回的 Future 上）；`RecordNlpController.generate/_saveCandidates` 改 run()+fold（generate 失败投影到既有 reviewing/error 分支，保留旧候选回退语义；`_saveCandidates` Left 记入失败候选 errorMessage，catch 仅兜底协议异常）；`record_edit_controller`（load/save/uploadImage）、`create.dart`（uploadImage/create）、`detail.dart`（_confirmMealAnalysis）、`change_record_date`、`record_detail_actions`、`fast_entry_dialog`（_saveChoice/_saveSelectedChoices/_undoCreatedRecord）改 run()+fold，Left 抛入既有 catch → 既有失败 toast/state；quick entry 各 usecase（quick_entry/quick_entry_meal/quick_entry_sleep/quick_entry_medication）在 wiring 点将 repo 方法包成 run()+fold（Left 抛入既有 catch），flow/executor 类保持纯 Future typedef 不动（medicine 4c 同款模式）。widget 层不导入 fpdart、不读 DioException、不解析 code/status。
- **被迫适配调用方**（仅接口签名变化的机械适配，不改 UI 行为）：today `lucent.dart`（fetchSummary/fetchRecords 三处改 run()+fold，Left 走既有 degraded 分支；协议 FormatException 不再被 catch 降级而是逃逸为该边界 Left）；today `quick_entry_water.dart`（create/delete 加 run()+fold）；health_data `health_sync.dart`（create 改 run()+fold，Left 计入 failed+errors；dedup fetchRecords 改 run()+fold，Left 记录后重抛中止同步，与旧 catch+rethrow 同语义）。
- **测试**：`test/record/**`、`test/today/**`、`test/health_data/**` 的 fake/断言同步 TaskEither 签名（`expectTaskRight/expectTaskLeft` 断言）；`test/helpers/mocks/record.dart` 的 MockRecordRepository 改 `TaskEither.right`；受接口影响的 `test/auth/session_gate_test.dart`、`test/assistant/page_test.dart` 的 fake 与 `integration_test/support/e2e_test_helpers.dart` 的 `E2eRecordRepository`/`E2eDailyRecordRepository` 同步签名；`lucent_daily_repository_test` 重写为 run()/Left/Right 断言并新增「enqueue 失败不掩盖网络失败」分支，删除 fetchRecordsResult 扩展测试组。

验证：`flutter test test/record test/today test/health_data` 与 HEAD 基线（git stash 对比）失败集逐一相同——失败均为基线已存在的页面 widget 测试问题（form_fields/edit_page/page_test/quick_type_settings_dialog/quick_entry_settings_page/health_sync_page 的 semantics/FToaster 环境问题），迁移后失败集完全一致、无新增失败；本次改写的 repository/datasource/controller/provider 测试（含新增失败分支）全部通过；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 退出码 0（today/record/health-data 规则提示更新 Active_UI_*.md 属提示项——本次为纯内部错误映射重构、无 UI 行为变化，未改 UI 文档）；改动文件已 `dart format`；`runGuarded|Result<|AppError|toAppError|fetchRecordsResult` 在 lib/features/record/** 0 命中；widget 层无 fpdart。未运行 build_runner（`@riverpod` 注解签名无变化，`.g.dart` 无需重新生成）；未运行 bootstrap_generated_sources.dart；未提交。

## 迁移 assistant repository 到 TaskEither

错误迁移 Task 4e：assistant 领域迁移到 fpdart `TaskEither<LucentFailure, T>`，沿用 Task 2/3/4a-4d 的 repository 边界模式（datasource 保持 Future/Stream 传输职责并抛 DioException / LucentFailure，`LucentErrorMapper.fromObject` 为唯一映射入口，非 problem+json/畸形 body 的 `FormatException` 从 `.run()` 直接传播，`LucentApiException` 仅保留为遗留兼容分支、不新增构造点）。

- **repository 边界**：`AssistantRepository` 普通方法（getCapabilities / listRecentConversations / getLatestConversation / openConversation / clearLatestConversation / renameConversation / deleteConversation / confirmProposals）签名改为 `TaskEither<LucentFailure, T>`；`LucentAssistantRepository` 用 `TaskEither.tryCatch` + mapper 包裹。无最新会话（getLatestConversation 返回 null）与空会话列表是合法 Right；服务端业务失败（4xx/5xx Problem Details）为 Left 保留 code/status，不本地猜 status。
- **Stream 语义保持**：`streamMessages` / `regenerateLastMessage` 保持 Stream 事件语义（chunk/result/error/cancelled 不包 TaskEither、不把断流强行转换），repository 与 datasource 原样转发；SSE 断流/取消按既有语义（Task 7 再统一 SSE error 事件消费，本次不动 sse.dart）。assistant datasource 的 `_requireData` 五处空成功体按 today `ai_remote` / record `record.dart` 先例抛 `LucentFailure.network(networkErrorCode: NetworkErrorCode.emptyResponse)`；SSE 结构不符保持 `requireMap`/`FormatException` 协议异常，经 mapper 转 Left(unknown) 或从 `.run()` 传播。
- **provider 消费**：`AssistantController` 全部 repository 调用改为 `.run()` + switch/fold——loadCapabilities / loadLatestConversation / loadRecentConversations / openConversation / clearConversation 的 Left 投影到既有 capabilityError / conversationError / recentConversationError 并复位 loading；rename / delete / confirm / dismiss 的 Left 在更新本地状态后重抛 `LucentFailure` 供页面 toast（`LucentErrorMapper.fromObject` 保留于流式 catch 与 legacy 兼容分支）。`_classifySendError` 增加 `LucentFailure` 与携带 `LucentFailure` 的 `DioException` 分类（网络连接类/取消 → streamInterrupted 触发 F-3 残句保留，其余 → server），`LucentApiException` 遗留分支保留（Task 6 删）。
- **页面消费**：page.dart 删除 6 处 `runGuarded`/`Result` 依赖（rename / delete / confirm / regenerateProposal / regenerate / resend），改为 try/catch 消费 controller 重抛的 `LucentFailure` / `StateError` 后 `userMessageFromError` toast；页面不导入 fpdart、不读 DioException、不解析 code/status，异步 action 全部走 provider 方法。
- **测试**：`test/assistant/**` 全部 fake 同步 TaskEither 签名（controller_test_helpers 的 Fake/ErrorThrowing、page_test 的各 repository 与 `_ConfirmProposalsStub`、`_FullyHangingAssistantRepository` 的 Completer 包 tryCatch）；`lucent_repository_test.dart` 断言改为 `expectTaskRight/expectTaskLeft`，新增失败分支——datasource 抛错 → Left(unknown)、网络连接失败 → Left(network)、空成功体 → Left(network/emptyResponse)、Problem Details 404 → Left 保留 code/status、rename/delete 失败保留 cause；`remote_data_source_test.dart` 新增空成功体 → `LucentFailure.network(emptyResponse)` 分支；controller 测试的 rename/confirm 失败断言从 `throwsA(isA<Exception>())` 改为 `throwsA(isA<LucentFailure>())`（本地 StateError 不变量断言不变）。

验证：`flutter test test/assistant` 与 HEAD 基线（git stash 对比）失败集逐一相同——基线失败为 page_test 的两个 semantics 环境问题（drawer 重命名 / 补名对话框，`node.isMergedIntoParent` 断言，与本次迁移无关），迁移后失败集完全一致、无新增失败；本次新增的 repository/datasource/controller 失败分支测试全部通过；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 退出码 0（assistant 规则提示更新 Active_UI_Today.md/Product_AI_Design.md 属提示项——本次为纯内部错误映射重构、无 UI 行为变化，未改 UI 文档）；改动文件已 `dart format`；`runGuarded|Result<|AppError|toAppError` 在 lib/features/assistant/** 0 命中；presentation/pages 与 widgets 无 fpdart。未运行 build_runner（`@riverpod` 注解签名无变化，`.g.dart` 无需重新生成）；未运行 bootstrap_generated_sources.dart；未提交。

审查修复（提交前）：_classifySendError 的 DioException 分支对遗留 LucentApiException 一并递归分类（防御完备性，无行为回归）。

## 迁移 settings repository 到 TaskEither

错误迁移 Task 5a：settings 领域迁移到 fpdart `TaskEither<LucentFailure, T>`，沿用 Task 2/3/4a-4e 的 repository 边界模式（datasource 保持 Future 传输职责并抛 DioException / LucentFailure，`LucentErrorMapper.fromObject` 为唯一映射入口，非 problem+json/畸形 body 的 `FormatException` 从 `.run()` 直接传播，`LucentApiException` 仅保留为遗留兼容分支、不新增构造点）。

- **repository 边界**：`UserSettingsRepository`（getSettings / updateSettings / enableSecurityPin / changeSecurityPin / disableSecurityPin）与 `NotificationPreferencesRepository`（getPreferences / patchPreferences）全部方法签名改为 `TaskEither<LucentFailure, T>`。`LucentUserSettingsRepository` 用 `TaskEither.tryCatch` + mapper 包裹；`LucentNotificationPreferencesRepository` 为接口唯一实现，直接返回 TaskEither（today suggestion datasource 先例），raw dio PATCH（清空可空睡眠时间）路径保留在 tryCatch 内。未配置的通知偏好行（`configured == false`）是合法 Right，不转失败。
- **datasource**：`SettingsProfileRemoteDataSource` 保持 Future；空成功响应体从 `requireBody` 抛 DioException 改为抛 `LucentFailure.network(networkErrorCode: NetworkErrorCode.emptyResponse)`（auth `_requireBody` / today `ai_remote` 先例）；成功体结构与生成客户端不符时保持协议异常抛出，抛前 `appTalker.error` 记录可诊断。
- **provider 消费**：`UserSettingsController.build` 用 `.run()` + fold 把 Left 投影到 AsyncValue.error；`_patch` / `applySettingsPatch` / 三个 security-pin 方法用 `.run()` + fold，Left 保持 state 不变并重抛 `LucentFailure` 供页面 toast（assistant controller 同款模式）。`NotificationSettingsController.build` 的 getPreferences / 首次迁移 patchPreferences 改为 `.run()` + fold——远端同步失败回退本地值是既有文档化 best-effort 合同（本地存储是主偏好面，迁移标记不置位、下次登录重试），保留记录+继续，catch 兜底协议 `FormatException` 逃逸 `.run()`；`_performRemotePreference` 的 patch 失败回滚本地 + 重抛语义保留（fold Left 与协议逃逸统一走既有 catch 回滚）。
- **页面消费**：ai.dart `_guardedApply`、data_export.dart `_requestExport`、language.dart `_handleLocaleTap`、security_pin.dart 三个 PIN 动作删除 `runGuarded`/`Result` 依赖，改为 try/catch 消费 controller/datasource 抛出的失败后 `userMessageFromError` toast（fallback `settingsSyncFailed`）；security_pin 的 PIN/elevation 语义不变（成功清 elevation token + 成功 toast，失败仅失败 toast）；data_export 的 `requestExport` 保持 `Future<DataExportRequestDataDto?>` 签名（无 settings repository，raw API 直调，report feature 的 runGuarded 调用点不受影响）。advanced.dart（resetProfilePreferences 失败恢复 locale 的既有 catch）与 about.dart（support feature 的 appInfoProvider / 本地 packageInfo，无 settings repository 消费）复核后无需改动。widget 层不导入 fpdart、不读 DioException、不解析 code/status。
- **被迫适配调用方**（仅接口签名变化的机械适配，不改 UI 行为）：today `lucent.dart` 的 `_waterTargetCount`（getSettings 改 `.run()` + fold，Left 与协议逃逸均走既有 defaultWaterTargetCount 降级并 talker 记录——文档化 best-effort 合同）；record `water_target.dart`（getSettings 改 `.run()` + fold，Left 与协议逃逸均回退 `recordWaterDefaultTargetCount` 并 appTalker.warning 记录，原注释文档化合同保留）。
- **测试**：`test/settings/**` 仓库层断言改为 run()/Right（`expectTaskRight`）；`user_settings_controller_test.dart` 的失败断言从 `DioException` 改为 `LucentFailure`（repo 边界映射后控制器抛/投影的均为 LucentFailure）；`notification_preferences_sync_test.dart`、`flow_test.dart`、medicine `reminder_notification_coordinator_test.dart` 的 `_FakeNotificationPreferencesRepository` 与 today `repository_test.dart`、record `detail_page_test.dart` 的 `_FakeUserSettingsRepository` 同步 TaskEither 签名（fake 失败分支返回 `TaskEither.left`）；rollback 测试断言从 `StateError` 改为 `LucentFailure`；`profile_remote_test.dart` 新增空成功体 → `LucentFailure.network(emptyResponse)` 与结构不符保持协议异常两个失败分支测试。页面 stub 控制器（`_StubSettingsController` / `_FakeUserSettingsController` 等）签名未变，无需改动。

审查修复（提交前）：settings 两个仓库空成功体统一为 LucentFailure.network(emptyResponse)（对齐先例）；补 notification_preferences 404 code/status 保留、空体 network Left、controller 本地降级合同与 today 水目标默认值降级测试。

## 迁移 notification repository 到 TaskEither

错误迁移 Task 5b：notification 领域迁移到 fpdart `TaskEither<LucentFailure, T>`，沿用 Task 2/3/4a-5a 的 repository 边界模式（datasource 保持 Future 传输职责并抛 DioException，`LucentErrorMapper.fromObject` 为唯一映射入口，非 problem+json/畸形 body 的 `FormatException` 从 `.run()` 直接传播）。

- **repository 边界**：`NotificationRepository` 全部方法（findAll / findOne / getUnreadCount / markAllAsRead / markAsRead / markAsUnread / delete）签名改为 `TaskEither<LucentFailure, T>`；`LucentNotificationRepository` 用 `TaskEither.tryCatch` + mapper 包裹。空列表、未配置详情（null）为合法 Right；服务端业务失败（4xx/5xx Problem Details）为 Left 保留 code/status，不本地猜 status。数据返回类方法空成功响应体按 `_requireData` 先例抛 `LucentFailure.network(emptyResponse)`（auth `_requireBody` / settings `_requireData` 先例）；void 变更方法接受空（204 风格）成功体为 Right（既有 `delete succeeds on empty 204-style body` 测试锁定）。
- **getUnreadCount 吞错降级评估**：原实现 catch→0 的"轮询类展示"合同（注释 + 两条测试锁定）确认为既有产品行为——未读徽章失败不打断页面（shell / mine / today / medicine 多处消费）。但 repository 边界不再 catch 后无条件成功：失败如实映射 Left，best-effort 降级上移到消费方——`notificationUnreadCount` provider fold Left → 0 并 appTalker 记录（下次轮询自然恢复）；today `_unreadNotificationsFlag` fold Left → hasUnreadNotifications=false 并 talker 记录（协议 FormatException 逃逸 `.run()` 亦被 catch 降级为 false，today 水目标同款文档化合同）。
- **provider 消费**：`notificationDetail`、`NotificationListController.build` 改 `.run()` + fold，Left 投影到 AsyncValue.error；`loadMore` 的 Left 与协议异常（FormatException 逃逸 `.run()`）均投影错误态（保持既有 catch → AsyncValue.error 形状）；`markAllAsRead` / `deleteNotification` / `markAsRead` / `markAsUnread` / `refresh` 改 `.run()` + fold（Left 重抛，成功路径保留既有 invalidate / 乐观更新 / 重新拉取）。widget 层不导入 fpdart、不读 DioException、不解析 code/status。
- **页面消费**：detail.dart 三处直接 repo 调用改为 `.run()` + fold——`_autoMarkRead` 自动标记已读属 best-effort，Left 仅 appTalker 记录（消除原未处理异步异常）；`onMarkRead` / `onMarkUnread` Left 投影为 `userMessageFromError` 失败 toast，成功分支（成功 toast / pop）不执行。list.dart 无直接 repo 调用，未改。
- **被迫适配调用方**（仅接口签名变化的机械适配，不改 UI 行为）：today `lucent.dart` 的 `_unreadNotificationsFlag`（如上）。
- **测试**：`test/notification/**` 仓库层断言改为 run()/Left/Right（`expectTaskRight/expectTaskLeft`），新增失败分支——getUnreadCount/findAll/findOne/delete/markAllAsRead 的网络失败 Left(network)、404 Problem Details code/status 保留（kind business）、空成功体 Left(network/emptyResponse)；`notificationUnreadCountProvider` 降级为 0 的合同测试保留并改在 provider 层断言（repository 层改为断言 Left）；`test/today/repository_test.dart` 的 `_FakeNotificationRepository` 同步 TaskEither 签名，新增 `_FailingNotificationRepository` 锁定"未读数 Left → dashboard 正常渲染且 hasUnreadNotifications=false"。

验证：`flutter test test/notification` 全部通过（基线全绿，迁移后新增 8 个 repository/provider 失败分支测试亦全绿，无新增失败）；`flutter test test/today` 全部通过（基线全绿，新增 1 个未读数 Left 降级测试，无新增失败）；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 退出码 0（notification/today 规则提示项未更新 Active_UI_Mine_Settings.md/Active_UI_Today.md，本次为纯内部错误映射重构、无 UI 行为变化）；改动文件已 `dart format`；`runGuarded|Result<|AppError|toAppError|DioException` 在 lib/features/notification/** 0 命中；presentation 层无 fpdart。未运行 build_runner（`@riverpod` 注解签名无变化，`.g.dart` 无需重新生成）；未运行 bootstrap_generated_sources.dart；未提交。

审查修复（提交前）：detail 页 mark 失败 toast fallback 改为失败语义；unread provider 与 _autoMarkRead 对协议 FormatException catch 记录并降级（对齐 today 消费方写法）；补 FormatException 传播与 provider 降级测试；findOne 接口注释修正为真实契约（not-found=404 Left(business)）。

## 迁移 legal repository 到 TaskEither

错误迁移 Task 5c：legal 领域迁移到 fpdart `TaskEither<LucentFailure, T>`，沿用 Task 2/3/4a-5b 的 repository 边界模式（datasource 保持 Future 传输职责并抛 DioException，`LucentErrorMapper.fromObject` 为唯一映射入口，非 problem+json/畸形 body 的 `FormatException` 从 `.run()` 直接传播）。

- **repository 边界**：`LegalRepository.findAll/findOne` 签名改为 `TaskEither<LucentFailure, T>`；`LucentLegalRepository` 用 `TaskEither.tryCatch` + mapper 包裹。合法空列表为 Right；服务端业务失败（4xx/5xx Problem Details）为 Left 保留 code/status，不本地猜 status。空成功响应体按 settings/notification `_requireData` 先例抛 `LucentFailure.network(networkErrorCode: NetworkErrorCode.emptyResponse)`（原 `requireData` 的 `StateError` 替换）。
- **404 → 内置资产 fallback 评估（保留）**：`plans/2026-07-10-legal-compliance-pages.md` P2-2 明确记录「远程优先 + assets fallback」为已完成的产品合同——法律/合规页必须在服务端尚未发布文档时仍可查看，`assets/legal/` 7 类 × zh/en 资产齐备。因此 404 回退资产是既有文档化 best-effort 合同，保留为**替代成功路径（Right）**：404（Problem Details 或裸 DioException 均可触发，与旧实现一致）→ `appTalker.warning` 记录后回退（补上原先静默的「记录」）；fallback 资产缺失在 findAll 跳过（既有「记录+继续」吞处理保留），在 findOne 无资产可回退时按既有抛错语义经 mapper 转 `Left(unknown)`（cause 保留原异常）。fallback 不是 catch-all：网络/5xx/其他 4xx rethrow 进 mapper → Left。
- **provider 消费**：`legalDocuments`、`legalDocument` 改 `.run()` + fold，Left 重抛投影到 AsyncValue.error（协议 `FormatException` 逃逸 `.run()` 亦被 Riverpod 捕获为错误态）；widget（list/detail 页）只消费 provider state，未改、不导入 fpdart、不读 DioException、不解析 code/status。
- **被迫适配调用方**（仅接口签名变化的机械适配，不改行为）：`integration_test/support/e2e_test_helpers.dart` 的 `E2eLegalRepository` 返回 `TaskEither.right`；页面/provider 测试的 fake 同步 TaskEither 签名（失败 fake 返回 `TaskEither.left(LucentFailure...)`）。
- **测试**：`test/features/legal/**` 仓库层断言改为 run()/Left/Right（`expectTaskRight/expectTaskLeft`）；新增失败分支——findAll/findOne 网络失败 Left(network)、500/403 Problem Details code/status 保留（kind server/business）、非 problem body → `FormatException` 从 `.run()` 传播、空成功体 → Left(network/emptyResponse)、普通异常 → Left(unknown) 且 cause 保留、findOne 404 无资产 → Left(unknown)；404 fallback 合同测试保留（findAll 404 → Right 空列表（单测无资产）、findOne 404 → Left(unknown)）；provider 新增 Left 投影 AsyncValue.error 断言。`legal_doc_type_test` 为纯实体测试，未改。

验证：`flutter test test/features/legal` 与 HEAD 基线（git stash 对比）失败集一致——基线全绿，迁移后新增失败分支测试亦全绿，无新增失败；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 退出码 0；改动文件已 `dart format`；`runGuarded|Result<|AppError|toAppError` 在 lib/features/legal/** 0 命中；widget 层无 fpdart。未运行 build_runner（`@riverpod` 注解签名无变化，`.g.dart` 无需重新生成）；未运行 bootstrap_generated_sources.dart；未提交。

## 迁移 support repository 到 TaskEither

错误迁移 Task 5d：support 领域迁移到 fpdart `TaskEither<LucentFailure, T>`，沿用 Task 2/3/4a-5c 的 repository 边界模式（datasource 保持 Future 传输职责并抛 DioException，`LucentErrorMapper.fromObject` 为唯一映射入口，非 problem+json/畸形 body 的 `FormatException` 从 `.run()` 直接传播）。

- **repository 边界**：`SupportRepository.getAppInfo` 签名改为 `TaskEither<LucentFailure, AppInfo?>`；`LucentSupportRepository` 用 `TaskEither.tryCatch` + mapper 包裹。空成功响应体按 settings/notification `_requireData` 先例抛 `LucentFailure.network(emptyResponse)`；服务端业务失败（4xx/5xx Problem Details）为 Left 保留 code/status，不本地猜 status。
- **AppInfo 可空语义决策**：接口与 provider 保留 `AppInfo?` 可空返回。依据后端契约（Lucent `AppInfoService` 构造时把未配置 env 映射为 null 字段、恒返回对象）——「未配置/可选元数据」由「字段全空的 AppInfo」表示，是合法 Right，About/Help 页消费方按既有文档化回退链处理（supportEmail → EnvReader、缺失 latestVersion → checkFailed、无 supportEmail → `kFallbackSupportUrl`），不转失败；Right(null) 保留为「无元数据可用」的合法值（当前实现因后端恒返回对象不会产生，测试 override 与消费方 null 守卫继续有效）。空成功响应体（`data: null`）才是协议违约，按 emptyResponse 先例为 Left(network)，不被吞成 Right(null)。
- **provider 消费**：`appInfo` provider 改 `.run()` + fold，Left 重抛投影到 AsyncValue.error（协议 `FormatException` 逃逸 `.run()` 亦被 Riverpod 捕获为错误态）；widget（settings about/help 页）只消费 provider state，未改、不导入 fpdart、不读 DioException、不解析 code/status。
- **被迫适配调用方**（仅接口签名变化的机械适配，不改行为）：`test/settings/help_settings_page_test.dart` 的 `_FakeSupportRepository` 返回 `TaskEither.right(null)`；`about_settings_page_test.dart` 经 `appInfoProvider.overrideWith` 覆写，provider 签名不变，无需改动。
- **测试**：`test/support/app_info_provider_test.dart` 改为 provider 级断言（Right 数据 / Right null / Left → AsyncValue.error）；新增 `test/support/data/repositories/lucent_repository_test.dart` 失败分支——网络失败 Left(network)、500/404 Problem Details code/status 保留（kind server/business）、非 problem body → `FormatException` 从 `.run()` 传播、空成功体 → Left(network/emptyResponse)、普通异常 → Left(unknown) 且 cause 保留、未配置全空字段保持 Right。

验证：`flutter test test/support` 与 HEAD 基线（git stash 对比）一致——基线全绿，迁移后新增失败分支测试亦全绿，无新增失败；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 退出码 0；改动文件已 `dart format`；`runGuarded|Result<|AppError|toAppError` 在 lib/features/support/** 0 命中；widget 层无 fpdart。未运行 build_runner（`@riverpod` 注解签名无变化，`.g.dart` 无需重新生成）；未运行 bootstrap_generated_sources.dart；未提交。

## 迁移 health_event repository 到 TaskEither

错误迁移 Task 5e：health_event 领域迁移到 fpdart `TaskEither<LucentFailure, T>`，沿用 Task 2/3/4a-5d 的 repository 边界模式（`LucentErrorMapper.fromObject` 为唯一映射入口，非 problem+json/畸形 body 的 `FormatException` 从 `.run()` 直接传播）。

- **repository 边界**：`HealthEventRepository` 全部方法（fetchActive/fetchById/fetchHistory/create/checkIn/end）签名改为 `TaskEither<LucentFailure, T>`；`LucentHealthEventRepository` 用 `TaskEither.tryCatch` + mapper 包裹。合法空历史为 Right；服务端业务失败（4xx/5xx Problem Details）为 Left 保留 code/status，不本地猜 status。
- **404 → null 语义决策（保留 Right(null)）**：`fetchActive` 404 = 无活动事件、`fetchById` 404 = 事件不存在，均为正常业务状态（Task 5 Step 1「未配置可选数据保持 Right」）——Today/Report 页在无活动事件时渲染「开始观察」入口，若转 Left 会回归为错误态。保留 404 → Right(null) 并补 `appTalker.warning` 记录（原静默，legal 404 fallback「记录+继续」先例）；非 404 的 4xx/5xx 与网络错误 rethrow 进 mapper → Left。`fetchActive`/`fetchById` 空成功体（data null）亦为 Right(null)（assistant `getLatestConversation` 可空先例）。
- **fetchHistory 空成功体**：`response.data == null`（无 body）按 settings/notification `_requireData` 先例抛 `LucentFailure.network(emptyResponse)`（原 `?? const []` 吞为空列表的默认值删除）；显式空 `items` 列表保持 Right。
- **StateError 协议不变量点（:105/:150/:165/:181，`_mapRequired`/`_mapStatus`/`_mapOutcome`/`_asString`）**：按任务指令保持协议异常——写/详情响应缺事件体、未知枚举值、字段类型不符均抛 `StateError`，抛前 `appTalker.error` 记录可诊断，经 mapper 转 `Left(unknown)`（cause 保留原异常），不转业务失败、不本地猜 code。注意：`_mapRequired` 的 null-dto 按任务指令归类为协议不变量（既有测试锁定 StateError 契约），与 settings/notification/legal `_requireData` 的 emptyResponse 先例不同，已在类注释说明。
- **provider 消费**：`ActiveHealthEvent` 的 build/refresh/create/checkIn/end 改为 `.run()` + fold——Left 重抛 `LucentFailure` 投影到 AsyncValue.error 或 sheet 提交路径；`_ensureAuthenticated`（AuthRequiredException）身份检查语义不变。sheets（start/end/check_in）保持 `onSubmit: Future<void>` 回调（widget 不导入 fpdart、不读 DioException、不解析 code/status），run()+fold 在 notifier 边界完成，sheet 的 catch 仍是既有 submitError 投影点（补注释说明投影契约）。`health_event.g.dart`/`active_event.g.dart` 注解签名无变化，无需 build_runner。
- **被迫适配调用方**（仅接口签名变化的机械适配，不改行为）：`test/report/page_test.dart` 的 `_FakeHealthEventRepository`、`integration_test/report/review_closed_loop_e2e_test.dart` 的 `E2eLoopHealthEventRepository` 返回 `TaskEither.right`；`integration_test/support/e2e_test_helpers.dart` 仅字段类型引用，未改。lib/ 内无其他 `HealthEventRepository` 生产调用方（today/report 页面消费的是 notifier 方法，未改）。
- **测试**：`test/health_event/**` 全部适配 run()/Left/Right（`expectTaskRight/expectTaskLeft`）——repository 测试新增失败分支（fetchActive/fetchHistory 网络失败 Left(network)、404 Problem Details code/status 保留、空成功体 → Left(network/emptyResponse)、写响应缺事件体 → Left(unknown) 且 cause 为 StateError、非 problem body → `FormatException` 从 `.run()` 传播、404 无事件 → Right(null)）；provider 测试的 fake 改 TaskEither（Completer 包 tryCatch）并新增「Left → AsyncError」断言。**retry 发现**：`LucentFailure` 非 `Error` 子类，Riverpod 容器默认 retry 会指数退避重试并保持 loading（`.future` 断言超时）——Left → AsyncError 断言按 report 套件先例用 `ProviderContainer(retry: (count, error) => null)` 关闭重试。

验证：`flutter test test/health_event` 与 HEAD 基线（git stash 对比）失败集逐一相同——失败均为基线已存在的页面 widget 测试问题（start_event_test 的「提交标题与关联项」「失败后保留标题并显示可重试错误」与 today_health_event_section_test 的「提交每日结果」，均属既有 FToaster/semantics 环境问题，变更前后失败集完全一致），迁移后无新增失败；本次新增的 repository/provider 失败分支测试与 report page_test 适配全部通过；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 退出码 0；改动文件已 `dart format`；`runGuarded|Result<|AppError|toAppError` 在 lib/features/health_event/** 0 命中；widget 层无 fpdart。未运行 build_runner（`@riverpod` 注解签名无变化，`.g.dart` 无需重新生成）；未运行 bootstrap_generated_sources.dart；未提交。

## 迁移 mine repository 与 pending sync 错误模型到 LucentFailure

错误迁移 Task 5f：mine 领域迁移到 fpdart `TaskEither<LucentFailure, T>`，并同步迁移 pending sync 错误模型（`PendingSyncErrorDetails`）从遗留错误形状到 `LucentFailure` 形状（kind 从遗留枚举改为 `LucentFailureKind`、code 从数值改为 Problem Details 字符串 code），沿用 Task 2/3/4a-5e 的 repository 边界模式（`LucentErrorMapper.fromObject` 为唯一映射入口，非 problem+json/畸形 body 的 `FormatException` 从 `.run()` 直接传播）。

- **repository 边界**：`MineRepository.fetchDashboard` 签名改为 `TaskEither<LucentFailure, MineDashboard>`；`signedOutDashboard` 为纯本地预览值保持 Future（today `signedOutDashboard` 先例）。`LucentMineRepository.fetchDashboard` 用 `TaskEither.tryCatch` + mapper 包裹——聚合的 health-context 快照失败如实映射 Left（快照失败不被默认值吞掉、不虚构 dashboard），服务端业务失败保留 Problem Details 的 code/status，不本地猜 status。
- **provider 消费**：`mineDashboard` 改 `.run()` + fold，Left 投影到 AsyncValue.error；widget 层（page/archive/sync banner/sync failed details）只消费 provider state 与 `PendingSyncEntry`，不导入 fpdart、不读 DioException、不解析 code/status。archive 页对 `healthContextSnapshotProvider.future` 的「记录+继续」best-effort catch（快照加载失败回退直接导航）复核为既有产品行为，保留。
- **pending sync 模型迁移（关键，跨 core/database）**：`PendingSyncErrorDetails.kind` 从遗留 `AppErrorKind?` 改为 `LucentFailureKind?`，`code` 从 `int?` 改为 `String?`；`fromAppError(AppError, raw)` 改为 `fromLucentFailure(LucentFailure, raw)`；`worker.dart` 两处 catch 路径从 `toAppError` + `fromAppError` 改为 `fromObject` + `fromLucentFailure`（网络失败保留 networkErrorCode、业务失败保留 Problem Details code/status）；`pending_sync_dao._parseErrorDetails` 的 JSON 解析 catch→null 防御保持。模型字段变更经 `dart run build_runner build` 重新生成 `.g.dart`/`.freezed.dart`（两文件均被 gitignore，不入提交）。
- **JSON 向后兼容（必须，有测试）**：`fromJson` 先经 `_normalizeLegacyJson` 归一化再走生成代码，解码永不抛错导致整行结构化详情丢失——旧 `kind` 字符串（'network'/'auth'/'server'/'business'/'unknown'）映射到对应 `LucentFailureKind`（'auth' → authentication），未知 kind 值回退 unknown、非字符串 kind 置 null；旧 `int` code 转字符串，其中遗留数值校验码 400001/400002 归一化为当前 Problem Details code `VALIDATION_FAILED`（使旧行「数据无效」文案映射继续生效，其余数值 code 转十进制字符串、整数 double 同值归一、非数值 code 置 null）。
- **sync_error_user_message 迁移**：`mapSyncErrorToUserMessage` 从遗留 kind/数值 code 分派改为基于 `LucentFailureKind`/`NetworkErrorCode`/Problem Details 字符串 code 分派（业务校验失败按服务端 code `VALIDATION_FAILED`，不本地猜 status；网络层失败仍优先走 `NetworkErrorL10n.map`）。
- **测试**：新增 `test/core/database/models/pending_sync_error_details_test.dart`（fromLucentFailure 字段拷贝、当前形状 round-trip、legacy kind 全量映射与未知值回退、int/double 数值 code 归一、完整 legacy 行解码）；新增 `test/mine/data/repositories/lucent_test.dart`（快照成功 Right、网络失败 Left(network)、业务失败 Left 保留 code/status、signedOutDashboard 纯 Future——失败分支用 `ProviderContainer(retry: (count, error) => null)` 关闭 Riverpod 默认重试，report/health_event 套件先例）；新增 `test/mine/presentation/mappers/sync_error_user_message_test.dart`（kind/code/networkErrorCode 各分派分支）；`test/mine/sync_failed_banner_test.dart` 的遗留 kind 改 `LucentFailureKind.network`；`test/helpers/mocks/mine.dart`、`test/mine/page_test.dart`（_EmptyPreviewMineRepository）、`test/auth/session_gate_test.dart`（_CountingMineRepository）的 mine fake 同步 TaskEither 签名。
- **被迫适配调用方**（仅接口签名变化的机械适配，不改行为）：如上三个测试 fake；`lib/core/providers/auth_guarded.dart` 文档示例同步 `.run()` + fold 写法（注释级，core 非 feature 代码）。health_context/medicine/record 的 `_enqueueWriteFailure` 走 `pendingSyncDao.enqueue`，enqueue 签名未变，无需改动；`product_event_service_test` 仅用 `dao.enqueue`，不受影响。

验证：`flutter test test/mine test/core/database test/auth/session_gate_test.dart` 与 HEAD 基线（git stash 对比）失败集逐一相同——基线 24 个失败与迁移后 24 个失败完全一致（均为基线已存在的 mine 页面 widget 测试问题：account_settings_page_test / edit_pages_test / allergy_edit_page_test / condition_edit_page_test / page_test 的 semantics/FToaster 环境问题），无新增失败；本次新增的模型兼容、repository、mapper 测试全部通过；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 退出码 0（core-database/core-providers/mine 规则提示项未更新 data-layer/state-management/Active_UI_Mine_Settings.md，本次为纯内部错误映射重构、无 UI 行为变化）；改动文件已 `dart format`（含 build_runner 重新生成的 .g/.freezed）；`runGuarded|Result<|AppError|toAppError|AppErrorKind` 在 lib/features/mine/** 与 lib/core/database/** 0 命中；widget 层无 fpdart。已运行 `dart run build_runner build`（模型字段变更重新生成 .g/.freezed，均被 gitignore 不入提交）；未运行 bootstrap_generated_sources.dart；未提交。

## 迁移 report repository 到 TaskEither

错误迁移 Task 5g（Task 5 最后一个）：report 领域迁移到 fpdart `TaskEither<LucentFailure, T>`，沿用 Task 2/3/4a-5f 的 repository 边界模式（datasource 保持 Future/Stream 传输职责，`LucentErrorMapper.fromObject` 为唯一映射入口，非 problem+json/畸形 body 的 `FormatException` 从 `.run()` 直接传播）。

- **repository 边界**：`ReportRepository.fetchDashboard` 与 `ReviewRepository` 全部方法（fetchCurrentReview/fetchHistory/fetchReview）签名改为 `TaskEither<LucentFailure, T>`；`signedOutDashboard` 为纯本地预览值保持 Future（today `signedOutDashboard` 先例）。「无事件」的 current 回顾（空信封）保持 `Right(null)`，合法空历史保持 Right；服务端业务失败（4xx/5xx Problem Details，如 detail 事件不存在 404）为 Left 保留 code/status，不本地猜 status。`ReportAiSummaryRepository.generateStream` 保持 Stream 事件语义（不包 TaskEither、不把断流强行转换，Task 7 再统一 SSE error 事件消费，本任务不动 `ai_summary_remote.dart` 的 SSE 解析）。
- **实现类**：`LucentReportRepository`、`LucentReviewRepository` 用 `TaskEither.tryCatch` + mapper 包裹（review 的 inline `ReviewRemoteDataSource` 保持 Future 传输）；`LucentReportAiSummaryRepository` 仅 Stream 转发，无需改动。datasource `report.dart` 与 review inline datasource 的空成功响应体按 settings/notification `_requireData` 先例改为抛 `LucentFailure.network(networkErrorCode: NetworkErrorCode.emptyResponse)`（原 `requireData` 的 `StateError` 替换）；`fetchCurrentReview` 的 `data` 可空保持原样（空信封语义）。
- **provider 消费**：`reportDashboard`、`reviewCurrent`、`reviewHistory`、`reviewDetail` 改 `.run()` + fold，Left 重抛投影到 AsyncValue.error（5s/10s 超时保留在 `.run()` 返回的 Future 上，fpdart TaskEither 无 timeout，超时语义不变）；`page.dart` 的 `onHistoryLoadMore` 改 run()+fold（Left 重抛，由 `ReviewHistorySection` 既有 catch 投影为加载更多失败行）。`providers/ai_summary.dart` 复核无需改动——`generateStream` 保持流语义，403 → disabled 与空结果/取消按既有 unknown/network 投影（today AI 分析先例），无旧类型引用。
- **runGuarded/toAppError 清理**：`clinic_summary_shared.dart` 的 `toAppError`/`AppErrorKind` 判定改为 `LucentErrorMapper.fromObject(...).kind == LucentFailureKind.network`；`export_actions.dart`（`handleReportExportAction`）的 `runGuarded` 改为 try/catch 消费 `requestExport` 抛出的失败，失败分支保持「记录 exported failure + `userMessageFromError(error, l10n:)` toast」（`requestExport` 仍为 settings 侧 raw API `Future` 签名，未迁移）；`share_management.dart` `_revoke`、`clinic_summary_preview_dialog.dart` `_createShare`/`_revokeShare` 的 `runGuarded` 改为 try/catch + 既有固定失败 toast（widget 不导入 fpdart、不读 DioException、不解析 code/status）。`pdf_download.dart`、`review_history.dart` 复核无旧类型引用、行为不变。
- **被迫适配调用方**（仅接口签名变化的机械适配，不改行为）：`test/helpers/mocks/report.dart`（fetchDashboard 返回 `TaskEither.right`，文档示例同步）、`integration_test/support/e2e_test_helpers.dart` 的 `E2eReviewRepository`、`integration_test/report/review_closed_loop_e2e_test.dart` 的 `E2eLoopReviewRepository` 同步 TaskEither 签名；mocktail 的 `MockReportRepository` 为动态 mock 无需改动。
- **测试**：`test/report/**` 仓库层断言改为 `expectTaskRight/expectTaskLeft`；`review_provider_test.dart` / `page_test.dart` / `widgets/review_detail_page_test.dart` / `more_actions_test.dart` / `review_semantics_test.dart` 的 fake 同步 TaskEither 签名（Completer 挂起 fake 包 tryCatch，失败 fake 返回 `TaskEither.left`），失败断言从 `DioException` 改为 `LucentFailure`；新增失败分支——dashboard/review 网络失败 Left(network)、404 Problem Details code/status 保留（kind business）、空成功体 Left(network/emptyResponse)、非 problem body → `FormatException` 从 `.run()` 传播、普通异常 → Left(unknown) 且 cause 保留；`remote_data_source_test.dart` 新增空成功体 → `LucentFailure.network(emptyResponse)` 分支。

验证：`flutter test test/report` 与 HEAD 基线（git stash 对比）失败集逐一相同——失败均为基线已存在的页面 widget 测试问题（semantics/FToaster 环境问题，变更前后失败集一致），迁移后无新增失败；本次新增的 repository/datasource/provider 失败分支测试全部通过；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 退出码 0；改动文件已 `dart format`；`runGuarded|Result<|AppError|toAppError` 在 lib/features/report/** 0 命中（Task 5 最后一个遗留旧类型的 feature，清理后全库旧类型只剩 core/errors 自身与待删文件）；widget 层无 fpdart。未运行 build_runner（`@riverpod` 注解签名无变化，`.g.dart` 无需重新生成）；未运行 bootstrap_generated_sources.dart；未提交。

审查修复（提交前）：export_actions/share_management/clinic_summary_preview_dialog 的失败 catch 补充 talker 失败日志（对齐 mine/assistant 先例，原 runGuarded 的 Sentry 观测不丢失）。

## 删除旧 Result、AppError 与 runGuarded

错误迁移 Task 6：删除自建旧错误体系（`Result<T>`/`Success`/`Failure`、`AppError`/`AppErrorKind`、`runGuarded`、`LucentErrorMapper.toAppError`），Task 2-5 后已无业务调用方。

- **删除文件**：`lib/core/errors/result.dart`、`lib/core/errors/run_guarded.dart`、`lib/core/errors/error.dart`（含 `AppErrorKind`）；对应测试 `test/core/errors/app_error_test.dart`、`result_test.dart`、`run_guarded_test.dart`、`error_mapper_to_app_error_test.dart`。`lib/core/errors/` 现仅剩 `lucent_failure.dart`、`network_error_l10n.dart`、`user_message.dart`，无 barrel 文件。
- **user_message.dart 重写**：移除 `AppError` 分支与 `toAppError` 委托，改为直接基于 `LucentFailure`/`DioException`（经 `LucentErrorMapper.fromObject`，`LucentFailure` 原样透传）取展示文案；`NetworkErrorL10n` 网络层映射与空消息 fallback 语义保留不变。generic `Exception` 仍映射为 `LucentFailure.unknown('An unexpected error occurred.')`，与旧行为一致（search 套件既有断言锁定）。
- **error_mapper.dart**：删除 `toAppError` 与 `_toAppErrorKind`（仅服务旧 AppError 的分支）及 `error.dart` import；`fromObject` 全部现有语义保留。**`LucentApiException` → `LucentFailure` 遗留兼容分支保留**——sse/map_utils/wechat 仍有 `LucentApiException` 构造点（Task 7 再清），删除该分支会破坏这些路径的映射。
- **复核无需改动**：`sentry_talker_observer.dart` 只处理 `LucentFailure`/`LucentApiException`/`DioException`，无 AppError 分支；`test/core/logger/logger_test.dart` 无旧类型引用；`test/core/database/models/pending_sync_error_details_test.dart` 的 `AppError` 字样仅为 legacy 数据解码契约的注释与字符串数据，非类型引用，保留。
- **零引用确认**：改动前 grep `Result<|Success<|Failure<|runGuarded|AppError|AppErrorKind|toAppError` 于 lib/ 与 test/，命中仅为待删除文件自身、error_mapper 的 toAppError 定义、user_message 的 AppError 引用与四个待删测试文件；lib/features/**/presentation/ 与 integration_test 均无遗漏引用。

验证：`flutter test test/core/errors test/core/logger` 全绿（`lucent_failure_test.dart` 保留且通过，被删测试文件不再存在）；`flutter analyze` 全仓 0 问题；`dart run scripts/check_doc_coverage.dart --warning-only` 退出码 0；改动文件已 `dart format`；改动后 grep 旧类型在 lib/ 0 命中、test/ 0 命中（均排除保留的 `LucentApiException`，test 侧仅剩 pending_sync 测试的 legacy 字符串数据）。未运行 build_runner（无注解签名变化）与 bootstrap_generated_sources.dart；未提交。

说明：doc-coverage 对 error_mapper/user_message 的文档更新警告（data-layer.md、ADR-0008）属 Task 9 文档收尾范围，已由计划跟踪；ADR-0008 的 Result/runGuarded 删除目标态表述在 Task 6 落地后即为准确状态。
