# 2026-08-23

## Today AI 数据源空响应使用 LucentFailure

修复 `TodayAiRemoteDataSource` 在直接资源迁移中引入的裸 `StateError`。`read()` 与 `refresh()` 在响应体为空或格式非法时，现在统一抛出 `LucentFailure.network(..., networkErrorCode: NetworkErrorCode.emptyResponse)`，由 `LucentErrorMapper` 映射为 `AppErrorKind.network`，不再穿透未分类异常到 UI。同步补充 `read` 与空/畸形 `refresh` 响应的单元测试。

验证：`test/today/ai_remote_data_source_test.dart` 全部通过；`flutter analyze` 无问题。

## 收口网络失败边界

错误迁移 Task 1：固定网络层错误分类、重试与 token refresh 的唯一语义（LucentErrorMapper → RetryPolicy → AuthInterceptor），全部收敛到 `LucentFailure`。

- **error_mapper 契约**：确认并锁定 HTTP 错误必须先校验 `application/problem+json` 再解析；缺少 body、错误媒体类型、字段类型不符（如 `code` 非字符串、`retryAfter` 为负/小数）保持 `FormatException`（协议不变量，不转业务 LucentFailure）；连接/超时/证书类无 response 错误映射为 `LucentFailure.network`。`target_error_contract_test.dart` 补充对应契约测试。
- **refresh 触发条件**：`AuthInterceptor._shouldRefresh` 从「401 且不在黑名单」改为白名单语义——仅当 Problem Details `code == 'AUTH_TOKEN_EXPIRED'`（`LucentFailure.isTokenExpired`）才触发 refresh；`AUTH_REQUIRED`、`AUTH_REFRESH_TOKEN_INVALID`、`AUTH_WRONG_PASSWORD`、普通 401/403 一律不刷新（401 清 session 并通知，403 保留 session）。保留 `skipAuthRefresh`/`hasRetriedAfterRefresh` 短路、`_refreshFuture` 并发去重，以及 refresh 明确认证失败（401/403）清 session 并把原始请求错误传回调用方、临时失败（网络/5xx/空 body）保留 session 的既有行为。
- **重试边界**：确认 `RetryPolicy` 写请求必须显式 `retryEnabled == true` 且带非空 `Idempotency-Key`；`retryable=false` 禁止重试；`retryable=true` 不扩大客户端允许的状态集合。`Retry-After` 只接受非负整数秒，负数/小数/HTTP-date 回退指数退避（`retry_policy_test.dart` 补充用例）。运行时链上 `RetryInterceptor` 注册于 `ErrorInterceptor` 之前，`onError` 决策时先经 `LucentErrorMapper` 映射失败（畸形 body 捕获 `FormatException` 视为无 hint）再交给 `RetryPolicy` 判断，使 `retryable=false` 与 `retryAfter` 在生产链上真正生效；最终错误仍由 `ErrorInterceptor` 映射，职责划分不变（`retry_interceptor_test.dart` 补充拦截器链测试验证）。
- **遗留类型**：`LucentApiException` 仅加遗留类型文档注释（不新增构造点、不加 `toAppError` 调用）。

验证：`flutter test test/core/network` 全部通过；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 无阻塞警告（core-network 规则 docs_any_of 建议文档未更新，属提示项）。

## 迁移认证 repository 到 TaskEither

错误迁移 Task 2：Auth/session repository 与 provider 迁移到 fpdart `TaskEither<LucentFailure, T>`。

- **repository 边界**：`AuthRepository` 全部方法与 `AuthSessionsRepository.listSessions/revokeSession` 签名改为 `TaskEither<LucentFailure, T>`；`LucentAuthRepository`、`LucentAuthSessionsRepository` 实现用 `TaskEither.tryCatch(() => datasource 调用 + DTO 映射 + session 持久化, (error, stackTrace) => LucentErrorMapper.fromObject(error))` 包裹，datasource 仍只负责 Future 传输并抛出 DioException。空响应体 `_requireBody` 按 today `ai_remote.dart` 先例改为抛 `LucentFailure.network(networkErrorCode: NetworkErrorCode.emptyResponse)`（保留 operation 上下文）；sessions 列表结构不符的 `StateError` 属协议不变量，保持抛出并经 mapper 转 `Left(unknown)`，非 problem+json 畸形 HTTP body 仍由 mapper 抛 `FormatException` 从 `.run()` 直接传播。
- **注销语义**：移除 `logout()` 的 `finally` 无条件 `_sessionStore.clear()`——远程注销成功（或无 refresh token 可注销）才在 Right 路径清本地 session；远程注销失败返回 Left 且不清本地 session、不写 token holder。`changePassword`/`deleteAccount` 成功清 session 的既有行为保留在 Right 路径内。登录成功后的 `_persistSession` 位于 Right 路径（持久化失败则整操作 Left）。
- **refresh 语义**：`refreshSession` 的 Left 保留服务端 Problem Details 的 code/status，`AUTH_REFRESH_TOKEN_INVALID` 时 `LucentFailure.isRefreshTokenInvalid == true`，不会被转成普通网络错误；网络类失败仍映射为 `LucentFailure.network`。
- **provider 消费**：`session_provider.restore()` 用 `run()` + Left 分类重写（快速恢复 fetchAccount → 认证类失败回落 refresh、网络连接错误保留 session store、其余清 store），`logout()` 改为 Right 清 UI 状态、Left 投影 errorMessage 且不误登出；`forms/{login,register,password_reset}.dart` 用 `run()` + switch 投影到既有 form state，不再引用 `Result`/`runGuarded`/`AppError`；`oauth_login.dart`、`account.dart`、`presentation/providers/sessions.dart`、`wechat_oauth.dart` 消费 `TaskEither.run()`（`_resolve` 适配器把 Left 重抛进既有 action-state 处理路径，等价投影）。widget 层（pages/widgets）不导入 fpdart、不读 DioException、不解析 code/status。
- **elevation 语义**：检查 `security_elevation.dart`、`security_elevation_token_holder.dart`、`security_elevation_interceptor.dart` 后确认无需改动——`verify()` 失败不写 token holder、401/403/网络失败与意外异常均返回 false 保持 Unverified；账户操作里的 403 elevation 失败由 `_isSecurityElevationFailure` 标记 `requiresSecurityElevation` 并清空本地 token，不吞成"普通未登录"；既有 `security_elevation_test.dart` 与 `account_provider_test.dart` 已锁定该语义。elevation token holder 与 session_store 未改动。
- **测试**：`test/auth/**` 仓库层测试改为消费 `run()` 断言 Left/Right；新增 `remote_data_source_failure_test.dart` 覆盖 AUTH_TOKEN_EXPIRED、AUTH_REFRESH_TOKEN_INVALID（code/status 保留）、AUTH_WRONG_PASSWORD、验证码错误、网络 timeout、Problem Details 4xx、非 problem body → FormatException 传播、空 body → emptyResponse、logout/changePassword/deleteAccount 失败保留本地 session；form/account/session/sessions 的 mock 与 provider 测试同步到 TaskEither 签名；`integration_test` 的 `E2eLucentAuthRepository` 同步接口签名。

验证：`flutter test test/auth test/core/network/interceptors/auth_interceptor_test.dart test/core/providers/security_elevation_test.dart` 通过，除 HEAD 基线已存在的页面 widget 测试失败（semantics 断言，与本次迁移无关，变更前后失败集一致）外无新增失败；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 无阻塞警告。未运行 build_runner（`@riverpod` 注解签名无变化，`.g.dart` 无需重新生成）；未运行 bootstrap_generated_sources.dart。

## 迁移 health_context 与 today repository 到 TaskEither

错误迁移 Task 3：health_context 与 today 领域迁移到 fpdart `TaskEither<LucentFailure, T>`，沿用 Task 2 的 repository 边界模式（datasource 保持 Future/Stream 传输职责，`LucentErrorMapper.fromObject` 为唯一映射入口，非 problem+json/畸形 body 的 `FormatException` 从 `.run()` 直接传播）。

- **health_context**：`HealthContextRepository` 全部方法签名改为 `TaskEither<LucentFailure, HealthContextSnapshot>`；`LucentHealthContextRepository` 用 `TaskEither.tryCatch` 包裹。fetch 缓存优先 + 后台刷新（30s 节流）、写失败入 pending sync 队列后 Left 的既有合同保留（网络失败仍先 `_enqueueWriteFailure` 再映射）。服务端失败（如资源不存在 404）保留 Problem Details 的 code/status，不本地猜测。
- **缓存写失败两路径（Step 3）**：路径 A——缓存为空时网络拉取后写缓存是请求成功必要条件，`dao.replace` 失败返回 Left；路径 B——后台刷新写缓存属 best-effort，talker 记录并继续返回缓存快照。写方法在远端成功后缓存写失败同样映射 Left（不 catch 后无条件成功）。两种路径均有 repository 测试。
- **today**：`TodayRepository.fetchDashboard`、`TodayAiRepository.read/refresh/generate`、`SuggestionRepository` 全部方法迁移到 TaskEither；`generateStream` 保持 Stream 事件语义（不包 TaskEither），`signedOutDashboard` 为纯本地值保持 Future。`LucentTodayRepository` 的 degraded 降级是产品行为，各 catch 点继续 talker 记录观测；仅意外异常成为 Left。`TodaySuggestionRemoteDataSource` 作为 `SuggestionRepository` 的唯一实现直接返回 TaskEither（`requireData` 空体经 mapper 转 unknown Left，未改传输层语义）。AI 分析空状态（materializationStatus empty/pending/stale/failed）与空建议 bundle 保持 Right/流语义，不转 server failure；`generate()` 流结束无结果属本地不变量，映射 unknown Left。
- **provider 消费**：`healthContextSnapshot`、`todayDashboard`、`todaySuggestion`（fetch/submitFeedback/explain）、`suggestionHistory`、`todayAiAnalysisController`（_fetch/refresh）改为 `.run()` + fold，Left 投影到 AsyncValue.error 或既有 action state；AI 分析 403 仍映射 disabled 态。widget 不导入 fpdart、不解析 code/status。
- **被迫适配调用方**（仅接口签名变化的机械适配）：mine `health_edit_forms.dart`（run()+fold 到既有 form state）；medicine `medicine_detail.dart` 与 search `add_to_box.dart`（`createCurrentMedicine`）；medicine `lucent_workspace.dart`（`fetchHealthContext`）。
- **测试**：`test/health_context/**`、`test/today/**` 全部适配 run()/Left/Right 断言；新增失败分支测试（health_context：缓存写失败路径 A/B、404 Problem Details code 保留、网络失败 Left；today AI：read/refresh 网络失败与 403 code 保留、suggestion：404 code 保留 / 网络超时 / 非 problem body → `FormatException` 从 `.run()` 传播）；其余 feature 测试因调用方适配同步更新 fake 签名（mine/medicine/search/scan/auth/session_gate），`integration_test/support/e2e_test_helpers.dart` 的两个 E2e 仓库 fake 同步接口。

验证：`flutter test test/health_context`、`flutter test test/today` 全部通过；受适配影响的 mine/medicine/search/scan/auth 测试套件与 HEAD 基线对比，失败集一致（均为基线已存在的 semantics/FToaster 环境问题，变更前后均为相同失败，无新增失败）；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 退出码 0（today/medicine/mine 规则提示更新 Active_UI_*.md、health-context/search 为可选建议，均属提示项——本次为纯内部错误映射重构、无 UI 行为变化，未改 UI 文档）。未运行 build_runner（`@riverpod` 注解签名无变化，`.g.dart` 无需重新生成）；未运行 bootstrap_generated_sources.dart；未提交。

## 迁移 today repository 到 TaskEither（提交补充）

错误迁移 Task 3 的 today 领域独立提交（health_context 领域在上一提交落地），并在本提交补充两轮审查的修复结论。

- **today repository**：`TodayRepository.fetchDashboard`、`TodayAiRepository.read/refresh/generate`、`SuggestionRepository` 全部方法迁移到 `TaskEither<LucentFailure, T>`；`generateStream` 保持 Stream 事件语义（不包 TaskEither）；`signedOutDashboard` 为纯本地值保持 Future。`LucentTodayRepository` 的 degraded 降级（各子数据源失败）是产品行为，各 catch 点保留 talker 日志观测；`generate()` 当前无生产消费者，仅保留 API 兼容，流结束无结果按本地不变量映射 Left(unknown)（接口与实现均已注释说明）。
- **provider 消费**：`todayDashboard`、`todaySuggestion`（fetch/submitFeedback/explain）、`suggestionHistory`、`todayAiAnalysisController`（_fetch/refresh）改为 `.run()` + fold；AI 分析 403 仍映射 disabled 态，refresh 的 `FormatException` 显式投影为 AsyncError；widget 不导入 fpdart、不解析 code/status。
- **复审修复补充**（覆盖两个领域）：health_context `_enqueueWriteFailure` 增加内部 try/catch + talker 日志（入队/flush 自身失败不得掩盖原始网络失败 kind，仍按网络失败 Left）；补充三类测试——写路径缓存写失败（远端成功 + dao.replace 失败 → Left 且不入队）、suggestion 空成功体 → Left(unknown)、ai_analysis refresh FormatException → AsyncError。
- **测试**：`test/today/**` 全部适配 run()/Left/Right 断言，新增失败分支（AI read/refresh 网络失败与 403 code 保留、suggestion 404 code 保留 / 网络超时 / 非 problem body FormatException 传播、空成功体 unknown）；`test/auth/session_gate_test.dart` 的 today fake 随接口同步。

验证：`flutter test test/today` 全部通过；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 退出码 0（today 规则提示项未更新 Active_UI_Today.md，本次为纯内部错误映射重构、无 UI 行为变化）。未运行 build_runner 与 bootstrap_generated_sources.dart。

## 迁移 search repository 到 TaskEither

错误迁移 Task 4a：search 领域迁移到 fpdart `TaskEither<LucentFailure, T>`，沿用 Task 2/3 的 repository 边界模式（datasource 保持 Future 传输职责并抛 DioException，`LucentErrorMapper.fromObject` 为唯一映射入口，非 problem+json/畸形 body 的 `FormatException` 从 `.run()` 直接传播）。

- **repository 边界**：`MedicineSearchRepository.search/fetchDetail` 签名改为 `TaskEither<LucentFailure, T>`；`LucentMedicineSearchRepository` 用 `TaskEither.tryCatch` 包裹。识别/搜索候选为空是合法 Right（空列表不是错误）；服务端业务失败（如资源不存在 404）保留 Problem Details 的 code/status，不本地猜 status。
- **fetchDetail 吞错修复**：原 catch→null 把失败与无数据混为一谈；现 4xx/5xx（含资源不存在）为 Left 保留 Problem Details code，网络类为 Left(network)，成功响应为 Right（Right 类型保留可空语义，null 表示无预览数据、非错误）。
- **datasource**：`MedicineSearchRemoteDataSource` 保持 Future 传输并抛 DioException；空成功响应体按 auth `_requireBody` 先例抛 `LucentFailure.network(networkErrorCode: NetworkErrorCode.emptyResponse)`。`recent_searches.dart`（`RecentSearchesLocalPreferences`）为纯本地 SharedPreferences 偏好存储、无网络边界，保持 Future 不迁移（同 today `signedOutDashboard` 纯本地值先例）。
- **provider 消费**：`medicineSearchNotifier` 的 search/selectResult 改为 `.run()` + fold；search Left 投影到既有 errorMessage（`userMessageFromError`），detail Left 仅记录 talker 并清空预览、不使搜索失败；5s 超时保留在 `.run()` 返回的 Future 上（fpdart TaskEither 无 timeout，超时语义不变）。`recentSearchesProvider` 为 AsyncNotifier 本地偏好消费，不改。widget 层（`add_to_box.dart` 等）不导入 fpdart、不读 DioException、不解析 code/status。
- **被迫适配调用方**（仅接口签名变化的机械适配）：`test/search/**` 全部 fake 与 `integration_test/support/e2e_test_helpers.dart` 的 `MockMedicineSearchRepository` 同步 TaskEither 签名；`test/helpers/mocks.dart` 的 mocktail mock 声明无需改动。
- **测试**：`test/search/**` 仓库层断言改为 run()/Left/Right；新增失败分支——search/fetchDetail 404 Problem Details code/status 保留、网络超时 Left(network)、非 problem body → `FormatException` 从 `.run()` 传播、普通异常 → Left(unknown)；新增 datasource 空成功响应体 → emptyResponse 与 404/超时 DioException 传播测试；notifier 补充 search Left 投影 errorMessage、detail Left 仅降级预览不清空结果。

验证：`flutter test test/search` 通过——除 HEAD 基线已存在的页面 widget 测试失败（page_test 的加入药箱/预检/来源切换与 recent_searches_test 的时序类断言为 semantics/FToaster 环境问题，medicine_search_notifier_test 的 isSearching 时序测试在整包运行时偶发失败、单独运行通过），变更前后失败集与 HEAD 基线逐一相同、无新增失败；本次新增的 repository/datasource/notifier 失败分支测试全部通过；`flutter analyze` 无问题；`dart run scripts/check_doc_coverage.dart --warning-only` 退出码 0（search 规则提示项未更新 Active_UI_Medicine.md，本次为纯内部错误映射重构、无 UI 行为变化）；改动文件已 `dart format`。未运行 build_runner（`@riverpod` 注解签名无变化，`.g.dart` 无需重新生成）；未运行 bootstrap_generated_sources.dart；未提交。

审查修复（提交前）：detail 预览路径对协议异常（非 problem+json 错误体导致的 FormatException 从 run() 逃逸）增加防御——`_fetchDetailPreview` 捕获后记录 talker 并返回 null，不使搜索失败、不产生未处理异步异常（新增 notifier 测试锁定）；同时修正 repository 类注释中「畸形上游也成 Left」的不准确表述（实际为 FormatException 传播）。
