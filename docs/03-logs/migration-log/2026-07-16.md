# Migration Log - 2026-07-16

## Luminous — 代码审查修复（SSE 错误映射去重 + 测试断言修复）

- **目标**：根据 `plans/2026-07-16-code-review-fixes.md` 修复 2 个遗留审查问题。
- **验证结果**：`flutter analyze` 零问题，`flutter test` 全量 2781 tests passed。
- **变更内容**：

### #1 SSE 错误映射逻辑提取到公共模块（🔴 严重）

- `lib/core/network/map_utils.dart`：新增 `requireMap(Object? data)` 和 `mapSseStreamError(Object? data)` 两个公共函数，复用已有的 `coerceToStringMap` 工具函数，消除三处 datasource 中逐字重复的私有方法。新增 `api_exception.dart` import。
- `lib/features/today/data/datasources/ai_remote.dart`：删除 `_requireMap` 和 `_mapStreamError` 私有方法，import 从 `api_exception.dart` 改为 `map_utils.dart`，调用替换为 `requireMap()` / `mapSseStreamError()`。
- `lib/features/report/data/datasources/ai_summary_remote.dart`：同上。
- `lib/features/assistant/data/datasources/assistant.dart`：同上，`_readMapList` 内部的 `_requireMap` 调用也同步替换为 `requireMap()`。

### #2 测试断言使用硬编码 ARB key 而非本地化文本（🟡 警告）

- `test/record/widgets/voice_entry_dialog_test.dart`：`closes sheet when X button tapped` 测试中 `find.text('recordVoiceEntryTitle')` 改为 `find.text(l10n.recordVoiceEntryTitle)`，并在测试方法开头添加 `final l10n = await AppLocalizations.delegate.load(const Locale('zh'));`，与同文件其他测试用例保持一致。修复后断言能正确验证 sheet 关闭逻辑。

## Luminous — SemanticColor 暗色对比度 + Drift 缓存一致性

- **目标**：完成审查遗留的暗色模式对比度验证和缓存一致性改进。
- **变更内容**：

### #1 SemanticColor 暗色对比度验证

- `lib/core/theme/theme.dart`：`_fixedPalette` 暗色模式 `subtle` alpha 从 0.08 提升至 0.10，确保空状态背景在深色背景上有足够区分度。`muted`（0.18）和 `border`（0.35）经 WCAG 2.1 分析验证通过——`solid` 在 `muted` 背景上对比度 >7:1（超 AAA）。完整 WCAG 分析表格和各色调的设计依据文档化在 `_fixedPalette` 的 dartdoc 注释中。

### #2 缓存常量统一

- 新建 `lib/core/database/cache_constants.dart`：统一定义所有时间常量——`backgroundRefreshThrottle`（30s）、`networkTimeoutShort`（5s）、`defaultMaxRetry`（5）、`syncBackoffBase`（30s）、`syncBackoffMax`（30min）。包含 library-level dartdoc 说明缓存策略（cache-first，无 TTL 过期，cachedAt 仅供 cleanup 使用）和同步策略（指数退避，permanently failed 后 UI 通知）。
- `lib/core/database/daos/pending_sync_dao.dart`：`PendingSyncEntry.backoffDelay` 从硬编码 `30` 和 `1800` 改为引用 `syncBackoffBase.inSeconds` 和 `syncBackoffMax.inSeconds`。
- `lib/features/health_context/data/repositories/lucent.dart`：`_refreshInBackground` 中的 `Duration(seconds: 30)` 改为引用 `backgroundRefreshThrottle`，删除 `// ignore_for_file: prefer_initializing_formals` 注释。

### #3 SyncWorker 失败 UI 通知

- `lib/core/database/daos/pending_sync_dao.dart`：新增 `permanentlyFailedCount()` 方法，查询 `retryCount >= maxRetry` 的永久失败项数量。
- `lib/core/database/sync/sync_worker.dart`：新增 `syncFailedCountProvider`（`@Riverpod(keepAlive: true)`），暴露永久失败计数给 UI 层。
- 新建 `lib/features/mine/presentation/widgets/sections/sync_failed_banner.dart`：`MineSyncFailedBanner`（`ConsumerWidget`），监听 `syncFailedCountProvider`，当 count > 0 时展示 warning 色警告横幅（`SemanticColor.warning` 的 subtle 背景 + border + `cloudAlert` 图标 + 失败项数量消息 + 「查看详情」操作按钮）。点击触发 `SyncWorker.flush()` 重试。
- `lib/features/mine/presentation/widgets/views/dashboard_view.dart`：mobile 和 desktop 布局顶部均插入 `MineSyncFailedBanner`。
- `lib/l10n/src/mine_zh.arb` / `mine_en.arb`：新增 `mineSyncFailedWarning`（带 `{count}` 占位符）和 `mineSyncFailedAction` 两个 l10n key。

### #4 多设备冲突策略文档化

- `docs/00-current/Current_State.md`：新增变更记录，明确 last-write-wins 行为——服务端时间戳为权威，客户端缓存为读取优化，离线写入通过 pending sync queue 最终一致化。
- `docs/00-current/TODO.md`：删除已完成的 SemanticColor 暗色对比度验证、TTL 常量统一、SyncWorker UI 通知、多设备冲突策略文档化四项 TODO。

