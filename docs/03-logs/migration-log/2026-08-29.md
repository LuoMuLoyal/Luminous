# 2026-08-29 迁移日志

## 代码审查修复（基于 08-28 审查文档）

### ShellPage Column 布局修复
- `lib/features/shell/presentation/page.dart`: 移除 `mainAxisSize: MainAxisSize.min`，
  让 Column 使用默认 `MainAxisSize.max`，与 `Expanded` 子组件协调一致，
  避免 debug 构建中的 RenderFlex 警告和 release 下 content 区域被压缩。

### 数据库 schema v5 迁移
- `lib/core/database/database.dart`: schemaVersion 从 4 提升到 5，
  新增 `if (from < 5)` 迁移步骤调用 `m.renameTable()` 将
  `report_dashboard_cache_entries` 重命名为 `review_dashboard_cache_entries`，
  修复已升级到 v4 的用户因表名不匹配导致缓存查询静默失效。

### LucideIconBridge 反查性能优化
- `scripts/generate_lucide_bridge.dart` + `lib/core/design/lucide_icon_bridge.dart`:
  新增 `static final _reverseMap` 预计算反查 Map，`nameOf()` 从 O(N) 线性扫描
  优化为 O(1) Map 查找。重新生成了 bridge 文件（2030 entries）。

### icon picker 国际化修复
- `lib/core/widgets/common/icon_picker_sheet.dart`: 将硬编码的中文分类标签
  和英文搜索提示改为通过 `AppLocalizations` 获取。
- 新增 `IconPickerCategory` 枚举，每个值提供 `label(l10n)` 方法。
- 新增 l10n key: `iconPickerCategory{Food,Health,Status,Body,General}`、
  `iconPickerSearchHint`、`iconPickerEmpty`。
  - 修改文件: `lib/l10n/src/common_zh.arb`、`lib/l10n/src/common_en.arb`。

### SoftIcon 类型校验修复
- `lib/core/widgets/common/soft_icon.dart`: 将 `else` 分支的 `icon as IconData`
  强制 cast 替换为 `else if (icon is IconData)` 类型检查 + assert 显式失败路径，
  当传入类型不匹配时不再抛 TypeError，而是触发 assert 并渲染 fallback 图标。
- 同文件修复 `flutter analyze` error：`icon` 字段类型为 `Object`，Dart 对
  实例字段不做类型提升（type promotion），`else if (icon is IconData)` 分支内
  `icon` 仍为 `Object` 类型。改为 `final IconData iconData = icon as IconData`
  显式声明局部变量类型，使 `Icon` 构造函数接收正确类型参数。

### FakeOcrModelManager 注释修正
- `test/helpers/mocks/scan.dart`: 修正 `modelsAvailable` 字段文档注释，
  原注释称 true 时返回 false，实际返回 true，含义完全相反。

### OCR 模块日志与进度修复
- `lib/features/scan/presentation/pages/box_scan.dart`: 两处 `catch (e)` 改为
  `catch (e, st)`，并在 `appTalker.error`/`appTalker.warning` 中传入 stack trace。
- `lib/features/scan/domain/services/ocr_model_manager.dart`: 抽取
  `final totalSteps = downloads.length + 1` 命名清晰，`onProgress?.call(1.0)`
  注释标注为仅在 config 拷贝成功后回调。
- `lib/features/scan/domain/services/paddle_ocr_provider.dart`:
  将 `catchError` 替换为 `async try/catch`，记录 stack trace 而非静默吞错。

## 代码审查修复第二弹（review-suggestion 项）

### IconSizeTokens.level5 调用点迁移（review-c2）
- `lib/features/shell/presentation/page.dart`: 桌面端侧边栏用户头像
  `FAvatar.raw` 的 size 从 `level5`(28px) 改为 `level6`(32px)，
  因为 avatar 类组件在重构前使用的是旧 `level5`=32px，重构后
  `level5` 变为 28px，需迁移到 `level6` 以保持视觉一致性。
  另一处 `level5` 调用（`suggestion.dart` suggestion card icon）
  28px 符合新定义，无需改动。

### ConnectivityBanner Forui-first 改造（review-s2）
- `lib/core/widgets/common/connectivity_banner.dart`: 将 `Material` 容器
  替换为 `ColoredBox`，消除对 `flutter/material.dart` 中 `Material` widget
  的依赖。文字样式从 `Theme.of(context).textTheme` 改为 Forui 的
  `context.theme.typography.body.sm`，保持 Forui-first 设计系统一致性。

### AuthSessionState 扩展 isReconnecting 字段（review-s4）
- `lib/core/auth/session_state.dart`: 新增 `isReconnecting` 字段，用于区分
  "首次冷启动恢复"（`isLoading=true`）与"超时后重试"（`isReconnecting=true`）。
  `isTimeout` 和 `isReconnecting` 均补充了文档注释。
- `lib/core/auth/session_provider.dart`: `restore()` 方法在检测到之前是
  timeout 状态时设置 `isReconnecting=true`。
- 重新生成了 `session_state.freezed.dart`。

### ConnectivityBanner widget test（review-s5）
- 新增 `test/core/widgets/connectivity_banner_test.dart`，覆盖三个场景:
  (a) 非 timeout 状态不渲染 banner;
  (b) timeout 状态渲染 banner 并显示正确的超时文案和 Retry 按钮;
  (c) 点击 Retry 按钮触发 `restore()` 调用。

### icon_picker_sheet 可扩展配置（review-w6）
- `lib/core/widgets/common/icon_picker_sheet.dart`: 将 `_curatedIcons` 常量
  重命名为 `defaultIconPickerCategories` 并改为 public，`showAppIconPicker`
  新增可选参数 `categories` 允许调用方注入自定义图标集（如远程配置），
  默认使用内置精选集。移除 TODO 注释。

### pubspec.yaml 模型 manifest 版本注释（review-s6）
- `pubspec.yaml`: 在 `paddle_ocr_native` override 注释中补充
  "模型 manifest 版本: v0.1.1-models (GitHub release tag)" 信息。
  CDN URL 迁移标注保留在 `ocr_model_manager.dart` 中（属 C-1 范围）。

## R-4 #21: legacy 图表改按 observedMetric 口径输出

### 后端改动（Lucent）

- `src/modules/reports/dto/report-dashboard-response.dto.ts`:
  `ReportTrendDto` 新增可选 `observedMetric: ReportObservedMetricDto` 字段，
  `values` 字段 description 标注 "Observed values only — unknown days are omitted, not zero-filled."
- `src/modules/reports/dashboard/computation.service.ts`:
  新增 `buildTrend()` 方法，从 observedSeries 提取 observed-only values（unknown 天省略不画），
  附带 `observedMetric` 汇总（observedCount/expectedCount/coverage/state）。
  medication/water trend 走 observed 路径；sleep 无 observedSleepSeries 走 scalar fallback。
- `src/modules/reports/dashboard/context.service.ts`:
  更新 medication/water scalar series 注释，明确 scalar 仅用于 metric sparkline 和 presenter，
  trend values 在 computation 中从 observedSeries 直接提取。
- `docs/openapi.json`: 重新导出，`ReportTrendDto` 包含 `observedMetric` 引用。
- `src/modules/reports/dashboard/computation.service.spec.ts`:
  更新 trend 断言为 `objectContaining`，新增 observedMetric 覆盖率断言。

### 客户端改动（Luminous）

- `generated/lucent_api/`: 重新生成 API client，`ReportTrendDto` 包含 `observedMetric` 字段。
- `lib/features/review/domain/entities/dashboard.dart`:
  `ReviewTrendSeries` 新增可选 `ReviewObservedMetric? observedMetric` 字段。
- `lib/features/review/data/repositories/lucent_dashboard.dart`:
  `_mapTrend` 映射 `dto.observedMetric` 到领域实体。
- `lib/features/review/presentation/widgets/sections/trend.dart`:
  - 移除 `values.every((v) => v == 0)` 空数据判断（unknown 天不再是 0，而是被省略）。
  - 空数据判断改为 `values.isEmpty`（observed-only values 为空即无记录）。
  - 图表标题行右侧新增覆盖率标注「有记录 N 天 / 范围 M 天」。
  - `_allSeriesEmpty` 辅助函数同步移除 0 值判断。
- `lib/l10n/src/review_zh.arb`、`review_en.arb`: 新增 `reviewTrendCoverage` l10n key。
