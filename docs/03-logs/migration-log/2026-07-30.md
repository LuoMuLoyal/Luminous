# 2026-07-30 Health Data Integration

## Lucent (Backend)

### Daily Records — vital / activity payload + source field

- `src/modules/daily-records/schemas/daily-record-candidates.schema.ts`
  - Added `vitalPayloadSchema` and `activityPayloadSchema` Zod schemas.
- `src/modules/daily-records/dto/create-record.dto.ts`
  - Added optional `source` field (`@IsString`, `@MaxLength(50)`, default `"manual"`).
  - Updated `payload` ApiProperty description to document vital/activity payload shapes.
- `src/modules/daily-records/dto/record-item.dto.ts`
  - Updated `payload` ApiProperty description to include vital/activity payload shapes.
- `src/modules/daily-records/services/records.service.ts`
  - `create()`: Added `ensureValidVitalPayload()` and `ensureValidActivityPayload()` calls.
  - `create()`: Added `source: dto.source ?? 'manual'` to `baseData`.
  - Added `ensureValidVitalPayload()` — validates `payload.vitalType` (string) and `payload.value` (number) for vital records.
  - Added `ensureValidActivityPayload()` — validates `payload.activityType` (string) and `payload.value` (number) for activity records.
- `src/modules/daily-records/services/records.service.spec.ts`
  - Added 8 tests for vital records (valid payload, no payload, missing vitalType, missing value).
  - Added 8 tests for activity records (valid payload, no payload, missing activityType, missing value).
- `docs/openapi.json`
  - Regenerated to reflect new `source` field and updated DTO descriptions.

## Luminous (Frontend)

### New `health_data` feature

- **Domain layer** (`lib/features/health_data/domain/`):
  - `entities/health_metric.dart` — `HealthMetric` freezed model + `HealthMetricType` enum (13 types).
  - `entities/health_permission.dart` — `HealthPermissionStatus` enum.
  - `entities/health_sync_result.dart` — `HealthSyncResult` freezed model.
  - `repositories/health_sync.dart` — `HealthSyncRepository` abstract interface.

- **Data layer** (`lib/features/health_data/data/`):
  - `datasources/health_platform.dart` — wraps `health` plugin (permissions, fetch, dedup).
  - `mappers/health_record_mapper.dart` — maps `HealthDataPoint` → `HealthMetric` (with sleep merging + BP pairing) → `DailyRecordCreateInput`.
  - `repositories/health_sync.dart` — `HealthSyncRepositoryImpl` with dedup by `(kind, occurredAt, source)`.
  - `providers/health_sync.dart` — Riverpod providers for datasource, mapper, repository.

- **Presentation layer** (`lib/features/health_data/presentation/`):
  - `providers/health_sync.dart` — `HealthSyncState` freezed state + `HealthSyncTimeRange` enum.
  - `providers/health_sync_controller.dart` — `HealthSyncController` Notifier (toggle types, set range, request permissions, fetch, sync).
  - `providers/health_auto_sync.dart` — `HealthAutoSyncPreference` local SharedPreferences toggle.
  - `pages/health_sync.dart` — Full page UI with metric type selection, time range, preview, sync result.
  - `routes.dart` — `HealthSyncRoute` at `/health-sync`.

### Record feature changes

- `domain/entities/inputs.dart` — Added `source` field to `DailyRecordCreateInput`.
- `domain/entities/type_mapping.dart` — Map `RecordEntryType.heartRate` / `weight` → `DailyRecordKind.vital`.
- `data/datasources/record.dart` — Pass `source` field in API payload.
- `data/repositories/lucent.dart` — `_isActiveRecordEntryType` returns `true` for `vitals` and `activity`.
- `presentation/widgets/forms/form_fields.dart` — Added `vital` and `activity` to `activeDailyRecordKinds`.

### Routing

- `lib/app/router.dart` — Added `health_data_routes` import, `Routes.healthSync` constant, and `$appRoutes` spread.

### Mine page

- `lib/features/mine/presentation/widgets/sections/account_security.dart` — Added "从健康 App 导入" tile (mobile-only).

### Settings

- `lib/features/settings/presentation/widgets/privacy_section.dart` — Added health auto-sync toggle.
- `lib/core/config/pref_keys.dart` — Added `healthAutoSyncEnabled` key.

### Platform configuration

- `pubspec.yaml` — Added `health: ^13.3.1` dependency + `dependency_overrides: device_info_plus: ^13.0.0`.
- `ios/Runner/Info.plist` — Added `NSHealthShareUsageDescription`.
- `android/app/src/main/AndroidManifest.xml` — Added Health Connect permissions + package visibility.
- `android/app/build.gradle.kts` — minSdk already 26 (no change needed).
- `scripts/arb_tools.dart` — Added `health_sync` to `fragmentRules`.

### L10n

- `lib/l10n/src/health_sync_zh.arb` — 32 new zh strings.
- `lib/l10n/src/health_sync_en.arb` — 32 new en strings.
- `lib/l10n/src/settings_zh.arb` — Added `settingsHealthAutoSyncTitle` / `settingsHealthAutoSyncSubtitle`.
- `lib/l10n/src/settings_en.arb` — Same keys in English.
