# 2026-07-30 迁移日志

## Scan 模块 — 07-30 审查问题修复

### 修复 1：PaddleOcrEngine 初始化失败后状态不一致

- **文件**: `lib/features/scan/domain/services/paddle_ocr_provider.dart`
- **变更**: 提取 `ensureInitialized()` 公共方法，init 失败时 `_initialized` 重置为 `false` 并 rethrow，允许下次调用重试。`recognize()` 改为调用 `ensureInitialized()` 而非内联 init。
- **原因**: ONNX 模型加载失败（存储空间不足、模型文件损坏、so 库加载失败）时，`_initialized` 保持 `true` 导致引擎卡在不一致状态，当前应用生命周期内 OCR 功能永久不可用，必须杀进程重启。

### 修复 2：OCR 入口 ABI 预检

- **文件**: `lib/features/scan/presentation/pages/box_scan.dart`
- **变更**: 用户选择 OCR 方式后、打开相机前，调用 `ensureInitialized()` 预检。init 失败时显示 `_showOcrUnavailableDialog` 对话框，提供关闭和"使用 AI 识别"两个操作。
- **原因**: Android `abiFilters` 仅打包 `arm64-v8a`，非 arm64 设备（模拟器、32 位 ARM）安装后运行正常但进入 OCR 扫描时 native 库加载失败。原流程让用户拍照后才发现不可用，体验差。
- **新增 l10n 键**: `scanOcrUnavailableTitle` / `scanOcrUnavailableMessage` / `scanOcrUnavailableUseAi`（`medicine_zh.arb` / `medicine_en.arb`）

### 修复 3：批准号正则精确化

- **文件**: `lib/features/scan/domain/services/medicine_ocr_extractor.dart`
- **变更**: `_approvalNumberPattern` 中 `\w{7,9}` 改为 `[A-Za-z0-9]{7,9}`，排除下划线。
- **原因**: `\w` 匹配 `[a-zA-Z0-9_]`，国药准字批准号格式为字母+数字，不应包含下划线。虽然实际 OCR 结果中极难出现下划线，但正则语义不精确。

### 新增微博 + Google OAuth 登录入口

根据 `plans/2026-07-29-oauth-weibo-google.md` 计划，为 Luminous 新增微博和 Google 两个 OAuth 登录入口。

**前端变更**：
- `lib/features/auth/domain/repositories/auth.dart`：接口新增 `createWeiboAuthorizeUrl` / `loginWithWeibo` / `createGoogleAuthorizeUrl` / `loginWithGoogle` 四个方法
- `lib/features/auth/data/datasources/auth.dart`：实现四个方法，调用 generated API 客户端
- `lib/features/auth/presentation/providers/oauth_login.dart`：`OAuthLoginState` 新增微博和 Google 的状态字段（isStarting/isCompleting/authorizeUrl/state），`OAuthLoginController` 新增 `startWeiboLogin` / `completeWeiboLogin` / `startGoogleLogin` / `completeGoogleLogin` 方法
- `lib/features/auth/presentation/widgets/shared/oauth_panels.dart`：`OAuthBrandColors` 新增微博红 `#E6162D` 和 Google 蓝 `#4285F4`；`OAuthButtonRow` 新增微博和 Google 的圆形品牌按钮及回调输入框
- `lib/features/auth/presentation/pages/login.dart`：新增 `weiboCode/weiboState/googleCode/googleState` 构造函数参数、`weiboCallbackController/googleCallbackController`、`webWeiboCallbackUri()/webGoogleCallbackUri()` 方法、微博和 Google 的 start/complete action handlers、深链接回调处理
- `lib/features/auth/presentation/routes.dart`：新增 `LoginOauthWeiboRoute` 和 `LoginOauthGoogleRoute` 路由
- `lib/app/router.dart`：新增 `loginOauthWeibo` 和 `loginOauthGoogle` 路由常量
- `lib/l10n/src/auth_zh.arb` + `auth_en.arb`：新增微博和 Google 的 l10n 键（`authWeibo*` / `authGoogle*` / `authIdentityProviderWeibo` / `authIdentityProviderGoogle`）
- `assets/icon/oauth/weibo.svg` + `google.svg`：品牌图标资源
- `test/auth/oauth_panels_test.dart`：新增微博和 Google 按钮的 widget 测试

### 验证

- `flutter analyze`: 0 error, 0 warning
- `flutter test test/auth/`: 244 passed, 0 failed
- `dart scripts/arb_tools.dart merge` + `flutter gen-l10n`: 成功
- `dart run build_runner build`: 成功

## 记录页 — elk_icon_picker 图标选择器集成

根据 `plans/2026-07-30-elk-icon-picker-integration.md` 计划，引入 `elk_icon_picker` 作为用户图标选择器 UI，通过 bridge map 将 kebab-case 图标名转换为 Forui `FLucideIcons` 的 `IconData`，实现记录页快捷入口图标的用户自定义。

**新增文件**：
- `scripts/generate_lucide_bridge.dart` — 解析 `forui_assets` 的 `assets.g.dart`，生成 bridge map（1991 条）
- `lib/core/design/lucide_icon_bridge.dart` — **生成文件**，`Map<String, IconData>` 映射表 + `resolve()` / `nameOf()` / `exists()` 方法
- `lib/core/widgets/common/icon_picker_sheet.dart` — 封装 `showElkIconPicker`，注入 Forui 主题色，返回 `IconData?`

**修改文件**：
- `lib/core/config/pref_keys.dart` — 新增 `recordQuickEntryCustomIcons` key
- `lib/features/record/data/quick_entry_preferences.dart` — `QuickEntryPreferences` 新增 `customIcons` 字段（`Map<String, String>`），controller 新增 `setCustomIcon` / `resetCustomIcon` / `resetAllCustomIcons` 方法
- `lib/features/record/presentation/widgets/sections/quick_entry_panel.dart` — `_applyPreferences` 中应用自定义图标覆盖；`_QuickRecordTile` 新增 `onLongPress` 回调，长按弹出图标选择器
- `lib/features/record/presentation/pages/quick_entry_settings.dart` — 新增「自定义图标」section，含提示文案
- `lib/l10n/src/record_zh.arb` + `record_en.arb` — 新增 `recordQuickSettingsIcons` / `recordQuickSettingsCustomIconTitle` / `recordQuickSettingsCustomIconHint` 三条 l10n 键

**验证**：
- `flutter analyze`: 0 issues found
- `flutter test`: 2650 passed, 7 pre-existing failures（均在不相关模块：auth form mixin / layout tokens / user settings controller）
- `dart scripts/generate_lucide_bridge.dart`: 生成 1991 条映射
- `dart scripts/arb_tools.dart merge` + `flutter gen-l10n`: 成功
