# Migration Log - 2026-07-21

## Luminous - Today compact summary cleanup

- **Files**:
  - `lib/features/today/presentation/widgets/views/dashboard_view.dart`
  - `lib/features/today/presentation/widgets/sections/summary.dart`
  - `lib/l10n/src/today_zh.arb`
  - `lib/l10n/src/today_en.arb`
  - `docs/00-current/Active_UI_Today.md`
  - `docs/02-reference/Localization.md`
- **Change**: Removed the standalone record hint from Today, compacted the summary card, and renamed the summary title to `Today summary` / `今日摘要`.
- **Notes**: The deleted record-hint strings were removed from the Today l10n fragments so they do not survive the next l10n merge.
- **Data source**: The summary metrics and five quick actions are frontend-derived from `TodayDashboard` and localized route definitions; they are not a backend-returned UI array. Today AI narrative content remains an on-demand SSE result.
- **Test fix**: The generated-summary widget test now scrolls the actual off-screen action button into view before tapping it.
- **Cleanup**: Removed the unused `shouldShowRecordHint` helper and its tests with the deleted record hint.

## Luminous — 审查报告修复：5 项问题（1🔴 + 4🟡）

- **来源**：`plans/luminous-review-2026-07-21.md`（基于 2026-07-20 审查报告回查）。
- **验证方式**：`flutter analyze` 零问题，`flutter test` 2756 个测试全部通过。

### 1. 🔴 公共分享页错误提示误导用户

- **文件**：`lib/features/report/presentation/pages/clinic_summary_shared.dart`
- **修复**：错误回调从统一显示"链接已过期或失效"改为使用 `LucentErrorMapper.toAppError(error)` 区分错误类型。网络错误（`AppErrorKind.network`）显示"网络连接失败，请检查网络后重试"+ `wifiOff` 图标 + 重试按钮（`ref.invalidate` 重新加载）；其他错误（含 404/410 链接失效）仍显示"链接已过期或失效" + `triangleAlert` 图标，无重试按钮。

### 2. 🟡 PDF 下载逻辑提取为公共工具

- **新增文件**：`lib/features/report/presentation/utils/pdf_download.dart`
- **修复**：提取 `downloadAndSharePdf()` 函数和 `PdfDownloadResult` 枚举，封装 Dio 二进制下载 → 判空 → 写临时文件 → SharePlus 分享的完整流程。`clinic_summary_shared.dart` 和 `clinic_summary_preview_dialog.dart` 的 `_downloadPdf` 方法均改为调用此函数，消除重复代码。差异点（endpoint、文件名前缀、分享标题、是否跳过认证）通过参数传入。

### 3. 🟡 通知列表 hover 状态改用 ValueNotifier

- **文件**：`lib/features/notification/presentation/widgets/shared/list_item.dart`
- **修复**：`_isHovered` 从 `bool` + `setState` 改为 `ValueNotifier<bool>` + `ValueListenableBuilder`。`MouseRegion` 的 `onEnter`/`onExit` 直接设置 `_isHovered.value`，hover 操作按钮区域用 `ValueListenableBuilder` 局部重建，不再触发整个列表项 widget 的 `build`。`dispose` 中调用 `_isHovered.dispose()`。

### 4. 🟡 `_formatDateTime` / `_MetaRow` 提取为公共组件

- **修改文件**：
  - `lib/core/utils/date_format_utils.dart`：新增 `formatDateTimeFull()` 函数（`yMd` + `Hm` 格式）。
  - `lib/features/report/presentation/widgets/shared/components.dart`：新增 `MetaRow` widget（80px 固定 label 宽度的横向键值行）。
  - `lib/features/report/presentation/widgets/shared/clinic_summary_content.dart`：移除私有 `_formatDateTime` 和 `_MetaRow`，改用 `formatDateTimeFull` + `MetaRow`。
  - `lib/features/report/presentation/widgets/dialogs/suggestion_history_detail_sheet.dart`：同上，移除私有重复定义。

### 5. 🟡 分享失败错误消息拼接格式不一致

- **文件**：`lib/features/report/presentation/widgets/dialogs/clinic_summary_preview_dialog.dart`
- **修复**：硬拼接的 `'${l10n.reportExportFailedToast}: ${error.message}'` 改为 l10n 参数化字符串 `l10n.reportExportFailedWithReason(error.message)`，中文显示"导出失败：xxx"，英文显示"Export failed: xxx"，冒号格式由各语言的 ARB 模板控制。

### 6. 登录/注册页 Send Code 失败提示 + 未登录启动进入预览页

- **文件**：
  - `lib/features/auth/presentation/pages/login.dart`
  - `lib/features/auth/presentation/pages/register.dart`
  - `lib/features/auth/presentation/pages/forgot_password.dart`
  - `lib/app/router.dart`
- **修复**：
  - 三个页面的验证码发送按钮在 `sendCode`/`sendResetCode` 返回 `false` 时，使用 `ref.read(...)` 读取最新的 `errorMessage` 并通过 `AppToast.show` 展示，避免后台报错但前端无提示。
  - 路由守卫从"未登录直接跳 `/login`"改为"未登录可访问主 tab 预览页及 `/settings`、`/assistant` 等公开页"，用户打开应用后先看预览内容，再自行决定是否登录。
- **验证**：`flutter analyze` 无问题。

- `reportClinicSummarySharedNetworkError`（"网络连接失败，请检查网络后重试" / "Network connection failed, please check your network and try again"，report_zh / report_en）
- `reportExportFailedWithReason`（"导出失败：{reason}" / "Export failed: {reason}"，report_zh / report_en，带 `{reason}` placeholder）

### 新增 l10n 键

## Luminous — Today 页摘要视觉与饮水目标优化

- **Files**:
  - `lib/features/today/presentation/widgets/sections/summary.dart`
  - `lib/features/today/data/repositories/lucent.dart`
  - `lib/features/today/domain/entities/dashboard.dart`
  - `lib/features/today/presentation/providers/dashboard.dart`
  - `lib/core/providers/data_change_bus.dart`
  - `lib/features/settings/presentation/providers/user_settings.dart`
  - `test/today/repository_test.dart`
  - `docs/02-reference/state-management.md`
  - `docs/00-current/Active_UI_Mine_Settings.md`
- **Change**:
  - 优化 `TodaySummarySection` 三个指标的字体与布局：图标从 14px 放大到 20px，label 从 10px 提升到 12px，数值从 14px 提升到 16px；图标与文字改为垂直居中对齐，列间距从 6px 增加到 10px，使整体比例更协调。
  - 饮水目标 `8` 不再硬编码：仓库从 `UserSettingsRepository` 读取 `waterTargetCount`；未登录/无设置/接口失败时回退到 `TodayDashboard.defaultWaterTargetCount`（0），空态显示 `0/0` 而不是假目标。
  - 修复 `TodayWaterSummary.progress`：当 `targetCount <= 0` 时返回 `0.0`，避免除零得到 NaN/Infinity 导致进度条异常。
  - 新增 `DataChangeTopic.userSettings`，`UserSettingsController` 成功写入设置后发射该事件；`todayDashboardProvider` 监听它，确保修改饮水目标后今日页自动刷新。
- **Notes**: 快捷动作（Quick actions）当前仍是 `buildQuickActionItems` 返回的固定 5 条：确认用药、快速记录、用药安全、提醒设置、健康档案。
- **验证**：`flutter analyze` 无问题，`flutter test test/today test/settings` 全部通过。

## Luminous — Record 页快速记录区补齐与视觉优化

- **Files**:
  - `lib/features/record/domain/entities/dashboard.dart`
  - `lib/features/record/data/repositories/lucent.dart`
  - `lib/features/record/presentation/widgets/sections/quick_entry_panel.dart`
  - `lib/features/record/presentation/widgets/shared/dashboard_tokens.dart`
  - `test/record/domain/entities/dashboard_entity_test.dart`
  - `test/record/dashboard_entity_test.dart`
  - `docs/00-current/Active_UI_Record.md`
- **Change**:
  - 移动端 `Quick record` 未登录/静态 dashboard 下从 5 个补齐到 7 个，新增 Medication / Mood；默认顺序统一为 Symptom、Medication、Water、Meal、Sleep、Mood、Note。
  - 静态 quick actions / filters 图标统一为 `briefcaseMedical` / `pill` / `droplets` / `utensils` / `moon` / `smile` / `notebookPen`；登录态 `softColor` 从 `primary` 改为 `neutral`，避免图标与背景同色。
  - `quick_entry_panel.dart` 的 `FAvatar` 背景从 `softColor.solid(context)` 改为 `softColor.subtle(context)`，去掉过深的圆形底色；头像容器尺寸放大，内部图标从 14px 级放大到约 20px。
  - `dashboard_tokens.dart` 的默认顺序与移动端筛选 preferred order 同步加入 `mood`。
- **Notes**: Medication 在快速记录中仍为"非创建型"入口（与 Today 一致），点击后的行为由上层回调决定；快捷入口显示数量上限保持 6 个 grid + Note 独立按钮。
- **验证**：`flutter analyze` 无问题，`flutter test test/record` 全部通过。

## Luminous — Record 页快速记录区补齐与视觉优化（TDD）

- **Files**:
  - `lib/features/record/domain/entities/dashboard.dart`
  - `lib/features/record/data/repositories/lucent.dart`
  - `lib/features/record/presentation/widgets/sections/quick_entry_panel.dart`
  - `lib/features/record/presentation/widgets/shared/dashboard_tokens.dart`
  - `test/record/presentation/widgets/sections/quick_entry_panel_test.dart`
  - `test/record/domain/entities/dashboard_entity_test.dart`
  - `test/record/dashboard_entity_test.dart`
  - `docs/00-current/Active_UI_Record.md`
- **TDD**:
  1. 回滚到原始状态（未登录 dashboard 只有 5 个 quick actions、Note 为灰色 `FButton`）。
  2. 新增 widget 测试断言：未登录 dashboard 有 7 个 quick actions；`RecordQuickEntryPanel` 渲染 6 个网格 tile + 1 个独立 Note tile；Note tile 不含 `FButton` 且含 `FAvatar`。
  3. 运行测试确认 3 个全红。
  4. 实现：补齐 Medication / Mood、把 Note 改为和网格 tile 一致的 `FAvatar` 圆形头像样式、保持 6+1 布局。
- **Change**:
  - 未登录/静态 dashboard 的 quick actions / filters 从 5 个补齐到 7 个。
  - 移动端默认顺序与筛选 preferred order 同步加入 `medication` 和 `mood`。
  - Note 不再使用灰色 `FButton`，改为圆形 `FAvatar` + label，与上面 6 个入口风格一致；Note 仍独占底部一行。
  - 图标/背景尺寸保持上一轮调整：`softColor.subtle(context)`、头像容器约 28–36px、图标约 20px。
- **验证**：`flutter analyze` 无问题，`flutter test test/record` 473 个测试全部通过。

## Luminous — Record 页 Note 入口改为横向低高度样式

- **Files**:
  - `lib/features/record/presentation/widgets/sections/quick_entry_panel.dart`
  - `test/record/presentation/widgets/sections/quick_entry_panel_test.dart`
  - `docs/00-current/Active_UI_Record.md`
- **TDD**:
  1. 更新 widget 测试，断言 Note tile 使用 `Row` 横向布局、不含 `Column`、不含 `FButton`。
  2. 运行测试确认红（当前为上下 Column 布局）。
  3. 实现：`_QuickRecordNoteButton` 从 `Column` 改为 `Row`，图标在左、文字在右；垂直 padding 从 `metrics.notePadding` 改为 `Spacing.level2`，水平 padding 保持 `Spacing.level4`。
  4. 测试绿。
- **Change**: 底部 Note 入口高度降低，视觉从“大按钮”变成紧凑的左图标右文字条，与上方圆形头像风格一致但横向排列。
- **验证**：`flutter analyze` 无问题，`flutter test test/record` 473 个测试全部通过。
