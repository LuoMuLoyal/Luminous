# Migration Log — 2026-08-31

## 目录结构整理(批次 1c 起)日志位置说明

- `plans/2026-08-30-directory-structure-cleanup.md` 的批次 1a/1b 实际执行于 8 月 30 日晚,其条目记录于 `2026-08-30.md`,位置正确。
- 批次 1c 起实际执行于本日;因该计划条目此前沿用 `2026-08-30.md` 追加(迁移日志只追加不回改),批次 1c/1d/2/3 的条目仍在该文件内,时间线以各条目内 commit hash 为准。自本日起,本计划的后续批次条目记录于本文件。

## 目录结构整理批次 4:core/network 分组

- 范围:`lib/core/network/` 直下裸文件按职责分组,目标 network/ 直下裸文件 ≤5(达成,余 2)。
- 分组清单(git mv 保历史):
  - `client/`(HTTP 执行栈):`dio_client.dart`、`client_providers.dart`、`session_store.dart`、`sse.dart`、`base_url.dart`、`retry_policy.dart`、`trace_context.dart`,及 `interceptors/` 子目录整体平移(auth/error/retry/trace 4 文件)。`client_providers.g.dart` 为 gitignored 生成物,随源文件平移(非 git mv),`part` 声明同目录继续有效。
  - `contract/`(响应与错误合同):`response_body.dart`、`problem_details.dart`、`error_code.dart`、`result_code.dart`、`error_mapper.dart`、`api_paths.dart`。
  - 根保留:`api.dart`(barrel 留根,仅更新 export 路径,barrel 消费者零改动)、`map_utils.dart`(纯 Map/响应体强制转换工具,非合同类型也非执行栈,被 client 与 datasource 两侧引用,留根最贴切)。
- 引用更新:
  - barrel `api.dart` export 全部改为 client//contract/ 相对路径并保持字母序。
  - 全库 grep `package:luminous/core/network/` 深引用(排除 `api.dart`),逐一替换为新路径,共 148 个 dart 文件(lib/test/integration_test);同批修正因 client/ 前于 contract/ 字母序变化产生的 17 处 `directives_ordering` 失序。
  - test/core/network/ 测试 import 全部更新,测试文件名未动(含 `target_error_contract_test.dart`)。
  - docs 同步:`docs/02-reference/data-layer.md`(路径 + client/contract 分组说明)、`docs/02-reference/OpenApi_Client.md`、`docs/00-current/Lucent_Contract_Snapshot.md` 的路径文字与 front-matter `updated`;历史日志与 `plans/` 未动。
- 验证:`flutter analyze` No issues found;`flutter test` 全量通过(All tests passed,~5 skip 为既有标记,与本批无关)。
