# Migration Log - 2026-07-17

## Luminous — 审查回查验证（luminous-review-2026-07-17.md）

- **目标**：根据 `plans/luminous-review-2026-07-17.md` 审查回查报告，验证 07-16 遗留问题的修复状态。
- **验证方式**：读取当前 HEAD（`bfda2cfc`）源码，逐项交叉验证 5 个问题的修复状态。
- **验证结果**：`flutter analyze` 零问题，所有 5 个问题均已修复。

### #1 SSE 错误映射重复（🔴 已修复）

- **审查报告状态**：基于 `55904759` 标记为"仍未修复"。
- **实际修复**：提交 `73126254`（2026-07-16 16:35）已修复。
- **验证内容**：
  - `lib/core/network/map_utils.dart` 包含公共 `requireMap(Object? data)` 和 `mapSseStreamError(Object? data)` 函数。
  - 三个 datasource 文件（`ai_summary_remote.dart`、`ai_remote.dart`、`assistant.dart`）均 import `map_utils.dart` 并调用公共函数，无私有 `_mapStreamError` / `_requireMap` 残留。
  - `rg "_mapStreamError|_requireMap" lib/` 零匹配。

### #2 router CustomTransitionPage 重复（🟡 已修复）

- **修复提交**：`55904759`。
- **验证内容**：`lib/app/router/helpers.dart` 包含 `tabFadePage` 函数（使用 `DurationTokens.tabPageTransitionIn/Out`），`router.dart` 5 个 Tab 路由全部调用 `tabFadePage(...)`。

### #3 Web 压缩无日志（🟡 已修复）

- **修复提交**：`55904759`。
- **验证内容**：`lib/core/utils/image_compressor.dart` `kIsWeb` 分支有 `appTalker.debug('Image compression skipped on Web (no plugin backend)')`，catch 分支有 `appTalker.warning` 日志。

### #4 box_scan 异常暴露（🟡 已修复）

- **修复提交**：`55904759`。
- **验证内容**：`lib/features/scan/presentation/pages/box_scan.dart` 使用 `AppLocalizations.of(context)!.scanRecognitionFailedToast` 替代原始 `$e`，异常通过 `appTalker.error` 记录。

### #5 today/suggestion.dart 空 catch 无日志（🟡 已修复）

- **修复提交**：历史提交。
- **验证内容**：`suggestion.dart`（widget）catch 块包含 `appTalker.error('submitFeedback failed: $e', e, st)`；`suggestion.dart`（provider）catch 块为有意的 stale-while-error 缓存兜底策略。

### 结论

审查报告中标记的 5 个问题（1 个 🔴 + 4 个 🟡）全部已修复。审查报告基于 `55904759`，而唯一的 🔴 问题（SSE 错误映射去重）在后续提交 `73126254` 中已解决。审查计划文件已删除。

## Luminous — 架构审查改进 #2 #3：notification/settings/support 分层补齐

- **来源**：`plans/2026-07-16-architecture-review.md` 第 2、3 项
- **目标**：notification feature 补 domain + data 层；settings 补 domain 实体；support DTO 转 domain 模型

### #2 notification feature 三层补齐

- 新建 `notification/domain/entities/notification.dart`：`NotificationType` 枚举、`NotificationItem`（列表项）、`NotificationDetail`（详情）、`NotificationPage`（分页结果 + `hasMore` getter）
- 新建 `notification/domain/repositories/notification.dart`：`NotificationRepository` 接口（`findAll`/`findOne`/`getUnreadCount`/`markAllAsRead`/`markAsUnread`/`delete`）
- 新建 `notification/data/repositories/lucent.dart`：`LucentNotificationRepository` 实现 + `notificationRepositoryProvider`，DTO → domain 映射在此层完成
- 重写 `notification/presentation/providers/notification.dart`：所有 provider 改为通过 repository 访问数据；状态类型从 `NotificationListResponseDto`/`NotificationDetailDto?` 改为 `NotificationPage`/`NotificationDetail?`；`NotificationListController` 内部分页/标记已读/删除逻辑不变，仅数据源从直调 API 改为调 repository
- 更新 `notification/presentation/pages/list.dart`、`detail.dart`、`widgets/shared/list_item.dart`：DTO 类型替换为 domain 实体；`detail.dart` 的 `markAsUnread` 操作从直调 `lucentClientProvider` 改为调 `notificationRepositoryProvider`
- 消费方（mine/top_bar、mine/page、mine/notifications_reminders、today/top_bar）无需改动——`notificationUnreadCountProvider` 导出路径和签名不变
- 删除了未被任何页面使用的 `notificationListPageProvider`

### #3 settings/support domain 层补齐

**settings：**
- 新建 `settings/domain/entities/user_settings.dart`：`UserSettings`（值对象）、`AssistantContextSettings`、`SecurityPinSettings`（读模型）、`AssistantContextPatch`（部分更新命令，nullable 字段）
- 新建 `settings/domain/repositories/user_settings.dart`：`UserSettingsRepository` 接口（`getSettings`/`updateSettings`/`enableSecurityPin`/`changeSecurityPin`/`disableSecurityPin`）
- 新建 `settings/data/repositories/lucent.dart`：`LucentUserSettingsRepository` 实现 + `userSettingsRepositoryProvider`，DTO ↔ domain 映射在此层完成
- 重写 `settings/presentation/providers/user_settings.dart`：状态类型从 `UserSettingsDataDto` 改为 `UserSettings`；`setAssistantContext` 参数从 `UpdateAssistantContextSettingsDto` 改为 `AssistantContextPatch`；`applySettingsPatch` 从接受 `UpdateUserSettingsDto` 改为接受 domain 参数；`_contextToUpdate` helper 删除（repository 处理映射）
- 更新消费方：`settings/presentation/pages/ai.dart`（`UpdateAssistantContextSettingsDto` → `AssistantContextPatch`）、`assistant/presentation/pages/page.dart`、`assistant/presentation/widgets/sections/controls_panel.dart`、`assistant/presentation/providers/conversation.dart`（`applySettingsPatch` 调用从 DTO 改为 domain 参数）

**support：**
- 新建 `support/domain/entities/support_resource.dart`：`SupportResource`、`AppInfo`（读模型）、`SupportResourceAction` 枚举
- 新建 `support/domain/repositories/support.dart`：`SupportRepository` 接口（`getResources`/`getAppInfo`）
- 新建 `support/data/repositories/lucent.dart`：`LucentSupportRepository` 实现 + `supportRepositoryProvider`
- 重写 `support/data/providers/resources.dart`：provider 返回类型从 `List<SupportResourceDto>`/`AppInfoDataDto?` 改为 `List<SupportResource>`/`AppInfo?`
- 更新消费方：`settings/presentation/pages/help.dart`（`SupportResourceDto` → `SupportResource`、`SupportResourceActionType` → `SupportResourceAction`）、`settings/presentation/pages/about.dart`（`AppInfoDataDto` → `AppInfo`）

### 测试更新

- `test/notification/`：4 个测试文件全部更新为 domain 实体
- `test/settings/`：5 个测试文件更新（user_settings_controller_test、more_subpages_test、page_test、help_settings_page_test、about_settings_page_test）
- `test/today/test_helpers.dart`：`UserSettingsDataDto` → `UserSettings`
- `test/assistant/page_test.dart`：`UserSettingsDataDto` → `UserSettings`、`UpdateAssistantContextSettingsDto` → `AssistantContextPatch`、`applySettingsPatch(UpdateUserSettingsDto)` → `applySettingsPatch({...})` 参数化
- `test/support/resources_providers_test.dart`：无需改动（fake API 返回 DTO，repository 映射为 domain，断言检查同名属性）

### 验证

`flutter analyze` 零 error / 零 warning（除 pre-existing BOM illegal_character）。

## Luminous — 架构审查改进 #1：消除 mock 数据寄生机制

- **来源**：`plans/2026-07-16-architecture-review.md` 第 1 项
- **目标**：删除 mock 数据，全部 repository 统一走 Lucent 实现

### 改动内容

**新增 `data/providers/` 文件（4 个新建 + 1 个追加）**：
- `today/data/providers/repository.dart`：`todayRepositoryProvider`（从 `mock.dart` 移出）
- `report/data/providers/repository.dart`：`reportRepositoryProvider` + `reportRemoteDataSourceProvider`（从 `mock.dart` 移出）
- `mine/data/providers/repository.dart`：`mineRepositoryProvider`（从 `presentation/providers/dashboard.dart` 移出）
- `medicine/data/providers/workspace.dart`：`medicineWorkspaceRepositoryProvider` + `medicineReminderRemoteDataSourceProvider`（从 `mock_workspace.dart` 移出）
- `record/data/providers/record_access.dart`（已有）：追加 `recordRepositoryProvider`（从 `mock.dart` 移出）

**删除 mock 文件（5 个 + 4 个 .g.dart）**：
- `today/data/repositories/mock.dart` + `mock.g.dart`
- `report/data/repositories/mock.dart` + `mock.g.dart`
- `record/data/repositories/mock.dart` + `mock.g.dart`
- `mine/data/repositories/mock.dart`（无 .g.dart）
- `medicine/data/repositories/mock_workspace.dart` + `mock_workspace.g.dart`
- `mine/presentation/providers/dashboard.g.dart`（mineRepositoryProvider 移走后不再需要）

**mock 类迁移至 test/ 目录**：
- `test/helpers/mocks/today.dart`：`MockTodayRepository`（含 `placeholderDashboard`、`previewDashboard`）
- `test/helpers/mocks/report.dart`：`MockReportRepository`（含 `previewDashboard`、`_signedOutDashboard`）
- `test/helpers/mocks/record.dart`：`MockRecordRepository`（含全部静态预览数据）
- `test/helpers/mocks/mine.dart`：`MockMineRepository`（含 mock dashboard 数据）
- `test/helpers/mocks/medicine.dart`：`MockMedicineWorkspaceRepository`（含 `previewWorkspace`）
- `test/helpers/feature_mocks.dart`：barrel export

**生产代码更新**：
- `main.dart`：删除全部 mock import 和 `kDebugMode` override 块，`ProviderScope` 简化为空 overrides
- `today/presentation/providers/dashboard.dart`：import 从 `mock.dart` 改为 `data/providers/repository.dart`
- `report/presentation/providers/dashboard.dart`：import 改为 `data/providers/repository.dart`；`signedOutFallback` 从 `const MockReportRepository().signedOutDashboard` 改为 `ref.watch(reportRepositoryProvider).signedOutDashboard`
- `record/presentation/providers/dashboard.dart`：import 从 `mock.dart` 改为 `data/providers/record_access.dart`
- `mine/presentation/providers/dashboard.dart`：删除 `mineRepositoryProvider` 定义和 `part 'dashboard.g.dart'`，改为 import from `data/providers/repository.dart`
- `medicine/presentation/providers/workspace.dart`：import 改为 `data/providers/workspace.dart`；`signedOutFallback` 从 `MockMedicineWorkspaceRepository.previewWorkspace` 改为 `ref.watch(medicineWorkspaceRepositoryProvider).signedOutWorkspace`
- `medicine/presentation/providers/reminders.dart`：import 改为 `data/providers/workspace.dart`
- `today/data/repositories/lucent.dart`：import 从 `mock_workspace.dart` 改为 `medicine/data/providers/workspace.dart`

**测试 + e2e import 更新**：
- 13 个 test 文件：mock import 改为对应 `data/providers/` + 相对路径 `feature_mocks.dart`
- `integration_test/support/e2e_test_helpers.dart`：mock import 改为对应 `data/providers/` + `../../test/helpers/feature_mocks.dart`；export 从 `mock.dart` 改为 `feature_mocks.dart` + `report/data/providers/repository.dart`

### 验证

`flutter analyze` 零问题。
