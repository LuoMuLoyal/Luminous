# 2026-07-25 迁移日志

## 桌面端审查问题修复（任务 1/3/4/5/6）

来源：`plans/2026-07-25-desktop-review-fixes.md`（审查报告改写的修复计划）

### W1: `_WindowTitleBarState` 实现 `WindowListener`（`lib/features/shell/presentation/page.dart`）

- `_WindowTitleBarState` 添加 `with WindowListener` mixin
- `initState` 中 `windowManager.addListener(this)`，`dispose` 中 `windowManager.removeListener(this)`
- 实现 `onWindowMaximize` / `onWindowUnmaximize` 回调，同步 `_isMaximized` 状态
- 修复：用户通过双击标题栏、系统菜单等方式最大化/恢复窗口后，按钮图标不再显示错误

### W3: 统一导航 API 调用风格（`lib/core/shortcuts/app_shortcuts.dart`）

- 4 处 `GoRouter.of(context).push()` / `GoRouter.of(context).go()` 替换为 `context.push()` / `context.go()` 扩展方法
- 与项目中其他文件（`page.dart` 等）保持一致的导航 API 风格

### W4: 修复 `DragTarget.onWillAcceptWithDetails` 悬停状态（`lib/features/record/presentation/widgets/sections/sidebar.dart`）

- 将悬停状态设置（`setState(() => _isDragHovering = true)`）从 `onWillAcceptWithDetails` 移到 `builder` 中
- `builder` 中通过 `candidateData.isNotEmpty` 判断是否设置悬停状态，避免无条件 `setState`

### W5: 缓存 `CommandPalette` 命令列表（`lib/core/widgets/command_palette/command_palette.dart`）

- 新增 `_allCommands` 实例字段，在 `initState` 中构建一次并缓存
- `_onQueryChanged` 仅对缓存列表进行过滤，不再每次按键重建完整命令列表

### W6: Mock `window_manager` MethodChannel（`test/desktop/desktop_layout_test.dart`）

- `setUpAll` 中添加 `MethodChannel('window_manager')` 的 mock handler
- `isMaximized` 返回 `false`，其他方法返回 `null`
- `tearDownAll` 中清除 mock handler
- 修复：跨平台测试稳定性，Windows/Linux 环境下不再 `MissingPluginException`

### 不执行：任务 2（SDK 约束提升）

- SDK 约束保持 `>=3.12.0 <4.0.0` 不变
- 当前开发环境 Flutter 3.44.0，`withValues` API 完全可用
- W2 根因（设计系统覆盖缺口）由后续任务 8 解决

## 设计系统 A 类 tone 迁移（任务 8-A）

来源：`plans/2026-07-25-desktop-review-fixes.md` 任务 8-A

将 7 处可直接映射到现有 `SemanticColorPalette` tone 的 `withValues(alpha:)` 替换为 `SemanticColor.xxx.subtle/muted/border(context)` 调用，消除运行时 alpha 计算。

| 文件 | 替换前 | 替换后 |
|---|---|---|
| `lib/core/widgets/common/desktop_hover.dart` | `colors.primary.withValues(alpha: 0.04)` | `SemanticColor.primary.subtle(context)` |
| `lib/core/widgets/common/desktop_hover.dart` | `colors.primary.withValues(alpha: 0.15)` | `SemanticColor.primary.border(context)` |
| `lib/features/settings/presentation/pages/page.dart` | `colors.primary.withValues(alpha: 0.08)` | `SemanticColor.primary.muted(context)` |
| `lib/core/widgets/command_palette/command_palette.dart` | `theme.colors.primary.withValues(alpha: 0.08)` | `SemanticColor.primary.muted(context)` |
| `lib/features/record/presentation/widgets/sections/sidebar.dart` | `colors.primary.withValues(alpha: 0.15)` | `SemanticColor.primary.border(context)` |
| `lib/features/today/presentation/widgets/shared/card_style.dart` | `SemanticColor.neutral.border(context).withValues(alpha: 0.86)` | `SemanticColor.neutral.border(context)` |
| `lib/core/widgets/common/page_state.dart` | `SemanticColor.neutral.border(context).withValues(alpha: 0.88)` | `SemanticColor.neutral.border(context)` |

附带清理：`desktop_hover.dart` 移除不再使用的 `forui` import 和 `theme`/`colors` 局部变量。

## 设计系统 B 类 tone 扩展（任务 8-B）

来源：`plans/2026-07-25-desktop-review-fixes.md` 任务 8-B

在 `SemanticColorPalette` 中新增 2 个预计算 tone，并将 3 处骨架屏场景的 `withValues` 替换为新 tone 调用。

### 新增 tone

| Tone | Light alpha | Dark alpha | 用途 |
|---|---|---|---|
| `shimmerBase` | 0.03 | 0.06 | 骨架屏/shimmer 基色，替代 `muted.withValues(alpha: 0.32~0.35)` |
| `disabled` | 0.50 | 0.50 | 禁用态前景色，替代 `solid.withValues(alpha: 0.5)` |

### 修改的文件

| 文件 | 改动 |
|---|---|
| `lib/core/design/semantic_color_palette.dart` | 新增 `shimmerBase` / `disabled` 字段、文档注释、`==` / `hashCode` / `toString` |
| `lib/core/design/semantic_color.dart` | 新增 `shimmerBase()` / `disabled()` 扩展方法 |
| `lib/core/design/semantic_colors.dart` | `_lerpPalette()` 新增 `shimmerBase` / `disabled` 的 `Color.lerp` |
| `lib/core/theme/theme.dart` | `_fixedPalette()` 预计算新 tone；`neutral` palette 手动构造处补充新字段 |

### 替换的调用

| 文件 | 替换前 | 替换后 |
|---|---|---|
| `lib/features/shell/presentation/desktop_tab_shell.dart` | `SemanticColor.neutral.muted(context).withValues(alpha: 0.32)` | `SemanticColor.neutral.shimmerBase(context)` |
| `lib/features/shell/presentation/deferred_content.dart` | `SemanticColor.neutral.muted(context).withValues(alpha: 0.32)` | `SemanticColor.neutral.shimmerBase(context)` |
| `lib/features/search/presentation/widgets/views/view.dart` | `SemanticColor.neutral.muted(context).withValues(alpha: 0.35)` | `SemanticColor.neutral.shimmerBase(context)` |

### 保留 `withValues` 的场景

- `button_styles.dart`（Forui CLI 生成文件，不动）
- 半透明填充场景（alpha 0.2-0.92 跨度太大，无法统一到单个 tone）
- 渐变端点（刻意微调的 alpha 值）
- 纯黑遮罩/阴影（非语义色，属于 C 类）

## 设计系统 B 类 tone 扩展——消灭全部 withValues（任务 8-B 续）

来源：`plans/2026-07-25-tone-b-elimination.md`

在 `SemanticColorPalette` 中新增 3 个预计算 tone，并将 16 处 B 类 `withValues(alpha:)` 全部替换为 tone 调用。

### 新增 tone

| Tone | Light alpha | Dark alpha | 用途 |
|---|---|---|---|
| `borderStrong` | 0.30 | 0.40 | 强调边框、拖拽指示、阴影 |
| `fill` | 0.55 | 0.55 | 半透明图标、圆形装饰背景 |
| `fillStrong` | 0.80 | 0.80 | 按钮/sparkline/图标/覆盖/文本/渐变 |

完成后 `SemanticColorPalette` 共 10 个 tone。

### 修改的核心文件

| 文件 | 改动 |
|---|---|
| `lib/core/design/semantic_color_palette.dart` | 新增 `borderStrong` / `fill` / `fillStrong` 字段、文档注释、`==` / `hashCode` / `toString` |
| `lib/core/design/semantic_color.dart` | 新增 `borderStrong()` / `fill()` / `fillStrong()` 扩展方法 |
| `lib/core/design/semantic_colors.dart` | `_lerpPalette()` 新增 3 个 `Color.lerp` |
| `lib/core/theme/theme.dart` | `_fixedPalette()` 预计算 3 个新 tone；`neutral` palette 补充新字段 |

### 替换的 16 处调用

**1 处用现有 `muted` tone**：`page.dart` — `mutedForeground.alpha:0.1` → `neutral.muted(context)`

**5 处 `borderStrong`**：
- `voice_entry_dialog.dart` — 阴影 `(primary/foreground).alpha:0.2` → `primary/neutral.borderStrong(context)`
- `hero.dart` — 边框 `primary.solid.alpha:0.24` → `primary.borderStrong(context)`
- `timeline.dart` — 拖拽边框 `primary.alpha:0.3` → `primary.borderStrong(context)`
- `account_settings_sections.dart` — destructive 边框 `alpha:0.4` → `destructive.borderStrong(context)`
- `sidebar.dart` — 拖拽指示 `primary.alpha:0.4` → `primary.borderStrong(context)`

**2 处 `fill`**：
- `voice_entry_dialog.dart` — 麦克风图标 `neutral.solid.alpha:0.55` → `neutral.fill(context)`
- `risk_finding_tile.dart` — 圆形背景 `solid.alpha:0.56` → `fill(context)`

**8 处 `fillStrong`**：
- `new_entry_panel.dart` — 按钮背景 `solid.alpha:0.68` → `fillStrong(context)`
- `record/components.dart` — sparkline `color.alpha:0.7`（`RecordBarChart.color` 改为 `SemanticColor` 类型）
- `categories.dart` — 按钮背景 `solid.alpha:0.74` → `fillStrong(context)`
- `today/components.dart` — 图标 `palette.solid.alpha:0.74` → `palette.fillStrong`
- `mobile_drugbox.dart` — 图标 `solid.alpha:0.78` → `fillStrong(context)`
- `risk_red_flag.dart` — 覆盖 `background.alpha:0.84` → `neutral.fillStrong(context)`
- `mobile_drugbox.dart` — 文本 `solid.alpha:0.92` → `fillStrong(context)`
- `today/components.dart` — 渐变 `palette.solid.alpha:0.92` → `palette.fillStrong`

### 附带清理

- `page.dart` 移除不再使用的 `theme` 局部变量
- `RecordBarChart` 的 `color` 参数从 `Color` 改为 `SemanticColor`

## 设计系统 token 升级：Motion / IconSize / Elevation / Gradient

新增 4 个 design token 文件，替换全部硬编码 `Curves.*`、`Icon(size: N)`、`BoxShadow` 和内联 `LinearGradient`。

### 新增 token 文件

| 文件 | Token | 说明 |
|---|---|---|
| `lib/core/design/motion.dart` | `MotionTokens` | 4 个 curve token（`entrance`/`exit`/`standard`/`snappy`）；`DurationTokens` 从 `durations.dart` 迁移至此 |
| `lib/core/design/icon_size.dart` | `IconSizeTokens` | 5 级 icon size（`level1`=12, `level2`=16, `level3`=20, `level4`=24, `level5`=32） |
| `lib/core/design/elevation.dart` | `ElevationTokens` | `raised(FColors)` / `glow(Color)` / `shadowColor(FColors)`，暗色模式 alpha 自动补偿 |
| `lib/core/design/gradient.dart` | `GradientTokens` | `semanticFill(SemanticColorPalette)` / `tintFade(Color, Color)` 两个命名渐变模式 |

`durations.dart` 已删除，`design.dart` barrel 更新导出。

### 替换汇总

| 类别 | 替换数 | 映射 |
|---|---|---|
| `Curves.*` → `MotionTokens.*` | 10 处 | `easeOutCubic`→`entrance`, `easeOut`→`snappy`, `easeInOut`→`standard` |
| `Icon(size: N)` → `IconSizeTokens.level*` | 30+ 处 | 12→level1, 14/16→level2, 18/20→level3, 22/24→level4, 32/36→level5 |
| 内联 `BoxShadow` → `ElevationTokens.*` | 2 处 | `timeline.dart`→`raised`, `voice_entry_dialog.dart`→`glow` |
| `foruiMaterialTheme` shadowColor → `ElevationTokens.shadowColor` | 1 处 | `theme.dart` |
| `LinearGradient` → 纯色/token | 3 处 | `hero.dart`×2→`SemanticColor.primary.subtle`（纯色），`components.dart`→`GradientTokens.semanticFill`（`filled` 参数替代旧 `gradient` 参数） |

## 桌面端窗口管理修复

### 窗口控制按钮位置

- **问题**：min/max/close 按钮在左侧导航栏的右上角（受 sidebar 宽度约束），而非应用程序窗口的右上角。
- **修复**：新增 `lib/core/widgets/common/desktop_window_chrome.dart`（`DesktopWindowChrome`），在 `bootstrap.dart` 的 app builder 中以 `Column` 布局置于应用顶部，横跨整个窗口宽度。按钮在 `Row` 末尾，定位在窗口右上角。
- `page.dart` 的 `_WindowTitleBar` 简化为 `StatelessWidget`，仅保留 `DragToMoveArea`（侧边栏头部拖拽区）和 macOS traffic-light padding。`_WindowControlButtons` / `_WindowButton` 类移至 `desktop_window_chrome.dart`。

### 窗口拖拽修复

- **问题**：`DragToMoveArea` 仅覆盖侧边栏头部，窗口其他区域无法拖拽。
- **修复**：`DesktopWindowChrome` 包含一个全宽 `DragToMoveArea`（32px 高），位于应用顶部。侧边栏头部的 `DragToMoveArea` 保留作为额外拖拽区。

### 侧边栏展开/收窄动画

- **问题**：`AnimatedContainer` 的 `width: collapsed ? 64 : null` 无法在具体数值与 `null` 之间插值，宽度瞬间跳变。
- **修复**：替换为 `AnimatedSize`（`duration: DurationTokens.widgetStandard` = 300ms，`curve: MotionTokens.standard` = easeInOut，`alignment: centerLeft`），内嵌 `SizedBox` 设置宽度。`AnimatedSize` 监听子组件实际渲染尺寸并平滑过渡，内容被 clip 住，标签逐渐揭开/收起。

## 报告页 UI 优化

来源：taste-skill 设计原则指导下的报告页视觉与结构优化

### P0：降级"生成总结"按钮权重

| 文件 | 改动 |
|---|---|
| `readiness.dart` | ready 状态主操作从 `FButton`(filled) → `FButton`(outline)；移除外层 `Expanded` 使按钮不再撑满整行 |
| `top_bar.dart` | `ReportActionBar` 生成按钮从 filled + Expanded → outline；sync 按钮从 secondary → outline |

### P0：趋势图 Empty State

| 文件 | 改动 |
|---|---|
| `trend.dart` | `_TrendChart.build()` 新增空态判断：当所有 series 无数据时渲染居中图标+标题+描述的 FCard |
| `report_zh.arb` | 新增 `reportTrendEmptyTitle` / `reportTrendEmptyBody` |
| `report_en.arb` | 同上英文版本 |

### P1：移动端内容重排 — Export 上移

| 文件 | 改动 |
|---|---|
| `dashboard_view.dart` | 移动端布局中 `ReportExportSection` 从 AI Summary 之后上移至 Trend 之后 Findings 之前 |

### P1：Preview 锁定卡片合并

| 文件 | 改动 |
|---|---|
| `dashboard_view.dart` | 移动端/桌面端 preview 布局中两个独立 `ReportPreviewLockedSection` 合并为一个 FCard 内的两个 `_PreviewLockedRow`，中间用 `AppDivider` 分隔。新增私有 widget `_PreviewLockedRow` |

### P2：Section 标题层级差异化

| Section | 之前 | 之后 |
|---|---|---|
| Trend / Findings / Patterns (数据展示) | level5 / w700 | 不变 |
| AI Summary | 已有 avatar 结构 | 不变 |
| Export (功能入口) | level5 / w700 | **level4 / w600** |
| Suggestion History (辅助信息) | level5 / w700 | **level4 / w600** |

### P2：导出区 Wrap 布局替代 GridView

| 文件 | 改动 |
|---|---|
| `export.dart` | `GridView.builder`(固定 2 列) → `Wrap` + 响应式 `_exportCardWidth()`；移动端自然换行 |

### P3：首位指标突出显示

| 文件 | 改动 |
|---|---|
| `metrics_grid.dart` | `_MetricCard` 新增 `isFirst` 参数；首位卡片添加 primary 色 2px 边框强调 + 数值字号从 level8 升至 level9 |

## 今日摘要卡片：查看依据改用 FCollapsible

- `summary.dart` 的「查看依据」展开/收起从 `AnimatedSwitcher` 改回 Forui `FCollapsible`，与 `suggestion_primary_card.dart` 保持一致的动画模式。
- `_TodaySummarySectionState` 重新引入 `SingleTickerProviderStateMixin`、`AnimationController`（`DurationTokens.widgetExpand`）和 `CurvedAnimation`（`MotionTokens.standard`）。
- 撤销 2026-07-22 的动画简化改动（`AnimatedSwitcher` 替换方案）。

## 报告页时间范围选择器改用 FPopoverMenu

- 报告页右上角的时间范围选择（近7天 / 近30天 / 自定义）从 `showModalBottomSheet`（移动端）/ `showAppDialog`（桌面端）改为 Forui `FPopoverMenu`，从 pill 按钮右下方弹出。
- 新增 `ReportRangeMenu` 组件（`top_bar.dart`），封装 `FPopoverMenu` + `ReportPeriodPill` 触发器。
- `range_picker_dialog.dart` 新增公开 `showReportCalendarPicker` 函数，"自定义"选项关闭 popover 后调用该函数弹出 `FCalendar` 日历选择器。
- `page.dart`、`trend.dart` 中所有 `ReportPeriodPill` + `showReportRangePickerDialog` 调用替换为 `ReportRangeMenu`。
- `showReportRangePickerDialog` 保留供测试使用。

## 移除桌面端侧边栏折叠功能

折叠动画效果不佳，彻底删除侧边栏折叠/展开（rail 模式）功能，侧边栏始终展开。

### 删除的文件

| 文件 | 说明 |
|---|---|
| `lib/core/providers/sidebar_preference.dart` | `SidebarPreferenceController`（AsyncNotifier\<bool\>）已删除，不再有折叠状态持久化 |

### 修改的文件

| 文件 | 改动 |
|---|---|
| `lib/features/shell/presentation/page.dart` | `_DesktopSidebar` 移除 `collapsed` 状态、`AnimatedSize` 包裹和 `sidebarPreferenceProvider` 依赖；`tabItem` 始终渲染 label；`_WindowTitleBar` / `_SidebarHeaderContent` / `_SidebarFooter` 移除 `collapsed` 参数；删除 `_CollapseToggleButton` 和 `_SidebarIconAction` 两个类；移除 `_collapsedSidebarWidth` 常量 |
| `lib/core/shortcuts/app_shortcuts.dart` | `AppShortcuts` 从 `ConsumerWidget` 降级为 `StatelessWidget`；移除 Ctrl/Cmd+B 快捷键和 `ToggleSidebarIntent` action |
| `lib/core/shortcuts/intents.dart` | 删除 `ToggleSidebarIntent` 类 |
| `lib/core/widgets/command_palette/command_palette.dart` | 移除侧边栏折叠命令条目和 `intents.dart` import |
| `lib/core/config/pref_keys.dart` | 移除 `sidebarCollapsed` key |
| `lib/l10n/src/common_zh.arb` | 移除 `desktopSidebarCollapse` / `desktopSidebarExpand` 字符串 |
| `lib/l10n/src/common_en.arb` | 同上英文版本 |
| `test/desktop/keyboard_shortcuts_test.dart` | 移除 `ToggleSidebarIntent` 和 `Ctrl+B activator` 两个测试用例 |

## 报告页趋势图改用 FTabs 单线切换

- 趋势图从多线折线图（所有指标同时展示、各线归一化到 [0,1]）改为 Forui `FTabs` + 单线折线图。
- 每个 tab 对应一个指标（用药/饮水/睡眠），选中 tab 时只渲染该指标的单条折线。
- Y 轴改为显示真实数据值（不再归一化），带 15% padding。
- 旧版 `_LegendDot` 图例行移除，当前值以大字号显示在图表上方。
- 折线图新增淡色区域填充（`belowBarData` alpha 0.08）。
- `_TrendChart`（多线）拆分为 `_TrendTabs`（tab 容器）+ `_SingleTrendChart`（单线图）+ `_TrendEmptyState`（共享空态）。

## 桌面端记录页 UI 修正与优化

来源：`plans/2026-07-25-desktop-record-page-ui-fix.md`

### 三栏比例调整（`dashboard_view.dart`）

- 左栏从 `ResponsiveSizing.sidebarWidth(context)`（280–360px）改为固定 **260px**
- 右栏从 `ResponsiveSizing.sidebarWidth(context)`（280–360px）改为固定 **220px**
- 中间区域从 `Expanded(flex: 6)` 改为 `Expanded`（无 flex 权重，自适应剩余空间）

### 新建记录右栏重构（`new_entry_panel.dart`）

- `RecordNewEntryPanel` 根据 `width >= Breakpoints.desktop` 切换桌面/移动布局
- 桌面端新增 `_DesktopEntryButton`（StatefulWidget）：32px 圆形图标 + 文字标签垂直排列，hover 状态使用 `SemanticColor.neutral.subtle(context)` 背景变化
- 桌面端主按钮改为 `FButtonVariant.primary`（移动端保持 `ghost`）
- 移动端布局（`Wrap` + `_NewEntryChip`）保持不变

### 日历面板优化（`sidebar.dart`）

- `_CalendarIconActionButton` 使用 `FButtonSizeVariant.xs`，图标尺寸从 `IconSizeTokens.level3`（20px）降为 `level2`（16px）
- 日历网格 `childAspectRatio` 从 `0.86` 改为 `0.95`，`mainAxisSpacing` / `crossAxisSpacing` 从 `Spacing.level2` 降为 `level1`
- 筛选面板「全选」按钮图标从 `chevronRight` 改为 `checkCheck`，尺寸从 `Spacing.level4` 改为 `IconSizeTokens.level2`

### 时间线面板优化（`timeline.dart`）

- 时间线标题右侧新增当前选中日期显示（`DateFormat.yMd(locale)`）
- 空状态 `_DesktopTimelineEmptyState` 使用 `Center` + `ConstrainedBox(maxWidth: 320)` 居中
- 时间线卡片去除内层 `FCard` 嵌套，改用 `DecoratedBox` + 左侧 3px 色条（`entry.accent.solid(context)`）区分记录类型，圆角使用 `RadiusTokens.level3`
- 图片占位符同样从 `FCard` 改为 `DecoratedBox`

### 摘要网格优化（`summary_grid.dart`）

- 桌面端固定 **4 列**（移动端保持动态 1–5 列）
- `minTileWidth` 从 140px 提升至 160px
- 图标容器固定 28px（不再使用 `ResponsiveSizing.scaleByWidth`），图标尺寸固定 `IconSizeTokens.level2`（16px）
- 数值字体从 `TypographyToken.level7` 降为 `level6`

## 桌面端登录/注册页 SaaS 分栏布局

来源：用户请求将桌面端认证页改为左图右表单的 SaaS 平台风格

### `AuthShell` 拆分桌面/移动布局（`shell.dart`）

- `AuthShell.build` 根据 `width >= Breakpoints.desktop` 分流到 `_DesktopAuthShell` 或 `_MobileAuthShell`
- 桌面端采用 `Row` 布局：左侧 `Expanded(flex: 5)` 品牌展示区 + 右侧 `Expanded(flex: 4)` 表单区
- 移动端布局逻辑不变（单列居中）

### 新增桌面品牌展示区 `_DesktopBrandPanel`

- 全屏 `GradientTokens.tintFade(colors.primary.withValues(alpha: 0.02), colors.background)` 极淡蓝色渐变，覆盖左右两栏，无割裂感
- `FScaffold` 设置 `childPad: false`，渐变 `DecoratedBox` 作为 scaffold child 直接绘制在全屏范围
- 展示 `assets/icon/app_icon_transparent.png`（3D 透明立体 logo，80px）
- 品牌标语（`authBrandTagline`）使用 `TypographyToken.level9.display` + `FontWeight.w800`
- 品牌描述（`authBrandDescription`）使用 `TypographyToken.level5.body`
- 三个特性项（`_BrandFeatureItem`）：结构化记录 / AI 趋势分析 / 隐私优先，每项含 40px 圆角图标 + 标题 + 描述

### 资源与 l10n

- `pubspec.yaml` 新增 `assets/icon/app_icon_transparent.png` 资源注册
- `auth_zh.arb` / `auth_en.arb` 新增 8 条品牌区文案（`authBrandTagline` / `authBrandDescription` / `authBrandFeature*` / `authBrandFeature*Desc`）
