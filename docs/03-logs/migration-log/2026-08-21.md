# Migration Log — 2026-08-21

## fix(report): 增量代码审查修复（08-20 审查报告 + 08-19 遗留）

依据 `plans/Luminous-review-2026-08-21.md` 审查报告，修复 2 项 🔴 严重、5 项 🟡 警告（08-20 当轮）与 6 项 08-19 遗留问题。

### 🔴 严重修复

- **security_elevation_dialog.dart** `DateTime.now()` 硬编码：引入 `DateTime Function()? now` 可注入参数，与 `SecurityElevationTokenHolder` 的可注入 `_now` 保持一致。提取 `_isElevationValid()` 和 `_resolvePinSettings()` 私有方法消除两处重复的 PIN 检查逻辑（`showSecurityElevationDialog` + `requestSecurityElevationOrSetup`）。
- **account.dart** `_isSecurityElevationFailure` 字符串匹配：移除 `contains('elevation token')` 模糊匹配，仅保留精确的 `elevation_token_invalid` 机器码与中文精确短语，避免误匹配令牌有效但其他条件触发的 403。

### 🟡 警告修复

- **device_session.dart** `AuthDeviceSession.fromJson`：为 `lastUsedAt` 可空日期解析添加注释，说明新建会话可能未使用过、API 省略此字段是合法状态。
- **sleep_reminder_notification_coordinator.dart** `_isInDndWindow`：为 `start == end` 返回 `true`（全天 DND）添加注释说明语义。
- **sleep_reminder_notification_coordinator.dart** 通知 ID base：为 `_notificationIdBase` 添加文档注释说明 ID 范围约束，在 `resync` 中添加 `assert(horizonDays <= 1000)` 防碰撞断言。
- **notification.dart** `_ScopedPreferences`：添加文档注释说明当前仅通知模块使用，当第二个模块需要用户隔离存储时提取到 `core/storage/`。
- **security_elevation_dialog.dart** PIN 检查逻辑重复：提取 `_resolvePinSettings()` 和 `_isElevationValid()` 私有方法（与 🔴 #1 一并完成）。

### 08-19 遗留修复

- **review_history.dart** `_pendingCursor` 未重置：在 `_syncFromFirstPage` 重置块中添加 `_pendingCursor = null`，防止筛选切换后迟到的旧 loadMore 请求结果混入新筛选数据。
- **review_history.dart** `_syncFromFirstPage` 副作用在 build 中：将调用从 `build` 的 `data:` 回调移至 `didUpdateWidget`，添加 `initState` 初始种子同步，遵守 build 纯度约束。
- **router.dart** 路由参数强制解包：`state.pathParameters['token']!` / `['eventId']!` 替换为 null 检查 + 防御性 fallback Scaffold，避免路径参数缺失时崩溃。
- **review_detail.dart** `AppLocalizations.of(context)!`：改为先获取 nullable 再做 null guard early return，防御性处理本地化缺失场景。
- **lucent_ai_summary.dart** 未知枚举值默认映射：`_mapPatternKind` 和 `_mapRange` 的 `_` 默认分支替换为显式 `unknownDefaultOpenApi` 分支并记录 warning 日志，不再静默映射。
- **clinic_summary_content.dart** `_sectionSelected` 语义不一致：为 findings/waterEntries/sleepEntries/noteEntries 四个 section 补齐 `_sectionSelected` 检查，与 profile/conditions/currentMedicines 保持一致。

### 验证

- `flutter analyze` 修改的 10 个文件零告警。
- `dart run scripts/check_doc_coverage.dart --warning-only` 确认需更新文档（本条 + current state）。

## chore(upgrade): Flutter 3.47.1 分支变基准备

- 将 `upgrade/flutter-3.47` 的两个升级提交变基到当前 `refactor`，保留当前分支已有改动并解决 TODO/迁移日志冲突。
- CI 目标从 Flutter 3.47.0 更新为 3.47.1；升级计划同步 Dart 3.13.1 与当前基线。
- 未执行 SDK 切换、依赖解析或构建；下一步先在该分支验证 3.47.1 的语义回归、analyze、test 与 Android 构建。

## chore(upgrade): Flutter 3.47.1 SDK 验证结果

- 通过 Flutter 官方 Windows release archive 安装 Flutter 3.47.1，确认 Dart 3.13.1；未保留旧 SDK 目录。
- 执行 ARB merge、OpenAPI client/l10n/build_runner 生成链路，`pubspec.lock` 更新了 Dart 3.13.1 下的依赖解析结果。
- `flutter analyze` 通过。
- `flutter test` 在 3.47.1 下复现 flutter/flutter#191095：`semantics.dart:3862` 的 `node.isMergedIntoParent` 与 dirty-node 断言；停止时 JSON 结果记录 3,842 个完成用例、90 个失败。
- `flutter build apk --release` 通过，产物为 `build/app/outputs/flutter-apk/app-release.apk`；`flutter build web --release` 通过，产物为 `build/web`。
- Android 构建仍输出插件使用 KGP 的迁移警告（flutter_image_compress_common、health、mobile_scanner、sentry_flutter、sign_in_with_apple），但本次构建成功；iOS/macOS 和真机插件行为未验证。

## chore(upgrade): 升级改动落地 refactor

- `upgrade/flutter-3.47` 已快进合并到 `refactor`，当前工作区最终分支为 `refactor`。
- `refactor` 当前 SDK 为 Flutter 3.47.1 / Dart 3.13.1；升级验证结果和测试阻塞状态保持不变。

## chore(flow-ui): 阶段 0 准备

- `pubspec.yaml` 添加 `flow_ui: ^0.1.0`，并将 `flutter.uses-material-design` 设为 `true`，仅为 FlowUI 的 Material Icons 资源打包提供配置。
- 执行 `flutter pub get` 后，`pubspec.lock` 解析到 direct main 依赖 `flow_ui 0.1.0`；锁文件的 Dart SDK 下限同步解析为 `>=3.12.2`。
- 阶段 0 的 adapter/page widget 测试暂不添加或伪造通过：adapter 实现在计划阶段 2a，相关测试延后至 2a 实现后执行。

### 验证

- `flutter pub get`：通过；输出包含 `+ flow_ui 0.1.0`。
- `flutter pub deps --style=compact`：通过；解析结果包含 `flow_ui 0.1.0 [flutter]`。
- `flutter analyze`：通过，`No issues found!`。
- `dart run scripts/check_doc_coverage.dart --warning-only`：通过，`Documentation coverage: no mapped code changes detected.`

## fix(flow-ui): 同步 Dart SDK 下限

- 将 `pubspec.yaml` 的 Dart SDK 下限同步为 `>=3.12.2`，与 `flow_ui 0.1.0` 的约束及 `pubspec.lock` 保持一致。

## feat(flow-ui): 阶段 1 Theme 桥接

- 新增 `flow_theme_bridge.dart`，从当前 Forui `FColors`、`SemanticColor` 和 `TypographyToken` 派生 assistant 使用的 `FlowTheme`；颜色以 `FlowColors.light/dark.copyWith` 保留未映射的 FlowUI 预设角色。
- `AssistantPage` 只在自身内容根部注入 FlowTheme，并保留父 `ThemeData` 的其他扩展；未修改 domain entity、repository、controller、Lucent/SSE、l10n、消息/输入/编排组件或 drawer。
- 新增 widget 测试覆盖 assistant 作用域、父主题扩展保留和 wrapper 外无 FlowTheme。

### 验证

- TDD red：桥接文件不存在时，`flutter test test/assistant/flow_theme_bridge_test.dart` 按预期因缺少 bridge/helper 编译失败。
- TDD green：`flutter test test/assistant/flow_theme_bridge_test.dart` 通过。
- `flutter test test/assistant/page_test.dart --plain-name "AI chat page renders on mobile screen size"` 通过。
- `flutter analyze` 通过，`git diff --check` 通过。

## fix(flow-ui): 阶段 1 初审测试缺口

- 在 `test/assistant/page_test.dart` 增加真实 `AssistantPage` 接线测试：检查 assistant 内容 ancestors 中恰有一个 `FlowTheme`，并确认外层 Material `Theme` 不含 FlowTheme。
- 扩展 `flow_theme_bridge_test.dart` 覆盖 dark ambient preset、Forui ambient 字体 family/package 编码保留，以及父 FlowTheme 被 bridge 替换且不重复。
- 保持 header/drawer 结构和计划文件不变；未处理 Flutter 3.47 已知 semantics/FToaster 基线失败。

### 验证

- `flutter test test/assistant/flow_theme_bridge_test.dart`：通过。
- `flutter test test/assistant/page_test.dart --plain-name "AssistantPage scopes one FlowTheme below MaterialApp"`：通过。
- `flutter test test/assistant/page_test.dart --plain-name "AI chat page renders on mobile screen size"`：通过。
- `flutter analyze`：通过，`No issues found!`。
- `dart run scripts/check_doc_coverage.dart --warning-only`：通过；无必需文档缺失，仅有可选建议。
- `git diff --check`：通过。

## fix(flow-ui): 阶段 1 复审 P2 sentinel 断言

- 将父 FlowTheme 替换测试改为使用明显不同的 sentinel surface/primary 颜色。
- 从 wrapper 子树直接读取 FlowTheme，断言数量为 1，surface/primary 与 `luminousFlowTheme` 派生值相等且不再等于 sentinel；保留已有父主题扩展断言。

## feat(flow-ui): 阶段 2a 消息列表 adapter

- 新增 assistant FlowUI adapter，仅负责 `AssistantMessage` / streaming draft 到 `FlowMessageData` 的映射，以及 `FlowThread.messageBuilder` 所需的 custom part、footer 和消息操作。
- 使用 `conversationId + index + messageIdFor(message)` 生成 FlowUI 稳定列表 id；proposal custom part 保留 controller 使用的 canonical message id，不修改 `AssistantMessage`、SSE mapper、repository 或 controller。
- 已完成 assistant 内容使用 `FlowCustomPart(type: 'markdown')` 渲染现有 `MarkdownBody` / `MarkdownStyle.ai`；user 和 streaming draft 使用 `FlowTextPart`，streaming draft 使用 `FlowMessageStatus.streaming`。proposal 使用 `FlowCustomPart(type: 'proposal')` 渲染现有 `AssistantProposalCard`。
- `conversation_message_list.dart` 保留同名 ConsumerWidget facade 和 empty/disabled `StateMessageView`，生产消息渲染改为 `FlowThread`；`message_bubble.dart` 未删除，仍供阶段 3 前的孤立测试使用。未改 `conversation_surface`、page 编排、input bar、starter prompts/welcome 或 FlowThinkingIndicator。
- footer 保留 timestamp、source strip、disclaimer、replaced label/muted content，以及 Clipboard + Toast copy action；`FlowMessageActions` 继续透传当前页面已有的 regenerate/resend 回调。
- 计划文字曾将 regenerate/resend callback 描述为 null，但当前代码和已提交 F-5b 行为已接通且有测试；本阶段选择保留这些 page 传入回调语义，未新增 controller 能力，也未静默回退为 null。
- assistant 生产路径继续只允许 external http/https Markdown link 在用户确认后调用 `externalUrlLauncher`，不会自动打开或放行其他 scheme。

### 验证

- TDD red：新增 `test/assistant/flowui_adapter_test.dart` 后，目标 adapter 文件/类型不存在，测试按预期无法加载。
- TDD green：adapter mapping、custom part、production message-list、actions 和 Markdown link confirmation 测试通过。
- `flutter analyze` 通过；`git diff --check` 通过；文档覆盖检查已运行。完整 `page_test.dart` 仍受 Flutter 3.47 已知 semantics framework assertion 影响，`widgets_test.dart` 与本阶段 adapter 测试通过。

## fix(flow-ui): 阶段 2a 审查修复

- 修正 reverse `FlowThread` 的滚动语义：`AssistantPage` 的自动滚动和 `AssistantConversationStack` 的回到最新按钮统一调用 `scrollAssistantToLatest`，目标为 offset `0`；page 的 near-latest 判断改为 `pixels <= 96`。
- 修正 adapter 的空 streaming draft 映射：空 draft 返回空 parts + `FlowMessageStatus.pending`，非空 draft 保持 `FlowTextPart` + `streaming`；`mapMessages` 仍不为 2e 提前追加 pending 行。
- 补充 reverse-list scroll regression test 和空 draft mapping test；copy/action 测试挂载真实 toaster，显式推进项目 Toast timer 和 Forui 动画，避免 `FToaster unavailable` 噪声及遗留 timer。

### 验证

- P1 scroll behavior test、P2-1 adapter mapping test 和 P2-2 copy/action test 通过。
- `flutter analyze`、文档覆盖检查、`dart format --output=none --set-exit-if-changed`、`git diff --check` 通过。

## fix(flow-ui): pending message 不创建 footer actions

- `AssistantFlowUiAdapter.buildMessage` 现在只为 `FlowMessageStatus.complete` 创建 footer；pending 和 streaming message 不创建 timestamp、Toast copy 或 `FlowMessageActions`。
- 增加 pending `FlowMessage` 实际渲染测试，确认 `FlowThinkingIndicator` 存在且 `FlowMessageActions` 不存在，并在测试结束时移除动画树。
