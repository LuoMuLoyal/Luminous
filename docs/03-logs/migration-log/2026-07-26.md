# 2026-07-26 迁移日志

## 代码审查修复（7-26 审查报告）

来源：`plans/Luminous-review-2026-07-26.md`

### 🔴 严重：`SemanticColorsAccess` 扩展防御性处理（`lib/core/design/semantic_colors.dart`）

- forui `FColors.extension<T>()` 内部使用 `_extensions[T]! as T`，若 `SemanticColors` 未注入主题数据，运行时抛出晦涩的 `TypeError`
- 改为遍历 `extensions` 集合查找 `SemanticColors` 实例，缺失时抛出描述性 `StateError`，包含修复指引
- 正常流程无行为变化（`appThemeData()` 始终注入 `SemanticColors`），仅在主题配置遗漏/测试环境时提供更清晰的错误信息

### 🟡 警告：报告页导出操作移除强制解包（`lib/features/report/presentation/pages/page.dart`）

- `_handleExportAction` 中 `_exportInputForKind(kind)!` 强制解包依赖远距离的 early return 契约（`clinicShare` 在前已 return）
- 改为先取值到局部变量 `input`，显式 `if (input == null) return` 后再传入 `runGuarded`
- 消除隐式耦合，后续重构调整 early return 条件或新增 export kind 不会引发运行时异常

### 7-25 审查报告核查与清理

- 核查 `plans/Luminous-review-2026-07-25.md` 全部 6 个警告项的当前状态
- W1/W3/W4/W5/W6 均已在 7-25 当日后续提交中修复
- W2（SDK 约束 `>=3.12.0` 与 `withValues` API 不匹配）为配置层面问题，当前开发环境 Flutter 3.44.0 不受影响，不阻塞
- 报告 83% 内容已过时，作为可操作文档无实质价值，已删除
