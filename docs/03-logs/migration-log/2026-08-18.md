# Migration Log — 2026-08-18

## feat(record): P1-1 摘要网格接线 + 饮水 ml 角标真实数据

依据 `plans/2026-08-16-record-remediation-plan.md` 的 P1-1（本节完成后已从计划文件删除）：后端 `GET /api/v1/user/daily-records/summary` 此前仅 Today 页消费，Record 页 `LucentRecordRepository.fetchDashboard` 硬编码 `summary: _staticSummary`（恒为空），导致桌面 `RecordSummaryGrid` 永不渲染、饮水角标「累计 ml」模式取不到值。本项完成纯客户端接线。

### 数据层

- `lib/features/record/data/repositories/lucent.dart` `fetchDashboard`：
  - 并行发起 `fetchRecords` 与 `fetchSummary(dateStr)`（两个 future 先创建后 await），各自独立 try/catch：fetchRecords 失败维持原降级（空列表 + `appTalker.error`）；fetchSummary 失败降级为 `const RecordDaySummary(items: [])` + `appTalker.error('...fetchSummary failed: $e')`，不抛异常、不影响 timeline。
  - 新增私有静态方法 `_toDaySummary(DailyRecordSummaryData, List<DailyRecordItem>)` 把后端 summaries 映射为 `RecordDaySummary.items`：跳过 `count <= 0`（稀疏语义：未知≠0）与无摘要文案基建的 kind（symptom/sleep/note/activity）；water 项 value = 当日 canonical ml 合计（kind==water、unit=='ml'、可解析且 >0 才累加，与详情页进度卡同规则），无可累加 ml 时回退显示 count + `summaryTimesUnit`（保证角标不显示裸 0）；meal/mood/vital 项 value = count + `summaryTimesUnit`；所有 item 统一 `accent=SemanticColor.primary`、`softColor=SemanticColor.neutral`、`detailKey=null`，保持 summaries 原有顺序。
  - `_staticSummary` 字段的唯一消费方被替换后成为死代码，随本项移除（P2-2 静态残留清理剩余 `_staticWeekDays`），保证 `flutter analyze` 零告警。

### L10n

- `RecordCopyKey` 枚举新增 `summaryMlUnit`（summary 键组末尾）；`recordCopy` 补 `summaryMlUnit => l10n.recordSummaryMlUnit`。
- `lib/l10n/src/record_zh.arb` / `record_en.arb` 新增 `recordSummaryMlUnit`（zh/en 均为 "ml"）；已执行 `dart scripts/arb_tools.dart merge` + `flutter gen-l10n` 同步生成文件。

### 测试

- `test/record/data/repositories/lucent_repository_test.dart`：`_FakeDailyRecordRepository` 扩展为可注入 `fetchSummaryResult` / `fetchSummaryError` 并记录 `lastFetchSummaryDate`；新增用例覆盖 ml 聚合、ml 缺失回退 count、count==0 跳过、无文案基建 kind 不生成 item、fetchSummary 失败降级且 timeline 不受影响、两个 fetch 使用相同 dateStr。既有用例语义保持不变（fake 默认空 summaries 时 `summary.items` 仍为空）。

### 文档

- `docs/00-current/Active_UI_Record.md`：新增「摘要网格与饮水角标」小节说明已接真实数据；front-matter `updated` 与 Last updated 改为 2026-08-18。
- `docs/02-reference/Localization.md`：记录新增键 `recordSummaryMlUnit`；front-matter `updated` 与 Last updated 改为 2026-08-18。
- `plans/2026-08-16-record-remediation-plan.md`：删除 P1-1 小节；「五、本计划内执行顺序」第 1 条更新为仅剩 P1-2。P2-2 的「依赖:P1-1」引用按计划保留。

### 验证

- `flutter analyze` 零告警。
- `flutter test test/record/data/repositories/lucent_repository_test.dart` 通过。
- `flutter test test/record` 全部通过。
- `dart run scripts/check_doc_coverage.dart --warning-only` 无必需文档缺失（仅 core-i18n 可选建议）。

## fix(record): P1-1 审查 P2 修复（4 项修复 + 1 项边界说明）

P1-1（摘要网格接线）审查报告共 6 个 P2：4 项修复，2 项接受现状（不做代码改动）。

### 修复

- `lib/features/record/data/repositories/lucent.dart` `_toDaySummary`：vital 摘要项由 count + `summaryTimesUnit` 改为优先展示 `summary.latest` 的值（如 "72 bpm"，`unitKey=null`）；latest 缺失或其 value 为空时回退 count + `summaryTimesUnit`。icon/titleKey（`summaryLatestVitalTitle`）不变，修复「最新体征」标题与次数数值的语义不匹配（P2-3）。
- `test/record/data/repositories/lucent_repository_test.dart`：新增 vital 用例——带 latest 时断言展示 latest 值（"72 bpm"、unitKey null），无 latest 时断言回退 count + times 单位。
- `plans/2026-08-16-record-remediation-plan.md` P2-2：方案更新为仅剩「核对 `_staticWeekDays` 的实际引用范围后一并清理」，并注明 `_staticSummary` 已于 P1-1 随本项移除（P2-1）。
- `docs/03-logs/migration-log/2026-08-18.md` / `docs/02-reference/Localization.md`：新增 `summaryMlUnit` 的位置描述由「枚举末尾」改为「summary 键组末尾」（P2-4）。
- 迁移日志验证表述由「无新增警告」改为「无必需文档缺失（仅 core-i18n 可选建议）」（P2-6）。

### 接受现状

- P2-2（筛选态饮水 ml 合计从被过滤的 records 计算）与 P2-5（pageSize=100 分页上限）为既有/非本任务引入行为，不做代码改动；前者在 `docs/00-current/Active_UI_Record.md` P1-1 小节追加边界说明，后者知晓即可不记录。

### 验证

- `flutter analyze` 零告警。
- `flutter test test/record/data/repositories/lucent_repository_test.dart` 通过。
- `dart run scripts/check_doc_coverage.dart --warning-only` 无必需文档缺失（仅 core-i18n 可选建议）。

## feat(record): P1-2 详情页饮水进度卡目标值改读 user-settings

依据 `plans/2026-08-16-record-remediation-plan.md` 的 P1-2（本节完成后已从计划文件删除）：详情页饮水进度卡目标值此前在 `lib/features/record/presentation/pages/detail.dart` 硬编码为固定 ml 常量，与 Today Analysis 使用 `user-settings.waterTargetCount` 的口径分裂。本项改读 `user-settings.waterTargetCount`，按 `waterTargetCount × 250 ml`（与 Today Analysis 及 Today 饮水概览同源换算）展示目标值；后端不动。

### 数据层

- 新增 `lib/features/record/data/providers/water_target.dart`：
  - `recordWaterTargetCountProvider`（`FutureProvider.autoDispose<int>`）读取 `user-settings.waterTargetCount`（杯数）。
  - 消费路径遵循跨 feature 导入规则：record 的 data 层经 settings 的 **domain 接口**（`UserSettingsRepository`）读取，经 `userSettingsRepositoryProvider` 装配，与 today 数据层先例（`today/data/providers/today_suggestion.dart`）一致；record 的 presentation 层不 import 任何 settings presentation provider。
  - 兜底：读取失败、未就绪或返回非正值时回退默认杯数（镜像 Lucent `USER_SETTINGS_DEFAULTS.waterTargetCount`，即默认 8 杯 × 250 ml = 2000 ml，与旧硬编码值一致），保证目标恒 > 0（防御进度条除零），进度卡不因设置不可用而崩溃或消失。兜底来源在代码注释中注明。
  - `recordWaterMlPerCount` 常量镜像 Lucent `WATER_TARGET_ML_PER_COUNT`（与 Today 概览 `TodayWaterSummary.waterMlPerTargetCount` 同源）。

### 展示层

- `lib/features/record/presentation/pages/detail.dart`：移除 `_waterDailyTargetMl` 常量；water 进度卡目标值改为 `recordWaterTargetCountProvider` 读取的杯数 × `recordWaterMlPerCount`。仅 water 记录且当日有 ml 合计时 watch 设置，其它记录类型不触发设置读取。进度文案仍传 ml 值，进度条 `value` 用新目标计算。

### 测试

- `test/record/detail_page_test.dart`：既有「550 / 2000 ml」用例不 override 设置，走默认兜底路径仍通过；新增用例 override `userSettingsRepositoryProvider`（`waterTargetCount` 设为非默认值）断言进度文案与进度条 `value` 均按 `waterTargetCount × 250` 计算。新增 `_FakeUserSettingsRepository`（模式参照 `test/today/repository_test.dart`），新增用例不打网络。
- 审查 P2 修复：`water_target.dart` 与 `detail_page_test.dart` 经 `dart format` 规范化；`_FakeUserSettingsRepository` 增加 `throwOnGet` 变体并新增「settings 读取失败 → 兜底默认目标（8×250=2000 ml）」显式用例，钉住失败兜底契约（此前仅靠真实 repo 在测试环境失败的隐式路径）。

### 文档

- `docs/00-current/Active_UI_Record.md`：「阶段 8 详情页增强」中 water 卡目标值表述由固定 ml 改为「目标值读 user-settings（waterTargetCount×250）」；「Last updated」行补充 P1-2（front-matter `updated` 维持 2026-08-18）。
- `plans/2026-08-16-record-remediation-plan.md`：删除 P1-2 小节；「五、本计划内执行顺序」删除第 1 条（P1-1、P1-2 均已完成），后续条目序号顺延。

### 验证

- `flutter analyze` 零告警。
- `flutter test test/record/detail_page_test.dart` 通过。
- `flutter test test/record` 全部通过。
- `dart run scripts/check_doc_coverage.dart --warning-only` 无必需文档缺失。

## feat(record): P2-1 详情页餐食分析摘要卡直接确认

依据 `plans/2026-08-16-record-remediation-plan.md` 的 P2-1（本节完成后已从计划文件删除）：确认餐食分析结果此前需进入编辑页勾选「确认当前结果」；本项让详情页摘要卡在未确认态直接提供「确认」按钮，复用现有 PATCH `analysisStatus='confirmed'` 链路（后端 `isMealAnalysisConfirmRequest` + `buildConfirmedMealPayload` + 模板学习不变），纯客户端。

### 展示层

- `lib/features/record/presentation/widgets/meal/analysis_summary_card.dart`：`MealAnalysisSummaryCard` 新增可选 `onConfirm`（`VoidCallback?`）与 `isConfirming`（默认 false），保持无状态展示组件可独立测试；仅当 `data.status == 'unconfirmed'` 且 `onConfirm != null` 时在卡片底部渲染轻量 outline 确认按钮（文案复用 `recordMealConfirmAction`），`isConfirming` 时按钮禁用并显示 `FCircularProgress` 加载指示（模式与 `edit_actions.dart` 一致）。
- `lib/features/record/presentation/pages/detail.dart`：`_RecordDetailBodyState` 新增 `_isConfirming` 本地布尔守卫与 `_confirmMealAnalysis()`——调用 `dailyRecordRepositoryProvider.update` 发送 `payload={'mealAnalysis':{'analysisStatus':'confirmed'}}`，成功后 `ref.invalidate(dailyRecordDetailProvider(...))`（badge 自动变为已确认）并弹通用保存成功 toast（`recordCreateSavedToast`），失败弹新增失败 toast 且状态不变；分析中/failed 分支保持不动。

### L10n

- `lib/l10n/src/record_zh.arb` / `record_en.arb` 新增 `recordMealConfirmFailedToast`（zh：确认失败，请稍后再试 / en：Failed to confirm, please try again）；已执行 `dart scripts/arb_tools.dart merge` + `flutter gen-l10n` 同步生成文件。成功文案复用既有 `recordCreateSavedToast`，未新增成功键。

### 测试

- `test/record/widgets/meal/analysis_summary_card_test.dart`：新增用例——confirmed 态不显示确认按钮；unconfirmed + onConfirm 显示按钮；onConfirm 为空不显示；isConfirming 时按钮禁用并显示 loading；点击按钮触发 `onConfirm` 回调。
- `test/record/detail_page_test.dart`：`_FakeRepo` 支持记录 `update` 调用（`lastUpdateId`/`lastUpdateInput`）并在成功后把返回与后续 `get` 的 `analysisStatus` 切换为 confirmed，新增 `updateThrows` 失败变体；新增用例——未确认 meal 详情显示确认按钮、点击后 `update` 收到含 `analysisStatus='confirmed'` 的 payload 且详情重载后按钮消失、badge 显示已确认；失败时按钮仍在且弹出失败 toast。

### 文档

- `docs/00-current/Active_UI_Record.md`：「阶段 8 详情页增强」补充餐食分析摘要卡直接确认说明；Last updated 行补充 P2-1（front-matter `updated` 维持 2026-08-18）。
- `docs/02-reference/Localization.md`：记录新增键 `recordMealConfirmFailedToast`；Last updated 行同步。
- `plans/2026-08-16-record-remediation-plan.md`：删除 P2-1 小节；「五、本计划内执行顺序」第 1 条更新为仅剩 P2-2、P2-5。

### 验证

- `flutter analyze` 零告警。
- `flutter test test/record/detail_page_test.dart` 通过。
- `flutter test test/record/widgets/meal/analysis_summary_card_test.dart` 通过。
- `flutter test test/record` 全部通过。
- `dart run scripts/check_doc_coverage.dart --warning-only` 无必需文档缺失。

## fix(record): P2-1 审查 P1 修复（DataChangeBus 补发 dailyRecords）

P2-1（详情页餐食分析摘要卡直接确认）审查发现 1 个 P1：`_confirmMealAnalysis()` 成功路径只 `ref.invalidate(dailyRecordDetailProvider(...))`，未 emit `DataChangeTopic.dailyRecords`。record 页 `recordDashboard` 是 keepAlive provider，仅在 DataChangeBus version 变化时重拉；时间线 meal badge 由实体级 `mealAnalysisStatus` 派生，导致详情页确认成功后返回记录页该条目「估算」badge 残留，直到其它刷新事件。

### 修复

- `lib/features/record/presentation/pages/detail.dart`：`_confirmMealAnalysis()` 成功路径在 invalidate 之后补 `ref.read(dataChangeBusProvider.notifier).emit(DataChangeTopic.dailyRecords);`（与 `record_detail_actions.dart` deleteRecord 成功路径同构）；文件头补 `package:luminous/core/providers/data_change_bus.dart` import；方法 doc 同步说明广播行为。
- `test/record/detail_page_test.dart`：成功用例改用 `ProviderContainer` + `UncontrolledProviderScope`（断言模式参照 `test/today/presentation/widgets/sections/quick_actions_test.dart`），确认成功后断言 `container.read(dataChangeBusProvider)[DataChangeTopic.dailyRecords]` 非空且大于 0；失败用例不动。

### 验证

- `flutter analyze` 零告警。
- `flutter test test/record/detail_page_test.dart` 通过。
- `flutter test test/record/widgets/meal/analysis_summary_card_test.dart` 通过。
- `dart run scripts/check_doc_coverage.dart --warning-only` 无必需文档缺失。
