# 2026-08-05 增量审查修复

来源：`plans/Luminous-review-2026-08-04.md`（1 🔴 + 7 🟡，代码验证全部属实；审查文档随后删除）

## 🔴 严重

### 1. batch 记录失败静默吞异常

- `lib/features/record/presentation/quick_entry/medication_flow.dart`
  - `recordConfirmedSelection` 中 `catch (_)` 改为 `catch (e, st)`，添加 `appTalker.error` 记录失败的药品名称、medicineId 和错误信息。
  - `MedicationQuickBatchResult` 新增 `errors` 字段（`Map<String, String>`），按 choice id 携带失败原因供 UI 展示。

## 🟡 警告

### 2. 重复代码：FilterChip 症状选择逻辑

- `lib/features/record/presentation/widgets/shared/copy.dart`
  - 新增 `toggleSymptomChoice` 公共函数，封装"构建 allChoices → 创建 Set → add/remove → 返回 List"逻辑。
- `lib/features/record/presentation/pages/quick_entry_settings.dart`
  - FilterChip `onSelected` 回调改用 `toggleSymptomChoice`。
- `lib/features/record/presentation/widgets/dialogs/quick_type_settings_dialog.dart`
  - 同上，`_SymptomSettings` 的 `onSelected` 回调改用 `toggleSymptomChoice`。

### 3. 重复代码：`_severityLabel` 方法

- `lib/features/record/presentation/widgets/shared/copy.dart`
  - 新增 `symptomSeverityLabel` 公共函数。
- `lib/features/record/presentation/pages/quick_entry_settings.dart` / `quick_type_settings_dialog.dart`
  - 移除各自的私有 `_severityLabel` 方法，改用公共函数。

### 4. 重复代码：睡眠时长选项列表

- `lib/features/record/domain/constants/fast_entry_choices.dart`
  - `_sleepDurationOptions` 重命名为公开 `kSleepDurationOptions`。
- `lib/features/record/presentation/pages/quick_entry_settings.dart`
  - 内联 `[360, 420, 480, 540]` 改用 `kSleepDurationOptions`。
- `lib/features/record/presentation/widgets/dialogs/quick_type_settings_dialog.dart`
  - `_durationOptions` 改用 `kSleepDurationOptions`。

### 6. 魔法数字：用药提醒时间窗口

- `lib/features/record/presentation/quick_entry/medication_flow.dart`
  - 提取 `kMedicationNearbyWindowBefore`（30 分钟）和 `kMedicationNearbyWindowAfter`（2 小时）命名常量，替代 `Duration(minutes: 30)` / `Duration(hours: 2)` 裸值。

### 7. `MedicationQuickBatchResult` 不携带错误信息

- 见 #1，`errors` 字段已添加。

### 8. 死代码：`_ruleText` 的 `water` 分支

- `lib/features/record/presentation/widgets/dialogs/quick_type_settings_dialog.dart`
  - 移除 `_ruleText` 中不可达的 `RecordEntryType.water` 分支（`water` 类型在 `build` 方法中走 `_WaterSettings`，不会落入 `else`）。

## 测试

- `test/record/quick_entry/medication_flow_test.dart`
  - 新增 `failed markDose records error in result and continues processing` 用例，验证部分失败时 succeeded/failed/errors 正确填充。
