# 2026-08-06 全仓库代码审查修复

来源：`plans/Luminous-review-2026-08-06.md`（7 🔴 + 9 🟡，经代码验证后实际修复 6 项严重 + 3 项警告，其余已修复或误报）

## 🔴 严重

### 1. `response.data!` 强制解包 — 运行时崩溃风险

- `lib/features/auth/data/datasources/auth.dart`
  - 新增 `_requireBody<T>(T? body)` 私有方法，对 null 响应体抛出 `StateError` 而非 `NullPointerException`。
  - 全文件 20 处 `response.data!.data` 替换为 `_requireBody(response.data).data`。

### 2. Enum 解析无 `orElse` — `StateError` 崩溃风险

- `lib/features/record/data/datasources/record.dart`
  - `_parseKind` 添加 `orElse: () => DailyRecordKind.note` 回退。
- `lib/features/medicine/data/datasources/dose_log_remote.dart`
  - `_parseStatus` 添加 `orElse: () => DoseLogStatus.planned` 回退。
- 注：审查列出的 10 处中，8 处已有 `orElse`，仅上述 2 处实际缺失。

### 3. `selectedResult` getter 使用 `firstWhere` 无 `orElse`

- `lib/features/search/domain/entities/entities.dart`
  - `selectedResult` 返回类型从 `MedicineSearchResult` 改为 `MedicineSearchResult?`。
  - 使用 `firstWhereOrNull` 替代 `firstWhere`，避免 `StateError`。
- `test/search/domain/entities/entities_test.dart`
  - 测试用例更新：匹配时使用 `!` 断言，不匹配时期望返回 `null` 而非抛出 `StateError`。

### 4. `catch (_)` 静默吞掉关键错误 — 10 处

以下文件的 `catch (_)` 改为 `catch (e, st)` 或 `catch (e)`，并通过 `appTalker` 记录错误详情：

- `lib/features/record/presentation/quick_entry/symptom_flow.dart` — `recordBatch` 批量记录失败日志
- `lib/features/record/presentation/services/quick_entry_executor.dart` — 水量记录失败 + 撤销失败日志
- `lib/features/record/presentation/widgets/dialogs/meal_confirmation.dart` — 饮食确认保存失败日志
- `lib/features/report/presentation/utils/pdf_download.dart` — PDF 下载失败日志
- `lib/features/today/presentation/pages/page.dart` — 仪表板刷新失败日志
- `lib/features/mine/presentation/widgets/sections/archive.dart` — 健康档案加载失败日志
- `lib/features/auth/presentation/widgets/shared/oauth_panels.dart` — Apple 登录检测失败日志
- `lib/features/settings/presentation/pages/about.dart` — 版本检查失败日志
- `lib/features/legal/data/repositories/lucent.dart` — 法律文档 fallback asset 加载失败日志
- 注：`dio_client.dart:198` 的 `catch (_)` 是 `Sentry.isEnabled` 布尔检查的合法保护，保留不动。
- 注：`medication_flow.dart:195` 的 `catch (e, st)` 在 8-05 修复中已添加，跳过。

### 5-7. `.first` 空保护 — 经验证已有保护

- `lib/features/medicine/presentation/pages/reminder/edit.dart` — `existing.first` 在 `isNotEmpty` 检查内；`activeMedicines.first` 在 `isEmpty` 检查后。已安全，跳过。
- `lib/features/mine/presentation/widgets/sections/account_hero.dart` — `gaps.first` 在 `gaps.length == 1` 条件内。已安全，跳过。

## 🟡 警告

### 9. `parseTimeOfDay` 重复实现

- `lib/features/record/presentation/quick_entry/sleep_flow.dart`
  - 私有方法 `_parseHourMinute` 删除，改用 `date_time_formatters.dart` 中的公共 `parseRecordTime`。
- 注：`medication_flow.dart` 中不存在审查所述的 `_parseTime`，可能为旧版本残留。

### 12. `firstWhere` 在搜索领域无安全回退

- `lib/features/search/presentation/pages/page.dart`
  - `firstWhere(orElse: () => ...last)` 改为 `firstWhereOrNull` + null 检查，避免空列表时 `.last` 崩溃。
- `lib/features/search/presentation/providers/medicine_search.dart`
  - `any` + `firstWhere` 竞态条件改为 `firstWhereOrNull` + null 检查。

### 10/13/14/16 — 经验证已修复或误报

- 🟡#10 用药时间窗口常量化：`kMedicationNearbyWindowBefore` / `kMedicationNearbyWindowAfter` 已提取。跳过。
- 🟡#13 `print()` 在生产代码中：`health_sync.dart` 已使用 `appTalker`。跳过。
- 🟡#14 Timer 未取消：`medicine_search.dart` 已有 `_debounceTimer?.cancel()`。跳过。
- 🟡#16 `moodEntries.first` 排序：时间线按 `occurredTime ?? occurredAt` 倒序排列，`first` 即最新。跳过。

## 验证

- `flutter analyze`：0 issues
- `dart run scripts/check_doc_coverage.dart --warning-only`：通过（warning-only）

---

# 2026-08-06 详情页 / 编辑页区分度精进（第一批）

来源：`plans/2026-08-06-record-detail-edit-differentiation.md`（第一批：hero 头部 + 编辑主操作 + dirty 提示条）

## 详情页 `lib/features/record/presentation/pages/detail.dart`

- 顶栏编辑图标按钮移除，页面明确为只读展示态。
- 头部升级为 Hero：`_KindIcon` 替换为 `_KindHeroAvatar`（52px 圆形 `FAvatar`），
  复用 `RecordDashboard.quickActionFor` + `resolveQuickActionIcon`（与快速记录面板图标同源，
  含用户自定义图标）；无 quick action 的类型（vitals / activity）回退 primary 样式。
- 标题旁新增 `_SourceBadge` 来源徽章（手动 / 本地 / AI / 导入，复用 `_sourceLabel`）；
  信息行中原来的「来源」行移除，避免与徽章重复展示（复制摘要仍含来源）。
- 底部操作重排：新增 `record-detail-edit-action` primary 主按钮「编辑这条记录」（复用 `editRecord`），
  上一条 / 下一条、复制摘要降为 ghost，删除 destructive 置底。

## 编辑页 `lib/features/record/presentation/pages/edit.dart`

- 表单顶部新增 `_EditStatusHint` 提示条：默认显示「修改后点击保存生效」（muted），
  dirty 时切换为警告色「有未保存的更改」（`SemanticColor.warning`），与返回丢弃确认形成闭环。

## l10n

- `lib/l10n/src/record_zh.arb` / `record_en.arb` 新增：`recordDetailEditAction`、
  `recordEditUnsavedHint`、`recordEditUnsavedWarning`；已 merge + gen-l10n。

## 测试

- `test/record/detail_page_test.dart`：断言 `record-detail-edit-action` 与「编辑这条记录」。
- `test/record/edit_page_test.dart`：断言 save-hint / unsaved-hint 提示条随 dirty 切换。

## 验证

- `flutter analyze`：0 issues
- `flutter test test/record/`：全部通过

---
