# Migration Log — 2026-08-16

## fix(security): 安全提升异常可见化并拒绝非法过期时间兜底

- `security_elevation.dart` `verify()`:裸 `on Object` 吞错拆分为三层——可预期失败(DioException:网络/HTTP 错误及业务码非 0 经 EnvelopeInterceptor 转换;LucentApiException:拦截器未生效的调用路径)直接返回 false 不产生噪音日志;意外异常(TypeError、空响应、解析错误等)用 `appTalker.error` 记录错误与堆栈后返回 false,消除生产"验证莫名失败"调试黑洞。
- 响应解包改用 `requireData` + 显式 `ensureEnvelopeSuccess` 校验业务码,替换裸 `response.data!`:拦截器未生效时,失败响应不再被误当作成功并签发提升令牌。
- `expiresAt` 解析失败不再回退"当前时间 + 15 分钟":记录警告日志并拒绝设置提升令牌、返回 false,杜绝服务端异常或被篡改的响应人为延长安全提升状态。
- 新增 `test/core/providers/security_elevation_test.dart`:覆盖成功路径、非法 expiresAt 拒绝、DioException 降级、业务码非 0、意外异常记录日志等用例。
- 验证:`flutter analyze` 无告警;security_elevation 与 user_settings_controller 相关测试全部通过。

## fix(auth): 刷新失败区分认证失效与临时故障,保留会话避免误登出

- `auth_interceptor.dart` `_doRefresh` 返回类型改为密封结果 `_RefreshOutcome`(Success/AuthFailure/TransientFailure),不再用 null 掩盖失败原因;刷新令牌被拒(刷新端点 401/403 或业务码 refreshTokenInvalid/tokenExpired)判为认证失效,网络连接类/超时/5xx/空响应体/缺 accessToken 判为临时故障。
- `onError` 只在认证失效时清会话并通知会话过期;临时故障保留会话、原 401 走常规错误路径——网络抖动或服务端瞬时故障不再强制登出。重试请求再次 401 仍判定为认证失效并清会话。
- `_shouldRefresh` 由"仅 tokenExpired 可刷新"改为"除明确不可刷新业务码(refreshTokenInvalid/loginRateLimited/wrongPassword)外均尝试刷新",tokenInvalid 类 401 也有机会经刷新恢复会话。
- 测试:改写"401 非 tokenExpired 不清会话"用例为新语义,新增网络错误/5xx 刷新失败保留会话、不可刷新业务码直接清会话等用例。
- 验证:`flutter analyze` 无告警,auth_interceptor 测试全部通过。

## fix(notification): 补全信封校验并降级未读数获取失败

- `notification/data/repositories/lucent.dart` `markAllAsRead` 补上 `ensureEnvelopeSuccess` 业务码校验(响应为 UnreadCountResponseDto 信封),与 findAll/findOne/getUnreadCount 一致,业务失败不再被静默当成成功。
- `delete` 生成客户端建模为 Response<void>(后端 204 无响应体):运行时对象实为 Response<Object>,转回后若响应体意外携带带 code 的信封则按同款模式校验业务码;空响应体视为成功(无信封可校验)。
- `getUnreadCount` 增加优雅降级:任何失败(业务码非 0、网络错误等)记录警告日志并返回 0,未读数徽章不再因单次失败进入错误态;接口签名保持 `Future<int>`。
- 测试:未读数非 0 业务码/网络错误均降级返回 0;新增 markAllAsRead/delete 业务码校验、delete 空响应体成功用例。
- 验证:`flutter analyze` 无告警,notification 测试全部通过。

## fix(robustness): OCR 映射与缓存解码防御,提醒规划与提案失败降级

- `scan/domain/services/medicine_ocr_extractor.dart`:双向混淆映射(0↔O、8↔B、1↔l 等)改为单向 `_ocrToCorrect` 纠正映射——key 不会出现在 value 中,重复应用幂等,不再存在二次应用被翻转回误读的循环地雷;注释明确禁止反向复用。
- `today/data/utils/suggestion_json_codec.dart` `_asMap`:非 String key(旧缓存迁移遗留的 int key 等)直接返回 null 走调用方降级,不再 `toString()` 静默转换掩盖 schema 问题(JSON 标准只允许 String key)。
- `medicine/domain/services/reminder_notification_planner.dart`:通知 ID 线性探测耗尽抛出的 StateError 在 `plan()` 内捕获,跳过该条提醒并记录日志、返回已分配通知,上层调度不再因单条异常整体崩溃。
- `assistant/presentation/pages/page.dart` `handleConfirmProposal`:失败分支改用统一 `userMessageFromError`(带本地化兜底),不再假设 `error.message` 非空。
- 测试:OCR 单向纠正语义(0/O、1/l 不再翻回)、提醒规划器 ID 探测耗尽不抛异常用例;codec/助理页既有用例回归。
- 验证:`flutter analyze` 无告警,scan/medicine/today/assistant 相关测试全部通过。

## fix(health_data): 健康同步预览指标标题本地化并统一身高类型

- `health_sync.dart` `_MetricsPreviewSection._metricTitle` 硬编码中文指标标题全部迁入 l10n:新增 `healthSyncMetricTitle*` 模板键(value/unit 等占位符,zh/en 双语,文案与既有硬编码显示一致);睡眠时长 h/m 拼接逻辑保留在 Dart(`_sleepDurationText`),仅把格式化字符串传入占位符;`_metricSubtitle` 保持不变。
- `HealthMetricType.height` 与 UI 一致性:平台导入链路完整支持身高(datasource `_toHealthDataTypes` 映射 `HealthDataType.HEIGHT`、mapper 双向映射与单位 cm、`DailyRecordKind.vital`、payload `vitalType: 'height'`、repository `_allSupportedTypes` 均含 height,且无按枚举名序列化依赖),决策为保留枚举并把 height 补入 `_MetricTypeSection` 可选项列表,与 `_metricLabel`/`_metricTitle` 既有 height 分支一致。
- 测试:`health_sync_page_test.dart` 新增"预览渲染本地化指标标题"用例(心率/血压/睡眠/身高/饮水各模板),并在指标类型列表用例断言身高按钮出现;height 加入列表后既有用例回归。
- 验证:`flutter analyze` 无告警;health_data 测试全部通过;`dart run scripts/check_doc_coverage.dart --warning-only` 通过。

## feat(medicine): 本地通知回执与推送回退能力上报接线

- `LocalNotificationGateway`：`schedule` 改为返回 `Future<bool>`（仅真正调度成功为 true，任何异常/不支持平台返回 false）；初始化接线 `onDidReceiveNotificationResponse` 与 `getNotificationAppLaunchDetails()`（冷启动点击），新增 `tapEvents` broadcast Stream（pending 缓冲 + 首个 listener 补发 launch details，参照 `jpush_gateway` 模式）；新增 `getActiveNotifications()`（异常返回空列表）。web/desktop 保持 no-op 安全。
- `ensureInitialized` 并发安全：首个调用在途时，后续调用者复用同一初始化 Future（memoize `_initializing`），避免误报 unavailable；插件平台绑定未注册（如测试环境）抛出的内部 `LateError`（toString 以 `LateInitializationError` 开头，Dart 3.12 无公开类型）在 initialize/launch details/cancel/schedule/active notifications 各读取路径统一兜底为不可用，不再逃逸为未处理异步错误。
- 通知负载：`MedicineReminderNotificationPlanner` 的 payload 从 `reminder.id` 改为 JSON `{"reminderId","date","time"}`，date/time 取提醒逻辑排程时刻（advanceMinutes 偏移不影响，DND 跳过项不产出）；新增 `ReminderNotificationPayload.tryParse` 负载模型（date `YYYY-MM-DD`、time `HH:mm` 严格形态正则，越界时间拒绝）。
- 仓储/数据源：`ReminderRepository` 新增 `reportLocalReceipt` 与 `reportLocalCapability`；远端实现走 raw Dio + `LucentApiPaths` 新增常量（`POST /user/reminder-deliveries/receipts` 幂等、`PUT /user/reminder-deliveries/local-capability`）。
- 协调器：`resync` 统计全部 `gateway.schedule` 返回值（全部成功才算成功）；resync 后按 remindersEnabled / 初始化结果 / 调度结果上报 `disabled` / `unavailable` / `active` 能力，fire-and-forget 且未登录不上报、失败记日志。
- 回执上报器：新增 `medicineReminderDeliveryReporterProvider`（keepAlive），订阅 `tapEvents` + ensureInitialized 后补扫活动通知，按 `reminderId|date|time` 会话去重幂等上报，失败只记日志不抛；`bootstrap.dart` 常驻 watch。
- l10n：新增 `medicineReminderDeliveryChannelInApp`（应用内通知 / In-app），`deliveryChannelLabel` 映射 `in_app`；Localization.md 记录所有权。
- 生成客户端：`scripts/bootstrap_generated_sources.dart` 增加 ReminderDeliveries 生成组（receipts / local-capability DTO 模型），重新生成 `generated/lucent_api`（`deserialize.dart` 引用新模型，缺失即编译失败）。
- 文档：`Mock_Or_Deferred.md` 投递历史口径、`TODO.md` 删除 worker 行、`Next_Plan.md` JPush 条件回退口径、`Runtime_Snapshot.md` 回执与能力上报链路、`Active_UI_Medicine.md` 投递历史、`Lucent_Contract_Snapshot.md` 新增两接口合同。
- 验证：`flutter analyze` 零告警；medicine 与 core/notifications 相关测试全部通过；全量测试除既有 `dio_client_test.dart` 401 用例（refactor 分支既有失败，与本次无关，未修改）外全部通过。

## F-3 客户端 appKey 配置核对

计划 `plans/2026-08-16-platform-notification-crosscutting-plan.md` P1#3 客户端部分（纯核对 + 配置链最小补齐；不提交 git，由主控审查后提交）。

核对范围与结论：

- Dart 侧：`lib/core/push/jpush_gateway.dart` 单例 `appKey` 来自 `String.fromEnvironment('JPUSH_APP_KEY')`、`production: kReleaseMode`、`isConfigured = appKey 非空 && 移动平台`；构建注入方式为 `--dart-define=JPUSH_APP_KEY`。仓库内注入点/文档描述点：`android/app/build.gradle.kts`（`-PJPUSH_APP_KEY` 或环境变量）、`.vscode/launch.json`（`--dart-define-from-file=.env`）、`.github/workflows/luminous-cd.yml`（Web 构建，无需 JPush）、`docs/00-current/Runtime_Snapshot.md`（JPush 配置段）。`.env` / `.env.example` 不含真实 AppKey，符合"密钥不入库"。
- Android/iOS 原生侧：iOS `Runner.entitlements` 使用 `$(APS_ENVIRONMENT)`，Debug/Profile = `development`、Release = `production`，与文档一致。**发现缺口**：`build.gradle.kts` 设置了 `JPUSH_APPKEY`/`JPUSH_CHANNEL` manifest placeholders，但 `AndroidManifest.xml` 从未消费 `${JPUSH_APPKEY}`/`${JPUSH_CHANNEL}`，Gradle→manifest 注入链断在消费端（JPush SDK 必填 meta-data，且 `:pushcore` 独立进程依赖它）。已补齐两个 meta-data 项（仍由构建期注入，不写真实密钥）。
- 后端语义：客户端 `isConfigured`（appKey 非空 + 移动平台）与服务端 `JpushPushProvider.isConfigured`（appKey + masterSecret 非空）无冲突；alias = 用户 UUID 双侧一致，服务端按 `audience.alias` 发 JPush。
- 文档同步：`Runtime_Snapshot.md` JPush 段补充注入链完整描述与"缺省静默禁用"口径；`README.md` 新增 "Mobile JPush builds need" 发布构建说明。
- 未做任何运行时行为改动；未写真实密钥。
- 验证：`dart run scripts/check_doc_coverage.dart --warning-only` 通过（无映射代码变更；Android manifest 不在 doc-map 规则内）。

## docs(plans): 通知横切计划已完成小节清理

- 按「Finishing a plan: delete the entire section」规则清理 `plans/2026-08-16-platform-notification-crosscutting-plan.md` 已完成小节：删除 P1#2（F-4 提醒投递记录三通道落库）、P1#3（F-3 JPush 密钥补齐）、P2#5（F-5 附带小瑕疵修复）三节，删除后重编号（P2 剩 F-6 → 2）。
- 计划文件保留剩余范围（均 0.1.0 后）：F-7 环境快照真实化、F-6 health_sync 自动同步执行器；第一节目标改为「完成两项改造」；第二节 F-2/F-5 行改为已完成口径；第四节跨计划引用改为「已完成，方案与实现见 Lucent ADR-0013」口径，上线密钥 checklist 只保留 F-7 高德；第五节执行顺序仅剩 F-7/F-6。
- `plans/README.md` 第 1 条计划描述同步为剩余范围（均 0.1.0 后），并标注投递三通道落库、JPush 密钥已完成。
- 交叉引用：medicine 计划三处指向「投递落库一节 / F-9 投递落库」的失效引用改为 ADR-0013 已完成口径；today/assistant/mine-settings 计划引用「天气真实化」「health_sync」小节的描述仍指向保留小节，无需改动。
- 验证：`dart run scripts/check_doc_coverage.dart --warning-only` 与 `dart run scripts/check_doc_links.dart --changed` 通过。

## F-13 告警纯派生 + F-16/17/18 死代码归档标注

用药管理改造计划任务 1（P0）实施。

- F-13：`mobile_dashboard_view.dart` 的告警派生改为直接传可空结果
  `workspace.riskCheckRecords?.bestRecord?.result`，删除 `?? const MedicineRiskCheckResult()`
  兜底——bestRecord 为 null 时 `medicineAlertsFromRiskCheck` 对 null 已返回 `const []`，
  不再用空结果伪造「全部通过」告警（未知不得映射为 0）。
- F-16：`safety_tips_remote.dart` / `providers/safety_tips.dart` / `entities/safety_tip.dart` /
  `utils/safety_tip_style.dart` 加文件头 TODO 归档注释，标注无 UI 消费方（死代码保留），不改行为。
- F-17：`workspace.dart` 的 `promisePoints` / `alerts` / `metricDosesToday` 字段、`MedicinePromisePoint` 类、
  4 个 `promisePoint*` 枚举值；`lucent_workspace.dart` 的 `alerts: const []` 与 `_defaultPromisePoints()`；
  `copy.dart` 的 4 个 promisePoint* 映射分支——加历史占位 TODO 注释，不删字段/枚举/键；
  4 个 `medicinePromisePoint*` l10n 键保留不删不改。
- F-18：`dose_log.dart` 的 `missed` 枚举值、`log_panels.dart` 的 missed color/icon/label 分支
  加「主路径不产生该状态」注释，前后端保留历史兼容。
- 测试：`test/medicine/page_test.dart` 新增页面级用例——注入含 riskCheckRecords（interaction high +
  allergy medium + coverage issue）的 workspace 渲染 MedicinePage，断言 findings 派生告警标题与 `+N`
  溢出文本；另断言无风险记录时空卡片出现且不出现「全部通过」告警。
- 验证：`flutter analyze`、`flutter test test/medicine`、
  `dart run scripts/check_doc_coverage.dart --warning-only` 通过。
