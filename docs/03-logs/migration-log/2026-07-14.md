# Migration Log - 2026-07-14

## Luminous — 7-14 审查报告改写为 Bug 修复计划

- **目标**：将 7-14 增量代码审查报告改写为可执行的 Bug 修复计划文档。
- **背景**：`plans/Luminous-2026-07-14.md` 原为审查报告格式（含逐条审查意见、新功能评估、重复造轮子检查等）。用户要求确认问题后改写为 bug 修复计划。
- **核实结果**：逐一核实报告中 13 个问题的代码文件，确认 12 个为真实 bug/隐患，#10（命名重构后文件路径歧义）为设计意图非 bug。
- **变更内容**：
  - 将 `plans/Luminous-2026-07-14.md` 从审查报告格式改写为 Bug 修复计划格式，按 P0/P1/P2/P3 四个优先级分 Phase 组织，每个问题包含问题确认、修复步骤、验证标准。
  - 新建本日 migration log。

## Luminous — 7-14 Bug 修复计划执行

- **目标**：执行 `plans/Luminous-2026-07-14.md` 中的全部 12 个 bug 修复。
- **验证结果**：`flutter analyze` 零问题，`flutter test --exclude-tags golden` 2287 个测试全部通过。
- **变更内容**：

### Phase 1 — P0 严重问题

- **#1 Web 编译产物 gitignore**：`.gitignore` 添加 `web/drift_worker.js`、`.js.deps`、`.js.map`、`sqlite3.wasm` 排除规则；`git rm --cached` 移除已追踪文件；`.github/workflows/flutter-ci.yml` 添加 `dart compile js web/drift_worker.dart` 编译步骤。
- **#2 GoRouter redirect 崩溃风险**：`lib/app/router.dart` 全局 `final router` 变量改为 `@Riverpod(keepAlive: true) GoRouter appRouter(Ref ref)` provider，`redirect` 闭包捕获 `ref` 替代 `ProviderScope.containerOf(context)`；`lib/app/bootstrap.dart` 使用 `ref.watch(appRouterProvider)` 和 `ref.read(appRouterProvider).refresh()`；`test/app/router_test.dart`、`integration_test/support/e2e_test_helpers.dart`、`fullstack_e2e_helpers.dart`、`mine_e2e_test.dart` 等测试文件同步更新引用。

### Phase 2 — P1 警告问题

- **#3 SyncWorker Stream 竞态**：`lib/core/database/sync/sync_worker.dart` `start()` 从 `void` 改为 `Future<void> async`，`await _subscription?.cancel()` 后再 `listen`；`syncWorker` provider 中改为 `unawaited(worker.start())`。
- **#4 PendingSyncDao counter 实例变量**：`lib/core/database/daos/pending_sync_dao.dart` `_counter` 从实例变量改为 `static int _counter = 0;`。
- **#5 测试 enum.length 脆弱断言**：6 个测试文件（`risk_check_test.dart` 5 处、`workspace_test.dart` 1 处、`reminder_sound_preference_test.dart` 1 处、`suggestion_test.dart` 2 处、`dashboard_entity_test.dart` 2 处、`record/dashboard_entity_test.dart` 2 处）的 `expect(Enum.values.length, N)` 改为 `containsAll([...])` 断言。
- **#6 AppImageCompressor 静默吞错**：`lib/core/utils/image_compressor.dart` `catch (_)` 改为 `catch (e, st)` + `appTalker.warning('Image compression failed, using original: $e', e, st)`。

### Phase 3 — P2 警告问题

- **#7 DAO providers 非 keepAlive**：`lib/core/database/database_providers.dart` 6 个 DAO provider 从 `@riverpod` + `ref.watch` 改为 `@Riverpod(keepAlive: true)` + `ref.read`。
- **#8 arb_tools.dart 防误用注释**：`scripts/arb_tools.dart` 文件顶部添加 CLI-only 同步 IO 防误用注释。
- **#9 条件导入 ArkWeb 防御**：`lib/core/database/database.dart` 条件导入上方添加 ArkWeb 已知风险注释。

### Phase 4 — P3 建议问题

- **#11 NLP 串行保存**：`lib/features/record/presentation/controllers/nlp_controller.dart` `_saveCandidates` 从 for 循环串行 `await` 改为 `Future.wait` 并行保存。
- **#12 CI timeout 偏紧**：`.github/workflows/flutter-ci.yml` `timeout-minutes` 从 25 改为 35。
- **#13 schemaVersion 迁移模板**：`lib/core/database/database.dart` `onUpgrade` 注释扩展为完整迁移模板（含 `addColumn` 示例和版本号范围策略说明）。
