# Migration Log - 2026-07-13

## Luminous — 7-12 审查回查文档关闭

- **目标**：关闭 7-12 审查报告中遗留的唯一未修复项（枚举命名重复 `SendVerificationCodeDtoSceneScene`），删除审查计划文档。
- **背景**：7-12 审查报告中 5 个 🟡 级别问题，4 个已在 57e7f498 / 4e61b2d0 中修复，唯一遗留的枚举命名重复问题经评估为 `openapi_retrofit_generator` 的系统性生成行为（30+ 个内联枚举均受影响），生成器配置无法控制，风险极低，决定不修复、关闭问题。
- **变更内容**：
  - 删除 `plans/luminous-review-2026-07-13.md`（审查回查计划文档，所有问题已关闭）。
- **文档更新**：
  - 新建本日 migration log。

## Luminous — 依赖清理与图片压缩接入

- **目标**：移除无用依赖 `cupertino_icons`，引入 `flutter_image_compress` 并在图片上传场景接入压缩逻辑。
- **评估纠正**：原计划同时移除 `collection`，但搜索发现 11 个文件直接 `import 'package:collection/collection.dart'`（使用 `firstOrNull` 等），保留不动。
- **变更内容**：
  - `pubspec.yaml`：删除 `cupertino_icons: ^1.0.8`（零引用，`uses-material-design: false` + forui `FLucideIcons` 全面替代）；新增 `flutter_image_compress: ^2.4.0`。
  - 新建 `lib/core/utils/image_compressor.dart`：`AppImageCompressor` 静态工具类，提供 `compressForUpload`（1280px / JPEG 85%）和 `compressForAiRecognition`（1920px / JPEG 90%）两个预设；Web 平台 fallback 返回原始 bytes；任何压缩异常 fallback 返回原始 bytes 不阻塞用户。
  - `lib/features/record/presentation/pages/create.dart`：`onPickImage()` 在 `readAsBytes()` 后插入 `AppImageCompressor.compressForUpload()`，`contentType` 固定为 `image/jpeg`。
  - `lib/features/record/presentation/pages/edit.dart`：同上。
  - `lib/features/scan/presentation/pages/box_scan.dart`：AI 识别路径在 `readAsBytes()` 后插入 `AppImageCompressor.compressForAiRecognition()`；OCR 路径不压缩（ML Kit 直接处理文件路径，压缩会降低识别精度）。
- **验证**：`flutter pub get` 成功，`flutter analyze` 零问题。

## Luminous — 桌面端 UI 优化

- **目标**：解决桌面端四个核心问题——页面布局不一致、动画不流畅、侧边栏子项设计不合理、右侧内容区视觉不佳。
- **变更内容**：
  - **侧边栏精简**：移除 `_DesktopSidebar` 中所有 `FSidebarItem.children`（Settings 9 子项、Record 1 子项、Medicine 3 子项、Mine 4 子项），侧边栏只保留 5 个扁平 Tab 项 + Settings + Help。移除 `_DesktopBreadcrumb` 面包屑组件及 ShellPage 桌面端的 `SafeArea`/`Padding`/`Column` 包裹。
  - **动画优化**：将 5 个 Tab 路由的 `NoTransitionPage` 改为 150ms 轻量 `FadeTransition`（无淡出）；将 `ShellDeferredContent` 默认 placeholder 从骨架屏改为与 `DesktopTabShell` 背景色一致的纯色 `ColoredBox`，消除骨架→内容闪烁。
  - **新建 `DesktopTabShell`**：`lib/features/shell/presentation/desktop_tab_shell.dart`，统一桌面端 Tab 页面外壳——`AppTopBar`（title/subtitle/trailing/bottom）+ 内容区（maxWidth 约束 + 统一 padding + 可选 RefreshIndicator + PageStorageKey）+ muted 背景色。
  - **逐页迁移**：
    - TodayPage：新增 `ShellDeferredContent` 包裹 + `DesktopTabShell`（`scrollable: false`）。
    - RecordPage：桌面端移除 `FScaffold`/`ResponsiveContentFrame`/`SafeArea`，使用 `DesktopTabShell`。
    - MedicinePage：删除 `_MedicineDesktopShell`，所有状态分支使用 `DesktopTabShell`；桌面端新增搜索栏 `_MedicineMobileSearchBar` 和 trailing 按钮（`_MedicineSafeGuardPill` + `_MedicineNotificationButton`），修复桌面端功能缺失。
    - ReportPage：删除 `_ReportDesktopShell`；提取 `ReportActionBar` 独立组件（从 `ReportTopBar.bottom` 拆出）；使用 `DesktopTabShell`（title + subtitle + trailing ReportPeriodPill + bottom ReportActionBar）。
    - MinePage：删除 `_MineDesktopShell`；`_IconActionButton` 改为公共 `IconActionButton`；使用 `DesktopTabShell`（trailing = 通知按钮 + 设置按钮 + onRefresh）。
  - **ARB 清理**：移除 16 个不再使用的 sidebar 子项 ARB key（`sidebarRecordCreate` / `sidebarMedicineSearch` / `sidebarMedicineRiskCheck` / `sidebarMedicineReminders` / `sidebarMineProfile` / `sidebarMineAllergy` / `sidebarMineCondition` / `sidebarMineMedicine` / `sidebarSettingsNotifications` / `sidebarSettingsTheme` / `sidebarSettingsLanguage` / `sidebarSettingsAccessibility` / `sidebarSettingsAi` / `sidebarSettingsExport` / `sidebarSettingsSecurityPin` / `sidebarSettingsAbout`），已 `grep` 确认无其他引用。
- **验证**：`flutter gen-l10n` 成功，`flutter analyze` 零问题，`flutter test` 2294 个测试全部通过。

## Luminous — 安全审查修复：GoRouter 全局重定向守卫

- **目标**：修复 2026-07-12 安全审查报告中 Luminous 唯一问题——GoRouter 缺少全局重定向守卫。
- **问题**：未认证用户可短暂访问受保护路由的 UI 框架（provider 完成认证检查前页面已渲染骨架）。
- **变更内容**：
  - `lib/app/router.dart`：GoRouter 添加 `redirect` 回调，通过 `ProviderScope.containerOf(context)` 读取 `authSessionProvider` 状态。恢复中（`isRestoring`）不跳转；未认证且不在 auth/public 路由 → 重定向 `/login`；已认证但在 auth 路由 → 重定向 `/`。auth 路由包括 `/login`、`/register`、`/forgot-password`；public 路由包括 `/legal`。
  - `lib/app/bootstrap.dart`：`ref.listen<AuthSessionState>(authSessionProvider, ...)` 回调中添加 `router.refresh()`，确保认证状态变化时自动触发 redirect 重新评估。同时修复 pre-existing BOM 字符问题。
- **验证**：`flutter analyze` 零问题。
