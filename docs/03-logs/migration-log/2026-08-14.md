# Migration Log — 2026-08-14

## Visit Summary and Product Measurement — 同步就诊摘要 API 合同

- Lucent 已完成 Workstream 2 Task 2-4：clinic summary 支持事件/日期范围 scope 与字段选择，share 改为持久化可撤销记录，公开读取失效 token 返回 404（原 410），新增 `DELETE /user/reports/clinic-summary/shares/:shareId`。
- 依跨项目合同重新生成 Flutter 客户端（`dart run scripts/bootstrap_generated_sources.dart`）：reports API 的 preview/share/export/preview-pdf 方法签名改为必传 `ClinicSummaryRequestDto`，新增 share 撤销方法与对应 DTO。
- 生成客户端尚未被现有调用点消费：`clinic_summary.dart` provider、预览对话框与相关测试仍使用旧签名，`flutter analyze` 暂时报 5 个 `missing_required_argument` 错误，归属 Workstream 2 Task 7/8 的客户端适配一并修复。

## Visit Summary and Product Measurement — 适配就诊摘要新请求合同

- `clinic_summary.dart` provider 与预览对话框的 preview/share 调用改为传空 `ClinicSummaryRequestDto`（字段全可选，服务端回退默认 last_30_days，行为与旧合同一致）。
- 测试夹具补齐 `ClinicSummaryDto` 新增必填字段（scopeLabel/start/end/selectedFields/coverage），provider 测试 mock 与 verify 同步携带请求参数。

## Visit Summary and Product Measurement — 补全就诊摘要生成与客户端适配

- bootstrap 脚本 `_reviewModels` 补充 7 个就诊摘要合同模型（RequestDto、Coverage/Entry、Profile、Dto、ShareScope、ShareResponse），并新增 `_inlineResponseModels`：Reports 导出接口的 201 内联响应模型（openapi-generator 7.x 过滤生成 `models=` 只接受命名 schema，内联模型由全量 supporting 输出以 snake_case 文件名复制，与既有 v1202 响应模型处理方式一致）。
- 重新生成后 `flutter analyze` 零问题，`flutter test test/report` 233 全绿；客户端调用点与夹具已按新合同适配（`ClinicSummaryRequestDto()` 空请求保持默认 last_30_days 语义，响应新增 scopeLabel/start/end/selectedFields/coverage 由夹具补齐）。

## Visit Summary and Product Measurement — 同步产品事件 API 合同

- Lucent Task 5 新增 `POST /api/v1/user/product-events`（批量上报，最多 50 条，白名单字段 + clientEventId 幂等）与 6 个 schema（ProductEventName/Surface/Result、UserDevicePlatform、CreateProductEventDto/BatchDto）。
- 依跨项目合同在 bootstrap 脚本 `_filteredClients` 追加 ProductEvents 过滤条目，重新生成客户端：新增 `product_events_api.dart` 与 6 个模型文件，barrel/deserialize 同步。客户端尚无消费方，`flutter analyze` 零问题。

## Visit Summary and Product Measurement — Task 7 产品事件类型与上报服务

- 新增 `lib/core/analytics/product_event.dart`：封闭 `ProductEvent` sealed union（suggestion_impression / review_opened / visit_summary_previewed / visit_summary_exported 四个 final 变体），无 `Map<String, dynamic>` 公共入口；每个变体只携带 typed 属性并内建 `toDto` 序列化到 `CreateProductEventDto`（appVersion/platform/occurredAt/clientEventId 由 service 注入），结构上无法表达症状/标题/备注/记录值/PDF URL/分享 token 等禁止字段。`kAllowlistedSuggestionRuleCodes` 镜像服务端 7 个建议规则码白名单。
- 新增 `lib/core/analytics/product_event_service.dart`：`ProductEventService` 先经生成的 `POST /user/product-events` 上报；DioException（离线/网络）时把 `dto.toJson()`（仅白名单属性键）入队 pending-sync 新 kind `product_event`，重试复用同一 `clientEventId`（服务端唯一约束幂等）。上报失败只记低敏日志、绝不抛给 UI。session 级去重：impression 按规则码、review_opened 全局各一次；preview/export 每次服务端响应各记一条（success/failure）。
- `lucentClientProvider` 暴露 `productEvents` getter：生成端 `LucentApi` 尚无 getter（ProductEvents 走过滤生成路径），用共享 Dio 直接构造 `ProductEventsApi`，不改生成客户端。
- 测试 `test/core/analytics/product_event_service_test.dart`（12 条）：DTO 字段、impression/review 去重、preview/export 不去重、离线入队 payload 精确键集（不含 eventStatus/metadata/自由文本）、重试 payload round-trip 保持 clientEventId、非 Dio 异常吞掉不入队。
- `docs/doc-map.yaml` 新增 `core-analytics` 覆盖规则（迁移日志 + Product_Safety_Privacy）。

## Visit Summary and Product Measurement — Task 7 建议卡可见曝光按会话去重上报

- `suggestion_primary_card.dart` 内新增 `_SuggestionImpressionTracker`：轻量可见性检测（无新依赖）——卡片 render box 与最近 `RenderAbstractViewport` 全局矩形相交判定进入可见区域；`Scrollable.position` 监听滚动重检；`TickerMode` 屏蔽非活动 shell 分支；本实例只报一次 + service 按规则码 session 去重，build/rebuild 不重复计数。规则码取卡片 `ruleId`。
- `test/today/suggestion_section_test.dart` 新增 3 条：可见一次/rebuild 不重报、视口外不报（滚动进入后报一次）、非白名单规则码走 widget→service 链路。

## Visit Summary and Product Measurement — Task 7 回顾呈现与摘要预览导出成功边界上报

- `report/presentation/pages/page.dart`：`ref.listen(reviewCurrentProvider)` 在 AsyncData 过渡（含确认无事件）且登录时上报 `review_opened`（surface=review），不在导航点击时记录；session 去重由 service 保证，refresh 重复呈现只产生一次网络事件。
- `clinic_summary_preview_dialog.dart`：preview 经 `ref.listen(clinicSummaryPreviewProvider)` 在服务端响应边界记录 `visit_summary_previewed`（surface=more），每次对话框呈现一条（riverpod 自动重试不重复计数）；`_downloadPdf` 按 `PdfDownloadResult` 记录 `visit_summary_exported`（success/empty/failed→failure），失败不得计为 exported。分享不走客户端事件（服务端 share_created/opened/revoked，Task 6）。
- `export_actions.dart`（导出流程实际位于 utils，计划文件清单中的 `sections/export.dart` 为纯展示组件、无法观察服务端结果）：`handleReportExportAction` 的 monthly/print 导出按 `runGuarded` 的 Success/Failure 记录 `visit_summary_exported`（surface=more，More sheet 与 legacy 导出卡同路径）。
- 测试：`test/report/page_test.dart` 新增 review_opened 4 条（呈现/失败不报/登出不报/refresh 再触发由 service 去重）；`test/report/clinic_summary_provider_test.dart` 新增对话框 3 条（preview success/failure、PDF 导出 failure）。

## Visit Summary and Product Measurement — Task 7 复审收敛测量边界

- `export_actions.dart`：`visit_summary_exported` 的 success 记录改为按请求实际状态收敛——HTTP 200 但 `dataExportUiStatusForRequest` 返回 idle/failed/unavailable 时记录 failure（与 `_handleExportResult` 的失败 toast 一致），requested/processing/completed/completedLinkMissing 才记 success；失败请求不再被计为 exported。
- `report/pages/page.dart`：review_opened 从 `ref.listen` 过渡监听改为 `_ReviewOpenedTracker`（ConsumerStatefulWidget 包住页面内容）。flutter_riverpod 3 会暂停 ticker-muted（隐藏 tab 分支）下 widget 的 watch 订阅并在恢复时重放最后事件，而 `ref.listen` 订阅不会被暂停——单纯加 TickerMode 门会漏掉「数据在隐藏时加载、用户随后切到回顾页」这一常见场景。tracker 用 watch 实例身份去重（每次取数完成一个新 AsyncData 实例，同一实例的 theme/筛选 rebuild 不重报），只在页面真正呈现的首个 build 记录；service 仍按 session 去重。登出态继续排除。
- `suggestion_primary_card.dart`：`_attachAndCheck` 改用 `Scrollable.maybeOf`，与 `_isVisibleInViewport` 的防御式风格一致。
- 测试：新增 `test/report/export_actions_test.dart`（4 条：failed/unavailable/抛错 → failure，completed → success）；`test/report/page_test.dart` 新增隐藏 tab 测试（ticker-muted 下数据加载不记录、恢复可见的首个呈现记录、可见刷新重报由 service 去重）。

## Visit Summary and Product Measurement — 同步分享列表 API 合同

- Lucent 新增 `GET /api/v1/user/reports/clinic-summary/shares`（当前用户分享列表：createdAt/expiresAt/revokedAt/accessCount/firstAccessedAt/lastAccessedAt/scope/selectedFields，永不返回 token），供分享管理 UI 使用。
- bootstrap 脚本 `_reviewModels` 补充 3 个列表 DTO，重新生成客户端；`flutter analyze` 零问题。

## Visit Summary and Product Measurement — Task 8 字段级隐私选择与分享复制撤销

- 预览弹窗新增字段级隐私选择面板（`clinic_summary_preview_dialog.dart` 内 `_FieldSelectionPanel`）：事件概况/症状变化/用药槽位/饮水/睡眠/备注六项 `FCheckbox`，默认只选五个非备注字段（自由文本备注默认不包含），最后一项不可取消（空选择被服务端拒绝）；切换即时重新请求预览。
- `clinicSummaryPreviewProvider` 改为按字段集合的 autoDispose family（`kClinicSummaryDefaultFields`/`kClinicSummaryAllFields` 常量导出）；preview 与 share-create 请求改走原始 Dio（`LucentApiPaths.clinicSummaryPreview`/`clinicSummaryShare`）：这两个接口的响应是 `{code,message,data}` 信封而生成客户端把 body 直接当裸 DTO 反序列化（生成端既有缺陷，preview 一直无法消费真实响应），且未选中的 section 键被服务端省略而生成的 `ClinicSummaryDto` 标记必填——provider 解信封后以空默认值补齐缺失 section 再反序列化，渲染以服务端回显的 `selectedFields` 为准。
- `ClinicSummaryContent` 各 section 改为按响应 `selectedFields` 渲染（空列表保持旧「全包含」语义）：未选字段即使有数据也不渲染（profile 随 event_overview 开关）。
- 预览 PDF 下载改 POST 携带字段选择（服务端预览 PDF 是 POST 接口，请求体带 scope + selectedFields）；`downloadAndSharePdf` 新增可选 `postBody`，公开分享页保持 GET 不变。
- 分享流程改为弹窗内三步：创建前确认（7 天有效 + 「链接持有者可查看」，无医生暗示文案）→ 创建后展示链接 + 复制（`Clipboard.setData` + toast）与撤销（`DELETE /shares/{shareId}`）→ 撤销后展示失效态；`clinicShareInFlightProvider` 语义保留。
- l10n：report 分片新增 `reportClinicSummaryField*`（6 项 + 标题 + 隐私提示）与 `reportShare*`（确认/已创建/复制/撤销/已撤销/失败）键；经 merge + gen-l10n 合并。
- 测试：`test/report/clinic_summary_provider_test.dart` 重写 preview 组（信封解包、缺 section 容忍、请求体只含所选字段、Dio 错误传播），新增对话框字段选择 3 条（六项默认/切换重取/最后一项不可取消）与分享流 2 条（创建前确认文案、创建→复制→撤销全链路含剪贴板与 DELETE 断言）；`clinic_summary_content_test.dart` 新增 selectedFields 门控 3 条；`TestForuiApp` 新增可选 `showToaster`（MaterialApp builder 内挂 FToaster，与生产 bootstrap 一致），默认关闭不影响既有测试。

## Visit Summary and Product Measurement — Task 8 分享管理面板

- 新增 `presentation/widgets/sheets/share_management.dart`：`showShareManagementSheet`（桌面 dialog / 移动 bottom sheet），列出 `GET /user/reports/clinic-summary/shares` 的分享记录——每行显示创建时间/到期时间/访问次数/最近访问（无访问显示「暂无访问」）、已撤销徽章 + 撤销时间，未撤销行提供撤销按钮；撤销成功后经 `clinicSummaryShareListProvider.notifier.revoke` 自动刷新列表；空列表与加载失败态（重试）齐备；不展示任何访问者身份信息（API 不提供，UI 也不推断）。
- `clinic_summary.dart` 新增 `clinicSummaryShareListProvider`（AsyncNotifier，authGuarded，revoke 后 invalidateSelf 重取）与 `revoke` 动作。
- More sheet 新增第五入口「分享管理」（`more-share-management`，警告色 share 图标）：`ReportMoreActionsSheet` 增加 `onShareManagement` 回调，`page.dart` 装配为登录守卫 → `showShareManagementSheet`。
- 测试：新增 `test/report/share_management_test.dart`（8 条：字段渲染/暂无访问/已撤销态/空态/撤销调用 DELETE 并刷新/无访问者身份/失败重试）；`more_actions_test.dart` 更新为五入口（新增入口断言与回调触发）。

## Visit Summary and Product Measurement — Task 8 复审修复：过敏元数据与 profile 占位

- 复审确认并修复两个问题：
  1. `_fillMissingSections` 的 profile 占位只给 `nickname`，但生成的 `ClinicSummaryProfileDto.fromJson` 要求 `nickname` 与 `sexAtBirth` 都在（`clinic_summary_profile_dto.g.dart` requiredKeys）——取消选择事件概况时 provider 抛 `CheckedFromJsonException`，弹窗显示加载失败。占位补上 `'sexAtBirth': ''`；新增回归测试（事件概况取消 → profile 键整体缺失 → provider 正常返回、占位不渲染）。
  2. 内容组件把过敏记录当作可取消的 section 门控渲染；服务端 `resolveSectionKeys` 现在恒把 `allergies` 纳入解析结果（过敏不是六个可选字段之一，属于与 findings/coverage 相同的元数据），响应回显的 `selectedFields` 与数据段都会重新包含过敏——客户端把 allergies 改为「非空即渲染」（不再受 `selectedFields` 门控），与元数据语义一致。
- 测试：`clinic_summary_provider_test.dart` 新增事件概况取消用例；`clinic_summary_content_test.dart` 门控用例改为断言 allergies 在 `selectedFields` 不含它时仍渲染。
- 全量验证：`flutter analyze` 零问题；`flutter test` 3115 passed / 1 skipped（含新增用例）；`dart run scripts/check_doc_coverage.dart --warning-only` 无缺失必选文档；`git diff --check` 通过。

## Visit Summary and Product Measurement — Task 8 复审收尾：分享列表刷新与字段选择锁定

- 分享创建成功后 `clinic_summary_preview_dialog.dart` 现在 `ref.invalidate(clinicSummaryShareListProvider)`：分享列表是 keepAlive 缓存，不失效的话创建后打开分享管理面板会看不到新分享。
- 字段选择面板新增 `enabled` 参数：确认步骤（confirm）保持可切换（影响正在创建的分享）；已创建/已撤销步骤锁定切换，避免预览在已展示的链接背后静默重取造成隐私误解。
- `_FieldToggle` 的 FCheckbox 补 `semanticsLabel`（register.dart 同款模式）：可见 label 是行内独立 Text，屏幕阅读器此前读不到勾选框含义。
- 分享管理面板：刷新顺序改为 hasValue 优先（revoke 触发 invalidateSelf 重取时保留旧列表、不闪 spinner）；撤销进行中状态从单槽 `_revokingShareId` 改为 `Set<String>`，不同行的撤销可并发，只有进行中的行显示 spinner。
- TODO.md：删除已完成的「Clinic Summary 字段级隐藏 UI 与隐私选择尚未实现」行；新增合同债条目——四个 section 键在 Lucent 响应合同中标记必填而服务端会省略未选键，客户端只能靠 `_fillMissingSections` 占位反序列化，持久修复是把四键改为可选（并顺带解决 `clinicSummarySharedProvider` 公开分享页的信封兼容问题）。
- 测试：新增 2 条——创建分享后列表 provider 重建并重新拉取（listCalls/通知计数断言）；confirm 阶段切换可用、created 阶段切换锁定且不再触发 preview 请求。
- 全量验证：`flutter analyze` 零问题；`flutter test` 3117 passed / 1 skipped；`dart run scripts/check_doc_coverage.dart --warning-only`、`git diff --check` 通过。

## Visit Summary and Product Measurement — Task 10 收口：分享页信封修复与漏斗合同同步

- **公开分享页信封兼容修复（延后债清偿）**：`clinicSummarySharedProvider` 此前仍调用生成客户端 `reportsControllerGetSharedClinicSummaryV1`，真实后端返回 `{code,message,data}` 信封而生成客户端按裸 `ClinicSummaryDto` 反序列化必抛（与 Task 8 preview/share-create 同型缺陷）。改为与 preview 一致的 raw Dio 方案：`LucentApiPaths` 新增 `clinicSummaryShared(token)` 路径常量（`skipAuthorization: true`，与公开 PDF 一致），provider 解信封 + 复用 `_fillMissingSections` 缺省补齐后反序列化；`_fillMissingSections` 保持 preview/shared 共用。测试改写为 scripted-adapter harness 4 条：信封解包 + 无 Authorization 头、缺 section 容忍（占位 profile 不渲染）、per-token 缓存、错误传播（404 走错误拦截器）。顺带删除测试中不再使用的 `_dto`/`_coverage` DTO 夹具。
- **漏斗合同模型补全（生成包完整性修复）**：Task 9 的 `pnpm export:openapi` 新增 5 个 `Funnel*` schema 后，过滤生成会为它们生成 deserialize 分支但 `_productEventsModels` 不含模型清单 → 生成包损坏（v1201 同型失败模式）。bootstrap 脚本 `_productEventsModels` 补 `FunnelDailyCountsDto`/`FunnelOptionalCountsDto`/`FunnelTotalsDto`/`FunnelWindowDto`/`FunnelResponseDto`（注释注明 admin funnel 客户端不消费，仅合同完整性）；重新生成后 `dart run build_runner build`（generated/lucent_api）通过、`flutter analyze` 零问题、再次 bootstrap 无漂移。
- 验证（2026-08-14）：`flutter analyze` 零问题；`flutter test` 全量 **3119 passed / 1 skipped**（跳过为既有；shared provider 4 条 + 全量回归）；`dart run scripts/run_daily_checks.dart`（analyze/format/test/OpenAPI sync 全过）；`dart run scripts/check_doc_coverage.dart --warning-only`、`dart run scripts/check_doc_links.dart`（137 文件无坏链）通过；桌面闭环 e2e `integration_test/report/review_closed_loop_e2e_test.dart` `-d windows` 实跑 1 test 通过；`git diff --check` 通过。移动端实机运行仍受 Windows 桌面宿主环境限制（既有说明，未伪造结果）。

## Visit Summary and Product Measurement — Task 10 文档收口与计划删除

- `docs/01-product/Product_Safety_Privacy.md`：产品事件测量节扩为收口口径（四客户端事件、白名单属性、无健康内容、离线队列载荷边界、服务端权威事件、管理员漏斗、失败不打断主流程），新增「分享与字段级隐私 UX」节（备注默认不选、链接持有者可查看、7 天 TTL + token 仅哈希、访问计数无身份、文案不暗示医生已收到）。
- `docs/00-current/Active_UI_Report.md`：新增「Visit Summary and Product Measurement 收口（Workstream 2 Task 10）」节（信封修复、漏斗模型同步、验证数字、计划已删）；「保留的文档化限制」改为四 section 键合同债 + 兼容期清理；底部引注改指向 TODO「产品闭环」节。
- `docs/00-current/TODO.md`：删除已完成的「增加最小、隐私克制的闭环测量」与「当前没有可靠的 Review 打开/分享访问/事件结果测量」两行；「大型产品闭环重构」节改为「产品闭环（程序已收口，仅剩延后项）」，移除计划链接；合同债条目更新为仅剩四 section 键服务端必填（分享页信封问题已由本日 raw Dio 修复解决）。
- `docs/00-current/Next_Plan.md`：当前目标改为 0.1.0 发布验证（ROADMAP「Current Release → 0.1.0」），移除计划链接。
- `docs/00-current/Work_Phase_Guide.md`：Phase 6 标注已完成，进入发布验证阶段。
- `ROADMAP.md`：Status 补产品闭环端到端可测量事实；What works 补就诊摘要字段级隐私/可撤销分享/分享管理与闭环测量；What's missing 移除「privacy-minimal closed-loop measurement」改为延后项与合同债；Product Loop Program 节改为完成态（计划文件已删）。
- `docs/02-reference/adr/0011-*.md`：两处计划文件链接改为反引号路径并标注「实施完毕文件已删，2026-08-14 收口」（ADR 正文决策不变，仅引用更新）。
- `plans/`：`2026-08-07-product-loop-program.md` 与 `2026-08-07-visit-summary-and-product-measurement.md` 实施完毕删除（实施完毕文件已删）；`plans/README.md` 移除 Active Program 节，改为剩余计划清单 + 删除说明。其余 parked 计划（ai-chat-redesign、native-bridging-roadmap、legal-compliance-pages）保留。历史迁移日志（2026-08-09/11/13）中对已删计划的引用保持历史原样，以本条目「实施完毕文件已删」为准。

## 文档治理 — 遗留项状态更新

- 验证遗留项并更新 TODO：AI 消息操作按钮拆分描述（复制已可用 Clipboard+toast；重新生成/重新发送仍为上下文菜单占位，Lucent assistant 无对应接口）；审查暂缓项数字更新（`report/presentation/pages/page.dart` 438 行而非 470；`!` 强制解引用约 80 处而非 15，Task 7/8/10 新增代码所致），文件路径补全为实际 `presentation/` 路径。
- 其余项核实未变：AI 会话重命名/删除（后端无 PATCH/DELETE conversations）、Markdown 模板、forui 0.25.0、GenUI、合同债、桌面/Web 冻结。

## 文档治理 — pre-commit 文档覆盖检查启用阻断模式

- `scripts/tooling_workflows.dart` pre-commit 的 doc-check 步骤去掉 `--warning-only`（保留 `--staged`，只评估暂存文件）：代码变更未携带任何 `docs/` 文件时 `check_doc_coverage.dart` 以 exit(1) 阻断提交，与 `docs/doc-map.yaml` 头部声明及 AGENTS.md 既有描述一致；`SKIP_DOC_CHECK=1` 旁路保持不变（脚本在非警告模式下读取该环境变量）。
- 每日检查（`run_daily_checks.dart`）与 push 检查保持建议性质：每日的 `--warning-only` 三级报告不阻断，push 检查不运行覆盖工具——两种通道均为非阻断，已在 doc-map 头部注释注明。
- `docs/doc-map.yaml` 头部注释补充验证模式（--verify）与 daily/push 建议性说明。
- 验证：仅暂存代码文件（无 docs/）时 `--staged` 退出码 1；代码 + 文档一起暂存时退出码 0；`SKIP_DOC_CHECK=1` 旁路仍生效。

## 文档治理 — 新增验证模式与引用检查

- `check_doc_coverage.dart` 新增 `--verify` 模式（镜像 Lucent `check-docs-updated.ts --verify`）：
  - 引用存在性：doc-map 规则的文档目标（字面量与 glob）必须真实存在；
  - 链接完整性：复用 `check_doc_links.dart` 的 vault 索引（`VaultIndex`/`checkDocFileLinks` 改为公开共享），wikilink 与相对链接必须可解析；
  - front-matter 完整性：`00-current`/`01-product`/`02-reference`（含 how-to）必须有 status/owner/quadrant/updated；
  - 新鲜度：`status: active` 且 `updated` 超过 90 天告警、`status: stale` 未归档告警，`status: frozen` 豁免；
  - 阅读入口：`status: active` 且 `quadrant: reference|explanation` 的文档必须被 doc-map 规则引用或至少被另一篇文档以 wikilink/链接引用（迁移日志与 04-archive 不计阅读入口，ADR/how-to 属固定通道豁免）；
  - feature 覆盖：`lib/features/*` 每个目录必须被至少一条规则的 code glob 覆盖（豁免列表 `exemptFeaturePatterns` 为空，显式文档化）。
- 阅读入口修复：`Desktop_UI.md` 补入 `Current_State.md` UI 索引与 `_MOC.md`；`architecture-upgrade-analysis.md` 补入 `_MOC.md` 02-reference 节。
- `docs/doc-map.yaml` 新增 `health-data`、`health-event` 两条 feature 规则（此前 `lib/features/health_data/**`、`health_event/**` 无任何规则覆盖）。
- `Desktop_UI.md` 标记 `status: frozen`：桌面端产品表面按工作区规则冻结，文档进入 90 天新鲜度豁免但保留完整 front-matter。
- `run_daily_checks.dart`（tooling_workflows）新增 `--verify` 步骤（阻断式）；每日 `--warning-only` 报告保持建议性。
- AGENTS.md 补充文档生命周期与 front-matter 规则（`status: frozen` vs `stale` 语义）。
- 测试：`test/scripts/doc_coverage_check_test.dart` 新增 verify 相关用例（frozen 豁免、front-matter、doc-map 孤儿、阅读入口、feature 覆盖）；新增 `test/scripts/check_doc_links_test.dart`（共享链接扫描）。
- 验证：`dart run scripts/check_doc_coverage.dart --verify` 通过；`flutter test test/scripts/` 工具链测试全绿。

## 文档治理 — 文档链接检查增量模式

- `check_doc_links.dart` 新增 `--changed`：读取 git 变更集（暂存 + 工作区 + 未跟踪）只检查变更文档的外链；变更集含文档删除/重命名（`--diff-filter=D`）时回退全量扫描，捕获指向已删文档的入链。默认（无参数）仍全量扫描，供每日/CI 使用。
- pre-commit 的 doc-links 步骤改用 `--changed`，提交反馈提速；链接检查仍为阻断式。
- 测试：`test/scripts/check_doc_links_test.dart` 新增 `filterChangedDocs` 用例。
- 验证：全量文档链接检查通过；仅修改单个文档时 `--changed` 只检查该文档；暂存删除被引用的文档时 `--changed` 全量回退并报出断链。

## 文档治理 — 约定迁移日志只写范围不写精确数字

- AGENTS.md 迁移日志条目补约定行：日志条目描述变更范围与验证结论，不写需要持续同步的精确数字（如测试总数），与 Lucent 仓库既有约定一致（Project_Governance 已同步说明；doc-map 头部指针由本日后续条目补齐）。

## 文档治理 — 迁移日志约定同步到文档映射头部

- `docs/doc-map.yaml` 头部补充迁移日志条目约定指针行（与 Lucent 仓库 doc-map 头部一致）：日志条目描述变更范围与验证结论，不写需要持续同步的精确数字（如测试总数），见 AGENTS.md。
- 修正上一条目「doc-map 头部注释与 Project_Governance 同步说明」的表述：bbfeb89c 仅同步了 Project_Governance.md 的说明，doc-map 头部指针由本条目补齐。

## 文档治理 — 增量链接检查覆盖重命名回退

- `check_doc_links.dart` 变更集收集（`collectDocChangeSet`）的 diff 查询统一加 `--no-renames`：默认重命名检测下纯 `git mv` 只显示为新路径（单条 R），`--diff-filter=D` 查不到旧路径，`--changed` 增量模式会漏掉指向旧名的入链（复审已复现）；关闭重命名检测后重命名呈现为删除+新增，旧路径进入 `deletedDocs`，触发全量扫描回退。
- 测试：`test/scripts/check_doc_links_test.dart` 新增 `collectDocChangeSet` 用例（临时 git 仓库）：纯 `git mv` 报告旧路径为删除、普通删除报告删除文档、仅编辑不产生删除。
- 顺带收口复审小项：frozen 豁免测试改用 120+ 天旧 `updated` 并补 active 对照组（原测试仅 1 天旧、不具判别力）；`analyzeDocFreshness` 改为经 `isFrozenDoc` 判定豁免（消除未使用导出）；`--verify` 新鲜度输入限定活跃文档（04-archive 不再被要求归档自身）；移除未用局部变量；断链消息统一 `docs/` 前缀；doc-map 与同日条目的豁免常量引用改为实际名 `exemptFeaturePatterns`；SKIP_DOC_CHECK 文档注明仅适用于阻断路径（`--verify` 不受旁路影响）；feature 探测补充语法匹配限制说明；pre-commit 注释说明覆盖检查（只评估暂存）与链接检查（暂存+工作区+未跟踪）的范围差异。
- 同日条目中的精确数字按约定改写为范围描述（工具链测试全绿、全量文档链接检查通过）。
- 验证：工具链测试全绿（含新变更集用例）；纯 `git mv` 场景端到端复现修复前后差异（修复前增量模式漏检，修复后全量回退并报出指向旧名的断链）；`--verify` 与全量链接检查通过。

## 合同债 — 同步就诊摘要响应合同为可选字段

- Lucent 已将 `ClinicSummaryDto` 的 profile/allergies/conditions/currentMedicines 四键改为可选并重新导出 openapi.json：服务端 `applySelectedFields` 省略未选键的 wire 行为如实入约（Lucent 迁移日志同日条目）。
- 旧 spec 中四 section 的 items 是破损的 `array of string`（`@ApiProperty` 无显式 type 时反射回退），Lucent 侧补显式 type 后生成客户端引用 `ClinicSummaryAllergyDto`/`ClinicSummaryConditionDto`/`ClinicSummaryMedicineDto` 三个模型——bootstrap 脚本 `_reviewModels` 补齐这三个模型后重新生成。
- 重新生成结果：`clinic_summary_dto.dart` 四键变为可空（`final ClinicSummaryProfileDto? profile;` 等，`required: false`），新增三个 section 模型文件，barrel/deserialize 同步；本次重生成未触碰其他生成面（此前一次失败运行的中间产物已由成功运行归一化）。
- 验证：`flutter analyze` 零问题；`test/report` 全绿。客户端占位反序列化移除见同日下一条目。
