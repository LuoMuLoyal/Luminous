# Migration Log — 2026-08-20

## feat(mine): 改造项 6 C-3 Mine「档案提醒」假数据卡真实化

依据 `plans/2026-08-16-health-event-remediation-plan.md` 的改造项 6（本节完成后已从计划文件删除）：`mine/data/repositories/lucent.dart` 的 `_buildAlerts` 此前硬编码三条静态卡（过敏卡 subtitle/badge 全静态「花粉、青霉素 / 2 项」，与用户真实档案无关；用药卡 badge 按 count 切换但 subtitle 静态「2 种药物」；隐私卡全静态），且 `dashboard.alerts` 在 presentation 层无任何渲染方——假数据驻留生产路径。本项删除硬编码静态数据，`MineDashboard.alerts` 保留为真实数据出口，改造为真实「档案提醒」卡并接入渲染。

### 数据层

- `lib/features/mine/domain/entities/dashboard.dart`：`MineStatusCard` 新增 `String? subtitleText` / `String? badgeText`（动态文本出口，freezed 默认 null；渲染时非空优先于对应 key）；`subtitleKey` / `badgeKey` 由 required 改为可空（既有 key 构造点全部兼容，仅静态卡使用）；`dashboard.freezed.dart`（gitignored）经 build_runner 重新生成。
- `lib/features/mine/data/repositories/lucent.dart`：
  - `fetchDashboard` 新增 `_resolveL10n()`：镜像 app shell 的 locale 解析（显式 zh-CN/en 优先，`system` 回退平台 locale）；locale 偏好读取失败（如测试环境无 SharedPreferences mock）时回退主 locale（zh），保证组合确定性。
  - `_buildAlerts(snapshot, l10n)` 重写：过敏卡仅 `summary.activeAllergyCount > 0` **且**存在 isActive 过敏时生成，subtitleText 为真实 active label 前 2 个（zh「、」/ en ", " 连接）+ 超出追加「等 N 项」后缀（`mineAlertAllergyMore(count)`），badgeText 为 `mineAlertAllergyCount(count)`（Lucent 权威 `activeAllergyCount`）；用药卡仅 `summary.currentMedicineCount > 0` 时生成，subtitleText 为真实 isCurrent 药物 displayName 前 2 个 + 超出追加「等 N 种」（`mineAlertMedicineMore(count)`），badgeText 为 `mineAlertMedicineCount(count)`，**移除「按时服用」虚假断言**（无数据支持）；隐私卡保留（静态产品提示、非用户数据，复用既有三键）；顺序为过敏 → 用药 → 隐私。
  - **不新增「档案缺口」卡**：`missingCoreProfileFields` 缺口已由 MineAccountHero 的 gap 展示（完成度/缺口口径一致），避免重复表达——决策记录于此条目。
- `MineCopyKey` 枚举删除 `alertAllergySubtitle` / `alertAllergyBadge` / `alertMedicineSubtitle` / `alertMedicineBadge` 四值，`mineCopy` switch 同步删除对应分支（grep 全仓确认无其他引用）。

### 展示层

- 新增 `lib/features/mine/presentation/widgets/sections/status_alerts.dart`：`MineStatusAlertsSection`（StatelessWidget，`alerts` 为空时渲染 shrink）——单 `FCard`（`FCardStyleDelta` 归零 padding）+ 行间 `AppDivider`；每行 SoftIcon + 标题（w700）+ 副标题（动态文本优先，muted 色）+ 右侧 badge（动态文本优先，w700），样式对齐 `archive.dart` 条目。Key：`mine-status-alerts-section`、`mine-status-alert-<titleKey.name>`。
- `lib/features/mine/presentation/widgets/views/dashboard_view.dart`：移动端与桌面左栏均在 `MineAccountHero` 之后、`MineArchiveSection` 之前渲染 `MineStatusAlertsSection`（仅 `dashboard.alerts.isNotEmpty` 时）。

### L10n

- `lib/l10n/src/mine_zh.arb` / `mine_en.arb` 新增 `mineAlertAllergyCount`（{count} 项 / {count} items）、`mineAlertAllergyMore`（等 {count} 项 / and {count} in total）、`mineAlertMedicineCount`（{count} 种 / {count} medicines）、`mineAlertMedicineMore`（等 {count} 种 / and {count} in total），均为 int 占位符；格式对齐既有「2 项」文案风格。
- 删除 `mineAlertAllergySubtitle` / `mineAlertAllergyBadge` / `mineAlertMedicineSubtitle` / `mineAlertMedicineBadge`（先 grep 全仓确认无引用）。
- 已执行 `dart scripts/arb_tools.dart merge` + `flutter gen-l10n` 同步生成文件。

### 测试

- `test/mine/domain/entities/dashboard_test.dart`：新增 `MineStatusCard` 组——key-only 构造动态文本默认 null；动态文本构造 key 为 null。
- `test/mine/page_test.dart`：新增真实数据渲染用例（真实过敏 label 前 2 个 + 「等 N 项」后缀、真实药名、动态 badge 文案、隐私卡恒在、行 key）与 count==0 用例（过敏/用药卡不出现、隐私卡仍在）；既有 dashboard 数据用例补充 alerts 组成断言（count>0 但无 item → 用药卡生成、过敏卡不生成）。
- `test/helpers/mocks/mine.dart`：`_mockAlerts` 改用动态文本构造，删除对已删枚举值的引用。

### 文档

- `docs/00-current/Active_UI_Mine_Settings.md`：根页结构与「档案提醒」小节更新；front-matter `updated` 与 Last updated 更新为 2026-08-18。
- `docs/02-reference/Localization.md`：记录 4 新增 + 4 删除键；Last updated 行同步。
- `plans/2026-08-16-health-event-remediation-plan.md`：删除改造项 6 小节；「五、本计划内执行顺序」删除第 1 条（0.1.0 前任务全部完成），仅剩 0.1.0 后条目；「一、目标与范围」C-3 目标行更新为已处置。

### 验证

- `flutter analyze` 零告警。
- `flutter test test/mine` 全部通过。
- `dart run scripts/check_doc_coverage.dart --warning-only` 无必需文档缺失。

### 修复（缺陷 P1：本地化移回展示层，实体结构化）

上一实现把 `AppLocalizations` 解析（`_resolveL10n`，读 `localeControllerProvider.future` + `AppLocalizations.delegate.load`）引入 data 层 `fetchDashboard`：widget 测试环境未 override `localeControllerProvider` 时 `_resolveL10n` 挂起，`mineDashboardProvider` 恒 loading，Mine 页既有测试批量失败；且 data 层解析本地化违反分层（本地化属 presentation 职责）。本项修复：

- 实体 `MineStatusCard` 改为结构化数据：新增 `kind`（`MineStatusCardKind` 枚举：allergy/medicine/privacy）、`items`（真实过敏 label / 药名全量，不截断）、`count`（权威计数，可空）；删除 `subtitleText` / `badgeText`；`subtitleKey` / `badgeKey` 保留（隐私卡用）。`dashboard.freezed.dart`（gitignored）经 build_runner 重新生成。
- data 层 `_buildAlerts(snapshot)` 恢复无 l10n 参数，删除 `_resolveL10n` / `_summaryText` / `_listSeparator` 及相应 import；卡只携带结构化数据。
- 展示层 `status_alerts.dart` 承担截断与本地化：副标题 = 前 2 项按分隔符拼接（zh「、」/ en ", "）+ 超出追加「等 N 项/种」后缀（`mineAlertAllergyMore/MedicineMore(count)`），badge = `mineAlertAllergyCount/MedicineCount(count)`；隐私卡走 `subtitleKey` / `badgeKey`。
- 测试：`test/mine` 既有失败全部恢复；实体与渲染断言改为 items/count/kind 与最终渲染文案。l10n 键（4 个）不变，未重新 merge/gen-l10n。

## docs(plans): 十份改造计划核对收口与 README 状态同步

对十份 2026-08-16 功能盘点改造计划逐一核对代码真相(十个只读核对代理,以 git 提交与当前代码为准),已完成项在计划文件中删除/标注,同步 `plans/README.md` 状态注释:

- **today**:删除 F-1/F-2/F-3/F-5/F-6/F-7/F-9/F-10/F-12/F-15/F-16/F-18/F-19 共 13 个已完成小节;F-8 节改写为仅剩 `remainingCount` 退役(degraded 降级标记已落地);保留 F-14(0.1.0 后);§一/§四/§五 同步。
- **report**:删除 R-1(历史翻页已完成);R-3/R-4 增补进展标注(R-3 服务端口径与死代码清理已落地、R-4 #20 已落地);R-5 口径统一为必做项(消除 §三/§六 矛盾表述);R-6 标注未完成;§一/§五 同步。
- **medicine**:F-15 保留项入口计数由 4 修正为 6(扫码/拍照由 scan-search 计划上浮)。
- **engineering-backend**:修正计划自身 3 处内部错误——F-10 交叉引用 P2-E→P2-F;P2-A 现状段如实标注 WORKER_MODE 设计从未实现;P2-F 陈旧注释定位改为 `guards/testing-shared-secret.guard.ts:19-20`;§四 队列口径 9→12 条。
- **record**:§四 ObservedMetric mapper 计数由三处修正为四处。
- **README**:#4 today、#7 health-event、#8 report 三条状态注释更新为核对后口径。
- 其余计划(platform-notification、scan-search、assistant、mine-settings)核对结论为文档与代码一致,无需改动。

核对结论摘要:十份计划中 mine-settings 6 项(0.1.0 前)全部未完成、today 剩 F-8 退役与 F-14、report 剩 R-3 视图装配/R-4 #19/#21/#22/R-5/R-6、engineering-backend 剩 P2-F 三处漂移修正(0.1.0 前唯一可立即落地项,待 Lucent 侧执行),其余计划 0.1.0 前任务已完成或按排期延期。

## feat(auth): P1-1 账号敏感操作接入 Security PIN elevation

- 修改邮箱、修改密码、解绑三方身份均在现有页面回调中先执行 `showSecurityElevationDialog(context, ref)`；有效 elevation token 自动跳过 PIN 对话框，请求继续复用 `SecurityElevationInterceptor` 注入请求头。`AuthAccountNotifier` 与领域接口保持 UI 无依赖，未启用/取消验证时不发起敏感请求。
- `AuthAccountState` 增加默认兼容的 `requiresSecurityElevation` 机器可读标志；仅当 `LucentErrorMapper` 解析出的 403、`LucentResultCode.forbidden` 与明确 elevation-token-invalid 文案同时匹配时置位，避免把解绑最后身份等普通 403 误报为 PIN 引导。页面在异步操作后重新读取最新 state，失败引导文案为“请验证安全 PIN”。
- 验证结论：TDD 红阶段定向测试确认 PIN 未启用时当前实现仍错误发起邮箱请求，且 elevation 状态测试在字段缺失时失败；绿阶段通过邮箱页、账号设置页、AuthAccountNotifier 定向测试。已执行 ARB merge/gen-l10n 与 build_runner；未修改 Lucent。

## fix(auth): P1-1 elevation 生命周期复审收口

- 失效 elevation token 会清理 controller 与 holder，过期 token 不再跳过 PIN；interceptor 测试验证有效 token 注入请求头、过期 token 不注入。相关窄定向验证通过。

## docs(auth): P1-1 复审 follow-up 暂记

- 复审确认 settings provider loading/error 时 `asData == null` 的 PIN 未启用误判，以及 token/dialog 到期边界统一与精确到期测试，均登记为 TODO，当前未修复。
- 核心 P1-1 接线已完成交付；本轮仅收口文档，不扩大到 P1-2，也不创建 commit。

## feat(settings): 添加远端通知偏好客户端模型与仓储

- 新增通知偏好 domain entity、repository/provider 与生成的 Lucent API DTO；nullable
  睡眠时间清除时保留 raw PATCH 兼容路径。
- 同步更新 API client contract 快照，客户端具备读取和部分更新远端偏好的基础能力。
