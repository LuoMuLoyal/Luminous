# Migration Log — 2026-08-20

## feat(mine): 改造项 8 C-1 `onboardingCompleted` 改造为引导流程状态（含 C-4 口径对齐）

依据 `plans/2026-08-16-health-event-remediation-plan.md` 的改造项 8（本节完成后已从计划文件删除）：`onboardingCompleted` 是无任何写入方的僵尸字段（无 onboarding 流程），唯一作用是 Mine 完成度分母。本项将其定位为「引导流程状态」字段（有真实写入方后纳入完成度），并在改造落地前同步调整完成度分母与 readiness gap 列表（C-4 口径对齐一并完成）。

### 数据层

- `lib/features/mine/data/repositories/lucent.dart` `_buildCompletion`：完成度分母从 7 项调整为 6 项，移除 `onboardingCompleted` 计数项（其余 6 项不变：过敏/当前用药/出生日期/身高/生理性别/体重）；代码注释说明 `onboardingCompleted` 为引导流程状态字段、当前无写入方、暂不纳入完成度，待有真实引导流程写入方后再纳入。口径从「有值」对齐为「有用」。
- `lib/features/mine/data/repositories/lucent.dart` `_buildProfile`：移除 `hasEmergencyContact` 计算与 `MineProfileSnapshot` 传参（grep 全仓确认该字段无 presentation 消费方，仅 gap 派生使用，随 emergencyContact gap 一并删除）。
- `lib/features/mine/domain/entities/dashboard.dart`：`MineProfileSnapshot` 删除 `hasEmergencyContact` 字段及 `signedOut` 构造对应值；`dashboard.freezed.dart`（gitignored）经 build_runner 重新生成，本地供测试。

### 展示层

- `lib/features/mine/presentation/widgets/sections/account_hero.dart`：readiness gap 列表从 `_deriveGaps` 移除 emergencyContact 缺口；`_ReadinessGapType` 枚举移除 `emergencyContact`；`_buildDescription` 单 gap 描述 switch 与 `_ReadinessGap.label` switch 移除对应分支。emergencyContact 为不排期字段（标注延后，填了无业务用途），不再作为 gap 引导用户填写；编辑页字段保留。

### L10n

- `lib/l10n/src/mine_zh.arb` / `mine_en.arb` 删除无引用的 `mineCompletenessGapEmergencyContact`、`mineReadinessGapDescEmergencyContact` 两键；已执行 `dart scripts/arb_tools.dart merge` + `flutter gen-l10n` 同步生成文件。`mineEditFieldEmergencyContactName/Phone` 仍被资料编辑页使用，保留。

### 测试

- `test/mine/page_test.dart`：完成度断言随分母口径更新（`_mockSnapshot` 下 4/7 → 3/6，percentLabel 由 57% 改为 50%），onboardingCompleted 不再计入。
- `test/helpers/mocks/mine.dart`：两处 `MineProfileSnapshot` 构造移除 `hasEmergencyContact`。
- 复核：`test/mine` 与全仓测试无 emergencyContact gap / `hasEmergencyContact` 断言残留。

### 文档

- `docs/00-current/Active_UI_Mine_Settings.md`：完成度口径更新为 6 项（按「有用」而非「有值」），缺口检测更新为 5 项并说明 emergencyContact 不再作为 gap；front-matter `updated` 与 Last updated 更新为 2026-08-18。
- `docs/02-reference/Localization.md`：记录删除两键；Last updated 行同步。
- `plans/2026-08-16-health-event-remediation-plan.md`：删除改造项 8 小节；「五、本计划内执行顺序」第 1 条由「改造项 6(C-3)与 7/8」改为「改造项 6(C-3)与 7」。

### 验证

- `flutter analyze` 零告警。
- `flutter test test/mine` 全部通过。
- `dart run scripts/check_doc_coverage.dart --warning-only` 无必需文档缺失。

## feat(mine/record): 改造项 7 C-1 `unitSystem` 接单位制显示切换

依据 `plans/2026-08-16-health-event-remediation-plan.md` 的改造项 7（本节完成后已从计划文件删除）：`unitSystem` 此前是无消费方的僵尸字段，但设置页/档案编辑页有真实填写入口（`HealthUnitSystem.metric | imperial`，存储为 `profile.unitSystem` 字符串）。本项接入单位制显示切换——体重 kg/lb、饮水 ml/fl oz，纯展示层换算，存储口径不变；后端无改动。

### 实现范围与边界

- ① Mine 档案区体重行 kg/lb 换算；② record 详情页饮水进度卡总量/目标 ml/fl oz 换算。
- 摘要瓦片、饮水角标、today 概览等其它 ml 展示点**不做**（保持存储口径 ml），见本日 `docs/00-current/Active_UI_Mine_Settings.md` / `Active_UI_Record.md` 补充说明。

### 换算工具

- 新增 `lib/features/health_context/domain/services/unit_conversion.dart`（health_context domain 层新建 `services/` 子目录，record/mine 均可引用）：
  - `isImperialUnitSystem(String?)`：`HealthUnitSystem.fromValue == imperial`；metric / 未设置（null）/ 未知字符串一律按公制。
  - `weightInLb(double? kg)`：kg × 2.2046226218；null 透传 null；展示时 `round()`（与 kg 行 round 展示一致）。
  - `waterInFlOz(num ml)`：ml × 0.0338140227；展示保留 1 位小数（如 "18.6"）。
  - 纯函数 + doc 注释说明「仅展示换算，存储口径不变」。

### Mine 侧

- `MineProfileSnapshot` 新增 `required String? unitSystem`；`mine/data/repositories/lucent.dart` `_buildProfile` 填 `profile.unitSystem`；`signedOut` 构造补 null；`dashboard.freezed.dart`（gitignored）经 `dart run build_runner build` 重新生成。
- `mine/presentation/widgets/sections/archive.dart` `_profileMeta` 体重行：metric/未设置用既有 `mineProfileWeightKg(round)`；imperial 用新键 `mineProfileWeightLb(weightInLb(kg).round())`。身高行保持 cm 不动（计划未列身高）。
- l10n：`mine_zh.arb` / `mine_en.arb` 新增 `mineProfileWeightLb`（zh/en 均为 "{weight}lb"，int 占位符，格式对齐既有 `mineProfileWeightKg`）。

### Record 侧

- `record/presentation/pages/detail.dart` `_RecordDetailBodyState.build`：读取 `ref.watch(healthContextSnapshotProvider).asData?.value.profile.unitSystem`（loading/error 时 `asData` 为 null → 回退公制）；仅 water 记录且有当日 ml 合计时 watch（与 `recordWaterTargetCountProvider` 同款条件 watch，非水记录不触发健康档案拉取）。
- imperial 时总量 `waterTotalMl` 与目标 `waterTargetMl` 均换算 fl oz（各自保留 1 位小数）并调用新键 `recordDetailDailyWaterProgressOz`；进度条 value 用原始 ml 比例（数值不变，仅文案单位换算）。
- l10n：`record_zh.arb` / `record_en.arb` 新增 `recordDetailDailyWaterProgressOz`（zh/en 均为 "{total} / {target} fl oz"，String 占位符，传已格式化 fl oz 字符串）。

### 测试

- 新增 `test/health_context/unit_conversion_test.dart`：isImperial 三态（metric/null/imperial）+ 未知值、kg→lb 数值与 round、ml→oz 数值与 1 位小数。
- `test/mine/page_test.dart` 新增 2 用例：档案 meta 体重行 metric 显示 kg（"27岁 · 170cm · 60kg"）、imperial 显示 lb（"27岁 · 170cm · 132lb"），身高保持 cm。
- `test/record/detail_page_test.dart`：既有 water 用例补 `healthContextSnapshotProvider` override 保持封闭（模式同 `test/record/page_test.dart`）；新增 imperial 用例断言 "18.6 / 67.6 fl oz" 且进度条 value 仍为 ml 比例。
- `test/helpers/mocks/mine.dart` 两处 `MineProfileSnapshot` 构造补 `unitSystem: null`；`test/mine/domain/entities/dashboard_test.dart` 空档案用例补 unitSystem 断言。

### 文档

- `docs/00-current/Active_UI_Mine_Settings.md`：健康档案小节补充单位制换算说明；front-matter `updated` 与 Last updated 改为 2026-08-18。
- `docs/00-current/Active_UI_Record.md`：阶段 8 详情页 water 卡补充 fl oz 换算说明与边界（摘要瓦片/角标仍为 ml）；Last updated 行同步。
- `docs/02-reference/Localization.md`：记录 `mineProfileWeightLb` 与 `recordDetailDailyWaterProgressOz` 两键；Last updated 行同步。
- `plans/2026-08-16-health-event-remediation-plan.md`：删除改造项 7 小节；「五、本计划内执行顺序」第 1 条更新为仅剩「改造项 6(C-3)」。

### 验证

- `flutter analyze` 零告警。
- `flutter test test/health_context/unit_conversion_test.dart`、`flutter test test/record/detail_page_test.dart`、`flutter test test/mine` 通过。
- `dart run scripts/check_doc_coverage.dart --warning-only` 无必需文档缺失。

## feat(mine): 改造项 6 C-3 Mine「档案提醒」假数据卡真实化

依据 `plans/2026-08-16-health-event-remediation-plan.md` 的改造项 6（本节完成后已从计划文件删除）：`mine/data/repositories/lucent.dart` 的 `_buildAlerts` 此前硬编码三条静态卡（过敏卡 subtitle/badge 全静态「花粉、青霉素 / 2 项」，与用户真实档案无关；用药卡 badge 按 count 切换但 subtitle 静态「2 种药物」；隐私卡全静态），且 `dashboard.alerts` 在 presentation 层无任何渲染方——假数据驻留生产路径。本项删除硬编码静态数据，`MineDashboard.alerts` 保留为真实数据出口，改造为真实「档案提醒」卡并接入渲染。

### 数据层

- `lib/features/mine/domain/entities/dashboard.dart`：`MineStatusCard` 新增 `String? subtitleText` / `String? badgeText`（动态文本出口，freezed 默认 null；渲染时非空优先于对应 key）；`subtitleKey` / `badgeKey` 由 required 改为可空（既有 key 构造点全部兼容，仅静态卡使用）；`dashboard.freezed.dart`（gitignored）经 build_runner 重新生成。
- `lib/features/mine/data/repositories/lucent.dart`：
  - `fetchDashboard` 新增 `_resolveL10n()`：镜像 app shell 的 locale 解析（显式 zh-CN/en 优先，`system` 回退平台 locale）；locale 偏好读取失败（如测试环境无 SharedPreferences mock）时回退主 locale（zh），保证组合确定性。
  - `_buildAlerts(snapshot, l10n)` 重写：过敏卡仅 `summary.activeAllergyCount > 0` **且**存在 isActive 过敏时生成，subtitleText 为真实 active label 前 2 个（zh「、」/ en ", " 连接）+ 超出追加「等 N 项」后缀（`mineAlertAllergyMore(count)`），badgeText 为 `mineAlertAllergyCount(count)`（Lucent 权威 `activeAllergyCount`）；用药卡仅 `summary.currentMedicineCount > 0` 时生成，subtitleText 为真实 isCurrent 药物 displayName 前 2 个 + 超出追加「等 N 种」（`mineAlertMedicineMore(count)`），badgeText 为 `mineAlertMedicineCount(count)`，**移除「按时服用」虚假断言**（无数据支持）；隐私卡保留（静态产品提示、非用户数据，复用既有三键）；顺序为过敏 → 用药 → 隐私。
  - **不新增「档案缺口」卡**：`missingCoreProfileFields` 缺口已由 MineAccountHero 的 gap 展示（完成度/缺口口径一致），避免重复表达——决策记录于此条目。
- `MineCopyKey` 枚举删除 `alertAllergySubtitle` / `alertAllergyBadge` / `alertMedicineSubtitle` / `alertMedicineBadge` 四值，`mineCopy` switch 同步删除对应分支（grep 全仓确认无其他引用）。

### 展示层

- 新增 `lib/features/mine/presentation/widgets/sections/status_alerts.dart`：`MineStatusAlertsSection`（StatelessWidget，`alerts` 为空时渲染 shrink）——单 `FCard`（`FCardStyleDelta` 归零 padding）+ 行间 `AppDivider`；每行 SoftIcon + 标题（w700）+ 副标题（动态文本优先，muted 色）+ 右侧 badge（动态文本优先，w700），样式对齐 `archive.dart` 条目。Key：`mine-status-alerts-section`、`mine-status-alert-<titleKey.name>`。
- `lib/features/mine/presentation/widgets/views/dashboard_view.dart`：移动端与桌面左栏均在 `MineAccountHero` 之后、`MineArchiveSection` 之前渲染 `MineStatusAlertsSection`（仅 `dashboard.alerts.isNotEmpty` 时）。

### L10n

- `lib/l10n/src/mine_zh.arb` / `mine_en.arb` 新增 `mineAlertAllergyCount`（{count} 项 / {count} items）、`mineAlertAllergyMore`（等 {count} 项 / and {count} in total）、`mineAlertMedicineCount`（{count} 种 / {count} medicines）、`mineAlertMedicineMore`（等 {count} 种 / and {count} in total），均为 int 占位符；格式对齐既有「2 项」文案风格。
- 删除 `mineAlertAllergySubtitle` / `mineAlertAllergyBadge` / `mineAlertMedicineSubtitle` / `mineAlertMedicineBadge`（先 grep 全仓确认无引用）。
- 已执行 `dart scripts/arb_tools.dart merge` + `flutter gen-l10n` 同步生成文件。

### 测试

- `test/mine/domain/entities/dashboard_test.dart`：新增 `MineStatusCard` 组——key-only 构造动态文本默认 null；动态文本构造 key 为 null。
- `test/mine/page_test.dart`：新增真实数据渲染用例（真实过敏 label 前 2 个 + 「等 N 项」后缀、真实药名、动态 badge 文案、隐私卡恒在、行 key）与 count==0 用例（过敏/用药卡不出现、隐私卡仍在）；既有 dashboard 数据用例补充 alerts 组成断言（count>0 但无 item → 用药卡生成、过敏卡不生成）。
- `test/helpers/mocks/mine.dart`：`_mockAlerts` 改用动态文本构造，删除对已删枚举值的引用。

### 文档

- `docs/00-current/Active_UI_Mine_Settings.md`：根页结构与「档案提醒」小节更新；front-matter `updated` 与 Last updated 更新为 2026-08-18。
- `docs/02-reference/Localization.md`：记录 4 新增 + 4 删除键；Last updated 行同步。
- `plans/2026-08-16-health-event-remediation-plan.md`：删除改造项 6 小节；「五、本计划内执行顺序」删除第 1 条（0.1.0 前任务全部完成），仅剩 0.1.0 后条目；「一、目标与范围」C-3 目标行更新为已处置。

### 验证

- `flutter analyze` 零告警。
- `flutter test test/mine` 全部通过。
- `dart run scripts/check_doc_coverage.dart --warning-only` 无必需文档缺失。

### 修复（缺陷 P1：本地化移回展示层，实体结构化）

上一实现把 `AppLocalizations` 解析（`_resolveL10n`，读 `localeControllerProvider.future` + `AppLocalizations.delegate.load`）引入 data 层 `fetchDashboard`：widget 测试环境未 override `localeControllerProvider` 时 `_resolveL10n` 挂起，`mineDashboardProvider` 恒 loading，Mine 页既有测试批量失败；且 data 层解析本地化违反分层（本地化属 presentation 职责）。本项修复：

- 实体 `MineStatusCard` 改为结构化数据：新增 `kind`（`MineStatusCardKind` 枚举：allergy/medicine/privacy）、`items`（真实过敏 label / 药名全量，不截断）、`count`（权威计数，可空）；删除 `subtitleText` / `badgeText`；`subtitleKey` / `badgeKey` 保留（隐私卡用）。`dashboard.freezed.dart`（gitignored）经 build_runner 重新生成。
- data 层 `_buildAlerts(snapshot)` 恢复无 l10n 参数，删除 `_resolveL10n` / `_summaryText` / `_listSeparator` 及相应 import；卡只携带结构化数据。
- 展示层 `status_alerts.dart` 承担截断与本地化：副标题 = 前 2 项按分隔符拼接（zh「、」/ en ", "）+ 超出追加「等 N 项/种」后缀（`mineAlertAllergyMore/MedicineMore(count)`），badge = `mineAlertAllergyCount/MedicineCount(count)`；隐私卡走 `subtitleKey` / `badgeKey`。
- 测试：`test/mine` 既有失败全部恢复；实体与渲染断言改为 items/count/kind 与最终渲染文案。l10n 键（4 个）不变，未重新 merge/gen-l10n。
