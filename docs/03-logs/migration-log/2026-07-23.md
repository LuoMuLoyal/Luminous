# 2026-07-23 代码审查修复

## TapGestureRecognizer 内存泄漏修复

### 背景

代码审查报告指出注册页 `_TermsAgreementText` 组件作为 `StatelessWidget`，在 `build()` 中创建 `TapGestureRecognizer` 实例但未调用 `dispose()`。每次父组件 rebuild 时旧的 recognizer 无法释放，造成内存泄漏。

### 修复

将 `_TermsAgreementText` 从 `StatelessWidget` 改为 `StatefulWidget`：
- `initState()` 中创建 `TapGestureRecognizer` 实例
- `didUpdateWidget()` 中更新回调（处理父组件 rebuild 时回调变化）
- `dispose()` 中释放 recognizer

**文件:** `lib/features/auth/presentation/pages/register.dart`

## 代码风格一致性重构 P0

### P0-A：文件命名修复（9 个源文件 + 8 个测试文件）

按 AGENTS.md File Naming Rules 消除所有 `_provider`/`_repository`/`_controller`/`_service` 后缀冗余和目录名前缀重复。

| 旧路径 | 新路径 |
|--------|--------|
| `settings/presentation/providers/profile_sync_provider.dart` | `settings/presentation/providers/profile_sync.dart` |
| `core/database/cache_cleanup_provider.dart` | `core/database/cache_cleanup.dart` |
| `scan/data/scan_repository.dart` | `scan/data/repositories/scan.dart`（同时移入 `repositories/` 子目录） |
| `record/presentation/controllers/nlp_controller.dart` | `record/presentation/controllers/nlp.dart` |
| `settings/data/services/notification_permission_service.dart` | `settings/data/services/notification_permission.dart` |
| `scan/domain/services/ocr_service.dart` | `scan/domain/services/ocr.dart` |
| `record/domain/services/voice_recording_service.dart` | `record/domain/services/voice_recording.dart` |
| `auth/presentation/services/wechat_oauth_service.dart` | `auth/presentation/services/wechat_oauth.dart` |
| `core/theme/theme_controller.dart` | `core/theme/preference.dart` |

同步重命名 8 个对应测试文件。共更新 38 个文件的 import 路径，重建 `build_runner` 生成代码。

### P0-B：`shared_widgets.dart` 拆分（7 个类 → 7 个独立文件）

将 `core/widgets/common/shared_widgets.dart` 中混合的 7 个职责不相关的组件拆分为独立文件：

| 类/函数 | 新位置 |
|---------|--------|
| `VerificationCodeField` | `auth/presentation/widgets/shared/verification_code_field.dart` |
| `MineEditFormLoading` | `mine/presentation/widgets/shared/mine_edit_form_loading.dart` |
| `SettingsSectionLabel` | `settings/presentation/widgets/shared/settings_section_label.dart` |
| `settingsPageVerticalPadding()` | `settings/presentation/utils/settings_page_padding.dart` |
| `SheetDragHandle` | `core/widgets/common/sheet_drag_handle.dart` |
| `SoftIcon` | `core/widgets/common/soft_icon.dart` |
| `IconActionButton` | `core/widgets/common/icon_action_button.dart` |

共更新 30 个消费方文件的 import 路径，删除原 `shared_widgets.dart`。

### 验证

- `flutter analyze` — 0 issues
- `flutter test` — 2751 passed，6 golden test 失败（预存环境问题，与重构无关）

## 代码风格一致性重构 P1

### P1-A：FCircularProgress 页面级加载清理（2 个文件）

将页面级 `FCircularProgress` 替换为 `StateSkeletonView` shimmer 骨架屏：

| 文件 | 修改 |
|------|------|
| `core/widgets/common/page_state.dart` | `_DefaultLoadingView` 从 `Center(child: FCircularProgress())` 改为 `StateSkeletonView` 通用骨架屏 |
| `report/presentation/pages/clinic_summary_shared.dart` | 页面 loading 从 `Center(child: FCircularProgress())` 改为 `StateSkeletonView` 骨架屏 |

**修正说明**：原计划中 `clinic_summary_content.dart`、`readiness.dart`、`data_export.dart` 的 `FCircularProgress` 用法经审查确认为**按钮级加载**（在 `FButton` 内作为 prefix/child），属于可接受用法，保留不动。

### P1-B：网络层硬编码中文消除

引入 `NetworkErrorCode` 枚举 + 表示层 l10n 映射机制，消除网络层 4 个文件中的硬编码中文字符串。

#### 新增文件

| 文件 | 用途 |
|------|------|
| `core/network/error_code.dart` | `NetworkErrorCode` 枚举（12 个错误码） |
| `core/errors/network_error_l10n.dart` | `NetworkErrorL10n.map()` — 枚举到 l10n 字符串映射 |
| `lib/l10n/src/network_zh.arb` | 中文 ARB fragment（12 条） |
| `lib/l10n/src/network_en.arb` | 英文 ARB fragment（12 条） |

#### 修改的文件

| 文件 | 修改 |
|------|------|
| `core/network/api_exception.dart` | 新增 `networkErrorCode` 字段 |
| `core/errors/error.dart` | `AppError` 新增 `networkErrorCode` 字段 |
| `core/network/error_mapper.dart` | `fromObject`/`toAppError` 传播 `networkErrorCode` |
| `core/network/envelope.dart` | 硬编码中文 → `NetworkErrorCode.businessFailure` |
| `core/network/sse.dart` | 硬编码中文 → `NetworkErrorCode.emptyStreamResponse` |
| `core/network/map_utils.dart` | 硬编码中文 → `NetworkErrorCode.businessFailure` / `invalidSsePayload` |
| `core/network/interceptors/error_interceptor.dart` | 8 条中文 fallback → 英文 dev message + `NetworkErrorCode` |
| `core/errors/user_message.dart` | 新增可选 `l10n` 参数，有 `networkErrorCode` 时映射 l10n |
| `scripts/arb_tools.dart` | `fragmentRules` 新增 `'network'` 条目 |
| `features/search/presentation/pages/page.dart` | 传入 `l10n` |
| `features/notification/presentation/pages/list.dart` | 传入 `l10n` |
| `features/notification/presentation/pages/detail.dart` | 传入 `l10n` |

同步更新 5 个测试文件的断言消息（从中文改为英文 dev message）。

#### 架构设计

```
网络层（纯 Dart，无 Flutter 依赖）
  LucentApiException { message: "English dev text", networkErrorCode: NetworkErrorCode }
      ↓
ErrorMapper
  AppError { message, networkErrorCode }
      ↓
表示层（有 BuildContext）
  userMessageFromError(error, l10n: AppLocalizations.of(context)!)
      → NetworkErrorL10n.map(networkErrorCode, l10n)
      → 本地化用户消息
```

### 验证

- `flutter analyze` — 0 issues
- `flutter test` — 2751 passed，6 golden test 失败（预存环境问题，与重构无关）

## 代码风格一致性重构 P2

### P2-A：AppDivider 评估决策

**结论：保留**。`AppDivider` 非零逻辑包装——去除默认 padding + 统一颜色为 `FColors.border`，有实际行为差异，不是零逻辑转发。每次手写 `FDividerStyleDelta` 容易遗漏，封装可防误用。

### P2-B：Magic Number 间距修复（7 个文件）

将硬编码间距值替换为 `Spacing.*` token，统一设计系统间距规范。

#### 确定性替换（精确匹配）

| 文件 | 硬编码 | 替换为 |
|------|--------|--------|
| `record/presentation/widgets/dialogs/fast_entry_dialog.dart` | `EdgeInsets.all(20)` | `EdgeInsets.all(Spacing.level5)` |
| `mine/presentation/widgets/sections/account_hero.dart` | `EdgeInsets.all(4)` | `EdgeInsets.all(Spacing.level1)` |

#### 最近 token 替换（方案 B：不扩展 scale）

| 文件 | 硬编码 | 替换为 | 说明 |
|------|--------|--------|------|
| `shell/presentation/page.dart` | `SizedBox(width: 8)` | `SizedBox(width: Spacing.level2)` (6) | 8 与 6/10 等距，取小 |
| `settings/presentation/pages/page.dart` | `SizedBox(height: 12)` | `SizedBox(height: Spacing.level3)` (10) | 12 与 10/14 等距，取小 |
| `settings/presentation/pages/page.dart` | `SizedBox(height: 20)` | `SizedBox(height: Spacing.level5)` (20) | 精确匹配 |
| `settings/presentation/pages/page.dart` | `SizedBox(width: 8)` | `SizedBox(width: Spacing.level2)` (6) | 同上 |
| `settings/presentation/pages/feature_flags.dart` | `SizedBox(width: 8)` | `SizedBox(width: Spacing.level2)` (6) | 同上 |
| `assistant/presentation/widgets/sections/hero.dart` | `SizedBox(width: 8, height: 8)` | `SizedBox(width: Spacing.level2, height: Spacing.level2)` (6) | 同上 |
| `assistant/presentation/widgets/shared/proposal_card.dart` | `SizedBox(height: 2)` + `EdgeInsets.only(bottom: 2)` ×2 | `Spacing.level1` (4) | 最近 token |

### P2-C：shell/page.dart l10n 硬编码 fallback 消除

**问题**：`_DesktopSidebar` 和 `_MobileBottomNavigationBar` 的 `l10n` 类型为 `AppLocalizations?`，多处使用 `l10n?.xxx ?? '中文'` 硬编码 fallback。

**修复**：
- `ShellPage.build` 中 `AppLocalizations.of(context)` 改为 `AppLocalizations.of(context)!`
- `_DesktopSidebar.l10n` 和 `_MobileBottomNavigationBar.l10n` 类型从 `AppLocalizations?` 改为 `AppLocalizations`
- 删除所有 `?? '设置'`、`?? '帮助'`、`?? 'Luminous'`、`?? 'Today'` 等硬编码 fallback
- `ShellTab.label()` 签名从 `AppLocalizations?` 改为 `AppLocalizations`，删除所有 `?? 'xxx'` fallback
- 同步更新 `test/shell/tab_test.dart`：从 null-fallback 单元测试改为 `testWidgets` + `MaterialApp` 的 l10n 集成测试

**文件**：`lib/features/shell/presentation/page.dart`、`lib/features/shell/presentation/tab.dart`、`test/shell/tab_test.dart`

### 验证

- `flutter analyze` — 0 issues
- `flutter test` — 2751 passed，0 failed（排除预存 golden test 环境问题）

---

## 代码风格一致性重构

### 背景

Luminous 经历了从 Material Design 到 Forui、从 GetX 到 Riverpod 的迁移，代码风格存在渐进式不一致。根据 `plans/2026-07-23-code-style-consistency.md` 计划执行全仓库风格统一重构。

### Phase 1：文件命名规范化

**纯类型词文件重命名**（违反 AGENTS.md Rule 2）：

| 旧路径 | 新路径 |
|--------|--------|
| `today/data/providers/repository.dart` | `today/data/providers/today_suggestion.dart` |
| `report/data/providers/repository.dart` | `report/data/providers/report.dart` |
| `mine/data/providers/repository.dart` | `mine/data/providers/mine.dart` |
| `search/data/mappers/mapper.dart` | `search/data/mappers/medicine_search.dart` |
| `health_context/data/mappers/mapper.dart` | `health_context/data/mappers/health_context.dart` |
| `auth/data/mappers/mapper.dart` | `auth/data/mappers/auth.dart` |

**类型后缀/目录前缀文件重命名**（违反 AGENTS.md Rule 1/3）：

| 旧路径 | 新路径 |
|--------|--------|
| `search/widgets/shared/result_widgets.dart` | `search/widgets/shared/results.dart` |
| `search/widgets/shared/header_widgets.dart` | `search/widgets/shared/headers.dart` |
| `mine/widgets/sections/archive_section.dart` | `mine/widgets/sections/archive.dart` |
| `medicine/pages/reminder_pages.dart` | `medicine/pages/reminders.dart` |
| `settings/utils/settings_page_padding.dart` | `settings/utils/page_padding.dart` |
| `settings/widgets/shared/settings_section_label.dart` | `settings/widgets/shared/section_label.dart` |

所有 import 路径、part 指令、.g.dart part-of 指令同步更新。`build_runner` 重新生成确认无冲突。

**相对导入统一为绝对导入**：`support/` 和 `settings/` feature 中的 6 个文件的 `../` 相对导入统一为 `package:luminous/` 绝对导入。

**Import 排序**：启用 `directives_ordering` lint 规则，`dart fix --apply` 自动修复 185 个文件的 import 排序。

### Phase 2：Colors.* 硬编码替换

- `barcode_scanner.dart`：`Colors.black/white/red` 替换为 `const Color(0xFFxxxxxx)` 字面量，添加相机 UI 特殊场景注释
- `sidebar.dart`、`voice_entry_dialog.dart`、`skeleton_view.dart`：`Colors.transparent` 替换为 `const Color(0x00000000)`

### Phase 3：目录结构规范化

- `settings/data/services/notification_permission.dart` → `settings/domain/services/notification_permission.dart`（符合 AGENTS.md 中 `domain/services/` 约定）
- `auth/presentation/services/wechat_oauth.dart` 保留原位（依赖 `data/providers/`，移至 `domain/` 会违反依赖方向）
- AGENTS.md Architecture 段补充 `mappers`、`utils`、`constants`、`models`、`services` 子目录规范
- `core/design/design.dart` barrel 注释更新为实际平铺结构描述

### Phase 4：Lint 规则启用

`analysis_options.yaml` 新启用 5 条 lint 规则：
- `always_declare_return_types`
- `eol_at_end_of_file`
- `prefer_const_declarations`
- `prefer_final_locals`
- `sort_constructors_first`

`dart fix --apply` 自动修复 126 处。手动修复 1 处 `sort_constructors_first`（`legal/domain/entities/doc_type.dart`）。

### Phase 5：中文 dartdoc 统一为英文

~30 个文件的中文 `///` dartdoc 注释翻译为英文，覆盖 `core/widgets/`、`core/network/`、`core/design/` 及各 feature 的 `top_bar.dart`、`skeleton_view.dart`、`page_padding.dart` 等文件。保留中文药品名和日期格式示例（非注释性中文）。

### Phase 6：Barrel 文件规范化

- 删除无人引用的 `search/widgets/shared/parts.dart`
- 纯 barrel 文件 (`report/widgets/shared/sections.dart`、`mine/widgets/shared/sections.dart`、`medicine/pages/reminders.dart`) 添加 `library;` 声明和文档注释

### 验证

- `flutter analyze` — 0 issues
- `flutter test` — 2751 passed（排除 6 个预存 golden test 环境失败）
- `dart run build_runner build` — 成功
- `dart run tool/check_doc_coverage.dart --warning-only` — 已运行
