# 2026-07-24 代码审查建议项修复 + medicines 搜索 API 响应信封修复

## medicines 搜索 API 响应信封修复（跨项目变更）

### 背景

Lucent 后端 `medicines.controller.ts` 的 `search` 方法返回了非标准信封（`{ code, message, data, meta }`），`meta` 字段放置分页信息。阶段 8 将其统一为标准 `successEnvelope`（`{ code, message, data }`），分页信息移入 `data.pagination`。

### 前端修改

- **重新生成 API 客户端** — `generated/lucent_api` 中 `MedicineSearchResponseDto.data` 类型从 `List<MedicineSearchItemDto>` 变为 `MedicineSearchDataDto`（包含 `items` 和 `pagination`），`meta` 字段移除，新增 `MedicineSearchDataDto` 模型，删除 `MedicineSearchMetaDto`
- **`lib/features/search/data/repositories/lucent.dart`** — `response.data.map(mapper.dtoToResult).toList()` → `response.data.items.map(mapper.dtoToResult).toList()`

### 验证

- `flutter analyze lib/features/search/` — No issues found

## 审查文档遗留建议项修复

### 背景

`plans/Luminous-review-2026-07-24.md` 审查报告"备注（非问题）"部分指出两个建议项，本轮逐一修复。

### 建议 1：error_code.dart 遗漏的中文 dartdoc

`docs(l10n)` commit（`12107534`）翻译了 `error_code.dart` 中 13 条中文 dartdoc，但遗漏了 `badResponse` 和 `unknown` 两条。

**文件:** `lib/core/network/error_code.dart`

| 行 | 旧 | 新 |
|----|----|----|
| 37 | `/// HTTP 状态码错误` | `/// HTTP status code error` |
| 40 | `/// 未知网络错误` | `/// Unknown network error` |

### 建议 2：冗余 null-aware 操作

`api_exception.dart` 第 34 行 `requestId != null && (requestId?.isNotEmpty ?? false)` 在已判空后仍使用 `?.` + `?? false`，`error.dart` 第 86 行 `requestId != null && requestId!.isNotEmpty` 同理。

统一为 `requestId != null && requestId!.isNotEmpty`（`!` 因 public final 字段不支持类型提升而必须保留）。

**文件:**

| 文件 | 行 | 旧 | 新 |
|------|----|----|----|
| `lib/core/network/api_exception.dart` | 34 | `requestId != null && (requestId?.isNotEmpty ?? false)` | `requestId != null && requestId!.isNotEmpty` |
| `lib/core/errors/error.dart` | 86 | 已为 `requestId != null && requestId!.isNotEmpty`（无需改动） | — |

### 验证

- `flutter analyze` — 0 issues（三个文件）
- `dart run tool/check_doc_coverage.dart --warning-only` — 已运行

## Barrel 导出清理

### 背景

`lib/` 中 20 个文件含 `export` 语句，其中 15 个为 barrel 滥用（5 个纯 widget barrel 文件 + 10 个非 barrel 文件夹带跨层 re-export）。本次清理移除全部 15 个滥用，保留 5 个合理的 `core/` barrel 和平台条件 export。

### 阶段 1 — 移除非 barrel 文件中的跨层 export

从 10 个 provider/service/page/datasource 文件中移除夹带的 `export` 语句，消费者改为直接导入源文件：

| 文件 | 移除的 export | 消费者改为直接 import |
|------|--------------|---------------------|
| `notification/presentation/providers/notification.dart` | `unread_count.dart`, `repositories/lucent.dart` | 8 个消费者（lib + test + integration_test） |
| `legal/presentation/providers/legal.dart` | `repositories/lucent.dart` | 3 个 test 消费者 |
| `settings/presentation/providers/user_settings.dart` | `repositories/lucent.dart` | 无（消费者不直接使用 repository provider） |
| `support/data/providers/resources.dart` | `repositories/lucent.dart` | 无 |
| `medicine/presentation/providers/reminders.dart` | `reminder_sound_preference.dart` | 9 个消费者（lib + test） |
| `medicine/presentation/providers/safety_tips.dart` | `safety_tip_style.dart` | 无 |
| `medicine/domain/services/risk_checker.dart` | `risk_medicine_detail.dart` | 3 个消费者（lib + test） |
| `medicine/data/datasources/dose_log_cached.dart` | `dose_log_remote.dart` | 5 个消费者（lib + test） |
| `auth/presentation/widgets/shared/shell.dart` | `branding.dart` | 4 个消费者（lib + test） |
| `auth/presentation/pages/account_settings.dart` | `account_settings_sections.dart` | 无 |

### 阶段 2 — 删除 5 个纯 widget barrel 文件

| 删除的 barrel 文件 | 消费者改为直接 import |
|-------------------|---------------------|
| `report/presentation/widgets/shared/sections.dart` | 2 个消费者（dashboard_view, page） |
| `mine/presentation/widgets/shared/sections.dart` | 2 个消费者（dashboard_view, page） |
| `search/presentation/widgets/shared/headers.dart` | 2 个消费者（view, results） |
| `record/presentation/widgets/shared/overview.dart` | 1 个消费者（dashboard_view） |
| `medicine/presentation/pages/reminders.dart` | 2 个消费者（routes, test） |

### 阶段 3 — AGENTS.md barrel 导出规范

在 `AGENTS.md` 中新增 "Barrel Exports" 章节，明确：
- 仅 `core/` 跨切面 barrel（`design.dart`、`api.dart`、`state_views.dart`）为合法 barrel
- 禁止 feature 级 barrel 文件
- 禁止跨层 re-export
- 平台条件 export 豁免

### 阶段 4 — `core/network/api.dart` 外部包 re-export 审查

`api.dart` 中 `export 'package:lucent_api/api/export.dart';` 经审查保留：14 个文件通过 `api.dart` 导入，其中 10+ 个依赖 `lucent_api` 类型（`LucentClient`、`*Api`）的 re-export。移除将迫使每个消费者添加单独的 `lucent_api` import，无实际收益。已添加注释说明理由。

### 验证

- `flutter analyze` — No issues found
- `flutter test --exclude-tags golden` — 2751 tests passed（6 个 golden 测试为预存在环境差异失败）

## LucentTodayRepository 显式依赖注入重构

### 背景

`LucentTodayRepository` 是项目中唯一持有 `Ref ref` 的 repository——其余所有 repository（`LucentAuthRepository`、`LucentMedicineWorkspaceRepository`、`LucentTodayAiRepository` 等）均已采用构造函数显式注入依赖。此次重构消除该不一致。

### 修改

**`lib/features/today/data/repositories/lucent.dart`** — 移除 `final Ref ref`，替换为 6 个显式依赖：

| 依赖 | 类型 | 说明 |
|------|------|------|
| `fetchHealthContextSnapshot` | `Future<HealthContextSnapshot> Function()` | 回调形式，保留原 `ref.read(provider.future)` 在调用时读取的行为 |
| `dailyRecordRepository` | `DailyRecordRepository` | 直接注入 |
| `cachedDoseLogDataSource` | `CachedDoseLogDataSource` | 直接注入 |
| `userSettingsRepository` | `UserSettingsRepository` | 直接注入 |
| `medicineReminderRemoteDataSource` | `MedicineReminderRemoteDataSource` | 直接注入 |
| `talker` | `Talker` | 直接注入 |

移除了对 `flutter_riverpod`、`logger.dart`、`health_context/data/providers/health_context.dart`、`workspace.dart`、`record_access.dart`、`settings/data/repositories/lucent.dart` 等 provider 文件的 import。repository 现在仅依赖 domain 接口和 data source 类。

**`lib/features/today/data/providers/today_suggestion.dart`** — `todayRepositoryProvider` 改为在 provider 函数中 `ref.watch` 全部依赖并传入构造函数。

### 验证

- `flutter analyze` — 0 issues
- `flutter test test/today/` — 231 tests passed
- `flutter test test/auth/session_gate_test.dart` — 3 tests passed

## Patrol 集成测试基础设施修复

### 背景

项目使用 patrol（^4.6.1）作为集成测试框架，但 Windows 下运行 `patrol test` 存在两个阻塞问题：
1. `patrol_cli` 4.4.0 与 `patrol` 4.6.1 版本不兼容（compatibility checker 报错）
2. `adb` 不在系统 PATH 中，patrol_cli 内部的 `adb` Dart 包调用 `io.Process.run('adb', [])` 时找不到可执行文件
3. 缺少 `MainActivityTest.java` androidTest 入口类和 Android Test Orchestrator 配置，导致 `PatrolJUnitRunner` 无法发现 Dart 测试

### 修复

#### 版本升级

- `patrol` 包：`^4.6.1` → `^4.7.1`（pubspec.yaml）
- `patrol_cli`：`4.4.0` → `4.6.0`（`dart pub global activate patrol_cli`）
  - 按 [兼容性表](https://patrol.leancode.co/documentation/compatibility-table)，patrol_cli 4.5.0+ 兼容 patrol 4.7.0+

#### adb PATH 问题

patrol_cli 内部的 `adb` Dart 包直接调用 `io.Process.run('adb', [])`，不搜索 Android SDK 目录。adb 位于 `D:\AndroidStudioSdk\platform-tools\adb.exe` 但不在系统 PATH 中。运行测试时需将该路径临时加入 PATH：
```powershell
$env:PATH = "D:\AndroidStudioSdk\platform-tools;$env:PATH"
```

#### Android Test Orchestrator 配置

**`android/app/build.gradle.kts`**:
- `defaultConfig` 中添加 `testInstrumentationRunner = "pl.leancode.patrol.PatrolJUnitRunner"`
- `defaultConfig` 中添加 `testInstrumentationRunnerArguments["clearPackageData"] = "true"`
- 新增 `testOptions { execution = "ANDROIDX_TEST_ORCHESTRATOR" }`
- `dependencies` 中添加 `androidTestUtil("androidx.test:orchestrator:1.5.1")`

#### MainActivityTest.java

**`android/app/src/androidTest/java/com/dev/luminous/MainActivityTest.java`** — 新建。`@RunWith(Parameterized.class)` 入口类，`@Parameters` 方法中调用 `PatrolJUnitRunner.setUp()` → `waitForPatrolAppService()` → `listDartTests()` 动态发现 Dart 测试，每个 Dart 测试作为参数化测试用例执行。

#### 测试代码修复

**`integration_test/support/e2e_test_helpers.dart`** — `_shellTabForLabel` 的 switch 中添加 `'今日'` 匹配（原仅匹配 `'今天'`），修复 `openTab($, '今日')` 无法识别 ShellTab 导致 fallback 到 `NavigationBar` finder 的问题。

### 验证

- `patrol test --target integration_test/today/today_e2e_test.dart --device emulator-5554`
- 5 个测试全部被发现并执行：2 个通过（assistant entry、tab navigation），3 个失败（时序问题，emulator 渲染慢导致 `settleE2e` 的 6 帧 100ms 不够，需后续调整）

### 运行方式

```powershell
cd Luminous
$env:PATH = "D:\AndroidStudioSdk\platform-tools;$env:PATH"
dart pub global run patrol_cli:main test --target integration_test/today/today_e2e_test.dart --device emulator-5554 --verbose
```

## l10n 翻译质量优化

### 背景

全量审查 11 个 ARB fragment（22 个文件），找出翻译不够精确、不够自然的键值对并修复。

### 修改的 fragment 文件及键

| Fragment | 键 | 旧值 | 新值 | 原因 |
|----------|-----|------|------|------|
| `today_zh` | `todayPrimarySuggestionSectionTitle` | 现在最该处理 | 优先处理 | 原译文生硬不自然 |
| `today_zh` | `todayPrimarySuggestionBadge` | 最高优先 | 最高优先级 | 缺"级"字，语法不完整 |
| `today_zh` | `todaySecondarySuggestionSectionTitle` | 接下来可处理 | 稍后处理 | 原译文被动且含糊 |
| `today_zh` | `todayQuickActionSectionTitle` | 轻动作 | 快捷操作 | "轻动作"语义错误 |
| `today_zh` | `todayStatusNeedsImprovement` | 待提升 | 待改善 | "改善"在健康语境中更自然 |
| `today_zh` | `todayGreetingTitleAfternoon` | 继续稳稳推进 | 继续保持节奏 | "稳稳"过于口语化 |
| `today_zh` | `todayObservationMedicineNote` | 轻量提示 | 参考性提示 | "轻量"为技术用语 |
| `today_zh` | `todayObservationSleepNote` | 更像值得留意的提示… | 作为留意事项参考，并非今日待办。 | 原译文结构松散 |
| `today_zh` | `todayObservationReportNote` | 不需要抢占今天的主行动位 | 无需作为今日重点行动 | "抢占主行动位"表述生硬 |
| `today_zh` | `todayMealRefreshAction` | 换一换 | 换一组 | "换一换"过于口语 |
| `today_zh` | `todayRecommendationCoffeeBody` | 影响睡眠质量 | 以免影响睡眠质量 | 缺少因果连词 |
| `today_zh` | `todayObservationReviewTag` | 去看看 | 去查看 | "去看看"过于随意 |
| `today_zh` | `todayMedicationSuggestionTitle` | 该确认用药了 | 该确认服药了 | "服药"比"用药"更准确 |
| `today_zh` | `todaySummaryFallbackNarrative` | 就可以放心了 | 即可安心收尾 | 原译文过于口语 |
| `today_en` | `todayStatusNeedsImprovement` | Needs lift | Needs improvement | "Needs lift"非标准英语 |
| `today_en` | `todayObservationLowConfidenceTag` | FYI | For reference | "FYI"过于随意 |
| `today_en` | `todayObservationMedicineNote` | lightweight hint | reference hint | "lightweight"为技术用语 |
| `report_zh` | `reportStatusNeedsImprove` | 待提升 | 待改善 | 与 today 统一用词 |
| `report_en` | `reportStatusNeedsImprove` | Needs lift | Needs improvement | 与 today 统一用词 |
| `medicine_zh` | `medicineStatusNeedsCheckin` | 稳定服用 | 待打卡 | 原值与 `medicineStatusStable` 相同（copy-paste 错误） |
| `medicine_zh` | `medicineReminderNotificationDefaultBody` | 该按时吃药了。 | 请按时服药。 | 原译文口语化 |
| `medicine_zh` | `medicineRiskCheckSeverityMedium` | 需确认 | 待复核 | "复核"更准确 |
| `medicine_en` | `medicineStatusNeedsCheckin` | Stable routine | Needs check-in | 原值与 `medicineStatusStable` 相同（copy-paste 错误） |
| `medicine_en` | `medicineSafetyGuardLabel` | Safe guard | Safety guard | 词形错误 |
| `notification_en` | `notificationActionNavigate` | Go to handle | Take action | "Go to handle"非标准英语 |
| `notification_en` | `notificationTypeIdentityLinked` | Linkage | Account linked | "Linkage"作为通知类型名不自然 |
| `common_zh` | `placeholderAddToUnlock` | 记录后解锁 | 添加记录后解锁 | 缺少动词 |
| `mine_zh` | `mineSectionSubtitle` | 共享在这一块安静的界面里 | 统一呈现在这个安静的页面中 | 原译文不自然 |

### 验证

- `dart scripts/arb_tools.dart merge` — 合并成功（zh: 1845 entries, en: 1843 entries）
- `flutter gen-l10n` — 成功
- `flutter analyze` — No issues found
