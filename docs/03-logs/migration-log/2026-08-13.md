## Review Experience — Task 4 Flutter Review 领域层

- 新增 Review 领域实体（`lib/features/report/domain/entities/review.dart`）：以健康事件为主单位的 `EventReview` 读模型，包含事件头部、四段 sections、三数据源 coverage、source timestamps 与 available actions。section state / coverage / source 的未知值映射为显式 `unknown` 成员，不折叠成空列表或 0；reasonCode 已知码按原文保留，未知码在生成 DTO 反序列化层被折叠为 `unknown_default_open_api` 占位符。
- 新增 `ReviewRepository` 接口（current / history / detail）与 `LucentReviewRepository` 实现：current 优先 active 事件否则最近 ended，无事件时空信封映射为 null；history 支持 status/cursor/limit 分页；detail 按事件 ID 取完整回顾，事件不存在时由后端 404 抛错。
- 新增 `reviewCurrent / reviewHistory / reviewDetail` provider：current 为主（keepAlive + dailyRecords/doseLogs 变化自动刷新），`reviewLastCurrentProvider` 保留最后一次成功数据；history keepAlive 缓存第一页；detail 为 autoDispose family，切换事件 ID 丢弃旧结果。失败重试由 `ref.invalidate` 驱动。
- `scripts/bootstrap_generated_sources.dart` 改为 TodayAnalysis 与 Reports 两次过滤生成（openapi-generator-cli 7.x 的 `apis` 只接受单值），generated client 新增 14 个 event review DTO、`reports_api.dart` 的 3 个 review 方法及 barrel/deserialize 同步。
- 验证：`flutter analyze` 无问题；`flutter test test/report` 156 通过（其中新增 repository/provider 测试 15 个）；`dart run scripts/check_doc_coverage.dart --warning-only` 通过。

## Review Experience — Task 4 审查修复

- `reviewLastCurrentProvider` 改为通过 `ref.listen(reviewCurrentProvider)` 只在被采纳的 AsyncData 上更新：riverpod 丢弃被更新重建覆盖的陈旧 fetch 完成，消除快速连续刷新时慢请求迟到覆盖新数据的竞态；登出/会话失效产生的 null 数据会清空缓存，不向新会话泄漏上一用户的 review。
- 新增 `DataChangeTopic.healthEvents`：health_event 的 create / end（含 outcome 确认）/ checkIn 在服务端确认成功且身份未变后发射（`ActiveHealthEvent` 内，不在 UI 点击处）；`reviewCurrentProvider` 与 `reviewHistoryProvider` 均 watch 该 topic 自动刷新。
- reasonCode 文档措辞修正为如实：已知码按原文保留，未知码在生成 DTO 反序列化层被折叠为 `unknown_default_open_api` 占位符；facts arguments 映射为 `Map.unmodifiable` 拷贝。
- `bootstrap_generated_sources.dart` 的过滤客户端改为 `_filteredClients` 数据驱动配置（新增合同面只需追加一条记录），生成结果与改动前一致。
- 补测试：healthEvents 变化触发 current 重新 fetch、history 的 status/cursor/limit 参数转发、登出清空 lastCurrent；保留失败保留旧数据 + retry 语义测试。
- 验证：`flutter analyze` 无问题；`flutter test test/report` 全绿；`dart run scripts/check_doc_coverage.dart --warning-only` 通过。

## Review Experience — Task 5 第五 Tab 用户任务名改为回顾

- 第五 Tab 用户可见任务名从「报告/Report」改为「回顾/Review」：只改 `tabReport` ARB key（`common` 分片）的取值，覆盖底部导航与桌面侧栏标签、Report 页 app bar 标题（`ReportTopBar` 与 `DesktopTabShell`）和命令面板入口，accessibility semantics 随标签文本生效。
- 保留不动：`/report` 路由路径、`ShellTab.report` 枚举、`SemanticIcons.tabReport` 图标 token、`features/report` 目录、telemetry key 与 deep link 行为（代码层重命名留待兼容期结束后评估）；导出/历史兼容上下文（`reportExport*`、`reportSuggestionHistory*`、诊所摘要等）文案保持 Report 口径。
- `Routes.report` 增加兼容路由注释；`docs/02-reference/Localization.md` 同步 l10n 所有权说明；生成文件 `app_*.arb` 与 `app_localizations*.dart` 为 gitignore 产物，仅提交 `lib/l10n/src/common_*.arb` 分片。
- 测试（先写后实现）：`tab_test.dart` 增加 zh 回顾 / en Review label 断言；`router_test.dart` 增加 `/report` 深链渲染第五 Tab（`ReportPage` + shell branch index）与标签文本断言。
- 验证：`flutter test test/app/router_test.dart test/shell/tab_test.dart` 全绿；`flutter analyze` 无新增告警；`dart run scripts/check_doc_coverage.dart --warning-only` 通过。

## Review Experience — Task 6 事件优先移动端视图

- 新增 `ReviewView`（`presentation/widgets/views/review_view.dart`）：事件头部 + 四段 + 历史的事件优先首屏，覆盖六种状态——loading（骨架屏）、active（今日 check-in）、ended（outcome）、partial（unknown 段显示 reasonCode 本地化的简短缺失原因，无 0 分/红色「需关注」）、no-event（「开始健康观察」入口 + 最近事件，无事件时轻量解释不生成周报）、error-with-cache（保留旧数据 + 轻量 stale 提示；无缓存时 `StateErrorView` + 重试）。布局用移动端约束，未新增桌面专属 breakpoint/sidebar。
- 新增六个 section widgets：`event_header`（状态 chip、今日 check-in、结束入口、ended outcome）、`what_happened`（`health_event` facts）、`key_changes`（`observed_changes`：结果变化 + 饮水/睡眠数值趋势，只陈述不做因果推断）、`completed_actions`（`completed_actions`：服药槽位计数 + 确认记录，最多 5 条折叠）、`next_step`（`active_check_in` / `event_ended` + `redFlags` warning 色调结构化展示）、`review_history`（按事件逐条列出，不按月份分组，失败仅一行提示）。共享 `review_section_card.dart`（卡片骨架 + 缺失原因行）与 `review_formatters.dart`（契约参数防御性读取、日期/结果/方向本地化）。
- `page.dart` 主内容切换为 `ReviewView`（基于 review 三个 provider）：事件交互复用 health_event 的 `StartEventSheet` / `CheckInSheet` / `EndEventSheet` 与 `ActiveHealthEvent` notifier（沿用 today 的既有装配惯例），成功后由 DataChangeBus 自动刷新 review。旧 dashboard 视图代码（`dashboard_view.dart` 及 sections、`top_bar.dart` 等）保留原文件未动、不再装配；page.dart 中旧 dashboard/导出/摘要的装配代码随切换移除（Task 7/8 收尾）。
- l10n：`report_zh.arb` / `report_en.arb` 分片新增 62 个 `reportReview*` key（另有 18 条 placeholder 元数据）（段落标题、状态/结果/类别、reason code、fact 文案、red flag、历史、无事件、错误与 stale 文案，含 placeholder 元数据），执行 `dart scripts/arb_tools.dart merge` + `flutter gen-l10n`（生成文件为 gitignore 产物）。
- 测试（先写后实现）：`review_view_test.dart` 覆盖六状态 + 预览态与段落顺序；`event_header_test.dart` 覆盖 active/已确认/ended；`review_sections_test.dart` 覆盖四段 available/unknown 与历史三态；`page_test.dart` 按新主内容重写（review-first 布局、骨架、preview、无事件、下拉刷新、错误重试、桌面同布局）。旧 dashboard 导出等页面级测试随主内容切换移除（Task 8 在 More 中重建导出测试）。
- 文档：`docs/00-current/Active_UI_Report.md` 新增「Review 事件优先视图」节并标注旧 dashboard 为保留未装配的 legacy；`docs/02-reference/Localization.md` 记录 `reportReview*` 所有权。
- 验证：`flutter analyze` 无问题；`flutter test test/report` 182 全绿（新增 28 个 section/view widget 测试；page_test 按新主内容重写为 7 个用例）；`dart run scripts/check_doc_coverage.dart --warning-only` 通过。

## Review Experience — Task 6 代码审查修复

- zh 窗口文案 `reportReviewWhatHappenedWindow` 模板从 `"{start} 至 {end}"` 改为 `"{start} – {end}"`：与「至今」分支组合后不再出现「8月1日 至 至今」的重复「至」；调用点（whatHappened、header/history 的 `–` 分隔）无需改动。
- `key_changes` 数值趋势 direction 缺失/未知时不再伪装「持平」，新增 `reportReviewChangeDirectionUnknown`（zh「方向未知」/ en「Unknown direction」），`reviewTrendDirectionLabel` 对 null 与未知值如实显示。
- `completed_actions` 的 check-in 记录日期改经 `reviewShortDateLabel` 本地化（`8月2日 · 好转`），与 header/history 一致；日期缺失时保留占位符。
- `review_history` 失败态新增轻量 inline 重试按钮（`review-history-retry`，调 `ref.invalidate(reviewHistoryProvider)`），经 ReviewView 新增的 `onHistoryRetry` 回调由 page 装配。
- page 层交互接线测试补三个用例（`test/report/page_test.dart`，override `healthEventRepositoryProvider` + `healthContextSnapshotProvider`）：「开始观察」提交调 create(title)；check-in 以当前 review 的 event id（`evt-active`）调 checkIn 并断言本地 YYYY-MM-DD 日期；结束入口调 end。另补 `review_formatters_test.dart`（日期解析失败回退、direction 未知兜底）与 history loading 分支测试。
- 跟进项登记：Review 页「开始观察」入口目前只转发标题，不带触发症状（reasonRecordId）与关联用药（currentMedicineIds）选项——留待 Task 7/8 与 today 的 `_openStart`（health context snapshot + 当日记录预读）对齐后补齐，已在 `page.dart` `_openStart` 注释登记。
- 验证：`flutter analyze` 无问题；`flutter test test/report` 全绿；`dart run scripts/check_doc_coverage.dart --warning-only` 通过。

## Review Experience — Task 7 主路径移除旧 dashboard

- 回归测试锁定主路径：`test/report/page_test.dart` 断言 primary page 不构建 `ReportScoreHero` / `ReportExportSection` / `ReportReadinessSection`（`canShowFullReport` 整页锁所在）、不含 7/30 天范围切换（`ReportTopBar` / `ReportRangeMenu` 只存在于 legacy 文件）、旧 `reportDashboardProvider` 被 override 为必失败时 review 主路径照常渲染（review providers 与 dashboard providers 解耦）。
- legacy 收尾：`dashboard_view.dart` 与 `shared/top_bar.dart` 文件头加 LEGACY 标注（兼容期保留、不再装配、Task 8 评估删除）；不删除任何文件；`skeleton_view.dart` 因仍被 ReviewView 骨架屏复用不标注 legacy。primary top bar（`page.dart` `_ReviewTopBar`）确认无 7/30 切换。
- 历史筛选按现有合同实现 status 轻量筛选：新增 `reviewHistoryStatusProvider`（null=全部 / active / ended），`reviewHistoryProvider` watch 该状态并在切换时带 status 重新拉取（`skipLoadingOnRefresh` 避免骨架闪动）；`ReviewHistorySection` 新增全部/进行中/已结束筛选按钮。时间范围不是 review list 合同的一部分（合同只有 status/cursor/limit），客户端不发明日期过滤，已在 provider 注释与 Active_UI_Report 文档如实说明。
- 单维趋势口径锁定：`observed_changes` 只在后端判定 coverage sufficient 时返回，客户端在 section unknown（`insufficient_coverage`）时只显示缺失原因、不渲染任何趋势行；补测试锁定 unknown 维度不影响其他可用 section（whatHappened / nextStep 照常渲染）。
- 「开始观察」对齐 today（登记跟进项闭环）：`page.dart` `_openStart` 预读 health context snapshot 的当前用药选项与按用户时区当天解析的症状记录选项（`dailyRecordListForDateProvider`），向 `create()` 转发 `reasonRecordId` / `currentMedicineIds`；选项加载失败静默降级为空列表。创建成功仍由 DataChangeBus（healthEvents topic）自动刷新 review。
- 共享 util 提取（登记跟进项闭环）：`_localDateKey` / `_readUserTimezone` 提取为 `lib/core/utils/local_date.dart`（`localDateKey` / `readUserTimezone`），today 与 report 共用；三处 status chip 未提取共享组件（event_header / review_history / key_changes 的 chip 装饰逻辑各自独立且改动面会牵动既有测试，本次跳过）。
- l10n：`report_en.arb` / `report_zh.arb` 分片新增 `reportReviewHistoryFilterAll`（全部/All）；active/ended 筛选按钮复用 `reportReviewStatusActive` / `reportReviewStatusEnded`。执行 merge + `flutter gen-l10n`（生成文件为 gitignore 产物）。
- 文档：`docs/00-current/Active_UI_Report.md` 更新 Task 7 收尾事实（status 筛选、开始观察对齐、legacy 标注、回归约束）；`docs/00-current/Active_UI_Today.md` 记录时区/日期键共享工具；`docs/02-reference/Localization.md` 记录筛选 key 所有权；`plans/2026-08-07-review-experience.md` 删除已完成的 Task 7 节。
- 验证：`flutter analyze` 无问题；`flutter test test/report` 198 全绿（page_test 新增 4 个回归用例）；`flutter test test/today test/health_event` 283 全绿（util 提取影响面）；`dart run scripts/check_doc_coverage.dart --warning-only` 通过。

## Review Experience — Task 7 代码审查修复

- `reviewHistoryProvider` 关闭 riverpod 默认指数退避自动重试（`@Riverpod(keepAlive: true, retry: _noHistoryRetry)`）：筛选切换失败立即进入 AsyncError，由 section 内轻量失败行 + inline 重试承接，而不是被 ~40s 静默重试掩盖；补 provider 级测试（无容器级 retry 覆盖时失败立即暴露且 repository 只调用一次）与 page 级测试（筛选调用抛错时失败行 + 重试按钮立即渲染）。
- `_openStart` 的两类选项读取改为 `(f1, f2).wait` 并行，happy path 等待减半；`_readReasonRecordOptions` 内的 `readUserTimezone` 加 2s 客户端上限（有意的快速降级取舍：选项读取失败只影响可选关联项、静默降级为空列表，快于 health context provider 自身的 5s 超时）。
- 注释修正：page.dart 类注释改为 Task 7 已完成口径并指向 LEGACY 文件头注；top_bar.dart LEGACY 头注消费者修正为实际（dashboard_view 与 trend）；local_date.dart 删除死代码初始化行。
- 补测试：review_sections_test 断言选中筛选按钮 primary 变体/未选中 outline；page 级筛选测试用 pageResolver 按 status 返回不同页面，断言列表实际更新为过滤后数据而非沿用旧页。
- 验证：`flutter analyze` 无问题；`flutter test test/report test/today test/health_event` 484 全绿；`dart run scripts/check_doc_coverage.dart --warning-only` 通过。

## Review Experience — Task 8 导出与就诊摘要迁入 More

- 新增 `presentation/widgets/sheets/more_actions.dart`：回顾页右上角「更多」sheet（`showReportMoreActionsSheet` + `ReportMoreActionsSheet`），四项入口——就诊摘要（打开 `showClinicSummaryPreviewDialog`，旧 clinicShare 流程）、PDF（月度导出 `reportMonthlyPdfExportRequest`）、打印/下载（打印导出 `reportPrintPdfExportRequest`）、历史报告（push `/report/legacy` 兼容页）；移动端 bottom sheet / 桌面端 dialog 双形态，入口点击先关 sheet 再触发流程。
- 导出/分享行为保持旧装配不变：`_handleExportAction` / `_handleExportResult` 从旧 page.dart 提取为共享 `presentation/utils/export_actions.dart`（`handleReportExportAction`），登录门槛 → clinicShare 预览 → security elevation → dataExport POST → 下载链接/toast 的链路与 d8c9c5f5e 完全一致，供 More sheet 与 legacy 兼容页复用；不改变任何后端 API 调用与数据流。
- 新增 legacy 兼容页 `presentation/pages/legacy_dashboard_compat.dart`（`LegacyDashboardCompatPage`）：按 d8c9c5f5e 旧 page 的 dashboard 装配重建（readiness 首卡 + 趋势/发现/建议历史 + AI 总结/规律 + 四张导出卡 + 7/30 天切换 ReportTopBar + 下拉刷新 + 旧数据缓存），路由 `/report/legacy`（`Routes.reportLegacyDashboard`，slide 进入、含 AppBackButton），沿用 `/report` 的公开预览语义（未登录显示 preview + 登录引导）；取舍：只重建移动端布局（与 ReviewView 一致，桌面宽度同一布局），不复刻旧 DesktopTabShell 双栏外壳。所有 legacy 文件未删除。
- `page.dart` 顶栏 `_ReviewTopBar` 右上角加 More 按钮（`review-more-action`，tooltip 更多）并接线四入口回调；主路径回归锁定：首屏无四张导出卡、无 `ReportExportSection`、无 7/30 切换。
- 文案口径：More sheet 就诊摘要入口用「就诊时按需使用 / Use as needed during your visit」，不暗示医生一定查看；`reportExportClinicShareTitle` 与 `reportClinicSummaryShare` 由「分享给医生/Share with doctor」改为「分享摘要/Share summary」（分享仍为用户显式动作，API 行为不变）；字段隐私与访问测量留待后续计划。
- l10n：`report_zh.arb` / `report_en.arb` 分片新增 9 个 `reportMore*` key（更多/就诊摘要/PDF 报告/打印下载/历史报告 的标题与副标题），执行 merge + `flutter gen-l10n`（生成文件为 gitignore 产物）。
- 测试（先写后实现）：新增 `test/report/widgets/more_actions_test.dart` 8 个用例——首屏无导出卡、sheet 四入口可见且无「分享给医生」文案、四入口回调触发、sheet 开合、页面级 More 开合、就诊摘要打开诊所预览、PDF/打印触发 PIN 验证环节、历史报告 push legacy 兼容页（迷你 GoRouter）。
- 文档：`docs/00-current/Active_UI_Report.md` 新增 Task 8 节；`docs/02-reference/Localization.md` 记录 `reportMore*` 所有权与分享口径变更；`docs/02-reference/routing.md` 登记 `/report/legacy`；`docs/00-current/TODO.md` 删除已闭环的「导出移入更多」行；`plans/2026-08-07-review-experience.md` 删除已完成的 Task 8 节。
- 验证：`flutter analyze` 无问题；`flutter test test/report` 209 全绿（含 clinic summary provider/content 测试）；`flutter test test/app test/shell` 96 全绿；`dart run scripts/check_doc_coverage.dart --warning-only` 通过。

## Review Experience — Task 8 审查修复

- 测试缺口补齐（`test/report/widgets/more_actions_test.dart` 新增 2 个用例，共 10 个）：(a) override `securityElevationControllerProvider`（已持有有效 elevation token，直接越过 PIN 弹窗）与抛错的 `dataExportControllerProvider`，点击 more-pdf 后断言以 `reportMonthlyPdfExportRequest` 输入发起请求、`handleReportExportAction` 的 Failure 分支弹出「导出失败」toast（测试用 FToaster 包裹页面，与生产装配一致，toast 计时器走完再结束测试）；(b) 未登录状态点击 more-pdf 不发起导出，断言 `auth-required-dialog` 与「未登录」文案出现（沿用既有未登录 override 惯例）。
- `page.dart` 删除 `_openClinicSummary` 的登录守卫副本：就诊摘要入口改为复用 `handleReportExportAction(clinicShare)`（守卫与跳转目标 `/report` 行为不变，与 PDF/打印同一路径）；More sheet 的 future 加 `unawaited()` 显式弃置。
- 验证：`flutter analyze` 无问题；`flutter test test/report` 211 全绿；`dart run scripts/check_doc_coverage.dart --warning-only` 通过。

## Review Experience — Task 9 视觉/无障碍/状态验证

- a11y 语义顺序（要求：事件标题 → 状态/结果 → 四段 → 历史 → More）：
  - `event_header.dart` 调整事件头部卡片顺序——事件标题移到最前，状态/结果 chip 行移到标题下方（视觉与语义顺序一致；原 chip 在标题上方，TalkBack 会先读状态再读标题）。
  - 状态 chip 参与 flex 收缩并加单行省略（`Flexible` + `maxLines: 1` + ellipsis）：en 文案（如 "In progress" + "End observation"）在 390 宽度下不再横向溢出（RenderFlex overflow，en 矩阵回归）。
  - `page.dart` 顶栏与内容区加 `Semantics(container: true, sortKey: OrdinalSortKey(1/0))`：只调整语义遍历顺序（内容先读、顶栏后读，满足「历史 → More」），不影响视觉布局、焦点顺序与点击命中；`review-more-action` 的 `FButton.icon` 增加 `semanticsLabel: reportMoreTitle`——图标按钮此前在语义树里是空 label 的不可读按钮。
  - `next_step.dart` 用药安全提醒标题行加 `Expanded`：en 长标题（"Medicine safety notes"）不再横向溢出。
  - 新增全宽动作按钮 `widgets/shared/constrained_action_button.dart`（check-in / 开始观察使用）：LayoutBuilder 在按钮外层取真实可用宽度，label 在按钮内容 Row 里被限宽换行/省略——Forui 按钮内容 Row 对非 flex 子项给无界宽度，长 en 文案（"Confirm today's result" 等，Ahem 测试字体为最坏宽度）此前直接撑破按钮 Row。
- golden 基础设施（仓库此前无 golden）：新增 `test/report/review_golden_test.dart`，四状态（active/ended/partial/no-event）手机宽度（390x844@1）核心 golden 四张，存 `test/report/goldens/review_{state}_zh_light.png`（zh + light + 常规字体；应用未打包自定义字体，golden 使用 Flutter 测试默认字体，中文以占位方块呈现，布局结构完全确定）；每张 golden 同步断言无综合分数（`report-score-hero`）、无整页 readiness 锁（`report-readiness-card`）、无默认导出矩阵（`report-export-section`）。
- 矩阵 widget 测试（不生成 golden，避免矩阵爆炸）：zh/en、dark、大字体（2x textScaler）各跑一遍四状态，断言 `tester.takeException() == null`（溢出即失败）+ 无 legacy 痕迹；另加大字体 + 超长标题 + unknown reason 组合不溢出用例。共 17 个用例（4 golden + 12 矩阵 + 1 长标题）。
- 新增 `test/report/widgets/review_semantics_test.dart`（5 用例）：按语义树深度优先遍历（`debugListChildrenInOrder(traversalOrder)`）收集 label，断言四状态内容顺序 + 整页含 More 的顺序；页面级断言用「按遍历顺序拼接的文本流」做子串位置检查（ListView 会把整段正文文本合并进一个语义节点，label 以换行拼接——该合并是 Flutter 滚动视图的既有语义行为，非本次引入）。
- 集成测试更新（离线/mock，Windows 桌面设备实跑）：
  - `integration_test/report/report_e2e_test.dart` 重写为 ReviewView 断言（6 用例）：signed-out 无事件卡 + 登录 banner（无 readiness 卡）、signed-in 无事件（开始观察入口 + 历史）、active 四段 + 历史、ended outcome、More 四入口 sheet、Tab 往返保状态。
  - `integration_test/shell/shell_navigation_e2e_test.dart` 报告 Tab 断言由 `report-readiness-card` 改为 `review-no-event-card`。
  - 新增 `integration_test/report/review_closed_loop_e2e_test.dart`：`E2eHealthEventLoopStore` 为 `HealthEventRepository` 与 `ReviewRepository` 共享的内存事实源，闭环「开始事件 → Record 写症状 → Medicine 槽位（桌面布局无剂量确认卡片，验证计划区渲染与数据源装配）→ Review 看到更新（DataChangeBus 自动刷新 active 事件 + 今日 check-in 入口）→ 结束并确认结果（Review 自动刷新为无事件 + 历史出现已结束事件与「好转」结果）」。
  - 环境限制（如实记录）：桌面窗口（1280x720）下 Today 健康事件卡片与 Medicine 剂量确认卡片为移动布局专属（桌面布局未做功能对等，与既有 medicine e2e 在桌面窗口失败同因），「开始事件」走 Review 无事件卡的同一入口（同 StartEventSheet + ActiveHealthEvent notifier + DataChangeBus 链路）；`tester.view.physicalSize` 缩到手机宽度会触发 riverpod 3.3.1 的 `UncontrolledProviderScope` setState-during-build 竞态（TickerMode 重建期间 subscription resume → 同步 invalidate），本环境无法以移动视口运行 e2e，未伪造运行结果。
- 验证：`flutter analyze` 无问题；`flutter test test/report` 233 全绿（golden 首次生成后复跑确定性通过，17 个 golden/矩阵用例 + 语义 5 用例）；`flutter test` 全量 3067 通过（1 跳过为既有）；`flutter test integration_test/report/report_e2e_test.dart integration_test/shell/shell_navigation_e2e_test.dart integration_test/report/review_closed_loop_e2e_test.dart -d windows` 11 个用例全绿；`dart run scripts/check_doc_coverage.dart --warning-only` 通过。

## Review Experience — Task 9 审查修复（规格审查反馈）

- Record 时间线 badge 桌面溢出修复并随本任务提交：`timeline_item.dart` 的 badge 行在桌面宽度下溢出 6.4px（确定性复现，阻断 `-d windows` e2e）。修复为 badge 外层加 `Flexible` 参与 flex 收缩 + badge 内层 Row 的文案也加 `Flexible`（内层 Row 对子项给无界宽度，仅外层限宽仍会在内层溢出，实际溢出点从外层 161 行转移到内层 193 行后一并修复）；过长时省略号截断。修复后三文件 e2e 11/11 全绿。
- 闭环 e2e 陈旧 key 修正：`tapMedicineDoseAction` helper 与闭环测试中的 `medicine-next-dose-action-*` 是旧 key，实际为 `medicine-plan-dose-action-*`（`mobile_records.dart`）；Medicine 剂量确认卡片桌面同样渲染（medicine 页桌面/移动共用 dashboard 视图），修正 key 后闭环第 3 步改为真实确认槽位并断言 mark 写入。
- `E2eDoseLogRemoteDataSource` 补 `mark` override：`_markDose` 实际走 `cachedDoseLogDataSourceProvider.mark`（`/medicine-dose-logs/mark` 端点，不再经 `create`），此前 fake 只覆盖 `create` 导致真实 HTTP 请求；mark 与 create 各自记录字段，`medicine_e2e_test.dart` 两个剂量用例断言同步改为 `mark*`（搜索用例仍为桌面既有失败，与本次无关）。
- golden 文档尺寸修正：golden 视口为 390 宽，内容高度随状态裁剪——`active/ended/partial` 为 390x844、`no_event` 为 390x517（此前文档笼统写 390x844@1）。
- 验证：三文件 e2e 分别实跑 `-d windows` 11/11 全绿（report 6 + shell 4 + 闭环 1；多文件单命令启动有 Windows 设备重启 flake，非测试失败）；`flutter analyze` 无问题；`flutter test test/report`、`flutter test test/record` 全绿；`dart run scripts/check_doc_coverage.dart --warning-only` 通过。
