# Migration Log — 2026-08-15

## fix(network): 认证拦截器回调保护与刷新失败日志

- `onSessionExpired` 回调包 try-catch：回调抛异常时记录日志后继续 `handler.next`，原请求不再挂起。
- token 刷新失败（DioException 与非 Dio 异常）记录 endpoint/statusCode 日志后返回 null，不再静默吞掉，便于生产排查。

## refactor(network): requireData 统一信封解包

- `envelope.dart` 新增 `requireData<T>(data, {operation})`：空响应错误消息携带 API 操作名，替代裸 `!` 崩溃。
- 替换 12 个数据层文件 25+ 处 `response.data!.data` 裸解包（含 assistant openConversation 的双层 `!`）。
- 调用点经 EnvelopeInterceptor 已保证信封非空，requireData 仅把失败路径变为可诊断的 StateError。

## fix(auth): 登出失败仍清除本地会话

- `logout()` 改 try-finally：登出请求 500/超时失败时本地 session 也强制清除，避免 token 残留。
- `_requireBody` 增加 operation 参数（22 处调用点带 API 名），空响应 StateError 可定位请求。
- 提取 `_trimOrNull` helper，收敛 8+ 处 trim + 空值判断重复（login/register/deleteAccount 与各 OAuth 方法）。

## fix(health-data): 去重分页循环页数上限

- `while(true)` 改为有界循环（每日期最多 100 页，500×100=5 万条记录上限），空页提前退出：后端 total 异常或页大小异常时不再无限请求 API。

## fix(record): 详情页分析轮询防重入与退避

- `Timer.periodic` 改为链式单次 Timer：invalidate 后 await provider future 覆盖整个请求周期，请求超时不再堆积并发。
- 失败按指数退避（5s → 10s → 20s → 30s 上限），成功回到基础间隔，dispose 照常取消。

## fix(record): NLP 失败状态与保存错误汇总

- generate 失败且保留旧候选时回到 reviewing 并清空 errorMessage，只有无候选可回退才进入 error 态，消除"审查中+错误提示"并存。
- 并行保存失败改为汇总去重后的全部错误信息（换行拼接），不再只暴露最后一个。

## fix(scan): OCR 批准文号输入截断与重复方法合并

- 批准文号匹配前截断 OCR 文本至 500 字符，防超长文本的正则开销。
- `_normalizeApprovalNumber`/`_cleanApprovalNumber` 合并为 `_applyOcrConfusion`（可选用 stripWhitespace），消除重复的字符映射遍历。

## fix(medicine): 通知 ID 线性探测上限

- `_allocateNotificationId` 线性探测加 1000 次上限，超限抛带上下文（已用 ID 数）的 StateError，防极端饱和死循环。

## fix(today): 建议缓存解码类型安全

- codec 全部裸 `as` 强制转换改为类型守卫 helper（_asString/_asMap/_asList）+ 默认值：缓存字段类型意外时按默认值恢复而非 TypeError 崩溃；枚举 fromJson 默认分支与调用方清缓存兜底保持。

## refactor(settings): 通知重置键列表

- `reset()` 的 14 个连续 `preferences.remove` 收敛为 `_resetKeys` 常量列表循环，新增设置项不会再遗漏重置路径。

## refactor(assistant): 滚动阈值常量

- 魔法数字 96 提取为 `_scrollProximityThreshold`（注明单位为逻辑像素），消除不同设备上的含义歧义。

## refactor(medicine): 药箱折叠常量

- 药箱最多展示行数 3 提取为 `_maxVisibleDrugboxItems`，take/比较/剩余计数三处统一引用。

## docs(core): 数据库 schema 版本历史注释

- `schemaVersion` 补注释：1 为初始六表，2 为 pendingSyncItems 增加 lastErrorDetails 列（对应 onUpgrade 迁移）。

## 方向计划修订:桌面 SaaS 路线调研论证(无代码变更)

- `plans/2026-08-14-product-surface-route.md` 修订:核心决策按 2026-08-14 讨论结论(临时草稿 `plans/adr-015-*`,非正式 ADR)——Flutter 只做手机 App + 移动 web(鸿蒙过渡),Flutter Desktop 与 Web(PC)冻结(打标签不删,不再维护);桌面工作台 = 独立 Next.js web 客户端(Lucent monorepo `apps/saas`),不复刻五大 Tab;路由边界:Flutter Web 检测宽度 > 768px 跳转 SaaS 登录页,不再维护「电脑浏览器跑 Flutter Web」形态;平台门控路由仅服务移动 web 能力降级。
- 调研结论(2026-08-15):判据「看清手机上看不清的东西」→ 核心能力为数据表/筛选/图表,web 生态碾压 Flutter(一票决定);WebView 性能澄清(WebView2=Chromium,瓶颈在数据层工程);升级路径不对称(web→Tauri 加壳可逆,Flutter→web 重写);Tauri 加壳触发条件两条,未满足不加壳。
- 范围:方向计划修订,无代码变更,不影响 0.1.0 发布门禁。

## docs(product): 长期健康伙伴方向与研究口径修正

- 产品总定位从“短期健康事件 + 自我用药”修正为长期健康伙伴：饮食、饮水、睡眠、心情、症状、活动和用药改为平级维度，健康事件保留为可选密集介入模式。
- 明确保留有覆盖率约束的日/周/月纵向洞察，删除综合健康评分和 legacy 泛化 AI 报告；餐食 AI 与上下文 Assistant 升为低负担输入和个性化理解的核心能力。
- GenUI 保留冻结、不删不推进；医疗问答语料保留并按说明书/科学数据库/开放问答分层；五个 Tab 等待后续单独讨论。
- 手机端继续承担当前首发与验证，桌面/Web 改为待研究方向；Next.js + Tauri 2 仅作候选，不写成已定技术决策。
- 市场研究明确区分公开证据、Luminous 产品假设、反证条件与一手用户验证，原用药市场和事件付费研究不再外推为整个产品方向。
- 本次只修改文档与未跟踪的研究材料，无运行时代码变化。

## docs(research): 用户价值证据补齐与旧口径一致性清理

- 新增长期健康伙伴用户价值验证，分别审查低负担输入、最小记录字段、主动建议打扰、上下文 AI 差异、非生病期价值和跨日/月数据信任；每项均区分外部证据、产品假设、反证条件与真实用户验证方法。
- 将原短期事件、用药、竞品和商业化材料明确降为领域或历史假设；删除自动判断可自药疗、自动生成疗程/购药/停换药建议、连续打卡和默认 CPS 等不符合当前定位或安全边界的产品结论。
- ADR-0011 标记为已被长期健康伙伴方向取代，同时保留其事件闭环已经实施的历史事实；五个 Tab 不在本轮调整，当前事件回顾与未来纵向洞察职责分开表述。
- 桌面/Web 改为当前不扩展但未来方向未决，Next.js + Tauri 2 只保留为大屏纵向阅读的待研究候选；GenUI 继续冻结保留，医疗问答语料继续保留并做信任分层。
- 本次无运行时代码变化。

## chore(contract): 重新生成 lucent_api 客户端

- Lucent 响应 DTO 信封契约修复(6 个 DTO 补 `data` 层)并重新 `export:openapi`;本仓库 `bootstrap_generated_sources.dart` 新增 `Notifications`/`UserSettings` 两个过滤生成组(此前仅 TodayAnalysis/Reports/ProductEvents,新增 schema 无生成来源),Reports/ProductEvents 模型列表补 `ClinicSummaryResponseDto`/`ClinicSummaryShareDataDto`/`FunnelDataDto`。
- 重新生成 `generated/lucent_api/`:6 个响应 DTO 均为 `{code,message,data}` 信封结构,新增 6 个 DataDto 模型。
- 契约快照同步至 `Lucent_Contract_Snapshot.md`。
- 验证:真实响应(通知列表/未读数)解析通过;`flutter analyze` 无告警。

## fix(notification): 列表与未读数适配信封契约

- `notification/data/repositories/lucent.dart`:访问路径改为 `dto.data.items/total/count`(生成 DTO 现为信封结构)。
- `test/notification/providers_test.dart`:mock 响应构造同步为 `NotificationListDataDto`/`UnreadCountDataDto` 信封。
- 验证:`flutter analyze` 无告警,notification 测试通过。

## refactor(report): clinic-summary 分享/预览改用生成客户端

- 契约修复后移除 raw Dio 手动解包 workaround:preview 走 `reportsControllerPreviewClinicSummaryV1`,share 走 `reportsControllerShareClinicSummaryV1`,shared 走 `reportsControllerGetSharedClinicSummaryV1`(保留 `skipAuthorization`);`api_paths.dart` 删除过时的信封解包常量与注释。
- `test/report/clinic_summary_provider_test.dart`:mock API 适配(openDialog stub preview/share、请求参数捕获),preview/shared 用例走真实客户端 + scripted adapter。
- 验证:`flutter analyze` 无告警,report 测试全量通过。

## refactor(core): security-pin 验证改用生成客户端

- `security_elevation.dart`:`verify` 由 raw Dio 手动解包改为 `userSettingsControllerVerifySecurityPinV1`(信封契约修复后生成客户端可直接解析);失败路径保持 `on Object` 吞错返回 false 语义。
- `test/settings/user_settings_controller_test.dart`:mock 响应构造同步为 `SecurityPinElevationDataDto` 信封。
- 验证:`flutter analyze` 无告警,settings 测试通过。

## chore(report): clinic-summary 调用与测试格式化收尾

- `clinic_summary.dart` preview 调用参数按 dart format 多行化;`test/report/clinic_summary_provider_test.dart` 与 `test/notification/providers_test.dart` 同步格式化对齐。无行为变更。
- 验证:`flutter analyze` 无告警,report/notification 测试通过。

## docs(research): 02-功能盘点按产品转型审核口径对齐

- 依据产品转型审核结论(`research/02-功能盘点/` 十份文档逐项修订,原审核文档已并入,不留独立副本):全部"删除"结论改写为"改造/归档"(保留代码与入口),并落定产品决策——提醒开关走"建议升级通知"(healthAlerts 细化 R1-R4 规则、每天最多 1 条、睡眠提醒先不管)、健康数据自动同步引入 workmanager、环境上下文接高德 API(城市手动选择、花粉/紫外线标注未实现)、kind 改回顾区筛选标签、weightKg 改体重记录维度、conditions 进助手上下文。
- 顺带修复 `docs/Glossary.md` 与 `docs/02-reference/adr/0011` 三处指向已不存在 `CONTEXT.md` 的坏链,统一改指 `docs/01-product/Product_Vision.md`;`Active_UI_Report.md` 同步 clinic-summary preview/share 走生成客户端的现行口径。
- 本次无运行时代码变化。

## fix(build): 解包修复 elk_icon_picker 的 web 图标 tree-shaking 失败

- 原因:`elk_icon_picker 0.1.3`(最新版,未修复)`icon_selection_preview.dart` 对 `ImportedIconSelection` 运行时构造 `IconData(codepoint, fontFamily: ...)`;web release 图标 tree-shaking 扫描到非 const IconData 直接报错,`flutter build web --release`(CI)失败。
- 方案:解包 0.1.3 到 `pkgs/elk_icon_picker-0.1.3/`(完整保留 LICENSE/LICENCE-LUCIDE/README),`pubspec.yaml` 用 `dependency_overrides` path 引用本地副本;该分支改为 const 兜底图标(`Icons.extension`),保留原因注释。此前尝试的 `patches:` 段被当前 pub 忽略,未采用。
- 影响:导入自定义字体图标(codepoint)在 web 端显示为占位图标;本应用只使用 Lucide 分支,无行为变化。
- 验证:`flutter analyze` 无告警;record quick-entry 相关测试全部通过;`flutter build web --release` 与 `flutter build web --wasm` 均构建成功(lucide.ttf 树摇裁剪 12.7%)。

## ci(cd): web 发布构建切换 wasm 目标(移动 web 兼容)

- `luminous-cd.yml` 的 Build Web step 增加 `--wasm`:产物为 wasm 主程序 + JS fallback,`flutter_bootstrap.js` 按浏览器 WasmGC 支持自动选择——现代安卓 Chrome 走 wasm(解析/启动更快),无 WasmGC 的浏览器(鸿蒙 ArkWeb、旧安卓 WebView)自动降级 JS,均可正常打开。
- GitHub Pages 无需额外配置:原生支持 `.wasm` MIME;未启用 wasm 多线程,不依赖 COEP/COOP 响应头。
- `docs/00-current/Runtime_Snapshot.md` 同步记录 web 构建目标。
- 验证:本地 `flutter build web --release --wasm` 构建成功,产物含 `main.dart.wasm`/`main.dart.js`/`flutter_bootstrap.js`(JS fallback 存在,降级路径可用);本次仅 CI 配置与文档变更,无运行时代码变化。

## fix(cd): 撤回 wasm 构建目标(移动端 skwasm 布局 bug 回归)

- 上文 wasm 条目使 CD 构建切到 `--wasm`,回归了 2026-07-23(commit 2a41f271)已修复的 skwasm 移动端布局 bug:移动浏览器内容区高度为 0,底部导航栏跑到顶部、页面主体空白(与 `web/flutter_bootstrap.js` 注释描述一致)。
- 修复:`luminous-cd.yml` 移除 `--wasm`,恢复纯 dart2js + canvaskit 构建(回到 7-23 状态);`web/flutter_bootstrap.js` 的 `initializeEngine()` 显式传 `renderer: 'canvaskit'`,防止未来 `--wasm` 构建再次静默切换默认渲染器为 skwasm。
- `docs/00-current/Runtime_Snapshot.md` / `Project_Governance.md` 同步修正构建目标描述为纯 JS + canvaskit。
- 验证:本地 `flutter build web --release`(不带 `--wasm`)构建成功,产物 `flutter_bootstrap.js` 默认渲染器为 canvaskit;本次仅 CI 配置与文档变更,无运行时代码变化。

## fix(build): elk_icon_picker 生成图标目录入库(CI web 构建失败修复)

- **问题**:CI(GitHub Actions)`flutter build web --release` 失败,`pkgs/elk_icon_picker-0.1.3/lib/src/gen/lucide_icons.g.dart` 不存在,dart2js 与 wasm dry-run 编译均报错;本地构建正常。
- **根因**:`.gitignore` 的 `**/*.g.dart` 通配规则把解包包自带的生成图标目录一并忽略,文件从未入库;本地磁盘有文件所以构建通过,CI 全新 checkout 后缺失即编译失败。该文件是 pub 发布包的一部分(pub cache 原包同样包含),不是本仓库的本地生成产物,解包时应随包提交。
- **修复**:`.gitignore` 在 `**/*.g.dart` 后增加例外 `!pkgs/elk_icon_picker-0.1.3/lib/src/gen/lucide_icons.g.dart`,并将该文件加入 git 跟踪。
- **验证**:`git check-ignore` 不再命中该文件,`git ls-files` 确认已跟踪,内容与 pub cache 原包一致;CI 重新部署后缺失问题消除。
