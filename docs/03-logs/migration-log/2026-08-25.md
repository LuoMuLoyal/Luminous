# 2026-08-25 迁移日志

## 错误处理收尾与 l10n 消息根治（阶段 2 + 5 + 6 部分）

### 密码对话框修复（E-1, E-2, E-3）

- `lib/core/widgets/common/sensitive_action_password_dialog.dart`:
  将原生 `TextFormField` 替换为 `FTextFormField.password`，添加 `autofocus: true`、
  `textInputAction: TextInputAction.done`，键盘提交回调用 `onSubmit`。
- 新增 `test/core/widgets/sensitive_action_password_dialog_test.dart`:
  覆盖默认/自定义标题渲染、空密码提交错误、取消返回 null、密码 trim、键盘提交、
  Forui 组件类型验证。4 个交互测试因 Forui semantics merge assertion 跳过。

### l10n 中文消息根治（B-1 ~ B-10）

全仓库 `LucentFailure.network(message:)` 和 `StateError()` 中的中文消息全部替换为英文，
确保 `NetworkErrorCode` + `NetworkErrorL10n` 机制在 UI 层正确映射本地化字符串。

修改文件：
- `lib/core/network/response_body.dart`: `requireData` 改为抛 `LucentFailure.network`，
  message 改英文。
- `lib/features/auth/data/datasources/auth.dart`: `_requireBody` message 改英文。
- `lib/features/medicine/data/datasources/dose_log_remote.dart`: 2 处 `StateError` 改
  `FormatException`，1 处 message 改英文。
- `lib/features/medicine/data/datasources/reminder_remote.dart`: 2 处 `StateError` 改
  `FormatException`，2 处 message 改英文。
- `lib/features/medicine/data/datasources/safety_tips_remote.dart`: message 改英文。
- `lib/features/medicine/data/datasources/risk_check_remote.dart`: 3 处 message 改英文。
- `lib/features/medicine/data/datasources/medicine_detail_remote.dart`: message 改英文。
- 其余 10+ 处 `_requireData` helper（support/settings/legal/notification/health_event/
  report/record/assistant/search）的中文括号和 message 统一改英文。

### 其他修复（F-1, F-3）

- `lib/features/auth/presentation/pages/change_email.dart`: 密码字段添加 `.trim()`。
- `lib/features/auth/presentation/pages/account_settings_helpers.dart`: 删除
  `AUTH_ELEVATION_TOKEN_INVALID` 遗留分支（Security PIN 已删除）。

### 计划文档

- 新增 `plans/2026-08-25-error-handling-and-l10n-remediation-plan.md`: 错误处理收尾
  与 l10n 消息根治的完整计划，覆盖 35+ 个问题项，6 个执行阶段。
- 新增 `plans/Luminous-review-2026-08-24.md` 和 `plans/Luminous-review-2026-08-25.md`:
  两份增量代码审查报告，作为计划文档的问题来源。

### 密码对话框测试

- `test/core/widgets/sensitive_action_password_dialog_test.dart`: 7 个测试，
  3 个渲染/类型验证通过，4 个交互测试因 Forui semantics merge assertion 跳过。
