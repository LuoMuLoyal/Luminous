# 2026-07-10 Migration Log

## 前端审查修复

基于 Lucent `plans/` 中三份审查文档（增量审查、全仓库审查、回查纠正）识别的 Luminous 问题，进行以下修复：

### P1: 次级建议错误状态静默修复

- **文件:** `lib/features/today/presentation/widgets/sections/suggestion_section.dart`
- **问题:** `TodaySecondarySuggestionsSection` 的 `error` 状态返回 `SizedBox.shrink()`，用户无法感知次级建议加载失败。
- **修复:** 新增 `_SecondarySuggestionErrorState` widget，在 error 状态显示错误图标 + 提示文案 + 重试按钮，点击重试时 `ref.invalidate(todaySuggestionProvider)`。

### P1: 图标映射硬编码提取

- **文件:** 新增 `lib/features/today/presentation/widgets/shared/suggestion_icon_mapping.dart`
- **问题:** `_iconFor()` 在 `suggestion_section.dart` 底部硬编码 11 个图标字符串映射，新增后端图标类型需同步修改前端。
- **修复:** 提取为独立 `SuggestionIconMapping` 类，使用 `Map<String, IconData>` 存储映射，提供 `resolve(String icon)` 方法，未知图标回退到 `FLucideIcons.sparkles`。`suggestion_section.dart` 中所有 `_iconFor()` 调用替换为 `SuggestionIconMapping.resolve()`。

### P2: 反馈提交后状态更新

- **文件:** `lib/features/today/presentation/widgets/sections/suggestion_section.dart`
- **问题:** 反馈提交成功后仅显示 Toast，未更新本地 UI 状态，用户可能在 provider re-fetch 期间重复操作。
- **修复:** `_SuggestionFeedbackRowState` 新增 `_submittedFeedback` 字段，提交成功后设置该字段，UI 切换为只读的「已反馈」指示器（check 图标 + 文案），替代交互式按钮组。

### 🔴: AI 解释重试次数限制

- **文件:** `lib/features/today/presentation/widgets/sections/suggestion_section.dart`
- **问题:** `_SuggestionAiExplainButton` 解释请求失败后显示重试按钮，但未限制重试次数，用户连续点击可能导致 LLM API 被频繁调用。
- **修复:** 新增 `_retryCount` 和 `_maxRetries = 3` 常量，每次重试递增计数。达到上限后显示 `_AiExplainUnavailable` widget（info 图标 + 「AI 解释暂时不可用，请稍后再试」文案），不再提供重试按钮。同时在 `aiGenerated == false` 的 data 分支也应用相同的重试上限逻辑。

### 🟡: Dashboard 超时可配置化

- **文件:** `lib/features/today/presentation/providers/dashboard_provider.dart`
- **问题:** `_todayDashboardTimeout` 硬编码为 `Duration(seconds: 5)`，弱网环境下可能频繁触发超时。
- **修复:** 改为 `int.fromEnvironment('DASHBOARD_TIMEOUT_SECONDS', defaultValue: 5)`，支持通过 `--dart-define=DASHBOARD_TIMEOUT_SECONDS=10` 编译时覆盖。

### ⚠️ 误判纠正: 反馈提交防抖

- **审查报告原问题:** `_SuggestionFeedbackRowState._submit` 反馈提交无防抖。
- **回查结论:** 代码中所有反馈按钮的 `onPress` 均检查 `_isSubmitting`（`onPress: _isSubmitting ? null : () => _submit(option)`），提交期间所有按钮统一禁用。此为误判，无需修复。

### 新增 l10n 字符串

- `todaySuggestionSecondaryErrorHint` — "更多建议加载失败" / "Failed to load more suggestions"
- `todaySuggestionAiExplainMaxRetry` — "AI 解释暂时不可用，请稍后再试" / "AI explanation is temporarily unavailable, please try again later"
- `todaySuggestionFeedbackSubmitted` — "已反馈" / "Submitted"

### 验证

- `flutter analyze` — 零问题
- `flutter test test/today/suggestion_section_test.dart` — 17/17 通过

## Auth 模块重构

对 `lib/features/auth/` 下的登录/注册模块进行架构级重构，消除代码重复、拆分 God Widget、分离 OAuth 逻辑。

### Step 5: AuthRemoteDataSource._persistSession 提取

- **文件:** `lib/features/auth/data/datasources/remote_data_source.dart`
- **变更:** 5 处重复的 `writeSession(LucentSessionTokens(...))` 提取为 `_persistSession(AuthSession)` 私有方法，`login` / `loginWithWechatWeb` / `loginWithWechatMobile` / `loginWithApple` / `loginWithQq` 统一调用。

### Step 1: AuthActionRunner 统一异步管道

- **文件:** 新增 `lib/features/auth/presentation/providers/shared/auth_action_runner.dart`
- **变更:** 提取 try-catch + LucentErrorMapper + talker 日志模式为 `runAuthAction<T>()` 函数，返回 `({T? value, String? error})` 记录类型。3 个表单 Provider 的 `submit` / `sendCode` 方法改用此函数。

### Step 4: OAuthCallbackParser 回调解析提取

- **文件:** 新增 `lib/features/auth/presentation/widgets/shared/oauth_callback_parser.dart`
- **变更:** 从 `login_page.dart` 中提取 `_WechatCallback` + `parseWechatCallback` 为独立的 `OAuthCallbackParser` 类和 `OAuthCallback` 值对象，支持 URL / query string / bare code 三种格式解析，可被 WeChat / QQ 共用。

### Step 3: OAuth 策略层 + OAuthLoginController

- **文件:**
  - 新增 `lib/features/auth/presentation/services/wechat_oauth_service.dart` — 平台感知 WeChat OAuth 服务（mobile SDK → desktop loopback → web fallback）
  - 新增 `lib/features/auth/presentation/providers/oauth_login_provider.dart` — `OAuthLoginController` notifier + `OAuthLoginState` 状态
- **变更:**
  - `WechatOAuthService` 封装 3 路平台检测逻辑，`tryMobileAuth()` / `tryDesktopAuth(forIdentityLink:)` / `createWebAuthorizeUrl(forIdentityLink:)` 同时服务登录和身份绑定
  - `OAuthLoginController` 接管原 `LoginFormNotifier` 的 7 个 OAuth 方法（`startWechatMobileLogin` / `startWechatDesktopLogin` / `completeWechatLogin` / `startQqLogin` / `completeQqLogin` / `loginWithApple` / `startWechatLogin` 编排方法）
  - `OAuthLoginState` 为纯 Dart 类（非 freezed），包含 10 个 OAuth 专属字段
  - `WechatLoginAttempt` sealed class（`WechatLoginCompleted` / `WechatLoginWebFallback` / `WechatLoginFailed`）作为 `startWechatLogin()` 返回类型
  - `AuthAccountNotifier.startWechatIdentityLink` 重构为使用 `WechatOAuthService`，消除内联平台检测代码

### Step 6: LoginPage 分解 + LoginFormState 瘦身

- **文件:**
  - `lib/features/auth/presentation/providers/forms/login_form_provider.dart` — `LoginFormState` 从 18 字段降至 11 字段（移除全部 OAuth 字段），`LoginFormNotifier` 从 393 行降至 ~170 行（移除全部 OAuth 方法）
  - 新增 `lib/features/auth/presentation/widgets/shared/oauth_panels.dart` — `WechatOAuthPanel` / `QqOAuthPanel` / `AppleOAuthPanel` 三个独立 widget
  - `lib/features/auth/presentation/pages/login_page.dart` — 从 700 行降至 ~370 行，OAuth 状态从 `loginFormProvider` 切换到 `oauthLoginProvider`，回调解析改用 `OAuthCallbackParser`
- **测试更新:** `test/auth/login_form_provider_test.dart` — OAuth 测试从 `loginFormProvider.notifier` 切换到 `oauthLoginProvider.notifier`

### 验证

- `flutter analyze` — 零问题
- `flutter test test/auth/` — 45/45 通过
- `flutter test test/mine/account_settings_page_test.dart` — 8/8 通过

## 架构优化 ADR 撰写

基于 Luminous 全量架构审查，撰写 5 份 `proposed` 状态的架构决策记录（ADR-0006 ~ 0010）：

- **ADR-0006**: `riverpod_generator` 与 Auth-Guarded Provider 工厂 — Provider 声明标准化、统一选型标准、提取 auth guard 复用
- **ADR-0007**: 网络层职责分离 — `LucentDioClient` God Class 拆分为 AuthInterceptor / ErrorInterceptor / RetryInterceptor，简化 15+ API provider 冗余链路
- **ADR-0008**: Result 类型与统一错误处理 — 轻量 `Result<T>` sealed class、`AppError` 统一错误类型、泛化 `runGuarded` helper、Repository 层返回 Result
- **ADR-0009**: 本地持久化与离线策略 — 引入 `drift` 作为本地 SQLite ORM，cache-first repository 模式、离线写入队列、设置页面"数据保留期"接入
- **ADR-0010**: 类型安全路由 — 引入 `go_router_builder`，`@TypedGoRoute` 注解替代手写路由声明，编译时参数类型检查

ADR README 索引同步更新。全部 ADR 状态为 `proposed`，待讨论确认后转为 `accepted`。

## ADR-0006 实施第一步：riverpod_generator 引入与 network_providers 迁移

ADR-0006 状态从 `proposed` 转为 `accepted`，开始分批实施。

### 依赖新增

- `riverpod_annotation: ^4.0.3` → `dependencies`（`lib/` 源码 import 注解）
- `riverpod_generator: ^4.0.4` → `dev_dependencies`（build_runner codegen）
- 项目已有 `build_runner`，`bootstrap_generated_sources.dart` 无需修改（已包含 `dart run build_runner build`）

### authGuarded helper 函数

- **新增文件:** `lib/core/providers/auth_guarded.dart`
- **作用:** 封装 auth session 检查 + `pendingAuthSessionResolution` + `AuthRequiredException` 模式，消除 `mineDashboardProvider`、`reportDashboardProvider`、`healthContextSnapshotProvider`、`todayDashboardProvider` 等 5+ 处重复 auth guard 逻辑
- **设计决策:** 实现为普通函数（非 generated provider），因为泛型 `authGuarded<T>` 作为 generated family provider 不适用于不同 feature 各自传入不同 `fetch` 函数的场景。各 feature 的 `@riverpod` 函数在函数体内调用 `authGuarded(ref: ref, fetch: ..., signedOutFallback: ...)`

### network_providers.dart 迁移

- **文件:** `lib/core/network/network_providers.dart`
- **变更:** 20 个手写 `final xxxProvider = Provider<Xxx>((ref) { ... });` 全部改为 `@riverpod` / `@Riverpod(keepAlive: true)` 注解形式
  - 3 个核心基础设施 provider（`lucentBaseUrl`、`lucentSessionStore`、`lucentDioClient`）使用 `@Riverpod(keepAlive: true)`
  - 17 个 API accessor provider（`lucentAuthApi`、`lucentHealthApi` 等）使用 `@riverpod`（autoDispose，因为它们是轻量 getter，底层 `LucentClient` 由 keepAlive 的 `lucentDioClientProvider` 持有）
- **import 优化:** 移除 `flutter_riverpod` import（`riverpod_annotation` 已导出 `Ref`/`Override`）；将 `api.dart` barrel import 拆分为具体文件 import（`dio_client.dart`、`session_store.dart`、`base_url.dart`），消除循环依赖
- **provider 名称兼容:** 函数名保留 `lucent` 前缀（如 `lucentAuthApi`），生成的 provider 名为 `lucentAuthApiProvider`，与现有代码引用完全兼容，零破坏性变更
- **新增产物:** `lib/core/network/network_providers.g.dart`（gitignored，由 `build_runner` 生成）

### 验证

- `flutter analyze --no-pub` — No issues found
- `flutter test --no-pub` — 947/947 全部通过

### 后续（ADR-0006 剩余工作）

以下工作将在后续批次完成，当前批次只迁移了核心基础设施层：

- 各 feature 的 datasource/repository provider 迁移为 `@riverpod` 注解
- ~~各 feature 的数据 provider 接入 `authGuarded` helper~~ → 已完成（见下方第二批）
- `TODO.md` 中 "Provider 一致性" 暂缓项可在全部迁移完成后删除

## ADR-0006 实施第二批：Feature 数据 provider 接入 authGuarded

将 ADR-0006 中定义的 `authGuarded` helper 函数接入全部 feature 数据 provider，消除 14 处重复的 auth session 检查模式。

### authGuarded 函数修正

- **关键修复:** 将 `authGuarded` 从 `async` 改为非 `async` 函数。原因：`async` 函数中的 `throw` 会被转换为 rejected Future，导致 Riverpod 的 `FutureProvider` 异步设置 error 状态（而非同步）。非 `async` 版本让 `throw AuthRequiredException()` 在调用方的同步上下文中执行，保持与原有手写代码一致的 error 传播行为。
- `session_gate_test.dart` 的 "signed out dashboard providers use local data only" 测试验证了此修正的必要性。

### 迁移的 Provider 清单（14 处）

| Feature | Provider | 模式 | signedOutFallback |
|---------|----------|------|-------------------|
| mine | `mineDashboardProvider` | A (signedOut → fallback) | `signedOutDashboard` |
| record | `recordDashboardProvider` | A | `signedOutDashboard(date, filterType:)` |
| today | `todayDashboardProvider` | A | `signedOutDashboard` |
| today | `todaySuggestionProvider` (AsyncNotifier) | D (null fallback) | `() async => null` |
| today | `suggestionExplanationProvider` (autoDispose family) | D | `() async => null` |
| medicine | `medicineWorkspaceProvider` | A | `previewWorkspace` |
| medicine | `medicineReminderListProvider` | B (throw) | 无 |
| medicine | `medicineTodayDoseLogsProvider` | B | 无 |
| medicine | `medicineReminderDeliveryLogProvider` | B | 无 |
| medicine | `medicineRiskCheckProvider` | B | 无 |
| health_context | `healthContextSnapshotProvider` | B | 无 |
| notification | `notificationUnreadCountProvider` | C (pending) | `pendingAuthSessionResolution()` |
| notification | `notificationListPageProvider` | C | `pendingAuthSessionResolution()` |
| notification | `notificationDetailProvider` (family) | C | `pendingAuthSessionResolution()` |
| report | `reportDashboardProvider` (family) | A | mock signedOutDashboard with query |

**模式说明:**
- **A:** `isConfirmedSignedOut` → signedOutFallback；`isRestoring` → pending；authenticated → fetch
- **B:** `isRestoring` → pending；`isConfirmedSignedOut` → throw `AuthRequiredException`；authenticated → fetch
- **C:** `isRestoring` → pending；`isConfirmedSignedOut` → pending（保持原有行为，signed-out 时不 flash error）
- **D:** `isRestoring` → pending（行为改进：原来直接返回 null）；`isConfirmedSignedOut` → null；authenticated → fetch

### 行为改进

- `todaySuggestionProvider` 和 `suggestionExplanationProvider` 在 session restoring 期间现在返回 pending（loading）而非 null。这是更正确的行为——session 恢复期间不应显示"无建议"，而应显示加载状态。
- `reportDashboardProvider` 的 signed-out 逻辑简化：原来有三段嵌套 if（`isConfirmedSignedOut` → mock, `isLoading` → pending, `!canAccessProtectedData` → pending），现在统一为 `authGuarded` + `signedOutFallback`。

### 未迁移的 Provider（不适用 authGuarded 模式）

以下 provider 的 auth 检查模式不适合 `authGuarded`，保持原样：

- `TodayAiAnalysisController` / `ReportAiSummaryController` — Notifier（非 FutureProvider），auth 检查返回 idle 状态而非 Future/throw
- `SettingsProfileSyncNotifier._syncPreferencesToBackend` — Notifier 方法，auth 检查为 no-op return
- UI 页面中的 `ref.watch(authSessionProvider)` — 用于条件渲染，非数据获取

### 验证

- `flutter analyze --no-pub` — No issues found
- `flutter test --no-pub` — 947/947 全部通过

## ADR-0006 实施第三批：Feature datasource/repository provider 迁移为 @riverpod 注解

将全部 Feature 层和 core 层剩余的手写 `Provider<T>` 声明迁移为 `@riverpod` 注解形式，与第一批 `network_providers.dart` 迁移风格保持一致。

### 迁移范围（约 40 个 Provider，25 个文件）

**Feature 层 — datasource provider:**
- `auth/data/providers/data_providers.dart` — `authRemoteDataSource`、`wechatDesktopOAuthCallbackListener`、`wechatMobileAuthClient`（3 个）
- `settings/data/providers/profile_data_providers.dart` — `settingsProfileRemoteDataSource`（1 个）
- `today/data/repositories/lucent_ai_repository.dart` — `todayAiRemoteDataSource`、`todayAiRepository`（2 个）
- `today/presentation/providers/suggestion_provider.dart` — `todaySuggestionRemoteDataSource`（1 个）
- `today/presentation/providers/recommendations_provider.dart` — `todayRecommendationsRemoteDataSource`（1 个，@Deprecated）
- `record/data/providers/providers.dart` — `dailyRecordRemoteDataSource`、`dailyRecordRepository`（2 个）
- `medicine/data/datasources/dose_log_remote_data_source.dart` — `doseLogRemoteDataSource`（1 个）
- `medicine/data/repositories/mock_workspace_repository.dart` — `medicineReminderRemoteDataSource`、`medicineWorkspaceRepository`（2 个）
- `medicine/data/repositories/risk_check_repository.dart` — `medicineRiskCheckRepository`（1 个）
- `medicine/presentation/providers/safety_tips_provider.dart` — `safetyTipsRemoteDataSource`（1 个）
- `search/data/repositories/lucent_repository.dart` — `lucentMedicineSearchRepository`、`medicineSearchRepository`、`medicineSearchRemoteDataSource`（3 个）
- `assistant/data/repositories/lucent_repository.dart` — `assistantRemoteDataSource`、`assistantRepository`（2 个）

**Feature 层 — repository provider:**
- `today/data/repositories/mock_repository.dart` — `todayRepository`（1 个）
- `record/data/repositories/mock_repository.dart` — `recordRepository`（1 个）
- `report/data/repositories/mock_repository.dart` — `reportRemoteDataSource`、`reportRepository`（2 个）
- `report/data/repositories/lucent_ai_summary_repository.dart` — `reportAiSummaryRemoteDataSource`、`reportAiSummaryRepository`（2 个）
- `health_context/data/providers/data_providers.dart` — `healthContextMapper`、`healthContextRemoteDataSource`、`healthContextRepository`（3 个）
- `mine/presentation/providers/dashboard_provider.dart` — `mineRepository`（1 个）

**Feature 层 — service/coordinator provider:**
- `auth/presentation/services/wechat_oauth_service.dart` — `wechatOAuthService`（1 个）
- `medicine/presentation/providers/reminder_notification_coordinator.dart` — `medicineReminderNotificationPlanner`、`medicineReminderNotificationCoordinator`、`medicineReminderNotificationNow`（3 个）
- `record/presentation/providers/time_provider.dart` — `currentRecordDateTime`（1 个）

**Core 层:**
- `core/logger/app_logger.dart` — `talker`（1 个，import 前缀 `talker` → `talker_pkg` 避免与同名函数冲突）
- `core/notifications/local_notification_gateway.dart` — `localNotificationGateway`（1 个）
- `core/router/external_url_launcher.dart` — `externalUrlLauncher`（1 个）
- `core/ai/ai_runtime_providers.dart` — `aiRuntimeEnvironment`、`aiRuntimeConfig`（2 个）

### Import 策略

- 纯 DI 文件（仅含 `Provider<T>` 声明，不含 `FutureProvider`/`AsyncNotifier` 等）：移除 `flutter_riverpod` import，仅保留 `riverpod_annotation`
- 混用文件（同时含 `FutureProvider`/`AsyncNotifier`/`AsyncNotifierProvider` 等）：保留 `flutter_riverpod` + 新增 `riverpod_annotation` 双 import
- 所有文件新增 `part 'filename.g.dart';` 指令

### 特殊处理

- `app_logger.dart`：`import 'package:talker_flutter/...' as talker` 中的 `talker` 前缀与生成的 `talker` 函数同名冲突，改为 `talker_pkg` 前缀
- `safety_tips_provider.dart`：`export` 指令必须位于 `part` 指令之前（Dart 语法要求）

### 验证

- `dart run build_runner build` — 成功生成 25 个 `.g.dart` 文件
- `flutter analyze` — No issues found
- `flutter test` — 947/947 全部通过

## 文档治理重构（跨仓库同步）

### 变更

- `README.md` — 全部 `../Lucent/docs/public/` 链接更新为 `../Lucent/docs/01-reference/contracts/`
- `docs/00-current/Next_Plan.md` — 合同引用路径从 `Lucent/docs/public/` 更新为 `Lucent/docs/01-reference/contracts/`
- `docs/_MOC.md` — 新增 `[[02-reference/how-to/README]]` 操作指南入口
- `docs/README.md` — 快速导航新增 how-to 引用
- 新增 `docs/02-reference/how-to/` 目录，含 4 篇指南：
  - `add-new-feature.md` — 新增 Feature 模块
  - `regenerate-api-client.md` — 再生 Lucent API 客户端
  - `add-localization.md` — 添加本地化文案
  - `run-tests.md` — 运行测试与检查

## ADR-0007 实施：网络层职责分离

ADR-0007 状态从 `proposed` 转为 `accepted`，完成全量实施。将 `LucentDioClient` God Class 拆分为三个独立拦截器，新增 `lucentClientProvider` 统一 API 访问入口。

### 新增拦截器文件

- **`lib/core/network/interceptors/auth_interceptor.dart`** — Token 注入（onRequest）+ 401 检测/刷新/重试（onError）+ Session 清理 + `onSessionExpired` 回调。从 `LucentDioClient._buildInterceptors()` + `_shouldRefresh()` + `_refreshTokens()` + `_doRefresh()` + `_retry()` 提取。持有主 Dio 实例用于重试，refreshDio 用于刷新调用。
- **`lib/core/network/interceptors/error_interceptor.dart`** — `DioException` → `LucentApiException` 映射。从 `LucentDioClient._mapToApiException()` + `_fallbackMessage()` 提取。放在拦截器链最后，确保 auth-refresh 和 retry 已有机会恢复请求后再映射错误。
- **`lib/core/network/interceptors/retry_interceptor.dart`** — 5xx / 超时自动重试（指数退避）。默认仅重试 GET 请求，支持 `extra['retryEnabled']` opt-in/opt-out。可重试状态码：408, 429, 500, 502, 503, 504。

### `dio_client.dart` 精简

- 从 ~367 行降至 ~100 行，仅保留 Dio 实例配置 + interceptor 注册 + `dio`/`client` getter + `onSessionExpired` setter（委托到 `AuthInterceptor`）+ `dispose()`。
- 移除 18 个 API getter（`healthApi` / `authApi` / `medicinesApi` 等）、session 代理方法（`writeSession` / `readAccessToken` / `readRefreshToken` / `clearSession`）、`medicinesHeaders` / `medicinesBypassCacheHeader`。

### `network_providers.dart` 更新

- 新增 `lucentClientProvider`（`@Riverpod(keepAlive: true)`）— 返回 `LucentClient`，作为全部 feature 代码的统一 API 访问入口。
- 17 个旧 `lucent*ApiProvider` 标记 `@Deprecated`，内部改为委托到 `lucentClientProvider.xxx`，保持向后兼容。

### `sse.dart` 增强

- 新增 `reconnect` 和 `maxReconnects` 参数，支持网络错误后自动重连（指数退避：1s, 2s, 4s）。

### Auth 层适配

- `AuthRemoteDataSource` 构造函数从 `LucentDioClient` 改为 `LucentClient` + `LucentSessionStore`，移除对 `LucentDioClient` session 代理方法的依赖。
- `AuthSessionNotifier` 的 `readAccessToken()` / `clearSession()` 改为直接使用 `lucentSessionStoreProvider`。
- `authRemoteDataSourceProvider` 更新为注入 `lucentClientProvider` + `lucentSessionStoreProvider`。

### Feature 消费者迁移

19 个 feature 文件从 `ref.watch(lucent*ApiProvider)` 迁移到 `ref.watch(lucentClientProvider).xxx`：

- today: `lucent_ai_repository.dart`, `recommendations_provider.dart`, `suggestion_provider.dart`
- support: `resources_providers.dart`
- settings: `user_settings_controller.dart`, `data_export_controller.dart`
- search: `lucent_repository.dart`
- scan: `scan_repository.dart`
- report: `mock_repository.dart`, `lucent_ai_summary_repository.dart`, `page.dart`
- record: `providers.dart`
- notification: `providers.dart`, `detail_page.dart`
- medicine: `safety_tips_provider.dart`, `mock_workspace_repository.dart`, `dose_log_remote_data_source.dart`
- health_context: `data_providers.dart`
- assistant: `lucent_repository.dart`

### 测试适配

- `test/core/network/dio_client_test.dart` — 移除已删除方法的测试（session operations / API getters），错误映射测试添加 `retryEnabled: false` 禁用重试干扰。
- `test/core/network/network_providers_test.dart` — 重写为测试 `lucentClientProvider`。
- `test/auth/remote_data_source_test.dart` — 构造函数适配 `LucentClient` + `LucentSessionStore`。
- `test/auth/test_helpers.dart` + `integration_test/support/e2e_test_helpers.dart` — `FakeAuthRemoteDataSource` / `E2eAuthRemoteDataSource` 构造函数适配。
- 5 个 feature 测试文件（support / settings×2 / notification / report）— override 从 `lucent*ApiProvider` 改为 `lucentClientProvider` + `_FakeLucentClient` 子类。

### 验证

- `dart run build_runner build` — 成功
- `flutter analyze` — No issues found
- `flutter test` — 940/940 全部通过
