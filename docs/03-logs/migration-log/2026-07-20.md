# Migration Log - 2026-07-20

## Luminous — UI/UX P0 修复：死路由、文案错误、Toast 键值复用、测试修复

- **来源**：`plans/2026-07-20-uiux-optimization-verification.md`（基于 `plans/2026-07-18-uiux-per-page-optimization.md`）。
- **验证方式**：`flutter analyze` 零问题，`flutter test` 2756 个测试全部通过。

### P0 修复 7 项

1. **死路由 `/medicine/reminders`**：`router.dart` 移除 `AppRoutes.medicineReminders` 常量（该路由无对应页面，跳转后 404）。用药页铃铛按钮改为跳转 `AppRoutes.notifications`（通知列表页）。Today 快捷操作"用药提醒"从 `medicineReminders` 改为 `medicineRemindersNew`（新建提醒页）+ `usePush: true`。
2. **`_MedicineSafeGuardPill` 窄屏断言**：窄屏下 `FButton` 的 `child: null` 触发 Forui 断言。修复：根据 `isNarrow` 条件切换 `FButton.icon`（仅图标）和 `FButton`（图标+文字），消除 `child: null`。
3. **提醒切换失败 Toast 复用 `settingsSyncFailed`**：`reminder_detail.dart` 的启停切换失败 Toast 从 `l10n.settingsSyncFailed` 改为专用 `l10n.medicineReminderToggleFailedToast`（"切换提醒状态失败"）。
4. **扫码识别中文案复用 `scanRecognitionFailedToast`**：`barcode_scanner.dart` 的识别中状态从 `l10n.scanRecognitionFailedToast`（"识别失败"）改为 `l10n.scanRecognizingHint`（"识别中…"），不再在识别过程中显示失败文案。
5. **帮助页空态/错误态 description 重复 title**：`help.dart` 的空态和错误态 `AppStateMessageView` 传入 `description` 与 `title` 完全相同。修复：移除 `description` 参数（`AppStateMessageView` 的 `description` 现已支持可选）。
6. **高级设置页残余分组标题**：`advanced.dart` 移除"恢复默认设置"的 `SettingsSectionLabel`（对应的 `FTileGroup` 已在前一次修改中删除，标题残留）。
7. **`AppStateMessageView.description` 改为可选**：`state_message.dart` 的 `description` 参数从 `required String` 改为 `String?`，`null` 时不渲染描述行。帮助页等仅需标题+图标的场景不再需要传入重复文案。

### 新增 l10n 键

- `medicineReminderToggleFailedToast`（"切换提醒状态失败" / "Failed to toggle reminder status."，zh + en）。
- `scanRecognizingHint`（"识别中…" / "Recognizing…"，zh + en）。

### 测试修复 3 项

1. **`reminder_form_body_test.dart` — `pumpAndSettle` 超时**：`isSaving: true` 时 `FCircularProgress` 是无限动画，`pumpAndSettle` 永远无法完成。修复：为 `_pumpForm` 增加 `settle` 参数，`isSaving` 测试中用 `pump(Duration(milliseconds: 100))` 替代 `pumpAndSettle()`。
2. **`dashboard_test.dart` — 时相关断言**：`TodayDashboard.signedOut()` 使用 `DateTime.now().hour` 动态计算时段，测试硬编码期望 `morning` 在下午运行时失败。修复：改为用 `todayDayMomentFromHour(DateTime.now().hour)` 动态计算期望值。
3. **其他测试修复**（前序会话完成）：`medicine_search_notifier_test.dart`（防抖等待）、`subpages_test.dart`（BOM 字符）、`providers_test.dart`（改用 repository 测试避免超时）、`ocr_entry_dialog_test.dart`、`reminder_pages_test.dart` 等。

## Luminous — UI/UX P1：跨页面共性 + 今日模块

- **来源**：`plans/2026-07-20-uiux-optimization-verification.md` § 4.1 + § 4.2。
- **验证方式**：`flutter analyze` 零问题，`flutter test` 2756 个测试全部通过。

### 4.1 跨页面共性

1. **C8 收尾 — PIN 上次修改时间**：`security_pin.dart` 的 `_lastChangedLabel` 方法从手拼 `yyyy-MM-dd HH:mm` 改为调用 `formatDateTimeLabel(dt.toIso8601String(), locale)`，尊重 locale 感知格式（zh: `1月18日 14:30`，en: `Jan 18, 14:30`）。
2. **C8 收尾 — legal 列表 updatedAt**：`legal/presentation/pages/list.dart` 的 `doc.updatedAt` 在传入 `l10n.legalListUpdatedAt()` 前先经 `formatDateTimeLabel()` 格式化，不再直接显示后端原始 ISO 串。
3. **error_mapper 中文兜底中性化**：`core/network/error_mapper.dart` 的 `_fallbackMessage()` 和 `fromObject()` 兜底消息从硬编码中文改为英文（locale-neutral），消除英文界面极端错误路径露中文问题。业务错误消息仍由服务端返回（已按 locale 本地化），仅网络层 fallback 受影响。

### 4.2 今日（today）

1. **用药指标分母修正**：`today/data/repositories/lucent.dart` 新增 `_todayScheduledMedicineIds()` 方法，通过查询今日有提醒计划的药品 ID 集合作为分母。`TodayMedicationSummary.medicineCount` 从 `medicines.length`（全部当前药品）改为今日有提醒的药品数；无提醒时回退为全部当前药品数，避免空值。
2. **置信度标签 FBadge 化**：`today/presentation/widgets/sections/observation.dart` 新增 `_ConfidenceBadge` 组件，按 `TodaySuggestionConfidence` 使用不同 `FBadgeVariant`：high→`primary`、medium→`secondary`、low→`outline`，实现视觉分级。原纯 `Text` 标签替换为 `FBadge`。
3. **置信度文案区分**：zh `todayObservationMediumConfidenceTag` 从"供参考"改为"值得留意"，与 low 的"仅供参考"拉开语义距离；en 从"For reference"改为"Worth noting"，与 low 的"FYI"区分。

### 测试更新

- `test/core/network/error_mapper_test.dart`：9 个测试的期望值从中文改为英文，测试名从"Chinese message"改为"locale-neutral message"。
- `test/core/errors/error_mapper_to_app_error_test.dart`：2 个测试的期望值同步更新；修复文件首行 BOM 字符。
- `test/features/legal/presentation/pages/legal_list_page_test.dart`：“renders updated date subtitle” 测试改用 `formatDateTimeLabel` 计算期望值，测试数据从 `'2026-01-01'` 改为 ISO datetime `'2026-01-15T10:30:00Z'`。

## Luminous — UI/UX P1：记录（record）模块

- **来源**：`plans/2026-07-20-uiux-optimization-verification.md` § 4.1 记录模块 P1 项。
- **验证方式**：`flutter analyze` 零问题，`flutter test` 2756 个测试全部通过。

### 修复项

1. **桌面筛选单选语义**：`sidebar.dart` 的 `_FilterRow` 图标从 `squareCheckBig` / `square`（复选框语义）改为 `circleCheck` / `circle`（单选语义），选中色从 `foreground` 改为 `primary`。点击已选中筛选可取消（`onFilterSelected` 传回 `null`）。
2. **创建表单 mood 类型对齐**：`form_fields.dart` 的 `activeDailyRecordKinds` 新增 `DailyRecordKind.mood`，使创建表单的类型选择器可选心情。
3. **语音 sheet 错误显示**：`voice_entry_dialog.dart` 新增 `errorMessage` 内联显示区（图标 + 红色文案），不再只存不显。
4. **语音 sheet 分类文案**：初始化失败的三类场景（权限拒绝 / 语音不可用 / locale 不支持）各自显示不同的 l10n 消息，不再统一报“麦克风权限未授权”。新增 `recordSpeechUnavailable` / `recordSpeechLocaleUnsupported` / `recordVoiceErrorHint` 键。
5. **语音结果可编辑**：识别结果从只读 `Text` 改为 `TextField`，用户可在识别后手动编辑修正。新增 `recordVoiceEditHint` 键。使用 `useTextEditingController` 持久化控制器，避免重建时光标跳动。
6. **必填校验内联化**：`create.dart` 的 `onSave` 校验从 `AppToast.show` 改为设置 `valueError` / `titleError` 状态，通过 `FTextField.error` 在字段下方内联显示错误提示。
7. **“标题（可选）”矛盾消除**：`form_fields.dart` 的标题字段标签从 `recordCreateFieldTitleOptional`（“标题（可选）”）改为 `recordCreateFieldTitle`（“标题”），消除与必填校验的矛盾。新增 `recordCreateFieldTitle` 键。
8. **保存中表单整体禁用**：`DailyRecordFormFields` 新增 `enabled` 参数，`create.dart` 在 `saving` 时传 `!saving.value`，所有字段（kind 选择器、值、单位、标题、备注）连同图片附件一起禁用。
9. **桌面骨架 3 列**：`skeleton_view.dart` 的 `_DesktopRecordSkeleton` 从 2 列（sidebar + expanded）改为 3 列（sidebar + center flex:6 + right sidebar），与真实 `_DesktopRecordDashboard` 的三栏布局对齐。
10. **NLP 预生成失败提示**：`nlp_dialog.dart` 新增 `RecordNlpStatus.error` 状态分支，显示图标 + 错误文案（`state.errorMessage` 或 `recordNlpGenerateFailedToast` 兜底）。新增 `recordNlpGenerateFailedToast` 键。

### 新增 l10n 键

- `recordSpeechUnavailable`（“语音识别不可用，请检查设备是否支持。” / “Speech recognition is unavailable on this device.”）
- `recordSpeechLocaleUnsupported`（“当前语言暂不支持语音识别。” / “Speech recognition does not support the current language.”）
- `recordVoiceErrorHint`（“识别出错，请重试。” / “Recognition error. Please try again.”）
- `recordVoiceEditHint`（“可在此编辑识别结果” / “You can edit the recognized text here”）
- `recordCreateFieldTitle`（“标题” / “Title”）
- `recordNlpGenerateFailedToast`（"解析失败，请检查内容后重试。" / "Parsing failed. Please check your input and try again."）

## Luminous — UI/UX P1：用药 / 搜索 / 扫码模块

- **来源**：`plans/2026-07-20-uiux-optimization-verification.md` § 4.4 用药/搜索/扫码 P1 项。
- **验证方式**：`flutter analyze` 零问题，`flutter test` 477 个相关测试全部通过。

### 修复项

1. **桌面搜索栏全状态可见**：`medicine/presentation/pages/page.dart` 的 `loadingBuilder`、`fatalErrorBuilder`、`emptyInsufficientBuilder` 三个桌面分支新增 `_MedicineMobileSearchBar()`，与 `readyBuilder` 保持一致。加载/错误/空态下用户仍可点击搜索栏跳转搜索页。
2. **搜索结果卡"已添加"态**：`SearchResultTile` 新增 `alreadyAdded` 参数。`SearchPage` 通过 `healthContextSnapshotProvider` 获取当前药品列表，构建 `source:sourceRefId` 集合传入 `MedicineSearchView` → `_MobileSearchLayout` / `_DesktopSearchLayout` → `_DesktopSearchPanel` → `SearchResultTile`。已添加的药品显示禁用 outline 按钮 + check 图标 + "已添加"文案，不可重复添加。
3. **拍照识别失败手动兜底**：`box_scan.dart` 的 catch 分支从 `AppToast.show` 改为 `_showScanFailureDialog()` 对话框，提供"重新拍照"（重试）和"手动搜索"（跳转 `/medicine/search`）两个操作。原先仅 toast 无手动兜底的问题已修复。
4. **死键清理**：`scanSearchFailedToast`（带 `{error}` 参数，零引用）从 zh/en ARB 文件中删除。`scanManualSearchToast` 从零引用改为在失败对话框 body 中使用。`scanManualSearchAction` 从零引用改为在失败对话框按钮中使用。
5. **扫码遮罩安全弹窗**：`box_scan.dart` 新增 `_dismissOverlay()` 函数，通过 `Navigator.canPop()` 检查后再 `pop()`，替代原来直接 `Navigator.of(context, rootNavigator: true).pop()` 的脆弱写法。
6. **扫码结果对话框关闭按钮**：`recognize_dialog.dart` 新增 `onClose` 可选回调和 `scanCloseAction` 按钮（ghost 变体）。用户可在无识别结果时直接关闭对话框，不必只能重拍。

### 新增 l10n 键

- `medicineSearchAlreadyAddedLabel`（"已添加" / "Already added"）
- `scanCloseAction`（"关闭" / "Close"）

### 删除 l10n 键

- `scanSearchFailedToast`（带 `@scanSearchFailedToast` 元数据，zh + en）

## Luminous — UI/UX P1：报告（report）模块

- **来源**：`plans/2026-07-20-uiux-optimization-verification.md` § 4.5 报告模块 P1 项。
- **验证方式**：`flutter analyze` 零问题，`flutter test` 125 个报告模块测试全部通过。

### 修复项

1. **切时间范围保留旧值**：`providers/dashboard.dart` 新增 `ReportLastDashboardNotifier` + `reportLastDashboardProvider`，缓存最近一次成功加载的 `ReportDashboard`。`page.dart` 通过 `ref.listen` 监听 dashboard provider，在数据到达时更新缓存；当新查询正在加载且无缓存值时，使用 `effectiveAsync = AsyncValue.data(cachedDashboard)` 作为 `resolvePageViewState` 的输入，使页面展示旧数据而非整页骨架。`isRefreshing` 仍从原始 `dashboardAsync.isLoading` 取值，保证加载指示器正确显示。
2. **AI 总结"自定义"范围日期兜底**：`providers/ai_summary.dart` 的 `generate()` 方法在 `range == custom` 且 dashboard query 非 custom 时，不再传 null 日期。改为从 `reportLastDashboardProvider` 读取缓存的 `startDate`/`endDate`；缓存不可用时回退到最近 7 天日期。确保所有自定义范围请求都携带有效日期。
3. **桌面 loading 假按钮禁用**：`page.dart` 的 `_buildLoadingShell` 中 `ReportActionBar` 传入 `isGenerating: true, isSyncing: true`，使两个按钮的 `onPress` 为 `null`（禁用态），消除加载态可点击的空回调假按钮。
4. **图表 tooltip 触点值+日期**：`trend.dart` 的 `getTooltipItems` 从恒显示 `series.currentValue` 改为根据 `spot.spotIndex` 取 `series.values[index]` 的实际触点值。tooltip 新增日期行，使用 `DateFormat.MMMEd(locale)` 格式化（如"7月20日周日" / "Sun, Jul 20"）。
5. **图表 Semantics 数值摘要**：`ReportTrendSection` 的 `Semantics.label` 从仅标题改为包含各指标当前值摘要（如"趋势, 睡眠: 7.5h, 饮水: 1200ml"）。

## Luminous — UI/UX P1：我的（mine）+ 设置（settings）模块

- **来源**：`plans/2026-07-20-uiux-optimization-verification.md` § 4.6 + § 4.7 P1 项。
- **验证方式**：`flutter analyze` 零问题，`flutter test test/mine/ test/settings/` 289 个测试全部通过。

### 修复项

1. **用药表单"途径/剂量"常用值选择器**：`current_medicine_edit.dart` 在剂量和给药途径文本框下方新增 `_QuickSelectChip` 快速选择芯片。剂量提供 4 个常用值（1片/2片/5ml/10ml），途径提供 4 个常用值（口服/外用/吸入/注射）。点击芯片直接填入对应文本框，用户也可手动输入自定义值。
2. **PIN 校验失败行内 error**：`security_pin.dart` 从 `AppToast.show` 改为行内 `error: Text(...)` 显示校验错误。新增 6 个 `ValueNotifier<String?>` 分别管理启用/确认/旧/新/确认新/禁用 PIN 的错误状态。通过 `useEffect` 注册 controller listener，用户编辑字段时自动清除对应错误。校验逻辑拆分为独立字段（旧 PIN 和新 PIN 分别校验）。
3. **帮助页重试 action**：`help.dart` 的错误态 `AppStateMessageView` 新增 `actionLabel` + `onAction`，点击重试调用 `ref.invalidate(supportResourcesProvider('help'))` 重新加载。
4. **advanced/feature_flags 弹层勾选图标占位**：`advanced.dart` 的 `_EndpointSheet` 和 `_LogLevelSheet`、`feature_flags.dart` 的 `_ProviderSheet` 中，`suffix` 从 `selected ? SettingsSelectionIcon(selected: true) : null` 改为 `SettingsSelectionIcon(selected: ...)`。`SettingsSelectionIcon` 在未选中时渲染 `Opacity: 0` 的图标占位，避免 tile 布局在选中/未选中间跳动。

### 新增 l10n 键

- `mineEditRouteQuickOral` / `mineEditRouteQuickTopical` / `mineEditRouteQuickInhaled` / `mineEditRouteQuickInjection`
- `mineEditDoseQuick1Tablet` / `mineEditDoseQuick2Tablets` / `mineEditDoseQuick5ml` / `mineEditDoseQuick10ml`
- `settingsHelpRetryAction`

