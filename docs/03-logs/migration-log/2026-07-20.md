# Migration Log - 2026-07-20

## Luminous — UI/UX P0 修复：死路由、文案错误、Toast 键值复用、测试修复

- **来源**：`plans/2026-07-20-uiux-optimization-verification.md`（基于 `plans/2026-07-18-uiux-per-page-optimization.md`）。
- **验证方式**：`flutter analyze` 零问题，`flutter test` 2756 个测试全部通过。

### P0 修复 7 项

1. **死路由 `/medicine/reminders`**：`router.dart` 移除 `AppRoutes.medicineReminders` 常量（该路由无对应页面，跳转后 404）。用药页铃铛按钮改为跳转 `AppRoutes.notifications`（通知列表页）。Today 快捷操作"用药提醒"从 `medicineReminders` 改为 `medicineRemindersNew`（新建提醒页）+ `usePush: true`。
2. **`_MedicineSafeGuardPill` 窄屏断言**：窄屏下 `FButton` 的 `child: null` 触发 Forui 断言。修复：根据 `isNarrow` 条件切换 `FButton.icon`（仅图标）和 `FButton`（图标+文字），消除 `child: null`。
3. **提醒切换失败 Toast 复用 `settingsSyncFailed`**：`reminder_detail.dart` 的启停切换失败 Toast 从 `l10n.settingsSyncFailed` 改为专用 `l10n.medicineReminderToggleFailedToast`（"切换提醒状态失败"）。
4. **扫码识别中文案复用 `scanRecognitionFailedToast`**：`barcode_scanner.dart` 的识别中状态从 `l10n.scanRecognitionFailedToast`（"识别失败"）改为 `l10n.scanRecognizingHint`（"识别中…"），不再在识别过程中显示失败文案。
5. **帮助页空态/错误态 description 重复 title**：`help.dart` 的空态和错误态 `AppStateMessageView` 传入 `description` 与 `title` 完全相同。修复：移除 `description` 参数（`AppStateMessageView` 的 `description` 现已支持可选）。
6. **高级设置页残余分组标题**：`advanced.dart` 移除"恢复默认设置"的 `SettingsSectionLabel`（对应的 `FTileGroup` 已在前一次修改中删除，标题残留）。
7. **`AppStateMessageView.description` 改为可选**：`state_message.dart` 的 `description` 参数从 `required String` 改为 `String?`，`null` 时不渲染描述行。帮助页等仅需标题+图标的场景不再需要传入重复文案。

### 新增 l10n 键

- `medicineReminderToggleFailedToast`（"切换提醒状态失败" / "Failed to toggle reminder status."，zh + en）。
- `scanRecognizingHint`（"识别中…" / "Recognizing…"，zh + en）。

### 测试修复 3 项

1. **`reminder_form_body_test.dart` — `pumpAndSettle` 超时**：`isSaving: true` 时 `FCircularProgress` 是无限动画，`pumpAndSettle` 永远无法完成。修复：为 `_pumpForm` 增加 `settle` 参数，`isSaving` 测试中用 `pump(Duration(milliseconds: 100))` 替代 `pumpAndSettle()`。
2. **`dashboard_test.dart` — 时相关断言**：`TodayDashboard.signedOut()` 使用 `DateTime.now().hour` 动态计算时段，测试硬编码期望 `morning` 在下午运行时失败。修复：改为用 `todayDayMomentFromHour(DateTime.now().hour)` 动态计算期望值。
3. **其他测试修复**（前序会话完成）：`medicine_search_notifier_test.dart`（防抖等待）、`subpages_test.dart`（BOM 字符）、`providers_test.dart`（改用 repository 测试避免超时）、`ocr_entry_dialog_test.dart`、`reminder_pages_test.dart` 等。

## Luminous — UI/UX P1：跨页面共性 + 今日模块

- **来源**：`plans/2026-07-20-uiux-optimization-verification.md` § 4.1 + § 4.2。
- **验证方式**：`flutter analyze` 零问题，`flutter test` 2756 个测试全部通过。

### 4.1 跨页面共性

1. **C8 收尾 — PIN 上次修改时间**：`security_pin.dart` 的 `_lastChangedLabel` 方法从手拼 `yyyy-MM-dd HH:mm` 改为调用 `formatDateTimeLabel(dt.toIso8601String(), locale)`，尊重 locale 感知格式（zh: `1月18日 14:30`，en: `Jan 18, 14:30`）。
2. **C8 收尾 — legal 列表 updatedAt**：`legal/presentation/pages/list.dart` 的 `doc.updatedAt` 在传入 `l10n.legalListUpdatedAt()` 前先经 `formatDateTimeLabel()` 格式化，不再直接显示后端原始 ISO 串。
3. **error_mapper 中文兜底中性化**：`core/network/error_mapper.dart` 的 `_fallbackMessage()` 和 `fromObject()` 兜底消息从硬编码中文改为英文（locale-neutral），消除英文界面极端错误路径露中文问题。业务错误消息仍由服务端返回（已按 locale 本地化），仅网络层 fallback 受影响。

### 4.2 今日（today）

1. **用药指标分母修正**：`today/data/repositories/lucent.dart` 新增 `_todayScheduledMedicineIds()` 方法，通过查询今日有提醒计划的药品 ID 集合作为分母。`TodayMedicationSummary.medicineCount` 从 `medicines.length`（全部当前药品）改为今日有提醒的药品数；无提醒时回退为全部当前药品数，避免空值。
2. **置信度标签 FBadge 化**：`today/presentation/widgets/sections/observation.dart` 新增 `_ConfidenceBadge` 组件，按 `TodaySuggestionConfidence` 使用不同 `FBadgeVariant`：high→`primary`、medium→`secondary`、low→`outline`，实现视觉分级。原纯 `Text` 标签替换为 `FBadge`。
3. **置信度文案区分**：zh `todayObservationMediumConfidenceTag` 从"供参考"改为"值得留意"，与 low 的"仅供参考"拉开语义距离；en 从"For reference"改为"Worth noting"，与 low 的"FYI"区分。

### 测试更新

- `test/core/network/error_mapper_test.dart`：9 个测试的期望值从中文改为英文，测试名从"Chinese message"改为"locale-neutral message"。
- `test/core/errors/error_mapper_to_app_error_test.dart`：2 个测试的期望值同步更新；修复文件首行 BOM 字符。
- `test/features/legal/presentation/pages/legal_list_page_test.dart`：“renders updated date subtitle” 测试改用 `formatDateTimeLabel` 计算期望值，测试数据从 `'2026-01-01'` 改为 ISO datetime `'2026-01-15T10:30:00Z'`。

## Luminous — UI/UX P1：记录（record）模块

- **来源**：`plans/2026-07-20-uiux-optimization-verification.md` § 4.1 记录模块 P1 项。
- **验证方式**：`flutter analyze` 零问题，`flutter test` 2756 个测试全部通过。

### 修复项

1. **桌面筛选单选语义**：`sidebar.dart` 的 `_FilterRow` 图标从 `squareCheckBig` / `square`（复选框语义）改为 `circleCheck` / `circle`（单选语义），选中色从 `foreground` 改为 `primary`。点击已选中筛选可取消（`onFilterSelected` 传回 `null`）。
2. **创建表单 mood 类型对齐**：`form_fields.dart` 的 `activeDailyRecordKinds` 新增 `DailyRecordKind.mood`，使创建表单的类型选择器可选心情。
3. **语音 sheet 错误显示**：`voice_entry_dialog.dart` 新增 `errorMessage` 内联显示区（图标 + 红色文案），不再只存不显。
4. **语音 sheet 分类文案**：初始化失败的三类场景（权限拒绝 / 语音不可用 / locale 不支持）各自显示不同的 l10n 消息，不再统一报“麦克风权限未授权”。新增 `recordSpeechUnavailable` / `recordSpeechLocaleUnsupported` / `recordVoiceErrorHint` 键。
5. **语音结果可编辑**：识别结果从只读 `Text` 改为 `TextField`，用户可在识别后手动编辑修正。新增 `recordVoiceEditHint` 键。使用 `useTextEditingController` 持久化控制器，避免重建时光标跳动。
6. **必填校验内联化**：`create.dart` 的 `onSave` 校验从 `AppToast.show` 改为设置 `valueError` / `titleError` 状态，通过 `FTextField.error` 在字段下方内联显示错误提示。
7. **“标题（可选）”矛盾消除**：`form_fields.dart` 的标题字段标签从 `recordCreateFieldTitleOptional`（“标题（可选）”）改为 `recordCreateFieldTitle`（“标题”），消除与必填校验的矛盾。新增 `recordCreateFieldTitle` 键。
8. **保存中表单整体禁用**：`DailyRecordFormFields` 新增 `enabled` 参数，`create.dart` 在 `saving` 时传 `!saving.value`，所有字段（kind 选择器、值、单位、标题、备注）连同图片附件一起禁用。
9. **桌面骨架 3 列**：`skeleton_view.dart` 的 `_DesktopRecordSkeleton` 从 2 列（sidebar + expanded）改为 3 列（sidebar + center flex:6 + right sidebar），与真实 `_DesktopRecordDashboard` 的三栏布局对齐。
10. **NLP 预生成失败提示**：`nlp_dialog.dart` 新增 `RecordNlpStatus.error` 状态分支，显示图标 + 错误文案（`state.errorMessage` 或 `recordNlpGenerateFailedToast` 兜底）。新增 `recordNlpGenerateFailedToast` 键。

### 新增 l10n 键

- `recordSpeechUnavailable`（“语音识别不可用，请检查设备是否支持。” / “Speech recognition is unavailable on this device.”）
- `recordSpeechLocaleUnsupported`（“当前语言暂不支持语音识别。” / “Speech recognition does not support the current language.”）
- `recordVoiceErrorHint`（“识别出错，请重试。” / “Recognition error. Please try again.”）
- `recordVoiceEditHint`（“可在此编辑识别结果” / “You can edit the recognized text here”）
- `recordCreateFieldTitle`（“标题” / “Title”）
- `recordNlpGenerateFailedToast`（"解析失败，请检查内容后重试。" / "Parsing failed. Please check your input and try again."）

## Luminous — UI/UX P1：用药 / 搜索 / 扫码模块

- **来源**：`plans/2026-07-20-uiux-optimization-verification.md` § 4.4 用药/搜索/扫码 P1 项。
- **验证方式**：`flutter analyze` 零问题，`flutter test` 477 个相关测试全部通过。

### 修复项

1. **桌面搜索栏全状态可见**：`medicine/presentation/pages/page.dart` 的 `loadingBuilder`、`fatalErrorBuilder`、`emptyInsufficientBuilder` 三个桌面分支新增 `_MedicineMobileSearchBar()`，与 `readyBuilder` 保持一致。加载/错误/空态下用户仍可点击搜索栏跳转搜索页。
2. **搜索结果卡"已添加"态**：`SearchResultTile` 新增 `alreadyAdded` 参数。`SearchPage` 通过 `healthContextSnapshotProvider` 获取当前药品列表，构建 `source:sourceRefId` 集合传入 `MedicineSearchView` → `_MobileSearchLayout` / `_DesktopSearchLayout` → `_DesktopSearchPanel` → `SearchResultTile`。已添加的药品显示禁用 outline 按钮 + check 图标 + "已添加"文案，不可重复添加。
3. **拍照识别失败手动兜底**：`box_scan.dart` 的 catch 分支从 `AppToast.show` 改为 `_showScanFailureDialog()` 对话框，提供"重新拍照"（重试）和"手动搜索"（跳转 `/medicine/search`）两个操作。原先仅 toast 无手动兜底的问题已修复。
4. **死键清理**：`scanSearchFailedToast`（带 `{error}` 参数，零引用）从 zh/en ARB 文件中删除。`scanManualSearchToast` 从零引用改为在失败对话框 body 中使用。`scanManualSearchAction` 从零引用改为在失败对话框按钮中使用。
5. **扫码遮罩安全弹窗**：`box_scan.dart` 新增 `_dismissOverlay()` 函数，通过 `Navigator.canPop()` 检查后再 `pop()`，替代原来直接 `Navigator.of(context, rootNavigator: true).pop()` 的脆弱写法。
6. **扫码结果对话框关闭按钮**：`recognize_dialog.dart` 新增 `onClose` 可选回调和 `scanCloseAction` 按钮（ghost 变体）。用户可在无识别结果时直接关闭对话框，不必只能重拍。

### 新增 l10n 键

- `medicineSearchAlreadyAddedLabel`（"已添加" / "Already added"）
- `scanCloseAction`（"关闭" / "Close"）

### 删除 l10n 键

- `scanSearchFailedToast`（带 `@scanSearchFailedToast` 元数据，zh + en）

## Luminous — UI/UX P1：报告（report）模块

- **来源**：`plans/2026-07-20-uiux-optimization-verification.md` § 4.5 报告模块 P1 项。
- **验证方式**：`flutter analyze` 零问题，`flutter test` 125 个报告模块测试全部通过。

### 修复项

1. **切时间范围保留旧值**：`providers/dashboard.dart` 新增 `ReportLastDashboardNotifier` + `reportLastDashboardProvider`，缓存最近一次成功加载的 `ReportDashboard`。`page.dart` 通过 `ref.listen` 监听 dashboard provider，在数据到达时更新缓存；当新查询正在加载且无缓存值时，使用 `effectiveAsync = AsyncValue.data(cachedDashboard)` 作为 `resolvePageViewState` 的输入，使页面展示旧数据而非整页骨架。`isRefreshing` 仍从原始 `dashboardAsync.isLoading` 取值，保证加载指示器正确显示。
2. **AI 总结"自定义"范围日期兜底**：`providers/ai_summary.dart` 的 `generate()` 方法在 `range == custom` 且 dashboard query 非 custom 时，不再传 null 日期。改为从 `reportLastDashboardProvider` 读取缓存的 `startDate`/`endDate`；缓存不可用时回退到最近 7 天日期。确保所有自定义范围请求都携带有效日期。
3. **桌面 loading 假按钮禁用**：`page.dart` 的 `_buildLoadingShell` 中 `ReportActionBar` 传入 `isGenerating: true, isSyncing: true`，使两个按钮的 `onPress` 为 `null`（禁用态），消除加载态可点击的空回调假按钮。
4. **图表 tooltip 触点值+日期**：`trend.dart` 的 `getTooltipItems` 从恒显示 `series.currentValue` 改为根据 `spot.spotIndex` 取 `series.values[index]` 的实际触点值。tooltip 新增日期行，使用 `DateFormat.MMMEd(locale)` 格式化（如"7月20日周日" / "Sun, Jul 20"）。
5. **图表 Semantics 数值摘要**：`ReportTrendSection` 的 `Semantics.label` 从仅标题改为包含各指标当前值摘要（如"趋势, 睡眠: 7.5h, 饮水: 1200ml"）。

## Luminous — UI/UX P1：我的（mine）+ 设置（settings）模块

- **来源**：`plans/2026-07-20-uiux-optimization-verification.md` § 4.6 + § 4.7 P1 项。
- **验证方式**：`flutter analyze` 零问题，`flutter test test/mine/ test/settings/` 289 个测试全部通过。

### 修复项

1. **用药表单"途径/剂量"常用值选择器**：`current_medicine_edit.dart` 在剂量和给药途径文本框下方新增 `_QuickSelectChip` 快速选择芯片。剂量提供 4 个常用值（1片/2片/5ml/10ml），途径提供 4 个常用值（口服/外用/吸入/注射）。点击芯片直接填入对应文本框，用户也可手动输入自定义值。
2. **PIN 校验失败行内 error**：`security_pin.dart` 从 `AppToast.show` 改为行内 `error: Text(...)` 显示校验错误。新增 6 个 `ValueNotifier<String?>` 分别管理启用/确认/旧/新/确认新/禁用 PIN 的错误状态。通过 `useEffect` 注册 controller listener，用户编辑字段时自动清除对应错误。校验逻辑拆分为独立字段（旧 PIN 和新 PIN 分别校验）。
3. **帮助页重试 action**：`help.dart` 的错误态 `AppStateMessageView` 新增 `actionLabel` + `onAction`，点击重试调用 `ref.invalidate(supportResourcesProvider('help'))` 重新加载。
4. **advanced/feature_flags 弹层勾选图标占位**：`advanced.dart` 的 `_EndpointSheet` 和 `_LogLevelSheet`、`feature_flags.dart` 的 `_ProviderSheet` 中，`suffix` 从 `selected ? SettingsSelectionIcon(selected: true) : null` 改为 `SettingsSelectionIcon(selected: ...)`。`SettingsSelectionIcon` 在未选中时渲染 `Opacity: 0` 的图标占位，避免 tile 布局在选中/未选中间跳动。

### 新增 l10n 键

- `mineEditRouteQuickOral` / `mineEditRouteQuickTopical` / `mineEditRouteQuickInhaled` / `mineEditRouteQuickInjection`
- `mineEditDoseQuick1Tablet` / `mineEditDoseQuick2Tablets` / `mineEditDoseQuick5ml` / `mineEditDoseQuick10ml`
- `settingsHelpRetryAction`

## Luminous — UI/UX P2：打磨批（低级别项）

- **来源**：`plans/2026-07-20-uiux-optimization-verification.md` § 五 P2 批。
- **验证方式**：`flutter analyze` 零问题，`flutter test` 2756 个测试全部通过。

### 4.1 记录（record）

1. **`date_bar` 硬编码字号**：`date_bar.dart` 的 `_weekdayFontSize = 11` / `_dateFontSize = 14` 从 `const double` 改为 `TypographyToken.level2` / `TypographyToken.level3` 的 `fontSize` 取值函数，消除硬编码魔数。
2. **移动端"回到今天"入口**：`mobile_timeline.dart` 新增 `onBackToToday` 回调，当选中日期非今天时在标题行右侧显示"回到今天"按钮。`dashboard_view.dart` 传入 `() => onDateSelected!(clock.now())` 回调。
3. **移动端筛选空态"清除筛选"**：`mobile_timeline.dart` 的 `_MobileTimelineEmptyState` 新增 `hasActiveFilter` / `onClearFilter` 参数，筛选激活且无结果时在空态显示"清除筛选"按钮。
4. **OCR 空结果反馈**：`ocr_entry_dialog.dart` 在 OCR 返回空文本时显示 `recordOcrEmptyResult` 提示（图标 + 文案），替代之前的无反馈状态。
5. **详情页编辑入口去重**：`detail.dart` 移除底部重复的"编辑"按钮（顶部 `PageScaffold.actions` 已有编辑入口），仅保留"删除"按钮。
6. **详情页标签固定宽 88**：`detail.dart` 的 `_DetailRow` 从 `SizedBox(width: 88)` 改为 `ConstrainedBox(constraints: BoxConstraints(minWidth: Spacing.level8 * 2, maxWidth: Spacing.level8 * 2 + Spacing.level4))`，允许标签宽度弹性。
7. **菜品删除按钮 tooltip/语义**：`dish_editor.dart` 的删除图标按钮新增 `Tooltip` 包裹和 `Icon.semanticLabel`。
8. **图片附件拍照入口**：`image_attachment_field.dart` 新增可选 `onCameraPick` 回调，有值时显示"拍照"按钮。`create.dart` 重构 `onPickImage` 为共享 `pickAndProcessImage(ImageSource)` 方法，新增 `onPickFromCamera` 调用 `ImageSource.camera`。
9. **脏状态返回确认**：`create.dart` 新增 `PopScope` + `_isDirty` + `_confirmDiscardChanges`，表单有未保存内容时返回会弹出确认对话框。
10. **NLP 候选睡眠时间选择器**：`nlp_candidate_editor.dart` 的 `_SleepCandidateFields` 从分钟数文本输入改为就寝/起床 `FTimeField.picker`，与主表单 `SleepStructuredFields` 保持一致。自动计算 `durationMinutes` 并显示时长。

### 4.4 用药 / 搜索 / 扫码

1. **搜索超时文案中性化**：`medicine_search.dart` 的 `TimeoutException` 消息从硬编码中文 `'请求超时，请检查网络后重试。'` 改为英文 `'Search timed out. Please try again.'`（实际不上屏，由 `LucentErrorMapper` 覆盖为兜底消息）。

### 4.5 报告（report）

1. **`emptyInsufficientBuilder` 死代码删除**：`page.dart` 移除 `emptyInsufficientBuilder` 分支（`resolvePageViewState` 未传 `isInsufficient` 回调，该分支不可达）。
2. **分数字号 token 外覆盖**：`score_hero.dart` 的分数字号从 `TypographyToken.level9.display(context).copyWith(fontSize: ResponsiveSizing.scaleByWidth(...))` 改为 `TextStyle(fontSize: ResponsiveSizing.scaleByWidth(...))`，消除先套 token 再覆盖的矛盾写法。

### 4.6 我的（mine）

1. **角色文案中性化**：`mine_zh.arb` / `mine_en.arb` / `app_zh.arb` / `app_en.arb` 的 `mineAccountStudentRole` 从"大学生" / "Student" 改为"用户" / "User"。

### 4.7 设置（settings）

1. **免打扰摘要 subtitle→details**：`notification.dart` 的 DnD tile 从 `subtitle:` 改为 `details:`，与同组其余 tile（睡眠提醒、提前提醒）保持一致。

### 4.8 助手 / 认证

1. **验证码冷却期禁用**：`change_email.dart` 的 `onSendCode` 在 `lastCooldownSeconds != null`（冷却中）时设为 `null`，防止冷却期间重复点击发送。
2. **助手 provider 硬编码文案清理**：`conversation.dart:298` 的 `sendError` 从硬编码中文 `'AI 流式响应已结束，但没有返回最终结果。'` 改为 `null`（UI 层已通过 `sendErrorType` 映射 l10n 键）。`conversation.dart:440` 的 `LucentApiException` 消息从中文改为英文。

### 新增 l10n 键

- `recordOcrEmptyResult`（"未识别到文字，请尝试更换图片" / "No text recognized. Try another photo."）
- `recordBackToTodayAction`（"回到今天" / "Back to today"）
- `recordDiscardChangesTitle`（"放弃修改" / "Discard changes"）
- `recordDiscardChangesMessage`（"当前内容尚未保存，确定离开吗？" / "You have unsaved changes. Leave anyway?"）
- `recordDiscardChangesAction`（"放弃" / "Discard"）
- `recordImageCameraAction`（"拍照" / "Take photo"）
- `recordCreateSectionBasicTitle`（"基本信息" / "Basic info"）
- `recordCreateSectionDetailsTitle`（"记录内容" / "Record details"）

## Luminous — UI/UX P2：打磨批续（低级别项第二批）

- **来源**：`plans/2026-07-20-uiux-optimization-verification.md` § 五 P2 批续。
- **验证方式**：`flutter analyze` 零问题，`flutter test` 2756 个测试全部通过。

### 4.1 记录（record）续

1. **locked 筛选策略一致**：`sidebar.dart` 的 `_FilterRow` onTap 增加 `filter.locked` 检查，locked 筛选在桌面端也禁用点击，与移动端 `mobile_filter.dart` 一致。
2. **创建页分区标题**：`create.dart` 在 `RecordOccurredAtFields` 前添加 `recordCreateSectionBasicTitle`，在 `DailyRecordFormFields` 前添加 `recordCreateSectionDetailsTitle`，字段不再无分区堆叠。
3. **切类型清理不适字段**：`create.dart` 的 `onKindChanged` 新增 `dailyRecordFormRules(newKind)` 检查，不显示的字段（`showValue`/`showTitle`/`showUnit` 为 false 时）自动清空 controller，避免切回时旧内容静默重现。

### 4.4 用药 / 搜索 / 扫码续

1. **图标魔数统一**：`mobile_safety.dart`、`mobile_drugbox.dart`、`risk_red_flag.dart`、`rows.dart`、`log_panels.dart`、`form_fields.dart` 的 `size: 16` / `size: 18` 统一替换为 `size: Spacing.level5`（18），消除魔数。
2. **提示音下拉固定宽 140**：`rows.dart` 的 `SizedBox(width: 140, child: FSelect(...))` 改为 `Flexible(child: FSelect(...))`，宽度自适应。

### 4.5 报告续

1. **装饰图标 ExcludeSemantics**：`readiness.dart` 的状态头像图标和时钟图标包裹 `ExcludeSemantics`，避免屏幕阅读器重复朗读相邻文字。
2. **就绪卡"生成总结"loading**：`readiness.dart` 的 `_PrimaryAction` ready 状态新增 `isGenerating` 参数，生成中禁用按钮 + 显示 `FCircularProgress`。`dashboard_view.dart` 两处 `ReportReadinessSection` 传入 `isGenerating: aiSummaryState.status == ReportAiSummaryCardStatus.loading`。

### 4.6 我的续

1. **无引用残留清理**：删除 `health_enum_l10n.dart` 中零引用的 `medicineSourceLabel` 函数；删除 `dashboard.dart` 的 `MineCopyKey.archiveRecordListTitle` 枚举值及 `copy.dart` 的对应映射。
2. **SyncBanner 注释修正**：`skeleton_view.dart` 的类文档注释从 `SyncBanner` 改为 `MineSyncFailedBanner`，与实际 widget 名称一致。

### 4.7 设置续

1. **垂直 padding helper 统一到全部子页**：`dnd.dart`、`sleep_reminder.dart`、`ai.dart`、`accessibility.dart`、`advanced.dart`、`data_storage.dart`、`feature_flags.dart`、`security_pin.dart`、`theme.dart` 共 9 个子页的内联 `MediaQuery.sizeOf(context).width < Breakpoints.mobile ? Spacing.level6 : Spacing.level7` 三元表达式替换为 `settingsPageVerticalPadding(context)` 共享函数。

## Luminous — UI/UX 收尾批：剩余低级别打磨项

- **来源**：`plans/2026-07-20-uiux-optimization-verification.md` § 二 未闭环清单。
- **验证方式**：`flutter analyze` 零问题，`flutter test` 2756 个测试全部通过。

### 4.5 报告（report）

1. **clinicShare 进行中态**：`providers/dashboard.dart` 新增 `ClinicShareInFlightNotifier` + `clinicShareInFlightProvider`，追踪独立的 clinic share API 请求状态。`page.dart` 的 `_handleClinicShare` 在调用前后设置/清除 in-flight 标志。`ReportExportSection` / `_ExportCard` 新增 `clinicShareInFlight` 参数，clinicShare 卡片在请求中显示 `loaderCircle` 图标并禁用按钮，请求完成后恢复。
2. **移动端"弹窗套弹窗"**：`range_picker_dialog.dart` 在移动端（width < `Breakpoints.desktop`）改用 `showModalBottomSheet` 替代 `showFDialog`，范围选择器和日历选择器均以底部动作单形式呈现，消除弹窗嵌套。桌面端保持原有 FDialog 居中弹窗不变。

### 4.7 设置（settings）

1. **通知组单 tile 合并**：`page.dart` 的通知 tile 从独立 `FTileGroup`（单 tile 分组）移入"通用"section 的 `FTileGroup`，消除碎片化分组。`_notificationSummary` 方法从 `SettingsPage` 移至 `_GeneralSection`。
2. **支持 URL 常量提取**：`about.dart` 的文件内 `_kFallbackSupportUrl` 常量移至 `support/domain/entities/support_resource.dart` 的顶层 `kFallbackSupportUrl` 常量，仅作兜底用途。
3. **健康档案入口返回栈**：`page.dart` 的"健康档案"tile 从 `pushAuthRequiredRoute(context, AppRoutes.mine)`（push 到 tab 根，产生异常返回栈）改为已登录时 `context.go(AppRoutes.mine)`（切换 tab，无返回栈问题），未登录时仍走 `pushAuthRequiredRoute` 弹登录对话框。

### 4.8 助手

1. **消息时间戳**：`message_bubble.dart` 新增 `createdAt` 可选参数，在消息内容下方显示 `HH:mm` 格式时间戳（locale 感知）。`conversation_surface.dart` 传入 `message.createdAt`。
2. **长按复制菜单**：`message_bubble.dart` 新增 `GestureDetector.onLongPress`，弹出 `showMenu` 上下文菜单，提供"复制文本"操作。复制后通过 `AppToast` 显示成功提示。
3. **生成中动画指示器**：`message_bubble.dart` 的流式生成状态从纯静态文本改为三圆点脉冲动画（`flutter_animate` scale 循环）+ 文本标签组合。
4. **切换会话动画指示器**：`conversation_surface.dart` 的"正在切换会话…"从纯静态文本改为旋转 `FCircularProgress` + 文本标签组合。

### 4.8 通知

1. **桌面端 hover 操作钮**：`list_item.dart` 转为 `StatefulWidget`，新增 `MouseRegion` hover 检测。桌面端（width >= `Breakpoints.desktop`）在 hover 时显示"已读/未读"和"删除"两个 `IconButton`（带 `Tooltip`），替代仅 `Slidable` 拖拽的操作方式。移动端保持 `Slidable` 不变。

### 4.8 认证

1. **改密/注销 Form 包裹**：`account_settings_sections.dart` 的 `PasswordSection` 和 `DeleteAccountSection` 从 `StatelessWidget` 改为 `StatefulWidget`，内部用 `GlobalKey<FormState>` + `Form` 包裹 `FTextFormField`，提交前调用 `formKey.currentState?.validate()`。`account_settings.dart` 的 `onChangePassword` 和 `onDelete` 回调移除手动空值检查 + toast（由 Form validator 接管）。
2. **头像预览与校验**：`account_settings.dart` 的 `_ProfileSection` 新增 `ValueListenableBuilder` 监听 avatar controller，在 URL 输入框上方显示圆形头像预览。URL 为空时显示默认用户图标，URL 有效时显示网络图片（`Image.network` + `errorBuilder` 兜底），URL 无效时预览旁显示红色提示文案。

### 新增 l10n 键

- `assistantCopyAction`（"复制文本" / "Copy text"）
- `assistantCopySuccess`（"已复制到剪贴板" / "Copied to clipboard"）
- `authAvatarPreviewEmpty`（"未设置头像" / "No avatar set"）
- `authAvatarPreviewReady`（"头像预览" / "Avatar preview"）
- `authAvatarPreviewInvalid`（"请输入有效的图片 URL" / "Please enter a valid image URL"）

### 测试更新

- `test/settings/page_test.dart`：`FTileGroup` 期望数量从 7 改为 6（通知 tile 合并到通用组后减少一个分组）。

## Luminous — 联调修正：SSE 超时修复 + 建议历史详情面板

- **来源**：`plans/2026-07-20-integration-fix-plan.md` 任务 1 + 任务 2。
- **验证方式**：`flutter analyze` 零问题（3 个涉及文件）。

### 任务 1：SSE 流式接收超时修复

1. **`lib/core/network/sse.dart`**：`LucentSseClient._postAndDecode` 的 `Options` 新增 `receiveTimeout: Duration.zero`。SSE 是长连接流式响应，主 Dio 实例的 `receiveTimeout: 10s` 是两次数据事件之间的最大间隔，AI 生成如果两次 chunk 间隔超过 10s 就会抛 `receiveTimeout` 异常导致 SSE 流提前中断。设为 `Duration.zero`（不限超时）避免此问题。

### 任务 2：建议历史详情面板

1. **新增 `lib/features/report/presentation/widgets/dialogs/suggestion_history_detail_sheet.dart`**：`showSuggestionHistoryDetailSheet` 函数，桌面端用 `showFDialog` + `AppDialogShell`，移动端用 `showModalBottomSheet`。内容展示建议类型图标+标题、生命周期 Badge、原因正文、规则 ID/版本/触发方式/置信度/生成时间等 meta 字段、用户反馈（如有）、过期时间（如有）、关闭按钮。
2. **`lib/features/report/presentation/pages/page.dart`**：`onSuggestionTap` 从 `null` 改为 `(item) => showSuggestionHistoryDetailSheet(context, suggestion: item)`，历史建议列表项变为可点击。新增 `suggestion_history_detail_sheet.dart` import。
3. **i18n 字符串**：在 `report_zh.arb` / `report_en.arb` 新增 21 个键（10 个详情面板标签 + 5 个建议类型 + 2 个触发方式 + 3 个置信度 + 1 个 `commonClose`）。

### 新增 l10n 键

- `commonClose`（"关闭" / "Close"，common_zh / common_en）
- `reportSuggestionHistoryDetailTitle`（"建议详情" / "Suggestion Detail"）
- `reportSuggestionHistoryDetailRuleId`（"规则 ID" / "Rule ID"）
- `reportSuggestionHistoryDetailRuleVersion`（"规则版本" / "Rule Version"）
- `reportSuggestionHistoryDetailTriggerType`（"触发方式" / "Trigger Type"）
- `reportSuggestionHistoryDetailConfidence`（"置信度" / "Confidence"）
- `reportSuggestionHistoryDetailGeneratedAt`（"生成时间" / "Generated At"）
- `reportSuggestionHistoryDetailFeedback`（"用户反馈" / "Your Feedback"）
- `reportSuggestionHistoryDetailFeedbackAt`（"反馈时间" / "Feedback At"）
- `reportSuggestionHistoryDetailExpiredAt`（"过期时间" / "Expired At"）
- `reportSuggestionHistoryDetailNoFeedback`（"未反馈" / "Not yet"）
- `reportSuggestionTypeConfirmedRisk`（"确认风险" / "Confirmed Risk"）
- `reportSuggestionTypeCompliance`（"依从性" / "Compliance"）
- `reportSuggestionTypeTrend`（"趋势" / "Trend"）
- `reportSuggestionTypeBehaviorAdvice`（"行为建议" / "Behavior Advice"）
- `reportSuggestionTypeCoverage`（"覆盖率" / "Coverage"）
- `reportSuggestionTriggerTypeEvent`（"事件触发" / "Event"）
- `reportSuggestionTriggerTypeTimer`（"定时触发" / "Timer"）
- `reportSuggestionConfidenceHigh`（"高" / "High"）
- `reportSuggestionConfidenceMedium`（"中" / "Medium"）
- `reportSuggestionConfidenceLow`（"低" / "Low"）

## Luminous — 联调修正：诊所摘要全链路接入

- **来源**：`plans/2026-07-20-integration-fix-plan.md` 任务 3-8。
- **验证方式**：`flutter analyze` 零问题。

### 任务 3：诊所摘要预览弹窗

1. **新增 `lib/features/report/presentation/providers/clinic_summary.dart`**：`clinicSummaryPreviewProvider`（autoDispose FutureProvider）调用 `reportsControllerPreviewClinicSummaryV1()` 获取 `ClinicSummaryDto`；`clinicSummarySharedProvider`（autoDispose family FutureProvider）调用 `reportsControllerGetSharedClinicSummaryV1(token)` 获取公开分享的摘要。
2. **新增 `lib/features/report/presentation/widgets/shared/clinic_summary_content.dart`**：`ClinicSummaryContent` 共享组件，展示生成时间、数据范围、脱敏个人信息（昵称/年龄/性别/血型）、过敏记录、疾病记录、当前用药、关键发现、免责声明，底部可选 [下载 PDF] / [分享] 按钮。
3. **新增 `lib/features/report/presentation/widgets/dialogs/clinic_summary_preview_dialog.dart`**：`showClinicSummaryPreviewDialog` 函数，桌面端用 `showFDialog` + `AppDialogShell`，移动端用 `showModalBottomSheet`。内部 `ConsumerStatefulWidget` 管理 PDF 下载和分享的 loading 状态。

### 任务 4：诊所摘要 PDF 下载

1. **`lib/core/network/api_paths.dart`**：新增 `clinicSummaryPreviewPdf`（`GET /api/v1/user/reports/clinic-summary/preview/pdf`）和 `clinicSummarySharedPdf(token)`（`GET /api/v1/user/reports/clinic-summary/shared/{token}/pdf`）路径常量。
2. **PDF 下载逻辑**：通过 Raw Dio 以 `ResponseType.bytes` 下载 PDF 二进制，保存到 `getTemporaryDirectory()` 临时文件后通过 `SharePlus.instance.share(ShareParams(files: [XFile(...)]))` 分享。

### 任务 5：诊所摘要分享链路改造

1. **`lib/features/report/presentation/pages/page.dart`**：`_handleExportAction` 的 `clinicShare` 分支从直接调用 `_handleClinicShare` 改为先弹出 `showClinicSummaryPreviewDialog(context)`，用户在预览弹窗内点击 [分享给医生] 按钮触发分享。
2. **移除 `_handleClinicShare` 方法**：原 `page.dart` 中的 `_handleClinicShare` 方法逻辑已迁移到弹窗内的 `_share` 方法，移除死代码及 `share_plus` 和 `network_providers` 导入。

### 任务 6：诊所摘要公开分享页（深链接）

1. **新增 `lib/features/report/presentation/pages/clinic_summary_shared.dart`**：`ClinicSummarySharedPage`（ConsumerStatefulWidget），接收 `token` 参数，watch `clinicSummarySharedProvider(token)`，展示 `ClinicSummaryContent` + [下载 PDF] 按钮。公开 PDF 下载调用 `shared/{token}/pdf`，`extra: {skipAuthorization: true}` 跳过认证拦截器。
2. **`lib/app/router.dart`**：`AppRoutes` 新增 `reportClinicSummaryShared = '/report/clinic-summary/:token'` 常量；`_publicRoutePrefixes` 新增 `'/report/clinic-summary'`；`routes` 列表新增 `GoRoute` 条目（`slidePage` 过渡）。

### 任务 7：i18n 字符串补充

在 `report_zh.arb` / `report_en.arb` 新增 21 个诊所摘要相关键：

- `reportClinicSummaryPreviewTitle` / `reportClinicSummaryGeneratedAt` / `reportClinicSummaryDataRange`
- `reportClinicSummaryProfileSection` / `reportClinicSummaryProfileNickname` / `reportClinicSummaryProfileAge` / `reportClinicSummaryProfileSex` / `reportClinicSummaryProfileBloodType`
- `reportClinicSummaryAllergiesSection` / `reportClinicSummaryConditionsSection` / `reportClinicSummaryMedicinesSection` / `reportClinicSummaryFindingsSection` / `reportClinicSummaryDisclaimerSection`
- `reportClinicSummaryEmpty` / `reportClinicSummaryDownloadPdf` / `reportClinicSummaryShare`
- `reportClinicSummaryPdfEmpty` / `reportClinicSummaryPdfFailed`
- `reportClinicSummarySharedTitle` / `reportClinicSummarySharedExpired` / `reportClinicSummarySharedDownloadPdf`
- `reportClinicSummaryNotSet` / `reportClinicSummaryLoading` / `reportClinicSummaryLoadFailed`
