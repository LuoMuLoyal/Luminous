# Migration Log - 2026-07-08

## Luminous — Today 根页重构为主动建议行动面板

- **目标**：把 Today 从 `overview / priorities / AI / recommendation / todo` 的平铺首页，收口为符合 `Product_Tab_Component_Blueprint` 的行动面板。
- **结构重构**：
  - 首屏改为 `主建议卡 → 次建议区 → 今日摘要 → 观察项 → 轻动作`，`TodayTopBar` 和 preview 登录提示继续保留。
  - 旧的 `TodayOverviewSection / TodayPrioritySection / TodayAiSummarySection / TodayRecommendationSection / TodayTodoSection` 已整体下线。
  - 新增 `suggestion_section / summary_section / observation_section / quick_actions_section`，统一使用 Forui `FCard / FTile / FBadge / FButton` 组织页面。
- **语义调整**：
  - 主卡只承接当前最高优先的用药或饮水动作，卡内补齐 `证据 / 边界 / 主动作`。
  - `今日摘要` 把概览指标和 AI 解释收拢到同一张低权重卡，不再和主建议抢主位。
  - 原 recommendation 列表降级为 `观察项` 数据源，低置信度内容不再以 `todo` 语义出现。
  - 轻动作区改为直接跳转入口，承接 `去确认 / 去记录 / 看解释 / 调提醒 / 完善档案`。
- **测试重写**：
  - `test/today/page_test.dart` 已从旧 recommendation/todo 断言改为验证新首页结构、signed-out 预览、桌面布局和 observation loading。
  - `test/today/ai_card_test.dart` 已改为围绕新的 `TodaySummarySection` 验证 preview / disabled / generate 三种状态。
- **文件**：
  - `lib/features/today/presentation/widgets/views/dashboard_view.dart`
  - `lib/features/today/presentation/widgets/sections/suggestion_section.dart`
  - `lib/features/today/presentation/widgets/sections/summary_section.dart`
  - `lib/features/today/presentation/widgets/sections/observation_section.dart`
  - `lib/features/today/presentation/widgets/sections/quick_actions_section.dart`
  - `lib/features/today/presentation/widgets/shared/view_models.dart`
  - `lib/l10n/app_zh.arb`
  - `lib/l10n/app_en.arb`
  - `lib/l10n/app_localizations*.dart`
  - `test/today/page_test.dart`
  - `test/today/ai_card_test.dart`
- **验证**：
  - `flutter gen-l10n`
  - `flutter test test/today`
  - `flutter analyze`

## Luminous — 7.8 审查问题修复（P1 + P2）

基于 `plans/luminous-review-2026-07-08.md` 审查报告，修复以下问题：

### P1-1: Mock 数据硬编码时间戳 (LUM-2026-0708-01)

- **问题**：`mock_repository.dart` 中 `previewDashboard` 和 `_signedOutDashboard` 使用固定时间戳 `'2026-07-07T14:32:00.000Z'`，演示时产生"报告生成于过去"的错觉。
- **修复**：改为 `clock.now().toIso8601String()` 动态生成当前时间，移除 `const` 关键字。
- **文件**：`lib/features/report/data/repositories/mock_repository.dart`

### P1-2: 实体默认值中的 epoch 日期 (LUM-2026-0708-02)

- **问题**：`dashboard.dart` 的 `signedOut()` 使用 `generatedAt: '1970-01-01T00:00:00.000Z'`，UI 上可能显示"1970年"。
- **修复**：改为空字符串 `''`，UI 格式化器已有 `DateTime.tryParse` + null 返回 `''` 的保护。
- **文件**：`lib/features/report/domain/entities/dashboard.dart`

### P2-1: 重复空值检查模式 (LUM-2026-0708-04)

- **问题**：`remote_data_source.dart` 中约 17 处 `if (body == null) throw ...` 重复模式。
- **修复**：提取 `_requireBody<T>(T? body, String message)` 私有方法，每处从 3 行简化为 1 行。
- **文件**：`lib/features/auth/data/datasources/remote_data_source.dart`

### P2-2: debugPrint 残留 (LUM-2026-0708-05)

- **问题**：`login_form_provider.dart` 中 9 处 `debugPrint` 调试日志，可能泄露敏感信息。
- **修复**：移除全部 `debugPrint` 调用（catch 块已有 `LucentErrorMapper` 提取用户可见错误）。
- **文件**：`lib/features/auth/presentation/providers/forms/login_form_provider.dart`

### P2-3: 日期格式化硬编码 (LUM-2026-0708-06)

- **问题**：`ui_formatters.dart` 用 `locale.startsWith('zh')` 手动切日期格式，不支持 zh-TW/zh-HK 等变体。
- **修复**：改用 `DateFormat.MMMd(locale)` 和 `DateFormat.MMMd(locale).add_Hm()` 让 `intl` 包自动处理本地化。
- **文件**：`lib/features/report/presentation/utils/ui_formatters.dart`

### P2-4: 颜色映射全部返回 primary (LUM-2026-0708-03)

- **问题**：`lucent_repository.dart` 的 `_metricColor` / `_insightColor` 所有数据类型都返回 `AppColors.primary`，视觉层次弱化。
- **修复**：为不同数据类型分配不同语义色 token：medication → primary、water → muted、sleep → foreground、general → primary。
- **文件**：`lib/features/report/data/repositories/lucent_repository.dart`

### 验证

- `flutter analyze` — No issues found!
- `flutter test test/report/` — 37/37 passed
- `flutter test test/auth/ --name "login"` — 15/15 passed
- 7 个预先存在的失败测试与本次修改无关（record 时间格式化器、session gate medicine plan、shell page desktop tabs）

## Luminous — Medicine 首页 Phase 1 重构

- **目标**：把 Medicine 根页收敛为符合 `Product_Tab_Component_Blueprint` 的四块主结构，不改 Lucent 合同。
- **首页结构调整**：
  - `My drugbox` 重构为 `当前用药盒` 主区，只负责当前药品列表和管理入口。
  - `用药记录` 主卡重构为 `今日服用计划`，把下一剂提示和当日确认动作集中到同一区块。
  - `安全检查预览` 重构为 `用药安全摘要`，首页直接复用风险检查三层语义：已确认风险 / 已确认安全 / 未覆盖或不确定。
  - `用药操作` 收敛为加药、查药、提醒设置、风险检查四项，不再把 `Record` tab 的“记录服药”作为首页主动作。
- **移除的首页区块**：
  - `Reference notice`
  - `Safety tips`
  - 旧的伪时间线式 `Medication records` 主视图
- **技术说明**：
  - 保持现有 `MedicineWorkspace` 和 Lucent schedule-only reminder / day-level dose log 合同不变。
  - 保持 Forui-first 组件风格，首页继续使用 `FCard`、`FButton`、`FAvatar`、`FBadge`、`FTappable` 等现成原语。
- **文件**：
  - `lib/features/medicine/presentation/widgets/views/mobile_dashboard_view.dart`
  - `lib/features/medicine/presentation/widgets/sections/mobile_drugbox_section.dart`
  - `lib/features/medicine/presentation/widgets/sections/mobile_records_section.dart`
  - `lib/features/medicine/presentation/widgets/sections/mobile_safety_section.dart`
  - `lib/features/medicine/presentation/widgets/sections/mobile_quick_operations_section.dart`
  - `lib/features/medicine/presentation/widgets/shared/mobile_shared.dart`
  - `test/medicine/page_test.dart`
- **验证**：
  - `flutter test test/medicine/page_test.dart`
  - `flutter analyze lib/features/medicine test/medicine/page_test.dart`

## Luminous — Lucent Phase 2 用药打卡合同同步

- **目标**：把 Lucent Phase 2 的用药打卡合同同步到 Luminous 生成客户端，先保证前后端接口一致，再由 UI 层接入。
- **合同变化**：
  - `MedicineDoseLogsApi` 新增 `POST /api/v1/user/medicine-dose-logs/mark` 对应的 `medicineDoseLogsControllerMarkV1()`。
  - `CreateDoseLogDto` 与 `DoseLogItemDto` 新增 `reminderId` / `scheduledTime`，用于区分同一天内不同提醒槽位。
  - 新增 `MarkDoseLogDto` 生成模型，供前端按提醒槽位执行幂等打卡。
- **影响**：
  - 现有 Medicine Phase 1 首页还能继续工作，因为旧的 `create/list/update/delete` 合同没有被拿掉。
  - 前端若要进入真正的 slot-aware 打卡流程，下一步应改由计划卡片调用 `mark` 接口，而不是继续只写 day-level dose log。
- **文件**：
  - `packages/lucent_openapi/lib/src/api/medicine_dose_logs_api.dart`
  - `packages/lucent_openapi/lib/src/model/create_dose_log_dto.dart`
  - `packages/lucent_openapi/lib/src/model/dose_log_item_dto.dart`
  - `packages/lucent_openapi/lib/src/model/mark_dose_log_dto.dart`
- **验证**：
  - `dart run tool/regenerate_lucent_openapi.dart`
  - `dart run tool/verify_lucent_openapi_sync.dart`

## Luminous — Medicine 根页接入 slot-aware 用药打卡

- **目标**：让 Medicine 根页真正消费 Lucent Phase 2 的 slot-aware dose-log 合同，而不是继续沿用“按药品 / 按天”粗粒度打卡。
- **实现**：
  - `DoseLogRemoteDataSource` 新增 `mark()`，直接调用 `POST /api/v1/user/medicine-dose-logs/mark`，请求体携带 `currentMedicineId`，并在有提醒槽位时携带 `reminderId` / `scheduledTime`。
  - `DoseLogItem` 与 `MedicineDoseSlot` 扩展了 `reminderId` / `scheduledTime`，让根页可以把当前 pending 槽位精确传回后端。
  - `LucentMedicineWorkspaceRepository` 改为按 reminder slot 组装今天计划和 hero 指标：同一种药多个提醒时，已完成一剂不会把剩余 pending 槽位一并视为完成。
  - Medicine 根页的“已服用 / 跳过”按钮现在只确认当前 pending slot；没有 reminder slot 的旧场景仍回退为 medicine-level 标记，保证过渡期可用。
- **文件**：
  - `lib/features/medicine/data/datasources/dose_log_remote_data_source.dart`
  - `lib/features/medicine/data/repositories/lucent_workspace_repository.dart`
  - `lib/features/medicine/domain/entities/workspace.dart`
  - `lib/features/medicine/presentation/pages/page.dart`
  - `lib/features/medicine/presentation/widgets/shared/workspace_parts.dart`
  - `lib/features/medicine/presentation/widgets/views/mobile_dashboard_view.dart`
  - `lib/features/medicine/presentation/widgets/sections/mobile_drugbox_section.dart`
  - `lib/features/medicine/presentation/widgets/sections/mobile_records_section.dart`
- **验证**：
  - `dart run build_runner build --delete-conflicting-outputs`
  - `flutter test test/medicine/`
  - `flutter test test/today/repository_test.dart`
  - `flutter analyze lib/features/medicine test/medicine test/today/repository_test.dart`

## Luminous — Medicine 首页对比度与搜索框高度修正

- **问题**：
  - `今日服用计划` 的时间 pill 和 `用药安全` 的 summary / 统计 badge 有低对比度文本，浅底上发白，看不清。
  - 顶部搜索框高度偏低，和首屏其他控件相比显得太扁。
- **修复**：
  - 今日服用计划里的蓝色圆角状态块根因是 `TintedStatusBadge` 只剩背景、文案视觉上被吞掉；该组件现改为直接用 `DecoratedBox` 控制浅底、边框和前景色，恢复状态文案显示。
  - 时间 pill 文本显式改为 `foreground` 深色，并加上中等字重，避免吃到 Forui 默认浅前景。
  - 用药安全 summary 标题在非 destructive 场景下改回主前景色；`未覆盖 / 不确定` 的左侧图标在 secondary 场景下显式改用可读前景色；统计 badge 文本显式改为深色前景。
  - 顶部搜索框不再只靠 padding 撑高，改为固定 `56px` 高度，扩大可点击区和视觉高度。
- **文件**：
  - `lib/features/medicine/presentation/widgets/sections/mobile_records_section.dart`
  - `lib/features/medicine/presentation/widgets/sections/mobile_safety_section.dart`
  - `lib/features/medicine/presentation/pages/page.dart`
  - `lib/features/medicine/presentation/widgets/shared/workspace_parts.dart`
- **验证**：
  - `flutter test test/medicine/page_test.dart`
  - `flutter analyze lib/features/medicine/presentation/pages/page.dart lib/features/medicine/presentation/widgets/shared/workspace_parts.dart lib/features/medicine/presentation/widgets/sections/mobile_safety_section.dart test/medicine/page_test.dart`

## Luminous — Record 根页职责收尾

- **目标**：按 `Product_Tab_Component_Blueprint` 继续把 Record 根页收紧为“事实写入 + 原始事实回看”，不引入日期范围模式。
- **调整**：
  - 移动端底部悬浮“自然语言”按钮已移除；自然语言录入统一由顶部输入条承接，语音 / OCR 继续作为输入条右侧辅助入口。
  - Record 首屏底部 `小贴士 / 查看记录指南` 辅助区块已移除，减少和 `快速记录 / 记录筛选 / 时间线` 主线竞争。
  - 移动端时间线不再静默只显示前 7 条；当日记录超过 7 条时，头部新增明确的 `查看全部记录 / 收起` 切换，仍停留在 Record 的原始记录语义内。
- **文件**：
  - `lib/features/record/presentation/pages/page.dart`
  - `lib/features/record/presentation/widgets/views/dashboard_view.dart`
  - `lib/features/record/presentation/widgets/sections/mobile_timeline.dart`
  - `lib/l10n/app_zh.arb`
  - `lib/l10n/app_en.arb`
  - `test/record/page_test.dart`
- **验证**：
  - `flutter gen-l10n`
  - `flutter test test/record/page_test.dart`
  - `flutter analyze lib/features/record/presentation/pages/page.dart lib/features/record/presentation/widgets/views/dashboard_view.dart lib/features/record/presentation/widgets/sections/mobile_timeline.dart test/record/page_test.dart`

## Luminous — Mine 根页通知与提醒区收尾

- **目标**：补齐 Mine 根页蓝图中缺失的轻量 `通知与提醒区`，只显示摘要和入口，不重复完整通知页内容。
- **实现**：
  - 新增 `MineNotificationsReminderSection`，挂到 Mine 根页移动端和桌面主列。
  - 分组三项固定入口：
    - `提醒设置`：摘要显示 `已开启 N 项 · 提前 X 分钟`
    - `免打扰`：摘要直接显示当前 DND 时段，未开启时显示 `未设置`
    - `通知收件箱`：摘要显示真实未读数；未登录时显示 `登录后查看`
  - 路由复用现有页面：`/settings/notifications`、`/settings/notifications/dnd`、`/notifications`
  - 收件箱入口保留登录门控；设置与免打扰继续作为本地偏好页可直接进入
- **文件**：
  - `lib/features/mine/presentation/widgets/sections/notifications_reminders.dart`
  - `lib/features/mine/presentation/widgets/views/dashboard_view.dart`
  - `lib/features/mine/presentation/widgets/shared/sections.dart`
  - `lib/l10n/app_zh.arb`
  - `lib/l10n/app_en.arb`
  - `test/mine/page_test.dart`
- **验证**：
  - `flutter gen-l10n`
  - `flutter test test/mine/page_test.dart`
  - `flutter analyze lib/features/mine/presentation/pages/page.dart lib/features/mine/presentation/widgets/views/dashboard_view.dart lib/features/mine/presentation/widgets/sections/notifications_reminders.dart test/mine/page_test.dart`

## Luminous — Mine 通知与提醒区图标颜色统一

- **问题**：`通知与提醒` 分组内三个前缀图标分别落在主色、浅色和深灰语义上，同一区块视觉不一致。
- **修复**：`提醒设置`、`免打扰`、`通知收件箱` 三个入口统一改为 `SoftIcon + AppColors.primary`，保持同一组入口的前景色和底色语义一致。
- **文件**：
  - `lib/features/mine/presentation/widgets/sections/notifications_reminders.dart`
  - `test/mine/page_test.dart`
- **验证**：
  - `flutter test test/mine/page_test.dart --plain-name "Mine notifications section uses a unified icon tone"`
  - `flutter test test/mine/page_test.dart`
  - `flutter analyze lib/features/mine/presentation/widgets/sections/notifications_reminders.dart test/mine/page_test.dart`

## Luminous — Mine / Report 蓝图收尾

- **目标**：把 Mine 和 Report 根页继续收紧到 `Product_Tab_Component_Blueprint` 的职责边界，不扩 Lucent 合同。

### Mine 根页

- **结构调整**：
  - 旧的 `账户与隐私` 混合分组已拆成 `AI 与隐私` 与 `账号与安全` 两个独立 section。
  - 移动端顺序调整为 `主卡 → 健康档案 → AI 与隐私 → 通知与提醒 → 账号与安全`。
  - 桌面端右侧已移除旧 `MineStatusOverview` 残留，改为承接两个新 section。
- **入口归位**：
  - `AI 设置` → `/settings/ai`
  - `报告分享` → `/settings`
  - `账号与安全` → `/account`
  - `安全 PIN 码` → `/settings/security-pin`

### Report 根页

- **结构调整**：
  - 顶部已移除旧的 snapshot 状态块，只保留标题、时间范围与操作按钮。
  - 桌面端首屏改为 readiness-first，不再额外插入独立数据不足横幅。
  - 新增 `历史建议回顾` section，移动端与桌面端都放在 `重点发现` 后。
- **数据来源约束**：
  - 当前历史建议回顾使用通知接口中的 `ai_proactive_suggestion` 作为真实数据源。
  - 当前只展示最近 3 条建议及 `已读 / 未读` 状态；不虚构 `已执行 / 已忽略 / 已延后`。

- **文件**：
  - `lib/features/mine/presentation/widgets/views/dashboard_view.dart`
  - `lib/features/mine/presentation/widgets/sections/ai_privacy.dart`
  - `lib/features/mine/presentation/widgets/sections/account_security.dart`
  - `lib/features/report/presentation/pages/page.dart`
  - `lib/features/report/presentation/widgets/views/dashboard_view.dart`
  - `lib/features/report/presentation/widgets/shared/top_bar.dart`
  - `lib/features/report/presentation/widgets/sections/suggestion_history_section.dart`
  - `lib/l10n/app_zh.arb`
  - `lib/l10n/app_en.arb`
  - `test/mine/page_test.dart`
  - `test/report/page_test.dart`

- **验证**：
  - `flutter gen-l10n`
  - `flutter test test/mine/page_test.dart`
  - `flutter test test/report/page_test.dart`
  - `flutter analyze`

## Luminous — 生成产物提交边界明确化

- 明确前端仓库的提交策略：
  - `build_runner` 生成的 `*.g.dart` / `*.freezed.dart`
  - `gen-l10n` 生成的 `lib/l10n/app_localizations*.dart`
  - 本地 path 依赖 OpenAPI 客户端 `packages/lucent_openapi/`
  - 上述产物都继续提交，因为 `flutter pub get` 不会自动运行 `build_runner`，而应用和测试直接依赖这些文件。
- `.gitattributes` 现已把这些路径标记为 generated，降低 GitHub diff / linguist 噪音。
- `.gitignore` 补充注释，明确这些前端生成源是有意保留在版本库中的，不应被误加到忽略规则里。

### 文件

- `.gitignore`
- `.gitattributes`
- `README.md`
- `docs/02-reference/OpenApi_Client.md`

### 验证

- `git check-attr linguist-generated -- lib/features/auth/domain/entities/session.g.dart`
- `git check-attr linguist-generated -- lib/l10n/app_localizations.dart`
- `git check-attr linguist-generated -- packages/lucent_openapi/lib/lucent_openapi.dart`
- `git check-ignore -v --no-index .dart_tool/package_config.json`
