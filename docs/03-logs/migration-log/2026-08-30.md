# 2026-08-30 迁移日志

## 审查报告修复 (Luminous-review-08-29)

基于 `plans/Luminous-review-08-29.md` 审查报告，修复以下问题：

### Critical

- **C-1 v5 数据库迁移防御性检查** (`lib/core/database/database.dart`):
  v5 迁移步骤的 `renameTable` 对 d607145 之后首次安装 v4 的用户会失败（表名已为
  `review_dashboard_cache_entries`）。改为先查询 `sqlite_master` 确认旧表名存在
  再执行 rename，避免 `no such table` 错误导致 App 启动崩溃。

### Warning

- **W-1 + S-4 SoftIcon 精简 cast + dead code 移除** (`lib/core/widgets/common/soft_icon.dart`):
  用 Dart 3 switch pattern 改写 if-else 链，消除冗余 `as` cast；`assert(false,...)`
  + fallback Icon dead code 改为 `throw StateError(...)`，release 模式下也能 fail loud。

- **W-2 connectivity_banner_test 弱断言删除** (`test/core/widgets/connectivity_banner_test.dart`):
  删除 `find.byType(ConnectivityBanner)` 和 `find.byType(SizedBox)` 两条永远通过
  的弱断言，测试名改为 "renders SizedBox.shrink when session is not timed out"。

- **W-3 OCR 进度回调 try/catch** (`lib/features/scan/domain/services/ocr_model_manager.dart`):
  config copy 步骤包裹 try/catch，失败时 log + rethrow，进度停留在 2/3 而非
  假信号 100%；成功路径注释更明确。

### Suggestion

- **S-1 `_reverseMap` dartdoc** (`lib/core/design/lucide_icon_bridge.dart`):
  补注释说明 lazy 初始化和 O(1) 稳态。

- **S-2 OCR CDN 决策：不迁移** (`lib/features/scan/domain/services/ocr_model_manager.dart`):
  明确 GitHub Releases 作为永久模型分发渠道，移除 `TODO(release-blocker)` 和
  strategy pattern follow-up。`pubspec.yaml` 注释同步更新。

- **S-3 connectivity_banner Material 依赖完整迁移** (`lib/core/widgets/common/connectivity_banner.dart`):
  `import 'package:flutter/material.dart'` → `import 'package:flutter/widgets.dart'`，
  彻底移除 Material 依赖。docstring 更新说明只使用 Flutter framework widgets +
  Forui `FButton`。

- **S-5 `_allSeriesEmpty` docstring** (`lib/features/review/presentation/widgets/sections/trend.dart`):
  补 docstring 说明 "empty" 严格定义为 `values.isEmpty`。

- **S-6 icon_picker_sheet widget 测试** (`test/core/widgets/icon_picker_sheet_test.dart`):
  新增 5 个 widget test：(1) 默认 5 分区 + 首图标可见 + ListView itemCount=5，
  (2) 搜索 "coffee" 只剩 Food 分区，(3) 搜索无匹配显示空状态，(4) 注入自定义
  categories 只渲染注入项，(5) 点击图标返回正确结果。`icon_picker_sheet.dart`
  的 `TODO(test)` 已移除。

- **S-7 reviewTrendCoverage nullable 兜底** (`trend.dart` + `review_zh.arb` + `review_en.arb`):
  新增 `reviewTrendCoveragePartial` l10n key（"已记录 {observed} 天" / "{observed} days
  recorded"），当 `expectedCount` 为 null 时走 partial 文案，不再隐藏整个覆盖标签。

- **S-8 pubspec.yaml 模型 manifest 注释** (`pubspec.yaml`):
  注释更新为 "permanent distribution channel"，移除 CDN TODO 引用。

### 文档更新

- `docs/02-reference/data-layer.md`: v5 schema 描述补充防御性 rename 说明。

### Git Hook 校验规则收紧

- **scope-empty 强制** (`scripts/tooling_workflows.dart`):
  `validateCommitMessage` 新增 `scope-empty` 规则，与 Lucent 的 commitlint
  `scope-empty: [2, 'never']` 对齐。无 scope 的提交（如 `docs: 整理文档`）
  现在会被 `commit-msg` hook 拦截。
- **header-max-length 对齐** (`scripts/tooling_workflows.dart`):
  header 长度上限从 200 收紧到 120，与 Lucent 的 commitlint 配置一致。

## 计划文件清理：已完成项删除

对照代码验证后，删除已完成或已过时的计划文件，更新仍活跃的计划：

- **删除 `plans/2026-08-01-ai-chat-redesign-plan.md`**：阶段 3（会话删除/重命名）已在代码中
  完整实现（`AssistantController.renameConversation/deleteConversation`、drawer 菜单、dialog），
  阶段 6（桌面 split-view）随 ADR-0012 冻结不再推进。
- **删除 `plans/2026-08-27-offline-first-and-report-to-review-rename.md`**：Phase 1（底线超时
  + cache-first）已落地（`sessionRestoreTimeout`、`_LoadingTimeoutWrapper`、`ReviewDao`/
  `ReviewDashboardDao` cache-first）；Phase 2（report→review 改名）已完成（`lib/features/
  report/` → `lib/features/review/`，路由 `Routes.review`，兼容映射 `'report' => Routes.review`）。
- **删除 `plans/2026-08-27-scaffold-unification-and-borderless-cards.md`**：方案已实施
  （`SurfaceTokens.scaffoldBackground` 灰底 `#FAFAFA`、`SurfaceTokens.containerBorder` 超淡边框、
  today/mine/review 页面已改 `SafeArea`、`card_style.dart` 边框收口）。
- **更新 `plans/2026-08-16-report-remediation-plan.md`**：R-3（Review 视图装配）和 R-4（legacy
  重装配 + 评分删除）已完成，仅保留 R-5（服务端 409 双保险）和 R-6（文档漂移更新）。
- **清理 `medicine/presentation/pages/page.dart`**：移除 error 分支的 `DecoratedBox(color:
  colors.background)` 残留包裹，改用 `SafeArea` 让 `FScaffold` 统一提供背景色；删除随之 unused
  的 `colors` 变量。
- **更新 `plans/README.md`**：移除已删计划引用和不存在的 error-handling 计划引用，更新 report
  计划描述。

## 设计系统统一与 Token 去代理化：Phase 1 RadiusTokens 退役

按 `plans/2026-08-30-design-system-unification-and-token-deriving.md` Phase 1 执行，圆角取用全面改走 Forui `FBorderRadius`（`context.theme.style.borderRadius.*`），随后删除 `RadiusTokens`。验证结论：`flutter analyze` 0 issue，design/widgets 定向测试与全量测试通过，`lib`/`test` 无 RadiusTokens 残留。

### refactor(core)：core 目录圆角取用改走 Forui FBorderRadius

- `lib/core/widgets/common/`（soft_icon、skeleton、sheet_drag_handle、page_state、icon_picker_sheet、desktop_window_chrome、desktop_hover）、`lib/core/widgets/command_palette/`：`BorderRadius.circular(RadiusTokens.levelN)` → `context.theme.style.borderRadius.xxx`；裸 double 用法 → `.xxx.topLeft.x`；`SkeletonText`/`InlineSkeletonBlock`/`StateSkeletonBlock` 的 const 默认参数改为可空 + build 内运行时兜底（xs2/md/lg）
- `lib/core/design/markdown_style.dart`：圆角改走 `borderRadius.sm/xs`；`colors.mutedForeground`/`colors.border` 同步改为 `SemanticColor.neutral.solid/border(context)`（字体与其余颜色留待后续 Phase）

### refactor(today)：today 圆角取用改走 Forui FBorderRadius

- `lib/features/today/presentation/`（views/skeleton_view、views/dashboard_view、shared/components、sections/suggestion_state_views、suggestion_primary_card、suggestion_interactive、suggestion、observation）：`RadiusTokens.*` 全部改走 `context.theme.style.borderRadius.*`；`TodayGlyphTile.radius` 默认参数改可空 + build 内兜底（sm）；skeleton 传参按默认值删除或改为 `.topLeft.x` 运行时解析

### refactor(record)：record 圆角取用改走 Forui FBorderRadius

- `lib/features/record/presentation/`（views/skeleton_view、shared/components、sections/timeline_item、timeline_drag_handler、summary_grid、quick_entry_panel、new_entry_panel、mobile_timeline、pages/edit、pages/detail、forms/image_attachment_field、dialogs/nlp_sheet、dialogs/meal_confirmation、meal/analysis_status_badge）：`RadiusTokens.*` 全部改走 `context.theme.style.borderRadius.*`；硬编码 `BorderRadius.circular(10)`→`md`、`(4)`→`xs2`；`const BorderRadius.vertical(top: Radius.circular(...))` 去 const 改运行时

### refactor(medicine)：medicine 圆角取用改走 Forui FBorderRadius

- `lib/features/medicine/presentation/`（views/skeleton_view、shared/workspace_parts、sections/mobile_safety、mobile_records、mobile_drugbox、risk/red_flag、risk/overview_tab_section、risk/finding_tile、pages/reminder/detail、pages/medicine_detail）：`RadiusTokens.*` 全部改走 `context.theme.style.borderRadius.*`

### refactor(review)：review 圆角取用改走 Forui FBorderRadius

- `lib/features/review/presentation/`（views/skeleton_view、views/review_view、sheets/share_management、sheets/more_actions、shared/reference_notice、shared/components、sections/trend、suggestion_history、review_suggestion_history、next_step、patterns、review_history、key_changes、event_header、findings、dialogs/suggestion_history_detail_sheet、range_picker_dialog、clinic_summary_preview_dialog）：`RadiusTokens.*` 全部改走 `context.theme.style.borderRadius.*`；底部弹窗 `BorderRadius.vertical(top: ...)` 圆角用 `.xxx.topLeft` 并去 const

### refactor(features)：其余 feature 圆角取用改走 Forui FBorderRadius

- mine（views/skeleton_view、pages/current_medicine_edit、sections/account_hero、sync_failed_details、sync_failed_banner）、settings（widgets/master_detail）、search（shared/results、medicine_add_precheck_dialog）、scan（dialogs/recognize_dialog、pages/barcode_scanner 硬编码 12→md）、auth（widgets/shared/shell、pages/account_settings_sections）、assistant（widgets/source_strip、shared/proposal_card、flowui_adapter、dialogs/capabilities_panel）、health_data（pages/health_sync）、notification（pages/detail、widgets/shared/list_item）：`RadiusTokens.*` 全部改走 `context.theme.style.borderRadius.*`

### refactor(design)：退役 RadiusTokens 圆角 token

- 删除 `lib/core/design/radius.dart` 与 `test/core/design/radius_tokens_test.dart`；从 `lib/core/design/design.dart` 移除 `export 'radius.dart'`
- `docs/02-reference/Design_System.md`：移除 RadiusTokens 相关活跃描述，命名段落补充 `.xxx.topLeft.x` / `.xxx.topLeft` 取法说明，front-matter updated 更新为 2026-08-30

## 设计系统统一与 Token 去代理化：Phase 2 TypographyToken 退役

按 `plans/2026-08-30-design-system-unification-and-token-deriving.md` Phase 2 执行，字体取用全面改走 Forui `FTypeface`（`context.theme.typography.body/display.*`），随后删除 `TypographyToken`。验证结论：`flutter analyze` 0 issue，design/widgets 定向测试通过，`lib`/`test` 无 TypographyToken 残留。

### refactor(core)：core 目录字体取用改走 Forui FTypeface

- `lib/core/widgets/common/`（dialog_shell、icon_picker_sheet、page_state、sensitive_action_password_dialog、state_message）、`lib/core/widgets/command_palette/`、`lib/core/design/markdown_style.dart`：`TypographyToken.levelN.body(context)` → `context.theme.typography.body.xxx`（level1→xs3、level2→xs2、level3→xs、level4→sm、level5→md、level6→lg、level7→xl、level8→xl2、level9→xl3、level10→xl4）；markdown_style 两套预置（legal/ai）标题阶梯与正文同步改用 FTypeface 命名

### refactor(today)：today 字体取用改走 Forui FTypeface

- `lib/features/today/presentation/`（sections/observation、suggestion、suggestion_interactive、suggestion_primary_card、suggestion_state_views、summary、shared/section、views/dashboard_view、views/skeleton_view）：`TypographyToken.*` 全部改走 `context.theme.typography.body/display.*`

### refactor(record)：record 字体取用改走 Forui FTypeface

- `lib/features/record/`（application/usecases/quick_entry_medication、quick_entry_sleep；presentation 下 pages、widgets/dialogs、forms、meal、nlp、sections 共 26 个文件）：`TypographyToken.*` 全部改走 `context.theme.typography.body/display.*`（application 层经 dialogContext/context 解析）

### refactor(medicine)：medicine 字体取用改走 Forui FTypeface

- `lib/features/medicine/presentation/`（pages/medicine_detail、page、pages/reminder/detail、widgets/reminder、widgets/risk、widgets/sections、widgets/shared 共 21 个文件）：`TypographyToken.*` 全部改走 `context.theme.typography.body/display.*`

### refactor(review)：review 字体取用改走 Forui FTypeface

- `lib/features/review/presentation/`（widgets/dialogs、sections、shared、sheets、views 共 27 个文件）：`TypographyToken.*` 全部改走 `context.theme.typography.body/display.*`

### refactor(features)：其余 feature 字体取用改走 Forui FTypeface

- mine（9）、settings（14）、search（5）、scan（1）、auth（10）、assistant（10）、health_data（1）、health_event（3）、legal（2）、notification（3）：`TypographyToken.*` 全部改走 `context.theme.typography.body/display.*`；`completed_actions.dart` 补 forui import，`mine/shared/shared.dart`、`settings/sign_out_tile.dart` 删除不再使用的 design barrel import

### refactor(design)：退役 TypographyToken 字体 token

- 删除 `lib/core/design/typography.dart`；从 `lib/core/design/design.dart` 移除 `export 'typography.dart'` 及注释行
- `test/core/design/markdown_style_test.dart`：断言改走 `context.theme.typography.body.xs/xs2`，注释同步 Forui 命名
- `docs/02-reference/Design_System.md`：移除 TypographyToken 活跃描述，Markdown 渲染段落改 Forui 字号命名（sm/lg/md/xs/xs2 阶梯）

## 设计系统统一与 Token 去代理化：Phase 3 颜色取用统一

按 `plans/2026-08-30-design-system-unification-and-token-deriving.md` Phase 3 执行，widget 层语义色取用统一走 `SemanticColor.*`（mutedForeground→neutral.solid、primary→primary.solid、destructive→destructive.solid、primaryForeground→primary.foreground、border→neutral.border，均已在 `_semanticColorsFor` 确认值等价）。验证结论：`flutter analyze` 0 issue，design/widgets 定向测试通过，`lib` 下目标属性残留为 0（仅保留分层职责豁免：theme 定义层、Material 桥接层、background/card/foreground/secondary/error 基础面色）。

### refactor(core)：core 语义色取用统一走 SemanticColor

- `lib/core/widgets/**` 与 `lib/core/design/markdown_style.dart`：`context.theme.colors.*` / 局部变量 `colors.*` 的 mutedForeground/primary/destructive/primaryForeground/border 全部改走 `SemanticColor.*.xxx(context)`；清理随之 unused 的 `final colors = ...` 声明
- `docs/02-reference/Design_System.md`：颜色区域新增「取色分层职责」说明（background/card/foreground 走 `context.theme.colors.*`/`SurfaceTokens.*` 是合理分层）；Markdown 渲染段散文同步语义色命名

### refactor(today)：today/shell 语义色取用统一走 SemanticColor

- `lib/features/today/presentation/**` 与 `lib/features/shell/presentation/page.dart`：同上迁移

### refactor(record)：record 语义色取用统一走 SemanticColor

- `lib/features/record/presentation/**`：同上迁移；`_trailingColor` 等 FColors 参数场景用 `paletteFromColors(colors).tone`

### refactor(medicine)：medicine 语义色取用统一走 SemanticColor

- `lib/features/medicine/presentation/**`：同上迁移

### refactor(review)：review 语义色取用统一走 SemanticColor

- `lib/features/review/presentation/**`：同上迁移

### refactor(features)：其余 feature 语义色取用统一走 SemanticColor

- mine、settings、search、scan、auth、assistant、health_data、health_event、legal、notification：同上迁移；`proposalStateColor` 等 FColors 参数场景用 `paletteFromColors(colors).tone`；`flow_theme_bridge.dart` 的 Material ColorScheme 桥接（onPrimary）与 `lib/theme/styles/button_styles.dart` 按分层职责保留

## 设计系统统一与 Token 去代理化：Phase 4 Spacing/IconSizeTokens 语义别名

按 `plans/2026-08-30-design-system-unification-and-token-deriving.md` Phase 4 执行：`Spacing` 与 `IconSizeTokens` 增加语义别名（xs/sm/md/lg/xl/xl2…）为主命名，`level*` 保留为向后兼容别名；不改任何调用点（旧代码自然接触时渐进迁移）。验证结论：`flutter analyze` 0 issue，design 定向测试通过，调用点数量前后一致（零调用点变更）。

### refactor(design)：Spacing/IconSizeTokens 增加语义别名

- `lib/core/design/spacing.dart`：xs=4…xl8=128 语义别名，level1-12 改为别名指向
- `lib/core/design/icon_size.dart`：xs=12…xl4=64 语义别名，level1-8 改为别名指向（use-case 注释保留）
- 测试：`spacing_tokens_test.dart` 追加别名等价/递增断言；新增 `icon_size_tokens_test.dart` 覆盖 8 级别名
- `docs/02-reference/Design_System.md`：命名段落更新为「语义别名为主命名、level* 为兼容别名」

## 设计系统统一与 Token 去代理化：Phase 5 颜色硬编码审计清理

按 `plans/2026-08-30-design-system-unification-and-token-deriving.md` Phase 5 执行。审计结论：58 处 `Color(0x...)` 中 52 处属合法硬编码（主题定义层、OAuth 品牌色、主题族预览示意色、扫码硬件覆盖层），6 处全透明 `Color(0x00000000)` 语义化为 `Colors.transparent`；5 处 `Colors.black/white/black54`（侧面板/命令面板系统级遮罩、品牌圆底白色图标、Material 桥接 error 前景）按例外保留并补注释。验证结论：`flutter analyze` 0 issue，定向测试通过。

### refactor(design)：颜色硬编码审计清理

- `Color(0x00000000)` → `Colors.transparent`（router/helpers、record/sidebar、mine/skeleton_view ×4）
- 合法硬编码补说明注释（settings/theme 预览色板、barcode_scanner 相机覆盖层、oauth_panels 白色图标、router/helpers 与 command_palette 遮罩、flow_theme_bridge error 前景）

## 设计系统统一与 Token 去代理化：Phase 6 文档同步

按 `plans/2026-08-30-design-system-unification-and-token-deriving.md` Phase 6 执行（该计划完成后按惯例删除，见后续提交）。验证结论：`flutter analyze` 0 issue；`check_doc_coverage --warning-only` 通过；`check_doc_links` 通过；`--verify` 的 review 规则缺口（doc-map.yaml 缺 `lib/features/review/**` 规则）经确认为 HEAD 既有问题，非本次引入。

### docs(design)：同步设计 token 文档引用

- `docs/Glossary.md`：删除 RadiusTokens/TypographyToken 词条；SemanticColor 词条 5 tone→10 tone；Spacing 词条改语义别名主命名
- `docs/00-current/Runtime_Snapshot.md`：Token 清单同步现状（Spacing/IconSizeTokens 语义别名、圆角/字体取 Forui 原生）；palette tone 列表修正为 10 tone；front-matter 更新
- `docs/00-current/Active_UI_{Today,Record,Medicine,Mine_Settings}.md`：已退役 token 引用改 Forui 命名（body.xs/xs2/xs3/sm/lg、borderRadius.xs/sm），数值语义不变
- `docs/02-reference/architecture.md`：layout vocabulary 描述改 Spacing/IconSizeTokens + Forui 原生 scale
- `docs/02-reference/Forui_Reference.md`：字体/圆角/颜色示例改 `context.theme.typography.*` / `context.theme.style.borderRadius.*` / `SemanticColor.*`；间距/图标条目改语义别名命名并修正 IconSizeTokens 数值
- `docs/02-reference/Design_System_Components.md`、`docs/02-reference/how-to/add-new-feature.md`：已退役 token 引用同步

### docs(agents)：移除已退役设计 token 引用

- `AGENTS.md`/`CLAUDE.md` 的 Design System 段落：`RadiusTokens`/`TypographyToken` 移除，改为 `SemanticColor`/`Spacing`/`IconSizeTokens` + Forui 原生圆角/字体取法说明

### refactor(design)：删除退役残留的 typography.dart

- 补充删除 `lib/core/design/typography.dart`（T2 退役提交只移除了 barrel export，文件本体漏删；全仓库零引用，`rg "TypographyToken" lib test` 清零）。`flutter analyze` 0 issue，design/widgets 定向测试通过

### docs(plans)：删除已完成的 Token 去代理化计划

- 删除 `plans/2026-08-30-design-system-unification-and-token-deriving.md`（六 Phase 全部完成，按 AGENTS.md「Finishing a plan」惯例删除）；`plans/README.md` 移除对应引用

## 回归修复：proposalStateColor 测试注入语义色扩展

- Phase 3 后 `proposalStateColor` 经 `SemanticColor.paletteFromColors(colors)` 取色（依赖 FColors 注入的 SemanticColors 扩展）；`test/assistant/ui_formatters_test.dart` 原用裸 `FTheme.neutral.light.touch.colors`（未注入）导致 4 个用例抛 StateError
- 修复：测试 colors 改用 `appThemeData(appDefaultThemeFamily, Brightness.light).colors`（应用真实主题，注入扩展），断言与实现同源相等、语义不变；移除不再使用的 forui import
- 验证结论：`flutter test test/assistant` 全绿；全量 `flutter test` 通过；`flutter analyze` 0 issue

## 可读性重构：主题取法按密度提取局部变量

设计系统迁移后 widget 代码大量内联 `context.theme.style.borderRadius.*` / `context.theme.typography.*` / `context.theme.colors.*`。按既有惯例（21 文件已提 `final typography`、83 文件已提 `final colors`）推广：方法内同一来源 ≥2 次时在方法顶部提取 `final borderRadius` / `final typography` / `final colors`，单次使用保持内联；`SemanticColor.X.tone(context)` 与自带 context 的内嵌闭包内取法一律不动。纯局部变量提取，值/行为零变化（词元级校验 + analyze 0 issue + 全量测试通过）。

### refactor(core)：core 主题取法提取局部变量

- `lib/core/design/markdown_style.dart`（legal 提 typography、ai 提 typography+borderRadius）、`lib/core/widgets/command_palette`、`connectivity_banner`、`sensitive_action_password_dialog`、`state_message`

### refactor(today)：today 主题取法提取局部变量

- `lib/features/today/presentation/` 11 个文件（sections/observation、suggestion、suggestion_interactive、suggestion_primary_card、suggestion_state_views、summary、shared/card_style、shared/components、shared/section、views/dashboard_view、views/skeleton_view）

### refactor(record)：record 主题取法提取局部变量

- `lib/features/record/presentation/` 19 个文件（pages/create、pages/detail、pages/quick_entry_settings、dialogs/nlp_sheet、quick_type_settings_dialog、sleep_merge、water_custom_amount_dialog、forms/image_attachment_field、meal/analysis_summary_card、meal/dish_editor、nlp/candidate_review、sections/mobile_filter、mobile_timeline、new_entry_panel、sidebar、summary_grid、timeline、timeline_item、views/skeleton_view）

### refactor(medicine)：medicine 主题取法提取局部变量

- `lib/features/medicine/presentation/` 12 个文件（pages/medicine_detail、pages/reminder/detail、widgets/reminder/log_panels、widgets/risk/coverage_issue_tile、finding_tile、metric_chip、overview_tab_section、red_flag、widgets/sections/mobile_drugbox、mobile_quick_operations、mobile_records、mobile_safety）

### refactor(review)：review 主题取法提取局部变量

- `lib/features/review/presentation/` 21 个文件（dialogs/clinic_summary_preview_dialog、suggestion_history_detail_sheet、sections/ai_summary、completed_actions、event_header、export、findings、key_changes、next_step、patterns、readiness、review_ai_summary、review_history、review_preview_locked、review_suggestion_history、suggestion_history、trend、shared/components、sheets/more_actions、share_management、views/review_view）

### refactor(features)：其余 feature 主题取法提取局部变量

- mine（6）、settings（6）、search（3）、scan（1）、auth（6）、assistant（5）、health_data（1）、health_event（3）、notification（2）

### docs(design)：记录主题取法约定

- `docs/02-reference/Design_System.md` 颜色段落追加「主题取法约定（2026-08-30 起）」：同方法同源 ≥2 次提取局部变量（borderRadius/typography/colors）、单次内联；`SemanticColor.*.tone(context)` 保持规范形态；自带 context 的内嵌闭包直接取用

## 模块边界卫生审计：Phase 1 required_dialog 提取到 core

按 `plans/2026-08-30-module-boundary-hygiene-audit.md` Phase 1 执行（用户已确认 Phase 2 测试归位跳过，见收尾报告）：跨切面 auth 路由检查工具从 auth feature 提取到 core，消除 41 处对 `features/auth/.../required_dialog.dart` 的跨 feature 深引用。验证结论：`flutter analyze` 0 issue，定向与全量测试通过，旧路径 lib/test 零残留。

### refactor(core)：auth 路由检查工具提取到 core

- 新建 `lib/core/router/routes.dart`：`CoreRoutes.login`/`CoreRoutes.home` 常量 + `loginRouteLocation()`（与 auth feature `LoginRoute` typed route 保持同步，drift 由测试防漂移）
- `lib/core/widgets/auth/required_dialog.dart` 追加 `pushAuthRequiredRoute`/`loginRouteForReturnTo`/`loginRouteForCurrentLocation`（逻辑逐行等价搬移，core 不再依赖 app/feature 路由）
- 新建 `test/core/router/routes_test.dart`：`loginRouteLocation` 与 `LoginRoute(returnTo:).location` 解码后等价对照（含空串/空格/中文/元字符用例）

### refactor(features)：调用点改走 core required_dialog

- 41 个 lib 文件 + 2 个测试文件：`import 'package:luminous/features/auth/presentation/widgets/shared/required_dialog.dart'` → `package:luminous/core/widgets/auth/required_dialog.dart`（含 import 排序修正）

### refactor(auth)：删除已迁移的 required_dialog

- 删除 `lib/features/auth/presentation/widgets/shared/required_dialog.dart`（符号已全部迁入 core，无残留引用）
- `plans/2026-08-30-module-boundary-hygiene-audit.md`：按惯例删除已完成的 Phase 1 小节（含 1.1-1.6 复选框）

## 模块边界卫生审计：Phase 3 模块 README 补充

按 `plans/2026-08-30-module-boundary-hygiene-audit.md` Phase 3 执行，为 3 个核心模块补 README（面向代码阅读者，与 docs 架构文档互补）。验证结论：`flutter analyze` 0 issue，doc-coverage warning-only 无异常。

### docs(design)：补充模块 README

- 新建 `lib/core/design/README.md`：design 层定位、17 文件清单与 barrel 导出、token 体系（Spacing/IconSizeTokens 语义别名、SemanticColor 6 色 × 10 tone）、圆角/字体取法约定、与 Design_System.md 分工
- 新建 `lib/features/health_context/README.md`：snapshot 数据模型树、provider 依赖图（keepAlive + DataChangeTopic + authGuarded）、被 7 个 feature 依赖的原因与清单（today/medicine/mine/record/review/scan/search——比计划审计快照的 6 个多 scan，以代码为准）
- 新建 `lib/features/today/README.md`：dashboard 聚合模式（_buildDashboard 多路合并 + section 降级）、数据 provider 机制、跨 feature 出边/入边依赖
- `lib/core/design/semantic_color.dart`：doc 注释 5 pre-computed tones → 10（与 palette 实际字段一致，随 README 同步修正）

## 模块边界卫生审计：收尾

- 计划三阶段处理完毕（Phase 1 完成、Phase 2 经协调者确认跳过——测试归位到 lib/features/*/tests/ 会破坏 `flutter test` 默认发现机制且无配置化路径，需改造 CI/脚本并改写 131 个相对导入，偏离 Flutter 生态惯例；保持 test/ 镜像结构，记录为按计划评估后的延后项；Phase 3 完成）
- 按惯例删除 `plans/2026-08-30-module-boundary-hygiene-audit.md` 并更新 `plans/README.md` 引用

## 文档检查修复：doc-map review 规则同步 + 过时 report 引用更新

运行 `check_doc_coverage --warning-only` 与 `--verify` 后修复：

- `docs/doc-map.yaml`：review feature 改名（2026-08-27 report → review）后规则 `code` glob 仍指向
  `lib/features/report/**`（设计系统 Phase 6 验证时确认为 HEAD 既有缺口），同步为 `lib/features/review/**`
  并更新规则注释。
- `docs/00-current/Project_Governance.md`：产品表面段落更新为改名已完成迁移（tab 枚举 `today / record /
  medicine / review / mine`，`/report` 保留兼容路由）；文档治理段落补充「feature 规则须随目录改名同步」
  与治理演进计划索引；front-matter updated 同步。
- `AGENTS.md` / `CLAUDE.md`：Routing 段落 Tab roots 与五 tab 枚举 `/report` → `/review`
  （以 `Routes.review = '/review'` 为准）。
- `docs/01-product/Product_Information_Architecture.md`：Tab 枚举同步 review 命名，front-matter updated 同步。
- 新建 `plans/2026-08-30-doc-governance-evolution.md`（文档治理从变更门禁演进到结果合规 + 单一事实源，
  分三阶段：止误报、派生文档生成化、实时规则与叙事文档降级）；`plans/README.md` 索引追加。

### 验证结论

- `check_doc_coverage --warning-only`：通过（docs-tooling 规则的迁移日志义务由本条满足）。
- `check_doc_coverage --verify`：通过。

## 文档治理计划重写（v2）

- 将 `plans/2026-08-30-doc-governance-evolution.md` 重写为 v2：经业界实践对照评审（Diátaxis 四分法、生成式参考文档、Rust doctest、Conventional Commits 自动 CHANGELOG），确认根本问题是文档面从未做存在性审计——同步机制建设（v1 主体）降级为 Phase 0 之后的工作。
- 主要变更：新增 Phase 0 文档面审计（四问法 + `Active_UI_*` 裁决 + front-matter 精简）；Phase 1 重排（纯减法豁免先行、新增正文路径校验、规则草案生成降级为可选）；Phase 2 修订（generated region 标记先行、路由生成器源改 `lib/app/router.g.dart`）；Phase 3 新增 Lucent 对齐项。
- 已确认决策：迁移日志保留必写义务（回溯价值）；front-matter 保留时间字段，`quadrant` 等导航字段进入审计评估；冻结手写「现状叙事」文档新增；`plans/README.md` 索引摘要同步。

## 文档治理 v2 增补：docs 目标目录结构与范围澄清

- 目标结构写入 plan（Phase 0「目标结构」小节）：docs 去编号、按 Diátaxis 语义重建（explanation / product / reference（含 adr、generated）/ howto / logs / archive + `README.md` 唯一索引，替代 `_MOC.md` 与 `Current_State.md` 索引职能）；设计不参考 Lucent 现有形态，Lucent 治理另行立项、不在本计划范围。
- 现状叙事裁决范围修正为 00-current 内 7 篇（`Active_Mobile_UI`、`Active_UI_*` 5 篇、`Desktop_UI`），`Lucent_Contract_Snapshot`、`Runtime_Snapshot` 等快照类一并入审；`Next_Plan.md`、`TODO.md` 与 `plans/` 职能重叠，评估归并。
- 检查器硬编码已盘点（readership/freshness 的 `03-logs/`、`04-archive/` 前缀豁免与文案等 4 处），目录重建顺序固定为先改检查器、再 `git mv` 平移、后删编号目录壳。

## feature README 策略澄清

- 结论：**README 默认不建、自愿创建**。模块职责文档由 library dartdoc 承担（IDE 悬停、同 diff 原子、永不漂移）；README 只写机器表达不了的内容（非显然设计约定、为什么、边界声明），禁止目录/字段/依赖清单转述与状态叙事。
- 依据：存量 `today`、`health_context` 两篇 README 约 6 成内容为代码投影（目录树、实体字段树、依赖清单），必漂移（review 改名后 today README 出现「旧名 report」人肉修正即为实例）；跨 feature 依赖图改由 Phase 2.4 生成器输出。plan 3.2 表述已同步修正（原「缺 README → IDE 提示」与本策略矛盾），B 支柱改为「library dartdoc 优先，README 为自愿例外」。

## feature README 策略修正（agent 开发工作流）

- 用户澄清工作流为 **agent 开发、人负责 review**，推翻「默认不建」的成本前提：README 同步义务由 agent 在 feature 开发同一 commit 内承担（成本趋近于零），而其价值升高——README 是 reviewer 的上下文重建入口与 agent 新会话的恢复入口。
- 修正结论：**每 feature 一篇 README（标配）**，内容红线不变——只写心智模型（职责一段 / 非显然设计约定 / 为什么 / 边界声明，≤~30 行），禁止目录/字段/依赖清单投影与状态叙事（投影与谁写无关、依然漂移，依赖图归 Phase 2.4 生成器）；API 级文档仍由 library dartdoc 承担。
- plan 同步修正：B 支柱、3.2 重写；Phase 0 新增 0.7（存量 2 篇瘦身 + 缺失 15 篇补齐），0.8 验收含「17 个 feature 全部有合规 README」。

## 批次 1a — daos 与 utils 文件去冗余类型后缀

目录名已表达类型，文件名去 `_dao` / `_utils` 后缀；仅文件名与 import/part 路径变更，类名与行为零变化。

### refactor(core)：daos 与 utils 文件去冗余类型后缀

- `lib/core/database/daos/` 8 个源文件去 `_dao` 后缀（current_medicine、daily_record、health_context、medicine_dose_log、pending_sync、review、review_dashboard、today_suggestion），`.g.dart` 生成物同步重命名，源文件 `part` 声明更新，build_runner 重新生成对齐
- `lib/core/utils/` 3 个文件去 `_utils` 后缀（date_format、string、type_conversion）
- test 镜像同步：`test/core/utils/` 三个测试文件同步去后缀；`dao_test.dart`、`dao_extended_test.dart` 与全部 lib/test 引用方仅更新 import 路径（含 `database.dart`、`sync/worker.dart` 的相对 import 与 `.g.dart` 的 `part of` 行）
- 验证结论：`flutter analyze` 0 issue；全量 `flutter test` 通过

## 批次 1b — core 目录名重复与纯类型词文件重命名

文件名与目录名重复或为纯类型词，按职责重命名；类名与行为零变化。候选名均与文件实际职责相符（logger→LogLevel 枚举 + Talker provider；theme→AppThemeFamily + 主题数据；shortcuts→AppShortcuts 装配；icon_tokens→SemanticIcons 注册表），无需调整。

### refactor(core)：目录名重复与纯类型词文件重命名

- `lib/core/logger/logger.dart` → `log_level.dart`（含 `logger.g.dart` → `log_level.g.dart` 生成物跟随与 `part` 声明更新，build_runner 重新生成对齐）
- `lib/core/theme/theme.dart` → `family.dart`；`lib/core/shortcuts/shortcuts.dart` → `bindings.dart`；`lib/core/design/icon_tokens.dart` → `semantic_icons.dart`（`design.dart` barrel export 同步）；`lib/app/router/helpers.dart` → `lib/app/router_helpers.dart`（删除空目录 router/）；`lib/core/widgets/command_palette/command_palette.dart` → `lib/core/widgets/common/command_palette.dart`（删除空目录 command_palette/）
- 全库 import 路径替换；改名后 directives_ordering 失序的 9 个文件修正 import/export 排序；`surface.dart` doc 注释中的路径文字同步
- 活文档路径文字同步：`docs/00-current/Runtime_Snapshot.md`、`docs/02-reference/{Design_System,Forui_Reference,architecture,Design_System_Migration}.md`、`lib/core/design/README.md`（迁移日志与历史 plans/research 为不可变记录，不回改）
- 验证结论：`flutter analyze` 0 issue；全量 `flutter test` 通过
