# 2026-08-22

## Assistant 日期与消息标识

将助手短日期格式提取为共享 helper，并将 message ID 实现移到独立工具，FlowUI adapter、模型测试和 formatter 测试统一复用该实现。未改变显示格式或 API 消费方式。

验证：formatter、模型和 FlowUI 相关测试通过。

## Assistant 来源条详情安全渲染

将来源条工具详情的可选字段改为局部值提升，移除 `detail!`、`disclaimer!` 和标签字段的非空断言；缺少详情时继续显示既有占位状态，完整字段时保持原有展示。

验证：assistant source strip widget 测试通过。

## Assistant 会话重命名并发保护

同一会话存在重命名请求时直接忽略后续调用，以首个请求为准；后端刷新失败保留乐观标题并记录 debug 日志。新增回归断言，覆盖后端调用次数和本地标题不被后续请求覆盖。

验证：controller conversation 测试通过。

## Record Toast 生命周期守卫

为 NLP sheet 的错误监听/空输入提示以及 record create page 的睡眠值校验 Toast 增加 `context.mounted` 检查，避免页面销毁后访问失效上下文。

验证：record create page 测试通过；相关 page 测试仍受既有 Flutter 语义树断言影响。

## Assistant 遗留组件与控制器测试清理

删除未接入 shipping path 的 controls/status 实验组件及已完成的 8-18 整改计划；将 controller 测试按 send、conversation、regenerate seam 拆分，保留原有行为断言。8-22 审查报告仅作为本次输入，结论已沉淀在本迁移日志中，现一并删除。

验证：assistant controller 拆分测试通过；`flutter analyze` 无问题。

## 响应契约硬切：Luminous 错误基础设施

确认 0.1.0 前功能计划工作已完成，保留的 0.1.0 后工作不再阻塞响应契约硬切；冻结新功能并在当前分支进入集中迁移窗口。新增严格的 RFC 9457 Problem Details 解析、`LucentFailure` 与直接使用 fpdart `Either` / `TaskEither` 的 repository 边界；解析器不识别旧 `{ code, message, data }` envelope，也不保留 `status`、`statusCode` 或 `requestId` 输出。重试策略收紧为允许状态 + GET 默认幂等边界，写请求必须显式开启并带 `Idempotency-Key`，`retryable: false` 只负责否决，`Retry-After` 只负责等待时间。

本次仅落地客户端目标态基础设施，生成客户端、旧错误 mapper、repository/provider 调用链和 Lucent HTTP 响应尚未切换；没有加入旧/新响应格式的运行时兼容分支。

验证：目标错误基础设施与重试测试通过；`flutter analyze` 通过；全量 `flutter test` 在既有 widget semantics/Toast/Drift 测试问题上未通过并已停止，未将这些无关失败混入本次修复。

## 响应契约硬切：Luminous HTTP 错误链切换

将 `ErrorInterceptor`、`LucentErrorMapper` 和 `AuthInterceptor` 切换到严格的
`ProblemDetails -> LucentFailure` 链路。HTTP 错误必须是 `application/problem+json`；旧成功
envelope 不再由 `EnvelopeInterceptor` 解析或参与 Dio 链。401 refresh 改为读取 Problem Details
中的稳定字符串 code，refresh 成功响应按目标态直接读取 token resource；非刷新认证错误清除会话，网络/超时/5xx 保留会话。

保留 `LucentErrorMapper.toAppError()` 作为 repository/provider 完成 Result 迁移前的临时内部投影；它不恢复旧 HTTP 响应解析。生成客户端仍等待 Lucent 导出新 OpenAPI 后同步，当前尚未切换成功资源 DTO。

验证：core/network、core/errors、目标错误链与目标 refresh 测试通过；`flutter analyze` 通过。

## 响应契约硬切：重生成直接资源客户端

Lucent 完成全部 controller 成功响应、Problem Details 错误响应和 OpenAPI 直接资源 schema 的硬切后，重新运行 `dart run scripts/bootstrap_generated_sources.dart`。生成客户端模型与 API 方法同步为直接资源、数组、nullable 读取和 `oneOf` 联合响应；不保留旧成功 envelope 解包路径。

验证：OpenAPI 生成器严格校验通过，bootstrap 完成 pub get、生成代码、build_runner 和 Dart 格式化；生成结果仅涉及 `generated/lucent_api/`。

## 响应契约硬切：Luminous 消费端收口

将剩余 raw Dio、会话数组、健康事件写入、扫描上传/识别和测试 fixture 切换到直接资源；`204` 操作只接受无 body 成功。认证 provider 测试错误样例同步为 `application/problem+json` 的 Problem Details，删除旧数字错误 envelope 的业务断言。未增加旧/新响应格式 fallback。

验证：`dart analyze lib`、`dart analyze test` 无问题；auth、session、security elevation、health event、notification、medicine、report、scan 等定向测试通过。全量 `flutter test` 仍受既有 Flutter semantics/Toast/Drift 测试环境问题阻塞，不能作为本次全绿结论。

## 错误契约收尾：SSE Problem Details

将 Assistant、Today Analysis、Report AI 的 SSE `error` 事件及服务关闭通知切换为严格 Problem Details；流事件额外使用 `status` 表示 `client_error`、`server_error`、`cancelled`、`server_shutdown` 或 `unknown`，不再发送 `statusCode`、数字错误码或裸 `message`。Luminous 新增严格 SSE 解析并将事件映射为 `LucentFailure`，畸形事件直接作为协议错误拒绝；同步更新 SSE DTO 描述与客户端测试。

验证：Lucent SSE mapper、registry、三个 controller 定向测试通过；Luminous Problem Details、Assistant、Today、Report SSE 定向测试通过，Dart 分析通过。
