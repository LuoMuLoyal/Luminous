# Luminous 待办事项

最后更新: 2026-05-29（基于 UI_Implementation_Plan.md 清理已完成/已覆盖项）

本文档汇总五轮代码审查中已确认但暂不修复的问题，以及测试覆盖缺口。按类别和优先级排序。

---

## 国际化

| #   | 问题                                                                      | 位置         | 影响                                 | 建议                                                                         |
| --- | ------------------------------------------------------------------------- | ------------ | ------------------------------------ | ---------------------------------------------------------------------------- |
| 1   | `medicine_ai_card.dart` 中文 section headers 和 `endsWith('建议')` 硬编码 | drug feature | 非中文 locale 下 AI 解析逻辑完全失效 | AI 返回结构化 JSON 替代自由文本，或做 locale-aware 解析；短期可先加 fallback |

---

## 架构

| #   | 问题                                                            | 位置                                | 影响                                                             | 建议                                                                                                                                                                               |
| --- | --------------------------------------------------------------- | ----------------------------------- | ---------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2   | 4 个 `AsyncNotifierProvider` 返回裸 `List<T>`，无 State wrapper | reminders / checkin / search / home | 与其余 12 个 provider 风格不一致，扩展时需额外处理 loading/error | 统一加 `XxxState` wrapper（含 `items`、`isLoading`、`errorMessage`）；注：home/medication 模块将在 UI 重构中重写，此问题在该部分自动解决，剩余 reminders/checkin/search 需手动迁移 |
| 3   | `BASE_URL` 默认值 `https://devluo.com` 硬编码                   | `lib/core/network/`                 | 生产地址泄露到源码，且不可通过环境切换                           | 通过 `--dart-define` 注入，或从 `.env` / CI/CD 变量读取；源码默认值改为空或 localhost                                                                                              |

---

## 测试覆盖缺口

以下 feature 完全无专属测试或缺少关键 provider test：

| #   | Feature  | 缺失                          | 优先级              |
| --- | -------- | ----------------------------- | ------------------- |
| 4   | `drug/`  | 完全无专属测试                | 高 — 药品是核心功能 |
| 5   | `login/` | 无 `login_provider_test.dart` | 高 — 认证是关键路径 |
| 6   | `album/` | 无 `album_provider_test.dart` | 中                  |
| 7   | `legal/` | 完全无专属测试                | 低 — 纯静态页面     |
