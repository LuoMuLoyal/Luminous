## Proactive Suggestion Runtime — Task 4 Flutter contract sync

- Lucent Task 4 已将 Today suggestion GET 改为只读物化结果，并在 OpenAPI 合同中增加
  `materializationStatus`、`sourceVersion`、`computedAt`、`retryAfterSeconds`。
- Luminous 依据 `Lucent/docs/openapi.json` 使用 `openapi-generator-cli` 7.22.0 的 `dart-dio`
  生成 `TodaySuggestionsDataDto`；本轮 OpenAPI diff 仅涉及该 DTO，因此定向再生该模型，随后由
  generated package 的 `build_runner` 生成 `.g.dart`。未手写生成客户端。
- 生成模型现在保留 `empty`、`pending`、`ready`、`stale`、`failed` 五种物化状态，以及最新来源版本、
  最近成功计算时间和建议轮询间隔；Today domain mapper 暂不消费这些字段，状态机接入留给 Task 8。
- `Lucent_Contract_Snapshot`、数据层与 API 客户端再生指南已同步当前生成器和 `scripts/` 工具路径；已完成的
  Task 4 段落从 proactive suggestion runtime 计划中删除。

验证：`openapi-generator-cli version`（7.22.0）、`dart run scripts/bootstrap_generated_sources.dart`
（含 generated package `dart pub get`、两侧 `build_runner`、`flutter gen-l10n`）、`dart run scripts/verify_lucent_openapi_sync.dart`
和 `dart format` 均通过；`flutter analyze --fatal-infos` 无 issue；`flutter test test/today` 232 tests
通过；`dart run scripts/check_doc_coverage.dart --warning-only` 无必需文档缺失，`dart run scripts/check_doc_links.dart`
和 `git diff --check` 通过。

## Proactive Suggestion Runtime — Task 5 baseline observation sync

- Lucent Task 5 已将 covered collector signal 的 baseline observation 接入 recompute worker；Luminous 无代码合同变更，Today 状态机仍留给 Task 8。
- baseline observation 以 `userId + dimension + localDate` 幂等；明确 coverage 的数值（包括 `0`）可写入，缺失或未知值不会被补成零。
- proactive suggestion runtime 计划已删除已完成的 Task 5 段落，后续从 Task 6 继续。

## Proactive Suggestion Runtime — Task 6 reminder slot alignment

- Lucent Task 6 已修正漏服时间计算：使用用户 profile timezone 解释 reminder slot，按 `reminderId` 或无歧义的历史 medicine+scheduledTime 匹配 dose log；同药多槽位不再合并。
- 服务端现在区分 `planned`、`taken`、`skipped`、`unconfirmed` 和 `overdueUnconfirmed`，`overdueUnconfirmed` 才进入 missed-dose rule；copy 仍要求用户确认是否已服用。Luminous 本阶段无生产代码或 OpenAPI 生成物变更。
- Today 状态机接入这些 slot 状态、服务端物化状态和失败回退语义仍属于 Task 8；本阶段仅同步当前状态和迁移记录。

## Proactive Suggestion Runtime — Task 7 Today Analysis contract sync

- Lucent Task 7 已新增 `GET /today-analysis` 只读物化合同和显式 refresh/generation cap；普通记录不会由客户端触发首次 LLM 生成。Luminous generated client 已同步 Today Analysis 合同，`today_analysis_api.dart` 现包含 GET 与 refresh endpoint，以及 generate/async/stream 的更新响应模型；Today domain/UI 状态映射仍留给 Task 8。
- `scripts/bootstrap_generated_sources.dart` 已支持使用过滤后的 OpenAPI Generator 输出，只复制 Task 7 API/模型及必要的 index/deserialize 文件，避免全量生成噪音；`scripts/verify_lucent_openapi_sync.dart` 会校验 GET/refresh 方法存在。已运行 bootstrap client 分支、generated package `build_runner`、`dart format` 和 verify，均通过。

## Today AI generate response envelope 修复

- Luminous `TodayAiRemoteDataSource.generate` 现在从 `TodayAnalysisGenerateResponseDto` envelope 中解包嵌套 `analysis`，并对 direct analysis data 做显式 `TodayAnalysisDataDto` 映射；空响应抛出明确错误，不静默返回伪造结果。新增 envelope 定向测试并更新旧 generate fixtures。
- 验证：`flutter test test/today/ai_remote_data_source_test.dart`（14 tests）、`flutter analyze --fatal-warnings`、`dart format` 均通过。
